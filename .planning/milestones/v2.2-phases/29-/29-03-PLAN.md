---
phase: 29-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/settings-page.js]
autonomous: true
requirements: [SC-4]
gap_closure: true
gap_ids: [G-29-4]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "白名单只接受合法域名格式：无点的裸词（test、com）被拒绝并 toast 提示"
    - "结构垃圾（a..b、.com、example.com.、-a.com、a-.com）被拒绝"
    - "合法域名（example.com、sub.example.co.uk）正常添加"
    - "addDomain 与 addWhitelistDomain 使用同一个结构校验函数"
  artifacts:
    - path: "src/settings-page.js"
      provides: "模块级 isValidDomain 纯函数 + 两处调用点接入"
  key_links:
    - "addWhitelistDomain 在字符黑名单校验后调用 isValidDomain，拒绝时走 showToast + highlightInputError(elements.whitelistDomainInput)"
    - "addDomain 在字符黑名单校验后调用 isValidDomain，拒绝时走 showToast + highlightInputError(elements.domainInput)"
---

<objective>
关闭 UAT gap G-29-4：为设置页域名输入补齐结构校验。当前 `addWhitelistDomain`（src/settings-page.js:1518）只有字符黑名单 `/[^\w.\-]/`，`test`、`com`、`a..b` 等结构垃圾全部放行；消费端 `isDomainWhitelisted`（main.js:347）不过滤脏条目——`com` 会匹配所有 .com 站点使白名单形同虚设，`test` 永不匹配静默失效。

Purpose: 在录入入口拦截非法域名，保证白名单数据的语义有效性（后缀匹配消费端依赖合法结构）
Output: `isValidDomain` 共享校验函数 + addWhitelistDomain / addDomain 两处接入
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-/29-UAT.md

# 现有代码（修复点与复用模式）
@src/settings-page.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 isValidDomain 共享校验函数并接入 addWhitelistDomain</name>
  <files>src/settings-page.js</files>
  <action>
    在 src/settings-page.js 模块级（addWhitelistDomain 之前的工具函数区域）新增带 JSDoc 的纯函数 isValidDomain(domain)，实现结构校验（closing UAT gap G-29-4 的 missing 项一）：
    - 至少含一个点（裸词 test、com 拒绝）
    - 以点分割后每段非空（a..b、.com、example.com.、.. 拒绝）
    - 每段不以连字符开头或结尾（-a.com、a-.com 拒绝）
    - 返回 boolean；函数内不弹 toast、不操作 DOM，保持纯函数可独立验证

    然后在 addWhitelistDomain（现 1518 行）现有字符黑名单校验之后追加结构校验分支：isValidDomain(trimmed) 为 false 时，复用现有交互模式——showToast 提示格式不合法并给出示例（如 '域名格式不合法，示例: example.com'），调 highlightInputError(elements.whitelistDomainInput)，return。保留字符黑名单校验（它仍负责拦空格、斜杠等非法字符），两者是叠加关系不是替换。

    遵循 AGENTS.md 规范：camelCase 函数名、2 空格缩进、单引号、JSDoc 注释。
  </action>
  <verify>
    <automated>node --check src/settings-page.js && node -e "const fs=require('fs');const src=fs.readFileSync('src/settings-page.js','utf8');const s=src.indexOf('function isValidDomain');if(s<0){console.error('isValidDomain not found');process.exit(1);}let d=0,e=-1;const b=src.indexOf('{',s);for(let j=b;j<src.length;j++){if(src[j]==='{')d++;else if(src[j]==='}'){d--;if(!d){e=j;break;}}}eval(src.slice(s,e+1));const cases={'example.com':true,'sub.example.co.uk':true,'test':false,'com':false,'a..b':false,'.com':false,'example.com.':false,'-a.com':false,'a-.com':false};for(const[dom,exp]of Object.entries(cases)){const got=isValidDomain(dom);if(got!==exp){console.error('FAIL',dom,'expected',exp,'got',got);process.exit(1);}}console.log('isValidDomain: all 9 cases pass');" && awk '/async function addWhitelistDomain/,/^}/' src/settings-page.js | grep -c 'isValidDomain'</automated>
  </verify>
  <done>
    isValidDomain 对 9 个用例（3 合法 / 6 非法）全部判定正确；addWhitelistDomain 函数体内存在 isValidDomain 调用且拒绝路径使用 showToast + highlightInputError；node --check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: addDomain（开发者模式抓取域名）接入共享校验</name>
  <files>src/settings-page.js</files>
  <action>
    addDomain（现 1247 行，开发者模式抓取域名）有同构弱校验（/\s/ + /[^\w.-]/），按 UAT gap G-29-4 的 artifacts 第二项顺带共用修复：在其现有字符校验之后追加与 Task 1 相同的结构校验分支，isValidDomain(trimmed) 为 false 时 showToast 提示格式不合法并给出示例，调 highlightInputError(elements.domainInput)，return。同样保留原字符黑名单校验作叠加。

    注意 addDomain 的提示元素是 elements.domainInput（与白名单的 whitelistDomainInput 不同），不要抄错。
  </action>
  <verify>
    <automated>node --check src/settings-page.js && awk '/async function addDomain/,/^}/' src/settings-page.js | grep -c 'isValidDomain' && test "$(grep -c 'isValidDomain' src/settings-page.js)" -ge 3</automated>
  </verify>
  <done>
    addDomain 函数体内存在 isValidDomain 调用且拒绝路径使用 showToast + highlightInputError(elements.domainInput)；全文件 isValidDomain 出现次数 ≥3（定义 + 两个调用点）；node --check 通过。UAT 测试 4 可复测通过：输入 test 被拒绝并提示。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → electron-store | 设置页输入的域名经 HTTP /api/settings/update 落盘，成为主进程安全判断依据 |
| 配置数据 → isDomainWhitelisted | 主进程 webRequest 消费白名单做后缀匹配，脏条目直接削弱该控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-03-01 | Tampering | addWhitelistDomain 录入入口 | high | mitigate | 入口结构校验（isValidDomain）：`com` 类脏条目会匹配所有 .com 站点使白名单失效，本计划在录入侧拦截 |
| T-29-03-02 | DoS | 域名输入框 | low | accept | 超长输入仅影响本地 toast 交互，trim + 同步校验无资源风险 |
</threat_model>

<verification>
- `node --check src/settings-page.js` 通过
- isValidDomain 9 个行为用例（3 合法 / 6 非法）全部通过（见 Task 1 automated）
- 两个 add 函数均接入 isValidDomain（grep 计数 ≥3）
- 复测 UAT 测试 4：输入 `test` 被拒绝并提示；`example.com` 正常添加为 pill 标签
</verification>

<success_criteria>
UAT gap G-29-4 的 truth 成立：白名单只接受合法域名格式，无点裸词被拒绝；结构垃圾（空段、前导/尾随点、连字符开头/结尾）同样被拒绝；拒绝交互复用现有 toast + 输入框高亮。
</success_criteria>

<output>
Create `.planning/phases/29-/29-03-SUMMARY.md` when done
</output>
