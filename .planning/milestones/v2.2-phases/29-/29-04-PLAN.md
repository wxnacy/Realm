---
phase: 29-
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [main.js, src/preload.js, src/renderer.js]
autonomous: true
requirements: [SC-5]
gap_closure: true
gap_ids: [G-29-6]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "设置页切换多媒体开关后，主界面地址栏旁的播放按钮立即显示/隐藏，无需重启"
    - "主进程 /api/settings/update 写入成功后向主窗口广播 settings:updated 事件"
    - "renderer 收到事件后重读设置并调用 updateMediaPlayerVisibility 应用显隐"
  artifacts:
    - path: "main.js"
      provides: "settings update 路由写入后的 settings:updated 广播（携带 changedKeys）"
    - path: "src/preload.js"
      provides: "onSettingsUpdated 订阅接口"
    - path: "src/renderer.js"
      provides: "settings:updated 订阅处理（替换失效的文档可见性监听）"
  key_links:
    - "main.js update 路由 → mainWindow.webContents.send('settings:updated', changedKeys)"
    - "preload onSettingsUpdated → ipcRenderer.on('settings:updated')"
    - "renderer 订阅回调 → realmAPI.getSettings() → updateMediaPlayerVisibility(enabled)"
---

<objective>
关闭 UAT gap G-29-6：修复播放按钮不能实时显隐。根因（已诊断）：renderer 的运行期同步通道是死代码——src/renderer.js:1936-1945 的文档可见性监听挂在主文档上，但 realm://settings 是同一 renderer 文档内的 webview tab，切 tab 只改 webview CSS 可见性，document.hidden 永不变；且设置写入走 webview 内 HTTP POST /api/settings/update → configStore.set（main.js:995-1002），主进程没有任何变更广播，按钮显隐只有重启走 init() 才刷新。

Purpose: 建立 main → renderer 的设置变更事件通道，让开关切换即时生效
Output: 三处改动（main.js 广播 + preload 暴露 + renderer 订阅），遵循 AGENTS.md「新增 IPC 三处都要改」约定
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-/29-UAT.md

# 现有代码（修复点与复用模式）
@main.js
@src/preload.js
@src/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.js 设置写入成功后广播 settings:updated</name>
  <files>main.js</files>
  <action>
    在 main.js 设置 HTTP 服务器的 'update' 路由（现 995-1002 行：`route === 'update' && req.method === 'POST'` 分支）中，configStore.set 循环之后、sendJson(res, 200, ...) 之前，向主窗口广播变更事件：
    - 收集 changedKeys = Object.keys(updates)（如 ['mediaPlayer.enabled']、['mediaPlayer.whitelist']）
    - 复用本文件 598-600 行的既有广播模式：const mainWindow = windowManager.getMainWindow(); 判空 + isDestroyed() 检查后 mainWindow.webContents.send('settings:updated', changedKeys)
    - 加注释说明用途：设置页在 webview 内通过 HTTP 写入，主进程是唯一能观测到写入的地方，需主动通知宿主 renderer 刷新（closing UAT gap G-29-6 missing 项一）

    频道命名 'settings:updated' 遵循仓内事件频道冒号约定（media:list-updated、bookmarks-bar:visibility-changed、shortcut:triggered）。不要对 webview guest 广播——目标是宿主主窗口的 renderer.js。遵循 AGENTS.md 规范：2 空格缩进、单引号。
  </action>
  <verify>
    <automated>node --check main.js && grep -c "settings:updated" main.js && grep -c "windowManager.getMainWindow" main.js</automated>
  </verify>
  <done>
    update 路由在 configStore.set 之后发送 settings:updated 且携带变更键数组；窗口判空/销毁检查齐全；node --check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: preload 暴露 onSettingsUpdated + renderer 订阅并替换死代码监听</name>
  <files>src/preload.js, src/renderer.js</files>
  <action>
    preload.js（closing UAT gap G-29-6 missing 项二）：在「应用设置」区 getSettings（现 769 行）附近新增带 JSDoc 的订阅接口 onSettingsUpdated(callback)，体内复用本文件既有 ipcRenderer.on 模式（参考 90/99/177 行）：ipcRenderer.on('settings:updated', (event, changedKeys) => callback(changedKeys))。

    renderer.js：
    - 删除现 1936-1945 行挂在 document 上的可见性同步监听块（死代码：realm://settings 是同文档 webview tab，document.hidden 永不因切 tab 改变，该监听对设置同步无效——UAT 已诊断）
    - 原位（init 流程内）替换为 window.realmAPI.onSettingsUpdated 订阅：回调收到 changedKeys 后，仅当存在以 'mediaPlayer' 开头的键时，重读 window.realmAPI.getSettings()，取 settings.mediaPlayer && settings.mediaPlayer.enabled，调用既有 updateMediaPlayerVisibility(enabled)（该函数本身无 bug，5169-5189 行，不要改动）；try/catch 包裹并用 console.warn('[Realm Renderer] ...') 记录失败，风格与被删块一致
    - init() 中既有的初始 updateMediaPlayerVisibility 调用保留（启动时刷新仍需要）
    - 遵循 AGENTS.md「state.currentContainer 同步约定」类似的即时性原则：事件驱动，无轮询
  </action>
  <verify>
    <automated>node --check src/preload.js && node --check src/renderer.js && grep -c 'onSettingsUpdated' src/preload.js && grep -c 'onSettingsUpdated' src/renderer.js && test "$(grep -c 'visibilitychange' src/renderer.js)" -eq 0</automated>
  </verify>
  <done>
    preload 暴露 onSettingsUpdated；renderer 订阅后按 changedKeys 过滤重读设置并调 updateMediaPlayerVisibility；失效的文档可见性监听已移除（全文件 visibilitychange 零匹配）；node --check 两文件均通过。复测 UAT 测试 6：设置页切开关，主界面播放按钮立即显隐，无需重启。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview guest → 主进程 | 设置页（webview 内）经 HTTP 写入 electron-store |
| 主进程 → 宿主 renderer | settings:updated 事件经 webContents.send 下发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04-01 | Spoofing | settings:updated 频道 | low | accept | 事件只能由主进程 webContents.send 发出；webview guest 受 contextIsolation 隔离无法触达 ipcRenderer 该频道 |
| T-29-04-02 | Information Disclosure | changedKeys 载荷 | low | accept | 仅向可信宿主 renderer 暴露设置键名（不含值），宿主文档本就能经 getSettings 读全量设置 |
</threat_model>

<verification>
- `node --check main.js && node --check src/preload.js && node --check src/renderer.js` 全部通过
- main.js 含 settings:updated 广播（grep ≥1）；preload/renderer 均含 onSettingsUpdated
- renderer.js 全文件 visibilitychange 零匹配（死代码已移除）
- 复测 UAT 测试 6：设置页切换开关 → 主界面地址栏旁播放按钮即时显隐，无需重启；重启后状态仍正确（init 初始调用保留）
</verification>

<success_criteria>
UAT gap G-29-6 的 truth 成立：切换功能开关后，主界面地址栏旁的媒体播放按钮立即显示/隐藏，无需重启。
</success_criteria>

<output>
Create `.planning/phases/29-/29-04-SUMMARY.md` when done
</output>
