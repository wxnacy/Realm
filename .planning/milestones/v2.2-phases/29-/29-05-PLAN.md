---
phase: 29-
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [media-sniffer.js, main.js, ipc-handlers.js, src/renderer.js]
autonomous: true
requirements: [SC-3]
gap_closure: true
gap_ids: [G-29-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "白名单非空时，非白名单站点的 webview dom-ready 不再注入嗅探脚本（per D-06/D-07）"
    - "已注入的旧脚本（SPA 站内导航、开关变更前注入）在非白名单站点上报时被主进程直接丢弃"
    - "网络路径按页面 URL（而非媒体 CDN URL）匹配白名单：白名单 bilibili.com 不误挡 bilivideo.com CDN 资源"
    - "白名单为空时两条路径全部放行（回归不破）"
  artifacts:
    - path: "media-sniffer.js"
      provides: "isDomainWhitelisted 纯函数导出（主进程唯一白名单匹配实现）"
    - path: "main.js"
      provides: "onResponseStarted 白名单匹配改为页面 URL（webContentsId → getURL()，回退 details.url）"
    - path: "ipc-handlers.js"
      provides: "media:report-detected 服务端白名单校验（防御纵深）"
    - path: "src/renderer.js"
      provides: "dom-ready 注入前的白名单检查（isPageWhitelisted + webview.getURL()）"
  key_links:
    - "renderer dom-ready → settings.mediaPlayer.whitelist → isPageWhitelisted(webview.getURL()) → 非白名单跳过注入"
    - "ipc-handlers media:report-detected → webContents.fromId(webContentsId).getURL() → mediaSniffer.isDomainWhitelisted → 非白名单丢弃"
    - "main.js onResponseStarted → webContents.fromId(details.webContentsId)?.getURL() → mediaSniffer.isDomainWhitelisted"
---

<objective>
关闭 UAT gap G-29-12：白名单过滤被脚本注入路径绕过。根因（已完整诊断于 .planning/debug/whitelist-bypass-script-injection.md）：媒体嗅探有两条路径，白名单只挂在网络路径（main.js:453 onResponseStarted）上；脚本注入路径完全失守——(1) src/renderer.js:847-855 dom-ready 注入前只查 settings.mediaPlayer.enabled 不查 whitelist；(2) ipc-handlers.js:1488 media:report-detected 收到上报直接入库无任何校验。另：网络路径按 details.url（媒体资源 URL，常是 CDN 域名）匹配白名单，与用户「按站点域名过滤」的心智不符——白名单 bilibili.com 会把 bilivideo.com CDN 的视频在网络路径误挡。

Purpose: 让白名单在两条嗅探路径上都生效，且统一按页面域名匹配（per D-06 空白名单=全放行、D-07 子域名自动包含）
Output: 三处修复（注入侧检查 + 服务端校验 + 网络路径语义修正），共 4 文件小范围改动
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-/29-UAT.md
@.planning/debug/whitelist-bypass-script-injection.md

# 现有代码（修复点与复用模式）
@media-sniffer.js
@main.js
@ipc-handlers.js
@src/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: isDomainWhitelisted 上移 media-sniffer.js 导出 + main.js 网络路径改按页面 URL 匹配</name>
  <files>media-sniffer.js, main.js</files>
  <action>
    media-sniffer.js（closing G-29-12 missing 项三的公共前置）：
    - 在模块顶层（VIDEO_TYPE_MAP 等常量区之后）新增纯函数 isDomainWhitelisted(url, whitelist)，语义逐字搬自 main.js 现 347-357 行：非数组或空数组返回 true（空白名单=全放行，per D-06）；hostname === domain || hostname.endsWith('.' + domain)（子域名包含，per D-07）；URL 解析失败返回 false。带完整 JSDoc（@param/@returns）
    - 在文件末尾既有 `module.exports = new MediaSniffer();` 之后追加一行：`module.exports.isDomainWhitelisted = isDomainWhitelisted;`（单例导出保持不变，纯函数挂为属性——main.js 与 ipc-handlers.js 都已 require 本模块，零新依赖）
    - 模块顶层无 electron require（electron 均为方法内懒加载），纯 node 可直接 require 本文件做行为验证

    main.js（closing G-29-12 missing 项三）：
    - 删除现 335-357 行的本地 isDomainWhitelisted 定义及其上方「媒体嗅探白名单过滤」注释头（实现已上移，避免双份漂移）
    - onResponseStarted 回调（现 444-457 行）白名单匹配从 details.url 改为页面 URL：参照本文件 2169 行既有懒加载模式取 webContents，回调内 `const { webContents } = require('electron');`（或加入顶部解构，二选一保持文件内一致）；`const pageUrl = webContents.fromId(details.webContentsId)?.getURL();`，匹配目标为 `pageUrl || details.url`（取不到页面 URL 时回退媒体 URL，行为不劣于现状）；调用改为 `mediaSniffer.isDomainWhitelisted(pageUrl || details.url, whitelist)`
    - 加注释说明语义：白名单按站点页面域名过滤（用户心智 per G-29-12 诊断），媒体资源常在 CDN 域（如 bilivideo.com），按 details.url 匹配会误挡白名单站点自身内容
    - 遵循 AGENTS.md：2 空格缩进、单引号、JSDoc
  </action>
  <verify>
    <automated>node --check media-sniffer.js && node --check main.js && node -e "const m=require('./media-sniffer.js');const f=m.isDomainWhitelisted;if(typeof f!=='function')process.exit(1);const c=[[['https://nnyy.in/a',['bilibili.com']],false],[['https://www.bilibili.com/v',['bilibili.com']],true],[['https://api.bilibili.com/x',['bilibili.com']],true],[['https://notbilibili.com/',['bilibili.com']],false],[['https://evilbilibili.com/',['bilibili.com']],false],[['https://nnyy.in/a',[]],true],[['https://nnyy.in/a',null],true],[['not-a-url',['bilibili.com']],false]];for(const[a,e]of c){if(f(...a)!==e){console.error('FAIL',a);process.exit(1)}}console.log('isDomainWhitelisted 8 cases pass')" && grep -c 'mediaSniffer.isDomainWhitelisted' main.js && ! grep -q 'function isDomainWhitelisted' main.js</automated>
  </verify>
  <done>
    isDomainWhitelisted 在 media-sniffer.js 单份实现并导出，8 个行为用例全过；main.js 本地定义已删（全文件 function isDomainWhitelisted 零匹配）；onResponseStarted 按页面 URL 匹配且带回退；node --check 两文件通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: ipc-handlers.js media:report-detected 服务端白名单校验</name>
  <files>ipc-handlers.js</files>
  <action>
    防御纵深（closing G-29-12 missing 项二）：即使修了注入侧，已注入的旧脚本（SPA 站内导航残留、开关/白名单变更前注入的页面）仍可上报，主进程必须兜底。

    - 文件顶部 electron 解构（现第 8 行）加入 webContents
    - media:report-detected 处理器（现 1488-1495 行）在 assertTrustedSender 与 webContentsId 类型校验之后、mediaSniffer.handleScriptDetected 之前插入白名单校验：
      1. `const whitelist = configStore.get('settings.mediaPlayer.whitelist', []);`（configStore 现 26 行已就位，无需新建）
      2. `const wc = webContents.fromId(webContentsId);` — 判空 + isDestroyed 双检查（对齐 main.js:2169 模式）：`if (!wc || wc.isDestroyed()) return { success: false };`，不调 handleScriptDetected
      3. `mediaSniffer.isDomainWhitelisted(wc.getURL(), whitelist)` 为 false 时静默丢弃：console.warn('[Realm IPC] media:report-detected 非白名单站点上报已丢弃: &lt;hostname&gt;') 并 return { success: false }，不入库
    - 白名单为空数组时 isDomainWhitelisted 恒 true，零行为变化（回归安全）
    - 不要重复校验 enabled——renderer 注入侧已查（per D-10），本任务只做白名单这一诊断项，保持手术式范围
    - JSDoc 更新：在处理器注释中补充「非白名单站点上报直接丢弃（per G-29-12 防御纵深）」
  </action>
  <verify>
    <automated>node --check ipc-handlers.js && grep -c 'mediaSniffer.isDomainWhitelisted' ipc-handlers.js && grep -c 'webContents.fromId' ipc-handlers.js</automated>
  </verify>
  <done>
    media:report-detected 在入库前完成 fromId 判空 + 页面 URL 白名单匹配，非白名单上报被丢弃且有日志；node --check 通过；grep 各 ≥1。
  </done>
</task>

<task type="auto">
  <name>Task 3: renderer.js dom-ready 注入前白名单检查</name>
  <files>src/renderer.js</files>
  <action>
    主要修复点（closing G-29-12 missing 项一）：webview dom-ready 注入嗅探脚本前增加白名单检查。

    - 在 renderer.js 模块顶层（媒体嗅探相关函数附近）新增纯函数 isPageWhitelisted(url, whitelist)，带 JSDoc，语义与主进程 isDomainWhitelisted 逐字一致（空白名单=true、精确或子域后缀匹配、解析失败=false）。renderer 受 contextIsolation 限制无法 require 主进程模块，此处按仓内 isValidDomain 先例（settings-page.js:1516）保留一份渲染侧实现；JSDoc 注明「与 media-sniffer.js isDomainWhitelisted 语义保持一致，改动需同步」
    - dom-ready 处理器（现 847-855 行）在既有 enabled 检查之后、注入之前插入：白名单取自同一次 `await window.realmAPI.getSettings()` 已拿到的 settings 对象（settings.mediaPlayer.whitelist，不再发第二次 IPC）；页面 URL 用 `webview.getURL()`（本文件 1076/1135 行既有模式），为空时回退 `webview.src`；isPageWhitelisted 为 false 时 return 跳过注入
    - realm:// 内部页面（hostname 为 favorites/settings 等）在非空白名单下自然不匹配、跳过注入——符合预期；空白名单下行为与现状完全一致
    - 错误处理沿用现有风格：检查失败走既有 try/catch 的 console.warn('[Realm Renderer] ...') 路径
    - 遵循 AGENTS.md：2 空格缩进、单引号
  </action>
  <verify>
    <automated>node --check src/renderer.js && test "$(grep -c 'isPageWhitelisted' src/renderer.js)" -ge 2 && node -e "const s=require('fs').readFileSync('src/renderer.js','utf8');const i=s.indexOf('dom-ready');const seg=s.slice(i,i+4000);if(!seg.includes('whitelist')){console.error('dom-ready 段未见 whitelist 检查');process.exit(1)}console.log('dom-ready whitelist check present')"</automated>
  </verify>
  <done>
    dom-ready 注入前完成白名单检查：复用已获取的 settings、webview.getURL() 取页面 URL、非白名单跳过注入；isPageWhitelisted 定义+调用 ≥2 处匹配；node --check 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview guest 页面 → 宿主 renderer | 注入脚本经 sendToHost 上报检测结果；guest 页面可控（任意站点） |
| 宿主 renderer → 主进程 | media:report-detected IPC 携带 webContentsId + videos |
| 主进程 → electron-store | 白名单配置读取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05-01 | Spoofing | media:report-detected | high | mitigate | Task 2：主进程不信任 renderer 上报的来源声明，webContents.fromId(webContentsId).getURL() 反查真实页面 URL 做白名单裁决，非白名单直接丢弃 |
| T-29-05-02 | Tampering | 已注入残留脚本（SPA 导航/变更前注入） | medium | mitigate | Task 2 服务端校验覆盖 Task 3 无法触及的存量注入；Task 3 阻断新注入 |
| T-29-05-03 | DoS | onResponseStarted 回调 | low | accept | webContents.fromId 为 O(1) 查表；回调仅增加一次 id 查找 + getURL，无热路径性能影响 |
</threat_model>

<verification>
- `node --check media-sniffer.js && node --check main.js && node --check ipc-handlers.js && node --check src/renderer.js` 全部通过
- isDomainWhitelisted 8 个行为用例全过（空/ null 白名单放行、子域匹配、非后缀相似域拒绝、无效 URL 拒绝）
- main.js 网络路径使用 mediaSniffer.isDomainWhitelisted 且本地定义已删；ipc-handlers.js 与 renderer.js 各有白名单检查
- 复测 UAT 测试 12（端到端，人工）：白名单 = [bilibili.com] 时开 nnyy.in → 媒体面板无视频；开 bilibili.com 视频页 → 正常嗅探（含 CDN 资源）；白名单清空 → 所有站点正常嗅探（回归）
</verification>

<success_criteria>
UAT gap G-29-12 的 truth 成立：白名单非空时，仅白名单域名（含子域）的站点会被嗅探，两条嗅探路径（网络拦截 + 脚本注入）均生效；非白名单站点不产出视频列表；白名单站点的 CDN 资源不被误挡。
</success_criteria>

<output>
Create `.planning/phases/29-/29-05-SUMMARY.md` when done
</output>
