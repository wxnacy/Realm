---
phase: 44-player-video-cache-and-local-media-library
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - media-m3u8-parser.js
  - tests/test-m3u8-playlist-parser.js
  - media-cache-manager.js
  - tests/test-media-cache.js
  - media-record-engine.js
  - main.js
  - ipc-handlers.js
  - src/player.js
autonomous: true
gap_closure: true
gap_ids: [G-44-7]
requirements: [D-17, D-22]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - AES-128 加密 HLS（#EXT-X-KEY:METHOD=AES-128）的缓存条目在抽屉中不再显示「转换为 MP4」按钮（D-17 隐藏语义）
    - 加密源经任务页续转等入口发起转换时，在保存弹框与 mux 流程之前被拒，文案「加密视频暂不支持转换」可见（convert-resume 反馈条映射）
    - enc.key 等 EXT-X-KEY 密钥 URI 不再被当分片落库——新缓存条目 meta.segments 数与清单分片数一致，playlist_order 不再失配
    - 历史被污染条目读取侧自愈带前提：仅对 meta.key_uris 已存在（修复后清单被重新拉取登记过）的条目，getConvertInfo 过滤把 key 键剔除出 segmentPaths；修复前落库且未重新播放（无 key_uris）的历史污染条目过滤为 no-op——该情形由嗅探干净拒绝 + 44-17 无 0 字节残留兜底（G-44-7 仍闭合，读取侧不做静默改动）
  artifacts:
    - media-m3u8-parser.js parsePlaylist 返回 hasEncryption + keyUris（含测试）
    - media-cache-manager.js meta.has_encryption / meta.key_uris 登记与 storeBuffer key 排除 / getConvertInfo 过滤（含测试）
    - media-record-engine.js meta.json hasEncryption 字段；main.js startConvertTask 入口 encrypted 早拒 + CONVERT_FAIL_TEXT.encrypted 文案
    - ipc-handlers.js player:drawer:list hasEncryption 透出；src/player.js 加密条目转换按钮隐藏
  key_links:
    - 时序依赖：hls.js 先请求清单（updatePlaylistIndex 此刻登记 meta.key_uris）后请求密钥/分片——storeBuffer 跳过判定所依赖的 key_uris 在密钥请求到达前必然已落盘
    - keyUris → resolveUri(绝对化) → segmentKeyOf → 与 segments map 键同源比对（O(1) 成员判定，无路径拼接）
---

<objective>
闭合 G-44-7 体验层与缓存分类两项 missing：① 转换入口/清单解析层检测 #EXT-X-KEY:METHOD=AES-128，直接报「加密视频暂不支持转换」，不进 mux 流程（当前要等嗅探阶段才失败）；② 缓存请求分类排除 EXT-X-KEY 的 key URI，杜绝 enc.key（16 字节）被当分片落库污染 meta.segments（53 > total 52，playlist_order 完整性判定失配回退 stored_at 序）。

Purpose: 泄漏层修复（44-17，并行 Wave 1，零文件交集）保证失败不留 0 字节半成品；本计划让加密源在入口就被显式拒绝、不再产生注定失败的任务，并修正缓存数据质量。诊断确证：hn.bfvvs.com/play/b2k7JoJd 源为 AES-128 加密，缓存落盘的是解密前密文，mux.js 无解密链路，转换对该源永远不可能成功。
Output: 清单解析加密检测（含 keyUris）→ 缓存层 key 排除 → 录制/缓存双入口早拒 + UI 隐藏，测试全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/convert-mp4-zero-bytes.md
@media-m3u8-parser.js
@media-cache-manager.js
@media-record-engine.js
@main.js
@ipc-handlers.js
@src/player.js

关键现状锚点：
- media-m3u8-parser.js parsePlaylist：行级解析，# 行只认 ENDLIST/MEDIA-SEQUENCE/TARGETDURATION/EXTINF，EXT-X-KEY 完全忽略
- main.js rewriteM3u8ForProxy（258 行）对 URI="..." 属性（含 EXT-X-KEY）也重写进 /proxy 且继承 cache=1 标记——密钥请求因此进缓存分支
- main.js handleProxyRequest 缓存 tee 分支（419 行）：cache=1 且非 m3u8 非 Range 即 store 落盘，无密钥排除
- media-cache-manager.js updatePlaylistIndex（593 行）/ getConvertInfo（620 行）/ storeBuffer（391 行）；store()（338 行）的 passthrough 与 storeBuffer 独立（349 行 readable.pipe(passthrough)），storeBuffer 早退不影响透传
- media-record-engine.js hasDiscontinuity 检测模式（207 行 regex 置 st.hasDiscontinuity，184 行写 meta）——hasEncryption 镜像该模式
- main.js CONVERT_FAIL_TEXT（3022 行）、startConvertTask（3077 行）、startConvertFromInput（3204 行）、convert-resume 错误映射（2304 行）
- 锁定决策（44-13/G-44-5）：播放器窗口不建 toast 基建——抽屉侧对不可转条目采用 D-17 隐藏语义而非点击后报错
</context>

<tasks>

<task type="auto">
  <name>Task 1: parsePlaylist 加密检测与密钥 URI 收集</name>
  <files>media-m3u8-parser.js, tests/test-m3u8-playlist-parser.js</files>
  <action>
media-m3u8-parser.js parsePlaylist 新增两个字段（行级正则手写，与文件既有风格一致，不引依赖）：

1. `hasEncryption: boolean`——#EXT-X-KEY 行 METHOD 属性 ≠ NONE 即 true（METHOD=AES-128 与 METHOD=SAMPLE-AES 都算；解析属性值时容忍引号与属性顺序，如 `#EXT-X-KEY:METHOD=AES-128,URI="enc.key",IV=0x...`）。多个 EXT-X-KEY 行任一命中即 true，且不因后续 METHOD=NONE 行回落（整清单出现过加密即视为加密流）。
2. `keyUris: string[]`——逐个 #EXT-X-KEY 行收集 URI="..." 属性的原始值（相对/绝对都保留原样，不在此解析为绝对 URL——绝对化是 updatePlaylistIndex 的职责，它才有 baseUrl）。METHOD=NONE 的行不收集。

JSDoc 返回契约同步补两字段。**不要**改动 EXTINF/MEDIA-SEQUENCE/TARGETDURATION/ENDLIST 既有解析行为与 resolveUri。

tests/test-m3u8-playlist-parser.js 新增 describe：AES-128 + 相对 URI → hasEncryption=true 且 keyUris=['enc.key']；METHOD=NONE → false 且 keyUris 空；无 KEY 标签清单 → false；SAMPLE-AES → true；多 KEY 行（NONE 在后）→ 仍 true；既有断言零改动。
  </action>
  <verify>
    <automated>node --check media-m3u8-parser.js && node tests/test-m3u8-playlist-parser.js</automated>
  </verify>
  <done>parsePlaylist 返回 hasEncryption 与 keyUris，既有解析行为零回归，新用例全绿</done>
</task>

<task type="auto">
  <name>Task 2: 缓存层密钥 URI 排除与加密标记登记</name>
  <files>media-cache-manager.js, tests/test-media-cache.js</files>
  <action>
media-cache-manager.js 三处改动（附加字段向后兼容，不升 META_VERSION——沿 44-05「附加字段不升版本」先例）：

1. updatePlaylistIndex（593 行）：在既有 playlist_order/total_segments/playlist_ended/has_discontinuity 旁新增 `meta.has_encryption = !!pl.hasEncryption`；`meta.key_uris` = pl.keyUris 逐个经 `resolveUri(uri, m3u8Url)` 绝对化后 `segmentKeyOf(...)` 得到的键数组（与 order 循环同款 try/catch，非法 URI 跳过）。key_uris 存的是与 meta.segments 同源的 segKey，供 storeBuffer O(1) 成员判定。
2. storeBuffer（391 行）：读出 mMeta 后、写盘登记前，若 `Array.isArray(mMeta.key_uris) && mMeta.key_uris.includes(segKey)` 则直接 return（不写文件不登记 segments），console.debug 一条「密钥 URI 跳过落盘」。镜像既有 MAX_SEGMENT_BYTES 超限跳过模式（store 359 行）——store() 的 passthrough 透传独立于 storeBuffer（readable.pipe(passthrough) 在 349 行先行），早退不影响播放器取流。
3. getConvertInfo（620 行）：keys 计算与 segmentPaths 构建时，若 meta.key_uris 存在则过滤掉其中的键（读取侧自愈仅对 key_uris 已存在的条目生效——即修复后清单被重新拉取登记过的条目；无 key_uris 的历史污染条目过滤为 no-op，key 键仍留在 segmentPaths，该条目由转换入口 has_encryption 早拒 + 44-17 无 0 字节残留兜底）；返回对象新增 `hasEncryption: !!meta.has_encryption`。JSDoc 契约同步并写明该前提。

时序依据（写进 JSDoc）：hls.js 先请求清单（main.js 在响应前调 updatePlaylistIndex，key_uris 此刻已落盘）后请求密钥/分片，storeBuffer 判定所依赖的 key_uris 必然先行就绪。适用域：该时序前提在直连媒体清单形态下成立（G-44-7 诊断案例即此形态）；master→variant 两级清单下 updatePlaylistIndex 按 videoIdOf(variantUrl) 落库、分片按 master vid 归属（44-05 既有行为），时序前提不成立——但该形态本就 completeness=null、转换按钮隐藏，不参与 key 排除机制，不影响本计划闭合 G-44-7。

tests/test-media-cache.js 新增用例：含 EXT-X-KEY 的清单 updatePlaylistIndex 后 meta.has_encryption=true 且 key_uris 含密钥键；storeBuffer 存密钥 URI → meta.segments 无该键、segments 目录无文件；存真实分片 → 正常登记且 playlist_order 长度 === segments 键数；污染 meta 两种输入分别钉死行为——① 有 key_uris 的污染 meta（segments 混入 key 键）经 getConvertInfo → segmentPaths 不含 key 文件且 hasEncryption=true；② 无 key_uris 的污染 meta（模拟修复前落库且未重新播放的历史条目）经 getConvertInfo → 过滤为 no-op，segmentPaths 仍含 key 键（读取侧不静默改动，该条目依赖入口早拒与 44-17 兜底）。既有断言零改动。
  </action>
  <verify>
    <automated>node --check media-cache-manager.js && node tests/test-media-cache.js</automated>
  </verify>
  <done>密钥 URI 不再落库为分片（新条目 segments 数 == 清单分片数），有 key_uris 的历史污染条目读取侧自愈、无 key_uris 的历史污染条目行为钉死为 no-op，新用例全绿</done>
</task>

<task type="auto">
  <name>Task 3: 转换入口加密早拒与抽屉按钮隐藏</name>
  <files>media-record-engine.js, main.js, ipc-handlers.js, src/player.js</files>
  <action>
四个文件按数据流顺序接线（字段命名沿各自现有惯例：录制 meta 用 camelCase hasDiscontinuity 同款 hasEncryption；缓存 meta 用 snake_case has_encryption 同款）：

1. media-record-engine.js：镜像 hasDiscontinuity 模式（207 行）——轮询清单时检测加密（优先用 parse 结果的 hasEncryption，若该处只有原始 buffer 则用与 DISCONTINUITY 同款行级正则 `/^#EXT-X-KEY:.*METHOD=(?!NONE)/m` 判定），任一版清单出现即置 st.hasEncryption=true；_writeMetaIndex（184 行区域）meta.json 增加 hasEncryption 字段。
2. main.js startConvertTask（3077 行）：入参加 `hasEncryption`，函数体最前（hasDiscontinuity 检查旁）`if (hasEncryption) return { ok: false, reason: 'encrypted' }`——保存弹框与任务注册都不发生。startConvertFromRecordTask（3185 行）透传 `hasEncryption: !!bundle.meta.hasEncryption`；startConvertFromInput 缓存分支（3237 行区域）在 completeness 检查旁加 `if (info.hasEncryption) return { ok: false, reason: 'encrypted' }`。CONVERT_FAIL_TEXT（3022 行）加 `encrypted: '加密视频暂不支持转换'`（纵深防御：万一某入口漏判进了 mux，失败文案也是本句）。convert-resume 端点错误映射（2304 行三元链）加 encrypted → '加密视频暂不支持转换'。
3. ipc-handlers.js player:drawer:list（2279 行）：缓存条目合并处新增 `item.hasEncryption = !!e.meta.has_encryption`；JSDoc 条目契约注释同步。
4. src/player.js renderDrawer 转换按钮（1008 行）：可见性条件追加 `&& !item.hasEncryption`（D-17 隐藏语义——加密条目不可转，按钮整体不渲染；锁定决策：播放器不建 toast 基建，故不在点击后报错）。注释补 G-44-7 依据。

**不要**改动：rewriteM3u8ForProxy 的 URI 重写行为（密钥请求仍须走 /proxy 透传供 hls.js 解密播放——只排除落库，不拦截播放）、T-44-01/02 路径安全约束、D-17 服务端复校结构。
  </action>
  <verify>
    <automated>node --check media-record-engine.js && node --check main.js && node --check ipc-handlers.js && node --check src/player.js && node tests/test-media-record-duration.js && node tests/test-media-task-registry.js</automated>
  </verify>
  <done>加密源在保存弹框前被拒（reason=encrypted，文案「加密视频暂不支持转换」），录制 meta 携带 hasEncryption，抽屉加密条目无转换按钮，既有录制/任务注册表测试零回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端 m3u8 清单 → meta.key_uris / has_encryption | 远端清单内容完全不可信，经行级解析落库 |
| 远端请求 URL → storeBuffer 跳过判定 | 请求 target 与 key_uris 键比对决定是否落盘 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-18-01 | Tampering | meta.key_uris（远端清单可控） | low | accept | key_uris 仅用于 storeBuffer 的「跳过落库」与 getConvertInfo 的「过滤成员」——是白名单式排除而非路径拼接输入；分片文件名仍由 segmentKeyOf 哈希派生 + _safePath 校验（T-44-01/02 路径安全约束不受削弱）。恶意清单把分片 URI 填进 EXT-X-KEY 的后果只是该分片不被缓存（播放走透传），无逃逸面 |
| T-44-18-02 | DoS | 超长 keyUris 数组 | low | accept | 清单行数受既有分片解析同款约束；key_uris 与 playlist_order 同源增长，无独立放大；成员判定 O(n) 每次 storeBuffer 一次，n 为密钥数（正常 ≤2） |
</threat_model>

<verification>
- node tests/test-m3u8-playlist-parser.js、node tests/test-media-cache.js、node tests/test-media-record-duration.js、node tests/test-media-task-registry.js 全绿
- 四个 --check 全过
- grep 断言：media-m3u8-parser.js 含 hasEncryption；media-cache-manager.js 含 has_encryption 与 key_uris；main.js 含 reason: 'encrypted' 与「加密视频暂不支持转换」；src/player.js 转换按钮条件含 !item.hasEncryption
- 端到端真机（UAT 承载）：播放 bfvvs 类 AES-128 源缓存完成后，抽屉条目无「转换为 MP4」按钮、Downloads 无新增 0 字节文件
</verification>

<success_criteria>
- 加密源转换在入口被显式拒绝（弹框前），不产生注定失败的转换任务，不进 mux 流程
- enc.key 不再污染 meta.segments；有 key_uris 的历史污染条目转换读取侧自愈（无 key_uris 的历史条目过滤为 no-op，由嗅探干净拒绝 + 44-17 无 0 字节残留兜底）
- 正常 MPEG-TS 明文源的缓存与转换链路零回归（全量相关测试通过）
</success_criteria>

<output>
Create `.planning/phases/44-/44-18-SUMMARY.md` when done
</output>
