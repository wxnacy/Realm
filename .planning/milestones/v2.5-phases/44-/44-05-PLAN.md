---
phase: 44-player-video-cache-and-local-media-library
plan: 05
type: execute
wave: 4
depends_on: ["44-02", "44-03", "44-04"]
files_modified:
  - package.json
  - package-lock.json
  - media-remuxer.js
  - tests/test-media-remuxer.js
  - main.js
  - ipc-handlers.js
  - src/player.js
  - src/preload.js
autonomous: true
requirements: [D-04, D-17, D-22, D-24, D-26]
estimate:
  tokens: 75000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - mux.js 6.3.0 已安装且在主进程可用（纯 JS，无需 asarUnpack——D-04 定稿，RESEARCH legitimacy 审计 OK）
    - TS→fMP4 转封装为后台 convert 任务：单一 Transmuxer 实例顺序 push、先注册 data 监听再 push、initSegment 首写（RESEARCH Pattern 3 硬约束）
    - mp4 产物经 showSaveDialog 选目录（默认 last-used 或下载目录并记住），产物命名「{标题} {YYYY-MM-DD HHmm}.mp4」同名追加「 (2)」（D-22/D-24）
    - 转换完成发系统通知 + shell.showItemInFolder（D-24，经 44-03 终态通知链路）
    - 录制停止后自动派生 convert 接力（completed → 弹框选目录 → convert 任务；取消弹框则保留已录分片可稍后从任务页续转）（D-22）
    - 抽屉条目「转换为 MP4」按钮仅分片齐全（完整度 100%）或录制中断条目可见，其余隐藏（D-17/UI-SPEC Disabled）
    - 转封装失败任务页展示「MP4 转换失败：{原因}」（UI-SPEC Copywriting）
  artifacts:
    - media-remuxer.js（convertToMp4({ segmentPaths, outputPath, onProgress })、sanitizeFilename(title)、buildOutputName(title, date)）
    - tests/test-media-remuxer.js（命名/sanitize/序号/Transmuxer 构造 smoke）
    - ipc-handlers.js：player:convert/start 通道；main.js：POST /api/tasks/convert-resume 端点 + record→convert 接力监听
    - src/player.js/player.css：抽屉条目「转换为 MP4」按钮（D-17 gating）
    - src/preload.js：playerAPI.startConvert
    - configStore 键：settings.lastMediaSaveDir
  key_links:
    - record 任务 completed（44-04）→ taskManager.onTaskCompleted → showSaveDialog → convertToMp4 后台任务 → 终态通知（44-03 链路）
    - 任务页「已落盘部分续转」按钮 → POST /api/tasks/convert-resume（44-03 预留路由）→ 同上 convert 链路
  prohibitions:
    - statement: 不使用 ffmpeg-static（D-04 定稿，nodejieba 事故教训）
      status: locked
      verification: package.json 无 ffmpeg-static 依赖；media-remuxer.js 仅 require('mux.js')
    - statement: 每个 TS 分片不得新建 Transmuxer 实例（时间轴断裂，RESEARCH Pitfall 5）
      status: locked
      verification: media-remuxer.js 单实例顺序 push + flush，grep 无循环内 new Transmuxer
---

<objective>
交付 mp4 转封装闭环：mux.js 引入与 media-remuxer 封装（D-04）、convert 后台任务（showSaveDialog 选目录 + 命名 + 通知 + 定位，D-24）、录制停止自动接力（D-22）、抽屉「转换为 MP4」入口（D-17）与任务页「已落盘部分续转」落地。

Purpose: 本阶段产物侧收口——录制/缓存内容变成可带走的 mp4 文件；D-04 是 locked 决策（mux.js 纯 JS，规避原生二进制打包风险）。
Output: media-remuxer.js、convert 任务集成、抽屉转换按钮、convert-resume 端点。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-/44-CONTEXT.md
@.planning/phases/44-/44-RESEARCH.md
@.planning/phases/44-/44-PATTERNS.md
@.planning/phases/44-/44-UI-SPEC.md

@main.js
@ipc-handlers.js
@src/player.js
@src/preload.js
@media-task-manager.js
@media-record-engine.js
@media-cache-manager.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-remuxer.js`：`convertToMp4({ segmentPaths, outputPath, onProgress })`（Promise，单实例 Transmuxer 流式写盘）、`sanitizeFilename(title)`（白名单替换 `[\\/:*?"<>|]` 等为 `_`）、`buildOutputName(title, now)` →「{标题} {YYYY-MM-DD HHmm}.mp4」
- `tests/test-media-remuxer.js`
- package.json 依赖：mux.js ^6.3.0
- ipc-handlers.js：`player:convert/start`（assertPlayerSender；D-17 校验：完整度 100% 或任务 status=interrupted）
- main.js：`POST /api/tasks/convert-resume` 端点（44-03 预留路由的落地）；taskManager.onTaskCompleted 中 type=record → convert 接力分支
- src/preload.js：`playerAPI.startConvert({ entryId | taskId })`
- src/player.js：抽屉条目「转换为 MP4」按钮（完整度 100% 或 interrupted 可见，否则隐藏）
- configStore 键：`settings.lastMediaSaveDir`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: mux.js 安装 + media-remuxer.js 转封装封装 + 命名/单测</name>
  <files>package.json, package-lock.json, media-remuxer.js, tests/test-media-remuxer.js</files>
  <read_first>
    - .planning/phases/44-/44-RESEARCH.md（Pattern 3 mux.js 顺序转封装含 README 时序警告原文、Package Legitimacy Audit 表 verdict OK、Pitfall 5）
    - download-manager.js:95-112（getUniqueFilePath 同名序号先例——D-22 同名「 (2)」照此）
    - tests/test-ai-bash-policy.js（node:test 结构）
    - package.json（dependencies 现状）
  </read_first>
  <action>
  1. `npm install mux.js@^6.3.0`——RESEARCH Package Legitimacy Audit 已验证 verdict OK（videojs 官方、118 万周下载、postinstall null、纯 JS 无原生模块），**无需**人工 checkpoint；确认不进 renderer（主进程 require），不触发 asarUnpack（AGENTS.md 原生模块纪律不适用，纯 JS）。
  2. media-remuxer.js：`convertToMp4({ segmentPaths, outputPath, onProgress })`——单一 `new muxjs.mp4.Transmuxer()` 实例跨全部分片；**先注册 `data` 监听再进入 push 循环**（README 原文硬约束）；首个 data 事件写 initSegment、后续只写 data（moof/mdat 顺序拼接即合法 fMP4）；循环内 `push(new Uint8Array(fs.readFileSync(tsFile)))` + `flush()` 每分片触发、`fs.createWriteStream(outputPath)` 流式写、`stream.end()` 收尾；`keepOriginalTimestamps: false`（默认归零时间轴）；onProgress(已处理数/总数)；含 EXT-X-DISCONTINUITY 的录制索引 → 直接 reject(reason='discontinuity') 不尝试（D-17 允许隐藏按钮 + 失败文案兜底，Pitfall 5）。
  3. `sanitizeFilename(title)`：替换 `[\\/:*?"<>|]` 与控制字符为 `_`、裁剪长度（≤80 字符）——远端标题不进路径注入面（RESEARCH Security 威胁表）；`buildOutputName(title, now)`：「{sanitize(title)} {YYYY-MM-DD HHmm}.mp4」（D-22 示例「直播间标题 2026-09-06 1430.mp4」）；同名冲突序号逻辑放集成层复用 download-manager getUniqueFilePath。
  4. tests/test-media-remuxer.js：sanitize（斜杠/冒号/引号/控制符/超长）、buildOutputName 格式正则、`new muxjs.mp4.Transmuxer()` 构造 smoke、data 监听先于 push 的结构断言（源码 grep 级或行为级均可）。
  </action>
  <verify>
    <automated>npm ls mux.js --depth=0 && node -e "const m=require('mux.js'); new m.mp4.Transmuxer(); console.log('transmuxer ok')" && node tests/test-media-remuxer.js</automated>
    <fails_when>npm ls 非零退出（mux.js 未安装）；或 node -e 非零退出（CJS 入口不可用）；或测试 fail ≥1 / pass 0</fails_when>
  </verify>
  <done>mux.js 入 dependencies 且主进程可 require/构造；remuxer 满足单实例顺序 push + 先监听后 push + 流式写盘 + discontinuity 拒转；命名/sanitize 有断言覆盖。</done>
  <reversibility rating="reversible">mux.js 为纯 JS 依赖，替换或回退只需 npm uninstall；A3 风险（Node 侧长时稳定）失败可降级「拼接 .ts + 提示」不阻塞主链路。</reversibility>
</task>

<task type="auto">
  <name>Task 2: convert 任务集成 — showSaveDialog/接力/续转/抽屉转换入口</name>
  <files>main.js, ipc-handlers.js, src/player.js, src/preload.js</files>
  <read_first>
    - download-manager.js:394-435（configStore 读默认目录 + showSaveDialogSync 先例——D-24 照此）、download-manager.js:95-112（getUniqueFilePath）、:756-768（showInFolder）
    - media-remuxer.js（Task 1 契约）、media-task-manager.js（onTaskCompleted 钩子）、media-record-engine.js（录制 meta.json 索引形状）、media-cache-manager.js（缓存条目 segments 索引形状）
    - ipc-handlers.js:2085（assertPlayerSender）
    - src/player.js（44-04 抽屉渲染结构——按钮插入点）
    - .planning/phases/44-/44-UI-SPEC.md（Copywriting「转换为 MP4」/失败文案、Disabled 规则）
    - .planning/phases/44-/44-03-PLAN.md（/api/tasks/convert-resume 预留路由与本任务的落地约定）
  </read_first>
  <action>
  1. 转换发起统一入口（main.js 内部函数 `startConvertTask({ segmentPaths, title, containerId, sourceTaskId? })`）：showSaveDialog（defaultPath = buildOutputName(title) 落在 `settings.lastMediaSaveDir`（缺省 app.getPath('downloads')）——D-24 记住选择写回 configStore；用户取消 → 不建任务返回 cancelled；路径确定 → getUniqueFilePath 同名「 (2)」序号 → taskManager.registerTask({ type:'convert', title, containerId }) → media-remuxer convertToMp4 后台执行、onProgress → updateProgress → 终态经 44-03 链路（completed 通知 + showItemInFolder；failed 带 error 文案「MP4 转换失败：{原因}」）。
  2. D-22 接力：taskManager.onTaskCompleted 注册分支——type=record 且 status=completed 时调 startConvertTask（meta.json 的分片列表 + title）——即「停止录制 → 弹框选目录 → 自动转换」；用户取消弹框则不建 convert 任务，已录分片保留，任务页该 record 条目仍可「已落盘部分续转」（D-22 与 D-24 的合成语义）。
  3. ipc-handlers.js：`player:convert/start`（assertPlayerSender；入参 entryId（缓存条目）或 taskId（interrupted record）——D-17 校验：缓存条目完整度 100% 或 record 任务 status=interrupted，否则返回 { ok:false, reason:'segments_incomplete' } 前端已隐藏按钮属纵深防御）；分别取 media-cache-manager / record meta.json 的分片路径列表 → startConvertTask。
  4. main.js：落地 `POST /api/tasks/convert-resume`（44-03 预留）：taskId → 该 record 任务的录制目录分片 → startConvertTask（弹框选目录在主进程发起）。
  5. src/preload.js playerAPI.startConvert；src/player.js 抽屉条目渲染补「转换为 MP4」按钮：仅 `completeness >= 100` 或条目对应 interrupted 录制时可见，否则隐藏（非置灰——UI-SPEC Disabled）；点击 → playerAPI.startConvert({ entryId })。
  </action>
  <verify>
    <automated>node --check main.js && node --check ipc-handlers.js && node --check src/player.js && node --check src/preload.js && grep -n "convert-resume" main.js && grep -n "lastMediaSaveDir" main.js && grep -n "startConvert" src/preload.js src/player.js && node tests/test-media-remuxer.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或任一 grep 无匹配退出码 1（convert-resume 端点/lastMediaSaveDir 记忆/preload 桥缺失）；或 remuxer 测试 fail ≥1</fails_when>
  </verify>
  <done>录制停止自动弹框接力转换（D-22）；任务页「已落盘部分续转」可用；抽屉仅齐片/中断条目显转换按钮（D-17）；产物命名/同名序号/last-used 记忆/完成通知+定位（D-24）就位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端媒体标题 → 产物文件名 | 标题含路径字符/控制符可造成写入位置偏移 |
| 用户选择目录 → 写盘 | showSaveDialog 返回路径即写盘许可 |
| 渲染进程 → convert IPC | renderer 被攻破时可触发转封装读任意缓存分片 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-15 | Tampering | 产物文件名注入（远端标题含 / 等） | high | mitigate | sanitizeFilename 白名单替换 + 长度裁剪；目录固定为用户所选，仅文件名来自标题 |
| T-44-16 | Elevation | 被攻破 renderer 滥用 player:convert/start | medium | mitigate | assertPlayerSender + D-17 服务端复校（segments_incomplete 拒绝）；分片路径仅取自 media-cache-manager / record meta 索引，不接受 renderer 传路径 |
| T-44-17 | DoS | 超大录制转封装内存膨胀 | medium | mitigate | 流式写盘（readFileSync 逐分片 + createWriteStream），非全量载入内存；单分片 readFileSync 大小受录制分片固有上限约束 |
| T-44-SC | Tampering | npm 安装 mux.js@^6.3.0 | high | mitigate | RESEARCH Package Legitimacy Audit：npm registry、10+ 年历史、1,178,431 周下载、github.com/videojs/mux.js、postinstall null、无原生模块——verdict OK（Approved），无需人工 checkpoint；安装后 `npm ls mux.js` 复核版本一致 |
</threat_model>

<verification>
- `npm ls mux.js --depth=0` 版本 ^6.3.0；`node tests/test-media-remuxer.js` 全绿
- `node --check` 全部改动 JS 通过
- 全套回归：`node tests/test-media-cache.js && node tests/test-m3u8-playlist-parser.js && node tests/test-media-task-registry.js && node tests/test-unified-navigation.js` 全绿
- 手工（UAT，打包版优先）：录制→停止→弹框→转换→通知→Finder 定位全链路（RESEARCH Pitfall 8/A3：通知与转封装真机行为在 make install 版验收）
</verification>

<success_criteria>
- mux.js 单实例顺序转封装可用且时间轴不裂（Pattern 3 约束全满足）
- D-22 接力、D-24 弹框/命名/通知/定位、D-17 按钮 gating 全部落地
- 任务页失败/续转文案与 UI-SPEC 一致；「已中断」录制可续转 mp4（崩溃不白录）
- 全套测试绿 + 零 ffmpeg-static
</success_criteria>

<output>
Create `.planning/phases/44-/44-05-SUMMARY.md` when done
</output>
