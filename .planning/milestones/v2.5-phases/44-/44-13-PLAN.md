---
phase: 44-player-video-cache-and-local-media-library
plan: 13
type: execute
wave: 3
depends_on: ["44-10", "44-11"]
files_modified:
  - src/renderer.js
  - src/tasks-page.js
  - src/tasks.html
  - src/styles/main.css
  - main.js
autonomous: true
gap_closure: true
gap_ids: [G-44-7, G-44-9]
requirements: [D-18, D-22, D-26]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "有 running 录制/转换任务时主窗口工具栏任务角标出现（数字 = running 数）、归零消失、点击跳 realm://tasks——监听真正注册（G-44-7：preload 命名空间错配修复后 media-task:count-changed 可达 renderer）"
    - "任务页任何 API 操作失败（cancel/show-in-folder/convert-resume 非 success 响应或网络异常）都有可见错误反馈：页头反馈条展示后端 error 字段，约 4s 自动消失，新操作立即覆盖（G-44-9 missing 第 1 项）"
    - "convert-resume 的 no_segments 失败展示解释性文案「录制中崩溃的任务暂无分片索引，暂不支持续转」而非裸机器码（G-44-9 missing 第 2 项）"
    - "不做无声失败：apiAction 的 console.error 保留为日志层，用户可见层由反馈条承担"
  artifacts:
    - src/renderer.js :10552 监听注册改用 mediaAPI 命名空间（单行修复）+ initMediaTaskBadge 注释同步 + maybePlayerUrl JSDoc 直连注释同步（44-09 配套）
    - src/tasks-page.js showTaskFeedback（textContent 写入 + 4s 定时消失 + 新操作覆盖）+ apiAction 失败/异常分支接线
    - src/tasks.html 页头下方 #taskFeedback 反馈条元素（CSS 类初始隐藏）
    - src/styles/main.css .task-feedback 样式
    - main.js convert-resume 端点错误文案映射（no_segments → 解释性中文）
  key_links:
    - main.js persistMediaTasks → broadcast('media-task:count-changed', {count}) → preload mediaAPI.onMediaTaskCountChanged → renderer updateMediaTaskBadge（修复后打通）
    - tasks-page apiAction 失败 → showTaskFeedback(data.error)（convert-resume no_segments 由 main.js 端点翻译为中文后展示）
  prohibitions:
    - statement: 禁止改 preload.js 挪动 onMediaTaskCountChanged 到 realmAPI（UAT missing 明示「取前者」——renderer 改用 mediaAPI，与同文件媒体面板 onMediaListUpdate 命名空间一致）
      status: locked
      verification: src/preload.js 零 diff（git diff --stat src/preload.js 为空）；renderer.js 注册处引用 mediaAPI 命名空间
    - statement: 硬崩溃续转能力（录制中周期写 meta / seq 文件名合成索引）不在本阶段实现（用户判定记 REVIEW.md 债务，本阶段只修失败反馈）
      status: locked
      verification: main.js 无录制中周期写 meta 的新增逻辑；readRecordTaskSegments 短路行为不变
---

<objective>
修复 G-44-7（单行根因）：主窗口任务角标永不更新——onMediaTaskCountChanged 定义在 preload mediaAPI 命名空间（src/preload.js:1537），而 renderer.js:10552 判断并调用的是 realmAPI.onMediaTaskCountChanged（不存在）→ 条件短路、监听从未注册。修复 = renderer 改用 mediaAPI 命名空间（UAT missing 指定方案，单行）。顺带同步 renderer.js 中 maybePlayerUrl 的 JSDoc（44-09 直连修复的注释一致性——导航权威文档约定「两处注释互引，改动时两份同步」）。

修复 G-44-9：任务页 API 操作失败无可见反馈（apiAction 仅 console.error）——加页头反馈条展示后端 error 字段（约 4s 自动消失或被新操作覆盖）；convert-resume 的 no_segments 在后端端点翻译为解释性中文（「录制中崩溃的任务暂无分片索引，暂不支持续转」）。硬崩溃续转能力本身维持 REVIEW.md 债务不动（用户判定）。

Purpose: 角标是媒体任务中心的唯一常驻入口（归零即消失，坏了等于没有）；无声失败让用户以为按钮坏了。
Output: 命名空间单行修复 + 反馈条 + 后端文案映射。

**已知债务（显式记录）**：① 续转能力本身——录制中周期性写轻量 meta.json 或按 &lt;seq&gt;.ts 文件名合成索引（UAT 第 9 项用户判定记 REVIEW.md 债务）；② WR-A/B/C/05/01-04、IN-01-04 未点名项维持债务。**tracer 说明**：gap-closure 小计划，无独立 tracer 收益。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-UAT.md
@.planning/phases/44-/44-09-PLAN.md

@src/renderer.js
@src/tasks-page.js
@src/tasks.html
@src/styles/main.css
@main.js
</context>

<artifacts_this_phase_produces>
- src/renderer.js：initMediaTaskBadge 内监听注册由 realmAPI 改为 mediaAPI 命名空间（:10552-10553）；:10540-10542 注释「经 preload realmAPI IPC」改为 mediaAPI；maybePlayerUrl JSDoc（:387-390）补直连说明（44-09 配套同步）
- src/tasks-page.js：新增 showTaskFeedback(message)（#taskFeedback textContent 写入 + 移除 hidden + 4s 定时器复位重挂 + hidden 恢复）；apiAction 非 success 分支与 catch 分支接线；loadTasks 失败分支不接（轮询静默，避免 5s 一次噪音）
- src/tasks.html：页头下方 `<div class="task-feedback hidden" id="taskFeedback" role="alert"></div>`
- src/styles/main.css：.task-feedback 样式（错误态红系边框/底色，页头下方留白）
- main.js：convert-resume 端点（:2284-2286）错误响应文案映射——no_segments → '录制中崩溃的任务暂无分片索引，暂不支持续转'
- 无新模块、无新依赖、preload.js 零改动
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: G-44-7 — 角标监听命名空间修复（mediaAPI）+ renderer 注释同步</name>
  <files>src/renderer.js</files>
  <read_first>
    - src/renderer.js:10538-10557（initMediaTaskBadge：注释 :10540-10542、监听注册 :10552-10553 现引用 realmAPI 命名空间——G-44-7 根因行）
    - src/preload.js:1537-1541（onMediaTaskCountChanged 在 mediaAPI 段的事实——本任务不改 preload，锁定见 prohibitions）与 src/renderer.js:10545 附近同文件 mediaAPI.onMediaListUpdate 的既有用法（命名空间一致性先例）
    - src/renderer.js:387-406（maybePlayerUrl 定义与 JSDoc——「hls.js 经 /proxy 同源代理拉流，需 token 鉴权」表述需按 44-09 直连结论同步；导航权威文档 docs/product/navigation-entry-points.md §五「两处注释互引，改动时两份同步」约定）
    - .planning/phases/44-/44-UAT.md G-44-7（root_cause：命名空间错配、主进程广播链路已核查正常）
  </read_first>
  <action>
    1. src/renderer.js:10552-10553：监听注册改用 mediaAPI 命名空间——`if (window.mediaAPI && window.mediaAPI.onMediaTaskCountChanged)` 且回调内 `window.mediaAPI.onMediaTaskCountChanged(...)`（preload.js 零改动，锁定见 prohibitions；UAT missing 明示取 renderer 侧单行方案）。
    2. initMediaTaskBadge JSDoc（:10540-10542）：「经 preload realmAPI IPC」改为「经 preload mediaAPI IPC」（与实际命名空间一致）。
    3. maybePlayerUrl JSDoc（:387-390）同步 44-09 直连结论：把「播放器页面 hls.js 经 /proxy 同源代理拉流，需 token 鉴权、容器 session 携带 Cookie」改写为「webview tab 播放器页面直连拉流（44-09 G-44-2，D-01：仅独立播放器窗口走 /proxy）；container/token 参数保留供页面上下文」。函数体零改动（包装逻辑不变）。
    4. updateMediaTaskBadge（:10523-10536）与 openUrl('realm://tasks') 跳转（:10548-10550）零改动（角标渲染与点击链路本就正确，坏的只是监听注册）。
  </action>
  <verify>
    <automated>node --check src/renderer.js && [ "$(grep -c 'window.mediaAPI.onMediaTaskCountChanged' src/renderer.js)" -ge 1 ] && ! grep -q 'window.realmAPI.onMediaTaskCountChanged' src/renderer.js && grep -q 'mediaAPI IPC' src/renderer.js</automated>
    <fails_when>node --check 语法错误非零退出；或 mediaAPI 命名空间注册缺失（计数 0 → gate 非零）；或 renderer 仍残留 realmAPI 命名空间的该监听引用（! grep 非零——错配未清）；或注释未同步（grep 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：initMediaTaskBadge 注册处引用 mediaAPI.onMediaTaskCountChanged（与 preload 定义同命名空间），realmAPI 命名空间引用清零
    - 行为语义：running 任务数变化 → count-changed 广播 → updateMediaTaskBadge 实际被调用（角标 count>0 显示、归零消失、点击跳 realm://tasks——后两者本已正确）
    - 源码断言：preload.js 零改动（git diff 确认）；maybePlayerUrl JSDoc 与 44-09 直连语义一致
  </acceptance_criteria>
  <done>录制/转换进行中主窗口工具栏角标出现、归零消失、点击跳任务页；监听注册与 preload 命名空间一致。</done>
</task>

<task type="auto">
  <name>Task 2: G-44-9 — 任务页可见失败反馈（反馈条）+ no_segments 解释性文案</name>
  <files>src/tasks-page.js, src/tasks.html, src/styles/main.css, main.js</files>
  <read_first>
    - src/tasks-page.js:288-307（apiAction 现状：非 success 仅 console.error + loadTasks；catch 仅 console.error——G-44-9 根因）、:91-118（bindZoneActions 三个操作按钮 → apiAction 调用点）、:53-57 与 :316-321（escapeHtml/textContent 惯例）
    - src/tasks.html:9-20（tasks-page 容器与页头结构——反馈条插在页头 header 之后；:6 CSP style-src 'self' 'unsafe-inline' 但仍按项目约定用 CSS 类初始隐藏 + JS 类切换）
    - src/styles/main.css 任务页段（task-card/task-error 附近——.task-feedback 样式插入位置与红系错误配色 token 沿用）
    - main.js:2258-2290（convert-resume 端点：:2284-2286 错误响应 `r.reason === 'cancelled' ? '已取消转换' : (r.reason || '续转失败')`——no_segments 裸机器码直出的位置）
    - .planning/phases/44-/44-UAT.md G-44-9（root_cause 两层：后端 400 no_segments 正确，前端无可见反馈；文案解释示例）
  </read_first>
  <action>
    1. src/tasks.html 页头（downloads-page-header div 之后）插入反馈条：`<div class="task-feedback hidden" id="taskFeedback" role="alert"></div>`——初始隐藏走 CSS 类（realm:// 页面约定），无内联样式。
    2. src/styles/main.css 任务页段追加 .task-feedback：块级、页头下留白、13px、圆角边框错误态配色（红系 border + 浅红底，沿用文件内既有错误色 token）；.task-feedback.hidden { display: none }。
    3. src/tasks-page.js 新增 showTaskFeedback(message)：模块级 `let feedbackTimer = null;`；函数体——取 #taskFeedback 元素（无则 return）、`el.textContent = message`（textContent 写入，后端 error 字符串零注入面）、classList.remove('hidden')、feedbackTimer 先 clear 再重挂 4s setTimeout 恢复 hidden（新操作立即覆盖旧文案——定时器重挂即「常驻至下次操作或 4s」语义）。
    4. apiAction（:288-307）接线：非 success 分支在 console.error 之后调 `showTaskFeedback(data.error || '操作失败，请重试')`；catch 分支调 `showTaskFeedback('请求失败，请重试')`。两处 loadTasks() 调用保持不动（失败后仍刷新列表）。loadTasks 自身的轮询失败分支（:126-139）**不接**反馈条——5s 轮询失败弹条是噪音，维持 console.error。
    5. main.js convert-resume 端点（:2285）错误文案映射：改为 `r.reason === 'cancelled' ? '已取消转换' : (r.reason === 'no_segments' ? '录制中崩溃的任务暂无分片索引，暂不支持续转' : (r.reason || '续转失败'))`——后端翻译（任务页零特判，其他调用方受益）。仅此一行条件表达式，续转能力本身不动（锁定见 prohibitions——readRecordTaskSegments 短路、录制中不写 meta 的现状保持）。
    6. 注释纪律：showTaskFeedback JSDoc 注明「textContent 写入防注入 + 4s 自动消失、新操作覆盖」语义。
  </action>
  <verify>
    <automated>node --check src/tasks-page.js && node --check main.js && grep -q 'id="taskFeedback"' src/tasks.html && grep -q 'function showTaskFeedback' src/tasks-page.js && grep -q 'task-feedback' src/styles/main.css && grep -q '暂不支持续转' main.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或反馈条元素/函数/样式任一缺失（对应 grep 非零）；或 main.js 无 no_segments 解释性文案映射（grep 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：showTaskFeedback 用 textContent 写入（非 innerHTML）且定时器重挂（新操作覆盖旧反馈）
    - 源码断言：apiAction 非 success 与 catch 两分支均接反馈条；loadTasks 轮询失败分支不接（防 5s 噪音）
    - 源码断言：main.js convert-resume 对 no_segments 返回解释性中文，cancelled 分支文案保持「已取消转换」
    - 行为断言（真机/UAT 承载）：对无 meta.json 的中断任务点「已落盘部分续转」→ 任务页出现「MP4 转换失败…/录制中崩溃的任务暂无分片索引，暂不支持续转」级反馈条，约 4s 消失；停止/定位失败同样有可见反馈
  </acceptance_criteria>
  <done>任务页 API 操作失败有可见反馈（后端 error 直显、no_segments 有解释文案），不再无声失败；续转能力维持债务现状。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| realm://tasks 页面 → /api/tasks/*（token 鉴权） | 后端 error 字符串进入任务页 DOM |
| 主进程 count-changed 广播 → 主窗口 renderer | 角标计数数据源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G13-01 | XSS | 后端 error 字符串注入反馈条 | low | mitigate | showTaskFeedback 用 textContent 写入（零 HTML 解析）；后端 error 亦为主进程自产文案（reason 映射表），无远端拼接 |
| T-44-G13-02 | DoS | no_segments 等失败被反复触发刷反馈条 | low | accept | 反馈条单例覆盖式展示（重挂定时器），无 DOM 累积；4s 自愈 |
| T-44-G13-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包，不触发包合法性 gate |

无新增 high 威胁（反馈条为纯展示层；任务 API 既有 token 鉴权边界不变）。
</threat_model>

<verification>
1. `node --check src/renderer.js && node --check src/tasks-page.js && node --check main.js` 全过
2. `node tests/test-media-task-registry.js`（26/26）与 `node tests/test-media-remuxer.js` 回归不红；`node tests/test-unified-navigation.js` 回归不红（renderer 导航链路仅注释变更）
3. 源码 gate：mediaAPI 命名空间注册 / 反馈条元素与函数 / no_segments 文案映射；preload.js 零 diff
4. 人工复测（UAT 第 7/9 项）：录制进行中主窗口出现任务角标（count=1）、停止后归零消失；中断任务（无 meta.json）点「已落盘部分续转」→ 反馈条显示「录制中崩溃的任务暂无分片索引，暂不支持续转」约 4s 消失
</verification>

<success_criteria>
- G-44-7 truth 达成：running 任务时角标显示、归零消失、点击跳 realm://tasks（监听真正注册，命名空间与 preload 一致）
- G-44-9 truth 达成：「已落盘部分续转」等操作失败有可见反馈，no_segments 有解释性文案，不做无声失败
- 续转能力维持债务（录制中周期写 meta / seq 合成索引未实现——用户判定）
- preload.js 零改动；renderer 导航链路仅注释变更（导航测试零回归）
</success_criteria>

<output>
Create .planning/phases/44-/44-13-SUMMARY.md when done
</output>
