---
phase: 44-player-video-cache-and-local-media-library
plan: 03
type: execute
wave: 2
depends_on: ["44-01", "44-02"]
files_modified:
  - main.js
  - src/tasks.html
  - src/tasks-page.js
  - src/renderer.js
  - src/preload.js
  - src/index.html
  - src/settings.html
  - src/settings-page.js
  - src/styles/main.css
  - docs/product/navigation-entry-points.md
autonomous: true
requirements: [D-05, D-06, D-25, D-26]
estimate:
  tokens: 85000
  raw_tokens: 55000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - realm://tasks 任务页可打开并展示进行中/已完成/失败已中断三区，状态徽标与文案符合 UI-SPEC Copywriting Contract（D-26）
    - 任务页数据经 `/api/tasks/*` + REALM_TOKEN 获取，主窗口不 fetch HTTP（走 realmAPI IPC 角标通道）（D-26 + AGENTS.md 分层约定）
    - 主窗口角标：活跃任务数 > 0 显示（可带数字），归零消失，点击经 openUrl('realm://tasks') 跳任务页（UI-SPEC zero-one-many）
    - 设置页多媒体分区可改缓存目录（showOpenDialog 选取）与容量上限（GB 数字，默认 10），即改即存并生效到 media-cache-manager（D-05/D-06）
    - 任务终态发 Electron Notification（失败不阻断状态更新），通知点击 shell.showItemInFolder（D-26）
    - media-cache-manager 构造参数接线含 isVideoActive = mediaTaskManager.isVideoActive（逐 playbackKey）：活跃录制任务对应的视频被淘汰豁免（D-07）
    - 抽屉列表与任务页空态渲染「暂无观看记录」/「暂无媒体任务」及 body 文案（UI-SPEC empty covered）
  artifacts:
    - src/tasks.html + src/tasks-page.js（downloads 同构三件套）
    - main.js：/tasks 路由 + /api/tasks/list、/api/tasks/cancel、/api/tasks/show-in-folder 端点 + mediaTaskManager 集成（persist/广播/Notification）
    - src/index.html + src/renderer.js：任务角标 + onMediaTaskCountChanged 监听
    - src/settings.html + src/settings-page.js：多媒体分区（缓存目录/容量 + 任务列表入口）
    - docs/product/navigation-entry-points.md 增补两个新导航入口
  key_links:
    - media-task-manager 终态 → notifyRenderer 广播 media-task:count-changed → 主窗口角标；→ /api/tasks/list → 任务页
    - settings.cacheDir/cacheMaxGB（configStore）→ media-cache-manager 构造参数
    - media-task-manager.isVideoActive(playbackKey) → media-cache-manager 构造注入 → 淘汰豁免活跃录制视频（D-07）
  prohibitions:
    - statement: 主窗口/渲染进程不得直接 fetch http://localhost:/api/*（Phase 38 事故；主窗口走 realmAPI IPC，realm:// 同源页面才走 HTTP）
      status: locked
      verification: grep src/renderer.js 无 fetch('/api/tasks；tasks-page.js 仅同源相对路径 fetch
    - statement: realm://tasks 页 markup 不写内联 style（CSP style-src 'self' 拦截）
      status: locked
      verification: sed 抽取 tasks.html 无 style=" 属性；初始隐藏走 CSS 类
---

<objective>
把 44-02 的任务注册表接入主进程（持久化 + 广播 + 系统通知），交付 `realm://tasks` 任务页、主窗口任务角标、设置页多媒体分区（缓存目录/容量 + 任务列表入口），并把两个新导航入口收敛进 openUrl 权威文档。

Purpose: D-25/D-26 的任务中心是录制（44-04）与转封装（44-05）的用户可见面；设置分区是缓存容量治理（D-05/D-06）的唯一入口。
Output: tasks.html/tasks-page.js、main.js 集成、角标、设置分区、导航文档更新。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-/44-CONTEXT.md
@.planning/phases/44-/44-RESEARCH.md
@.planning/phases/44-/44-PATTERNS.md
@.planning/phases/44-/44-UI-SPEC.md
@.planning/phases/44-/44-VALIDATION.md

@main.js
@src/downloads.html
@src/downloads-page.js
@src/settings.html
@src/settings-page.js
@src/renderer.js
@src/preload.js
@docs/product/navigation-entry-points.md
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `src/tasks.html`、`src/tasks-page.js`（页面级 init/loadTasks/renderZone 函数）
- main.js：`/tasks`、`/tasks/*` 路由；`/api/tasks/list`（GET）、`/api/tasks/cancel`（POST）、`/api/tasks/show-in-folder`（POST）端点；mediaTaskManager 初始化与 `media-task:changed` / `media-task:count-changed` 广播；`settings:choose-cache-dir` IPC handler
- src/renderer.js：任务角标渲染（badge 元素 + 计数更新 + 点击 openUrl('realm://tasks')）
- src/preload.js：`realmAPI.onMediaTaskCountChanged(cb)`、`realmAPI.chooseCacheDir()`
- src/settings.html/settings-page.js：多媒体分区（settings.cacheDir / settings.cacheMaxGB / 任务列表按钮）
- settings 键：`settings.cacheDir`（默认 userData/media-cache）、`settings.cacheMaxGB`（默认 10）
- 注：「已中断任务续转」的 `/api/tasks/convert-resume` 端点与抽屉「转换为 MP4」属 44-05，本计划任务页先渲染该按钮并调用该端点（44-05 落地前按钮返回 404 不算本计划缺陷）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: media-task-manager 主进程集成 + realm://tasks 任务页三件套</name>
  <files>main.js, src/tasks.html, src/tasks-page.js</files>
  <read_first>
    - main.js:2394-2411（/api 端点 token 校验 + sendJson 结构）、main.js:2419-2464（内部页路由分支）、main.js:2474-2481（srcRoot/nodeModulesRoot 安全检查）、main.js:2499-2507（服务器启动 + realmPort/REALM_TOKEN）
    - media-task-manager.js（44-02 产出：createMediaTaskManager 契约）
    - src/downloads.html 全文（91 行骨架：CSP meta、base href、styles/main.css、原生 dialog——整页照抄改 /tasks/）
    - src/downloads-page.js:40-220（init token、syncTheme、loadDownloads 列表渲染、空态、dialog 确认）
    - .planning/phases/44-/44-UI-SPEC.md（Component #7 任务页规格、Copywriting Contract 状态文案、Project Conventions 1/3）
    - .planning/phases/44-/44-PATTERNS.md（tasks.html/tasks-page.js Analog 节）
  </read_first>
  <action>
  1. main.js 集成 mediaTaskManager：whenReady 后 `createMediaTaskManager({ persist, now })`——persist 写 `userData/media-tasks.json`；启动时读该文件调 restoreTasks（running→interrupted，D-18）；终态变更时 `notifyRenderer('media-task:changed', task)` 与 `notifyRenderer('media-task:count-changed', { count })`（广播写法照 download-manager.js:186-205）；终态 completed/failed 时发 Electron Notification（文案：completed convert「MP4 转换完成：{文件名}」、completed record「录制已保存」、failed 带 error 原因；notification 事件 click → shell.showItemInFolder(outputPath)；通知失败 console.warn 不阻断——Pitfall 8）。
  2. main.js 新增端点（token 校验 + sendJson 照抄 :2394-2411 结构）：`GET /api/tasks/list` → listTasks()；`POST /api/tasks/cancel`（body/taskId → cancelTask，仅 running 可取消）；`POST /api/tasks/show-in-folder`（taskId → shell.showItemInFolder(outputPath)，仅 completed/interrupted 且路径存在）。`POST /api/tasks/convert-resume` 本计划只预留路由位（44-05 实现，未命中返回 404 JSON）。
  3. main.js 路由映射加 `/tasks` 分支（照 :2451 downloads 分支抄：`/tasks` → src/tasks.html，`/tasks/*` → src/ 子路径）。
  4. src/tasks.html：整页照抄 downloads.html 骨架，改 `<base href="/tasks/">`、`<script src="tasks-page.js">`；页面标题「媒体任务」20px/600；三区（进行中/已完成/失败已中断）16px/600 分区标题；列表区独立滚动（页面级不滚，沿用 downloads 惯例）；空态「暂无媒体任务」+ body「直播录制和 MP4 转换任务会在这里显示进度」。
  5. src/tasks-page.js：照 downloads-page.js 结构——init 从 URL 取 token、syncTheme 照抄、loadTasks 拉 `/api/tasks/list?token=` 渲染三区；卡片含状态徽标（running「进行中」accent / completed「已完成」success / failed「失败」danger / cancelled「已取消」/ interrupted「已中断」warning，UI-SPEC 文案）、进度条 4px accent 填充、产物路径 13px 次要色（任意断行 + title tooltip）、操作按钮（进行中：停止/取消；已完成/已中断：定位；已中断：「已落盘部分续转」按钮 POST /api/tasks/convert-resume）；监听留空轮询（5s 定时刷新即可，与 downloads-page 惯例一致）。样式类写进 src/styles/main.css（`--bg-*` token 体系，禁止内联 style——AGENTS.md 内部页面 CSP）。
  </action>
  <verify>
    <automated>node --check main.js && node --check src/tasks-page.js && grep -q 'base href="/tasks/"' src/tasks.html && [ "$(sed -n '/<body/,/<\/body>/p' src/tasks.html | grep -c 'style="')" -eq 0 ] && grep -n "api/tasks/list" main.js && grep -n "media-task:count-changed" main.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 base href 缺失 / tasks.html body 含内联 style（grep 计数非 0）/ api/tasks/list 或 media-task:count-changed 无匹配（grep 退出码 1）</fails_when>
  </verify>
  <done>realm://tasks 页可经 /tasks 路由加载（token 鉴权），三区渲染与状态文案符合 UI-SPEC；/api/tasks/list|cancel|show-in-folder 可用；任务终态广播 + 系统通知 + restore interrupted 生效。</done>
</task>

<task type="auto">
  <name>Task 2: 主窗口任务角标 + 设置页多媒体分区 + 导航文档收敛</name>
  <files>src/index.html, src/renderer.js, src/preload.js, src/settings.html, src/settings-page.js, src/styles/main.css, main.js, docs/product/navigation-entry-points.md</files>
  <read_first>
    - src/renderer.js:344-406（realmUrlToHttp + openUrl 统一入口——角标点击与设置入口必须经此）
    - src/preload.js（realmAPI 块与 onMediaListUpdate 惯例）
    - src/settings.html:530-575（43 期 AI 分区布局惯例）+ src/settings-page.js（即改即存惯例、43 期 aiBashWhitelist 分区写法）
    - download-manager.js:394-435（configStore 读取 + showSaveDialog 先例——showOpenDialog 同款）
    - .planning/phases/44-/44-UI-SPEC.md（Component #8 设置分区、#9 角标；Copywriting「更改目录…」建议采纳）
    - docs/product/navigation-entry-points.md（现行结构，增补格式照既有条目）
  </read_first>
  <action>
  1. 角标：src/index.html 工具栏/侧边栏放徽标容器（8px 圆点 `var(--accent-color)`，可带数字，样式加 src/styles/main.css）；src/renderer.js 监听 `realmAPI.onMediaTaskCountChanged`——count>0 显示（badge 数字=活跃任务数），归零隐藏（UI-SPEC zero-one-many：点击统一 openUrl('realm://tasks')，不逐个弹）；点击走 `openUrl('realm://tasks')` 收敛统一导航入口。
  2. src/preload.js realmAPI 增加 `onMediaTaskCountChanged(cb)`（监听 media-task:count-changed，返回清理函数惯例）与 `chooseCacheDir()`（invoke settings:choose-cache-dir）。
  3. 设置页多媒体分区（src/settings.html 新 section + settings-page.js）：「缓存目录」只读路径展示 + 「更改目录…」按钮（调 realmAPI.chooseCacheDir → 主进程 dialog.showOpenDialog 目录选择 → configStore.set('settings.cacheDir')，不手输路径——V5 输入校验约定）；「缓存上限」数字输入（GB，默认 10，即改即存 `settings.cacheMaxGB`，前端校验 1-1024 整数，非法值回退默认——沿用设置页校验惯例）；「任务列表」按钮 → openUrl('realm://tasks')。
  4. main.js：`settings:choose-cache-dir` IPC handler（assertTrustedSender + showOpenDialog openDirectory）；media-cache-manager 构造参数接线——cacheRoot 读 `settings.cacheDir`（缺省 `path.join(app.getPath('userData'), 'media-cache')`）、capacityBytes 读 `settings.cacheMaxGB * 1024**3`（缺省 10GB，D-06）、**isVideoActive 回调接 `mediaTaskManager.isVideoActive`（44-02 契约的逐 playbackKey 查询）**——淘汰时 D-07 豁免经此回调查询 running record 任务对应的视频；修改 cacheDir 后重建 manager 实例（旧缓存目录保留不删，重建时同样注入三个参数）。
  5. docs/product/navigation-entry-points.md 增补两个入口：主窗口任务角标点击（→ realm://tasks）、设置页多媒体分区「任务列表」按钮（→ realm://tasks），标注 disposition 与容器参数（无）。
  </action>
  <verify>
    <automated>node --check src/renderer.js && node --check src/settings-page.js && node --check src/preload.js && node --check main.js && grep -n "realm://tasks" src/renderer.js && grep -n "cacheMaxGB" src/settings-page.js main.js && grep -n "isVideoActive" main.js && grep -n "onMediaTaskCountChanged" src/preload.js src/renderer.js && node tests/test-unified-navigation.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或任一 grep 无匹配退出码 1（含 isVideoActive 接线缺失）；或 tests/test-unified-navigation.js 非零退出（导航入口回归破坏）</fails_when>
  </verify>
  <done>角标随活跃任务数显隐并经 openUrl 跳任务页；设置页可改缓存目录/容量并即时生效到 media-cache-manager；导航文档含两个新入口；既有 32 项导航断言不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost HTTP → /api/tasks/* | 本机任意进程可扫端口，需 token 门禁 |
| 设置输入 → configStore / 缓存路径 | 目录选取与数字输入若不校验可写坏配置或指缓存到任意目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-07 | Spoofing | 未持 token 调 /api/tasks/* | high | mitigate | 全部端点照抄 main.js:2394-2411 的 REALM_TOKEN 校验，无例外 |
| T-44-08 | Tampering | 缓存目录被指到系统目录 | medium | mitigate | 缓存目录仅经 dialog.showOpenDialog 选取（不手输），media-cache-manager 写盘侧仍有 cacheRoot 前缀 + realpath 校验（44-01）兜底 |
| T-44-09 | Tampering | cacheMaxGB 非法值 | low | mitigate | 前端 1-1024 整数校验 + 主进程侧缺省回退 10GB |
| T-44-10 | Information Disclosure | 任务页向无 token 请求泄漏任务路径 | medium | mitigate | /api/tasks/* 无 token 一律 403，不回任何字段 |
| T-44-SC | Tampering | npm 安装（本计划无新增依赖） | high | mitigate | 本计划零新依赖；mux.js legitimacy 审计在 44-05（verdict OK） |
</threat_model>

<verification>
- `node --check` 全部改动 JS 通过
- `node tests/test-unified-navigation.js` 全绿（新入口收敛 openUrl 无回归）
- 手工（UAT）：realm://tasks 三区渲染、空态文案、角标显隐、设置分区即改即存
</verification>

<success_criteria>
- 任务页与 downloads/history 同构（CSP/base/token/暗色主题），状态文案与 UI-SPEC 一致
- 角标 zero-one-many 行为正确且点击收敛 openUrl
- settings.cacheDir/cacheMaxGB 即改即存并实际作用于 media-cache-manager
- 主窗口零 fetch HTTP、tasks.html 零内联 style
</success_criteria>

<output>
Create `.planning/phases/44-/44-03-SUMMARY.md` when done
</output>
