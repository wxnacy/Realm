---
phase: 44-player-video-cache-and-local-media-library
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - media-remuxer.js
  - tests/test-media-remuxer.js
autonomous: true
gap_closure: true
gap_ids: [G-44-7]
requirements: [D-22]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - convertToMp4 任何「stream 创建后、首条 data 写盘前」失败路径（encrypted_stream / unsupported_container / cancelled / 早期 transmux 异常）reject 后，outputPath 处不残留 0 字节文件（含事件循环再转 100ms 后仍不存在）
    - 转换失败与清理行为不再依赖异步 open 的时序——产物文件在 fail() 执行前已确定性存在或从未创建
  artifacts:
    - media-remuxer.js（产物 WriteStream 改为同步 fd 创建，G-44-7 泄漏层修复）
    - tests/test-media-remuxer.js 新增 G-44-7 产物泄漏回归 describe（密文拒转 + 协作式取消两路径延迟存在性断言）
  key_links:
    - fs.openSync(outputPath, 'w') 同步建 fd → fs.createWriteStream(outputPath, { fd })——文件在 Promise 执行体内同步存在，fail() 的 unlinkSync 必然命中，无排队 open 竞态
---

<objective>
修复 G-44-7 泄漏层（核心必修）：media-remuxer.js convertToMp4 中 fs.createWriteStream(outputPath) 的异步 open() 与 fail()/empty_output 的 unlinkSync 之间的竞态——凡「stream 创建后、首条 data 写盘前」失败（encrypted_stream / unsupported_container / cancelled / 早期 transmux 异常）都泄漏 0 字节 mp4 残留（unlinkSync ENOENT 被静默吞掉后，排队的 open() 仍会创建文件）。

Purpose: 用户以「Downloads 残留 0 字节 mp4」感知转换失败，误以为转换完成；这是 G-44-7 双因链中用户直接看到的半边（另一半——AES-128 加密源的入口拦截——由 44-18 处理，本计划与其零文件交集、可并行）。
Output: 确定性无泄漏的 convertToMp4 + 泄漏回归测试全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/convert-mp4-zero-bytes.md
@media-remuxer.js
@tests/test-media-remuxer.js

诊断现场证据链：最小复现脚本已 100% 稳定复现（debug session「Current Focus」区）——createWriteStream 的 open(O_CREAT) 经 nextTick 排队未执行 → 嗅探拒绝 → fail() 同 tick unlinkSync 抛 ENOENT 被 catch 吞掉 → 事件循环随后执行排队的 open() 创建 0 字节文件；stream.destroy() 无法取消已入 libuv 队列的 open 系统调用。
</context>

<tasks>

<task type="auto">
  <name>Task 1: convertToMp4 产物流改同步 fd 创建，消除 unlink 竞态</name>
  <files>media-remuxer.js</files>
  <action>
修改 convertToMp4（当前 187 行 `const stream = fs.createWriteStream(outputPath)`）的产物流创建方式：用 `fs.openSync(outputPath, 'w')` 同步创建 fd，再 `fs.createWriteStream(outputPath, { fd })`（保留 `fs.createWriteStream` 字面调用——tests/test-media-remuxer.js:100 的结构断言依赖它）。openSync 包在 try/catch 内，失败（目录不存在/无写权限）按 `remuxError('write_failed', err.message)` reject，不创建 stream。

时序语义变化（写进 convertToMp4 的 JSDoc）：产物文件自 Promise 执行体起同步存在，fail()（193-200 行）的 `fs.unlinkSync(outputPath)` 不再可能 ENOENT-by-race——排队的异步 open 不复存在。stream.destroy() 在 autoClose 默认值下负责关闭传入的 fd，成功路径 stream.end() 同样收尾 fd，两路径均无需手动 closeSync。

**不要**改动：mux.js 时序硬约束（data 监听先于 push、单实例 Transmuxer、流式写盘）、嗅探检查点位置、empty_output 终检、shouldCancel 协作式取消、module.exports。这些路径的行为契约不变，只是清理时序从「竞态」变「确定」。

G-44-7 关联：这是泄漏层修复（G-44-7 missing 第 1 项）；加密源的入口前置拦截与 key URI 缓存分类在 44-18 处理，本任务不做（不越界）。
  </action>
  <verify>
    <automated>node --check media-remuxer.js && node tests/test-media-remuxer.js</automated>
  </verify>
  <done>convertToMp4 产物流经同步 fd 创建；既有 23 项测试全绿（监听先于 push/单实例/流式写盘等结构断言不受影响）</done>
</task>

<task type="auto">
  <name>Task 2: G-44-7 产物泄漏回归测试（密文拒转 + 协作式取消）</name>
  <files>tests/test-media-remuxer.js</files>
  <action>
在 tests/test-media-remuxer.js 新增 describe「G-44-7 产物泄漏回归」，沿用文件既有惯例（node:test + assert、fs.mkdtempSync 临时目录、crypto.randomBytes 造密文分片——参考既有「G-44-4b 首片格式嗅探」describe 的高熵分片构造）：

1. 密文分片 reject 后产物不存在：随机字节分片（≥4KB 高熵，命中 sniff ③ encrypted_stream）→ assert.rejects reason === 'encrypted_stream' → **延迟 ≥100ms 后**（等潜在异步 open 落地，正是本次竞态的复现窗口）断言 fs.existsSync(outputPath) === false。这是 debug session 最小复现脚本的测试化。
2. 协作式取消路径同断言：正常 TS 分片（0x47 开头有效 TS 或沿用既有测试的 TS fixture）+ shouldCancel 恒 true → reason === 'cancelled' → 延迟检查产物不存在。
3. 失败前产物从未被创建的入参校验路径（no_segments / discontinuity）本就不建文件，不必重复覆盖。

文件头部注释补一行 G-44-7 覆盖说明（与既有 G-44-4b 条目并列）。**不要**改动既有 23 项测试的任何断言。
  </action>
  <verify>
    <automated>node tests/test-media-remuxer.js</automated>
  </verify>
  <done>新增回归用例全绿：密文拒转与取消两条失败路径延迟检查后产物文件均不存在；总测试数 ≥25 且 0 失败</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地文件系统 ← remuxer 清理 | unlink/openSync 的 outputPath 来自 showSaveDialog 用户选择（startConvertTask），信任面不变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-17-01 | Tampering | media-remuxer.js openSync(outputPath, 'w') | low | accept | outputPath 由主进程 showSaveDialog 产生并经 getUniqueFilePath 去重，非远端可控输入；信任面与既有 createWriteStream 完全一致，无新增攻击面 |
</threat_model>

<verification>
- node --check media-remuxer.js 通过
- node tests/test-media-remuxer.js 全绿（含新增 G-44-7 泄漏回归）
- 复现等价性：debug session 的 /tmp/repro-zero-byte.js 场景（密文分片 reject）在修复后 100ms 检查文件不存在——由 Task 2 第 1 条测试承载
</verification>

<success_criteria>
- 任何「stream 创建后、首条 data 写盘前」失败路径 reject 后 Downloads 不残留 0 字节 mp4
- 清理行为确定性（同步 fd 先于一切失败检查点），不依赖事件循环时序
- mux.js 时序硬约束与既有 23 项测试零回归
</success_criteria>

<output>
Create `.planning/phases/44-/44-17-SUMMARY.md` when done
</output>
