---
phase: 44-player-video-cache-and-local-media-library
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/player.js
  - src/player.html
  - main.js
  - docs/product/navigation-entry-points.md
autonomous: true
gap_closure: true
gap_ids: [G-44-2, G-44-2b, G-44-4a]
requirements: [D-01, D-03, D-21]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "webview tab 模式播放器直连拉流：hls.js loadSource 拿到原始 m3u8 URL（不经 /proxy），与 D-01「webview 像网页自身播放一样直接访问，播放器（独立窗口）才走 proxy」一致（G-44-2）"
    - "独立播放器窗口（mode=independent）仍经 /proxy 同源代理：proxiedUrl 对 isIndependentMode 保持原改写行为，缓存/续播/转封装索引链路零回归"
    - "webview 流量不再进入 /proxy，空索引 meta 副作用（G-44-2b）随之消失；m3u8 清单登记的 cacheEnabled 门槛保持存在（防回归）"
    - "录制进行中工具栏录制按钮显示停止方块图标（红点闪烁之外图标状态可辨识），非录制时显示描边圆点（G-44-4a）"
  artifacts:
    - src/player.js proxiedUrl（守卫改为仅 isIndependentMode 走代理；isWebviewMode 判定与注释移除/改写）
    - src/player.html btn-record 内 icon-stop 停止方块图标（缺省 display:none）
    - src/player.js updateRecordUi 双图标显隐切换（iconRecord / iconStop）
    - main.js will-navigate m3u8 分支注释同步（直连语义）
    - docs/product/navigation-entry-points.md §五 m3u8 双实现说明同步
  key_links:
    - will-navigate/maybePlayerUrl 包装 → localhost player 页 → proxiedUrl（webview 直连 / 独立窗口 /proxy）
    - updateRecordUi ← btnRecord 点击 / syncRecordUi / onRecordStateChanged（红点与按钮图标同源状态）
  prohibitions:
    - statement: 禁止给 webview tab 播放器流量重新附加 cache/vid 参数或恢复其 /proxy 路由（D-01 锁定）
      status: locked
      verification: player.js proxiedUrl 守卫仅含 isIndependentMode（源码 gate）
    - statement: m3u8 清单登记（touchVideo/updatePlaylistIndex）的 cacheEnabled 门槛不得移除（G-44-2b 防回归）
      status: locked
      verification: main.js handleProxyRequest m3u8 分支 `if (cacheEnabled && mediaCache) {` 存在（源码 gate）
---

<objective>
修复 G-44-2（major）：webview tab 模式播放走了 /proxy 路由，导致直接播放 m3u8 时无法识别多媒体——按 D-01 决策让 webview tab 播放器回归直连（仅独立播放器窗口走 /proxy）。随此修复 G-44-2b（webview 代理播放登记空索引 meta 的副作用）自然消失，并断言既有 cacheEnabled 登记门槛作为防回归护栏。顺带修复 G-44-4a：录制进行中工具栏按钮无停止图标（按钮变空白）。

Purpose: webview 直连是用户在 UAT 中明确锁定的行为决策（D-01）；停止图标是录制链路的基础可视反馈。
Output: player.js 直连守卫 + 播放器双图标切换 + 注释/产品文档同步。

**tracer 说明**：本计划为 gap-closure（每个 gap 已有诊断结论与最小修复面），首尾垂直切片无信息增益，按任务性质直接分 wave——修复本身就是一条已诊断的端到端路径（webview 播放链路），verify 用真机回归测试 + 源码 gate 承担。

**已知债务（不在本 plan）**：WR-A/B/C/05/01/02/03/04、IN-01/02/03/04 中未被 G-44-2/2b/4a 点名的项维持债务不动（44-REVIEW.md）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-UAT.md
@.planning/phases/44-/44-CONTEXT.md
@.planning/phases/44-/44-UI-SPEC.md
@docs/product/navigation-entry-points.md

@src/player.js
@src/player.html
@main.js
</context>

<artifacts_this_phase_produces>
- src/player.js：proxiedUrl 守卫改写（`if (!isIndependentMode || !/^https?:\/\//i.test(url)) return url;` 形态）；`const isWebviewMode` 声明移除（不再被引用）；:308-318 区块注释改写为「webview 直连 / 独立窗口 proxy」双语义
- src/player.js：`const iconStop = document.getElementById('icon-stop')`（约 :100 元素引用区）；updateRecordUi 新增 iconStop 显隐分支
- src/player.html：btn-record 内新增 `<svg id="icon-stop" ... style="display:none"><rect .../></svg>` 停止方块
- main.js will-navigate m3u8 分支：仅注释更新（:669-670 「经 /proxy 同源代理拉流需鉴权」→ 直连语义），零逻辑变更
- docs/product/navigation-entry-points.md §五：maybePlayerUrl 条目补直连说明
- 无新 IPC、无新模块、无新依赖
</artifacts_this_phase_produces>

<tasks>

<task type="auto" reversibility="reversible">
  <name>Task 1: G-44-2/2b — webview tab 播放器直连（proxiedUrl 仅独立窗口走 /proxy）</name>
  <files>src/player.js, main.js, docs/product/navigation-entry-points.md</files>
  <read_first>
    - src/player.js:300-335（isWebviewMode 判定 :312、isIndependentMode 判定 :318、proxiedUrl 定义 :326-335——当前守卫按 isWebviewMode 放行 webview tab，这是 G-44-2 根因）、:178（hls.loadSource(proxiedUrl(url))——唯一消费点）
    - src/player.js:337-345（webview tab 分支 paramUrl && !isIndependentMode——隐藏标题栏逻辑与 transport 无关，不动）
    - main.js:662-689（will-navigate m3u8 分支：包装 player 页 URL 与 :669-670 注释）、:319-321 与 :399-408（handleProxyRequest 的 cacheEnabled 判定与 m3u8 清单登记门槛——G-44-2b 防回归断言对象，44-01 起即存在）
    - .planning/phases/44-/44-UAT.md G-44-2 / G-44-2b 条目（root_cause：webview 也走 proxy 路由；ac1be3ae 空索引 meta）
    - docs/product/navigation-entry-points.md §五（m3u8 双实现说明，权威文档同步点）
  </read_first>
  <action>
    按 D-01（用户 UAT 锁定：webview 像之前那样直接访问，播放器——独立窗口——才走 proxy）改写 webview tab 播放器的传输层：

    1. src/player.js proxiedUrl（:326-335）：守卫从 isWebviewMode 改为 isIndependentMode——`if (!isIndependentMode || !/^https?:\/\//i.test(url)) return url;`，其余 /proxy URL 组装逻辑（token/container/referer/cache 透传）保持不变（独立窗口行为零回归）。JSDoc 的「webview tab 与独立窗口 localhost 模式」改为「仅独立播放器窗口（mode=independent）」。
    2. src/player.js :308-318 区块注释改写：webview tab 模式播放器直连拉流（D-01：像网页自身播放一样直接访问源站，不经 /proxy；防盗链/Cookie 由 webview 容器 session 天然携带）；仅独立窗口经 /proxy（token 鉴权、Referer 可控、分片缓存仅此链路）。`const isWebviewMode` 声明随之删除（唯一消费点在 proxiedUrl 守卫，改写后不再被引用；保留会成为死变量）。<!-- planner-discipline-allow: const isWebviewMode -->
    3. main.js will-navigate m3u8 分支注释（:669-670）同步：把「播放器页面 hls.js 经 /proxy 同源代理拉流需鉴权、容器 session 携带 Cookie」改写为「webview tab 播放器页面直连拉流（44-09 G-44-2，D-01：仅独立播放器窗口走 /proxy）；container/token 参数保留供页面上下文使用」。逻辑零变更（params 组装与 playerUrl 构造不动）。
    4. docs/product/navigation-entry-points.md §五：maybePlayerUrl 条目补一句「包装出的 webview tab 播放器页面直连拉流，不走 /proxy（G-44-2 修复，D-01）；仅独立播放器窗口经 /proxy（分片缓存仅此链路）」。
    5. G-44-2b 处置（零代码变更，断言护栏）：webview 回归直连后其流量不再进入 /proxy，空索引 meta 副作用自然消失；同时断言 main.js m3u8 清单登记分支的 `if (cacheEnabled && mediaCache) {` 门槛仍存在（44-01 起即有，作为「即使未来 webview 复用 /proxy 也不会登记空索引」的防回归护栏，见 verify）。
  </action>
  <verify>
    <automated>node --check src/player.js && node --check main.js && [ "$(grep -A5 'function proxiedUrl' src/player.js | grep -c 'isIndependentMode')" -ge 1 ] && ! grep -q 'isWebviewMode' src/player.js && grep -q 'if (cacheEnabled && mediaCache) {' main.js && node tests/test-unified-navigation.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 proxiedUrl 函数体前 5 行无 isIndependentMode（计数 0 → gate 非零，直连守卫未落地）；或 player.js 仍残留 isWebviewMode 引用（! grep 非零——死变量未清）；或 main.js m3u8 登记的 cacheEnabled 门槛缺失（G-44-2b 护栏被破坏）；或 tests/test-unified-navigation.js 非零退出（导航/包装链路回归）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：webview tab 模式（http 加载、无 mode=independent 参数）下 proxiedUrl 返回原始 URL——hls.js 直接请求源站 m3u8；独立窗口（mode=independent）下 proxiedUrl 仍改写为 /proxy URL（token/container/referer/cache 透传不变）
    - 源码断言：player.js 无 isWebviewMode 残留（声明与守卫均移除）
    - 源码断言：main.js m3u8 清单登记保持 cacheEnabled 门槛（touchVideo/updatePlaylistIndex 仅 cache=1 流量触达）
    - 回归：node tests/test-unified-navigation.js 32 项断言全绿（maybePlayerUrl 包装链路未动）
  </acceptance_criteria>
  <done>webview tab 播放器直连拉流（不再经 /proxy，m3u8 直接播放可正常识别），独立窗口 proxy/缓存链路零回归；空索引 meta 副作用随直连消失且有 cacheEnabled 护栏断言。</done>
</task>

<task type="auto">
  <name>Task 2: G-44-4a — 录制按钮停止图标（player.html icon-stop + updateRecordUi 双图标切换）</name>
  <files>src/player.html, src/player.js</files>
  <read_first>
    - src/player.html:82-85（btn-record 现状：仅 icon-record 一个描边圆点；同文件 :58-59 icon-play/icon-pause 的「双 svg + display:none 切换」是现成模式）与 :6（CSP 含 unsafe-inline——markup 内联 style 属性可用，与 icon-pause 同款写法）
    - src/player.js:96-110（元素引用区，iconRecord 在 :100；iconStop 引用加在同区）
    - src/player.js:815-823（updateRecordUi：录制中 iconRecord display:none 但无停止图标可切换 → 按钮空白，G-44-4a 根因）
    - .planning/phases/44-/44-UI-SPEC.md Component 1（录制按钮：未录制=描边圆点，录制中=停止图标让位红点语义）
  </read_first>
  <action>
    1. src/player.html btn-record（:83-85）内、icon-record 之后新增停止方块图标：`<svg id="icon-stop" viewBox="0 0 24 24" style="display:none"><rect x="6" y="6" width="12" height="12" rx="1.5" fill="currentColor"/></svg>`——与 icon-play/icon-pause 同款双图标切换模式（fill 用 currentColor，随按钮态着色）。
    2. src/player.js 元素引用区（:100 iconRecord 旁）新增 `const iconStop = document.getElementById('icon-stop');`。
    3. updateRecordUi（:818-823）：在 iconRecord 显隐行旁加对称分支——录制中显示 iconStop、隐藏 iconRecord；非录制反之（iconRecord 恢复 'block'，iconStop 'none'）。btnRecord.title 切换逻辑保持不变。
    4. 不得改动 btnRecord 点击处理（:845-868）、红点（record-dot）与 syncRecordUi/onRecordStateChanged 链路——G-44-4a 仅是图标显隐缺陷。
  </action>
  <verify>
    <automated>node --check src/player.js && [ "$(grep -c 'icon-stop' src/player.html)" -ge 1 ] && [ "$(grep -A6 'function updateRecordUi' src/player.js | grep -c 'iconStop')" -ge 1 ]</automated>
    <fails_when>node --check 语法错误非零退出；或 player.html 无 icon-stop 元素（计数 0 → gate 非零）；或 updateRecordUi 函数体前 6 行无 iconStop 显隐切换（计数 0 → gate 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：btn-record 同时含 icon-record 与 icon-stop 两个 svg，icon-stop 缺省 display:none（与 icon-pause 同款模式）
    - 源码断言：updateRecordUi 同时切换 iconRecord 与 iconStop 显隐（录制中 iconStop 可见）
    - 行为语义：录制中按钮显示停止方块（不再空白可点无反馈）；停止后恢复描边圆点
    - 回归保护：红点、录制启停 IPC 链路、title tooltip 未被触碰
  </acceptance_criteria>
  <done>录制进行中工具栏按钮显示停止图标，红点闪烁之外图标状态可辨识；停止后恢复录制圆点。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview tab 播放器 → 源站 | 直连后 hls.js 请求直接出容器 session（原本经主进程 ses.fetch） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G09-01 | Information Disclosure | webview 直连后 /proxy 的 Referer 净化不再适用于该链路 | low | accept | webview 内普通网页本就直连（含其自身播放器），本次仅让包装播放器回到与普通页面相同的信任级；无新增注入面 |
| T-44-G09-02 | Spoofing | token 参数仍在 webview player 页 URL 中但不再被消费 | low | accept | token 仅为 /api、/proxy 鉴权用；页 URL 中 token 经 httpUrlToRealm 展示剥离、tab 持久化存原始 m3u8，不外泄给源站（不随 hls.js 请求发送） |
| T-44-G09-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包，不触发包合法性 gate |

无新增 high 威胁（直连即恢复 webview 普通页面同款网络位面；proxy 服务端面缩小——仅独立窗口流量）。
</threat_model>

<verification>
1. `node --check src/player.js && node --check main.js` 全过
2. `node tests/test-unified-navigation.js`（32 项断言）全绿；`node tests/test-media-cache.js` 回归不红（handleProxyRequest 未动，护栏断言用）
3. 源码 gate：proxiedUrl 守卫仅 isIndependentMode；updateRecordUi 双图标切换；main.js cacheEnabled 登记门槛在
4. 人工复测（UAT 第 2/4 项）：webview tab 内直接播放 m3u8 正常识别为多媒体且行为与 Phase 43 一致（无缓存落盘）；独立窗口播放/缓存/续播不回归；录制中按钮显示停止方块
</verification>

<success_criteria>
- G-44-2 truth 达成：webview tab 内播放直连、不走 proxy 路由，m3u8 直接播放可正常识别为多媒体（D-01）
- G-44-2b 随直连消失：webview 流量不再产生空索引 meta，cacheEnabled 登记门槛作为护栏留存
- G-44-4a truth 达成：录制中按钮显示停止图标
- 导航/缓存/录制既有测试零回归
</success_criteria>

<output>
Create .planning/phases/44-/44-09-SUMMARY.md when done
</output>
