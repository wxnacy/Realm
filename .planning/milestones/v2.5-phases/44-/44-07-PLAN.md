---
phase: 44-player-video-cache-and-local-media-library
plan: 07
type: execute
wave: 6
depends_on: ["44-06"]
files_modified:
  - media-cache-manager.js
  - main.js
  - tests/test-media-cache.js
autonomous: true
gap_closure: true
gap_ids: [CR-02, CR-03]
requirements: [D-06, D-07, D-08, D-18]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 21000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "容量上限（D-06 capacityBytes）在生产写路径生效：storeBuffer 落盘登记成功后即执行容量水位检查（不依赖 ENOSPC/EDQUOT），累计超 capacityBytes 自动触发一轮 FIFO 淘汰（写目录自身豁免 + 活跃任务豁免语义保持）——单测断言「只 storeBuffer、不显式调 evictIfNeeded」即发生淘汰"
    - "水位检查不做每分片全库扫描：tracked 水位累计（首次写盘惰性初始化 + 淘汰后权威同步），O(1) 判定；listEntries 全扫仅发生在真正越过容量时一次"
    - "设置页改小 cacheMaxGB 立即触发一轮淘汰：main.js 经新方法 setCapacityBytes 更新（而非直改 capacityBytes 字段），方法内同步 evictIfNeeded + 水位同步"
    - "interrupted/failed record 任务 outputPath 为 null 时续转不再 400：readRecordTaskSegments 按 path.join(RECORD_ROOT, task.id) 补算录制目录（task.id 形态白名单校验防路径穿越），convert-resume 能读到 meta.json 分片索引"
    - "RECORD_ROOT 常量单一来源：createRecordEngine 注入值（原 :2891 内联路径）与补算回退同源，杜绝两处漂移"
  artifacts:
    - media-cache-manager.js：setCapacityBytes(bytes) 新方法 + storeBuffer 内容量水位检查（含 tracked 水位字段与方法）
    - main.js：模块/作用域内 RECORD_ROOT 常量（提自 createRecordEngine 内联）；readRecordTaskSegments 的 outputPath 缺失补算回退；:1337-1342 改调 setCapacityBytes
    - tests/test-media-cache.js：D-06/D-07 既有淘汰单测适配新自动淘汰语义 + 新增「写路径自动淘汰」「setCapacityBytes 缩小触发淘汰」「单视频超限不自删（自豁免）」断言
  key_links:
    - storeBuffer 成功登记 → 水位累计 → evictIfNeeded（listEntries 磁盘权威总量 → 按 last_watched FIFO 整目录删）→ tracked 同步 r.total
    - main.js cacheMaxGB 更新处理器 → mediaCache.setCapacityBytes(gb*B) → evictIfNeeded（内部 total<=capacity 短路）
    - record 任务 restore 快照（outputPath=null）→ readRecordTaskSegments 补算 RECORD_ROOT/<task.id> → meta.json → startConvertTask（D-18 崩溃不白录）
  prohibitions:
    - statement: 不得在 storeBuffer 每片写盘后无条件调 evictIfNeeded 做全库扫描（大视频数千分片 = 数千次全树扫描的性能回归）
      status: locked
      verification: storeBuffer 内淘汰判定只比对 tracked 水位与 capacityBytes（一次加法 + 一次比较），evictIfNeeded 调用位于水位越过分支内
    - statement: 不得改动 evictIfNeeded 的 FIFO 排序与 D-07 豁免语义（isVideoActive 回调 + exemptVideoIds 集合）
      status: locked
      verification: evictIfNeeded（media-cache-manager.js:453-481）排序/豁免分支原样保留
---

<objective>
修复 CR-02（容量 FIFO 淘汰在生产路径从不执行——evictIfNeeded 唯一调用点在 ENOSPC 分支，10GB 上限形同虚设，缓存只增到物理盘满）与 CR-03（interrupted/failed record 任务 outputPath 恒 null，readRecordTaskSegments 返回 null → convert-resume 恒 400 no_segments，「崩溃不白录」兜底失效）。

Purpose: 让 D-06/D-07 的容量语义在生产写路径真实生效（且不引入每分片全库扫描的性能回归）；恢复 taskId→录制目录映射，使已落盘分片可续转。改动全部在 media-cache-manager.js / main.js 集成层。
Output: storeBuffer 容量水位淘汰 + setCapacityBytes + RECORD_ROOT 常量与补算回退 + 配套单测适配与新增。

**与 44-06 的 wave 关系**：44-07 与 44-06 共享文件（media-cache-manager.js、main.js），wave 顺延为 6（depends_on 44-06，接续 44-05 的 wave 4），避免同文件并行编辑冲突。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-VERIFICATION.md
@.planning/phases/44-/44-REVIEW.md
@.planning/phases/44-/44-02-PLAN.md
@.planning/phases/44-/44-04-PLAN.md
@.planning/phases/44-/44-05-PLAN.md

@media-cache-manager.js
@main.js
@tests/test-media-cache.js
</context>

<artifacts_this_phase_produces>
本计划新增符号/契约：
- media-cache-manager.js：`MediaCacheManager#setCapacityBytes(bytes)`（新公开方法：校验赋值 + 立即 evictIfNeeded + 水位同步）；实例字段 `this._trackedTotal`（null=未初始化）+ 内部 `_diskTotalBytes()`（listEntries 求和）——水位累计判定；storeBuffer 成功登记后插入水位检查分支
- main.js：作用域内 `const RECORD_ROOT`（录制根目录常量，提自 createRecordEngine recordRoot 内联 :2891）；readRecordTaskSegments 内 recordDir 补算回退（task.id 白名单 /^[0-9a-fA-F-]{8,64}$/）；settings:update 的 cacheMaxGB 分支改调 `mediaCache.setCapacityBytes(gb * 1024 * 1024 * 1024)`
- tests/test-media-cache.js：D-06/D-07 describe 改造 + 新增「写路径自动淘汰 / setCapacityBytes 缩小触发 / 单视频自豁免」用例
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CR-02 — storeBuffer 写路径容量水位淘汰 + setCapacityBytes（改容量即淘汰）</name>
  <files>media-cache-manager.js, main.js, tests/test-media-cache.js</files>
  <read_first>
    - media-cache-manager.js 全文结构：constructor（:84-94，capacityBytes/DEFAULT_CAPACITY_BYTES）、storeBuffer（:369-415，写盘登记成功点 :409-410 return 前）、_writeWithEvictRetry（:424-442，evictIfNeeded 唯一现存调用点 :432）、evictIfNeeded（:453-481：listEntries 全扫 + last_watched FIFO + isVideoActive 豁免 + exemptVideoIds）、listEntries（:583-600，_dirSize 递归统计——每片调用即全库扫描的成本来源）、_invalidateSegment（:295-309）与 deleteEntry（:488-499）
    - main.js settings:update 的 cacheMaxGB 即改即存分支（:1333-1342：当前直写 mediaCache.capacityBytes = gb*...）
    - tests/test-media-cache.js「FIFO 淘汰与活跃豁免（D-06/D-07）」（:142-183：现有测试直接显式调 evictIfNeeded，未覆盖生产写路径——新语义下自动淘汰会先于显式调用发生，既有断言需适配）与「磁盘满强淘重试（D-08）」（:185-234，verify 语义在新写路径下仍成立的分析见 action 第 4 条）
    - .planning/phases/44-/44-REVIEW.md CR-02（行号 76-90：storeBuffer 成功后主动淘汰 + main.js 改容量触发；「每片后调用开销 = 一次 listEntries 扫描；若担心开销可每 N 片或容量水位触发」——本任务采用水位方案）与 44-VERIFICATION.md gaps 第 3 条
  </read_first>
  <action>
    1. media-cache-manager.js constructor（:84-94 内）：新增实例字段 `this._trackedTotal = null;`（null 表示未初始化——进程内首次写盘前以磁盘实际总量惰性初始化）。
    2. 新增内部方法 `_diskTotalBytes()`：`return this.listEntries().reduce((sum, e) => sum + (e.size || 0), 0);`（复用 listEntries 的磁盘权威口径，供惰性初始化与淘汰后同步）。
    3. 新增公开方法 `setCapacityBytes(bytes)`：入参非有限正数时回落 DEFAULT_CAPACITY_BYTES；赋值 `this.capacityBytes` 后**立即**执行 `try { const r = this.evictIfNeeded(); this._trackedTotal = r.total; } catch (err) { console.warn('[Realm] 容量变更淘汰失败:', err.message); }`（evictIfNeeded 内部已有 total<=capacity 短路：容量调大且未超时零淘汰零成本，仅一次扫描；调小即立即按 FIFO 收敛到新上限——「设置页改小容量触发一轮淘汰」的语义落点）。方法加 JSDoc 注明 CR-02 语义。
    4. storeBuffer（:369-415）：在 `this._writeMeta(videoId, mMeta);`（:409）之后、`return { ok: true, size };`（:410）之前插入容量水位检查分支——语义与实现：
    - `this._trackedTotal = (this._trackedTotal === null ? this._diskTotalBytes() : this._trackedTotal) + size;`（首写惰性全扫一次，此后每片 O(1) 加法）
    - `if (this._trackedTotal > this.capacityBytes)` 时 `try { const r = this.evictIfNeeded(new Set([videoId])); this._trackedTotal = r.total; } catch (err) { console.warn(...) }`——exempt 传正在写入的 videoId（防删自己目录，与 _writeWithEvictRetry :432 同款豁免）；evictIfNeeded 以磁盘扫描为权威，淘汰后把返回的 r.total 同步回水位（自校正：外部删除/deleteEntry 造成的水位漂移在此收敛）
    - 分支内注释写明 CR-02 根因（旧实现 evictIfNeeded 仅 ENOSPC 可达、容量上限生产失效）与水位方案取舍（避免每片全库扫描的性能回归）
    - skipped 早退（:390-392，D-10 已登记跳过）与 disk_full 早退（:396-397）路径不进水位检查（无新增字节）
    - 不触碰 _writeWithEvictRetry 的 ENOSPC 强淘（D-08 兜底保留，两层机制并存）
    5. main.js settings:update（:1337-1342）：把直写 `mediaCache.capacityBytes = gb * 1024 * 1024 * 1024;` 改为 `mediaCache.setCapacityBytes(gb * 1024 * 1024 * 1024);`（改小容量同步触发一轮淘汰——CR-02 missing 第 2 项）。JSDoc 无；旁边注释补「CR-02：容量变更即触发一轮淘汰」。
    6. tests/test-media-cache.js 适配与新增：
    - 「超容量按 last_watched 升序整目录删除」用例（:143-161）**改造**：capacityBytes:100 + sizes 60/50/40 的旧写法在自动淘汰下于 v2 写入即触发（60+50>100 淘汰 v1），显式 evictIfNeeded 断言失效。改为 capacityBytes:130 + sizes 60/50/40：三次 storeBuffer **不显式调 evictIfNeeded**，断言第三次写入后（60+50+40=150>130）v1 目录已被自动删除、v2/v3 存活、磁盘总量 ≤ 上限——直接验证生产写路径淘汰（D-06/D-07 生产生效的回归锚）
    - 「活跃任务豁免淘汰」用例（:163-183）**保留显式 evictIfNeeded 调用**但把 capacityBytes 调大（如 100→150，sizes 80/50，使自动淘汰不提前触发：80≤150、130≤150）后再显式调 evictIfNeeded() 验证豁免语义（该用例的意图是 D-07 豁免分支本身，不受写路径自动淘汰干扰）
    - 新增用例 A「写路径超限自动淘汰」：capacity 100，三个视频 60/50/40（lastWatched 依次 1000/2000/3000）→ 第三次 storeBuffer 后断言 evicted 最旧 v1（last_watched 1000）目录不存在、其余存活（与改造后的上一条用例互补：一条验证阈值、一条验证 FIFO 顺序）
    - 新增用例 B「setCapacityBytes 缩小触发淘汰」：capacity 500 存两视频共 150（不触发自动淘汰）→ `c.setCapacityBytes(100)` 后断言按 last_watched 最旧目录被删、剩余 ≤100、`c.capacityBytes === 100`
    - 新增用例 C「单视频超限自豁免不自杀」：capacity 100 单视频写 150 → 自动淘汰 exempt 自身 → 目录仍存在（防止把「正在写的视频」误删的回归锚；超限收敛留待后续写其他视频/活跃解除时发生）
    - 既有 D-08 ENOSPC 用例（:185-234）不需改逻辑：验证路径（先落旧片 60 后 ENOSPC 写新片）在新水位检查下行为不变（旧片写入时自豁免无法自删、新片 ENOSPC 强淘删旧片重试），但**重跑确认**全绿
  </action>
  <verify>
    <automated>node --check media-cache-manager.js && node --check main.js && node tests/test-media-cache.js && grep -n "setCapacityBytes" main.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 test-media-cache.js 非零退出（fail ≥ 1，含新增/适配断言失败）；或 main.js 无 setCapacityBytes 调用（grep 退出码 1——改容量未桥接新方法）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（单测）：仅经 storeBuffer 写盘（不显式调 evictIfNeeded）累计超 capacityBytes 即发生整目录 FIFO 淘汰，最久未看先删、正在写入的 videoId 自豁免、活跃任务豁免不回归
    - 行为断言（单测）：setCapacityBytes 缩小容量立即触发一轮淘汰（改小即收敛），容量字段生效
    - 源码断言：main.js cacheMaxGB 更新走 mediaCache.setCapacityBytes（grep 命中），不再直写 capacityBytes 字段
    - 性能 guard 源码断言：storeBuffer 内淘汰判定无 listEntries/evictIfNeeded 的无条件调用（仅水位越过分支内一次）
    - 既有 D-08 ENOSPC 强淘用例与其余用例全绿
  </acceptance_criteria>
  <done>容量 FIFO 淘汰在 storeBuffer 生产写路径自动生效（水位方案，无每片全扫）；设置页改小 cacheMaxGB 即时触发淘汰；D-07 豁免语义不回归。</done>
</task>

<task type="auto">
  <name>Task 2: CR-03 — RECORD_ROOT 常量 + readRecordTaskSegments 目录补算回退（interrupted 续转 200）</name>
  <files>main.js</files>
  <read_first>
    - main.js:2874-2895（createRecordEngine 调用：recordRoot 内联 `path.join(app.getPath('userData'), 'media-records')` :2891——本任务提为常量；注释块 :2874-2877 说明 recordRoot 独立于缓存库 D-23）
    - main.js:2909-2931（readRecordTaskSegments：`const recordDir = task.outputPath; if (!recordDir || typeof recordDir !== 'string') return null;` :2915-2916——CR-03 断裂点；metaPath/segments 读取 :2917-2930 逻辑保持）
    - main.js:3019-3030（startConvertFromRecordTask 消费 readRecordTaskSegments）与 :2161-2193（convert-resume 端点：interrupted/failed/completed 准入 :2182-2185 → startConvertFromRecordTask :2186 → no_segments 400 路径）
    - media-task-manager.js registerTask（:124 outputPath: null 初始）/ completeTask（:151-166 终态才写 outputPath）/ restoreTasks（:199-235 outputPath 白名单字段恢复）——理解为什么 interrupted/failed 保存值为 null
    - media-record-engine.js startRecord（:98-100 目录即 recordRoot/<taskId>）与 stopRecord（:265-279 completeTask({ outputPath: recordRoot/<taskId> })）——目录与 outputPath 同源关系
    - .planning/phases/44-/44-REVIEW.md CR-03（行号 92-104：最小修复 = startConvertFromRecordTask/readRecordTaskSegments 内按 path.join(recordRoot, task.id) 补算；recordRoot 与 createRecordEngine 注入值同源建议提常量）
  </read_first>
  <action>
    1. 在 createRecordEngine 调用（main.js:2878）**之前**、同作用域内新增模块级常量声明：`const RECORD_ROOT = path.join(app.getPath('userData'), 'media-records');`，上覆注释说明：录制根目录（D-23：独立于缓存库不参与容量淘汰）；record 任务 outputPath 缺失时（interrupted/failed 快照）readRecordTaskSegments 依此补算 taskId→目录映射（CR-03）。
    2. createRecordEngine 的 deps 里 `recordRoot: path.join(app.getPath('userData'), 'media-records')`（:2891）改为 `recordRoot: RECORD_ROOT`——常量单一来源，杜绝引擎写目录与补算回退两处漂移。
    3. readRecordTaskSegments（:2914-2931）：把开头两行（:2915-2916）替换为补算逻辑：
    - `let recordDir = (task && typeof task.outputPath === 'string' && task.outputPath) ? task.outputPath : null;`（completed 任务沿用引擎写下的 outputPath，不动）
    - 为 null 时补算：`const tid = task && task.id; if (typeof tid !== 'string' || !/^[0-9a-fA-F-]{8,64}$/.test(tid)) return null; recordDir = path.join(RECORD_ROOT, tid);`（task.id 形态白名单——media-tasks.json 可被本地篡改，防补算路径穿越；crypto.randomUUID 输出天然命中该白名单）
    - 其后 metaPath/parse/segmentPaths 逻辑（:2917-2930）原样保留（metaPath 不存在/解析失败仍返回 null：硬崩溃未写 meta.json 的录制无索引可转，属既有边界，本任务不扩展）
    - 函数 JSDoc（:2910-2912）补一句：outputPath 缺失（interrupted/failed 恢复快照）时按 path.join(RECORD_ROOT, task.id) 补算（CR-03）
    4. 注释纪律：不得在注释里保留「outputPath 恒 null 导致 return null」这类会把 verify 判为歧义的字面描述；改动后函数内不再有对 task.outputPath 的无条件 return null 分支。
  </action>
  <verify>
    <automated>node --check main.js && grep -n "RECORD_ROOT" main.js | grep -c "const RECORD_ROOT = path.join(app.getPath('userData'), 'media-records')" | grep -qv '^0$' && node tests/test-media-task-registry.js</automated>
    <fails_when>node --check 语法错误非零退出；或 RECORD_ROOT 常量声明缺失（计数 0 → grep -qv 失败）；或 test-media-task-registry.js 非零退出（回归不红）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：RECORD_ROOT 常量声明存在且 createRecordEngine 的 recordRoot 与 readRecordTaskSegments 补算引用同一常量（main.js 内 `path.join(app.getPath('userData'), 'media-records')` 字面仅出现于常量声明处）
    - 源码断言：readRecordTaskSegments 内存在 task.id 形态白名单校验（/^[0-9a-fA-F-]{8,64}$/）后才 path.join(RECORD_ROOT, tid) 补算——无 outputPath 时不再直接 return null
    - 行为语义：interrupted/failed record 任务（outputPath=null、task.id 为 uuid）经 convert-resume 可读到 recordRoot/<taskId>/meta.json → 不再恒 400 no_segments；completed 任务 outputPath 路径不受影响
    - 回归：media-task-registry 26 条单测全绿（注册表未改动）
  </acceptance_criteria>
  <done>taskId→录制目录映射恢复：interrupted/failed 任务续转链路可读盘；RECORD_ROOT 单一来源，无目录穿越注入面。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 磁盘缓存总量/容量决策 → 删除行为 | 本机缓存目录内容（含 meta.json 可能被篡改的 last_watched/total_size）驱动整目录删除 |
| media-tasks.json 恢复快照 → 路径拼接 | 本地可篡改 JSON 的 task.id/outputPath 进入文件系统路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G07-01 | Tampering | 补算路径拼接（task.id 入路径） | medium | mitigate | readRecordTaskSegments 补算前 task.id 过 /^[0-9a-fA-F-]{8,64}$/ 白名单（uuid 形态）才 path.join；outputPath 直用分支仍受 RESTORE_FIELDS 类型白名单 + show-in-folder existsSync 既有约束（T-44-05） |
| T-44-G07-02 | DoS | 水位漂移致淘汰扫描时机偏差 | low | accept | 水位仅作「是否扫描」决策；evictIfNeeded 始终以磁盘 listEntries 为权威并在每次执行后同步 r.total，漂移最坏 = 提前/滞后一次扫描，无数据损坏面 |
| T-44-G07-03 | DoS | 淘汰误删正在写盘分片 | medium | mitigate | storeBuffer 自动淘汰 exempt 传 new Set([videoId])（写目录自豁免，与 _writeWithEvictRetry :432 同款）；D-07 isVideoActive 活跃豁免保持 |
| T-44-G07-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包，不触发包合法性 gate |

无新增 high 威胁（两项修复均为补上既有设计语义，路径拼接经白名单收敛，未开新注入面）。
</threat_model>

<verification>
1. `node --check media-cache-manager.js && node --check main.js` 全过
2. `node tests/test-media-cache.js`（改造后 D-06/D-07 + 新增 A/B/C 用例 + 既有 D-08/D-09/路径安全全绿）；`node tests/test-media-task-registry.js`（26/26）与 `node tests/test-m3u8-playlist-parser.js` 回归不红
3. 源码 gate：main.js 存在 `const RECORD_ROOT = path.join(app.getPath('userData'), 'media-records')` 且 recordRoot/补算共用；settings cacheMaxGB 走 setCapacityBytes
4. 人工复测（VERIFICATION human_verification 相应项）：设置页把 10GB 改小为 1GB，缓存超限视频被逐出；崩溃/中断的录制任务在任务页点「已落盘部分续转」能进入弹框选目录（不再 400）
</verification>

<success_criteria>
- CR-02 truth 达成：按视频粒度 FIFO 淘汰在生产写路径生效（D-06/D-07）——缓存总量收敛于 capacityBytes，活跃任务豁免不回归
- CR-03 truth 达成：崩溃/失败中断的录制可「已落盘部分续转」（D-18 崩溃不白录）——interrupted/failed 任务 outputPath=null 时经 RECORD_ROOT/<task.id> 补算读到 meta.json
- 水位方案无每分片全库扫描的性能回归；ENOSPC 强淘（D-08）兜底保留
- 单测套件全绿（含对既有 D-06/D-07 用例的语义适配）
</success_criteria>

<output>
Create .planning/phases/44-/44-07-SUMMARY.md when done
</output>
