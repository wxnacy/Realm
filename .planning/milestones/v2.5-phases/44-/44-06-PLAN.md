---
phase: 44-player-video-cache-and-local-media-library
plan: 06
type: execute
wave: 5
depends_on: ["44-05"]
files_modified:
  - main.js
  - media-cache-manager.js
  - src/player.js
  - tests/test-media-cache.js
autonomous: true
gap_closure: true
gap_ids: [CR-01, CR-05]
requirements: [D-03, D-05, D-10]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "handleProxyRequest 中缓存命中先于回源：mediaCache.lookup(target…) 出现在 ses.fetch(target…) 之前（源码顺序断言），命中请求不再发起任何源站请求"
    - "缓存 key 统一为请求 URL（target）：main.js 全文不再存在以 finalUrl 为 key 的 lookup/store 调用（grep gate），与 updatePlaylistIndex 登记的 playlist_order segKey（resolveUri(uri, finalUrl)）语义同源，转封装顺序不受影响"
    - "缓存命中响应 Content-Type 与落盘时一致：storeBuffer 登记 contentType 字段，lookup 命中回放（hit.contentType），默认回落 application/octet-stream"
    - "m3u8/带 Range 请求不进入命中优先路径（pre-fetch lookup 显式排除），m3u8 仍走 rewrite 清单分支（回归保护）"
    - "store() 与 main.js 透传分支对源流挂 'error' 监听：源站 RST 不再抛 uncaught exception 崩主进程，且响应流收尾（res.end 兜底）不悬挂"
    - "hlsRetryCount 在成功拉到分片（FRAG_LOADED）后复位为 0——3 次瞬时错误恢复后降级重试不永久失效（WR-06，被 CR-01 断网照播 missing 点名）"
  artifacts:
    - main.js handleProxyRequest 的 pre-fetch 命中分支（key=target，命中直返不回源）
    - media-cache-manager.js store() 的源流 error 监听（pipe 前挂载，destroy passthrough/collector）
    - media-cache-manager.js lookup() 命中结果扩展 contentType；storeBuffer 分片条目可选 contentType 字段
    - src/player.js hls 分支 FRAG_LOADED 监听内 state.hlsRetryCount = 0
    - tests/test-media-cache.js 新增 store() 源流 error 单测（不崩 + tee 收尾）
  key_links:
    - /proxy 分片命中路径 → 磁盘读盘响应（CR-01 后不再依赖源站可达性，D-10 断网照播成立）
    - store 未命中 tee：resp.body → Readable.fromWeb → store() 内部 readable error → passthrough destroy → main.js tee.on('error') → res.end()（截断响应交由 player 重试链路）
    - player.js fatal network error → startLoad 重试 → FRAG_LOADED → hlsRetryCount 复位（WR-06）
  prohibitions:
    - statement: 不引入 electron/原生依赖到 media-cache-manager.js（去 Electron 化硬约束）
      status: locked
      verification: node -e "require('./media-cache-manager'); console.log('pure-node ok')" 输出 pure-node ok
    - statement: lookup 的 D-09 大小/sha256 校验删除回源逻辑必须保留，不因命中优先而弱化
      status: locked
      verification: media-cache-manager.js lookup() 内 st.size !== entry.size / sha256 比对分支仍存在
---

<objective>
修复 CR-01（缓存命中仍先回源——「重开秒开不重复回源」与「断网照播」两条 failed truth 的共同根因）与 CR-05（缓存 tee 路径源流无 'error' 监听——一次源站 RST 即可让主进程崩溃），并随 CR-01 一并处理 WR-06（hlsRetryCount 成功后不复位，累计 3 次后 D-10 降级重试永久失效——CR-01 的 missing 中点名）。

Purpose: 把缓存命中路径从「先回源再查盘」翻转为「先查盘、命中不回源」——分片缓存才有省流量/秒开/断网可看的实际收益；同时堵住主进程崩溃级缺陷与降级重试失效。三者全在 main.js / media-cache-manager.js / src/player.js 集成热路径，纯逻辑模块与既有单测语义不动。
Output: handleProxyRequest 命中优先分支 + store() 源流 error 防护 + hlsRetryCount 复位 + 配套单测与源码顺序 gate。

**files_modified 说明（超出白名单的 1 项）**：`src/player.js` 属「禁止改动（除非必须）」的例外——WR-06 复位逻辑在 player.js hls 事件监听内，且被 CR-01 断网照播 truth 的 missing 清单点名要求一并修复，属必须；改动仅限 hls ERROR/LEVEL_LOADED 区块附近新增一个 FRAG_LOADED 监听，不触碰双模式判定/缓存模式逻辑。

**已知债务（不在本 plan）**：WR-01/02/03/04、IN-01/03/04 经判定与 5 个 failed truths 无直接关联，不纳入 gap-closure 范围。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-VERIFICATION.md
@.planning/phases/44-/44-REVIEW.md
@.planning/phases/44-/44-03-PLAN.md
@.planning/phases/44-/44-04-PLAN.md
@.planning/phases/44-/44-05-PLAN.md

@main.js
@media-cache-manager.js
@src/player.js
@tests/test-media-cache.js
</context>

<artifacts_this_phase_produces>
本计划新增符号/契约（集成层，无新模块）：
- main.js handleProxyRequest：新增 pre-fetch 命中分支（无新具名函数；局部 const `vidParam`/`vidOk`/`isM3u8Target`）；分片 miss 分支改为 tee-only（移除原分支内 lookup 调用）
- media-cache-manager.js：lookup() 命中结果新增 `contentType` 字段；storeBuffer 分片条目新增可选 `contentType` 字段；store() 内部新增源流 error 处理（内联）
- src/player.js：hls 分支新增 `Hls.Events.FRAG_LOADED` 监听（内联，复位 `state.hlsRetryCount = 0`）
- tests/test-media-cache.js：新增「store() 源流 error 不崩溃且 tee 收到 error」单测
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — handleProxyRequest 缓存命中先于回源（key=target，命中直返不回源）</name>
  <files>main.js</files>
  <read_first>
    - main.js 全文 §handleProxyRequest（当前 304-436 行，逐行读：cacheEnabled 判定 :313、target :314、headers/Range :345-350、ses.fetch :356、contentType/finalUrl/isM3u8 :358-360、m3u8 分支 :362-382、分片缓存分支 :384-410（内含 lookup(finalUrl) :390 与 store(finalUrl) :407）、透传分支 :412-428、catch 502 :429-435）
    - main.js:142-151（mediaCache/videoIdOf 模块级引用）与 :260-272（rewriteM3u8ForProxy 的 vid 继承：子请求 vid = 首请求 vid，segKey 与 updatePlaylistIndex 同源的前提）
    - media-cache-manager.js §lookup（255-286 行，命中 {hit,data,size}、D-09 校验删片）与 §storeBuffer 条目登记（399-410 行：{size, sha256, stored_at}）与 §updatePlaylistIndex（514-531 行：segKey = segmentKeyOf(resolveUri(seg.uri, m3u8Url))）
    - .planning/phases/44-/44-REVIEW.md CR-01（行号 51-74：命中丢弃 resp.body 的连接悬挂问题 + key 语义建议）与 44-VERIFICATION.md gaps 第 1/2 条
  </read_first>
  <action>
    重排 handleProxyRequest（main.js:304-436）的缓存查询与回源顺序，**cache key 语义（决策）**：统一用**请求 URL（target）**做 lookup 与 store 的 key，废弃重定向后 finalUrl 做 key 的做法——finalUrl 只有回源后才能得知，命中优先下无法用作查询 key；且 target 与 updatePlaylistIndex 登记的 playlist_order segKey（resolveUri(uri, m3u8最终URL)）在分片请求上同源（rewrite 输出的 /proxy?url= 即 resolveUri 结果），转封装顺序索引的一致性不回归。

    1. 在 headers/Range 组装之后、`try` 之前（当前 :345-350 之后）插入 pre-fetch 命中分支：
    - `const vidParam = reqUrl.searchParams.get('vid') || '';` 与 `const vidOk = /^[a-f0-9]{16}$/.test(vidParam);`
    - `const isM3u8Target = /\.m3u8(\?|$)/i.test(target);`（URL 层 m3u8 预判——m3u8 响应必须走下方 rewrite 清单分支，不得被命中优先截走；伪装 m3u8 的 URL 因 cache 中本无条目（旧实现只有 !isM3u8 才落盘）必然 miss，行为不变）
    - `if (cacheEnabled && mediaCache && vidOk && !req.headers.range && !isM3u8Target)` 时调 `mediaCache.lookup(target, vidParam)`；命中（`hit.hit`）则 `res.writeHead(200, { 'Content-Type': hit.contentType || 'application/octet-stream', 'Content-Length': hit.size, 'Cache-Control': 'no-store' })` + `res.end(hit.data)` + `return`——**不发起 ses.fetch**，命中失败（D-09 校验删片）自然落到下方 fetch 回源。
    2. 分片缓存分支（当前 :386-410）**移除其中的 lookup 二次查询**（命中已在上方处理），只保留 tee 未命中回源流：`res.writeHead(200, outHeaders)` 后改为 `const tee = mediaCache.store(target, vidParam, Readable.fromWeb(resp.body), { contentType: contentType || 'application/octet-stream' }); tee.on('error', () => { if (!res.writableEnded) res.end(); }); tee.pipe(res); return;`——store 的 key 改传 **target**（第 4 参 meta 里带 contentType，供 Task 2/缓存命中回放）。tee 挂 error 监听收尾响应（与 Task 2 的 store 内部 destroy(err) 联动：passthrough destroy(err) 触发本监听 → res.end()，播放器侧得到截断响应走自身重试）。
    3. m3u8 分支（:362-382）与透传分支（:412-428）保持现状不动（透传分支的源流 error 处理在 Task 2 补）。
    4. JSDoc/注释纪律：handleProxyRequest 头注释补一句「CR-01：命中优先，缓存 key = 请求 URL（target）」。**注释中不得出现字面串 `lookup(finalUrl` 或 `store(finalUrl`**（下方回归 gate 会以这两个字面串做不存在断言，注释命中会误判失败）。旧代码被替换删除，不要以注释形式残留。
    5. 迁移注记：旧实现以 finalUrl 为 key 落盘的存量分片条目本 plan 不做迁移——这些键不再被查询，成孤儿数据，由容量淘汰（44-07 CR-02 修复后生产生效）按 total_size 正常回收，接受此代价（缓存非持久资产）。
  </action>
  <verify>
    <automated>node --check main.js && ! grep -nE 'lookup\(finalUrl|store\(finalUrl' main.js && [ "$(grep -n 'mediaCache.lookup(target' main.js | head -1 | cut -d: -f1)" -lt "$(grep -n 'ses.fetch(target' main.js | head -1 | cut -d: -f1)" ] && node tests/test-media-cache.js</automated>
    <fails_when>node --check 语法错误非零退出；或 grep 仍匹配到 finalUrl 为 key 的 lookup/store 调用（! grep 失败，退出码 1）；或 mediaCache.lookup(target 的行号不小于 ses.fetch(target 的行号（test -lt 非零，含 grep 无匹配时的空值比较错误 exit 2）；或 test-media-cache.js 非零退出（fail ≥ 1）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：handleProxyRequest 函数体内 `mediaCache.lookup(target` 的声明行号 < `ses.fetch(target` 的行号（命中先于回源）
    - 源码断言：main.js 全文无 `lookup(finalUrl` / `store(finalUrl` 调用（key 统一为 target）
    - 行为断言：命中分支返回 200 + Content-Length=hit.size + Cache-Control no-store 且直接 return，不触达 ses.fetch；未命中才 tee 落盘；m3u8-likely（URL 含 .m3u8）与带 Range 请求被命中分支显式排除
    - 既有单测不回归：node tests/test-media-cache.js 全绿（本任务不改 manager，仅为 Task 2 预留）
  </acceptance_criteria>
  <done>缓存命中分片不再回源（lookup 在 fetch 前、无 finalUrl-key 调用残留）；m3u8/Range 不误入命中路径；既有缓存单测全绿。</done>
</task>

<task type="auto">
  <name>Task 2: CR-05 — store() 源流 error 监听 + main.js 透传分支同款防护 + 单测</name>
  <files>media-cache-manager.js, main.js, tests/test-media-cache.js</files>
  <read_first>
    - media-cache-manager.js §store()（324-354 行：两次 pipe :327-328、collector error :347-349、passthrough error :350-352——collector/passthrough 的监听救不了源流 readable，Node 流契约：source 'error' 无监听 → uncaught exception → 主进程退出）与 JSDoc（:313-323）
    - media-cache-manager.js lookup()（255-286 行：命中返回形状 {hit,data,size}，本任务扩展 contentType）与 storeBuffer 登记（:399-410 行：segments[segKey] = {size, sha256, stored_at}，追加 contentType 字段不升 META_VERSION——沿 44-05「附加字段向后兼容」先例）
    - main.js 透传分支（:412-428，重点 :424-425 的 `Readable.fromWeb(resp.body).pipe(res)`——Phase 27 遗留同款风险面）与 Task 1 新写的 tee 挂载点
    - .planning/phases/44-/44-REVIEW.md CR-05（行号 127-143：store 只给 collector/passthrough 挂了 error，救不了源流；main.js:425 同款遗留）
    - tests/test-media-cache.js（结构参照：makeCacheRoot/makeBuffer 助手、store tee 透传测试 :92-110 的轮询等索引写法）
  </read_first>
  <action>
    1. media-cache-manager.js store()（:324-354）：在两次 `readable.pipe(...)`（:327-328）**之前**给源流挂 error 监听——监听体做三件事：console.warn（文案形如「[Realm] 回源流中断（透传与落盘同步终止）: {err.message}」）、`passthrough.destroy(err)`（触发集成层 Task 1 已挂的 tee.on('error') → res.end() 收尾截断响应）、`collector.destroy()`。监听注册语句须以 `readable.on('error', (err) => {` 起始、含上述三个动作，且**必须出现在首个 `readable.pipe(` 之前**（pipe 不负责 source 的 error，监听挂晚会漏掉同步错误）。destroy(err) 的错误事件异步派发，Task 1 的 tee.on('error') 在同一 tick 同步挂载，无竞态。store() 的 JSDoc（:313-323）补一句调用契约：「本方法消费 readable 并在内部处理其 'error'（destroy 双 tee + warn），调用方无需再给源流挂监听」。
    2. 扩展 lookup 命中回放 contentType：segments[segKey] 条目若带 `contentType`（字符串且 ≤200 字符）则命中返回 `{ hit: true, data, size, contentType }`，否则不带该字段（由 main.js 命中分支回落 application/octet-stream——Task 1 已按 `hit.contentType || 'application/octet-stream'` 消费）。
    3. storeBuffer 登记条目（:405）追加 contentType：`const ct = m.contentType; if (typeof ct === 'string' && ct && ct.length <= 200) entry.contentType = ct;`（entry 为 {size, sha256, stored_at} 扩展；不升 META_VERSION，缺字段兼容，D-17/转封装读取路径不消费该字段不受影响）。
    4. main.js 透传分支（:424-425）：`Readable.fromWeb(resp.body)` 改为先 `.on('error', (err) => { console.warn('[Realm] 视频透传流中断:', err.message); if (!res.writableEnded) res.end(); })` 再 `.pipe(res)`（错误已处理不崩主进程；headers 已发，end 兜底收尾截断响应，客户端 fetch 报错走播放器自身重试）。
    5. tests/test-media-cache.js 新增 describe「store() 源流 error 健壮性（CR-05）」两条单测：
    - 源流 push 部分数据后 destroy(new Error('source reset')) → 测试进程不崩溃（监听生效），tee（返回值）收到 'error'（集成层 res.end 挂钩的等价物），且不出现 unhandled rejection/exception
    - storeBuffer 带 { contentType: 'video/mp2t' } 落盘 → lookup 命中返回 contentType === 'video/mp2t'（回放契约）；不传则命中结果无 contentType 字段（回落默认）
    写法参照 :92-110（Readable + tee 'data'/'end' 轮询）：造 error 源可用 `new Readable({ read() {} })` + 外部 `src.destroy(err)`，或 read() 内 push 一段后 destroy；断言等待用短轮询/setTimeout，**不要用裸 sleep 依赖时序**（可用既有 20ms 轮询模式）。
  </action>
  <verify>
    <automated>node --check media-cache-manager.js && node --check main.js && node tests/test-media-cache.js && node -e "require('./media-cache-manager'); console.log('pure-node ok')"</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 test-media-cache.js 非零退出（fail ≥ 1，含新增 CR-05 断言失败）；或 pure-node ok 未输出（manager 引入 electron 依赖）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：store() 内 `readable.on('error'` 出现在首个 `readable.pipe(` 之前（监听先于 pipe）
    - 源码断言：store() 的 error 处理同时 destroy passthrough 与 collector（透传与落盘同步终止）
    - 行为断言（单测）：源流 error 不抛未捕获异常、tee 收 'error'；contentType 落盘-回放闭环成立
    - 源码断言：main.js 透传分支 `Readable.fromWeb(resp.body)` 后紧跟 `.on('error'`（同款防护补齐）
    - 既有 15 条缓存单测 + 新增单测全绿；media-cache-manager 保持纯 Node 可加载
  </acceptance_criteria>
  <done>源流错误在三处消费路径（store tee 内部/集成层 tee 挂钩/纯透传分支）均有监听，不再有 uncaught exception 崩溃面；缓存命中回放 contentType 契约成立；单测覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: WR-06 — hlsRetryCount 成功拉到分片后复位（D-10 降级重试不永久失效）</name>
  <files>src/player.js</files>
  <read_first>
    - src/player.js:35-36（state.hlsRetryCount 定义与注释）、:160（initPlayer 换流时归零）、:185-203（Hls.Events.ERROR 处理：独立模式 + NETWORK_ERROR + 非直播时 `state.hlsRetryCount < 3` 递增重试，达到 3 后仅 showCacheFallbackBanner 不再 startLoad——WR-06：重试成功恢复后计数不复位，长会话累计 3 次瞬时错误后降级重试永久失效）、:206-208（既有 LEVEL_LOADED 监听——直播判定）
    - .planning/phases/44-/44-REVIEW.md WR-06（行号 177-181：监听 FRAG_LOADED/LEVEL_LOADED 成功事件将计数归零；「成功拉到数据即视为恢复」）与 44-VERIFICATION.md gaps 第 2 条 missing（「同时修复 WR-06」）
  </read_first>
  <action>
    在 hls 初始化分支（initPlayer case 'hls'）内、既有 LEVEL_LOADED 监听（:206-208）**旁**新增一个成功信号复位监听：`hls.on(Hls.Events.FRAG_LOADED, () => { state.hlsRetryCount = 0; });`——事件名用 `Hls.Events.FRAG_LOADED`（分片数据到达 = 最精确的「网络恢复」信号；LEVEL_LOADED 在直播刷新/清单级也触发，粒度不如 FRAG_LOADED，二者取一选 FRAG_LOADED），前一行写一行注释说明 WR-06 语义（成功拉到分片即复位，避免长会话累计 3 次瞬时错误后 D-10 降级重试永久失效）。
    <!-- planner-discipline-allow: hlsRetryCount = 0 -->（该字面是待写入 player.js 的复位赋值本身，且下方 verify 以 FRAG_LOADED 邻近行的正向计数 gate 断言其存在——按 #429 规则以 allow 注释豁免）
    - 只加这一处监听 + 该行内联复位；**不得改动** :185-203 ERROR 分支的重试逻辑（<3 递增 / ≥3 提示条语义保持），不得改动 :160 换流归零，不得改动模式判定（isIndependentMode/isWebviewMode）与 proxiedUrl 链路——CR-01 修复后缓存照播自动成立，player 无需其他变更
    - 保持字面 `state.hlsRetryCount = 0` 写法（下方 gate 按 FRAG_LOADED 邻近行计数；勿改写为等价形式）
    - 事件名用 `Hls.Events.FRAG_LOADED`（分片数据到达 = 最精确的「恢复」信号；LEVEL_LOADED 在直播刷新/清单级也触发，粒度不如 FRAG_LOADED，二者取一即可，选 FRAG_LOADED）
  </action>
  <verify>
    <automated>node --check src/player.js && [ "$(grep -A4 'Hls.Events.FRAG_LOADED' src/player.js | grep -c 'hlsRetryCount = 0')" -ge 1 ]</automated>
    <fails_when>node --check 语法错误非零退出；或 FRAG_LOADED 监听体附近（其后 4 行内）无 `hlsRetryCount = 0` 复位（计数为 0 → test -ge 1 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：hls 分支注册了 Hls.Events.FRAG_LOADED 监听且监听体内将 state.hlsRetryCount 置 0
    - 行为语义：瞬时网络错误 → startLoad 重试成功（FRAG_LOADED 到达）→ 计数归零，下次再遇错误仍可重试（不达 3 次即永久降级）
    - 回归保护：ERROR 分支重试阈值逻辑、双模式判定、续播/进度上报等未被触碰
  </acceptance_criteria>
  <done>FRAG_LOADED 到达即复位 hlsRetryCount；D-10 断网降级重试在同会话内可持续生效（配合 CR-01 从缓存恢复数据）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源站响应流 → 主进程 → 播放器/磁盘 | 不可信网络流进入本地消费路径（本 plan 防其崩溃面） |
| 磁盘缓存命中响应 → 播放器 | CR-01 后命中数据直接来自本机缓存目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G06-01 | DoS | 源流 error 无监听 → uncaught → 主进程崩溃（CR-05） | high | mitigate | store() pipe 前挂 readable.on('error') destroy 双 tee + main.js 透传分支同款监听；集成层 tee.on('error') end res 防悬挂；单测覆盖 error 源不崩 |
| T-44-G06-02 | Spoofing | 缓存命中内容类型伪造 | low | accept | contentType 仅作回放字段（落盘时记录、≤200 字符、非可信）；hls.js 分片消费不依赖 content-type 决定 demux 路径，最坏退化为 octet-stream 默认；路径/校验仍走 _safePath + D-09 哈希（T-44-01/02 不因本改动削弱） |
| T-44-G06-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包；改动仅限既有文件，不触发包合法性 gate |

无新增 high 威胁（源流 error 处理本为消 high 缺陷的修复；内容回放不打开 _safePath 之外的读路径）。
</threat_model>

<verification>
1. `node --check main.js && node --check media-cache-manager.js && node --check src/player.js` 全过
2. `node tests/test-media-cache.js`（15 既有 + 新增 CR-05 单测）全绿；`node tests/test-m3u8-playlist-parser.js` / `node tests/test-media-task-registry.js` / `node tests/test-media-remuxer.js` 回归不红
3. 源码顺序 gate：handleProxyRequest 内 lookup(target) 行号 < ses.fetch(target) 行号；main.js 无 finalUrl 为 key 的 lookup/store
4. 人工复测（VERIFICATION human_verification 第 1/3 项，CR-01 修复后执行）：真机重开已看视频已看部分立即起播、无分片回源；断网时已缓存分片继续播 + 降级提示条约 4s 消失
</verification>

<success_criteria>
- CR-01 truth 达成：缓存命中先于回源——「重开秒开不重复回源」（D-05）与「断网已缓存分片照播」（D-10）在生产路径成立
- CR-05 truth 达成：缓存 tee 与纯透传路径源流错误有监听，分片传输中断不再崩主进程
- WR-06 随 CR-01 修复：降级重试计数成功即复位，同一会话内多次瞬时错误均可恢复
- 既有 71 项单测 + 新增单测全绿；media-cache-manager 仍纯 Node 可加载；m3u8/Range/webview 透传路径零行为回归（D-01 不破坏）
</success_criteria>

<output>
Create .planning/phases/44-/44-06-SUMMARY.md when done
</output>
