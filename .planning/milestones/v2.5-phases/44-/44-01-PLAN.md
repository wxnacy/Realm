---
phase: 44-player-video-cache-and-local-media-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.js
  - ipc-handlers.js
  - src/player.js
  - src/player.css
  - src/preload.js
  - media-cache-manager.js
  - player-history-manager.js
  - tests/test-media-cache.js
autonomous: true
requirements: [D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
coupling_justified:
  - "44-02（同 wave 1）：Task 2 引用的 isVideoActive 仅是注入回调契约，本计划测试用 stub，无构建期依赖；真实接线在 44-03（wave 2，depends_on 已含两者）"
estimate:
  tokens: 95000
  raw_tokens: 65000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - 独立播放窗口从 localhost 加载 player 页（URL 带 token/cache=1/mode=independent），红绿灯标题栏保留、containerId 非空（D-02）
    - 带 cache=1 的分片流量落盘 `<缓存目录>/<视频ID>/segments/`，重开同一视频已看分片从磁盘命中（D-03/D-05）
    - webview tab 模式流量不带 cache 标记、行为与现状完全一致（D-01）
    - 重开视频 loadedmetadata 后 seek 到上次位置，匹配 key = origin+pathname，query 时效 token 不参与（D-12）
    - 播放进度每 5 秒 + 暂停/关窗时落盘到观看历史（D-13）
    - FIFO 淘汰按最后观看时间整目录删除且豁免活跃任务；分片校验失败删片回源播放不中断；磁盘满强淘后重试（D-07/D-08/D-09/D-10）
    - 断网/源站失效时已缓存分片照播，未缓存分片重试失败后显示降级提示条「部分分片加载失败，已缓存部分可继续观看」约 4s 自动消失、不打断播放（D-10/UI-SPEC Component 5）
    - 观看历史记录（player_history 表）独立于缓存条目，不随淘汰消失（D-11）
  artifacts:
    - media-cache-manager.js（去 Electron 化：MediaCacheManager 类，constructor 注入 cacheRoot/isVideoActive/capacityBytes）
    - player-history-manager.js（SQLite player_history 表，主键 playback_key upsert）
    - tests/test-media-cache.js
    - main.js handleProxyRequest 缓存分流钩子 + 'cache' 透传参数 + Cache-Control: no-store
    - ipc-handlers.js media:play loadURL localhost 化 + player:progress/player:drawer:list/player:cache:delete IPC
    - src/player.js mode=independent 模式判定 + 进度节流上报 + 续播 seek + D-10 降级提示条
    - src/player.css 降级提示条样式
    - src/preload.js playerAPI 新通道（reportProgress/getDrawerList/deleteCacheEntry）
  key_links:
    - player.js proxiedUrl(cache=1) → main.js handleProxyRequest 缓存分流 → media-cache-manager 读写盘
    - player.js reportProgress → ipc-handlers player:progress → player-history-manager upsert → 重开时 loadedmetadata seek
    - media-task-manager.isVideoActive(playbackKey)（44-02 契约）→ 44-03 集成注入 media-cache-manager 构造参数 → 淘汰豁免（D-07）
  prohibitions:
    - statement: webview tab 模式不做缓存，保持现状直连（D-01）
      status: locked
      verification: handleProxyRequest 缓存分支仅在 searchParams.get('cache')==='1' 时进入，webview 流量 URL 不含 cache 参数
    - statement: 缓存 key/目录名禁用原始 URL 拼路径（RESEARCH 反模式）
      status: locked
      verification: 视频ID/分片名均为 sha256 hex（^[a-f0-9]+$），grep media-cache-manager.js 无 path.join 直接拼接 URL 的调用
---

<objective>
端到端打通「独立窗口 → /proxy 分片缓存 → 观看历史续播」主链路：独立播放窗口 localhost 化（D-02），/proxy 层按视频组织的分片磁盘缓存（D-03/D-05~D-10），双层观看历史与精确续播（D-11~D-13）。

Purpose: 本阶段核心体验「重开秒开 + 接着看」的完整闭环；缓存目录结构与元数据格式在此定形（D-03 costly，后续改动需迁移）。
Output: media-cache-manager.js、player-history-manager.js、tests/test-media-cache.js 及 main.js/ipc-handlers.js/player.js/preload.js 集成改造。

**Specless 说明**：本阶段 REQUIREMENTS.md 无需求 ID（TBD），specless probe 跳过；must_haves 由 ROADMAP Phase 44 Goal + CONTEXT.md D-XX 决策 + RESEARCH.md 发现 + UI-SPEC 推导，requirements 字段以 D-XX 为覆盖骨架。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-/44-CONTEXT.md
@.planning/phases/44-/44-RESEARCH.md
@.planning/phases/44-/44-PATTERNS.md
@.planning/phases/44-/44-VALIDATION.md

@main.js
@ipc-handlers.js
@src/player.js
@src/preload.js
@history-manager.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-cache-manager.js`：`MediaCacheManager` 类（constructor 注入 `{ cacheRoot, capacityBytes, isVideoActive }`）、`playbackKey(rawUrl)`、`videoId(rawUrl)`、`lookup(finalUrl)`、`store(finalUrl, webStream, meta)`、`evictIfNeeded()`、`verifySegment()`、`listEntries()`
- `player-history-manager.js`：`player_history` 表（playback_key 主键）、`upsertProgress()`、`getByKey()`、`listRecent()`
- `tests/test-media-cache.js`
- main.js：handleProxyRequest 内缓存分流分支、`rewriteM3u8ForProxy` 透传白名单新增 `'cache'`、内部响应 `Cache-Control: no-store`
- ipc-handlers.js：`player:progress`、`player:drawer:list`、`player:cache:delete` 通道；media:play 的 `loadURL(localhost)` 链路与 `mode=independent` 参数
- src/player.js：`mode=independent` 模式分支、`cache=1` 附加、进度节流上报（5s + pause/关窗）、loadedmetadata 续播 seek
- src/preload.js：`playerAPI.reportProgress / getDrawerList / deleteCacheEntry`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端切片 — 独立窗口 localhost 化 + /proxy 缓存命中/落盘最小闭环</name>
  <files>main.js, ipc-handlers.js, src/player.js, src/preload.js, media-cache-manager.js</files>
  <read_first>
    - ipc-handlers.js:1996-2067（media:play 全链路：:2034 session partition、:2040 loadFile、:2043-2046 did-finish-load 后 media:play-url、:2049-2052 closed 清理）
    - main.js:234-265（rewriteM3u8ForProxy，:241 透传白名单）、main.js:277-364（handleProxyRequest：:279 token 校验、:319 Range 透传、:326 ses.fetch、:329 finalUrl、:330 isM3u8 判定、:332-338 m3u8 分支、:340-356 流式透传）、main.js:555-575（既有 m3u8 → localhost player URL 构建先例）、main.js:2466-2471（/player 路由已存在）、main.js:2507（realmPort/REALM_TOKEN 获取方式）
    - src/player.js:256-308（模式判定/paramUrl/webview-player 类/proxiedUrl）、src/player.js:580-700（refreshCurrent :627 resume 模式）
    - src/preload.js:1533-1565（playerAPI 块）
    - .planning/phases/44-/44-PATTERNS.md（main.js handleProxyRequest 缓存插入点标注、download-manager 路径安全段）
    - .planning/phases/44-/44-RESEARCH.md（Pitfall 1/2/3/4/7、Pattern 1、Open Question 3）
  </read_first>
  <action>
  按 D-02（per D-02 决策）把独立播放窗口从 file:// 收口到 localhost：
  1. ipc-handlers.js media:play 的 loadFile（:2040）改为 loadURL，URL 形如 `http://localhost:${realmPort}/player/?url=<encodeURIComponent(url)>&token=${REALM_TOKEN}&container=${containerId}&referer=<encodeURIComponent(referer||'')>&cache=1&mode=independent`（realmPort/REALM_TOKEN 获取参照 main.js:2507 既有导出方式；mediaList 仍走 did-finish-load 后 `media:play-url` IPC 传递——RESEARCH Open Question 1 推荐方案）。
  2. main.js rewriteM3u8ForProxy 透传白名单（:241）由 `['token','container','referer']` 加入 `'cache'`（RESEARCH Open Question 3），使分片/密钥/子清单请求自动继承 cache 标记。
  3. src/player.js 模式判定（:256-308）引入第三形态：`urlParams.get('mode') === 'independent'` 时进入独立窗口 localhost 模式——不加 `webview-player` 类（保留标题栏与红绿灯，Pitfall 1），token/container/referer/cache 从 URL 参数取，仍经 `playerAPI.onPlayUrl` 接收 mediaList，proxiedUrl 在既有转发参数（:271-280）基础上附加 `cache=1`；既有 webview tab 分支（paramUrl && !mode）行为一字不改（D-01）。
  4. 新建 media-cache-manager.js（去 Electron 化，顶层无 electron/原生 require）：`MediaCacheManager` 类 constructor 注入 `{ cacheRoot, capacityBytes, isVideoActive }`；`playbackKey(rawUrl)` 返回 origin+pathname（D-12）；`videoId` = sha256(playbackKey) hex 前 16 位；分片名 = sha256(分片URL) hex；`store()` 把分片写进 `<cacheRoot>/<videoId>/segments/`，元数据 JSON 存 `<cacheRoot>/<videoId>/meta.json`（D-05 字段：m3u8 地址、标题、分片索引映射、总大小、最后播放进度、最后观看时间）；索引记**实际落盘字节数**（写完后 stat，Pitfall 4，不信任上游 Content-Length）。
  5. main.js handleProxyRequest 在 m3u8 分支（:332-338）之后、流式透传分支（:340）之前插入缓存分流钩子：`cacheEnabled = reqUrl.searchParams.get('cache') === '1'`；仅 `cacheEnabled && resp.ok && !isM3u8 && !req.headers.range` 进入缓存分支（Pitfall 3：Range 请求永远透传不落盘）；缓存 key 用重定向后 `finalUrl`（:329）；命中 → 读盘响应（Content-Length 用实际字节数）；未命中 → `Readable.fromWeb(resp.body)` tee：一边 pipe 给 res 一边写盘（Pattern 1）。m3u8 文本不含 `EXT-X-ENDLIST`（live）时不做任何缓存（Pitfall 2；重写逻辑照旧每次现做）。/proxy 与内部页响应统一加 `Cache-Control: no-store`（Pitfall 7 双缓存）。
  本任务只做最小可用的写盘/读盘/索引闭环；淘汰、哈希校验、磁盘满重试由 Task 2 补全。真实语义（毫秒级命中/画质）由 UAT 手工验证，本任务 verify 保证结构与 key 语义正确。
  </action>
  <verify>
    <automated>node --check main.js && node --check media-cache-manager.js && node --check ipc-handlers.js && node --check src/player.js && node --check src/preload.js && node -e "const m=require('./media-cache-manager'); const c=new m.MediaCacheManager({cacheRoot:'/tmp/realm-cache-smoke'}); if(c.videoId('https://a/x.m3u8?t=1')!==c.videoId('https://a/x.m3u8?t=2')) process.exit(1); if(!/^[a-f0-9]{16}$/.test(c.videoId('https://a/x.m3u8'))) process.exit(2); console.log('smoke ok')"</automated>
    <fails_when>任一 node --check 输出 SyntaxError 且退出码非零；或 smoke 脚本退出码 1（query 时效 token 参与了 key）或 2（videoId 非 16 位 hex）</fails_when>
  </verify>
  <done>独立窗口从 localhost 加载 player 页且带 mode=independent/cache=1；带 cache 标记的分片首次经 /proxy 回源并落盘、二次命中读盘；webview tab 流量无 cache 标记不进缓存分支；live m3u8 不落盘；videoId 对 query 时效 token 不敏感。</done>
  <reversibility rating="costly">缓存目录结构（视频ID 哈希目录 + segments/ + meta.json）与元数据格式在此定形落盘，后续改动需迁移已有缓存（D-03）；实现前按 D-05 字段清单定稿 schema。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 缓存完整化 — FIFO 淘汰/哈希校验/磁盘满强淘 + tests/test-media-cache.js</name>
  <files>media-cache-manager.js, tests/test-media-cache.js</files>
  <read_first>
    - media-cache-manager.js（Task 1 产出，先读当前实现再扩展）
    - history-manager.js:99-117（FIFO 淘汰结构：ORDER BY + LIMIT 删除，套用为 last_watched 排序 + 整目录删除）
    - download-manager.js:616-633（删除前 path.resolve + 根目录前缀校验的安全写法）
    - agent-workspace.js（resolveInside 双基准 + 已存在路径 realpath 复核先例，缓存根 symlink 防逃逸参照）
    - tests/test-ai-bash-policy.js（node:test + describe/test 纯逻辑测试结构）
    - .planning/phases/44-/44-VALIDATION.md（Wave 0 对 tests/test-media-cache.js 的覆盖要求）
  </read_first>
  <behavior>
    - playbackKey('https://a/x.m3u8?t=abc') === playbackKey('https://a/x.m3u8?t=xyz')（D-12：query 不参与）
    - 超容量时按 last_watched 升序淘汰整视频目录；isVideoActive(playbackKey)===true 的视频不被淘汰（D-07；回调按 playbackKey 匹配，与 44-02 的逐视频查询契约一致）
    - 写盘 ENOSPC → 触发一轮强制淘汰（豁免仍生效）→ 重试成功；豁免后仍放不下 → 返回失败并带原因（D-08）
    - 读取时哈希/大小校验失败 → 删除该分片文件 → 返回 miss（调用方回源），播放不中断（D-09）
    - 增量续存：已登记分片再次 store 时跳过不重写（D-10）
    - videoId/分片名均匹配 /^[a-f0-9]+$/；任何读写 resolve 后必须位于 cacheRoot 内，缓存根内 symlink 指向外部的路径被拒绝
  </behavior>
  <action>
  1. `evictIfNeeded()`：按 meta.json 的 last_watched 升序遍历，整目录删除（fs.rmSync recursive）直到总大小 ≤ capacityBytes（默认 10GB，D-06）；每删一个前查注入的 `isVideoActive(playbackKey)`，活跃任务豁免（D-07）——media-cache-manager **不得直接 require media-task-manager**，解耦靠注入回调；回调契约（逐 playbackKey 查询）由 44-02 定义、44-03 集成时把 `mediaTaskManager.isVideoActive` 接进构造参数，本计划测试用注入 stub 即可，不构成对 44-02 的构建期依赖。
  2. `store()` 写盘失败（ENOSPC/EDQUOT）→ 先 `evictIfNeeded()` 强制一轮（豁免规则仍生效）再重试一次；仍失败 → 返回 `{ ok:false, reason:'disk_full' }`，调用方在任务/播放侧呈现「缓存写入失败：磁盘空间不足」（D-08）。
  3. 写盘完成后计算 sha256 与实际字节数登记进 meta.json 分片索引（D-09）；`lookup()` 读盘前校验大小+哈希，失败即删该分片文件并返回 miss。
  4. 路径安全：所有读写经 path.resolve 后必须以 cacheRoot 为前缀；已存在路径 realpath 复核仍在 cacheRoot 内（参照 agent-workspace resolveInside，防 symlink 二段式逃逸）；删除目录同样过此校验（download-manager.js:616 模式）。
  5. 测试文件照 tests/test-ai-bash-policy.js 结构，constructor 注入临时目录（fs.mkdtemp），用 node:crypto 现场造小分片文件——不 require electron，不触碰真实网络。
  </action>
  <verify>
    <automated>node tests/test-media-cache.js</automated>
    <fails_when>非零退出码，或 node:test 汇总出现 "fail 1" 及以上，或 "pass 0"</fails_when>
  </verify>
  <done>tests/test-media-cache.js 全绿（D-05~D-10、D-12 各至少一条断言）；淘汰豁免、强淘重试、校验删片回源、续播 key、路径安全行为均有断言覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: 观看历史 + 精确续播 + 进度节流上报 + 抽屉数据后端</name>
  <files>player-history-manager.js, ipc-handlers.js, src/player.js, src/player.css, src/preload.js, main.js</files>
  <read_first>
    - history-manager.js:15-95（延迟初始化骨架、表创建、sanitize 惯例——player-history-manager 整套照抄，差异点见 44-PATTERNS.md「player-history-manager.js」节）
    - download-manager.js:213-236（INSERT OR REPLACE upsert 写法）
    - ipc-handlers.js:2085（assertPlayerSender 用法）、ipc-handlers.js:2049-2052（playerWindow 现有 closed 清理——close 拦截改造点）
    - src/player.js:580-700（refreshCurrent :627、initPlayer 引擎分支 :115）
    - .planning/phases/44-/44-RESEARCH.md（D-11/D-12/D-13、Pitfall 6、进度节流 Code Example）
  </read_first>
  <action>
  1. 新建 player-history-manager.js（SQLite，history.db 加表或独立小库均可——RESEARCH Open Question 2 推荐）：表 `player_history`（playback_key TEXT PRIMARY KEY, url TEXT, title TEXT, last_position REAL, duration REAL, last_watched INTEGER），写入走 `INSERT ... ON CONFLICT(playback_key) DO UPDATE`（D-11）；无 FIFO 淘汰；better-sqlite3 延迟初始化（app.whenReady 后由 main.js init，顶层无原生 require）。
  2. ipc-handlers.js 新增（均 assertPlayerSender）：`player:progress`（ipcRenderer.send 单向，{ url, title, position, duration } → 主进程算 playbackKey 后 upsert，同时刷新 media-cache-manager 元数据的 last_position/last_watched——双层同步 D-11/D-13）；`player:drawer:list`（合并缓存库条目 + 观看历史，按 last_watched 降序，条目含 { title, url原值, playbackKey, cacheSize, completeness, lastPosition, lastWatched }——D-14/D-15 数据形状）；`player:cache:delete`（按 videoId 整目录删缓存条目，cacheRoot 边界校验，观看历史记录保留——D-16 后端 + 删除确认文案「观看历史保留」语义）。
  3. src/preload.js playerAPI 增加：`reportProgress(data)`（ipcRenderer.send）、`getDrawerList()`、`deleteCacheEntry(videoId)`（invoke）。
  4. src/player.js：① 进度节流——`setInterval 5000ms` 内 video 非暂停且 state.currentUrl 存在时 reportProgress；`pause` 事件即报；② 续播——initPlayer 成功路径上按 playbackKey 查历史，last_position > 0 且非直播时挂 `loadedmetadata` once seek（复用 :627 refreshCurrent 的 resume 模式，D-12）；③ 关窗兜底——playerWindow close 拦截（改 :2049 closed 为 close + closed 两段）：close 时向 renderer 同步要一次最终进度（超时 500ms 兜底放行，Pitfall 6 防窗口销毁后 async IPC 丢失）；④ **D-10 降级提示条**——hls.js ERROR 事件为 fatal network error（断网/源站失效）且非直播时：已缓存分片照常命中播放（既有缓存读盘链路天然生效），对未缓存分片重试失败后经 player 顶部提示条显示「部分分片加载失败，已缓存部分可继续观看」（UI-SPEC Component 5 文案），约 4s 自动消失、不打断播放；样式走 src/player.css 新增提示条类（--player- token、player-text-secondary 14px）。
  5. main.js 在 whenReady 初始化 player-history-manager 并注入 ipc-handlers（经 module.exports 参数或既有组装惯例）。
  </action>
  <verify>
    <automated>node --check player-history-manager.js && node --check ipc-handlers.js && node --check src/player.js && node --check src/preload.js && node --check main.js && grep -n "player:progress" ipc-handlers.js && grep -n "reportProgress" src/preload.js && grep -n "已缓存部分可继续观看" src/player.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或任一 grep 无匹配行退出码 1（player:progress 通道、reportProgress 桥或 D-10 降级提示条文案未落地）</fails_when>
  </verify>
  <done>进度 5s/暂停/关窗三路落盘 player_history；重开视频 seek 到上次位置且 key 对 query token 不敏感；断网/源站失效时已缓存分片照播并显示「已缓存部分可继续观看」降级提示条（D-10）；抽屉数据接口返回双层合并列表；删除缓存条目保留观看历史。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端媒体源 → /proxy → 磁盘缓存 | 不可信远端内容经容器 session 回源后落盘本机 |
| localhost HTTP → realmServer | 本机任意进程可扫端口访问 /proxy（既有 token 防「开放代理」） |
| 磁盘缓存目录 ↔ 文件系统 | 缓存 key/路径处理不当即路径穿越/symlink 逃逸写盘面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Tampering | 缓存目录/分片文件名 | high | mitigate | 视频 ID 与分片名一律 sha256 hex（禁原始 URL 入路径）；读写 resolve 后必须位于 cacheRoot 内 |
| T-44-02 | Elevation | 缓存根 symlink 逃逸 | high | mitigate | 已存在路径 realpath 复核仍在 cacheRoot 内（agent-workspace resolveInside 先例） |
| T-44-03 | DoS | 恶意超大分片/清单撑爆磁盘 | medium | mitigate | D-06 容量上限 + D-08 强淘重试；单分片写盘走统一 store 入口便于加大小上限 |
| T-44-04 | Spoofing | 未持 token 调用 /proxy 缓存分支 | high | mitigate | 缓存分支在 handleProxyRequest 既有 REALM_TOKEN 校验（main.js:279）之后，不新增任何无鉴权入口 |
| T-44-SC | Tampering | npm 安装（本计划无新增依赖） | high | mitigate | 本计划零新依赖；Phase 44 唯一新依赖 mux.js 的 legitimacy 审计在 44-05（verdict OK，见 RESEARCH 审计表） |
</threat_model>

<verification>
- `node tests/test-media-cache.js` 全绿（D-05~D-10、D-12）
- `node --check main.js ipc-handlers.js src/player.js src/preload.js media-cache-manager.js player-history-manager.js` 逐个通过
- 手工（UAT）：重开已看视频秒开且接续上次位置；webview tab 播放行为不变
</verification>

<success_criteria>
- 独立播放窗口 localhost 化完成且标题栏/容器信息不丢失（Pitfall 1 无症状）
- 带 cache=1 分片流量落盘 `<缓存根>/<视频ID>/segments/`，二次请求磁盘命中
- 续播 key = origin+pathname；进度 5s/暂停/关窗三路落盘
- 淘汰/强淘/校验删片行为与 D-07/D-08/D-09/D-10 一致且有单测覆盖
- webview tab 模式零行为变化（D-01）
</success_criteria>

<output>
Create `.planning/phases/44-/44-01-SUMMARY.md` when done
</output>
