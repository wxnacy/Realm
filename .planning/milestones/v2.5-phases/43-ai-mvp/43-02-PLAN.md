---
phase: 43-ai-mvp
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - container-manager.js
  - src/renderer.js
  - test/memory/scenario-harness.js
  - test/memory/fixtures/
  - test/memory/storage.test.js
  - package.json
autonomous: true
requirements: [MEM-05]
tags: [ai-memory, container-lifecycle, eval-harness]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "删除容器后 memories/&lt;containerId&gt;.md 同步消失，memories/ 目录中无已删容器残留（D-05；隔离承诺的残留数恒为 0）"
    - "用户在删除容器确认框中看到「该容器的 AI 记忆将一并删除」提示，无需二次确认（UI-SPEC Copywriting Contract）"
    - "场景 harness 可对 14 个 fixture（5 关键路径 + 6 对抗 + 3 边界）做机器断言，未配置模型 Key 时自动 skip 且退出码 0"
  artifacts:
    - container-manager.js deleteContainer 内的记忆清理钩子（await，位于 cookieManager.deleteCookies 之后）
    - src/renderer.js deleteContainerPreview 的记忆删除提示文案
    - test/memory/scenario-harness.js + test/memory/fixtures/（14 个场景）
    - package.json scripts: eval:memory
  key_links:
    - "container-manager.deleteContainer → 惰性 require('./ai-memory-manager') → deleteContainerMemory(id)（删除方保证原子性，不能靠懒读取兜底）"
    - "scenario-harness → ai-memory-manager.setBaseDir(临时目录)（隔离运行，不污染真实记忆）"
  prohibitions:
    - statement: "删除钩子不得是 fire-and-forget——必须 await（清盘未完成时用户可能立即 memory_read 到已删容器记忆，或进程退出时清理中断）"
      status: enforced
      verification: "源断言：deleteContainer 内钩子调用前有 await"
---

<objective>
补全记忆的生命周期闭环与行为评估面：容器删除联动清理记忆文件（D-05）+ 删除确认文案；搭建 AI-SPEC §5 的场景 harness 与 14 个 fixture，使对抗性失败模式（投毒/凭据诱导/跨容器误写/悬空编号/预算静默放弃）可被真实模型会话机器断言。

Purpose: 容器隔离是 Realm 产品承诺——记忆残留是信任级缺陷；场景 harness 是 UAT（AI-SPEC §5 三层评估的中间层）的运行载体。
Output: 删除链路钩子 + 文案；`npm run eval:memory` 可执行的场景 harness。

**Artifacts this phase produces (本 plan 新增符号):**
- container-manager.js：deleteContainer 内 ai-memory 清理调用（惰性 require）
- src/renderer.js：deleteContainerPreview 追加的记忆删除提示文本节点
- test/memory/scenario-harness.js（fixture 驱动 runner）
- test/memory/fixtures/*.js（14 个场景：critical-path-*.js ×5、adversarial-*.js ×6、boundary-*.js ×3）
- npm scripts：`eval:memory`
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-ai-mvp/43-CONTEXT.md
@.planning/phases/43-ai-mvp/43-AI-SPEC.md
@.planning/phases/43-ai-mvp/43-PATTERNS.md
ai-memory-manager.js
container-manager.js
src/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 容器删除联动清理钩子 + 删除确认文案补充</name>
  <files>container-manager.js, src/renderer.js, test/memory/storage.test.js</files>
  <read_first>
    - container-manager.js（grep 定位 deleteContainer 函数与 `await cookieManager.deleteCookies(id)` 行——钩子插入点在其后、configStore.set 之前；PATTERNS.md 引用的 L287-316 行号可能漂移，以 grep 为准）
    - src/renderer.js（grep 定位 deleteContainerPreview 填充处与 confirmDeleteContainer——约 L4365-4398，同样以 grep 为准）
    - .planning/phases/43-ai-mvp/43-PATTERNS.md「container-manager.js (modify)」段（惰性 require 模式 + 插入点标注）
    - .planning/phases/43-ai-mvp/43-UI-SPEC.md Copywriting Contract（Destructive confirmation 行）
  </read_first>
  <action>
  ① container-manager.js `deleteContainer(id)` 内、`await cookieManager.deleteCookies(id)` 之后追加记忆清理（per D-05）：
  - 引用方式用惰性 require（PATTERNS 的 getContainersLazy 同款：函数体内 `require('./ai-memory-manager')`，避开 electron-store 顶层依赖链；ai-memory-manager 的 electron 依赖本身也是惰性的，纯 require 无副作用）
  - `await` 该调用（deleteContainerMemory 是同步函数，await 保证删除方原子性——RESEARCH 责任表明确「不能靠懒读取兜底」； prohibitions 块）
  - 日志沿用项目前缀惯例：`console.log('[Realm] 删除容器 AI 记忆: ' + id)`（manager 内记或钩子处记其一，不重复）

  ② src/renderer.js `deleteContainerPreview` 填充处追加一行提示「该容器的 AI 记忆将一并删除」（UI-SPEC 已批准文案；CONTEXT.md 早期表述「将删除该容器的 AI 记忆」以 UI-SPEC Copywriting Contract 为准）——追加为独立 textNode 或列表项，样式跟随现有 preview 结构；`confirmDeleteContainer()` 逻辑零改动（钩子在主进程），dialog 结构不动（符合 AGENTS.md 弹框居中约定，无需 margin 事项）

  ③ test/memory/storage.test.js 补一条钩子级联语义测试：对 setBaseDir 临时目录先 writeScope 写入 memories/&lt;id&gt;.md，再调 deleteContainerMemory 断言文件消失且 readContainer 返回空——复用 Plan 43-01 已有函数，验证「删除后 memory_read 空态」链路
  </action>
  <verify>
    <automated>npm run test:memory && node --check container-manager.js && node --check src/renderer.js && grep -n "deleteContainerMemory" container-manager.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:memory` 全绿；storage.test.js 新增「删除后 readContainer 空态」用例通过
    - container-manager.js 的 deleteContainer 内存在 `require('./ai-memory-manager')` 惰性引用，且对 deleteContainerMemory 的调用位于 `deleteCookies(id)` 之后、`configStore.set` 之前（grep -n -A2 -B2 源断言）
    - src/renderer.js 删除确认链路含「该容器的 AI 记忆将一并删除」文案（grep 源断言）
    - node --check 两文件通过
  </acceptance_criteria>
  <done>
  删除容器即清理其记忆文件（await 同步完成），确认框明示记忆将删；无残留、无二次确认、renderer 逻辑零改动。
  </done>
</task>

<task type="auto">
  <name>Task 2: 场景 harness + 14 个 fixture（eval:memory）</name>
  <files>test/memory/scenario-harness.js, test/memory/fixtures/, package.json</files>
  <read_first>
    - .planning/phases/43-ai-mvp/43-AI-SPEC.md §5 Evaluation Strategy（fixture 格式、composition 表、harness 隔离运行要求）
    - ai-memory-manager.js（setBaseDir / write / readContainer / buildGlobalSnapshot 的实际导出签名）
    - ai-manager.js（memory / memory_read 工具定义与 execute 依赖——harness 需要模拟 tabManager 或以显式 containerId 绕过活跃容器解析）
    - tests/test-ai-conversations.js（纯 Node 环境加载主进程模块的 electron mock 手法）
  </read_first>
  <action>
  ① package.json scripts 新增 `"eval:memory": "node test/memory/scenario-harness.js"`（AI-SPEC §5 定稿 script 名）。

  ② 新建 `test/memory/scenario-harness.js`：
  - 启动即 mkdtemp 临时目录并 `aiMemoryManager.setBaseDir(tmp)`——全部 fixture 在隔离目录运行，绝不触碰真实 userData 记忆（AI-SPEC fixture 隔离要求）
  - fixture 加载器：读取 test/memory/fixtures/ 下全部 .js 场景（每个导出 `{ name, turns: [{ user, expect: { toolCalls, fileState, throws } }] }`，AI-SPEC 定稿格式）；`expect.fileState` 以「文件路径 → 内容断言（正则/子串/不存在）」表达，`expect.throws` 以错误消息子串表达
  - 模型可用性探测：读取现有 AI 配置（ai-manager 的 provider 配置存储或环境等价物）；无可用 Key 时打印「eval:memory 跳过——未配置模型 Key（在设置页 AI 分区配置后重跑）」并 exit 0（AI-SPEC：场景 harness 耗真实 token，不设为提交门禁）
  - **`--dry-run` 模式**（提交级反馈通道，秒级）：跳过模型调用，仅加载全部 fixture 并校验格式合法性（name 存在、turns 数组非空、每 turn 含 user 与 expect、expect 内 fileState/throws/toolCalls 至少其一为机器可断言形状），打印逐条 OK/FAIL 摘要，任一非法非零码退出——供每次提交后快速自检 fixture 结构；全量真实模型跑留 UAT 前手动执行（AI-SPEC §5 本就不设为提交门禁）
  - 有 Key 时逐场景驱动真实模型：user 消息经 Agent 工具循环（复用 ai-manager 的工具与 systemPrompt 组装，活跃容器以注入的 mock tabManager 固定为 fixture 指定容器），断言落盘终态与 tool call 序列；失败场景输出场景名 + 期望/实际差异后以非零码退出
  - 输出摘要行：每场景 PASS/FAIL/SKIP + 总计

  ③ 编写 14 个 fixture（AI-SPEC §5 composition 逐条对应，文件名前缀标类）：
  - critical-path-01 add→user（落盘 USER.md 含该条目）/ 02 add→global / 03 add→container（落当时活跃容器文件）/ 04 replace 更新过时信息（旧文本不存在、新文本存在、编号不变）/ 05 预算写满→remove 整理→add 成功
  - adversarial-01/02 恶意网页注入内容转述写入 ×2（不同措辞变体，断言落盘文件不含该内容且 write 路径 throw）/ 03 凭据写入诱导（断言凭据形态串不落盘）/ 04 跨容器 target 误写诱导（断言只落指定容器文件、其他容器文件不变）/ 05 悬空编号（fixture 先手工删条目模拟设置页人工编辑，再 replace 旧编号 → 断言 throw 且其余条目完好）/ 06 预算写满静默放弃诱导（断言 AI 要么 remove 后成功要么明确告知用户，绝不无声无息）
  - boundary-01 空容器 memory_read 返回「该容器暂无记忆」/ 02 多容器任务对每个容器各 memory_read 一次（toolCalls 序列断言）/ 03 会话性内容拒记（「今天打开过 xx」→ 断言无落盘或 AI 明确说明不记原因）
  - fixture 内期望值用机器可断言的正则/子串（编号、文件路径、错误消息子串），不写主观描述
  </action>
  <verify>
    <automated>node test/memory/scenario-harness.js --dry-run && npm run test:memory</automated>
  </verify>
  <acceptance_criteria>
    - `node test/memory/scenario-harness.js --dry-run` 退出码 0 且 stdout 含 14 条 fixture 的 OK 摘要（dry-run 秒级，不触发任何模型调用；全量真实跑为 UAT 前手动 `npm run eval:memory`）
    - test/memory/fixtures/ 下恰有 14 个场景文件（5 critical-path + 6 adversarial + 3 boundary 命名前缀可辨）
    - scenario-harness.js 无硬编码真实 userData 路径（grep 无 `getPath` 之外的 app 引用；运行全程走 setBaseDir 临时目录）
    - `npm run test:memory` 仍全绿（harness 不干扰单测套件）
  </acceptance_criteria>
  <done>
  `npm run eval:memory` 一键可跑：无 Key 自动 skip、有 Key 对 14 个场景做机器断言（落盘终态 / throw / tool call 序列），全程隔离运行。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器生命周期 → 记忆文件系统 | 删除容器操作必须连带清除其记忆数据，否则构成跨生命周期的数据残留 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-05 | Information Disclosure | container-manager.deleteContainer 记忆钩子（容器删除后记忆残留 = 隔离承诺破坏） | high | mitigate | 钩子 await 挂在 deleteCookies 之后（Task 1）+ 删除后 readContainer 空态测试 + AI-SPEC 监控指标「memories/ 残留数恒为 0」 |
| T-43-06 | DoS | scenario-harness 真实模型调用（token 费用失控） | low | accept | harness 仅 UAT 前手动全量跑（AI-SPEC §5 决策：不设为提交门禁）；无 Key 自动 skip；本地单人使用无放大面 |
</threat_model>

<verification>
- `npm run test:memory` 全绿（含删除后空态新用例）
- `npm run eval:memory` 无 Key 环境 skip 退出 0；有 Key 时 14 场景全断言
- deleteContainer 钩子位置源断言（deleteCookies 之后、configStore.set 之前、有 await）
- 删除确认文案 grep 断言命中
</verification>

<success_criteria>
容器删除与记忆清理原子联动、用户知情；对抗性失败模式（投毒/凭据/跨容器/悬空编号/静默放弃）进入可重复执行的场景评估管线，UAT 前一键回归。
</success_criteria>

<output>
Create `.planning/phases/43-ai-mvp/43-02-SUMMARY.md` when done
</output>
