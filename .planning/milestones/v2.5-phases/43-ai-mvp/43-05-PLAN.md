---
phase: 43-ai-mvp
plan: 05
type: execute
$SCHEMA: plan-gap-closure
wave: 1
depends_on: []
gap_closure: true
gaps: [G-43-2]
files_modified:
  - src/settings-page.js
autonomous: true
requirements: [MEM-06]
estimate:
  tokens: 16000
  raw_tokens: 11000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "保存成功后「已保存」success 提示可见约 2 秒（UI-SPEC success 契约：--success-color 文字、2 秒后消失、不弹框），不再被 finally 的字数刷新同步覆盖（G-43-2）"
    - "success 提示存活期内用户继续编辑使「已保存」失效（输入即清除守卫），切 tab/取数提示不受守卫抑制"
    - "字数统计、超限变红、保存按钮 disabled 的既有行为全部不变；超限提示优先级高于成功提示"
  artifacts:
    - path: src/settings-page.js
      provides: aiMemoryState.successHintActive 守卫字段 + saveAiMemory/updateAiMemoryCount/input 监听三处联动
  key_links:
    - "saveAiMemory finally → updateAiMemoryCount → resetAiMemoryHint：原 0 帧覆盖链路被守卫短路（超限时守卫让位于 danger 超限提示）"
  prohibitions:
    - "守卫不得抑制超限 danger 提示——保存期间/之后内容超限时必须立即显示超限 hint 并恢复保存禁用（超限优先于成功提示）"
---

<objective>
闭合 43-UAT.md 的 G-43-2（minor）：realm://settings AI 记忆分区保存成功后「已保存」success 提示从未可见——saveAiMemory 成功分支 setAiMemoryHint('已保存','success') 后，finally 块同步调用 updateAiMemoryCount() → 无超限时 resetAiMemoryHint() 把 hint 立即覆盖为生效时机文案，「已保存」存续 0 帧（UAT 100ms 轮询 4 秒无一帧命中），UI-SPEC 的 success 反馈契约失效。

Purpose: 保存成功反馈是用户确认「写盘已完成」的唯一即时信号（D-12 显式保存交互的关键闭环）。
Output: src/settings-page.js 成功提示存活期守卫 + UI 自动化帧轮询验证记录。

**Flagged assumptions（specless probe，D-06）**：本 plan 只闭合 G-43-2；MEM-06 其余 truths（端点/分区渲染/tab 切换/超限阻断等 17 项断言）已由原 43-03 与 UAT Test 3 通过项覆盖，不重提。

**Artifacts this phase produces（本 plan）**：无新文件；新符号：`aiMemoryState.successHintActive`（布尔守卫字段）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-ai-mvp/43-UAT.md
@.planning/phases/43-ai-mvp/43-UI-SPEC.md
@.planning/phases/43-ai-mvp/43-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 成功提示存活期守卫（G-43-2 源级修复）</name>
  <files>src/settings-page.js</files>
  <read_first>
    - src/settings-page.js:3525-3535 —— aiMemoryState 字段定义（新增 successHintActive 的位置）
    - src/settings-page.js:3569-3589 —— setAiMemoryHint / resetAiMemoryHint（守卫的作用对象）
    - src/settings-page.js:3594-3617 —— updateAiMemoryCount（finally 覆盖链路的源头，守卫插入点：hint 联动段 L3608-3616）
    - src/settings-page.js:3717-3759 —— saveAiMemory（成功分支 L3743-3750 置位、finally L3754-3758 触发覆盖）
    - src/settings-page.js:3778-3781 —— textarea input 监听（守卫清除点）
    - .planning/phases/43-ai-mvp/43-UI-SPEC.md —— Copywriting Contract「保存成功：『已保存』（状态行内 --success-color 文字，2 秒后消失）」
    - .planning/phases/43-ai-mvp/43-UAT.md —— G-43-2 root_cause 与 missing 的两个修复方向（本任务取「守卫」方向，不动 finally 结构）
  </read_first>
  <action>
    在 src/settings-page.js 实现成功提示存活期守卫，四处改动：

    1. **aiMemoryState 加字段**（约 L3533 busy 字段旁）：新增 `successHintActive: false`，JSDoc 注释说明语义——「已保存」success 提示存活期内为 true，期间 updateAiMemoryCount 不覆盖 hint。
    2. **saveAiMemory 成功分支**（L3744 附近）：setAiMemoryHint('已保存', 'success') 之后置 aiMemoryState.successHintActive = true；既有 2 秒 setTimeout 回调内（hint.textContent 判断保留）在 resetAiMemoryHint() 的同时置 aiMemoryState.successHintActive = false（含 hint 已被别处覆盖、跳过 reset 的分支也要清守卫——直接放在回调首行清守卫最简）。
    3. **updateAiMemoryCount 守卫**（hint 联动段，L3608-3616）：在既有 noContainer 早退之后——若 aiMemoryState.successHintActive 为 true 且 !over，直接 return（字数文本、count.danger 类、saveBtn.disabled 的更新在此之前已完成，不受影响）；若 successHintActive 为 true 但 over 为 true（保存期间用户改超限），先清守卫再走既有 setAiMemoryHint(AI_MEMORY_OVERFLOW_HINT, 'danger') 分支——超限优先于成功提示。
    4. **守卫清除点**：textarea input 监听（L3780 的 updateAiMemoryCount 调用处）改为先置 aiMemoryState.successHintActive = false 再调 updateAiMemoryCount——用户输入使「已保存」立即失效，避免 2 秒窗口内输入时 hint 停留在成功态。

    同时核对：switchAiMemoryTab / loadAiMemory 的 hint 设置走 setAiMemoryHint 直接覆盖，天然不受守卫影响（守卫只拦 updateAiMemoryCount 内部联动），无需额外改动；但切 tab 后守卫若残留 true 会在下一次保存前抑制联动——在 switchAiMemoryTab 开头清一次守卫兜底。

    保留既有失败路径（catch 分支 danger 提示）与零容器空态（WR-03）行为不变。
  </action>
  <verify>
    <automated>node --check src/settings-page.js && grep -c "successHintActive" src/settings-page.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/settings-page.js` 退出码 0
    - `grep -c "successHintActive" src/settings-page.js` ≥ 4（字段定义 + saveAiMemory 置位/复位 + updateAiMemoryCount 读取 + input 清除）
    - `grep -n "successHintActive" src/settings-page.js` 的命中覆盖 saveAiMemory、updateAiMemoryCount、textarea input 监听、switchAiMemoryTab 四处函数体
    - 源读断言：updateAiMemoryCount 中守卫分支在 `!over` 时 return，不触达 resetAiMemoryHint；over 时清除守卫并显示 AI_MEMORY_OVERFLOW_HINT
    - 源读断言：saveAiMemory 的 2 秒回调无论 hint 是否仍为成功态都把守卫复位（无残留 true 路径）
  </acceptance_criteria>
  <done>saveAiMemory 成功路径的「已保存」不再被 finally 的 updateAiMemoryCount 同步覆盖（G-43-2 根因消除，源级闭合）</done>
</task>

<task type="auto">
  <name>Task 2: UI 自动化帧轮询验证（G-43-2 行为闭环）</name>
  <files>/tmp/verify-g43-2-hint.js（临时验证脚本，不入库）, .planning/phases/43-ai-mvp/43-05-SUMMARY.md</files>
  <read_first>
    - /tmp/realm-uat43.js、/tmp/realm-uat43-debug.js —— UAT 已验证可行的 playwright _electron 驱动方式（全局 playwright 库启动 dev 应用、选 index.html 主窗口、进入 realm://settings guest、hint 轮询方法），环境失效时按 docs 参照重写
    - src/settings-page.js:3717-3759 —— 被验证的 saveAiMemory 流程
    - .planning/phases/43-ai-mvp/43-UI-SPEC.md —— success 契约验收基准
  </read_first>
  <action>
    参照 /tmp/realm-uat43*.js 的既有插桩方式编写临时验证脚本 /tmp/verify-g43-2-hint.js（不提交入库）：playwright _electron 启动 dev 应用（NODE_ENV=development），定位 index.html 主窗口 → 打开 realm://settings → AI 记忆分区（默认全局记忆 tab）→ 100ms 间隔轮询 #aiMemoryHint 的 textContent 与 classList → 触发一次保存（textarea 有内容且未超限）→ 断言：

    1. 至少 1 帧命中 textContent === '已保存' 且 hint 含 ai-memory-hint-success 类（G-43-2 的失败点：UAT 4 秒轮询零命中）。
    2. 约 2 秒后该成功帧消失，hint 恢复为生效时机文案「保存后将在新会话生效」。
    3. 保存前后字数统计 #aiMemoryCount 正常刷新（守卫未误伤既有联动）。

    运行后删除临时脚本不提交。结果（帧捕获时间线摘要）记录进 43-05-SUMMARY.md。

    **Fallback（UI 自动化环境不可用时）**：以 Task 1 的源级断言为准闭合，在 43-05-SUMMARY.md 显式记录「G-43-2 行为验证转人工」并列出下次 UAT 的复核步骤（进入 AI 记忆分区 → 保存 → 观察「已保存」绿色提示约 2 秒）。
  </action>
  <verify>
    <automated>node /tmp/verify-g43-2-hint.js</automated>
    <human-check>fallback 路径：用户下次 UAT 按 SUMMARY 记录的步骤复核「已保存」提示可见约 2 秒</human-check>
  </verify>
  <acceptance_criteria>
    - 验证脚本退出码 0，输出确认「已保存」成功帧被捕获（≥1 帧）且约 2 秒后恢复生效时机文案
    - 43-05-SUMMARY.md 记录帧时间线摘要（或 fallback 转人工说明）
  </acceptance_criteria>
  <done>「已保存」success 提示按 UI-SPEC 契约真实可见约 2 秒（G-43-2 行为闭环，或已明确转人工复核）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer 设置页 UI → /api/ai-memory → 记忆文件 | 用户本人编辑通道（不经 D-11 扫描，CONTEXT.md 已豁免） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-09 | Tampering（信息误导） | updateAiMemoryCount 守卫误抑制超限 danger 提示 | medium | mitigate | 守卫设计为超限优先：successHintActive 且 over 时清除守卫并显示 AI_MEMORY_OVERFLOW_HINT（Task 1 验收断言覆盖）；保存后 2 秒回调必复位守卫（无残留路径）|
| T-43-10 | Repudiation（状态歧义） | 成功提示期间用户误以为已生效 | low | accept | 生效时机文案在 2 秒后恢复展示（D-04 冻结快照语义），成功提示是瞬时反馈非持久声明 |
</threat_model>

<verification>
- `node --check src/settings-page.js` 通过
- UI 自动化：保存后「已保存」success 帧被捕获且约 2 秒消失（或 fallback 转人工已记录）
- 字数统计/超限阻断/失败提示行为回归不变（Task 1 源读断言 + Task 2 断言 3）
</verification>

<success_criteria>
G-43-2 闭合：「已保存」success 提示按 UI-SPEC 契约（success 色、约 2 秒、不弹框）真实可见，finally 覆盖链路被守卫短路且不引入超限提示被抑制的新缺陷。
</success_criteria>

<output>
Create .planning/phases/43-ai-mvp/43-05-SUMMARY.md when done
</output>
