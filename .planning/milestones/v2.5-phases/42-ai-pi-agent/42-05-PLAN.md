---
phase: 42-ai-pi-agent
plan: 05
type: execute
wave: 3
depends_on:
  - 42-03
  - 42-04
files_modified:
  - src/renderer.js
  - src/index.html
  - src/styles/main.css
  - AGENTS.md
autonomous: true
requirements:
  - CONV-02
gap_closure: true
gap_ids: [G-42-5, G-42-6, G-42-7]

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "右键「重命名」后行内编辑框出现且面板保持打开，确认后列表立即显示新标题（G-42-5）"
    - "确认框点「删除」后对话及消息真正删除（IPC 实际发出），目标 id 不依赖会被清空的共享状态（G-42-6）"
    - "删除确认框显示在屏幕中央而非左上角（G-42-7）"
    - "AGENTS.md 记录弹框居中全局约定（用户明确要求，G-42-7）"
  artifacts:
    - "src/renderer.js — 菜单项 stopPropagation + 删除确认对话框 dataset 闭包 + dialog 纯 showModal 用法"
    - "src/index.html — #aiConvDeleteDialog 摘除误用的 .ai-modal-overlay 全屏遮罩类"
    - "src/styles/main.css — .ai-conv-delete-dialog 显式 margin:auto 居中规则 + ::backdrop"
    - "AGENTS.md — 弹框居中与 <dialog> 约定章节"
  key_links:
    - "菜单项 click stopPropagation ↔ document 级关闭器不再触发（下拉保开、convContextTarget 不被清）"
    - "dataset.conversationId ↔ 确认处理器结构化闭包 ↔ ai:delete-conversation IPC 必达"
    - "margin:auto ↔ 抵消全局 * { margin: 0 } 对 UA dialog 居中的覆盖"
---

<objective>
修复对话右键菜单与删除确认框交互（G-42-5、G-42-6）及弹框居中（G-42-7）：重命名行内编辑真正可用，删除确认真正删除，弹框居中并按用户要求把约定沉淀进 AGENTS.md。

Purpose: UAT Test 6/8 失败 — 重命名点击后面板关闭无任何反应、删除点击后静默 no-op（同根因：菜单项点击事件冒泡到 document 级关闭器）、确认框钉在左上角（全局 * { margin: 0 } 清掉 UA dialog 居中 + 全屏 div 遮罩类误用于 dialog）。
Output: 事件竞态修复 + 结构化删除目标传递 + dialog 居中 + AGENTS.md 约定
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-ai-pi-agent/42-UAT.md
@.planning/debug/conversation-rename-no-response.md
@.planning/debug/conversation-delete-not-effective.md
@.planning/debug/delete-dialog-top-left-position.md
@src/renderer.js
@src/index.html
@src/styles/main.css
@AGENTS.md

**前置事实（诊断已确认，直接采用）**：
- 竞态时序：菜单项 click 在 target 阶段执行处理器，同一次点击随后冒泡到 document——①6518 行外部点击关闭器把对话框/菜单外的一切点击判为「面板外」执行 closeConvDropdown()（菜单 append 在 document.body，不在 #aiConvDropdown 内），新建的行内编辑框随面板 display:none 被隐藏，隐藏带焦点元素触发 blur → submitRename → renderConvList 销毁编辑框；②showConvContextMenu 经 setTimeout(0) 注册的 handleConvContextMenuClose 再次 closeConvContextMenu()，其内部无条件置 state.convContextTarget = null——删除确认处理器随后读该状态恒空，ai:delete-conversation IPC 从未发出。
- 后端链路完整健壮（ipc-handlers.js 空 id 抛错、ai-conversations-manager.js DELETE + CASCADE），无需改动。
- main.css 100-104 全局 * { margin: 0 } 覆盖原生 dialog 的 UA margin:auto；.ai-modal-overlay（5124-5132）是为 realm:// settings 全屏 div 遮罩设计（position:fixed; inset:0; flex 居中、无 margin/宽高），UA fit-content 尺寸的 dialog 套上后过约束解析钉死在 top:0; left:0。width/height:100% 方案已被否（UA max-width/max-height 截断致 19px 偏移）。
- 项目内既有正确参照：.modal（main.css 688 margin:auto）、.download-delete-modal、.download-clear-modal 均带 margin:auto + 注释。
- Electron 43 已像素级验证：该 dialog 补 margin:auto 后即居中。

**工作区未提交改动（必须保留，不得回退）**：src/renderer.js 的 createNewConversation 响应读取修复（见 42-03 context），修复提交时一并纳入。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 菜单项事件竞态修复 — stopPropagation + 删除目标 dataset 闭包（G-42-5 + G-42-6）</name>
  <files>src/renderer.js</files>
  <read_first>
    - src/renderer.js (外部点击关闭器 6518-6526、showConvContextMenu 6859-6904、closeConvContextMenu 6917-6923、renameConversation 6930-6988、showDeleteConfirm 6995-7010、closeDeleteConfirm 7015-7022、确认按钮绑定 6508-6515)
    - 42-UAT.md G-42-5 / G-42-6 (missing 项)
  </read_first>
  <action>
1. 菜单项处理器加 e.stopPropagation()（per G-42-5 missing「重命名/删除菜单项点击处理器加 e.stopPropagation()」；G-42-6 同根因配套）：showConvContextMenu 内 renameItem 与 deleteItem 的 click 处理器签名加事件参数，处理器首行调用 e.stopPropagation()，阻断同一次点击冒泡到两个 document 级关闭器（6518 外部关闭器与 handleConvContextMenuClose）。处理器内既有的 closeConvContextMenu() + renameConversation(conversationId) / showDeleteConfirm(conversationId, title) 调用顺序保留——显式关菜单发生在 target 阶段，不再有关闭器二次执行。

2. 删除确认结构化传参（per G-42-6 missing 首选「showDeleteConfirm 把对话 id 闭包进确认处理器或存 elements.aiConvDeleteDialog.dataset，确认流程不再依赖会被 closer 清空的共享状态」）：
   - showDeleteConfirm：移除 state.convContextTarget 赋值，改为 elements.aiConvDeleteDialog.dataset.conversationId = String(conversationId)；确认文案更新与 showModal 展示逻辑保留。
   - 确认按钮处理器（6508-6515，setup 阶段一次性绑定）：读取 elements.aiConvDeleteDialog.dataset.conversationId，非空才 await deleteConversation(id)，随后 closeDeleteConfirm()。
   - closeDeleteConfirm：移除 state.convContextTarget = null，改为 removeAttribute('data-conversation-id') 清理 dataset。
   - state.convContextTarget 若再无其他读取方，连同 showConvContextMenu 里的赋值一并删除该状态字段（grep 确认无残留引用后再删；仍有引用则仅解除删除链路依赖）。

3. 行内编辑期间面板保持打开（per G-42-5 truth「点击重命名后标题变为可编辑输入框，输入新名称确认后列表立即显示新标题」）：步骤 1 的 stopPropagation 已保证下拉面板不被外部关闭器隐藏——renameConversation 插入的 input 在 #aiConvDropdown 内，后续点击 input 命中 closer 的 contains(e.target) 豁免，面板稳定保持打开。核对 renameConversation 现有 blur → submitRename 行为：点击面板外其他区域时面板关闭、blur 提交重命名（提交先于/伴随关闭均为可接受结果）；Enter 提交、Escape 取消语义不动。

4. handleConvContextMenuClose（6909-6912）：保留现有移除自身监听的逻辑；经步骤 1 的 stopPropagation，菜单项点击不再触发它，仅真正的面板外点击触发——语义正确，无需额外改动。
  </action>
  <verify>
    <automated>node --check src/renderer.js && grep -c "stopPropagation" src/renderer.js && grep -c "dataset.conversationId" src/renderer.js</automated>
  </verify>
  <acceptance_criteria>
    - renameItem / deleteItem click 处理器首行 e.stopPropagation()
    - 删除确认目标存于 #aiConvDeleteDialog dataset，确认处理器读 dataset，不再读 state.convContextTarget
    - closeDeleteConfirm 清理 dataset
    - 行内编辑期间下拉面板保持打开
    - node --check 通过
  </acceptance_criteria>
  <done>重命名行内编辑完整可用（G-42-5）；确认删除真正发出 IPC 并删除对话与消息（G-42-6）</done>
</task>

<task type="auto">
  <name>Task 2: 删除确认框居中 — dialog 专用类 + 显式 margin:auto（G-42-7）</name>
  <files>src/index.html, src/styles/main.css, src/renderer.js</files>
  <read_first>
    - src/index.html (935 行 dialog 现状)
    - src/styles/main.css (全局重置 100-104、.modal 688 margin:auto 正确参照、.ai-modal-overlay 5124-5132、.bookmark-edit-modal 2899-2909 dialog 重置参照)
    - src/renderer.js (showDeleteConfirm / closeDeleteConfirm 中 style.display 操作)
    - 42-UAT.md G-42-7 (missing + debug_session 像素级验证结论)
  </read_first>
  <action>
1. src/index.html 935 行：dialog 的 class 从 ai-modal-overlay 改为 ai-conv-delete-dialog（per G-42-7 missing「#aiConvDeleteDialog 自身规则补 margin: auto」+「绝不要把全屏 div 遮罩类用到 dialog 上」）。内部 .ai-modal 结构、行内样式、id 不动。

2. src/styles/main.css 在 AI 面板样式段（.ai-modal-overlay 附近）新增规则：
   - .ai-conv-delete-dialog { margin: auto; border: none; padding: 0; background: transparent; } ——margin:auto 居中是项目既定惯例（与 .modal 及两个下载对话框一致，均带注释），border/padding/background 重置让内层 .ai-modal 全权负责外观；附注释说明全局 * { margin: 0 } 会清掉 UA 的 dialog 居中，必须显式声明。
   - .ai-conv-delete-dialog::backdrop { background: rgba(0, 0, 0, 0.6); } ——原 .ai-modal-overlay div 遮罩提供的压暗背景由 ::backdrop 等价承接。
   - .ai-modal-overlay 类本身保留不删（realm:// settings 页的 div 遮罩仍在用，per G-42-7 missing「settings.html 的 div 遮罩用法保持不变」）。

3. src/renderer.js showDeleteConfirm / closeDeleteConfirm：删除对 elements.aiConvDeleteDialog 的 style.display = 'flex' / 'none' 手动切换（per AGENTS.md 既有约定：显隐用 showModal()/close() 原生机制，且 display:flex 会与 margin:auto 居中布局相互干扰）。保留 showModal() / close() 调用与其存在性判断；Task 1 加的 dataset 读写不动。
  </action>
  <verify>
    <automated>grep -c "ai-conv-delete-dialog" src/index.html src/styles/main.css && grep -c "margin: auto" src/styles/main.css && node --check src/renderer.js</automated>
  </verify>
  <acceptance_criteria>
    - #aiConvDeleteDialog 不再挂 .ai-modal-overlay 类，改挂 .ai-conv-delete-dialog
    - main.css 含 .ai-conv-delete-dialog 的 margin:auto 规则与 ::backdrop 压暗规则
    - .ai-modal-overlay 类保留供 realm:// div 遮罩使用
    - renderer 不再对 dialog 做 style.display 手动切换
  </acceptance_criteria>
  <done>删除确认框（modal 状态下）像素级居中，遮罩压暗等效迁移到 ::backdrop</done>
</task>

<task type="auto">
  <name>Task 3: AGENTS.md 弹框居中全局约定沉淀（G-42-7 用户明确要求）</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (整体章节风格——中文小节 + 规则列表，参考「内部页面 CSP：markup 内联 style 会被拦截」小节的写法与位置)
    - 42-UAT.md G-42-7 (missing 第四项：用户明确要求的约定全文要点)
  </read_first>
  <action>
在 AGENTS.md 开发要点区域（「内部页面 CSP：markup 内联 style 会被拦截」小节之后）新增小节「弹框居中约定（dialog 必须显式 margin: auto）」，用 Edit 插入、风格与既有小节一致（规则条目 + 反例 + 参照）。内容覆盖用户要求的全部要点：

1. 全局约定：以后所有弹框都必须显示在屏幕中央（用户强约定，源自删除确认框三次钉在左上角的事故）。
2. 主窗口统一用原生 dialog 元素 + showModal()/close()，且该 dialog 的类规则必须显式声明 margin: auto——main.css 全局 * { margin: 0 } 重置会清掉 UA 的 dialog 居中；不要用 width/height: 100% 撑满方案（UA max-width/max-height 截断导致偏移，已验证否决）。
3. 绝不要把全屏 div 遮罩类（如 .ai-modal-overlay：position:fixed; inset:0; flex 居中）用到 dialog 元素上——UA fit-content 尺寸 + inset 全 0 过约束解析会把盒子钉在 top:0; left:0；dialog 的遮罩压暗用 ::backdrop。
4. realm:// 内部页面（CSP style-src 'self'）不适用本条：继续用 div 遮罩 + JS CSSOM display 切换（与「内部页面 CSP」小节的 CSSOM 约定衔接）。
5. 正确参照：.modal、.download-delete-modal、.download-clear-modal、.ai-conv-delete-dialog（均显式 margin: auto）。
  </action>
  <verify>
    <automated>grep -c "弹框居中" AGENTS.md && grep -c "margin: auto" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md 新增弹框居中约定小节，含 dialog 显式 margin:auto、禁止全屏 div 遮罩类用于 dialog、::backdrop、realm:// 页面豁免四要点
    - 小节风格与 AGENTS.md 既有小节一致
  </acceptance_criteria>
  <done>用户要求的弹框居中约定沉淀进 AGENTS.md，后续弹框开发有据可依</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM dataset→IPC | dataset.conversationId 作为删除目标传入主进程 |
| user input→DOM | 重命名输入、确认文案插值回显 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-10 | Tampering | dataset.conversationId → ai:delete-conversation | low | mitigate | dataset 仅由 showDeleteConfirm 以 String(conversationId) 写入；IPC 侧 assertTrustedSender + 空 id 抛错校验沿用 |
| T-42-11 | Tampering | 确认文案标题插值 | low | mitigate | 沿用 textContent 赋值路径（非 innerHTML），无注入面 |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | 无新增依赖 |
</threat_model>

<verification>
1. 右键对话项 → 重命名：菜单关闭后面板保持打开，标题原位变输入框；改名的确认（Enter/失焦）后列表立即显示新标题（G-42-5）
2. 右键对话项 → 删除 → 确认框居中显示且文案含标题；点「删除」后对话与消息从库中消失（ DevTools console 无 ai:delete-conversation 报错，列表立即刷新）；删除当前对话后列表到达空状态/切换（G-42-6 + G-42-7）
3. 确认框出现位置为屏幕中央（非左上角），背景压暗正常（G-42-7）
4. AGENTS.md 可检索到弹框居中约定小节
5. 回归：对话切换、新建、列表打开/关闭、模型选择器下拉不受影响
</verification>

<success_criteria>
- 重命名与删除两条右键链路真实可用（G-42-5、G-42-6）
- 弹框居中 + ::backdrop 等效迁移（G-42-7）
- 弹框居中约定进入 AGENTS.md（用户明确要求）
- 工作区既有未提交改动被保留并随修复一并提交
</success_criteria>

<output>
创建 `.planning/phases/42-ai-pi-agent/42-05-SUMMARY.md` when done
</output>
