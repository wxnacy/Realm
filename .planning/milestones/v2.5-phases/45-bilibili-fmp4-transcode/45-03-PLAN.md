---
phase: 45-bilibili-fmp4-transcode
plan: 03
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - media-cache-manager.js
  - main.js
  - tests/test-media-cache.js
autonomous: true
requirements: [D-03, D-05, D-07]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 缓存链路 MAP URI 不进 segments map——playlist_order 对齐判定与完整度分母不含 init（D-07/Pitfall 2，G-44-7 key URI 同款坑：「混入 segments 会污染 playlist_order 完整性判定」）
    - init 本体留存 <videoDir>/init（_safePath 双基准校验 + 固定常量文件名）+ meta.init_segment = { size, sha256, uri_key } 登记；重复 init 请求（uri_key 相同）跳过重写（D-10 增量语义）
    - updatePlaylistIndex 登记 meta.map_uris（segKey 数组形态与 key_uris 对齐，容忍 MAP 轮换——Pitfall 8）+ meta.has_fmp4_map；附加字段不升 META_VERSION（44 先例）
    - getConvertInfo 返回 initPath（存在性校验后）与 hasFmp4Map；历史无 init_segment 的 fMP4 条目 initPath=null
    - startConvertFromInput 透传 initPath；hasFmp4Map && !initPath 早拒 { ok:false, reason:'init_missing' }（D-03 第二链路覆盖，文案 45-02 已闭环引导重播）
  artifacts:
    - media-cache-manager.js：updatePlaylistIndex map_uris/has_fmp4_map 登记；storeBuffer MAP 分支（排除+留存 init）；getConvertInfo 返回 initPath/hasFmp4Map
    - main.js：startConvertFromInput initPath 透传 + init_missing 早拒
    - tests/test-media-cache.js：「EXT-X-MAP 排除与 init 留存（D-05/D-07）」用例组
  key_links:
    - hls.js 先请求清单（updatePlaylistIndex 落盘 map_uris）后请求 init/分片 → storeBuffer O(1) includes 判定命中 map_uris → init 本体落盘（时序前提直接继承 key_uris 先例）
    - 缓存条目「转换为 MP4」→ startConvertFromInput → getConvertInfo().initPath → convertToMp4(initPath) → concatFmp4ToMp4（45-01 分流）
  prohibitions:
    - statement: init 绝不进 segments map（D-07 完整性口径：init 分片不计入完整度分母）
      status: locked
      verification: tests 断言 storeBuffer MAP key 后 meta.segments 无该键、completeness 与 playlist_order 对齐不回归
    - statement: 自动化测试禁止真实网络（Pitfall 7）；lookup() init 命中回放本阶段不做（RESEARCH Q5 Wave-2 优化：miss 回源+留存已满足功能，非范围缩减——列入 deferred）
      status: locked
      verification: tests 全部 stub；media-cache-manager.js lookup() 无 init 分支（保持 miss 回源语义）
---

<objective>
缓存链路 fMP4 支持（D-03 第二链路）：EXT-X-MAP 登记与 init 留存（D-05，复刻 44-18 key_uris 先例）、完整性判定排除 init（D-07）、转换入口 initPath 透出与历史条目 init_missing 早拒。

Purpose: 缓存的 fMP4 VOD 条目（B 站直播回放等）此前连 init 分片都没存，拼接无从下手；本计划补齐缓存侧 init 留存与转换透出，让 fMP4 缓存条目可转 mp4。
Output: cache-manager MAP 三件套（登记/排除+留存/透出）、startConvertFromInput 透传与早拒、缓存用例组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bilibili-fmp4-transcode/45-CONTEXT.md
@.planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md
@.planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md
@.planning/phases/45-bilibili-fmp4-transcode/45-01-SUMMARY.md
@.planning/phases/45-bilibili-fmp4-transcode/45-02-SUMMARY.md

@media-cache-manager.js
@main.js
@tests/test-media-cache.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-cache-manager.js`：meta 附加字段 `map_uris`（string[]）、`has_fmp4_map`（boolean）、`init_segment`（{ size, sha256, uri_key }）、`map_byterange`；getConvertInfo 返回字段 `initPath`、`hasFmp4Map`；storeBuffer 返回 `{ ok:true, skipped:true, reason:'map_uri' }` 新分支
- `main.js`：startConvertFromInput 早拒 reason `init_missing`（hasFmp4Map && !initPath）+ convertToMp4 入参 initPath 透传
- tests/test-media-cache.js：「EXT-X-MAP 排除与 init 留存（D-05/D-07）」describe 用例组
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache-manager MAP 登记 + init 排除留存 + getConvertInfo 透出（D-05/D-07）</name>
  <files>media-cache-manager.js, tests/test-media-cache.js</files>
  <read_first>
    - media-cache-manager.js（lines 137-162 _safePath 双基准、391-433 storeBuffer key_uris 排除段、605-643 updatePlaylistIndex 登记段、658-724 getConvertInfo 读取侧过滤+返回）
    - tests/test-media-cache.js（lines 411-499 密钥 URI 排除 describe、501-586 AES-128 解密材料 describe——四个新用例与既有 key 用例一一对应）
    - .planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md（media-cache-manager.js 段三处先例原文 + Pitfall 2 污染警告）
    - .planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md（Q5 缓存链路草案、Pitfall 2/8、Assumption A4）
  </read_first>
  <behavior>
    - 含 EXT-X-MAP 清单 updatePlaylistIndex → meta.map_uris=[segKey(init 绝对 URL)]、has_fmp4_map=true
    - storeBuffer 命中 map_uris → 不进 segments、本体落盘 <videoDir>/init、meta.init_segment={ size, sha256, uri_key }、返回 skipped:'map_uri'
    - 二次同 uri_key init 请求 → 不覆盖已留存 init（首 init 为准，D-10 增量语义）
    - 完整性：MAP key 存在时 completeness 计算与 playlist_order 对齐判定不含 init（分母不污染）
    - getConvertInfo 有 init_segment 且文件存在 → initPath 非 null + hasFmp4Map；历史无 init_segment 条目 → initPath=null
    - mapByterange 非空清单 → 记 meta.map_byterange 但不登记 map_uris（不支持形态，转换落 init_missing 兜底）
  </behavior>
  <action>
  1. updatePlaylistIndex（照 lines 611-643 key_uris 登记复刻）：`pl.mapUri` 非空时 `meta.map_uris = [segmentKeyOf(resolveUri(pl.mapUri, m3u8Url))]`（数组形态容忍 MAP 轮换——Pitfall 8；resolveUri 非法 URI try/catch 跳过同款）+ `meta.has_fmp4_map = true`；`pl.mapByterange` 非空 → 另记 `meta.map_byterange` 且不登记 map_uris（整资源形态以外不支持，RESEARCH Route D）。附加字段不升 META_VERSION（lines 605-610 注释先例）。
  2. storeBuffer（照 lines 417-432 key_uris 段复刻，**关键差异**：key 只留 hex 进 meta，init 本体必须写盘——拼接必需品）：命中 `mMeta.map_uris`（O(1) includes 判定；时序前提继承：hls.js 先请求清单后请求 init）→ 跳过 segments 登记，经 `_safePath`（lines 137-162 resolve+realpath 双基准）写 `<videoDir>/init`（固定常量文件名不进远端输入，T-44-11），登记 `meta.init_segment = { size: buffer.length, sha256, uri_key: segKey }`；`uri_key` 相同且 init_segment 已存在 → 跳过重写（D-10 增量语义）；返回 `{ ok:true, skipped:true, reason:'map_uri' }`。**绝不进 segments map**（Pitfall 2）。
  3. getConvertInfo（lines 663-676 + 712-724）：剔除集在 key_uris 之外并入 map_uris（allKeys 过滤同款两处）；`<videoDir>/init` 存在性校验后返回对象追加 `initPath`（无 → null）与 `hasFmp4Map: !!meta.has_fmp4_map`。
  4. tests/test-media-cache.js 新增「EXT-X-MAP 排除与 init 留存（D-05/D-07）」describe（与 key describe 一一对应：登记断言 ↔ :423、跳过+留存 ↔ :434/:533、读取侧过滤 ↔ :459、透出 ↔ :514），覆盖 <behavior> 六案例；init 夹具用 mux.js generator（45-01 同法）。
  </action>
  <verify>
    <automated>node tests/test-media-cache.js && node -e "require('./media-cache-manager'); console.log('pure-node ok')"</automated>
    <fails_when>测试非零退出（fail ≥1 / pass 0——含既有 key_uris/AES/完整性用例回归）；node -e 非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "map_uris" media-cache-manager.js` 命中 ≥3 处（updatePlaylistIndex 登记、storeBuffer 判定、getConvertInfo 过滤）
    - `grep -n "init_segment" media-cache-manager.js` 命中（登记 + 透出）
    - `grep -n "_safePath" media-cache-manager.js` 在 init 写盘路径上命中（路径越界校验复用）
    - 行为断言全绿：<behavior> 六案例对应测试全部 pass；既有 describe（411-499/501-586）零回归
  </acceptance_criteria>
  <done>fMP4 缓存会话 init 本体留存可取、完整性判定不含 init、getConvertInfo 透出 initPath；历史无 init 条目 initPath=null（转换侧早拒兜底）。</done>
  <reversibility rating="reversible">附加 meta 字段不升版本，旧条目缺字段=能力关闭；回退即删分支，存量 init 文件由容量淘汰自然回收（缓存非持久资产，44-06 先例）。</reversibility>
</task>

<task type="auto">
  <name>Task 2: startConvertFromInput initPath 透传 + 历史条目 init_missing 早拒（D-03）</name>
  <files>main.js</files>
  <read_first>
    - main.js（lines 3215-3273 startConvertFromInput、3258-3264 key_unavailable 早拒段——新早拒插在同一位置）
    - .planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md（main.js 段早拒语义模式摘录）
    - .planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md（Q5 缓存链路「历史无 init 的 fMP4 缓存条目」段、Open Question 2 / Assumption A4）
  </read_first>
  <action>
  1. main.js startConvertFromInput：`getConvertInfo` 返回后、`if (info.hasEncryption)` 早拒段同一位置（lines 3258-3264）插入——`info.hasFmp4Map && !info.initPath` → `return { ok: false, reason: 'init_missing' }`（历史无 init 的 fMP4 缓存条目早拒；文案 45-02 已闭环「请重新播放……后再转换」——重播后 init 经 /proxy 留存即可转换，自愈路径 = key_unavailable 同款引导）。
  2. convertToMp4 调用透传 `initPath: info.initPath`（编排其余零改动，D-06 全盘复用）。TS 条目 initPath=null 不影响既有路径。
  3. lookup() init 命中回放**不做**（RESEARCH Q5 Wave-2 优化：miss 回源 + storeBuffer 留存已满足功能闭环；记入 SUMMARY deferred 供后续立小项）。
  </action>
  <verify>
    <automated>node --check main.js && grep -n "init_missing" main.js && grep -n "initPath" main.js && node tests/test-media-cache.js && node tests/test-media-remuxer.js</automated>
    <fails_when>node --check 非零退出；grep 无命中非零退出（早拒或透传未落码）；任一测试非零退出（缓存/remuxer 回归）</fails_when>
    <human-check>
    缓存链路 fMP4 转换 + QuickTime 兼容性（D-03 第二链路真机复验 + RESEARCH A1 [ASSUMED] 关闭）：
    1. `npm run dev`，播放一个 fMP4 VOD 源（B 站直播或回放——A4：VOD 形态未实测，若遇 BYTERANGE/差异形态应落 init_missing 拒转文案而非坏产物，此分支也算验证点）
    2. 确认 init 已经 /proxy 留存（缓存目录出现 init 文件），抽屉/任务页点「转换为 MP4」
    3. mpv 或 VLC 打开产物可播（画面+声音）即通过；总时长垃圾值不判 fail（D-02）
    4. 用 QuickTime Player 打开任一拼接产物确认可播（A1：失败不阻塞——mpv/VLC 已实测兜底，结果记入 UAT 供用户决策）
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "hasFmp4Map && !info.initPath" main.js` 或等价早拒结构命中（init_missing 早拒落码于 key_unavailable 同区段）
    - `grep -n "initPath" main.js` 在 startConvertFromInput 调用点命中（透传落码）
    - 缓存/remuxer 测试套件全绿零回归
  </acceptance_criteria>
  <done>fMP4 缓存条目可经任务页/抽屉转 mp4；历史无 init 条目走 init_missing 文案引导重播；真实流验证留 end-of-phase human-check。</done>
  <reversibility rating="reversible">编排层单入口增量，回退即删早拒与透传两行。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端清单 mapUri → 缓存目录 init 文件 | mapUri 为远端不可信输入；segKey sha256 决定登记键，落盘位置为固定常量 |
| 缓存 meta.json → getConvertInfo/initPath | 本地 meta 可被篡改（T-44 系列先例：字段类型白名单校验语义沿用） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-05 | Tampering | init 写盘路径越界 | high | mitigate | init 路径经 _safePath（resolve+realpath 双基准，lines 137-162 复用）+ 文件名固定常量 'init' 不进远端输入（T-44-11）；uri_key 为 segKey sha256 不参与路径拼接 |
| T-45-06 | Tampering | init 混入 segments 污染完整性判定 | medium | mitigate | map_uris O(1) 成员判定排除（Pitfall 2 硬约束）；测试断言 meta.segments 无 init 键 + completeness/playlist_order 对齐不回归 |
| T-45-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新依赖（D-01）——本计划无安装动作，供应链面零增量 |
</threat_model>

<verification>
- 全套件：`node tests/test-media-cache.js && node tests/test-media-remuxer.js && node tests/test-media-record-duration.js && node tests/test-m3u8-playlist-parser.js`（VALIDATION.md full suite，< 30s）
- 接线落码：grep map_uris/init_segment/initPath/init_missing 命中
- 人工验证：<verify><human-check> 缓存链路 fMP4 转换 + QuickTime A1（end-of-phase UAT 收割）
</verification>

<success_criteria>
- 缓存 fMP4 会话 init 留存且完整性分母不含 init（D-05/D-07 双达成）
- 历史无 init 条目 init_missing 早拒 + 文案引导重播自愈
- 四条测试套件全绿，Phase 45 全链路（parser → 录制/缓存 → 编排 → 拼接）闭环
</success_criteria>

<output>
创建 `.planning/phases/45-bilibili-fmp4-transcode/45-03-SUMMARY.md` 当完成
</output>
