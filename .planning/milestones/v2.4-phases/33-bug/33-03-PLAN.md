---
phase: 33-bug
plan: 03
type: gap-closure
wave: 3
depends_on: ["33-02"]
files_modified:
  - src/renderer.js
requirements: [AF-07]
---

# Plan 33-03: Gap Closure — 地址保存横幅处理逻辑

**Phase:** 33-bug (自动填充 — 增强 + Bug 修复)
**Goal:** 修复 VERIFICATION.md 中的 G-1 gap，在 renderer.js 中添加地址保存横幅和自动填充处理逻辑
**Mode:** gap-closure

---

## Gap Summary

**G-1:** src/webview-preload.js 发送 `address:form-detected` IPC 消息，但 src/renderer.js 中没有对应的处理函数和横幅显示逻辑。

**Evidence:**
- `src/webview-preload.js:433` 发送 `ipcRenderer.sendToHost('address:form-detected', detectedData)`
- `src/webview-preload.js:436` 发送 `ipcRenderer.sendToHost('address:autofill-request')`
- `src/index.html:302` 包含 `#addressSaveBanner` HTML 结构
- `src/renderer.js` 中没有任何 address 相关代码

---

## Tasks

### Task 1: 在 ipc-message handler 中添加 address 相关处理

**File:** `src/renderer.js`
**Location:** ~line 1008 (ipc-message handler)

在现有的 `credential:autofill-request` 分支后添加：

```javascript
} else if (e.channel === 'address:form-detected') {
  // 地址表单检测：显示保存地址横幅
  handleAddressFormDetected(e.args[0]);
} else if (e.channel === 'address:autofill-request') {
  // 地址自动填充请求：查询地址并发送到 webview
  handleAddressAutofillRequest(webview);
}
```

<automated>grep -c "address:form-detected" src/renderer.js && grep -c "address:autofill-request" src/renderer.js</automated>

**Verification:** webview 检测到地址表单时，renderer.js 能接收到消息；自动填充请求能正确处理

---

### Task 2: 添加 elements 引用

**File:** `src/renderer.js`
**Location:** ~line 150 (elements 对象)

在 credential 相关元素后添加：

```javascript
addressSaveBanner: document.getElementById('addressSaveBanner'),
addressSaveConfirmBtn: document.getElementById('addressSaveConfirmBtn'),
addressNeverBtn: document.getElementById('addressNeverBtn'),
addressLaterBtn: document.getElementById('addressLaterBtn'),
```

<automated>grep -c "addressSaveBanner" src/renderer.js && grep -c "addressSaveConfirmBtn" src/renderer.js</automated>

**Verification:** 元素引用正确绑定

---

### Task 3: 添加 state 变量

**File:** `src/renderer.js`
**Location:** state 对象初始化处

添加：

```javascript
state.pendingAddressData = null;  // 暂存地址数据（保存按钮需要）
state.addressBannerTimer = null;  // 地址横幅自动消失定时器
```

<automated>grep -c "pendingAddressData" src/renderer.js && grep -c "addressBannerTimer" src/renderer.js</automated>

**Verification:** 状态变量正确初始化

---

### Task 4: 实现 handleAddressFormDetected 函数

**File:** `src/renderer.js`
**Location:** 在 handleCredentialFormSubmitted 函数后（~line 8382）

参考 `handleCredentialFormSubmitted` 模式实现：

```javascript
/**
 * 处理地址表单检测事件
 * 显示保存地址提示横幅（Chrome 风格）
 *
 * @param {Object} data - 地址数据 { name, phone, address, ... }
 */
async function handleAddressFormDetected(data) {
  if (!data) return;

  const containerId = state.currentContainer;

  try {
    // 检查该容器是否已有保存的地址
    const existing = await window.realmAPI.addressAPI.getAddress(containerId);
    if (existing) return; // 已有地址，不重复提示

    // 显示保存横幅
    showSaveAddressBanner(data);
  } catch (err) {
    console.error('[Realm Renderer] 处理地址表单检测失败:', err);
  }
}
```

<automated>grep -c "handleAddressFormDetected" src/renderer.js</automated>

**Verification:** 函数能正确检查已有地址并显示横幅

---

### Task 5: 实现 handleAddressAutofillRequest 函数

**File:** `src/renderer.js`
**Location:** 在 handleAddressFormDetected 函数后

参考 `handleAutofillRequest` 模式实现：

```javascript
/**
 * 处理地址自动填充请求
 * 查询当前容器的地址，找到后发送到 webview 进行填充
 *
 * @param {HTMLElement} webview - 发起请求的 webview 元素
 */
async function handleAddressAutofillRequest(webview) {
  const containerId = state.currentContainer;

  try {
    const address = await window.realmAPI.addressAPI.getAddress(containerId);
    if (address) {
      // 发送填充指令到 webview
      webview.send('address:do-autofill', address);
    }
  } catch (err) {
    console.error('[Realm Renderer] 地址自动填充查询失败:', err);
  }
}
```

<automated>grep -c "handleAddressAutofillRequest" src/renderer.js</automated>

**Verification:** 函数能正确查询地址并发送到 webview

### Task 6: 实现 showSaveAddressBanner 函数

**File:** `src/renderer.js`
**Location:** 在 showSaveCredentialBanner 函数后

参考 `showSaveCredentialBanner` 模式实现：

```javascript
/**
 * 显示保存地址提示横幅（Chrome 风格）
 * 10 秒后自动消失
 *
 * @param {Object} data - 地址数据
 */
function showSaveAddressBanner(data) {
  const banner = elements.addressSaveBanner;
  if (!banner) return;

  // 清除之前的定时器
  if (state.addressBannerTimer) {
    clearTimeout(state.addressBannerTimer);
    state.addressBannerTimer = null;
  }

  // 暂存地址数据（保存按钮需要）
  state.pendingAddressData = data;

  // 显示横幅
  banner.classList.remove('hidden');
  banner.offsetHeight; // 强制重排以触发动画
  banner.classList.add('visible');

  // 10 秒自动消失
  state.addressBannerTimer = setTimeout(() => {
    hideAddressBanner();
  }, 10000);

  // 绑定按钮事件
  setupAddressBannerButtons();

  // ESC 键隐藏
  const escHandler = (e) => {
    if (e.key === 'Escape') {
      hideAddressBanner();
      document.removeEventListener('keydown', escHandler);
    }
  };
  document.addEventListener('keydown', escHandler);
}
```

<automated>grep -c "showSaveAddressBanner" src/renderer.js</automated>

**Verification:** 横幅能正确显示、自动消失、ESC 关闭

---

### Task 7: 实现 setupAddressBannerButtons 函数

**File:** `src/renderer.js`

```javascript
/**
 * 绑定地址保存横幅按钮事件
 * 每次显示时重新绑定，避免闭包捕获旧数据
 */
function setupAddressBannerButtons() {
  const saveBtn = elements.addressSaveConfirmBtn;
  const neverBtn = elements.addressNeverBtn;
  const laterBtn = elements.addressLaterBtn;

  if (saveBtn) {
    saveBtn.onclick = async () => {
      if (state.addressBannerTimer) {
        clearTimeout(state.addressBannerTimer);
        state.addressBannerTimer = null;
      }
      const addrData = state.pendingAddressData;
      if (addrData) {
        try {
          await window.realmAPI.addressAPI.saveAddress({
            containerId: state.currentContainer,
            ...addrData,
          });
        } catch (err) {
          console.error('[Realm Renderer] 保存地址失败:', err);
        }
      }
      state.pendingAddressData = null;
      hideAddressBanner();
    };
  }

  if (neverBtn) {
    neverBtn.onclick = () => {
      if (state.addressBannerTimer) {
        clearTimeout(state.addressBannerTimer);
        state.addressBannerTimer = null;
      }
      // 地址没有"永不保存"功能，直接关闭
      state.pendingAddressData = null;
      hideAddressBanner();
    };
  }

  if (laterBtn) {
    laterBtn.onclick = () => {
      if (state.addressBannerTimer) {
        clearTimeout(state.addressBannerTimer);
        state.addressBannerTimer = null;
      }
      state.pendingAddressData = null;
      hideAddressBanner();
    };
  }
}
```

<automated>grep -c "setupAddressBannerButtons" src/renderer.js</automated>

**Verification:** 三个按钮功能正常

---

### Task 8: 实现 hideAddressBanner 函数

**File:** `src/renderer.js`

```javascript
/**
 * 隐藏地址保存横幅
 * 移除 visible class 触发退出动画，动画结束后添加 hidden class
 */
function hideAddressBanner() {
  const banner = elements.addressSaveBanner;
  if (!banner) return;

  banner.classList.remove('visible');
  setTimeout(() => {
    banner.classList.add('hidden');
  }, 300);
}
```

<automated>grep -c "hideAddressBanner" src/renderer.js</automated>

**Verification:** 横幅隐藏动画正常

---

## Verification Checklist

- [ ] webview 检测到地址表单时，renderer.js 接收到 `address:form-detected` 消息
- [ ] webview 发送自动填充请求时，renderer.js 接收到 `address:autofill-request` 消息
- [ ] 首次访问（无已保存地址）时显示保存横幅
- [ ] 已有保存地址时不显示横幅
- [ ] 点击"保存地址"按钮后地址成功保存
- [ ] 点击"永不"按钮关闭横幅
- [ ] 点击"暂不"按钮关闭横幅
- [ ] ESC 键关闭横幅
- [ ] 10 秒后横幅自动消失
- [ ] 横幅显示/隐藏动画正常
- [ ] 有已保存地址时，访问含地址表单的网页自动填充

---

## Dependencies

- `window.realmAPI.addressAPI` (已存在于 preload.js)
- `#addressSaveBanner` HTML 结构 (已存在于 index.html)
- `address:form-detected` IPC 通道 (已存在于 webview-preload.js)
- `address:autofill-request` IPC 通道 (已存在于 webview-preload.js)

---

## Estimated Effort

~30 lines of handler code + ~100 lines of banner functions = ~130 lines total

**Risk:** Low — 完全参考现有凭据横幅模式实现
