---
phase: 33-bug
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - address-manager.js
  - main.js
  - ipc-handlers.js
  - src/preload.js
  - src/webview-preload.js
  - src/renderer.js
  - src/index.html
  - src/settings.html
  - src/settings-page.js
  - src/styles/main.css
autonomous: true
requirements: [AF-06, AF-07]

must_haves:
  truths:
    - 用户浏览含地址表单的网页时，弹出保存地址提示横幅
    - 用户保存的地址使用 safeStorage 加密存储（macOS Keychain）
    - 用户再次访问含地址表单的网页时，地址字段自动填充
    - 用户可以在设置页查看和编辑已保存的地址
    - 每个容器独立存储一个地址（覆盖写入）
  artifacts:
    - address-manager.js（新建 — SQLite + safeStorage 地址存储模块）
    - main.js（新增 /api/address/* HTTP 路由 + address-manager 初始化）
    - ipc-handlers.js（新增 address:save/get/delete IPC handler）
    - src/preload.js（暴露 addressAPI）
    - src/webview-preload.js（地址表单检测 + 地址自动填充）
    - src/renderer.js（地址保存横幅逻辑）
    - src/index.html（地址保存横幅 HTML）
    - src/settings.html（地址管理卡片 UI）
    - src/settings-page.js（地址管理逻辑）
    - src/styles/main.css（地址卡片 + 横幅样式）
  key_links:
    - webview-preload.js AddressDetector -> ipc-message address:form-detected -> renderer.js showSaveAddressBanner()
    - renderer.js addressSaveConfirmBtn -> realmAPI.addressAPI.save -> ipc-handlers.js -> address-manager.js -> safeStorage
    - webview-preload.js address:do-autofill -> AutofillEngine.fillAddressFields()
    - settings-page.js addressApi() -> main.js handleAddressApi() -> address-manager.js
    - address-manager.js saveAddress() -> safeStorage.encryptStringAsync() -> SQLite addresses 表
---

<objective>
实现地址表单全栈功能：新建 address-manager.js 模块（SQLite + safeStorage 加密存储），在 main.js 注册 /api/address/* HTTP 路由和 IPC handler，在 webview-preload.js 实现地址表单检测和自动填充，在 renderer.js 实现地址保存横幅，在设置页新增地址管理卡片。

Purpose: 用户需要保存和自动填充地址表单信息（AF-06, AF-07），实现类似 Chrome 地址自动填充的体验。
Output: 地址功能完整可用 — 检测、保存横幅、加密存储、自动填充、设置页管理。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-bug/33-CONTEXT.md
@.planning/phases/33-bug/33-RESEARCH.md
@.planning/phases/33-bug/33-UI-SPEC.md
@credential-manager.js
@main.js
@ipc-handlers.js
@src/preload.js
@src/webview-preload.js
@src/renderer.js
@src/index.html
@src/settings.html
@src/settings-page.js
@src/styles/main.css
@.planning/phases/33-bug/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 地址后端 — address-manager.js + HTTP API + IPC + preload</name>
  <files>address-manager.js, main.js, ipc-handlers.js, src/preload.js</files>
  <read_first>
    - credential-manager.js（全文 — address-manager.js 的同构参考）
    - main.js:1257-1342（handleDownloadsApi HTTP 路由模式）
    - main.js:1496-1555（realmServer 路由分发）
    - ipc-handlers.js:993-1070（credential:* IPC handler 注册模式）
    - src/preload.js:1000-1030（credentialAPI contextBridge 暴露模式）
    - 33-RESEARCH.md Pattern 2（address-manager.js 结构参考）
    - 33-CONTEXT.md D-05, D-06（字段定义和存储策略决策）
  </read_first>
  <behavior>
    - address-manager.js 有 initDatabase、saveAddress、getAddress、deleteAddress 四个函数
    - addresses 表有 UNIQUE(container_id) 约束（每容器单地址，D-06）
    - saveAddress 使用 safeStorage.encryptStringAsync 加密三个字段
    - getAddress 使用 safeStorage.decryptStringAsync 解密返回 {name, phone, address}
    - /api/address/get?containerId=xxx 返回当前容器地址
    - /api/address/save 接收 {containerId, name, phone, address} 保存地址
    - /api/address/delete 接收 {containerId} 删除地址
    - IPC address:save/get/delete 可供主窗口 renderer 调用
    - preload.js 暴露 addressAPI（saveAddress, getAddress, deleteAddress）
  </behavior>
  <action>
    1. 新建 address-manager.js（同构 credential-manager.js，per D-05, D-06）：
       - 延迟加载 better-sqlite3（与 credential-manager 一致）
       - initDatabase(): 创建 addresses 表 —— id, container_id(UNIQUE), encrypted_name(BLOB), encrypted_phone(BLOB), encrypted_address(BLOB), created_at, updated_at; CREATE INDEX idx_addresses_container_id
       - saveAddress(containerId, name, phone, address): safeStorage.isAsyncEncryptionAvailable 检查 -> encryptStringAsync 加密三个字段 -> INSERT OR REPLACE INTO addresses（per D-06 每容器单地址）
       - getAddress(containerId): SELECT * FROM addresses WHERE container_id = ? -> decryptStringAsync 解密三个字段 -> 返回 {name, phone, address} 或 null
       - deleteAddress(containerId): DELETE FROM addresses WHERE container_id = ?
       - module.exports = { initDatabase, saveAddress, getAddress, deleteAddress }
    2. 在 main.js 中扩展：
       - require('./address-manager.js') 在文件顶部（与 credential-manager 并列）
       - 在 app.whenReady 回调中调用 addressManager.initDatabase()（与 credentialManager.initDatabase 并列）
       - 新增 handleAddressApi 函数（参考 handleDownloadsApi 模式）：
         - token 鉴权
         - GET /api/address/get?containerId=xxx -> addressManager.getAddress()
         - POST /api/address/save -> {containerId, name, phone, address} -> addressManager.saveAddress()
         - POST /api/address/delete -> {containerId} -> addressManager.deleteAddress()
       - 在 realmServer 路由分发中注册：if (reqPath.startsWith('/api/address/')) { handleAddressApi(req, res, reqUrl); return; }
    3. 在 ipc-handlers.js 中新增 address:* IPC handler（参考 credential:* 模式）：
       - ipcMain.handle('address:save', async (event, { containerId, name, phone, address }) => addressManager.saveAddress(containerId, name, phone, address))
       - ipcMain.handle('address:get', async (event, { containerId }) => addressManager.getAddress(containerId))
       - ipcMain.handle('address:delete', async (event, { containerId }) => addressManager.deleteAddress(containerId))
    4. 在 src/preload.js 中暴露 addressAPI（参考 credentialAPI 模式）：
       - addressAPI: { saveAddress: (data) => ipcRenderer.invoke('address:save', data), getAddress: (containerId) => ipcRenderer.invoke('address:get', { containerId }), deleteAddress: (containerId) => ipcRenderer.invoke('address:delete', { containerId }) }
  </action>
  <verify>
    <automated>node -e "const am = require('./address-manager.js'); console.log(typeof am.initDatabase, typeof am.saveAddress, typeof am.getAddress, typeof am.deleteAddress);"</automated>
  </verify>
  <acceptance_criteria>
    - address-manager.js 存在且导出 initDatabase、saveAddress、getAddress、deleteAddress
    - addresses 表有 UNIQUE(container_id) 约束
    - saveAddress 使用 safeStorage.encryptStringAsync 加密 name/phone/address
    - getAddress 使用 safeStorage.decryptStringAsync 解密并返回明文
    - main.js 注册 /api/address/* HTTP 路由（get, save, delete）
    - ipc-handlers.js 注册 address:save/get/delete IPC handler
    - preload.js 暴露 addressAPI 对象
  </acceptance_criteria>
  <done>地址后端模块、HTTP API、IPC handler、preload API 全部就绪</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 地址前端 — 表单检测 + 自动填充 + 保存横幅 + 设置页卡片</name>
  <files>src/webview-preload.js, src/renderer.js, src/index.html, src/settings.html, src/settings-page.js, src/styles/main.css</files>
  <read_first>
    - src/webview-preload.js:74-340（FormDetector + AutofillEngine 模式，地址检测/填充复用此框架）
    - src/renderer.js:1002-1010（credential:form-submitted 处理模式）
    - src/renderer.js:8370-8540（凭据保存横幅逻辑，地址横幅复用此模式）
    - src/index.html:285-320（credential-save-banner HTML 结构，地址横幅复用）
    - src/settings.html:288-320（settings section 结构参考）
    - src/settings-page.js:54-120（API 调用模式）
    - src/styles/main.css:6958-6990（credential-save-banner 样式，地址横幅复用）
    - 33-UI-SPEC.md C-04, C-05, C-06（地址卡片 + 地址横幅规范）
    - 33-CONTEXT.md D-09, D-10, D-11, D-12（地址检测策略决策）
    - 33-RESEARCH.md Pattern 3（地址表单检测代码参考）
  </read_first>
  <behavior>
    - AddressDetector.scanForAddressForms() 扫描页面匹配姓名/手机号/地址字段（D-09）
    - 2/3 字段匹配即触发地址保存横幅（D-10）
    - 地址检测在 did-finish-load 后执行一次（D-12）
    - 地址自动填充：页面加载后查询地址数据，填入对应字段
    - 地址保存横幅：工具栏下方滑入，10 秒自动消失（D-07）
    - 设置页地址卡片：查看模式 + 编辑模式（D-08）
  </behavior>
  <action>
    1. 在 src/webview-preload.js 中扩展地址表单检测和自动填充（per D-09, D-10, D-11, D-12）：
       - 新增 AddressDetector 对象（在 FormDetector 之后）：
         - scanForAddressForms(): 扫描 input[type=text], input[type=tel], textarea，匹配特征：
           - 姓名字段：name/id/placeholder/autocomplete 含 name/realname/username/姓名/收件人/联系人
           - 手机号字段：type=tel 或 name/id/placeholder 含 phone/mobile/tel/手机/电话
           - 地址字段：name/id/placeholder 含 address/addr/地址/详细地址/收货地址
         - 2/3 匹配阈值（D-10）：匹配到 2 个字段即返回 candidates，否则返回 null
         - 排除包含 password 字段的表单（避免与登录表单冲突）
       - 新增地址自动填充逻辑（在 AutofillEngine 中扩展）：
         - fillAddressFields(fields, data): 使用 native setter 填入 name/phone/address 值（复用 AutofillEngine 的 setInputValue 方法）
       - 在页面加载完成事件中（did-finish-load 相关）：
         - 调用 AddressDetector.scanForAddressForms()
         - 如果检测到地址表单：通过 ipcRenderer.sendToHost('address:form-detected', {name, phone, address}) 通知主窗口
         - 同时检查是否有保存的地址（通过 IPC address:get），有则自动填充
    2. 在 src/renderer.js 中新增地址保存横幅逻辑（复用 credential-save-banner 模式，per C-05, D-07）：
       - 在 webview 的 ipc-message 监听中新增 address:form-detected 处理：
         - 提取检测到的字段值
         - 调用 showSaveAddressBanner(data)
       - 新增 showSaveAddressBanner(data) 函数：
         - 显示 #addressSaveBanner（移除 hidden 类）
         - 10 秒后自动隐藏（setTimeout）
         - "保存地址"按钮：调用 realmAPI.addressAPI.saveAddress({containerId, name, phone, address})，成功后按钮变绿勾消失
         - "永不"按钮：记录永不保存（复用 credential never_save 机制或新增 address_never_save），横幅消失
         - "暂不"按钮：横幅消失
         - ESC 键和点击外部：等同于"暂不"
       - 新增 hideAddressBanner() 函数（slideUp 动画）
    3. 在 src/index.html 中新增地址保存横幅 HTML（per C-05）：
       - 在 credential-save-banner 之后插入 address-save-banner div
       - 结构：address-save-info（图标 + 文案）+ address-save-actions（保存/永不/暂不 三个按钮）
       - z-index 9997
    4. 在 src/settings.html 中新增地址管理卡片（per C-04）：
       - 在 #settings-autofill section 的凭据表格之后，新增"收货地址"settings-group
       - 包含：地址卡片（查看模式 + 编辑模式）、空状态
       - 查看模式：姓名/手机号/详细地址字段 + 编辑/删除按钮
       - 编辑模式：三个输入框（text/tel/textarea）+ 保存/取消按钮
    5. 在 src/settings-page.js 中新增地址管理逻辑（per D-08）：
       - 新增 addressApi(route, options, query) 函数（参考 settingsApi 模式）
       - 新增 loadAddress() 函数：获取当前容器地址，有数据显示查看模式，无数据显示空状态
       - 新增 renderAddressView(address) 函数：填充查看模式字段（textContent）
       - 新增 toggleAddressEdit() 函数：切换查看/编辑模式
       - 新增 saveAddress() 函数：验证姓名和手机号非空，手机号 11 位数字，调用 save API
       - 新增 deleteAddress() 函数：弹出确认对话框，确认后调用 delete API
       - 在 switchSettingsPage 的 case 'autofill' 中调用 loadAddress()
       - 在容器切换逻辑中重新调用 loadAddress()
    6. 在 src/styles/main.css 中新增地址相关样式：
       - .address-save-banner: 与 credential-save-banner 同构，z-index 9997
       - .address-card: 背景 #252525，边框 #404040，内边距 16px
       - .address-view / .address-edit: 查看/编辑模式布局
       - .address-field: 标签 + 值的两列布局
       - .address-empty-state: 空状态提示样式
  </action>
  <verify>
    <automated>grep -c 'AddressDetector' src/webview-preload.js && grep -c 'address-save-banner' src/index.html && grep -c 'showSaveAddressBanner' src/renderer.js && grep -c 'address-card' src/settings.html</automated>
  </verify>
  <acceptance_criteria>
    - webview-preload.js 有 AddressDetector 对象，scanForAddressForms 实现 2/3 匹配阈值
    - webview-preload.js 有地址自动填充逻辑（fillAddressFields 或类似函数）
    - index.html 有 address-save-banner HTML（位于 credential-save-banner 之后）
    - renderer.js 有 showSaveAddressBanner 和 hideAddressBanner 函数
    - renderer.js 处理 address:form-detected ipc-message
    - settings.html #settings-autofill section 有地址管理卡片（查看+编辑模式）
    - settings-page.js 有 loadAddress、renderAddressView、toggleAddressEdit、saveAddress、deleteAddress 函数
    - main.css 有 .address-save-banner 和 .address-card 样式
    - 地址横幅 z-index 9997（与 credential-save-banner 同级）
    - 地址检测排除含 password 字段的表单（避免与登录表单冲突）
  </acceptance_criteria>
  <done>地址表单检测、保存横幅、自动填充、设置页管理卡片全部可用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview guest → HTTP API | 设置页通过 HTTP API 访问地址数据，token 鉴权 |
| webview preload → ipc-message | 地址检测结果通过 ipc-message 传递给主窗口 renderer |
| renderer → IPC → address-manager | 地址保存通过 IPC 调用主进程，safeStorage 加密 |
| HTTP API → address-manager.js | HTTP 路由层校验参数后调用存储层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Information Disclosure | 地址 PII 明文存储 | critical | mitigate | safeStorage 加密存储 name/phone/address（macOS Keychain） |
| T-33-06 | Tampering | /api/address/* HTTP API | high | mitigate | REALM_TOKEN 鉴权，所有 HTTP 端点校验 token |
| T-33-07 | Tampering | 地址卡片 innerHTML XSS | high | mitigate | DOM 构建 + textContent 渲染（WR-13 模式） |
| T-33-08 | Information Disclosure | 地址横幅泄露敏感信息 | low | accept | 横幅仅显示"检测到地址表单"提示，不显示具体地址内容 |
| T-33-09 | Elevation | 地址保存无确认 | medium | mitigate | 横幅需要用户主动点击"保存"按钮（per D-07） |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | 本阶段无新包安装 |
</threat_model>

<verification>
1. address-manager.js 可正确初始化数据库和执行 CRUD 操作
2. /api/address/* HTTP 路由注册完成，token 鉴权生效
3. IPC address:save/get/delete handler 注册完成
4. preload.js 暴露 addressAPI
5. webview-preload.js 地址表单检测正确匹配 2/3 字段
6. 地址保存横幅在检测到地址表单时正确弹出
7. 地址自动填充在有保存地址时正确填入字段
8. 设置页地址卡片可查看、编辑、删除地址
9. 地址数据使用 safeStorage 加密存储（grep 验证 encryptStringAsync）
10. 所有 DOM 渲染使用 textContent（grep 验证无 innerHTML）
</verification>

<success_criteria>
- AF-06 完成：用户可以保存地址表单信息（姓名、电话、地址）
- AF-07 完成：用户可以在地址表单中自动填充已保存的地址信息
- 地址检测、保存横幅、加密存储、自动填充、设置页管理全部可用
- 每个容器独立存储一个地址（覆盖写入）
</success_criteria>

<output>
创建 `.planning/phases/33-bug/33-02-SUMMARY.md` when done
</output>
