---
phase: 51-zip
plan: 03
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - ai-skills-manager.js
  - ai-manager.js
  - main.js
  - src/settings.html
  - src/settings-page.js
  - tests/test-skills-import.js
  - tests/helpers/make-malicious-zip.js
  - tests/test-skills-http-api.js
  - tests/test-ai-skills.js
  - tests/fixtures/skill-corpus/*.md
autonomous: true
requirements: [USER-03, USER-05, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06]
user_setup: []

estimate:
  # ⚠️ 未经本项目校准：estimate-check --calibrated 报 ratio 1.50 / over_budget=true，
  #    但 confidence=low 且 sample_count=0（本项目尚无校准样本）⇒ 该数字是**量级参考而非精确值**。
  #    T1 跨 7 个文件是 tracer 的形态使然（每层一个文件），不适用「单任务 > 5 文件即拆」；
  #    不要据此重切已经跑通的纵切形状。
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

key-decisions:
  - '**本计划 T1 是阶段的主 tracer**：一条薄纵切把「设置页选 zip → `POST /api/skills/import`（raw binary）→ `readRawBody` → 解压校验 → `importId` 句柄 → 预览 → commit → **唯一落盘实现** → 回读 → 重扫 + 补播」一次打通。选这条纵切为 tracer 的理由：本阶段**唯一**的架构不确定性是「三条来源能不能真的汇进一个落盘函数、且设置页 guest 能不经 base64 把 `File` 当 body 发出来」—— 这条路径一次跑通能同时证伪「raw body 形态选错」「同源 POST 触发预检」「落盘被写成第二份」三类假绿。**tracer 任务跨 7 个文件是形态使然（每层一个文件），不适用「单任务 > 5 文件即拆」的常规限制**；T2/T3 严守 ≤ 4'
  - '**三种来源必须写进 `<importDir>/pkg.zip` 再统一 `yauzl.openPromise(pkgPath)`**（而不是上传分支走 `fromBufferPromise` 省一次写盘）：代价是一次 ≤ 32 MiB 的临时写，换来的是「三条来源共用**同一条**解压入口」这条判据**结构上成立**（P9 第 5 条），也避免 32 MiB Buffer 在内存里存两份。这是把「只有一个落盘实现」从口号变成代码形状的做法'
  - '**`IMPORT_SKILL_ERROR` 在本计划一次定义完整码表**（20 键），后续 `51-05` **只用不增**；`51-04` 只补**一个**真实缺失的原因（`INVALID_NAME`，改名非法 —— 见 `51-04` 的 key-decisions，最终 21 键）—— 避免「常量跨 wave 追加」造成的三处账本（常量 + 产品文档 §十三 + `AGENTS.md` 测试清单）反复漂移。码表**不得**并入 `MANAGE_SKILL_ERROR`（CR-3：`tests/test-manage-skill.js:1589` 把它锁死为**恰 11 键**，`:1605` 逐字写着「不得再新立第十二键」）'
  - '**`LIMITS` 一字不加、一字不改**：导入的六类限额全部进新常量 `IMPORT_LIMITS`（`LIMITS` 的「只允许加项」纪律在本阶段表现为**零改动** —— 六类限额与原五项不同量，混进去会让 50 的 `limits` 投影凭空多出六个数）。**端点与前端零字面量**：六类限额与 `PREVIEW_LIST_LIMIT` 一律随 preview 响应回传，设置页不写任何数值'
  - '**`readRawBody` 的唯一差异是 `chunks.push` + `Buffer.concat`**，三条教训逐条照抄（**累积中拒收**：`if (rejected) return;` **先于**累加 size / **不断连**：`req.resume()`，禁 `req.destroy()` 与 `Connection: close` / **`res` 缺失降级**：`canRespond`）。**禁止**「先 `arrayBuffer()` 再判大小」（那已经吃掉内存，判据对象是「**不无上限读入内存**」）。**内存上界必须如实写成 maxBytes（≈32 MiB）**，不得声称「零堆增长」'
  - '**既有两个冻结计数器必须同批刷新（三处账本一起改）**：`main.js` 的 `await readJsonBody(req` 调用点计数在 `tests/test-skills-http-api.js` 里被钉成**恰 59**（并断言 `total === withRes`），而 import 分支的 JSON 模式**必须**用 `readJsonBody`（复用 50 D-16 已固化的形状）⇒ 计数变 **60**，同批改测试里的两个常量与注释。`res.writeHead(` 的基线 **14 处不变**（新分支一律走 `sendJson`，不得新增响应发送点）'
  - '**复用的是 `validateManagedSkillName` 那一份校验器**（49 D-11 / 50 的复用纪律）：导入的 name 判定与**改名输入框**走同一函数；发现第二份正则一律视为缺陷（`prohibitions` 具名登记）'
  - '**技能根定位的「全包扫描」不得限定 `skills/*`**（CR-7 实测：`anthropics/skills` 的 `template/SKILL.md` 位于**包根**）。多根拒绝文案里给出**实测的 20 根**与 `tree/<ref>/<path>` 的正确形态（CR-8：`find-skills` 给出的候选正是这类仓库 URL，用户必须自己补子路径；**处置走拒绝文案**，不改内置技能正文 —— 改 `skills-builtin/**` 会连带触发 `THIRD_PARTY_NOTICES.md` 五要素与零安装语义扫描）'
  - '**威胁扫描归 T3，且只扩表不加接线**（49 D-08 的交接契约）：`SKILL_THREAT_PATTERNS` 落 `ai-skills-manager.js`，**与 `INJECTION_PATTERNS` / `CREDENTIAL_PATTERNS` 分表**（后两者住 `ai-memory-manager.js`、是 hard-fail 表；技能域三类的效力是**不拒绝**、高亮 + 必勾）⇒ 需要一个**并列的、返回命中列表的**扫描函数。扩表后 49 的 `manage_skill` 写入路径**自动获得**技能域模式 —— 这是契约兑现，**不是**越界（须在 `51-07` 成文）'
  - '**语料回归夹具的形态取舍（显式登记）**：CR-12 要求「`anthropics/skills` 的语料固化为精简样本」。本计划用**两条等价判据**满足其实质（真实语料零误伤 + 正命题命中）：① 直接扫 `skills-builtin/**`（**已在仓、归属已由 P10 覆盖**）；② `tests/fixtures/skill-corpus/*.md` 由**本仓自撰**、逐条复刻 `51-RESEARCH.md` 「❌ 看似危险但实测不能收」表里的 11 类误伤形态（含各自实测误伤数作为注释）。**不**把上游字节复制进仓库：`tests/**` 虽被 `build.files` 排除（不进发布产物），但仓库是公开的，复制上游文本会触发 `THIRD_PARTY_NOTICES.md` 的五要素同步（P10 的登记面），而判据的价值在**形态**不在**字节**。若用户要求逐字复制，追加一个夹具目录 + 一条 `THIRD_PARTY_NOTICES` 条目即可（不改变本计划任何判据）'
  - '**T1 落地时 T2 的闸门尚未到位**（如压缩比 / 归一化查重 / 解压后递归 `lstat`）：这是 tracer-first 的固有代价 —— 本计划的**完成判据是三条任务全绿**，不是任一条单独绿。T2 必须在本计划内闭合这些闸，不得推迟到后续计划'

must_haves:
  truths:
    - '同一包内 SKILL.md 计数 = 1 ⇒ 进预览；= 0 或 ≥ 2 ⇒ 整包拒绝，回传含实测条目数的可操作提示（提示给出 tree/<ref>/<path> 形态的替代地址示例）'
    - 'preview 调用返回 {importId, preview} 且不产生 skills/<name>/；仅 commit 调用后才出现该目录；预览六字段（名称 / description 原文 / 目录树+计数 / 字节数 / 脚本清单 / 扫描结论两栏）在渲染快照中齐备'
    - '含 symlink entry 的包在两条判据路（central-directory external attributes 与解压后递归 lstat）各自被拒；两路都断言整包拒绝（skills/ 下零新增）而非跳过该条目'
    - 'entry 名为空串 / 仅斜杠 / 仅点 时整包拒绝（不静默跳过）'
    - '长度与查重判据都取 NFD 归一化 + 小写后的字符串；判定对象是归一化值而非原始字节或码点个数 —— 用例：café(NFC U+00E9) 与 cafe+组合重音(NFD) 同包 ⇒ 拒绝整包'
    - '六类限额各有一例：单 entry / 累计字节 / entry 数 / 压缩比 / 嵌套深度（按技能根相对计）/ SKILL.md 64 KiB；另 uncompressedSize === 0xFFFFFFFF 一例被显式拒绝'
    - '解压目标由 mkdtempSync 生成且解压前为空；越界对照组真跑一次并断言 root 外零新文件；落点复核在父目录链最近已存在祖先为 symlink 的形态下拒绝'
    - 'SKILL_THREAT_PATTERNS 三类各有一条正样本命中；skills-builtin/** 全部文本零命中（回归夹具）；description 与 body 各有一条命中用例证明双扫'
    - '**三种来源汇进同一个落盘实现**（P9 第 5 条的机械判据）：全仓 `importUserSkill(` 的出现位置为「`ai-skills-manager.js` 的定义 1 处 + `ai-manager.js` 的调用 1 处」，`main.js` 与 `src/settings-page.js` 各 0 处 —— 源码扫描可判'
    - '**本计划只交付「无同名冲突」路径上的六字段预览**：冲突三档（覆盖 / 改名 / 取消）与落点事务由 `51-04` 交付，网络地址来源由 `51-05` 交付。本计划的 `commit` 在 `skills/<name>/` 已存在时**拒绝**（返回 `unsupported_url` 之外的显式失败码）而**不**静默覆盖 —— 这条中间态是**有意**的：它保证 `51-04` 之前不存在任何静默覆盖路径'
  artifacts:
    - 'ai-skills-manager.js：`IMPORT_LIMITS`、`IMPORT_SKILL_ERROR`（20 键完整码表）、`validateEntryName()`、`locateSkillRoot()`、`readSkillPackageEntries()`、`extractAndValidatePackage()`、`parseSkillFrontmatter()`、`deriveImportName()`、`collectPreviewStats()`、`buildImportPreview()`、`importUserSkill()`（唯一落盘实现）、`SKILL_THREAT_PATTERNS`（T3）、`scanSkillThreats()`（T3）、上述符号的 `module.exports` 追加段'
    - 'ai-manager.js：`this._skillImports`（Map）、`previewSkillImport()`、`commitSkillImport()`（调用侧重扫恰一次 + 补播恰一次）、`getSeededSkillNamesSafe()` 的注入用法'
    - 'main.js：`MAX_SKILL_PACKAGE_BYTES`、`readRawBody(req, res, { maxBytes })`、`handleSkillsApi` 的 `import` 分支（按 Content-Type / mode 分流）'
    - 'src/settings.html：`#skillImportOpen` 入口按钮 + `#skillImportFile` 隐藏 file input + `#skillImportModal` 极简弹框外壳（复用 50 的 `.ai-modal-overlay` / `.ai-modal` 类，**零新增 CSS**）'
    - 'src/settings-page.js：`importZipAsSkill()` / `commitSkillImport()` / `renderSkillImportPreview()`（全部走 DOM API 赋值，region 标记内）'
    - 'tests/test-skills-import.js（**新建套件**）+ tests/helpers/make-malicious-zip.js（**新建夹具生成器**）+ tests/fixtures/skill-corpus/*.md（**新建语料夹具**）'
    - 'tests/test-skills-http-api.js 增补（`readRawBody` 四组 + 源码契约组 + 调用点计数刷新 59 → 60）；tests/test-ai-skills.js 增补（技能域三类值域 + 否定语境护栏）'
  key_links:
    - '单一落盘链：`previewSkillImport`（三条来源的唯一入口）→ `importUserSkill`（唯一落盘实现）→ `skills/<name>/`。**判据**：`importUserSkill(` 在全仓恰 2 处出现（定义 + 调用），`main.js` 与前端 0 处'
    - '单一解压链：三条来源都写 `<importDir>/pkg.zip` → `yauzl.openPromise(pkgPath)` → `for await (const entry of zf.eachEntry())`（**开流必须在循环体内** —— `eachEntry()` 的 iterator `return()` 会 `close()` 整个 zipfile，先枚举后开流 100% 得 `Error: closed`）'
    - '常量单源链：`IMPORT_LIMITS` → preview 响应的 `limits` 字段 → 设置页渲染。设置页**不得**出现 `32` / `2000` / `100` / `16` 这类限额字面量'
    - '写路径收口（D-19）：`await this.ensureSkillsFresh()` 恰一次 + **调用侧** `windowManager.broadcast(''skills:changed'')` 恰一次；`syncAgentSystemPrompt()` 的**函数体逐字不改**（46-04 的方法体断言 + 48 的广播次数断言同时钉着它）。⚠️ 本阶段完成后写路径份额到 3/3，但**读侧 / 兜底**份额（`refreshSkillsForPanel()` / `ensureSkillsFresh()`）**不是同一个量**，**不得**声称 P8 6/6'
    - '读盘而非缓存快照：冲突判定 / 数量闸 / 存在性一律读盘（`env.exists` / `env.listDir`），不用模块级缓存 —— bash 可随时改写磁盘'
  prohibitions:
    - statement: '不得把威胁扫描的「未命中」呈现成「这个技能是安全的」背书（预览 / 状态行 / 文档 / 测试命名均计入）'
      status: unresolved
      verification: judgment
      reason: 'D-12 / P3 明文：三类的性质是**启发式、降低概率**，不是安全边界；P3 原文把真正的边界写成「导入预览确认 + 沙箱」。任何「扫描通过 = 安全」的表述都会让用户在**合法的同类技能**上形成错误预期，并掩盖「扫描表必然滞后于新措辞」这一事实'
    - statement: '不得在加载期对**已在盘上**的技能做威胁扫描后丢弃或隐藏'
      status: unresolved
      verification: judgment
      reason: '会推翻 46 D-08（「不丢弃命名不规范的合法技能」），并让**已导入的合法技能在某次升级后消失**（49 D-05 已付过这条论证）。扫描只在**导入**与 49 的**写入侧**生效'
    - statement: '不得把「上传体积上限」当作 zip 炸弹的防护，也不得在测试/文档里声称「上传闸拦下了炸弹」'
      status: unresolved
      verification: judgment
      reason: 'CR-5 的方向纠正（实测 `bomb-ratio.zip` = 101,923 B 声明解出 104,857,600 B，ratio ≈ 1027:1）⇒ 上传闸对炸弹**零贡献**。正确表述是「**合法包**的解压总量 ≤ 32 MiB ⇒ 其**压缩后**体积必然 ≤ 32 MiB，故上传闸不会误杀合法包」；**压缩比闸与累计解压闸是两道独立的自建闸**，测试必须分别断言「压缩比闸拦下了」（而不是「上传闸拦下了」）'
    - statement: '不得为让落盘「有第二个消费者」而在 `main.js` / 前端再写一段落盘 / 落点校验逻辑；也不得为「消除重复」把 `importUserSkill` 与 `createManagedSkill` 合并'
      status: unresolved
      verification: judgment
      reason: '两条写路径的对象不同（`manage_skill` 写 `managed-skills/<name>/SKILL.md` 单文件；导入整体接管 `skills/<name>/` 目录）且冲突语义相反（`resolveManagedTarget` 是 managed 视角，导入是 user 视角），合并会让两侧的拒绝面互相污染。50 的 `deleteUserSkill` 与 `resolveManagedTarget` 已明文付过这条代价'
---

<objective>
打通 **Phase 51 的主纵切**并把 zip 一侧的**全部**安全判据落地：设置页选一个 zip → `POST /api/skills/import`（raw binary）→ 解压到 `mkdtemp` 空目录并按六类限额 / 逃逸族 / symlink 两路 / 归一化查重逐条校验 → 用不透明 `importId` 返回六字段预览 → 用户确认后经**唯一落盘实现**写进 `skills/<name>/` → 回读验证 → 重扫 + 补播。

Purpose: 本阶段是 v2.6 里安全面最大的一环（P2 / P4 两条 S1 门禁），且它的**唯一**结构性风险是「三条来源是否真的汇进一条管线」。T1 先把这条纵切跑通（含最小但真实的 entry 校验），T2 把校验补到**完整**（六类限额 + 逃逸族 + 六类实测漏网 + 归一化查重 + 解压后递归 `lstat` + 落点最近已存在祖先 realpath + 越界对照组），T3 交付技能域威胁扫描（SEC-06）与双扫。

Output: `ai-skills-manager.js` 的导入面（校验器 / 解压 / 定位 / 预览 / 唯一落盘实现 / 威胁扫描表）、`ai-manager.js` 的两阶段转发、`main.js` 的 `readRawBody` 与 `import` 路由、设置页最小导入入口、新建套件 `tests/test-skills-import.js` 与夹具生成器 `tests/helpers/make-malicious-zip.js`。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-RESEARCH.md
@.planning/phases/51-zip/51-PATTERNS.md
@.planning/phases/51-zip/51-VALIDATION.md
@.planning/phases/51-zip/COVERAGE.md
@ai-skills-manager.js
@ai-manager.js
@main.js
@agent-workspace.js
@src/settings.html
@src/settings-page.js
@tests/test-manage-skill.js
@tests/test-skills-http-api.js
@docs/product/ai-skills.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: zip 导入端到端纵切 —— readRawBody → 解压校验 → importId 预览 → 唯一落盘实现 → 回读</name>
  <files>ai-skills-manager.js, ai-manager.js, main.js, src/settings.html, src/settings-page.js, tests/test-skills-import.js, tests/helpers/make-malicious-zip.js</files>
  <read_first>
    - `ai-skills-manager.js`：`:1-30`（文件头依赖纪律注释 —— **本任务必须改它**：原文写「不得直接引入 SDK 的传递依赖」，而 D-14 把 `yaml` 提升为直接依赖、本阶段还要首次引入 `yauzl` ⇒ 注释须显式豁免这两个包并写明理由，否则后续开发者会按旧注释把它们删掉）、`:48-54`（`LIMITS` —— **一字不改**）、`:83-86`（`SKILL_SIZE_WALK_MAX_ENTRIES` / `_DEPTH` = 5000 / 16 —— CR-4 的深度口径要对齐 16 这个先例值）、`:115-160`（`yamlScalar` 与惰性 require 三件套 `getAiMemoryManagerLazy` / `getAgentWorkspaceLazy` / `getBuiltinSkillsSeederLazy` 的形态）、`:1200-1250`（`MANAGE_SKILL_ERROR`（**恰 11 键，不改**）/ `makeManageSkillError` / `isSeededName` —— 新码表与错误构造照它的形状）、`:1269-1300`（`validateManagedSkillName` —— **改名与导入共用这一份**）、`:1537-1545`（`scanSkillText` —— T3 的并列函数在此附近落）、`:1564-1610`（`buildSkillFileText` / `validateSkillFileSize` / `sandboxErrorCode` —— 64 KiB 闸的口径来源）、`:1634-1666`（`atomicWriteSkillFile` 的「失败必须清理 + 错误折叠」语义）、`:1677-1703`（`managedSkillPaths` 的「路径恒由 `path.join` 计算」与 `countManagedSkills` 的读盘计数）、`:1782-1873`（`createManagedSkill` 的九步顺序 —— 导入侧的对应顺序见 PATTERNS ⑯）、`:2030-2070`（`deleteUserSkill` 与 `env.remove(..., {recursive:true})`）、`:2109-2145`（`module.exports` 的分段注释风格 —— 新导出另起一段 `// 51 新增（导入面：…）`）
    - `agent-workspace.js`：`:82-84` / `:102-104` / `:113-115`（`getTmpDir` / `getSkillsDir` / `getManagedSkillsDir` —— **路径只经这三个访问器**）、`:205-231`（`createSandboxEnv` 的 `root` / `deny` / `guardResult`，以及 **51-01 新增的 `guardForWriteResult`** —— 落盘的 `env.renameFile` 走它）、`:265-352`（`writeFile` / `renameFile` / `createDir` / `remove` / `createTempDir` 的 Result 契约：**永不 throw，失败编码进 Result**）、`:327-336`（`createTempDir` 会再加一层 `tmp-` 前缀 —— 与本任务 `fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))` 的命名形状**不同**，清扫正则必须与**实际产物**成对）
    - `node_modules/yauzl/README.md`：`:94`（默认选项 `{autoClose:true, lazyEntries:false, decodeStrings:true, validateEntrySizes:true, strictFileNames:false}`）、`:373-425`（`openReadStream` 与 `validateEntrySizes` 的语义边界）；`node_modules/yauzl/index.js`：`:13-62`（导出面与 `*Promise` 打开器强制 `lazyEntries:true`）、`:437-443`（`eachEntry` 的 `cleanup()` → `close()`）、`:871-884`（`validateFileName` 的**三**条判据 —— 只拦 `\` / 盘符与绝对路径 / `..`）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### Pattern 3`（`:445-465` 错误的四类处理面）、`### Anti-Patterns to Avoid`（`:467-474`）、`### Common Pitfalls` 1-3（`:493-526`）、`### 实测 1`（`:582-677` 的 ④ 样本实测表与 ⑤ `validateEntrySizes` 语义表）、`### 实测 2` 的「技能根定位」表（`:743-767`）、`### 实测 3`（`:793-837` 的零预检与 413 实测）、`### 实测 4`（`:839-876` 同设备与两段 rename）、`### 实测 6`（`:932-974` 的 `yaml` 解析形态逐条）、`### Code Examples` 例 2-6（`:1236-1352`）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### ai-skills-manager.js`（`:45-326` 的 ①-⑱ —— 新段落必须逐条对齐其形态）、`### main.js`（`:517-657` 的 ①-⑥）、`### src/settings-page.js`（`:661-829` 的 ①-⑦）、`### src/settings.html`（`:833-874`）、`### tests/test-skills-import.js`（`:916-963`）、`## 三条「本阶段容易抄错」的点`（`:1195-1244`）
    - `.planning/phases/51-zip/51-CONTEXT.md`：D-01 / D-02 / D-03 / D-04 / D-06 / D-07 / D-10 / D-11 / D-18 与 CR-3 / CR-4 / CR-6 / CR-7 / CR-11
  </read_first>
  <action>
    1. **`ai-skills-manager.js` —— 常量与校验器（零 electron 依赖不变）**：
       - 改文件头依赖纪律注释：显式豁免 `yauzl`（zip 解析）与 `yaml`（frontmatter 解析，与 SDK 同版精确钉 `2.9.0`），写明「这两个是本模块的直接依赖，不是 SDK 的传递依赖的顺手引入」。
       - 新增 `const IMPORT_LIMITS = Object.freeze({ MAX_ENTRY_BYTES: 1 * 1024 * 1024, MAX_TOTAL_BYTES: 32 * 1024 * 1024, MAX_ENTRIES: 2000, MAX_COMPRESSION_RATIO: 100, MAX_NESTING_DEPTH: 16, PREVIEW_LIST_LIMIT: 50, MAX_PENDING_IMPORTS: 3, IMPORT_TTL_MS: 10 * 60 * 1000 })`。**`LIMITS` 一字不改**（六类限额与原五项不同量）。`MAX_NESTING_DEPTH = 16` 与 `SKILL_SIZE_WALK_MAX_DEPTH` 同值 —— 注释里登记 CR-4 的口径：**按「定位到的技能根」相对计**，不是按压缩包根（实测包根口径下 `anthropics/skills` 的 `docx` 恰好 depth 8，会把官方技能正好打满）。
       - 新增 `const IMPORT_SKILL_ERROR = Object.freeze({ … })`，**一次定义 20 键**（后续计划只用不增）：`UNSUPPORTED_URL: ''unsupported_url''`、`DOWNLOAD_FAILED: ''download_failed''`、`REDIRECT_LIMIT: ''redirect_limit''`、`NOT_A_ZIP: ''not_a_zip''`、`INVALID_ZIP: ''invalid_zip''`、`UNSUPPORTED_ZIP64: ''unsupported_zip64''`、`UNDECODABLE_ENTRY: ''undecodable_entry''`、`UNSAFE_ENTRY: ''unsafe_entry''`、`LIMIT_EXCEEDED: ''limit_exceeded''`、`SKILL_ROOT_COUNT: ''skill_root_count''`、`FRONTMATTER_INVALID: ''frontmatter_invalid''`、`OVERSIZE: ''oversize''`、`INJECTION_DETECTED: ''injection_detected''`、`SEEDED_CONFLICT: ''seeded_conflict''`、`IMPORT_EXPIRED: ''import_expired''`、`IMPORT_NOT_FOUND: ''import_not_found''`、`TOO_MANY_PENDING: ''too_many_pending''`、`READBACK_FAILED: ''readback_failed''`、`UNKNOWN: ''unknown''`，另加 `CONFLICT_UNRESOLVED: ''conflict_unresolved''`（本计划中间态用：`skills/<name>/` 已存在且未给出冲突选择 ⇒ 拒绝而非静默覆盖）。新增 `makeImportError(code, message, extra)`（照 `makeManageSkillError` 形状）。
       - 新增 `function validateEntryName(rawNameBuffer, decodedName)` → `null | { code, reason }`，**判据顺序不可换**：① 原始字节面（`rawNameBuffer.toString(''latin1'')` 里的 `[\x00-\x1F\x7F]`）—— **必须先判原始字节**（CP437 会把 `\x01` 解码成 `☺`，只看解码值会漏）；② 解码后含 `\`（默认 `strictFileNames:false` 时库已把 `\` 归一成 `/`，此处判的是漏网形态）；③ `path.posix.normalize(decodedName)` **之后**判 `=== ''..''` / `startsWith(''../'')` / `startsWith(''/'')`（**不得**用字符串 `includes(''..'')`）；④ 盘符 `/^[A-Za-z]:/`、UNC `startsWith(''//'')`；⑤ NTFS ADS（含 `:`）；⑥ 每个路径段不得以空格或点结尾；⑦ 不含空段（`//`）；⑧ 双向控制符 `/[\u202A-\u202E\u2066-\u2069]/`。**空串 / 仅 `/` / 仅 `.`** 一律拒绝（SEC-03#empty）。返回的 `reason` 是**可读原因**（不含被拒内容原文以外的解释性文字；被拒名字本身可出现在**开发日志**里，但**不得**进用户可见文案 —— 拒绝文案走 `51-06` 的闭合白名单表）。
       - 新增 `function normalizedEntryKey(name)` → `name.normalize(''NFD'').toLowerCase()`（查重键，SEC-03#encoding）。
       - 新增 `function parseSkillFrontmatter(text)` → `{ ok, name, description, allowedTools, reason }`：用 `yaml` 的 `parse`（`require(''yaml'')`），**catch 后返回 `ok:false` 并带 `YAMLParseError.message`（自带 line/column）**；解析值必须是**非 null 对象且非数组**（`parse` 对空字符串返回 `null`、顶层数组是合法 YAML 但取 `.name` 得 `undefined` —— 两者都要显式判）；`name` 只有 `typeof === ''string''` 且非空才算存在；`description` 缺失或 `trim()` 等于空字符串 ⇒ `ok:false`（SDK 会**整条丢弃**该技能 ⇒ 必须当硬错误）；`allowed-tools` 归一化：字符串（逗号分隔）或数组 ⇒ 数组；`null` ⇒ 视为缺失。
       - 新增 `function deriveImportName(frontmatter, rootRel)`：优先 `frontmatter.name`；缺失则取技能根的目录 basename；两者都拿不到合法值 ⇒ `makeImportError(FRONTMATTER_INVALID, …)`。得到的 name **必须**过 `validateManagedSkillName`（**复用那一份，不写第二份正则**），拒绝原因原样带出。
    2. **`ai-skills-manager.js` —— 解压与全树校验（T1 落「最小但真实」的一组，T2 补全）**：
       - 新增 `async function readSkillPackageEntries(zipPath, { destDir })`：`const zf = await yauzl.openPromise(zipPath)`（`decodeStrings` / `validateEntrySizes` 保持默认 `true`）；**在 `for await (const entry of zf.eachEntry())` 循环体内**逐条：entry 数闸（`> MAX_ENTRIES` ⇒ `LIMIT_EXCEEDED`，message 含限额名与当前值）、`validateEntryName`、symlink 与非普通文件判定（**先看 `entry.versionMadeBy >>> 8 === 3` 再判 `(entry.externalFileAttributes >>> 16) & 0xF000`**：`0xA000` ⇒ `UNSAFE_ENTRY`（symlink）、既非 `0x8000` 也非 `0x4000` 且非 0 ⇒ `UNSAFE_ENTRY`（chrdev / fifo / socket））、`uncompressedSize === 0xFFFFFFFF || compressedSize === 0xFFFFFFFF` ⇒ `UNSUPPORTED_ZIP64`、单 entry 字节闸、累计字节闸（用 `uncompressedSize` 预检 + 边读边累加）、`entry.fileName.endsWith(''/'')` ⇒ 目录条目跳过写入、`!entry.canDecodeFileData()` ⇒ `UNDECODABLE_ENTRY`（加密 / 不支持的压缩方法）。写盘用 **Node `fs`（不是沙箱 `env.writeFile`）** —— 理由（D-04）：yauzl 给的是流；symlink 判定与「最近已存在祖先 realpath」复核都要在写**之前**自己控制；解压目标是 `mkdtemp` 出来的全新空目录（前缀内不可能有预埋链接）。**目录必须先 `fs.mkdirSync(path.dirname(target), { recursive: true })`**。
       - 新增 `async function extractAndValidatePackage(env, { importDir, scopeRel })`（**入参契约先行定死，`51-05` 要复用同一形状**）：**调用方**负责建 `importDir`（`fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))`）并把包写到 `<importDir>/pkg.zip`；本函数只做 ① 解压到 `<importDir>/pkg/`（**目录必须先 `fs.mkdirSync(..., { recursive: true })`**）、② 剥**恒定**顶层前缀 `<repo>-<ref>/`（GitHub zipball 实测恒包一层；**先**剥再定位，否则会解出 `skills-main/skills/foo/SKILL.md` = 幽灵技能来源之一）、③ 调 `readSkillPackageEntries`、④ 返回 `{ importDir, pkgRoot: <importDir>/pkg, entries }`。**不得**用 `os.tmpdir()`、**不得**用 `path.join(__dirname, …)`、**不得**在本函数内再建一个临时根（临时根的创建只在调用方一处）。⚠️ 与之并列的还有一个**同为「准备 pkgRoot」**的函数 `prepareFromRawFile({ importDir, name, text })`（`51-05` 交付；**签名不含依赖注入形参** —— 它只用 Node `fs` 写盘，不需要 `deps`）：它把单文件写进 `<importDir>/pkg/<name>/SKILL.md` 并返回同一个 `pkgRoot` 形状 ⇒ 两条准备路径之后的**全树校验 / 技能根定位 / 预览 / 落盘**必须是同一段代码。
       - 新增 `function locateSkillRoot(pkgRoot, scopeRel)`（**同步、纯 fs 只读**）：① `scopeRel` 非空 ⇒ 限域到该子路径；② 限域目录下**直接**有 `SKILL.md` ⇒ 该目录为根；③ 否则**全包扫描**收集所有 `<任意深度>/SKILL.md`（**不得**限定 `skills/*` —— CR-7：`template/SKILL.md` 在包根）；④ 命中数 ≠ 1 ⇒ `makeImportError(SKILL_ROOT_COUNT, message)`，message 含**实测条目数**、一个**可直接复制的 `tree/<ref>/<path>` 示例**，并在 `>1` 时列出（最多 20 个）根路径。
    3. **`ai-skills-manager.js` —— 预览与唯一落盘实现**：
       - 新增 `async function collectPreviewStats(env, pkgRoot, rootRel)`：**一次遍历**产出 `tree`（全量条目 `{ path, kind }`，同一父节点下**目录在前**、同级按路径字符串的 **UTF-16 码元序**（JS `<`/`>`，**不用 `localeCompare`** —— 它随机器 locale 变，会破坏预览可复现性））、`fileCount` / `dirCount` / `bytes`（只累加 `kind === ''file''`，**目录的 `size` 是 inode 数据不得累加**）、`maxFileBytes`、`depth`（**按技能根相对计**）；脚本清单 `scripts` 的判据 = **扩展名白名单**（`.py .sh .bash .zsh .js .mjs .cjs .ts .rb .pl .php .ps1 .bat .cmd .exe .dylib .so .node`）**或** POSIX 可执行位；遍历**必须**有 `MAX_ENTRIES` / `MAX_NESTING_DEPTH` 护栏（超限截断并在 preview 里带 `truncated: true`，**禁静默截断**）。
       - 新增 `async function buildImportPreview(env, { pkgRoot, rootRel, origin, seededNames })`：组装 `{ name, description, tree, fileCount, dirCount, bytes, maxFileBytes, scripts, scan, allowedTools, conflict, limits }`。`description` 是**原文**（不净化、不截断语义 —— 预览的意义就是让用户判断净化是否误伤）；`allowedTools` 形状 `{ status: ''ok'' | ''missing'' | ''unparsed'', value }`；`scan` 形状 `{ injection: { hit: false }, heuristic: [] }`（T3 填启发式；**注入类命中在本函数之前就已整包拒绝** ⇒ 到了这里只能是 `hit:false`）；`conflict` 本计划**只出** `{ kind: ''none'' }` 或 `{ kind: ''taken'' }`（后者由调用方在 `51-04` 细化为三档）；`limits` = `IMPORT_LIMITS` 与 `LIMITS` 合并投影出的 **camelCase 数值表**（前端零字面量）。
       - 新增 `async function importUserSkill(env, { srcDir, name, conflict, newName }, { seededNames })` —— **唯一落盘实现**：① seeded 同名 ⇒ `SEEDED_CONFLICT`（**拒绝、不提供覆盖**）；② `skills/<resolvedName>/` 已存在 ⇒ 本计划返回 `CONFLICT_UNRESOLVED`（**不静默覆盖**；`51-04` 在这里接三档事务）；③ `managed-skills/<name>/` 已存在 ⇒ 不覆盖（`51-04` 给「永久遮蔽」提示）；④ 数量闸（`MAX_USER_SKILLS`，**读盘**统计，失败按 0 计）；⑤ **组装后全文的 64 KiB 闸**：导入不重写文件 ⇒ 判据对象 = 包内 `SKILL.md` 的**实际字节数**（注释里逐字写明这条：49 的不变式是「写侧权威字节闸口的判据对象 = 组装后的全文」，导入路径不组装 ⇒ 包内文件即组装结果）；⑥ `env.renameFile(<pkgRoot>/<rootRel>, <skillsDir>/<resolvedName>)`（同设备、原子；`.tmp/` 与 `skills/` 同 root ⇒ 无 EXDEV，T2 有一条实跑断言）；⑦ **回读验证**：`refreshSkills(env, { disabled, rootDirs })` 恰一次后确认 `<resolvedName>` 出现在快照里且 `filePath` 指向 `skills/`；不在 ⇒ 把刚移入的目录 `rename` 回 `<importDir>` 并抛 `READBACK_FAILED`（`extra.diagnostics` 带 `diagnostics` 原文**含 path**）；⑧ 返回导入报告 `{ name, source: ''zip'', bytes, files, scripts, scan, warnings, allowedTools }`。
       - `module.exports` 另起一段 `// 51 新增（导入面：…）`，导出：`IMPORT_LIMITS`、`IMPORT_SKILL_ERROR`、`validateEntryName`、`parseSkillFrontmatter`、`deriveImportName`、`locateSkillRoot`、`extractAndValidatePackage`、`buildImportPreview`、`importUserSkill`（`makeImportError` **刻意不导出** —— 与 `makeManageSkillError` 同款：判定全在模块内，调用方只消费带 `code` 的错误对象）。
    4. **`ai-manager.js` —— 两阶段转发（判定零实现）**：
       - 构造期/字段：`this._skillImports = new Map()`（`Map<importId, { dir, preview, createdAt, origin }>`）。
       - `async previewSkillImport({ kind, buffer, url })`：`kind === ''zip''` ⇒ ① `fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))` 建 `importDir`、② 把 `buffer` 写到 `<importDir>/pkg.zip`（**上传路径也走磁盘**，使三条来源共用同一条解压入口 —— 见 key-decisions）、③ `extractAndValidatePackage(env, { importDir, scopeRel: null })`、④ `locateSkillRoot` + `collectPreviewStats` + `buildImportPreview`、⑤ `this._skillImports.set(crypto.randomUUID(), { dir: importDir, preview, createdAt: Date.now(), origin: ''zip'' })` → `return { importId, preview }`；`kind === ''url''` ⇒ **本计划直接抛 `UNSUPPORTED_URL`**（`51-05` 替换为真实分流；这是**真实失败**而不是占位静默）。**用户可见的失败文案里不得出现临时路径**。
       - `async commitSkillImport({ importId, conflict, newName })`：`this._skillImports.get(importId)` 命中校验（不命中 ⇒ `IMPORT_NOT_FOUND`）→ `importUserSkill(this.sandboxEnv, { srcDir, name, conflict, newName }, { seededNames: this.getSeededSkillNamesSafe() })` → **成功出口**：`await this.ensureSkillsFresh();`（**恰一次**）+ `windowManager.broadcast(''skills:changed'');`（**调用侧补播恰一次**，逐字复用 `uninstallUserSkill` 的形状与它 JSDoc 里的论证）→ 从 Map 删除 + `env.remove(dir, { recursive: true, force: true })` → 返回 `{ ...report, management: this.getSkillsForManagement() }`。
       - ⚠️ **`syncAgentSystemPrompt()` 的函数体逐字不得改**（补播只在调用侧）；`getSeededSkillNamesSafe()` 是 seededNames 的**唯一**来源（新方法不得直接调 `getSeededSkillNames()`）。
    5. **`main.js` —— `readRawBody` + `import` 分支**：
       - 新增 `const MAX_SKILL_PACKAGE_BYTES = 32 * 1024 * 1024;`（紧邻 `MAX_JSON_BODY_BYTES` / `MAX_JSON_BODY_BYTES_LARGE`，并补注释：**与 `MAX_JSON_BODY_BYTES_LARGE`（书签导入专用）同数不同量**，与 P7 的单 entry 1 MB 同理；命名不得复用）。
       - 新增 `function readRawBody(req, res, { maxBytes = MAX_SKILL_PACKAGE_BYTES } = {})`：逐行照抄 `readJsonBody` 的形状，**唯一差异**是 `chunks.push(chunk)` + `Buffer.concat(chunks)`。三条教训一个不落：`canRespond` 能力探测（`res` 缺失只 reject、不崩主进程）、`tooLarge()` 里 `sendJson(res, 413, { error, limit })` + `req.resume()`（**禁** `req.destroy()`、**禁** `Connection: close`）、`data` 监听器内 **`if (rejected) return;` 先于累加 size**（顺序即判据）。**禁止**「先 `arrayBuffer()` 再判大小」。
       - `handleSkillsApi` 内（`token` 鉴权之后、`uninstall` 分支之后、404 之前）新增 `route === ''import'' && req.method === ''POST''` 分支：`!aiManager` ⇒ 503；按 `Content-Type` 分流 —— `application/zip` / `application/x-zip-compressed` / `application/octet-stream` ⇒ `const buf = await readRawBody(req, res, { maxBytes: MAX_SKILL_PACKAGE_BYTES })` → `sendJson(res, 200, await aiManager.previewSkillImport({ kind: ''zip'', buffer: buf }))`；`application/json` ⇒ `const body = await readJsonBody(req, res, { maxBytes: MAX_SKILL_PACKAGE_BYTES })` → `mode === ''url''` 走 `previewSkillImport({ kind: ''url'', url: body.url })`、`mode === ''commit''` 走 `commitSkillImport({ importId: body.importId, conflict: body.conflict, newName: body.newName })`；其它 `Content-Type` ⇒ 400 带 `code: ''unsupported_url''`-级别的显式码（**不得**落到 `readJsonBody` 的 JSON.parse 上产生误导性的 400）。**handler 仍是零判定转发层**：不得出现 `kind ===` / `source ===` / `isSeededName` / `managed-skills` 这类判定素材（只有上面明列的 **Content-Type / `mode` 分流**这一层入参解析，这正是 D-03 把分流放在入参解析层的全部理由）。
       - **同批刷新调用点计数**：import 分支用了 `readJsonBody(req, res, …)` ⇒ `tests/test-skills-http-api.js` 里 `total === 59` 的断言改为 `60`、`>= 57` 的守卫保持、注释里的实测值同步（**三处账本一起改**）。`res.writeHead(` 的 14 处基线**不变**（新分支一律走 `sendJson`）。
    6. **`src/settings.html` + `src/settings-page.js` —— 最小导入入口（零新增 CSS）**：
       - `settings.html`：在技能管理区标题行右侧加 `button#skillImportOpen.btn.btn-secondary.btn-sm`（文案 `导入技能`；**不是** `.btn-primary`）；区内加隐藏 `<input type="file" id="skillImportFile" class="skill-import-file-input" accept=".zip,application/zip">`（隐藏走**类规则**，**绝不写 markup 内联 style**）；在 `#skillManageConfirm` **之后**加 `div#skillImportModal.ai-modal-overlay`（内层 `div.ai-modal` + 标题 + `p#skillImportStatus` + `div#skillImportPreview` + 动作区两个按钮），**类全部是既有类**（`.ai-modal-overlay` / `.ai-modal` / `.btn.btn-secondary.btn-sm`）⇒ 本任务**零新增 CSS**。
       - `settings-page.js`（全部代码放进既有的 `/* Phase 50 skill-manage region: start/end */` **之内或紧跟其后的一对新 region 标记** `/* Phase 51 skill-import region: start/end */`）：`importZipAsSkill(file)`（`skillsApi(''import'', { method: ''POST'', headers: { ''Content-Type'': ''application/zip'' }, body: file })` —— `File` 直接作 body，**不设 base64、不 FileReader**；Content-Type **显式**给出，因为 OS 识别出的 `file.type` 可能是 `application/x-zip-compressed`）、`renderSkillImportPreview(preview)`（**零 HTML 字符串模板**：全树 `document.createElement` + `textContent` + `el.title = …`）、`commitSkillImport(importId)`、弹框开/关 + `Escape` + 焦点归还（半成品：完整状态机与必勾 / 冲突 / 截断归 `51-06`）；文件输入的 `change` 处理**必须** `finally { input.value = '' }`（否则同一文件第二次选不触发；⚠️ `<input>` **没有** `reset()` 方法）。失败反馈走既有 `setSkillManageHint`（弹框内失败走 `#skillImportStatus`）。
    7. **`tests/helpers/make-malicious-zip.js`（新建，纯 Node、零外部依赖）**：导出 `buildZip({ entries })` —— 手写 local header / central directory / EOCD，支持：`stored` 与 `deflate`（`zlib.deflateRawSync`）、任意 `externalFileAttributes` 与 `versionMadeBy`、任意声明的 `uncompressedSize`（含 `0xFFFFFFFF`）、`data descriptor`（gpb bit3）、加密位（gpb bit0）、任意压缩方法号、UTF-8 标志位开关、任意 entry 名（含 NUL / 控制字符 / RTL / 尾随空格与点）；另导出 `skillPackage({ name, description, body, extraEntries })` 组一个**合法**单技能包。**不得**依赖 `python3` / `zip` 命令行（新机器必须能跑）。
    8. **`tests/test-skills-import.js`（新建套件，`node:test` 自跑形态）**：文件骨架照抄 `tests/test-manage-skill.js`（头注释含「用法: node tests/test-skills-import.js」、`require(''node:test'')`、`readSource`、`withTempRoot(t)`（`setWorkspaceDir(临时根)` + `aiSkills._resetCacheForTest()` + 复原）、`makeEnv`）。**seededNames 必须显式注入**（纯 Node 下 `getSeededSkillNamesSafe()` 降级为 `[]` ⇒ seeded 用例会假绿）。本任务落的用例：
       - **tracer 端到端**：`buildZip` 造一个合法单技能包 → `previewSkillImport({ kind: ''zip'', buffer })` → 断言返回 `{ importId, preview }` 且 **`skills/<name>/` 不存在**；再 `commitSkillImport({ importId })` → 断言 `skills/<name>/SKILL.md` 存在、内容与包内逐字一致、`refreshSkills` 后快照里能查到该 name 且 `filePath` 在 `skills/` 下（**这就是「回读验证」的机器判据**）。
       - `importUserSkill(` 出现位置扫描（**P9 第 5 条的机械判据**）：`ai-skills-manager.js` 恰 1 处（定义）、`ai-manager.js` 恰 1 处（调用）、`main.js` 与 `src/settings-page.js` 各 0 处。
       - 三种来源共用一条解压入口的源码判据：`yauzl.openPromise(` 在 `ai-skills-manager.js` 恰 1 处。
       - `preview` 不落盘的负向断言（对比 commit 前后的 `fs.existsSync` 差分，**不是**断言「没调用过 fs」）。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js && node --check ai-manager.js && node --check main.js && node --check src/settings-page.js && node --check tests/helpers/make-malicious-zip.js && node --check tests/test-skills-import.js</automated>
    <fails_when>non-zero exit，或 stderr 出现 `SyntaxError`</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const mgr=fs.readFileSync("ai-skills-manager.js","utf8");for(const t of ["IMPORT_LIMITS","IMPORT_SKILL_ERROR","function validateEntryName(","function parseSkillFrontmatter(","function locateSkillRoot(","async function extractAndValidatePackage(","async function buildImportPreview(","async function importUserSkill("])if(mgr.indexOf(t)===-1)bad.push("ai-skills-manager.js 缺 "+t);if(!/LIMITS = \{[\s\S]*?MAX_SKILL_MD_BYTES: 64 \* 1024,[\s\S]*?MAX_USER_SKILLS: 50,[\s\S]*?SKILLS_PROMPT_CHAR_BUDGET: 8000,[\s\S]*?MAX_MANAGED_SKILLS: 50,[\s\S]*?MAX_SKILL_DESCRIPTION_CHARS: 1024,\s*\}/.test(mgr))bad.push("LIMITS 五项被改动（只允许加项，本阶段零改动）");const am=fs.readFileSync("ai-manager.js","utf8");for(const t of ["async previewSkillImport(","async commitSkillImport("])if(am.indexOf(t)===-1)bad.push("ai-manager.js 缺 "+t);const main=fs.readFileSync("main.js","utf8");for(const t of ["function readRawBody(req, res, { maxBytes = MAX_SKILL_PACKAGE_BYTES } = {})","const MAX_SKILL_PACKAGE_BYTES = 32 * 1024 * 1024;"])if(main.indexOf(t)===-1)bad.push("main.js 缺 "+t);if(bad.length){console.error("导入面主干缺失:\n"+bad.join("\n"));process.exit(1)}console.log("导入面主干 ok（常量/校验器/解压/定位/预览/唯一落盘/转发/raw body）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `导入面主干 ok`，或 stderr 列出任意一条缺失项</fails_when>
    <automated>node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const cnt=(f,tok)=>{const t=stripC(fs.readFileSync(f,"utf8"));return (t.match(new RegExp(tok.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),"g"))||[]).length};const a=cnt("ai-skills-manager.js","importUserSkill("),b=cnt("ai-manager.js","importUserSkill("),m=cnt("main.js","importUserSkill("),s=cnt("src/settings-page.js","importUserSkill(");if(a!==1)bad.push("ai-skills-manager.js 的 importUserSkill( 应恰 1 处（唯一落盘实现的定义），实测 "+a);if(b!==1)bad.push("ai-manager.js 的 importUserSkill( 应恰 1 处（唯一调用点），实测 "+b);if(m!==0)bad.push("main.js 出现 importUserSkill( "+m+" 处（落盘不得下沉到 handler）");if(s!==0)bad.push("src/settings-page.js 出现 importUserSkill( "+s+" 处（前端不得落盘）");const z=cnt("ai-skills-manager.js","yauzl.openPromise(");if(z!==1)bad.push("yauzl.openPromise( 应恰 1 处（三种来源共用一条解压入口），实测 "+z);if(bad.length){console.error("单一落盘实现判据不合格:\n"+bad.join("\n"));process.exit(1)}console.log("单一落盘实现 ok（定义 1 / 调用 1 / handler 与前端各 0 / 解压入口 1）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `单一落盘实现 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node --check main.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const main=stripC(fs.readFileSync("main.js","utf8"));const bad=[];const i=main.indexOf("function readRawBody(req, res,");if(i===-1){bad.push("缺 readRawBody")}else{const j=main.indexOf("\n  }\n",i);if(j===-1)bad.push("readRawBody 的函数体未闭合（窗口判据失效）");const b=main.slice(i,j===-1?i+2600:j);for(const t of ["Buffer.concat(","req.resume()","canRespond","BODY_TOO_LARGE"])if(b.indexOf(t)===-1)bad.push("readRawBody 缺 "+t);if(b.indexOf("req.destroy(")!==-1)bad.push("readRawBody 用了 req.destroy()（会让客户端拿不到 413）");if(/headers\s*=\s*\{[^}]*[Cc]onnection/.test(b))bad.push("readRawBody 设了 Connection 头（同上）");const first=b.indexOf("if (rejected) return;");const sizeAt=b.indexOf("size += ");if(first===-1||sizeAt===-1||first>sizeAt)bad.push("data 监听器内的顺序不对：必须「先判 rejected 早退」再累加 size")}const h=main.indexOf("route === \x27import\x27");if(h===-1)bad.push("handleSkillsApi 缺 import 分支");else{const hEnd=main.indexOf("sendJson(res, 404, { error: \x27Not Found\x27 })",h);if(hEnd===-1)bad.push("handleSkillsApi 的 404 兜底不在 import 分支之后（窗口判据失效）");const hb=main.slice(h,hEnd===-1?h+3000:hEnd);if(hb.indexOf("readRawBody(req, res")===-1)bad.push("import 分支未使用 readRawBody");if(hb.indexOf("MAX_SKILL_PACKAGE_BYTES")===-1)bad.push("import 分支未显式声明 maxBytes（50 D-16 的交接要求）")}if(bad.length){console.error("传输面不合格:\n"+bad.join("\n"));process.exit(1)}console.log("传输面 ok（Buffer.concat + req.resume + 无 destroy/Connection + 顺序 + import 分支显式上限）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `传输面 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出（调用点计数未同批刷新会在此转红）</fails_when>
  </verify>
  <acceptance_criteria>
    - 六条门禁全绿；`node tests/test-skills-import.js` 的 `# fail 0`；`node tests/test-skills-http-api.js` 的 `# fail 0`（含刷新后的调用点计数 60）。
    - **可失败性（本门禁组落地前必须逐条在当前树上实跑，并记录失败项全部属于「新代码未落地」类）**：① 「导入面主干缺失」在本仓当前树上报 8 条（`IMPORT_LIMITS` … `importUserSkill(` 全缺）；② 「单一落盘实现」当前树报 `ai-skills-manager.js 应恰 1 处（实测 0）` —— **注意它同时含两个方向**（多了也报），所以「把落盘复制到 handler」会转红；③ 「传输面」当前树报缺 `readRawBody`；④ 三条门禁都用**函数名 / token 定界**（不用固定字符数窗口），`readRawBody` 的窗口用「到下一个 `await readJsonBody(req` 为止」界定。
    - **负向 token 先剥注释再判（本轮修订）**：门禁四（传输面）里的 `req.destroy(` 与 `Connection` 头两条负向判据改在 `stripC`（与该计划门禁三同一份五态剥注释器、**保长替换** ⇒ 原文与剥离面下标一一对应、切片不错位）之后的 `main.js` 文本上判定。**理由**：本任务第 1 步逐字要求「超限只 `req.resume()`（**禁** `req.destroy()`、**禁** `Connection: close`）」，那条说明性注释在原文上判定会让门禁**恒红**。**一对验证（在合成实现上实跑）**：① 注释形态（含 `req.destroy()` 字样的说明性注释）⇒ `传输面 ok`、`exit 0`；② 单点变异（把 `req.resume()` 换成 `req.destroy()`）⇒ 报「readRawBody 用了 req.destroy()（会让客户端拿不到 413）」—— 判别力零损失。
    - **单点变异（执行者落地后必须自行跑，各确认转红后复原）**：① 在 `main.js` 的 import 分支里再写一次 `env.renameFile(..., skills/<name>)` ⇒ 「单一落盘实现」报 `main.js 出现 importUserSkill(`（改写成内联 rename 的那种复制形态会让该判据**不**转红 —— 因此**另外**有一条「解压入口恰 1 处」与 51-04 的「落盘唯一性」共同覆盖，执行者须在 SUMMARY 里如实记录这条判据**覆盖不到**的形态）；② 把 `readRawBody` 的 `req.resume()` 改成 `req.destroy()` ⇒ 传输面报 `用了 req.destroy()`；③ 交换 `if (rejected) return;` 与 `size += ` 的顺序 ⇒ 传输面报顺序错误。
    - **端到端可失败性**：tracer 用例的「commit 后 `skills/<name>/SKILL.md` 存在」若把 `commitSkillImport` 换成只返回不落盘 ⇒ 转红；「未 commit 前不产生目录」若把 preview 也落盘 ⇒ 转红。两条都是**差分断言**（比对 commit 前后的 `fs.existsSync`），不是「没调用过 fs」。
    - 六类限额与 `PREVIEW_LIST_LIMIT` 的数值**只出现在 `ai-skills-manager.js`**：`node -e` 断言 `src/settings-page.js` 不含 `32 * 1024 * 1024` / `2000` / `100` / `4194304` 之类导入限额字面量（前端零字面量）。
    - `syncAgentSystemPrompt()` 的函数体**逐字未改**：以本计划起点 sha 为基线（`BASE=$(cat .git/gsd-plan-head-before-51-03 2>/dev/null || git rev-parse HEAD)`），`git diff $BASE -- ai-manager.js` 中该函数所在区间**零行变化**（这是本计划唯一的「不许碰」区）。⚠️ 必须带基线 sha —— 本计划的改动在验收时尚未提交，裸 `git diff` 对该场景恒空。
  </acceptance_criteria>
  <done>设置页选一个 zip 能走完「预览（六字段骨架）→ 确认 → 落进 `skills/<name>/` → 回读可见 → 重扫 + 补播」；三条来源共用一条解压入口与一个落盘实现（源码可判）；`skills/<name>/` 已存在时**拒绝**而非静默覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: zip 全量校验（六类限额 / 逃逸族 / 归一化查重 / 两路 symlink / 落点 realpath）+ 夹具固化 + readRawBody 413 四组</name>
  <files>ai-skills-manager.js, tests/test-skills-import.js, tests/helpers/make-malicious-zip.js, tests/test-skills-http-api.js</files>
  <read_first>
    - `ai-skills-manager.js`：Task 1 落地后的 `readSkillPackageEntries` / `extractAndValidatePackage` / `collectPreviewStats` / `importUserSkill`（**逐行读** —— 本任务的闸必须补进**既有**实现，不得另起一套）、`validateEntryName`、`IMPORT_LIMITS`
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 1` 的 ④ 样本实测表（**逐行对齐**：symlink-file/dir/both、special-chrdev/fifo/socket、`win-dir-no-unixmode`、zip-slip 拦下/放行的 27 个样本、`collide-case` / `collide-nfc-nfd`、`bomb-ratio` / `bomb-many-entries` / `liar-usize-huge`、`zip64-forced`、`empty.zip`、`encrypted`、`method-bzip2`）与 ⑤ `validateEntrySizes` 语义表、⑥ data descriptor 形态；`### 实测 4`（`:839-876` 同设备 / 两段 rename / 回滚 / symlink 三形态）；`### 实测 5` 的技能根定位表（`:743-767`）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`## Code Examples` 例 1（`:1213-1234` 两种打包方式 —— Python `zipfile` 可完全控制 `external_attr`，手写 local/central/EOCD 表达别处表达不了的形态；**本任务要用纯 Node 复刻这两条能力，不得引入 python3 依赖**）、例 2（`:1236-1262` entry 名安全校验）、例 3（`:1264-1295` 逐 entry 解压含三处自建闸）
    - `tests/test-skills-http-api.js`：`:58-94`（`stripCodeComments` —— **逐字复用**）、`:102-108`（窗口切片写法）、`:312-360`（可注入 `rejectForm` 的内联服务器夹具）、`:410-445`（`postStream` 的真实字节流客户端 —— **不得**用 fetch，避免 undici 缓冲语义干扰堆判据）、`:450-551`（SEC-09 的 ①/①b/②/④ 四组）、`:553-617`（源码契约组）、`:645-690`（`res.writeHead(` 14 与 `await readJsonBody(req` 的计数断言 —— Task 1 已把 59 改成 60，本任务复核它仍在）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### tests/test-skills-import.js`（`:916-963` 的 ①-⑤）、`### tests/test-skills-http-api.js（增补）`（`:1000-1004`）、`## Pattern 6 的实测配方`（`:1176-1191`）、`### symlink 拒绝的语义（两处镜像）`（`:1104-1115`）
  </read_first>
  <action>
    1. **补齐 `readSkillPackageEntries` 的全部闸**（逐条对齐 SEC-02 / SEC-03 / SEC-04 / SEC-05）：
       - **压缩比闸**（独立于累计字节闸，**不得**只断言「上传闸拦下了」）：`uncompressedSize > 0 && compressedSize > 0 && uncompressedSize / compressedSize > MAX_COMPRESSION_RATIO` ⇒ `LIMIT_EXCEEDED`（message 含「压缩比」、当前比值与限额）。
       - **嵌套深度闸**：**按定位到的技能根相对计**（不是包根）；超 `MAX_NESTING_DEPTH` ⇒ `LIMIT_EXCEEDED`（message 含「嵌套深度」、当前深度与限额）。
       - **单 entry 字节闸的流内兜底**：`validateEntrySizes` 对 **deflate** 条目是**流中**才报错（实测：已流过 16 KiB 才报），**不能**替代自建计数器 ⇒ `openReadStreamPromise` 后用 `pipeline` 边写边累加，超限即 `rs.destroy()` 并抛 `LIMIT_EXCEEDED`。
       - **全量 entry 名 NFD + 小写归一化查重**：同一个包里两个 entry 的 `normalizedEntryKey()` 相等 ⇒ `UNSAFE_ENTRY`（`name_conflict` 语义，**拒绝整包**）。用例：`Skill/SKILL.md` 与 `skill/SKILL.md`（大小写）、`café`（NFC U+00E9）与 `cafe\u0301`（NFD）**同包**。
       - **解压后递归 `lstat` 复核整棵树**（D-17 第 2 层）：解压完成后用 `fs.readdirSync(dir, { withFileTypes: true })` 递归（形态镜像 `builtin-skills-seeder.assertNoSymlink`），`isSymbolicLink()` ⇒ **拒绝整包**（**不是**跳过该条目 —— 断言点是「整包拒绝」）；同时每个文件 `fs.realpathSync` 后确认仍在 `importDir` 内。**注意与 `measureSkillDir` 的语义相反**：后者对 symlink 是 `continue`（为统计），本处是**抛错**，两处不可混抄。
       - **落点复核用「最近已存在祖先的 realpath」**（D-17 第 3 层 / SEC-05）：落盘的 `srcDir` 与 `destDir` 的父目录链上最近已存在祖先做 `realpathSync` 后必须仍在 `skills/` 的 root 内（`env.renameFile` 已由 51-01 的写面判据保护，本处是**第二道**：显式断言，使「51-01 被回退」时本阶段仍有一条独立判据）。
       - **`SKILL.md` 64 KiB 闸**（D-11）：在**任何落盘之前**、按包内 `SKILL.md` 的**实际字节数**判定（导入不重写文件 ⇒ 包内文件即「组装后的全文」）；超限 ⇒ `OVERSIZE`（message 含限额与当前值）。
    2. **`importUserSkill` 的两段 `rename` 与「同设备」实跑断言**（本任务只补「无冲突路径」的原子性；三档冲突与回滚在 `51-04`）：断言 `.tmp/`、`skills/`、`mkdtemp` 产物三者 `dev` 相同（`fs.statSync(...).dev`），据此保证 `rename` 不 EXDEV；落盘失败时**不得留半成品**（失败分支 `env.remove(srcDir, { recursive: true })` 只在确实搬动过时执行）。
    3. **`tests/helpers/make-malicious-zip.js` 扩展**：补齐 CR-12 列出的**全部**形态 —— symlink（file / dir / 混装）、非普通文件（chrdev `0o020000` / fifo `0o010000` / socket `0o140000`）、Windows 造包（`versionMadeBy = 0x0014` ⇒ 高 16 位无 Unix mode）、逃逸族 **12 例**（`../evil.txt` / `a/b/../../../evil.txt` / `./../../evil.txt` / 绝对路径 / `C:/evil.txt` / `C:\evil.txt` / `\\srv\share\e` / `//srv/share/e` / `a//tmp/x.txt` / `evil.txt:ads` / `ctl\x01evil/SKILL.md` / `nul\x00evil/SKILL.md` / `evil.` / `evil ` / `x\u202ey/SKILL.md`）、冲突名 4 例（大小写 2 + NFC/NFD 2）、炸弹 3 例（高压缩比 / 2500 条目 / 深目录）、`0xFFFFFFFF` 声明、加密条目（gpb bit0）、不支持的压缩方法（method = 12）、空包（只有 EOCD，22 B）、data descriptor（gpb bit3）、正常 zip64。**每个样本在生成器里附一句注释说明它对应的实测条目**。
    4. **`tests/test-skills-import.js` 增补用例**（逐条对齐 SEC-02..05 与 CR-12）：
       - **symlink 两路分别测**：central-directory 属性路（改 `externalFileAttributes` 的样本）与解压后递归 `lstat` 路（用「属性正常但磁盘上真是链接」的样本 —— 通过 `fs.symlinkSync` 在**解压前**就把链接预置进 `pkg/` 目录，模拟库 attribute 解析漏网）；两路都断言**整包拒绝**且 `skills/` 下**零新增**。
       - 逃逸族 **12 例逐类一例**（含六类实测漏网）；空 entry 名（空串 / 仅 `/` / 仅 `.`）各一例。
       - 归一化查重两例（大小写 / NFC-NFD）。
       - **六类限额逐类一例** + `0xFFFFFFFF` 一例 + 加密与不支持方法各一例 + 空包一例 + data descriptor 一例（**断言它被正常接受** —— 实测不构成额外风险，尺寸取自 central directory）+ 正常 zip64 一例（接受）。
       - **越界对照组真跑一次**（SEC-05）：在临时根**之外**再建一个目录，跑一次全部恶意样本，最后断言该目录下**零新文件**（**不是**断言「没写过」）；并对「父目录链最近已存在祖先为 symlink」的形态断言落点复核拒绝。
       - **压缩比闸的独立归因**（CR-5）：对高压缩比炸弹样本，断言失败码是 `LIMIT_EXCEEDED` 且 message 含**压缩比**（而不是断言「上传闸拦下了」）—— 另有一条对**上传闸**的独立断言（32 MiB 上限不含 101 KB 的炸弹 ⇒ 上传闸放行它，证明它对炸弹零贡献）。
       - **深度口径的边界样例**（CR-4）：造一个「技能根相对深度 = 16」的包（**接受**）与一个 = 17 的包（**拒绝**），使「按技能根相对计」这条口径有正反两例；断言实现里**不存在**按包根计深的判据。
    5. **`tests/test-skills-http-api.js` 增补**：`readRawBody` 的 ①（超限 413 + body 可 `JSON.parse` + `limit === MAX_SKILL_PACKAGE_BYTES`）/ ①b（带 `content-length` 的同一请求同样 413）/ ②（413 路径**不产生 `unhandledRejection`**）/ ④（**反向对照**：`rejectForm: ''destroy''` **拿不到** 413 —— 失败信息逐字写「destroy 形态把 socket 在响应可读之前撕掉 ⇒ 客户端拿不到 413」）四组 + 源码契约组（三参签名 / `canRespond` 降级 / `sendJson(res, 413` / `req.resume()` / 无 `req.destroy(` / 无 `Connection` 头 / `data` 监听器内的顺序），`maxBytes` 换成 `MAX_SKILL_PACKAGE_BYTES`。**复用**既有 `startSec09Server` / `postStream` 夹具形态，**不得**用 `fetch` 发请求。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js && node -e 'const fs=require("fs");const src=fs.readFileSync("ai-skills-manager.js","utf8");const bad=[];const need=[["MAX_COMPRESSION_RATIO","压缩比闸"],["MAX_NESTING_DEPTH","嵌套深度闸"],["normalizedEntryKey","归一化查重键"],["isSymbolicLink(","解压后递归 lstat 复核"],["realpathSync(","落点/整树 realpath 复核"],["MAX_SKILL_MD_BYTES","SKILL.md 字节闸"],["0xFFFFFFFF","zip64 声明特例"],["canDecodeFileData(","加密/不支持方法闸"]];for(const [t,name] of need)if(src.indexOf(t)===-1)bad.push("缺 "+name+"（token: "+t+")");const i=src.indexOf("function validateEntryName(");const b=src.slice(i,src.indexOf("\n}\n",i));if(b.indexOf("normalize(")===-1&&b.indexOf("posix.normalize(")===-1)bad.push("entry 名判据未在 posix.normalize 之后判（字符串 includes(\"..\") 会被 a/../b 之外的同形形态绕过）");if(b.indexOf("includes(\"..\")")!==-1||b.indexOf("includes(\x27..\x27)")!==-1)bad.push("entry 名判据用了字符串 includes(\"..\")（必须归一化后判）");if(bad.length){console.error("校验闸缺失:\n"+bad.join("\n"));process.exit(1)}console.log("校验闸 ok（压缩比/深度/归一化查重/lstat/realpath/64KiB/zip64/可解码 八项齐备）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `校验闸 ok`，或 stderr 列出任意一条缺失项</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 45（LESSONS：一个样本一例的矩阵不允许压成个位数），或进程以非零退出</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/test-skills-import.js","utf8");const bad=[];for(const s of ["symlink","chrdev","fifo","socket","0xFFFFFFFF","zip64","data descriptor","nf-c","NFD","compress","ratio","depth","existsSync"])if(t.indexOf(s)===-1)bad.push("用例组缺 token: "+s);if(t.indexOf("dev")===-1)bad.push("缺同设备（dev）断言（两段 rename 的 EXDEV 前提）");const g=fs.readFileSync("tests/helpers/make-malicious-zip.js","utf8");if(g.indexOf("deflateRawSync")===-1)bad.push("夹具生成器缺 deflateRawSync（无法造炸弹样本）");if(g.indexOf("externalFileAttributes")===-1&&g.indexOf("externalFileAttr")===-1)bad.push("夹具生成器缺 external_attr 控制（无法造 symlink 样本）");if(bad.length){console.error("用例/夹具覆盖不足:\n"+bad.join("\n"));process.exit(1)}console.log("用例与夹具覆盖 ok（symlink/非普通文件/炸弹/归一化/同设备/deflate/external_attr）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `用例与夹具覆盖 ok`，或 stderr 列出任意一条缺失 token</fails_when>
  </verify>
  <acceptance_criteria>
    - 四条门禁全绿；`node tests/test-skills-import.js` 的 `# fail 0` 且 `# tests` ≥ 45；`node tests/test-skills-http-api.js` 的 `# fail 0`。
    - **可失败性（落地前逐条在当前树上实跑）**：门禁一当前树报 8 项缺失（八条闸全未落地），**无**「合规实现也修不掉」的断言；门禁四当前树报缺 `tests/test-skills-import.js` 的 token（新套件尚未落地）。执行者落地后自行做的单点变异（各确认转红后复原）：① 把压缩比闸的判据换成 `uncompressedSize > MAX_TOTAL_BYTES` ⇒ 炸弹用例转红（**证明「压缩比闸拦下了」而不是「上传闸拦下了」**）；② 把深度判据改成按包根计 ⇒ 深度正反两例会同时转红；③ 把 `posix.normalize` 换成 `includes(''..'')` ⇒ `a/../b` 之外的样本仍绿但**归一化前形态**的样本会转红（记录哪一类样本对该变异不敏感 —— 这正是「判据顺序不可换」的证据）。
    - **两路 symlink 各自独立**：把 central-directory 属性判定注释掉 ⇒ 属性路用例转红而 `lstat` 路用例仍绿（两条判据互不遮蔽）；反之亦然。
    - **越界对照组是「真跑一次」**：该用例必须在**同一轮**里跑完全部恶意样本，并在最后统计 root 外目录的文件数 —— 断言 `=== 0`。把 `validateEntryName` 的某类判据去掉后，该用例应转红（**至少对 `../` 那一类验证一次**）。
    - 夹具生成器**零外部依赖**：`node tests/test-skills-import.js` 在**没有** `python3` / `zip` 命令行的环境下必须能跑（生成器只用 `zlib` 与手写字节）。
    - `tests/test-skills-http-api.js` 的调用点计数断言仍为 Task 1 刷新后的值（60），`res.writeHead(` 仍为 14。
  </acceptance_criteria>
  <done>zip 一侧的六类限额、逃逸族十二类、六类实测漏网、归一化查重、两路 symlink 判定、解压后递归 `lstat`、落点最近已存在祖先 realpath、64 KiB 闸与 zip64 特例全部落地并有逐类用例；越界对照组真跑一次；夹具生成器固化进仓库且零外部依赖；`readRawBody` 的 413 四组与源码契约组增补完成。</done>
</task>

<task type="auto">
  <name>Task 3: 技能域威胁扫描（SKILL_THREAT_PATTERNS 三类 + description/body 双扫 + 语料零误伤回归）</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js, tests/test-skills-import.js, tests/fixtures/skill-corpus/*.md</files>
  <read_first>
    - `ai-skills-manager.js`：`:1518-1560`（`scanSkillText` 与它对 `getAiMemoryManagerLazy().scanInjectionPatterns` 的复用 —— **本任务只扩表、不加接线**）、`:1200-1250`（错误码与 `makeManageSkillError`）、Task 1 落的 `buildImportPreview`（`scan` 字段的落点）
    - `ai-memory-manager.js`：`:34-63`（`INJECTION_PATTERNS` / `CREDENTIAL_PATTERNS` 两张表的形态与逐条 name）与 `:76-108`（`scanInjectionPatterns` 的 `{safe, reason?}` 返回形状）—— **本任务不得改这个文件**（第二份 hard-fail 表必然独立漂移）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 7`（`:976-1045`）—— **本任务的唯一权威**：A 类 9 条 / B 类 9 条 / C 类 8 条的**逐条正则**与正样本示例、`A5` 必须写 `>[&]?`、`B6` 必须**双向**、`B7` 必须要求 `$` 前缀、`B8` 必须用 `printenv` 而非裸 `env`、`C2` 的否定语境前瞻（**lookbehind `(?<!...)` 实测无效**）、以及「❌ 看似危险但实测不能收」表的 11 条与其**实测误伤数**
    - `skills-builtin/find-skills/SKILL.md`（**逐字读**）—— `- 本技能不会建议绕过任何确认提示；任何绕过确认的做法都属于越界。` 这一行是 C 类护栏的**唯一**回归夹具来源（Pitfall 7）
    - `ai-attachments-manager.js` 的 `DENIED_HOME_PREFIXES`（`SKILL_THREAT_PATTERNS` 的词表来源之一 —— ⚠️ **当前未导出**，若要用**必须**在 `ai-attachments-manager.js` 里导出它，但那会引入一条跨模块耦合；本任务**允许**改为在 `ai-skills-manager.js` 内联一份**只含路径前缀的常量**并注释其出处，二选一须在 SUMMARY 记明）
    - `tests/test-ai-skills.js`：`M` 组与 `L` 组（`:1-60` 的文件骨架与 `mkdtempSync` 夹具；**新增用例的落点与命名风格**）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### 威胁扫描（复用单点，扩表不加接线）`（`:1151-1153`）与 `### ai-skills-manager.js` 的 ⑨（`:168-180`）
  </read_first>
  <action>
    1. **`ai-skills-manager.js` 新增 `SKILL_THREAT_PATTERNS`**（**分表**：不得塞进 `scanInjectionPatterns` 的 hard-fail 表 —— 三类的效力是「不拒绝、高亮 + 必勾」）：按 `51-RESEARCH.md` 实测 7 的 PASS 清单逐条落（A 类 9 条 / B 类 9 条 / C 类 8 条），每条带 `{ id: ''A1''…, class: ''exfil'' | ''credential'' | ''confirm_bypass'', pattern, name }`；**必须逐字保留研究实测过的形态**：`A5` 写 `/>[&]?\s*\/dev\/(tcp|udp)\//i`、`B6` 双向、`B7` 要求 `$` 前缀、`B8` 用 `printenv`、`C2` 用**前置否定词前瞻**（`/^(?![^\n]{0,40}(不会|不得|禁止|不要|不建议|绝不|不能))[^\n]{0,40}(绕过|跳过|忽略)[^\n]{0,8}(确认|审批|授权|权限检查)/`）。表头注释写明：**启发式、降低概率、不是安全边界**；三类**不拒绝**、由用户必勾确认；**收 A3（`scp`/`rsync` 到远端）但在文档点名它会命中合法部署技能**（它不是零误伤）。
    2. **`ai-skills-manager.js` 新增 `function scanSkillThreats(text, field)`**（**并列于** `scanSkillText`，返回命中列表而**不** throw）：返回 `[{ id, class, name, field }]`。同时导出。
    3. **接线（单点）**：在 `buildImportPreview` 里对 **description 原文** 与 **body 原文** 各跑一次并填入 `preview.scan.heuristic`（每条带 `field` 使「双扫」可判）；`preview.scan.injection` 仍恒为 `{ hit: false }`（注入类命中在更早的阶段就整包拒绝了）。**注入 / 凭据的硬拒判据落在解压校验阶段**（`frontmatter` 解析之后、落盘之前）：description 跑**两组**（`includeCredentials: true`）、body **只跑注入组**（`includeCredentials: false`）—— 这是 49 D-08 的分字段口径，**不得调换**；命中 ⇒ `INJECTION_DETECTED`（整包拒绝）。⚠️ **不得**把技能域三类接进这条 hard-fail 路径。
    4. **`tests/fixtures/skill-corpus/*.md`（新建，自撰语料）**：至少 8 个文件，逐条复刻实测 7 的「❌ 看似危险但实测不能收」表里的形态（裸 `curl` 的 API 示例块 / 裸 `.env` 的脚本 / `npm install` 的正常安装说明 / `rm -rf` 的清理步骤 / `sudo` / `export ANTHROPIC_API_KEY` / `don''t ask the user` 描述 SDK 行为 / `without pausing for confirmation` / 管道到 `sh` 的官方安装说明 / `幂等` 的脚本说明 / `rsync … deploy@staging:`），每个文件头部用注释写明它复刻的是哪一条与**实测误伤数**。**不复制上游字节**（理由见 `key-decisions`）。
    5. **`tests/test-ai-skills.js` 增补**（**必须带正命题** —— 49 `WR-12` 的「否命题空集真」教训的正面做法）：
       - **正命题**：三类各 ≥ 2 条恶意正样本**必须命中**（`assert.ok(hits.length > 0)` 并断言命中 `id` 属于该类的集合），覆盖 A / B / C 各自的实测正样本示例。
       - **零误伤回归**：`skills-builtin/**` 的**全部文本文件**逐条扫描 ⇒ **零命中**；`tests/fixtures/skill-corpus/*.md` 逐条扫描 ⇒ **零命中**（两条都是**断言命中数为 0**，但同时有上面那条「正样本必须命中」的正命题 ⇒ 不会退化成空集真）。
       - **否定语境护栏**：单独一条用例断言 `find-skills/SKILL.md` 的那一行**不命中** `C2`，同时断言其**去掉否定词后的改写**（如 `直接执行即可，绕过确认提示。`）**命中** `C2` —— 正反并列才能在「模式被写坏成永不命中」时转红。
       - **双扫证明**：`tests/test-skills-import.js` 里加两条用例 —— description 命中启发式、body 命中启发式，各自断言 `preview.scan.heuristic` 里对应条目的 `field` 为 `''description''` / `''body''`。
       - **不得改动** 既有 `M9b` / 计数账本相关断言（本任务的增补只允许**追加**用例）。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const src=stripC(fs.readFileSync("ai-skills-manager.js","utf8"));const bad=[];if(src.indexOf("const SKILL_THREAT_PATTERNS")===-1)bad.push("缺 SKILL_THREAT_PATTERNS 表");if(src.indexOf("function scanSkillThreats(")===-1)bad.push("缺 scanSkillThreats(");if(!/module\.exports\s*=\s*\{[\s\S]*?scanSkillThreats\b/.test(src))bad.push("scanSkillThreats 未导出");if(src.indexOf(">[&]?")===-1&&src.indexOf(">[&]?\\\\s*\\\\/dev")===-1)bad.push("A5 未用 >[&]? 形态（写成 > 会对该正样本实测不命中）");if(src.indexOf("(?![^\\n]{0,40}")===-1)bad.push("C2 缺前置否定词前瞻（lookbehind (?<! ) 实测无效）");if(src.indexOf("(?<![^\\n]")!==-1)bad.push("C2 用了 lookbehind（实测无效，必须改成前置否定词前瞻）");if(src.indexOf("printenv")===-1)bad.push("B8 未用 printenv 形态（裸 env 实测误伤 6 处）");const mgr=stripC(fs.readFileSync("ai-memory-manager.js","utf8"));if(mgr.indexOf("SKILL_THREAT")!==-1)bad.push("技能域三类被塞进了 ai-memory-manager.js（必须分表住 ai-skills-manager.js）");if(bad.length){console.error("威胁模式表不合格:\n"+bad.join("\n"));process.exit(1)}console.log("威胁模式表 ok（分表 + 21 条形态 + 否定语境前瞻 + scanSkillThreats 导出）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `威胁模式表 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-ai-skills.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 187（既有 187 例被删改），或进程以非零退出</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`tests/test-ai-skills.js` 的 `# tests` ≥ 187 + 新增数且 `# fail 0`（既有 187 例**零删改**）。
    - **正命题（硬要求）**：三类各有 ≥ 2 条恶意正样本**必须命中** —— 存在一条「把某类模式改成永不命中」即可让对应正命题转红；同时存在「`skills-builtin/**` + 自撰语料零命中」的零误伤断言（两条同在对 ⇒ 不会退化成空集真）。
    - **否定语境护栏的正反并列**：`find-skills/SKILL.md` 那一行不命中 **且** 其去否定词的改写命中（执行者至少对 `C2` 做一次「删掉 `(?!…)` 前瞻」的变异，确认 `find-skills` 那条**转红**，记录在 SUMMARY）。
    - **负向 token 先剥注释再判（本轮修订）**：门禁一里的 `(?<![^\n]`（lookbehind 形态）与 `SKILL_THREAT`（分表归属）两条负向判据改在剥注释面判定 —— 本阶段把「**lookbehind 实测无效**」写成**实测结论注释**是预期行为（`AGENTS.md` 的「五个坑」逐字要求登记它），原文判定会让门禁**恒红**。**一对验证（合成实现上实跑）**：① 注释形态（C2 注释里写明 `lookbehind (?<![^\n]…) 实测无效`）⇒ `威胁模式表 ok`、`exit 0`；② 单点变异（把 lookbehind 真写进 `SKILL_THREAT_PATTERNS` 的正则）⇒ 报「C2 用了 lookbehind（实测无效，必须改成前置否定词前瞻）」。
    - **双扫可判**：description 与 body 各有一条命中用例且 `field` 正确 —— 只测一个字段会在「另一字段忘了扫」的实现上全绿。
    - **扩表的连带效果成文**：`SKILL_THREAT_PATTERNS` 落 `ai-skills-manager.js` 后，**49 的 `manage_skill` 写入路径自动获得技能域模式**（49 D-08 的交接契约兑现）—— 执行者须在 SUMMARY 里点名这条连带效果（`51-07` 负责写进 `docs/product/ai-skills.md` 与 `AGENTS.md`），**不得**把它当成 51 的越界而在本阶段把它从写入侧摘掉。
    - `ai-memory-manager.js` **零改动**：同一个基线 sha 下 `git diff $BASE -- ai-memory-manager.js` 为空（裸 `git diff` 恒空 ⇒ 无效判据）。
  </acceptance_criteria>
  <done>技能域三类威胁模式（21 条）落 `ai-skills-manager.js` 并与注入 / 凭据表分表；`scanSkillThreats` 返回命中列表且 `buildImportPreview` 对 description 与 body 双扫；`skills-builtin/**` 与自撰语料零命中、三类正样本必命中、`find-skills` 的否定句不被误伤。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 设置页 guest（`http://localhost:PORT/settings`）→ `POST /api/skills/import` | 携带任意二进制的**不可信** body；唯一授权是 URL 里的 `REALM_TOKEN` |
| 上传的 zip 字节 → yauzl → 临时解压目录 → `skills/` | 包内一切内容（entry 名 / 元数据 / 压缩声明 / 正文）都不可信；解压本身是「不可信字节 → 真实文件系统」的越界时刻 |
| 解压产物 → 沙箱落点 | 落盘后该目录会成为 AI 读取的技能根（其 `description` 无条件进每次请求的 system prompt） |
| 预览渲染 → 用户判断 | 预览里的一切字符串都来自不可信包（`51-06` 的前端注入纪律）；本计划只保证**服务端**不把不可信内容当判据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-11 | Tampering | zip-slip（`..` / 绝对路径 / 盘符 / UNC / 反斜杠 / ADS / 控制字符 / 尾随空格与点 / 空段 / RTL） | high | mitigate | `validateEntryName` 八条判据（**先判原始字节**再判解码值；`path.posix.normalize` **之后**判 `..`）；12 类样本逐类一例；越界对照组真跑一次断言 root 外零新文件 |
| T-51-12 | Tampering / Elevation | symlink entry 写出 root 外（yauzl 自身**不报错**） | high | mitigate | **两路独立**：central-directory 属性（先看 `versionMadeBy >>> 8 === 3` 再判 `(extAttr >>> 16) & 0xF000`）与解压后递归 `lstat` + `realpathSync`；两路都**拒绝整包**（断言点不是「跳过条目」）；落点再经 51-01 的写面判据做第三道 |
| T-51-13 | DoS | zip bomb（高压缩比 / 2500 条目 / 深目录） | high | mitigate | **三道独立闸**：central-directory `uncompressedSize` 预检、**压缩比闸**（`MAX_COMPRESSION_RATIO`）、**累计字节闸**（边读边累加 + 流内 `rs.destroy()`）；entry 数闸与深度闸（按技能根相对计）；磁盘上界 ≤ `MAX_TOTAL_BYTES` |
| T-51-14 | DoS / Tampering | `uncompressedSize === 0xFFFFFFFF` 静默通过（参与压缩比运算会得到 4 GiB 的荒谬值） | high | mitigate | 显式 `=== 0xFFFFFFFF` ⇒ `UNSUPPORTED_ZIP64`；加密条目与不支持的压缩方法经 `canDecodeFileData()` ⇒ `UNDECODABLE_ENTRY` |
| T-51-15 | Tampering | 名字碰撞覆盖（大小写 / NFC-NFD） | high | mitigate | 全量 entry 名 `normalizedEntryKey`（NFD + 小写）查重 ⇒ 冲突即拒绝整包；两例用例（大小写 / NFC-NFD） |
| T-51-16 | Tampering | 幽灵技能（frontmatter 解析失败但文件已落盘 ⇒ 加载管线整条丢弃） | high | mitigate | 落盘**前**自行 `yaml` 解析 + 类型检查（`null` / 数组 / 标量都显式判）+ 组装后全文（此路径 = 包内文件）64 KiB 预筛 + 落盘**后**经 `refreshSkills` 回读验证，失败即回滚 |
| T-51-17 | Tampering | 注入类内容经 `description` 进每次请求的 system prompt | high | mitigate | `scanSkillText` 硬拒（description 跑注入 + 凭据两组 / body 只跑注入组 —— 49 D-08 的分字段口径）；命中 ⇒ `INJECTION_DETECTED` 整包拒绝 |
| T-51-18 | Tampering | 拒绝路径静默（P12 / SKILL-06） | high | mitigate | 每条拒绝给机器可读码 + 可读原因（20 键码表一次定义）；限额类 message 含「哪个限额 + 当前值」；`51-06` 用闭合白名单表把码映射成文案且**不解析 message** |
| T-51-19 | Information Disclosure / DoS | `readRawBody` 无上限读入内存 ⇒ 主进程 OOM | high | mitigate | 累积中拒收（`if (rejected) return;` 先于累加）+ `req.resume()`（**不** `destroy`、**不** `Connection: close` ⇒ 413 可达）+ `Content-Length` 快路径（仅加速）；内存上界 = `maxBytes` ≈ 32 MiB（如实成文，不声称零增长） |
| T-51-20 | Denial of Service | import 分支的 400 路径把非 JSON body 喂给 `JSON.parse` 产生误导性错误 | medium | mitigate | 按 `Content-Type` 显式三分支（zip 三种 MIME / json / 其它），其它 MIME 直接给显式码；`readJsonBody` 的解析失败仍在既有 `catch` 里折叠成 400 + `code` |
| T-51-21 | Elevation of Privilege | 落盘路径被 handler 或前端接管 | high | mitigate | `importUserSkill` 是**唯一**落盘实现（全仓出现位置恰 2 处：定义 + 调用，`main.js` / 前端各 0）；路径恒由 `path.join` + `getSkillsDir()` 计算；目录名一律用**校验后的 name**（绝不用包内原始目录名拼路径） |
| T-51-22 | Tampering | 解压入口被复制成第二条（直链 SKILL.md 日后另写一条） | high | mitigate | `yauzl.openPromise(` 恰 1 处（源码判据）；三条来源都写 `<importDir>/pkg.zip` 再进同一入口；`51-05` 复用同一函数的 `scopeRel` 与 `<importDir>` 约定 |
| T-51-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本计划**零安装动作**（两个依赖由 `51-02` 落地并已过 blocking-human 合法性闸）⇒ 无新增供应链面 |
</threat_model>

<verification>
- 三个任务的全部 automated 门禁绿；新增套件 `tests/test-skills-import.js` 的 `# fail 0`；既有三个套件（`test-ai-skills` / `test-skills-http-api` / `test-builtin-skills-seeder`）的 `# fail 0`。
- **门禁可失败性**：每条源码扫描判据都在当前树上实跑过（失败项全为「新代码未落地」类），且每条都做过至少一次**单点变异**（记进 SUMMARY）。凡负向 token 禁令（`includes(''..'')` / `req.destroy(` / lookbehind）一律**先剥注释**再判（`stripCodeComments` 的词法级剥除，字符串与正则内的字面量不被误判为注释）。
- **本计划不覆盖**（留给后续计划，不得在 SUMMARY 里声称已交付）：三档冲突与两段 rename 回滚（51-04）、URL 分流与网络下载（51-05）、完整 UI（51-06）、文档与账本（51-07）、`/api/skills/import` 的真实 Electron 端到端（含 32 MiB 实测）—— 那条是 `51-VALIDATION.md` 的 Manual-Only 项。
- **诚实边界**：`preview` 的一切「结论」都是启发式 + 限额判据，**不是**「这个技能是安全的」背书（`51-06` 上屏、`51-07` 定稿）。
</verification>

<success_criteria>
- 设置页选一个 zip → 预览六字段 → 确认 → 落进 `skills/<name>/` → 回读可见（**这一步在 `51-06` 之前是开发态的最小入口，但路径是真的**）。
- 恶意 / 畸形包**整包拒绝**且**工作区外零写入**：symlink（两路）、逃逸族十二类、归一化查重、六类限额、zip64 声明、加密 / 不支持方法 —— 逐类有例。
- 三条来源汇进一个解压入口与一个落盘实现（源码可判）；`skills/<name>/` 已存在时拒绝而非静默覆盖。
- 技能域三类威胁模式分表落地、双扫、零误伤（内置技能 + 自撰语料）且正样本必命中。
- 每条拒绝路径有机器可读码 + 可读原因；限额文案含限额名与当前值。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-03-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-03）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 模块常量（导出） | `IMPORT_LIMITS`（8 键：`MAX_ENTRY_BYTES` / `MAX_TOTAL_BYTES` / `MAX_ENTRIES` / `MAX_COMPRESSION_RATIO` / `MAX_NESTING_DEPTH` / `PREVIEW_LIST_LIMIT` / `MAX_PENDING_IMPORTS` / `IMPORT_TTL_MS`） | `ai-skills-manager.js` |
| 模块常量（导出） | `IMPORT_SKILL_ERROR`（**20 键**，独立命名空间，不与 `MANAGE_SKILL_ERROR` 合并） | `ai-skills-manager.js` |
| 模块常量（导出） | `SKILL_THREAT_PATTERNS`（A 类 9 / B 类 9 / C 类 8） | `ai-skills-manager.js` |
| 内部函数 | `makeImportError(code, message, extra)`（**不导出**） | `ai-skills-manager.js` |
| 导出函数 | `validateEntryName(rawNameBuffer, decodedName)`、`normalizedEntryKey(name)`、`parseSkillFrontmatter(text)`、`deriveImportName(frontmatter, rootRel)`、`locateSkillRoot(pkgRoot, scopeRel)`、`scanSkillThreats(text, field)` | `ai-skills-manager.js` |
| 导出函数（异步） | `readSkillPackageEntries(zipPath, opts)`、`extractAndValidatePackage(env, opts)`、`collectPreviewStats(env, pkgRoot, rootRel)`、`buildImportPreview(env, opts)`、`importUserSkill(env, opts, { seededNames })`（**唯一落盘实现**） | `ai-skills-manager.js` |
| 类字段 + 方法 | `AIManager.prototype._skillImports`（Map）、`previewSkillImport({ kind, buffer, url })`、`commitSkillImport({ importId, conflict, newName })` | `ai-manager.js` |
| 常量 + 内部函数 | `MAX_SKILL_PACKAGE_BYTES`（32 MiB）、`readRawBody(req, res, { maxBytes })` | `main.js` |
| HTTP 路由 | `POST /api/skills/import`（三分支：raw zip / `mode:'url'` / `mode:'commit'`） | `main.js` |
| DOM id | `#skillImportOpen`、`#skillImportFile`、`#skillImportModal`、`#skillImportStatus`、`#skillImportPreview` | `src/settings.html` |
| 前端函数 + region | `importZipAsSkill()`、`commitSkillImport()`、`renderSkillImportPreview()`、`openSkillImportModal()` / `closeSkillImportModal()`、`/* Phase 51 skill-import region: start/end */` | `src/settings-page.js` |
| 测试套件（**新文件**） | `tests/test-skills-import.js` | `tests/` |
| 测试夹具生成器（**新文件**） | `tests/helpers/make-malicious-zip.js`（导出 `buildZip({ entries })` / `skillPackage(opts)`） | `tests/helpers/` |
| 测试语料（**新文件**） | `tests/fixtures/skill-corpus/*.md`（≥ 8 个自撰语料文件） | `tests/fixtures/` |

### 本阶段其余计划将新建（此处仅登记，避免在评审时被当作「应已存在」）

| 计划 | 符号 |
|------|------|
| 51-04 | `resolveImportConflict()`、`sweepSkillImportResidue(env)`、`AIManager.prototype.cancelSkillImport()`、`AIManager.prototype.sweepSkillImports()`、`IMPORT_LIMITS.IMPORT_TTL_MS` / `MAX_PENDING_IMPORTS` 的消费点、覆盖的 `skill-replace-<rand>` 备份目录形状 |
| 51-05 | `HOST_WHITELIST`、`classifyImportUrl(url)`、`downloadPackage(url, { maxBytes })`、`MAX_REDIRECTS`、`isWhitelistedHost(hostname)`（去 `www.` 前缀归一化）、`tests/test-skills-import-net.js` |
| 51-06 | `.skill-import-*` CSS 段（含 3 条作用域覆盖）、`#skillImportPanelLocal` / `#skillImportPanelUrl` / `#skillImportAck` / `#skillImportConflict` / `#skillImportGate` / `#skillImportCancel` / `#skillImportConfirm` / `#skillImportFetch` / `#skillImportPick`、`SKILL_IMPORT_ERROR_TEXT`（闭合白名单表）、`renderSkillImportScan()` / `renderSkillImportTree()` / `renderSkillImportConflict()`、`tests/uat-51-import-modal.js` |
| 51-07 | `docs/product/ai-skills.md` §十三【导入】+ 安全边界定稿、`docs/product/ai-agent-workspace.md` §七 的 SEC-10 行为变更条目、`AGENTS.md` 导入维护约定与测试行、counts-parity 的 `suites` / `cells` 扩展（12 → 20） |
