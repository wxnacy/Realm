---
phase: 51-zip
plan: 07
type: execute
wave: 6
depends_on: [51-06]
files_modified:
  - docs/product/ai-skills.md
  - docs/product/ai-agent-workspace.md
  - AGENTS.md
  - .planning/phases/51-zip/51-VALIDATION.md
autonomous: true
requirements: [USER-03, USER-04, USER-05, USER-08, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, SEC-08, SEC-10]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

key-decisions:
  - '**本计划不设 tracer**（纯文档 / 账本 / 验证契约收口，无新代码路径）。它是**阶段的最后一个 plan**：在它之前，所有行为面都已落地并被验证；本计划只做「把已实现的行为**如实**写下来 + 把例数账本刷新到实测值」'
  - '**文档的权威性是硬要求**：`AGENTS.md` 的维护约定条目是**跨阶段契约**（下一个人改导入面时必须照它同步文档）。因此本计划新增的导入维护约定必须写清三件事：① 产品说明权威 = `docs/product/ai-skills.md` 第十三节；② 改动**哪几类东西**必须同步（限额常量 / 错误码表 / 威胁模式表 / 白名单 / 技能根定位口径 / 覆盖语义 / 落点边界）；③ 本阶段的**四条不变式**（见下条）'
  - '**四条必须成文的不变式（照 `AGENTS.md` 既有的「不变式」写法）**：① **写侧权威字节闸口的判据对象 = 组装后的 `SKILL.md` 全文**；导入路径**不重写文件** ⇒ 判据对象 = **包内 `SKILL.md` 的实际字节数**（这条口径差必须在文档里说清，否则后续会有人「顺手」改成按正文长度判）；② **上传体积闸对 zip 炸弹零贡献**（CR-5 的方向纠正：正确表述是「合法包的解压总量 ≤ 32 MiB ⇒ 其压缩后体积必然 ≤ 32 MiB，故上传闸不会误杀合法包」）；**压缩比闸与累计解压闸是两道独立的自建闸**；③ **嵌套深度按「定位到的技能根」相对计**（不是包根 —— 包根口径下 `anthropics/skills` 的 `docx` 恰好 depth 8，会把官方技能打满）；④ **覆盖的临时备份不是版本历史**（成功后即刻删除）'
  - '**必须如实披露的五条诚实边界**（照 `AGENTS.md` 的「诚实边界必须成文」惯例，且**不是**只写进文档就算 —— 其中四条已在 UI 上屏）：① 威胁扫描三类的性质是**启发式、降低概率，不是安全边界**；② **DNS rebinding 残余风险**（`isPrivateHost` 走 Node `dns.lookup`，实际请求走 Electron `net.fetch`（Chromium 网络栈，可能走系统代理 / DoH / 不同 resolver）⇒ **被校验的 IP ≠ 被连接的 IP**，是先解析再请求的固有 TOCTOU；主机白名单把收益压到接近零，但那是**缓解**不是**消除**）；③ **SEC-10 加固后的行为变更**（`bash` 里「先 `ln -s <沙箱外路径>` 再写该链接」**会被拒** —— 这是**正确**行为但属行为变更；沙箱内自指链接仍放行；`exec` 的命令内容**不在**这一层校验，本加固**不**声称封闭 bash 写盘）；④ `allowed-tools` **当前运行时不被强制、仅供参考**；⑤ `skills.sh` / `www.skills.sh` / `objects.githubusercontent.com` **保留为未来兼容、本阶段无消费者**（CR-1 的如实表述 —— **不得**沿用「承载产品闭环」那条已被实测推翻的理由）'
  - '**错误码表必须与常量逐字一致**：第十三节的码表以 `IMPORT_SKILL_ERROR` 的**21 个值**为全集（`51-03` 一次定义的 20 键 + `51-04` 补的 `INVALID_NAME`），逐码给「何时触发 / 用户可见文案（或文案来源）」；**不得**只写一部分或用「等」字收尾（那是「有码无文案」的温床）。同时按 49/50 的「加码纪律」写明：新增码要**三处一起刷**（常量 + 本节码表 + `AGENTS.md` 测试清单）'
  - '**`MANAGE_SKILL_ERROR` 的恰 11 键锁不得被本计划触碰**：文档里要**显式**写明「导入错误码是**独立命名空间**（`IMPORT_SKILL_ERROR`），不得并入 `MANAGE_SKILL_ERROR`（后者被 `tests/test-manage-skill.js` 锁死为恰 11 键）」—— 这条写进文档是为了让下一个想「统一码表」的人先看到锁的存在'
  - '**威胁模式表扩表的连带效果必须成文**：`SKILL_THREAT_PATTERNS` 落 `ai-skills-manager.js` 后，**49 的 `manage_skill` 写入路径自动获得技能域模式**（49 D-08 的交接契约兑现）。不写进文档，后续会被当成 51 的意外副作用或被误读成「49 漏做了」'
  - '**counts-parity 的 `cells` 下限从 12 提到 20**：五个既有套件当前已在两个账本文件里贡献 **16 个单元**（实测 `cells=16`：`AGENTS.md` 5 + `docs/product/ai-skills.md` 11），两个新套件各需在**两个文件**里都有账本单元 ⇒ 下限 = 16 + 2×2 = **20**。⚠️ **cells 只是下限阈值；权威判据是紧随其后的逐套件覆盖断言**（`for (const base of Object.keys(meas)) if (!hit[base]) bad.push(…)`）—— 漏写任一新套件的账本单元会**指名**报「账本未覆盖 X」，而只把单元数凑够不会报错，故不得靠阈值判断齐不齐。⚠️ **新套件名不得含 `picker`**（该脚本按文件名是否含 `picker` 决定是否插 `--test`；两个新套件都含 `node:test`，用自跑形态即可）。⚠️ **本项目没有 `npm test`** —— 文档与账本里出现的任何跑法都必须写成具名 `node tests/<file>.js`（该口径由「跑法必须具名」的**正命题**判据守住，不用「禁止某字面量」的否命题 —— `AGENTS.md` 里本来就有一句引用它的纪律句）'
  - '**`51-VALIDATION.md` 的收口是本计划的判据之一，但「转正」的边界必须与 50 同款**：`wave_0_complete` 改 `true`（本阶段可自证）；`status` **保持 `draft`**、`nyquist_compliant` **保持 `false`** —— 两者由 `/gsd:validate-phase`（§6）判定，**不在本计划自证**（该文件的 frontmatter 注释逐字写着生命周期 `draft → validated (set by validate-phase §6)`；50 的先例同样保持 `status: draft` + 一条显式勾选说明「`nyquist_compliant: true` set in frontmatter —— **不在本阶段自证**」）。`## Per-Task Verification Map` 的 `Status` 列与 `**Approval:**` 行**保持 `pending`**（由 `/gsd:verify-work` 回填 —— 50 的矩阵上方逐字写着「该列由 `/gsd:verify-work` 回填，本文件不在计划内」）；本计划只负责回填 `Task ID` / `Plan` / `Wave` / `File Exists` 四列、勾上 `## Wave 0 Requirements` 与 `## Validation Sign-Off` 里**本阶段能自证**的条目，并把收口实测写进 `Manual-Only Verifications` 的对应行'

must_haves:
  truths:
    - '`docs/product/ai-skills.md` 有第十三节【导入】，覆盖：两种来源与一条管线 / 两阶段预览与句柄生命周期 / 六类限额（含深度按技能根相对计的口径说明）/ 逐 entry 校验与整包拒绝 / 威胁扫描两级效力与双扫 / 名称冲突三档 / 覆盖的备份与回滚（明确「不是版本历史」）/ 成功报告与失败码表 / 安全边界与已知限制（五条诚实边界）'
    - '`docs/product/ai-agent-workspace.md` §七 记录了 SEC-10 的行为变更（`ln -s` 出去再写被拒 = 正确行为 + 沙箱内自指链接仍放行 + `exec` 不在这一层校验），并明确「本加固不声称封闭 bash 写盘」'
    - '`AGENTS.md` 新增导入维护约定条目（产品说明权威 = 第十三节；改动哪些东西必须同步；四条不变式）+ 测试行补两个新套件的例数账本（**具名跑法** `node tests/<file>.js`）；counts-parity 命令的 `suites` 数组含两个新套件且 `cells` 下限为 20'
    - '`51-VALIDATION.md` 的 `wave_0_complete: true` 在**全量套件全绿 + counts-parity 通过**之后被设置，`## Validation Sign-Off` 里本阶段能自证的条目逐条勾上；而 `status` 保持 `draft`、`nyquist_compliant` 保持 `false`（**不在本计划自证** —— 由 `/gsd:validate-phase` 判定，与 50 先例同款），矩阵的 `Status` 列与 `**Approval:**` 行保持 `pending`（由 `/gsd:verify-work` 回填）'
    - '文档里**不出现**已被实测推翻的旧理由：不得写「`skills.sh` 承载 find-skills 的候选闭环」（实测 `find-skills` 输出的是 GitHub 仓库地址）、不得写「上传闸拦住炸弹」、不得写「威胁扫描通过 = 安全」、不得把 `TD-48-01` 写成已修'
    - '文档里出现的**每一个**限额数值 / 错误码 / 白名单条目都与实现**逐字一致**：码表全集 = `IMPORT_SKILL_ERROR` 的**全部 21 个值**（判据按 `Object.values()` 取值逐个查找，不写死数字）；六类限额值 = `IMPORT_LIMITS`；白名单六条 = `HOST_WHITELIST`'
  artifacts:
    - 'docs/product/ai-skills.md：新增 `## 十三、导入（zip + 网络地址）` 与其子节（能力 / 管线 / 限额 / 错误码表 / 安全边界与已知限制定稿）；§七 的测试清单补两个新套件'
    - 'docs/product/ai-agent-workspace.md：§七 新增 SEC-10 行为变更条目'
    - 'AGENTS.md：新增「用户技能导入的维护约定」条目 + 测试行补两个新套件例数'
    - '.planning/phases/51-zip/51-VALIDATION.md：Per-Task Verification Map 的 `Task ID` / `Plan` / `Wave` / `File Exists` 四列回填 / `Wave 0 Requirements` 勾选 / `Validation Sign-Off` 按 50 同款勾选（第六条 `nyquist_compliant: true` 保持未勾并注明「不在本阶段自证」）/ frontmatter 只改 `wave_0_complete: true`（`status` 保持 `draft`、`nyquist_compliant` 保持 `false`）'
  key_links:
    - '文档 ↔ 常量的一致性链：`IMPORT_SKILL_ERROR`（21 值）↔ 第十三节码表 ↔ `src/settings-page.js` 的 `SKILL_IMPORT_ERROR_TEXT`（三处一致，由 `51-06` 的双向覆盖判据 + 本计划的码表全量断言共同守住）'
    - '文档 ↔ 限额的一致性链：`IMPORT_LIMITS`（八键）↔ 第十三节限额表 ↔ preview 响应的 `limits`（前端零字面量）：三处的数值必须逐字一致'
    - '账本链：`AGENTS.md` 测试行 ↔ `docs/product/ai-skills.md` 的例数判据命令 ↔ 实测 `# tests`（counts-parity 是**可重跑**判据，不是人工核对）'
    - '维护约定链：`AGENTS.md` 的导入条目指向第十三节；第十三节末尾指向 `AGENTS.md`（双向引用，照 50 的 §十二 形态）'
  prohibitions:
    - statement: '不得把「威胁扫描未命中」写成安全背书，也不得把四条诚实边界降级为脚注 / 括号 / tooltip'
      status: unresolved
      verification: judgment
      reason: 'D-12 / P3：三类的性质是启发式；真正的边界是「导入预览确认 + 沙箱」。文档里的措辞直接决定后续维护者的判断 —— 一处「扫描通过即可放心」的措辞会让下一个人据此删掉必勾确认'
    - statement: '不得为「技能包签名 / 校验和 / 发布者验证」引入或宣称自建机制'
      status: unresolved
      verification: judgment
      reason: 'REQUIREMENTS 的 Out of Scope 明文；无服务端信任根。**canon-referral（ADR-550 D6）**：这条属 OWASP / ASVS V6 的「不自造密码学」，故**不**铸成 prohibition 项而是留一条 breadcrumb —— 即本条只作为文档纪律存在，不作为 `must_haves.prohibitions` 的承重项'
    - statement: '不得在 `51-VALIDATION.md` 里把未跑通的门禁勾成通过（含把人工面写成自动化面）'
      status: unresolved
      verification: judgment
      reason: '验证契约是下一阶段与里程碑审计的判据来源。把「真实 GitHub 端到端」这类 Manual-Only 项勾成自动化通过，会让审计得出错误的覆盖率结论（这是 50 收尾带出的「证据只活 /tmp」同族问题，只是方向相反）'
    - statement: '不得在文档里写入本阶段的临时路径 / 本机绝对路径 / 具体 sha 作为可复现前提'
      status: unresolved
      verification: judgment
      reason: '`.tmp/skill-import-<rand>` 一类的路径是运行期产物，写进文档会让读者以为那是稳定接口；本机绝对路径与 sha 会在别的机器上失效。文档应写**路径形状**（`agent-workspace/.tmp/skill-import-<6 位随机>/`）与**访问器名**（`getTmpDir()`）'
---

<objective>
把本阶段的**全部行为面如实写进产品文档与维护约定**，并把例数账本与验证契约收口到实测值：`docs/product/ai-skills.md` 新增第十三节【导入】+ 安全边界与已知限制章节定稿（ROADMAP 的 `Doc sync` 条款要求「在此定稿」）；`docs/product/ai-agent-workspace.md` 记录 SEC-10 的行为变更；`AGENTS.md` 新增导入维护约定与测试清单；counts-parity 的 `cells` 从 12 提到 20；`51-VALIDATION.md` 回填并把 `wave_0_complete` 置真（`status` / `nyquist_compliant` 与 50 同款**不在本阶段自证**，留给 `/gsd:validate-phase`）。

Purpose: 本阶段的五条诚实边界（启发式非安全边界 / DNS rebinding 残余风险 / SEC-10 行为变更 / `allowed-tools` 不被强制 / 白名单里两条「无消费者」的如实表述）**只在文档里活一次就会在下一个阶段被忘掉**。ROADMAP 把它写成了本阶段的交付条款，`AGENTS.md` 的维护约定是它的执行机制。

Output: `docs/product/ai-skills.md` 第十三节、`docs/product/ai-agent-workspace.md` §七 的 SEC-10 条目、`AGENTS.md` 的导入维护约定与测试行、`51-VALIDATION.md` 的回填与签字。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-RESEARCH.md
@.planning/phases/51-zip/51-VALIDATION.md
@.planning/phases/51-zip/51-UI-SPEC.md
@.planning/phases/51-zip/COVERAGE.md
@docs/product/ai-skills.md
@docs/product/ai-agent-workspace.md
@AGENTS.md
@ai-skills-manager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/product/ai-skills.md` 第十三节【导入】+ 安全边界与已知限制定稿</name>
  <files>docs/product/ai-skills.md</files>
  <read_first>
    - `docs/product/ai-skills.md`：现有十二节的标题与结构（`## 一、能力` … `## 十二、管理面（设置页 + /api/skills/*）`）、`### 11.3`（**错误码表范式** —— 新码表照它写）、`### 11.6` / `### 12.6`（**诚实边界小节的三处既有范式**）、`### 12.8 请求体体积闸（/api/* 通用机制）`（D-01 的交接面 —— 第十三节要与它衔接）、`### 12.9 维护约定与测试` 与 `## 七、测试与验证` 的例数判据命令（**Task 3 要改的 `suites` 数组与 `cells` 下限在这里**）
    - `.planning/phases/51-zip/51-CONTEXT.md`：`<domain>` 的十条交付 / `<decisions>` 的 D-01..D-19 与 CR-1..CR-12 / `<deferred>` 的「不在本阶段」（写文档时必须把 defer 项如实标出，避免被读成漏做）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 7` 的「❌ 看似危险但实测不能收」表（**11 条误伤形态与实测误伤数** —— 第十三节的「启发式非安全边界」一节必须引用它作为论据）、`### 实测 8` 的缺口与行为变更、`### 实测 3` 的 32 MiB / 413 实测、`### 实测 4` 的同设备与回滚、`### 实测 6` 的 frontmatter 解析形态
    - `.planning/phases/51-zip/COVERAGE.md`（白名单六条与四类 `OPT-OUT` 的如实表述 —— 第十三节要与它一致）
    - `ai-skills-manager.js`：`IMPORT_LIMITS` 八键与 `IMPORT_SKILL_ERROR` 二十键的**实际取值**（文档数值必须逐字取自这里，**不得**从 CONTEXT 的建议值抄）
  </read_first>
  <action>
    1. **新增 `## 十三、导入（zip + 网络地址）`**，子节逐条（每条都要给出**用户可见的行为**与**边界**，不给实现细节）：
       - **13.1 两种来源与一条管线**：本地上传 zip（`Content-Type: application/zip` 的 raw binary，非 base64）/ 网络地址（GitHub 仓库、`tree/<ref>/<path>` 子目录、`blob` 与 raw 直链）；三者都落到同一个临时包再进**同一条**解压校验与落盘管线；**直链 SKILL.md 不构成第二条落盘路径**。
       - **13.2 两阶段预览与句柄**：`preview` 只解压校验、**不写 `skills/`**，返回 `{ importId, preview }`；`commit` 只收 `importId` + 冲突选择 + 可选新名；`importId` 是**不透明句柄**（客户端**永远不能指定落点**）；TTL 10 分钟（过期给明确码「预览已过期，请重新选择文件」）、同时待确认上限 3、崩溃残留清扫（退出清本进程登记项 / 启动清陈旧残留）。
       - **13.3 六类限额**（数值取自 `IMPORT_LIMITS`，逐条给值）：单 entry 字节 / 累计解压字节 / entry 数 / **压缩比** / **嵌套深度（按技能根相对计 —— 必须解释为什么不是按包根计）** / `SKILL.md` 64 KiB（复用 `LIMITS.MAX_SKILL_MD_BYTES`）；另注明 `0xFFFFFFFF`（zip64 声明）显式拒绝、加密条目与不支持的压缩方法拒绝；**上限类失败文案一律含「哪个限额 + 当前值」**。
       - **13.4 逐 entry 校验与整包拒绝**：symlink entry（central directory 属性 + 解压后递归 `lstat` **两条判据路**）、非普通文件、逃逸族（`..` / 绝对路径 / 盘符 / UNC / 反斜杠 / NTFS ADS / 控制字符 / 尾随空格与点 / 空段 / 双向控制符）、全量 entry 名 **NFD + 小写**归一化查重 —— **任一命中即拒绝整包**（不是跳过该条目）。
       - **13.5 威胁扫描的两级效力**：注入 / 凭据类命中 ⇒ **整包拒绝**（`description` 跑两组、正文只跑注入组）；技能域三类（文件外发 / 凭据回显 / 诱导跳过确认）⇒ **不拒绝**，预览高亮 + 必勾确认。**必须写明为什么**：三类是启发式，而合法技能必然命中（`51-RESEARCH.md` 实测 7 的「不可收」表里，`claude-api` / `docx` / `pptx` / `mcp-builder` 这些**官方技能**会被裸 `curl` / 裸 `.env` / `npm install` 类模式命中 —— 硬拒会让它们永久无法导入且用户无法自救）；**必勾复选框后端不校验**（`importId` 已等价于「用户看过预览并确认」，后端再加一个布尔是「客户端能伪造就等于没有」的假安全）—— 这是**有意选择**，须写明。
       - **13.6 名称冲突三档**：与内置同名 ⇒ **拒绝导入**（内置名受保护）；与已有用户技能同名 ⇒ **三选一：覆盖 / 改名 / 取消**（不静默覆盖）；与 AI 自建（managed）同名 ⇒ **不允许覆盖**，提示「导入后将被永久遮蔽」，可改名 / 取消。改名的 name 走**与写入侧同一份**校验器。
       - **13.7 覆盖的备份与回滚**：覆盖 = 先把 `skills/<name>/` 移到临时备份 → 再移入新目录 → 成功后**即刻删除**备份；失败自动恢复旧目录；落盘后**回读验证**，失败即回滚并展示诊断原文。**必须显式写**：「这是**临时**备份，**不是**版本历史 / 恢复机制；导入不提供「撤销上一次导入」」（避免被读成「既然备份了就能恢复」）。
       - **13.8 成功报告与错误码表**：成功报告字段（`name` / `source` / `bytes` / `files` / `scripts` / `scan` / `warnings` / `allowedTools?`）；**错误码表**（`IMPORT_SKILL_ERROR` 的 **21 个值逐码一行**，给「何时触发」与「用户可见文案（或文案来源 = 后端原文）」）；并写明码表是**独立命名空间**、**不得**并入 `MANAGE_SKILL_ERROR`（恰 11 键被测试锁死）、**新增码要三处一起刷**。
       - **13.9 `allowed-tools`**：解析并展示，**在当前运行时不被强制**（SDK 的 `Skill` 类型无该字段 ⇒ 任何「按技能授权工具集」的说法都是虚假安全感）；缺失时不渲染该行、解析失败时渲染「（无法解析）」；免责标注恒与值同排。
       - **13.10 安全边界与已知限制（本阶段定稿 —— ROADMAP 的 Doc sync 条款）**：五条诚实边界逐条成文（启发式非安全边界 / DNS rebinding 残余风险 / SEC-10 行为变更指向 `ai-agent-workspace.md` / `allowed-tools` 不被强制 / 白名单里三条「保留为未来兼容，本阶段无消费者」）+ 两条既开未修的挂账如实标注（`TD-48-01` **仍开未修**，本阶段的责任是**不扩大缺口**；`#skillImportGate` 的禁用原因变化**不在 live region 内**）+ 「本阶段不做」清单（多技能勾选 = ECO-01、Contents API 列一层、技能包签名 / 校验和、版本历史 / 恢复按钮、加载期威胁扫描、`importId` 跨设置页实例共享）。
    2. **诚实用词纪律（写作时逐条核对，交付后由 Task 3 的判据扫描）**：不得出现「扫描通过 = 安全」「上传闸拦住炸弹」「`skills.sh` 承载候选闭环」「`TD-48-01` 已修」；DNS rebinding 段必须同时给出「缓解（白名单把收益压到接近零）」与「未消除」两侧；路径一律写形状（`agent-workspace/.tmp/skill-import-<6 位随机>/`）或访问器名（`getTmpDir()`），**不写**本机绝对路径。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const d=fs.readFileSync("docs/product/ai-skills.md","utf8");const bad=[];if(d.indexOf("## 十三、导入")===-1&&d.indexOf("## 十三")===-1)bad.push("缺第十三节【导入】");const sec=d.slice(d.indexOf("## 十三"));for(const t of ["两种来源","一条管线","importId","TTL","压缩比","嵌套深度","技能根","symlink","NFD","整包拒绝","启发式","必勾","覆盖","回滚","版本历史","IMPORT_SKILL_ERROR","allowed-tools","DNS rebinding","不是安全边界","不扩大"])if(sec.indexOf(t)===-1)bad.push("第十三节缺要点: "+t);for(const wrong of ["扫描通过即可放心","上传闸拦住","承载产品闭环","TD-48-01 已修","已修复引号逃逸"])if(sec.indexOf(wrong)!==-1)bad.push("第十三节出现已被推翻/不实的表述: "+wrong);const mgr=require("./ai-skills-manager");if(!mgr.IMPORT_SKILL_ERROR||!mgr.IMPORT_LIMITS){console.error("IMPORT_SKILL_ERROR / IMPORT_LIMITS 未导出（口径失效：先落 51-03/51-04）");process.exit(1)}const miss=Object.values(mgr.IMPORT_SKILL_ERROR).filter(v=>sec.indexOf(v)===-1);if(miss.length)bad.push("码表缺码: "+miss.join(","));for(const k of ["MAX_ENTRY_BYTES","MAX_TOTAL_BYTES","MAX_ENTRIES","MAX_COMPRESSION_RATIO","MAX_NESTING_DEPTH","PREVIEW_LIST_LIMIT","MAX_PENDING_IMPORTS","IMPORT_TTL_MS"])if(sec.indexOf(String(mgr.IMPORT_LIMITS[k]))===-1)bad.push("限额表缺 "+k+" 的当前值 "+mgr.IMPORT_LIMITS[k]);if(bad.length){console.error("第十三节不合格:\n"+bad.join("\n"));process.exit(1)}console.log("第十三节 ok（十个子节要点 + 常量全量码覆盖 + 八个限额值逐字 + 无过时表述）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `第十三节 ok`，或 stderr 列出任意一条缺失要点 / 缺失码 / 缺失限额值 / 过时表述</fails_when>
    <automated>node -e 'const fs=require("fs");const d=fs.readFileSync("docs/product/ai-skills.md","utf8");const bad=[];if(/\/Users\/wxnacy|\/Volumes\/|\/private\/var/.test(d))bad.push("文档出现本机绝对路径（应写路径形状或访问器名）");const i=d.indexOf("## 十三");const sec=d.slice(i);if(!/未消除|无法根除|固有|不是消除/.test(sec))bad.push("DNS rebinding 段未如实给出「未消除」一侧");if(!/缓解|压到接近零|接近零/.test(sec))bad.push("DNS rebinding 段未给出缓解论述");if(sec.indexOf("TD-48-01")===-1)bad.push("缺 TD-48-01 仍开未修的如实标注");if(sec.indexOf("live region")===-1&&sec.indexOf("禁用原因")===-1)bad.push("缺「禁用原因不在 live region 内」的已知边界");if(bad.length){console.error("诚实边界不合格:\n"+bad.join("\n"));process.exit(1)}console.log("诚实边界 ok（无本机路径 + rebinding 两侧 + 两条挂账）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `诚实边界 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 两条门禁全绿：**码表全量覆盖**（由 `require(''./ai-skills-manager'').IMPORT_SKILL_ERROR` 的取值逐个在文档中查找 —— 手抄漏一个即转红；**不写死键数**，`51-04` 加码后为 21 键）、**八个限额值逐字**（从 `IMPORT_LIMITS` 取值比对，避免写建议值）。
    - 十个子节要点齐备（能力 / 管线 / 句柄 / 限额 / 逐 entry 校验 / 扫描两级效力 / 冲突三档 / 覆盖与回滚 / 报告与码表 / `allowed-tools`） + 安全边界与已知限制定稿节。
    - **不得出现被推翻的旧理由**：`skills.sh` 承载闭环 / 上传闸拦炸弹 / 扫描通过即安全 / `TD-48-01` 已修 —— 四条各有一条负向判据。
    - **defer 项如实标注**：ECO-01（多技能勾选）、Contents API、签名 / 校验和、版本历史 / 恢复按钮、加载期扫描、跨实例共享 —— 在「本阶段不做」清单里逐条出现（否则会被读成漏做）。
    - **可失败性**：门禁一在本计划落地前必然转红（缺第十三节）；执行者落地后做一次单点变异 —— 从文档里删掉一个码字面量 ⇒ 「码表缺码」转红。
  </acceptance_criteria>
  <done>`docs/product/ai-skills.md` 新增第十三节【导入】，十个子节 + 安全边界与已知限制定稿节齐备，20 个错误码与 8 个限额值与实现逐字一致，五条诚实边界与两条挂账如实成文，且无一条被实测推翻的旧表述。</done>
</task>

<task type="auto">
  <name>Task 2: `AGENTS.md` 导入维护约定 + 测试行；`ai-agent-workspace.md` 的 SEC-10 行为变更</name>
  <files>AGENTS.md, docs/product/ai-agent-workspace.md</files>
  <read_first>
    - `AGENTS.md`：`## AI 工作区与 Bash 权限` 段的三块维护约定（技能发现与调用 / AI 自建技能 / 技能管理面 —— **条目的写法与四条硬约束的编号形态**）、其后的**测试行**（单行多套件 + 例数，形如 `node tests/<file>.js`（说明，N 例））、`### 打包排除项的前提` 条目、`### 开发正式环境差异 → 发布前必查`（新增依赖四条 —— `51-02` 已走过，本条只需在导入约定里交叉引用）
    - `docs/product/ai-agent-workspace.md`：`## 七、安全边界（诚实声明）`（`:122` 起 —— SEC-10 的行为变更在此披露）与其 §四 / §五（bash 三档策略的位置，撤销/收紧时要交叉引用）
    - `.planning/phases/51-zip/51-CONTEXT.md`：`<domain>` 的八条前置约束（维护约定必须把它们转成可执行的同步清单）
  </read_first>
  <action>
    1. **`AGENTS.md` 新增一条维护约定：「用户技能导入（zip + 网络地址）的维护约定」**，放在「技能管理面（设置页 + `/api/skills/*`）的维护约定」之后。内容：
       - **产品说明权威 = `docs/product/ai-skills.md` 第十三节**；改动**限额常量（`IMPORT_LIMITS` / `LIMITS`）/ 错误码表（`IMPORT_SKILL_ERROR`）/ 威胁模式表（`SKILL_THREAT_PATTERNS`）/ 主机白名单（`HOST_WHITELIST`）/ 技能根定位口径 / 冲突与覆盖语义 / 落点边界（`importId` 不透明）/ `/api/skills/import` 的 body 分流与体积上限**时，**必须同步该章节与该节测试清单**。
       - **四条硬约束**（编号，照既有条目的写法）：① 判定 / 校验 / 解压 / 落盘**全住 `ai-skills-manager.js`**（零 electron 依赖）⇒ `main.js` 的 handler 与 `ai-manager.js` 只做转发，**落盘实现全仓只有一个调用点**（源码扫描判据）；② **三条来源共用一条解压入口**（上传 / zipball / 直链 SKILL.md 都先落 `pkg.zip` 或 `pkg/`，再进 `extractAndValidatePackage` 或 `prepareFromRawFile`，之后是**同一段**后段）；③ **`LIMITS` 只允许加项**、导入限定值住 `IMPORT_LIMITS`、**端点与前端零字面量**；④ **覆盖的临时备份不得扩张成版本历史**；另加一条**码表隔离**：导入码不得并入 `MANAGE_SKILL_ERROR`（恰 11 键被测试锁死）。
       - **四条不变式**（逐字，见 key-decisions）：字节闸判据对象 / 上传闸对炸弹零贡献 / 深度按技能根相对计 / 备份不是版本历史。
       - **诚实边界五条**（指向第十三节 13.10 与 `ai-agent-workspace.md` §七）。
       - **本阶段踩过的坑（供后来者复用）**：`yauzl` 的 `eachEntry()` 会 close 整个 zipfile（开流必须在 `for await` 体内）、`validateFileName` 放行六类、`uncompressedSize === 0xFFFFFFFF` 静默通过、`yaml` 必须精确钉 `2.9.0`（`^` 会双实例）、威胁模式的「诱导跳过确认」类必须带**前置否定词前瞻**（lookbehind 实测无效，否则会命中内置 `find-skills` 自身）。
    2. **`AGENTS.md` 测试行补两个新套件**：按既有格式追加 `node tests/test-skills-import.js`（导入面：zip 校验 / 六类限额 / 冲突三档 / 覆盖回滚 / 句柄生命周期 / 错误码矩阵 / 单点落盘源码扫描，**N 例**）与 `node tests/test-skills-import-net.js`（网络面：URL 分流 / 白名单 / 逐跳 / 跳数 / magic bytes / 流式上限，**本地 stub server**，**M 例**），另在既有条目里补 `tests/test-agent-workspace.js` 的**新增例数**（写面加固用例组）、`tests/test-skills-http-api.js` 的 `readRawBody` 413 组、`tests/test-ai-skills.js` 的技能域模式组。⚠️ **例数一律取实测**（Task 3 会机械核对）；两个新套件的跑法一律写成具名 `node tests/<file>.js`，**不得**把 `npm test` 当作跑法。⚠️ 注意 `AGENTS.md` 别处本来就有一句**引用**该字面量的既有纪律句（「**禁止 `npm test`**」，Phase 50 写的），那是既有内容、**不要**动它 —— 本任务的判据是**正命题**（具名跑法必须出现），不是「禁止某字面量」。
    3. **`docs/product/ai-agent-workspace.md` §七 新增 SEC-10 条目**：加固后**`bash` 里「先 `ln -s <沙箱外路径> link` 再写 `link`」会被拒** —— 这是**正确**行为（沙箱封闭）但属**行为变更**；**沙箱内自指的 symlink 仍放行**（realpath 归一化后仍在 root 内）；**本加固不封闭 `bash` 的写盘能力**（命令内容不在这层校验，边界仍是三档权限 + 确认卡片）；读面（`readTextFile` 一族）语义一字未改。并在 §四 / §五（bash 三档策略）交叉引用一句，使「为什么有的写入被拒」能从这里追到那里。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const a=fs.readFileSync("AGENTS.md","utf8");const bad=[];if(a.indexOf("用户技能导入")===-1&&a.indexOf("技能导入")===-1)bad.push("AGENTS.md 缺导入维护约定条目");for(const t of ["IMPORT_LIMITS","IMPORT_SKILL_ERROR","SKILL_THREAT_PATTERNS","HOST_WHITELIST","importId","ai-skills.md","test-skills-import.js","test-skills-import-net.js","eachEntry","2.9.0","lookbehind","MANAGE_SKILL_ERROR"])if(a.indexOf(t)===-1)bad.push("AGENTS.md 缺 token: "+t);for(const f of ["test-skills-import.js","test-skills-import-net.js"])if(a.indexOf("node tests/"+f)===-1)bad.push("AGENTS.md 未用**具名跑法** node tests/"+f+"（两个新套件的跑法必须是具名 node 命令）");const wa=fs.readFileSync("docs/product/ai-agent-workspace.md","utf8");for(const t of ["SEC-10","ln -s","自指","行为变更"])if(wa.indexOf(t)===-1)bad.push("ai-agent-workspace.md 缺 token: "+t);if(bad.length){console.error("维护约定不合格:\n"+bad.join("\n"));process.exit(1)}console.log("维护约定 ok（条目 + 四类同步面 + 五个坑 + SEC-10 行为变更）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `维护约定 ok`，或 stderr 列出任意一条缺失 token</fails_when>
    <automated>node -e 'const fs=require("fs");const a=fs.readFileSync("AGENTS.md","utf8");const bad=[];if(a.indexOf("只允许加项")===-1&&a.indexOf("只允许加")===-1)bad.push("缺「LIMITS 只允许加项」纪律");if(!/不得并入|不得并入 `MANAGE_SKILL_ERROR`|独立命名空间/.test(a))bad.push("缺「导入码不得并入 MANAGE_SKILL_ERROR」的码表隔离纪律");if(a.indexOf("落盘实现")===-1)bad.push("缺「落盘实现只有一个调用点」硬约束");if(!/压缩比[\s\S]{0,80}独立/.test(a)&&a.indexOf("零贡献")===-1)bad.push("缺「上传闸对炸弹零贡献 / 两道独立闸」不变式");if(a.indexOf("技能根相对")===-1)bad.push("缺「深度按技能根相对计」不变式");if(a.indexOf("版本历史")===-1)bad.push("缺「备份不是版本历史」不变式");if(bad.length){console.error("不变式不合格:\n"+bad.join("\n"));process.exit(1)}console.log("不变式 ok（四条 + 码表隔离 + 落盘唯一性）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `不变式 ok`，或 stderr 列出任意一条缺失项</fails_when>
  </verify>
  <acceptance_criteria>
    - 两条门禁全绿；`AGENTS.md` 的导入维护约定是**可执行清单**（列出「改动哪些东西必须同步哪一节」），不是泛泛一句「记得更新文档」。
    - **四条不变式 + 码表隔离 + 落盘唯一性**各有独立 token 判据（本任务最容易被写成「写了一句就算」——判据按 token 与正则双查）。
    - **五个坑如实登记**（`eachEntry` close / `validateFileName` 六类放行 / `0xFFFFFFFF` 静默 / `yaml` 精确钉版 / 威胁模式的前置否定词前瞻）—— 它们都是本阶段**实测踩中或实测校准**的结论，写进维护约定能让下一个改这块的人不再重踩。
    - **测试行的措辞与例数**：Task 3 会交叉核对；本任务写入的例数若与实测不符，Task 3 会转红（即**不允许**先写占位数）。
    - **修订期实跑证据（红轮，当前树）**：门禁一 `exit 1`，stderr 为「AGENTS.md 缺导入维护约定条目」+ 10 条「缺 token: …」+「**AGENTS.md 未用**具名跑法** node tests/test-skills-import.js**」与「…**-net.js**」两条。⚠️ **修订说明**：该判据原先是「`AGENTS.md` 不得出现 `npm test`」的**否命题**，而 `AGENTS.md:337` 里本来就有一句**引用**该字面量的既有纪律句（Phase 50 写的「**禁止 `npm test`**」）⇒ **对任何忠实实现恒红**；现改为「两个新套件的跑法必须具名」的**正命题**（位置无关，既接受写进既有单行多套件行、也接受新起一行）。
    - `AGENTS.md` 里两个新套件的**跑法必须具名**（`node tests/test-skills-import.js` / `node tests/test-skills-import-net.js` 两条子串都在），`ai-agent-workspace.md` 的 SEC-10 条目明确「行为变更 + 正确行为」双重定性，并写清 `exec` 不在这一层校验。
  </acceptance_criteria>
  <done>`AGENTS.md` 有导入维护约定（权威文档指向 / 同步面清单 / 四条硬约束 / 四条不变式 / 五条诚实边界 / 五个实测坑）与两个新套件的测试行；`ai-agent-workspace.md` §七 记录了 SEC-10 的行为变更与「本加固不封闭 bash 写盘」的边界。</done>
</task>

<task type="auto">
  <name>Task 3: 例数账本收口（cells 12 → 20）+ 全量套件 + `51-VALIDATION.md` 回填与签字</name>
  <files>docs/product/ai-skills.md, .planning/phases/51-zip/51-VALIDATION.md, AGENTS.md</files>
  <read_first>
    - `docs/product/ai-skills.md`：`### 11.8 维护约定与测试` **文末的 ```bash 块**（`:552-563`）—— 这是 counts-parity 判据命令**唯一被本阶段门禁消费**的副本（含 `const suites=[…]` 与 `if(cells<12)`）：本任务的门禁三（verify 块里的第 3 条命令）**从该文件原文抽取命令后执行**，且只读 `AGENTS.md` 与该文件这两个账本 ⇒ `.planning/**` 下的任何副本都不参与判定。
      - **取证口径（本机可跑；⚠️ 本机 `grep` 不支持 `--hidden`，别用它）**：① `grep -rln 'const suites=' docs AGENTS.md` ⇒ **实测 1 个文件**：`docs/product/ai-skills.md` —— 这才是「唯一被门禁消费」的可复现判据（`docs/` + `AGENTS.md` 正是门禁唯一读取的两个账本文件）；② `grep -rln 'const suites=' docs .planning AGENTS.md` ⇒ **实测 10 个文件**：`docs/product/ai-skills.md` + `.planning/phases/49-manage-skill-ai/49-0{6,7,8}-PLAN.md` + `.planning/phases/50-api-skills/{50-05-PLAN.md,50-RESEARCH.md,50-VALIDATION.md}` + `51-PATTERNS.md` + `51-RESEARCH.md` + **本计划自身** ⇒ 「全仓只有一处」**不成立**。⚠️ **用 `-l`（文件级）计数，别用行数** —— 同一文件里的多处引用会把行数放大，而本计划本身也含该字面量（修订期实测行级读数为 `grep -rn … --exclude-dir=node_modules --exclude-dir=.git` ⇒ 26 行、`rg --no-ignore --hidden` ⇒ 25 行，其中 1 行为二进制 `dist/mac-arm64/…/app.asar`），该读数**随文档改动而变**，不是判据。
      - **其余 9 个文件均为不参与判定的副本**：`51-PATTERNS.md`、`51-RESEARCH.md`、`50-VALIDATION.md:119`、`50-05-PLAN.md`、`50-RESEARCH.md`、`49-0{6,7,8}-PLAN.md` 以及本计划自身。其中 `50-VALIDATION.md:119` 的 **Phase 50 历史副本**（5 套件版）**本阶段不改它**，理由两条：① 它**不被本阶段门禁消费**（门禁只读 `docs/product/ai-skills.md` + `AGENTS.md`）；② 该副本在本树上**仍自洽仍绿**（其 `cells=16` 实测已 ≥ 它自己的下限 12，5 套件版 `suites` 与它引用的两个账本单元一一对应），改它没有功能收益，却会**污染 Phase 50 已签字的收口记录**。⚠️ **不要在 `## 七、测试与验证` / `### 12.9` 里找第二条命令** —— 那两节只有**例数账本行**（`**N 例，实测**`），没有命令副本。**本任务要改的就是 `docs/product/ai-skills.md` 的那一处**。
    - `AGENTS.md` 的测试行（Task 2 已补两个套件 —— 本任务把例数换成**实测值**）
    - `.planning/phases/51-zip/51-VALIDATION.md`：frontmatter 的 `status` / `nyquist_compliant` / `wave_0_complete` 与 `## Per-Task Verification Map` / `## Wave 0 Requirements` / `## Validation Sign-Off` 三节；以及它的 frontmatter 注释里写着生命周期「`draft (seeded by plan-phase) → validated (set by validate-phase §6)`」
    - `.planning/phases/50-api-skills/50-VALIDATION.md`（**50 先例，本任务的收口形态照它**）：`status: draft` + `nyquist_compliant: false` + `wave_0_complete: true`；矩阵上方注明「`Status` 保持 `⬜ pending` —— 该列由 `/gsd:verify-work` 回填」；Sign-Off 第六条保持未勾并逐字注明「**不在本阶段自证**，由 `/gsd:validate-phase` 判定」；`**Approval:**` 保持 `pending`
    - `tests/test-skills-import.js` / `tests/test-skills-import-net.js`（**先跑一遍拿实测 `# tests`**，不得凭估算写账本）
  </read_first>
  <action>
    1. **刷新 counts-parity 判据命令**（`docs/product/ai-skills.md` 的**唯一被本阶段门禁消费的一处**，`### 11.8` 文末 ```bash 块；取证见本任务 `read_first` 的三条可跑命令）：
       - `suites` 数组追加 `"tests/test-skills-import.js"` 与 `"tests/test-skills-import-net.js"`（3 → 5 → 7）。
       - `cells` 下限由 `12` 改为 `20`，并把该行注释改为**唯一权威的表述**：「五个既有套件当前已在两个账本文件里贡献 16 个单元（实测 `cells=16`：`AGENTS.md` 5 + 本文件 11）；两个新套件各需在**两个文件**里都有账本单元 ⇒ 下限 = 16 + 2×2 = 20。**cells 只是下限阈值；权威判据是紧随其后的逐套件覆盖断言**（`for (const base of Object.keys(meas)) if (!hit[base]) bad.push(…)`）。」
       - **取值口径一字不改**：例数一律用文档现成的 `matchAll(/(\d+)\s*例/g)` 取值（带单位「例」），**不得**退化成取段内首个数字（`seg.match(/\d+/)` 会撞上同段里其它数字，例如 `AGENTS.md` 测试行里的既有说明文字）。
       - **逐套件覆盖断言必须保留**（`for (const base of Object.keys(meas)) if (!hit[base]) bad.push(file+" 的账本未覆盖 "+base)`）—— 它是「两个新套件必须在两个账本文件里都有单元」的**唯一执行机制**，删掉它这条判据就没有判别力了。
       - ⚠️ `picker` 分支逻辑**不动**（两个新套件名不含 `picker` ⇒ 用 `node tests/<file>.js` 自跑形态；它们都 `require(''node:test'')` ⇒ 会输出 TAP）。
    2. **实测并回填例数**：跑 `node tests/test-skills-import.js` 与 `node tests/test-skills-import-net.js` 取 `# tests` 数值，写进 `AGENTS.md` 测试行与 `docs/product/ai-skills.md` 的例数账本（**两处都要有该套件的账本单元** —— 该脚本要求两个文件都有，缺一个即 fail）；同时更新被本阶段增补的套件例数（`tests/test-agent-workspace.js` / `tests/test-skills-http-api.js` / `tests/test-ai-skills.js`）到**实测值**。
    3. **跑全量套件**（9 个，逐条具名命令）并确认 `# fail 0`：
       `node tests/test-manage-skill.js` / `node tests/test-ai-skills.js` / `node tests/test-skill-picker-model.js` / `node tests/test-skills-management.js` / `node tests/test-skills-http-api.js` / `node tests/test-agent-workspace.js` / `node tests/test-builtin-skills-seeder.js` / `node tests/test-skills-import.js` / `node tests/test-skills-import-net.js`。
    4. **`51-VALIDATION.md` 收口**（**只有在步骤 2/3 全绿之后**；**形态逐条照 50 先例**）：
       - `## Per-Task Verification Map` 的 `Task ID` / `Plan` / `Wave` / `File Exists` 四列逐行回填：`Task ID` 取**真实存在的任务**（形如 `51-01-T1` … `51-06-T3`，与各 PLAN 的 `<name>Task N:` 对应），`File Exists` 列把新套件那几行的 `❌ W0` 改为 `✅` 并填实际文件名。**`Status` 列保持 `⬜ pending`** —— 该列由 `/gsd:verify-work` 回填，本文件不在计划内（照 50 的矩阵上方那句话的写法，把这句话也写在矩阵上方）。
       - `## Wave 0 Requirements` 的八个勾选框逐条勾上（含夹具生成器固化、语料回归夹具、counts-parity 同步）。
       - `## Validation Sign-Off` 六条里**本阶段能自证的**逐条勾（第 1 条 All tasks have … verify or Wave 0 dependencies / 第 2 条 Sampling continuity / 第 3 条 Wave 0 covers all MISSING references / 第 4 条 No watch-mode flags / 第 5 条 Feedback latency 在 20 秒内），第六条 `nyquist_compliant: true set in frontmatter` **保持未勾**并逐字注明「**不在本阶段自证**，由 `/gsd:validate-phase` 判定」（50 先例原文）。`**Approval:**` **保持 `pending`**（由 `/gsd:verify-work` 回填）。
       - frontmatter：**只改** `wave_0_complete: true`；`status` **保持 `draft`**、`nyquist_compliant` **保持 `false`** —— 两者由 `/gsd:validate-phase`（§6）判定，**不得自证**（50 先例同样如此；该文件 frontmatter 的注释就写着 `validated (set by validate-phase §6)`）。
       - **不得**把 `## Manual-Only Verifications` 的五项写成「已自动化」——它们保持人工面（真实 GitHub 端到端 / 403·429 限流 / 32 MiB 本机耗时 / 真实 CSP 下的预览渲染（本阶段已由 uat 驱动覆盖大部分，但真实数据集读数仍属人工确认项）/ `skills.sh` 真实可达性），但要把**本阶段实际跑过的那几项**（如 `skills.sh` 的 curl 证据、uat 驱动的读数）记进对应行，使「哪些已跑、哪些没跑」一眼可辨。
    5. **收口后的自检**：跑一遍 counts-parity 命令（**从 `docs/product/ai-skills.md` 原文抽取，不另抄一份**），确认输出 `counts-parity ok` 且 `cells` ≥ 20。
  </action>
  <verify>
    <automated>node tests/test-manage-skill.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-ai-skills.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-skill-picker-model.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>任一套件出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-agent-workspace.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-builtin-skills-seeder.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)" && node tests/test-skills-import-net.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>任一套件出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs"),cp=require("child_process");const bad=[];const d=fs.readFileSync("docs/product/ai-skills.md","utf8");const si=d.indexOf("const suites=[");if(si===-1){console.error("docs/product/ai-skills.md 缺 counts-parity 命令（未找到 const suites=[ ）");process.exit(1)}const k=d.indexOf("measured=",si);if(k===-1){console.error("无法界定 counts-parity 命令的范围（未找到 measured= ）");process.exit(1)}const cmd="const cp=require(\"child_process\"),fs=require(\"fs\");\n"+d.slice(si,d.indexOf("\n",k));for(const t of ["tests/test-skills-import.js","tests/test-skills-import-net.js"])if(cmd.indexOf(t)===-1)bad.push("命令的 suites 未含 "+t);if(cmd.indexOf("Object.keys(meas)")===-1||cmd.indexOf("hit[base]")===-1)bad.push("命令缺逐套件覆盖断言（for (const base of Object.keys(meas)) if (!hit[base]) …）—— 它是「两个新套件必须在两个账本文件里都有单元」的唯一执行机制");if(cmd.indexOf("(\\d+)\\s*例")===-1)bad.push("例数取值口径不是文档现成的 (\\d+)\\s*例（取段内首个数字会撞上同段其它数字）");let out="";try{out=cp.execFileSync(process.execPath,["-e",cmd],{encoding:"utf8",stdio:["ignore","pipe","pipe"]})}catch(e){bad.push("命令执行失败（非零退出）: "+String((e&&e.stderr)||(e&&e.message)||"").slice(0,500))}if(out.indexOf("counts-parity ok")===-1)bad.push("命令未输出 counts-parity ok（stdout: "+out.trim().slice(0,200)+"）");const m=out.match(/cells=(\d+)/);if(!m)bad.push("命令未输出 cells=<n>");else if(Number(m[1])<20)bad.push("账本单元数 "+m[1]+" < 20（五个既有套件实测贡献 16 + 两个新套件在两个账本文件里各 ≥1 单元 ⇒ 下限 20）");if(bad.length){console.error("例数账本未收口:\n"+bad.join("\n"));process.exit(1)}console.log("例数账本 ok（命令取自 docs/product/ai-skills.md 原文 + 覆盖断言在位 + "+(out.trim().split("\n").pop()||"")+"）")'</automated>
    <fails_when>非零退出，或 stdout 未出现 `例数账本 ok`，或 stderr 报出「suites 未含 <新套件>」/「缺逐套件覆盖断言」/「例数取值口径不是 (\d+)\s*例」/「命令执行失败」/「cells < 20」，或 stderr 报出某账本单元的例数与实测不符（命令自身会 throw）</fails_when>
    <automated>node -e 'const fs=require("fs");const v=fs.readFileSync(".planning/phases/51-zip/51-VALIDATION.md","utf8");const bad=[];const fm=v.match(/^---\n([\s\S]*?)\n---\n/)[1];if(!/wave_0_complete:\s*true/.test(fm))bad.push("wave_0_complete 未置 true");if(!/status:\s*"?draft"?/.test(fm))bad.push("status 必须保持 draft（validated 由 /gsd:validate-phase 判定，不得自证）");if(!/nyquist_compliant:\s*false/.test(fm))bad.push("nyquist_compliant 必须保持 false（不得自证）");const n=(v.match(/- \[x\]/g)||[]).length;if(n<10)bad.push("Wave 0 / Sign-Off 勾选数 "+n+" < 10（八个 Wave 0 + 五个可自证的 Sign-Off ⇒ 期望 ≥ 13）");const keys=(v.match(/\b51-0\d-T\d\b/g)||[]);if(keys.length<15)bad.push("Per-Task 矩阵按真实任务重键的行数 "+keys.length+" < 15（19 行矩阵须逐行重键）");if(/51-0\d-\?\?|51-0\?-\?\?/.test(v))bad.push("Per-Task 矩阵仍残留占位 Task ID（51-0x-?? / 51-0?-??）");if(v.indexOf("❌ W0")!==-1)bad.push("Per-Task 矩阵的 File Exists 列仍有 ❌ W0 未回填");const rows=v.split("\n").filter(l=>/^\|/.test(l));if(!rows.some(l=>/\|\s*(?:⬜\s*)?pending\s*\|/.test(l)))bad.push("矩阵的 Status 列必须保持 ⬜ pending（该列由 /gsd:verify-work 回填；全部填绿会被读成自证）");const ap=v.match(/\*\*Approval:\*\*\s*(.+)/);if(!ap||!/pending/i.test(ap[1]))bad.push("Approval 行必须保持 pending（由 /gsd:verify-work 回填）");if(bad.length){console.error("验证契约未按 50 同款收口:\n"+bad.join("\n"));process.exit(1)}console.log("验证契约 ok（wave_0_complete 置真 + status/nyquist 不自证 + 矩阵重键 "+keys.length+" 行 + Status/Approval 保持 pending + 勾选 "+n+" 条）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `验证契约 ok`，或 stderr 列出任意一条未收口项。⚠️ 本判据**只在矩阵行与 `Approval` 行上**判「是否仍保持 pending」—— **不得**退回「全文不含 `pending`」的形态：`51-VALIDATION.md:72` 的状态图例（`*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*`）与 `Manual-Only` 表里的既有文本都含该词，全文判会对任何忠实实现**恒红**（该图例不在本任务的改写范围内）</fails_when>
  </verify>
  <acceptance_criteria>
    - 四条门禁全绿；九个套件逐条跑过且 `# fail 0`；counts-parity（**从 `docs/product/ai-skills.md` 原文抽取的那一条**）输出 `counts-parity ok` 且 `cells` ≥ 20。
    - **例数全部是实测值**：`AGENTS.md` 与 `docs/product/ai-skills.md` 两处的账本单元与实测 `# tests` 逐字一致（判据命令现场跑套件取值再比对，不是人工核对）。
    - **`cells` 下限在「被本阶段门禁消费的那一份副本」里被改成 20**（`docs/product/ai-skills.md` 的 `### 11.8` ```bash 块内的 `if(cells<12)`，约 `:560`；同批把上面那句「基线由 8 提到 12」的**陈旧注释**改成实测口径「实测基线 16 ⇒ 下限 = 16 + 2×2 = 20」）。**取证口径（文件级，可跑，已实测）**：`grep -rln 'const suites=' docs AGENTS.md` ⇒ **1 个文件**（`docs/product/ai-skills.md` —— 「唯一被门禁消费」的可复现判据）；`grep -rln 'const suites=' docs .planning AGENTS.md` ⇒ **10 个文件**（即上述 1 份 + 9 份不参与判定的副本，含本计划自身）。⚠️ 用 `-l` 别用行数：行级读数会被同一文件里的多处引用放大且随文档改动而变。⇒ 全仓存在多份副本，「唯一活副本」的说法**不成立**；成立的是「**只有 `docs/product/ai-skills.md` 这一份被本阶段门禁消费**」（门禁三从该文件原文抽取命令后执行，且只读 `AGENTS.md` 与该文件两个账本，`.planning/**` 下的副本不参与判定）。`50-VALIDATION.md:119` 与 `50-05-PLAN.md`、`50-RESEARCH.md`、49 的三份计划、`51-PATTERNS.md` / `51-RESEARCH.md` / 本计划自身的副本**均不参与** ⇒ 不改它们。
    - **`51-VALIDATION.md` 按 50 同款收口**：`wave_0_complete: true` 被设置，而 **`status` 保持 `draft`、`nyquist_compliant` 保持 `false`**（**不在本阶段自证**，由 `/gsd:validate-phase` 判定）；矩阵四列回填、`Status` 列与 `**Approval:**` 行**保持 `pending`**（由 `/gsd:verify-work` 回填）；`Wave 0 Requirements` 勾齐、`Validation Sign-Off` 勾上可自证的五条（第六条注明「不在本阶段自证」）。
    - **Manual-Only 表如实**：五项保持人工面身份，且把「本阶段实际跑过的」与「仍未跑的」分开标注（**不得**整表标成已自动化）。
    - **偏离说明（有意，且与 50 同款）**：本计划**不**把 `51-VALIDATION.md` 的 `status` 改成 `validated`、也不勾 `nyquist_compliant: true` —— 该文件的 frontmatter 注释逐字写着生命周期是 `draft (seeded by plan-phase) → validated (set by validate-phase §6)`，且 50 的先例同样是「保持 draft/false + 显式注明不自证」。自证会让 `/gsd:validate-phase` 与里程碑审计失去一处独立判据。
    - **修订期绿轮（可满足性）探针（证明这两条判据不是恒红）**：① 门禁三在一个**合成树**上转绿 —— 7 个套件 stub（分别输出 `# tests 55/187/115/49/32/120/60`）+ 7 套件版命令（`cells<20`、逐套件覆盖断言在位）+ 两个账本文件里两个新套件各有单元 ⇒ 实跑输出 `例数账本 ok（命令取自 docs/product/ai-skills.md 原文 + 覆盖断言在位 + cells=21 measured={…}）`，`exit 0`（**命令是从文档原文抽取并执行的 ⇒ 抽取逻辑本身也被这条绿轮验证**）。② 对该合成树做单点变异（删掉文档侧两个新套件的两处账本单元、命令里的 `suites` 不动）⇒ 命令自身 throw「docs/product/ai-skills.md 的账本未覆盖 test-skills-import.js / …-net.js」**且**门禁报「账本单元数 17 < 20」⇒ 覆盖断言与阈值**两条机制都可判别**。③ 门禁四在按 50 同款收口的 `51-VALIDATION.md` 副本上转绿（`验证契约 ok（wave_0_complete 置真 + status/nyquist 不自证 + 矩阵重键 18 行 + Status/Approval 保持 pending + 勾选 14 条）`，`exit 0`）⇒ 判据可被正确实现满足。
    - **可失败性（红轮，当前树）**：门禁三实测 `exit 1`，stderr 同时报「命令的 suites 未含 tests/test-skills-import.js」「命令的 suites 未含 tests/test-skills-import-net.js」「账本单元数 16 < 20」；门禁四实测 `exit 1`，stderr 报「wave_0_complete 未置 true」+「勾选数 0 < 10」+「矩阵重键行数 0 < 15」+「残留占位 Task ID」+「仍有 ❌ W0 未回填」（**注意它不再报「仍有 pending 未收口」** —— 那句话会被 `51-VALIDATION.md:72` 的状态图例命中，是本次修订移掉的假红）。执行者落地后做一次单点变异 —— 把某个账本数字改错 ⇒ 门禁三的 stderr 指名报出「文件:行号 + 套件名 + 取到的值 ≠ 实测」。
  </acceptance_criteria>
  <done>例数账本与验证契约收口到实测值：两个新套件进 `suites`、`cells` 下限 20、两个账本文件里的单元与实测逐字一致（逐套件覆盖断言在位）；九个套件全绿；`51-VALIDATION.md` 回填并把 `wave_0_complete` 置真，而 `status` / `nyquist_compliant` 与矩阵 `Status` / `Approval` **保持 50 同款的自证边界**（留给 `/gsd:validate-phase` 与 `/gsd:verify-work`），人工面与自动化面的界线清晰。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 后续维护者的判断 | 文档里的措辞（尤其诚实边界与限额口径）直接决定后续改动是否安全；一句被推翻的旧理由会传播一手 |
| 验证契约 → 里程碑审计 | `51-VALIDATION.md` 是审计的判据来源；勾错的项会让覆盖率结论失真 |
| 例数账本 → 门禁 | 账本与实测不符 ⇒ 该门禁本身失去意义（它会恒红或被绕过） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-47 | Repudiation | 诚实边界只写进文档、未上屏（或反之） | medium | mitigate | 四条已在 UI 上屏（`51-06` 的源码判据）且本节与 `AGENTS.md` 各写一份；第十三节 13.10 与 `AGENTS.md` 双向引用 |
| T-51-48 | Spoofing | 文档沿用已被实测推翻的旧理由（`skills.sh` 承载闭环 / 上传闸拦炸弹 / 扫描通过即安全） | high | mitigate | 四条各有一条**负向 token 判据**（出现即转红），且第十三节要求「为什么」写成实测依据 + 指向研究章节 |
| T-51-49 | Repudiation | 验证契约被勾成通过而未真跑 | high | mitigate | 四道门禁现场跑九个套件 + counts-parity 现场取值比对 + `wave_0_complete` 与勾选数判据（`status`/`nyquist` 保持不自证）；Manual-Only 表要求区分「已跑 / 未跑」 |
| T-51-50 | Tampering | 文档写入本机绝对路径 / 运行期随机路径 / 具体 sha 作为可复现前提 | low | mitigate | 负向判据扫 `/Users/wxnacy` / `/Volumes` / `/private/var`；要求写路径形状或访问器名 |
| T-51-51 | Tampering | 账本数字凭估算写入（与实测不符 ⇒ 门禁失真） | medium | mitigate | counts-parity 命令**现场跑套件取值**再比对；`cells` 下限 20 防「少写单元」 |
| T-51-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本计划**零安装动作**、零代码改动（只改文档与 `.planning` 验证契约） |
</threat_model>

<verification>
- 本计划三个任务的 automated 门禁（共十条）全绿；九个套件逐条 `# fail 0`；counts-parity 从文档原文抽取执行得 `counts-parity ok` 且 `cells` ≥ 20。
- **跨计划回归**：`importUserSkill(` 全仓恰 2 处、`yauzl.openPromise(` 恰 1 处、`MANAGE_SKILL_ERROR` 恰 11 键 —— 三条判据在阶段收口时仍须成立（它们分别由 `51-03`/`51-04`/`51-05` 落地的用例承载，本计划只复核）。
- **本计划不改任何代码**：`git status` 应只显示 `docs/`、`AGENTS.md` 与 `.planning/` 下的文件。
- **阶段的 `STATUS` 双账本口径**（供编排层收口，不由本计划写状态）：`syncAgentSystemPrompt()` 的**写路径**份额在本阶段完成后到 **3/3**（49 的 `manage_skill` + 50 的启停 / 卸载 + 51 的导入）⇒ `STATE.md` 的 ⚠️ 可闭合；但**读侧 / 兜底**份额（`refreshSkillsForPanel()` / `ensureSkillsFresh()`）**不是同一个量** —— 收口时**分别记两个数**，**不得**声称 P8 失效链 6/6 全覆盖。
- **本阶段未完成的技术债如实挂账**（不隐藏、不声称已修）：`TD-48-01`（`escapeHtml` 不转义引号，本阶段只做到「不扩大缺口」）、`#skillImportGate` 禁用原因不在 live region、`/api/skills/set-disabled` 无存在性校验（50 收尾带出，本阶段按需求边界不处置）、真实 GitHub 端到端与 32 MiB 本机实测仍属人工面。
</verification>

<success_criteria>
- `docs/product/ai-skills.md` 第十三节 + 安全边界定稿节齐备，`IMPORT_SKILL_ERROR` 的全部 21 码 / `IMPORT_LIMITS` 的 8 限额值与实现逐字一致（判据按常量取值逐个查，不写死数字），五条诚实边界与两条挂账成文，无被推翻的旧表述。
- `AGENTS.md` 有可执行的导入维护约定（同步面清单 + 四条硬约束 + 四条不变式 + 五个实测坑）与两个新套件的**具名**测试行；`ai-agent-workspace.md` §七 记录 SEC-10 行为变更。
- counts-parity 命令含两个新套件、`cells` 下限 20、逐套件覆盖断言在位、两个账本文件与实测逐字一致且现场可重跑（命令从文档原文抽取执行，不另抄一份）。
- `51-VALIDATION.md` 按 50 同款收口：`wave_0_complete` 置真、矩阵与勾选回填，而 `status` / `nyquist_compliant` / 矩阵 `Status` / `Approval` 保持**不自证**的边界，人工面与自动化面界线清晰。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-07-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-07）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 文档章节 | `## 十三、导入（zip + 网络地址）`（13.1–13.10 十个子节，含安全边界与已知限制定稿节） | `docs/product/ai-skills.md` |
| 文档条目 | SEC-10 行为变更条目（`ln -s` 出去再写被拒 + 自指链接仍放行 + `exec` 不在这一层校验） | `docs/product/ai-agent-workspace.md` §七 |
| 维护约定条目 | 「用户技能导入（zip + 网络地址）的维护约定」（权威文档指向 / 同步面清单 / 四条硬约束 / 四条不变式 / 五条诚实边界 / 五个实测坑） | `AGENTS.md` |
| 账本单元 | 两个新套件的例数（`AGENTS.md` 测试行 + `docs/product/ai-skills.md` 例数账本各一） | `AGENTS.md` / `docs/product/ai-skills.md` |
| 判据命令变更 | counts-parity 的 `suites` 追加两个套件、`cells` 下限 12 → 20（**唯一被本阶段门禁消费的副本 = `docs/product/ai-skills.md` §11.8**；全仓另有 13 处文本命中，均为 `.planning/**` 下的历史 / 引用副本，本阶段不改） | `docs/product/ai-skills.md` |
| 验证契约收口 | `51-VALIDATION.md` 的 Per-Task Map 四列回填 / Wave 0 勾选 / Sign-Off 按 50 同款勾选 / frontmatter 的 `wave_0_complete: true`（`status` / `nyquist_compliant` **不自证**） | `.planning/phases/51-zip/51-VALIDATION.md` |

### 本阶段全部计划的新建符号总表（供评审对照，避免单项遗漏）

| 计划 | 新建符号摘要 |
|------|--------------|
| 51-01 | `resolveInsideForWrite` / `guardForWriteResult` / 五个写方法与两个临时目录方法的切换 / `tests/test-agent-workspace.js` 写面用例组 |
| 51-02 | `dependencies.yauzl`（`^3.4.0`）/ `dependencies.yaml`（裸 `2.9.0`）/ lock 条目 |
| 51-03 | `IMPORT_LIMITS` / `IMPORT_SKILL_ERROR`（20 键；`51-04` 再加 `INVALID_NAME` ⇒ 最终 21 键）/ `validateEntryName` / `normalizedEntryKey` / `parseSkillFrontmatter` / `deriveImportName` / `locateSkillRoot` / `readSkillPackageEntries` / `extractAndValidatePackage` / `collectPreviewStats` / `buildImportPreview` / `importUserSkill` / `SKILL_THREAT_PATTERNS` / `scanSkillThreats` / `readRawBody` / `MAX_SKILL_PACKAGE_BYTES` / `POST /api/skills/import` / `AIManager.previewSkillImport` / `AIManager.commitSkillImport` / 最小导入 DOM 与前端函数 / `tests/test-skills-import.js` / `tests/helpers/make-malicious-zip.js` / `tests/fixtures/skill-corpus/**` |
| 51-04 | `resolveImportConflict` / `countUserSkills` / `userSkillPaths` / `isImportResidueName` / `IMPORT_TMP_PREFIXES` / `IMPORT_SKILL_ERROR.INVALID_NAME` / `AIManager.cancelSkillImport` / `AIManager.sweepSkillImports` / main.js 两处清扫接线 / `.tmp/skill-replace-<rand>/` |
| 51-05 | `HOST_WHITELIST` / `MAX_REDIRECTS` / `FETCH_TIMEOUT_MS` / `isWhitelistedHost` / `classifyImportUrl` / `downloadPackage` / `prepareFromRawFile` / `getSearchManagerLazy` / `tests/test-skills-import-net.js` |
| 51-06 | `.skill-import-*` CSS 段 + `--skill-error-text` / `--skill-limit-text` / 导入弹框完整 DOM / `setImportState` 等前端函数 / `SKILL_IMPORT_ERROR_TEXT` / `SKILL_IMPORT_CONFIRM_TEXT` / `sanitizeDisplayString` / `tests/uat-51-import-modal.js` |
| 51-07 | 本节上表六项 |
