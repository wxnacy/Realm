---
phase: 51-zip
plan: 05
type: execute
wave: 4
depends_on: [51-03, 51-04]
files_modified:
  - ai-skills-manager.js
  - ai-manager.js
  - tests/test-skills-import-net.js
  - tests/test-skills-import.js
autonomous: true
requirements: [USER-04, SEC-08]
user_setup: []

estimate:
  # ⚠️ 未经本项目校准：estimate-check --calibrated 报 ratio 1.30 / over_budget=true，
  #    但 confidence=low 且 sample_count=0（本项目尚无校准样本）⇒ 该数字是**量级参考而非精确值**。
  #    不要据此重切已经有效的计划形状（三个任务分别对应「分类器 / 下载器与接线 / 端到端证据」）。
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

key-decisions:
  - '**本计划不设 tracer**：网络面**不开启新纵切** —— 它最终仍汇入 `51-03` 已跑通的「`pkgRoot` → 全树校验 → 技能根定位 → 预览 → 唯一落盘实现」那条链，本计划交付的是**这条链的第二个准备器**（`prepareFromRawFile`）与一条受限下载路径。tracer 落在 `51-01` 与 `51-03`'
  - '**三条来源共用一条管线的**机械形式**（P9 第 5 条）在本计划被兑现为「两个准备器 + 一段共用后段」**：`extractAndValidatePackage(env, { importDir, scopeRel })`（zip，`51-03`）与 `prepareFromRawFile({ importDir, name, text })`（单文件直链，本计划）**都只负责产出 `{ pkgRoot }`**；其后的全树校验 / `locateSkillRoot` / `collectPreviewStats` / `buildImportPreview` / `importUserSkill` **是同一段代码**（判据：`importUserSkill(` 全仓仍恰 2 处；`yauzl.openPromise(` 仍恰 1 处 —— 本计划**不新增**解压入口）。⚠️ **不得**把直链分支写成「另一条落盘路径」（P9 明令禁止的形态）'
  - '**复用 `search-manager.isPrivateHost` 的判据，但必须经惰性 require + 依赖注入接出**：`search-manager.js:22` 顶层 `require(''electron'')` ⇒ `ai-skills-manager.js` 的「零 electron 依赖」约束要求**不得**在模块顶层 require 它（沿用文件内既有的 `getAiMemoryManagerLazy` / `getAgentWorkspaceLazy` 惰性模式，新增 `getSearchManagerLazy()`）。**判据（私网网段表）只有一份**，不得复制；同时为可测性把**三个依赖做成可注入形参**并在**默认值表达式**里接真实实现：`{ fetchImpl, isPrivateHost, hostWhitelist }`。理由：纯 Node 测试里 `net` 不存在（`net.fetch` 不可用），且本地 stub server 的 `127.0.0.1` **必然**被真实的 `isPrivateHost` 判为私网 ⇒ 不给注入缝就无法在「不依赖实时外网」（CR-12）的前提下测任何一条网络路径'
  - '**跳数超限必须抛错**（P9 第 2 条，顺手修既有缺陷）：`search-manager.fetchUrl` 的重定向循环走完**不抛**「重定向过多」，而是带着最后一个 3xx 掉出 ⇒ 报误导性的 `HTTP 301`（`search-manager.js:1594-1620` 逐字）。本计划的循环在 `hop > MAX_REDIRECTS` 时抛 `REDIRECT_LIMIT`，**并有一条断言「失败码不是 `HTTP 3xx` 形态」的用例**'
  - '**白名单是「归一化后精确成员判定」**：`hostname.toLowerCase()` 去掉**前导 `www.`** 后与白名单集合做**精确** `includes`。**不得**用 `endsWith(''github.com'')` / 后缀正则（实测 `evilgithub.com` 会被放行）。`www.` 归一化是 CR-1 的等价做法：它让 `www.skills.sh` **可达**（否则 `skills.sh` 的 308 会在第一跳被自己的逐跳校验拒），同时**不**放松 `github.com.evil.com` 这类攻击形态（归一化只剥一个前缀，不剥后缀）'
  - '**`api.github.com` 显式拒绝**（CR-1b）：它在白名单里只服务 `find-skills` 的检索，导入管线无载荷语义（D-06 不做 Contents API）⇒ 分类器直接回 `unsupported_url` 并给可读原因，避免出现「白名单放行但行为未定义」的第四分支。同理 `objects.githubusercontent.com` 与 `skills.sh` 保留在白名单但本阶段**不构造指向它们的 URL** ⇒ 同样回 `unsupported_url` 并在产品文档如实写成「保留为未来兼容，本阶段无消费者」（`51-07`）'
  - '**仓库地址的 ref 缺省策略（本计划的裁量，必须成文）**：`github.com/<o>/<r>`（无 `tree`/`blob` 段）⇒ 依次尝试 `refs/heads/main` → `refs/heads/master`；两次都 404 ⇒ **`download_failed`** 且 message 明说「未找到默认分支（已尝试 main / master），请在地址中带上 `/tree/<ref>/<path>`」。**不得**静默换 ref 或猜其它分支名（猜错会解出一个与用户意图不符的包）'
  - '**magic bytes 是 zip 分支的权威判据，`content-type` 只用于把错误说得更清楚**：codeload 的 404 是 **14 字节 `text/plain`**（实测）⇒ 若先按 content-type 拒绝，用户会拿到「不是 zip」而不是「地址 / ref 不存在」。所以顺序是：先读进临时文件（带上限）→ 再判 magic（`50 4B 03 04` 或空包 `50 4B 05 06`）→ 不匹配时**用 content-type 与状态码补充原因**。⚠️ **raw 分支不得做 magic 校验**（实测 raw 的 `content-type` 是 `text/plain`、内容是文本）—— 改为「非空 + 前 512 字节无 NUL」'
  - '**下载落盘用 `Readable.fromWeb(resp.body)` + `pipeline`**（流式累加 + 超限即中止），并按 `media-remuxer.js:550-563` 的既有形态做**失败清理**（半成品 `unlinkSync` + `settled` 幂等）—— 判据对象是「**不无上限读入内存**」，不得 `arrayBuffer()`'
  - '**本计划不碰 `main.js` 的 body 读取与路由**（`51-03` 已交付 raw body 与三分支；`mode:''url''` 分支的转发已就位）⇒ 本计划只改 `ai-skills-manager.js` 与 `ai-manager.js` 的 `url` 分支，以及新增套件'

must_haves:
  truths:
    - '仓库 URL / tree/<ref>/<path> / raw 与 blob 直链三形态各自转出预期的 zipball 或 raw 目标 URL；zipball 顶层 <repo>-<ref>/ 前缀被剥离；api.github.com 与白名单外主机回 unsupported_url'
    - 'https 强制 / 白名单精确匹配（evilgithub.com 与 github.com.evil.com 均拒）/ 逐跳私有地址拒 / 跳数超限抛错而非报 HTTP 3xx / magic bytes 不匹配拒 / 流式上限命中即中止且临时文件被删'
    - '**直链 SKILL.md 与 zip 走同一落盘函数**：两条准备路径（`extractAndValidatePackage` / `prepareFromRawFile`）产出的 `pkgRoot` 进入**同一段**全树校验 + 技能根定位 + 预览 + `importUserSkill`；判据是「`importUserSkill(` 全仓恰 2 处」与「`yauzl.openPromise(` 恰 1 处」同时成立'
    - '**可测性不牺牲判据**：`isPrivateHost` 的私网判据只有一份（`search-manager` 的导出，经惰性 require）；可注入的三个依赖（`fetchImpl` / `isPrivateHost` / `hostWhitelist`）在**生产调用点全部使用默认值** —— 源码扫描以**正命题**断言：① `previewSkillImport` 内**每个** `downloadPackage(` 调用的首参**逐字** `undefined`（`downloadPackage(deps, …)` 与 `downloadPackage({ … })` 两种可达形态都转红）；② 三个键在 `ai-manager.js` 出现 **0** 次；③ 三个键各自只在 `downloadPackage` 的**参数默认值表达式**里出现一次，且分别接 `getSearchManagerLazy().isPrivateHost` / `HOST_WHITELIST` / `require(''electron'').net.fetch`（默认值被改成放宽值也转红）'
    - '逐跳校验对**每一跳**生效：stub server 造一条 `白名单内 → 302 到私网地址` 的链 ⇒ 在**第二跳**被拒（而不是只校验首跳）；造一条超过 `MAX_REDIRECTS` 的链 ⇒ 抛 `REDIRECT_LIMIT` 且**不是** `HTTP 3xx` 文案'
    - '流式上限命中即中止并清理半成品：stub server 持续吐字节直到超过上限 ⇒ 抛 `DOWNLOAD_FAILED`（message 含上限与当前值），且 `<importDir>` 下**不残留**半成品文件（`unlinkSync` 已执行）'
  artifacts:
    - 'ai-skills-manager.js：`HOST_WHITELIST`、`MAX_REDIRECTS`、`FETCH_TIMEOUT_MS`、`isWhitelistedHost(hostname)`、`classifyImportUrl(url)`、`downloadPackage(deps, opts)`、`prepareFromRawFile({ importDir, name, text })`（**无依赖注入形参**）、`getSearchManagerLazy()`、`module.exports` 追加段'
    - 'ai-manager.js：`previewSkillImport` 的 `kind === ''url''` 分支（分类 → 下载 → 准备 `pkgRoot` → 与 `51-03` **同一段**后段）'
    - 'tests/test-skills-import-net.js（**新建套件**，`node:test` 自跑形态）：分类器值域矩阵 + 本地 stub server 的六类网络面 + 两来源同管线判据'
  key_links:
    - '单一落盘链（回归判据）：`importUserSkill(` 全仓恰 2 处（定义 + 调用）—— 本计划结束时仍须成立；`yauzl.openPromise(` 恰 1 处（直链分支**不得**引入第二个解压入口）'
    - '判据单源链：`search-manager.isPrivateHost`（唯一私网判据）→ `getSearchManagerLazy()` → `downloadPackage` 的默认 `isPrivateHost`。**不得**在 `ai-skills-manager.js` 里复制一张网段表（第二份必然独立漂移；`search-manager.js:169-181` 的 11 条网段是唯一权威）'
    - '上限单源链：`IMPORT_LIMITS.MAX_TOTAL_BYTES`（解压累计上限）与 `main.js` 的 `MAX_SKILL_PACKAGE_BYTES`（HTTP body 上限）**同值不同量**；两者的相等性由一条**跨文件源码断言**守住（改一处的字面量会让它转红）—— 只出现在本计划的收尾门禁里'
    - '写路径收口不变（D-19）：网络导入成功出口仍是 `await this.ensureSkillsFresh()` 恰一次 + **调用侧**补播恰一次；`syncAgentSystemPrompt()` 函数体逐字未改'
  prohibitions:
    - statement: '不得复用 `search-manager.fetchUrl` 抓技能包，也不得把它的 `res.text()` / 字符数截断形态搬到本阶段'
      status: unresolved
      verification: judgment
      reason: 'Anti-Pattern 7 逐字：`fetchUrl` 取文本、`FETCH_DEFAULT_MAX_LENGTH = 12_000` **字符**上限、`await res.text()` 一次性读入 ⇒ 对二进制是**破坏性的**（一个 3.9 MB 的 zipball 会被截成 12 KB 的乱码）。本阶段只复用它的**判据**（`isPrivateHost`），不复用函数'
    - statement: '不得放宽 https-only、白名单精确匹配、逐跳私有地址校验、跳数上限与流式字节上限中的任意一条（含「为通过测试」而在生产路径传入自定义 `hostWhitelist` 或 no-op 的 `isPrivateHost`）'
      status: unresolved
      verification: judgment
      reason: '这五条就是 P9（S2）门禁的全部承重面；放宽任一条都会让 SSRF 面重新打开。可注入形参是**测试缝**，生产调用点必须走默认值 —— 源码扫描已把这条钉成判据（见 must_haves.truths 第四条）'
    - statement: '不得把 DNS rebinding 的残余风险写成「已缓解 / 已消除」（也不得省略这段披露）'
      status: unresolved
      verification: judgment
      reason: '`isPrivateHost` 走 Node `dns.lookup`，而实际请求走 Electron `net.fetch`（Chromium 网络栈，可能走系统代理 / DoH / 不同 resolver）⇒ **被校验的 IP ≠ 被连接的 IP**，属「先解析再请求」的固有 TOCTOU。主机白名单把收益压到接近零，但那是**缓解**不是**消除**；D-16 要求如实披露（`51-07` 写进产品文档与威胁模型）'
    - statement: '不得为「支持更多来源」而放宽白名单或新增未实测的 URL 形态分支（例如 Contents API 列一层、GitHub release 附件）'
      status: unresolved
      verification: judgment
      reason: '每个新增分支都是一条**未验证的网络路径**：Contents API 未鉴权只有 60 req/h 且 `STATE.md` 明文它从未发过真实网络请求验证过（D-06 的全部理由）；release 附件属另一类产品语义。要加须单独立项并重走 SSRF 门禁'
---

<objective>
交付**网络地址导入**：把 GitHub 的三种 URL 形态分流出「zipball」与「raw 单文件」两条下载路径，经 https-only + 主机白名单 + 逐跳私网校验 + 跳数上限 + 流式字节上限 + magic bytes 六道校验下载到临时区，再**汇入 `51-03` 的同一条管线**（同一段全树校验、同一次技能根定位、同一个落盘实现）。

Purpose: 这是 **P9（S2）SSRF 门禁**的承重面，也是 `51-VALIDATION.md` 里唯一需要真实网络形态知识的入口。它的结构风险有两条：① 直链 SKILL.md 很容易被写成「第二条落盘路径」（P9 明令禁止）；② 逐跳校验的事后补救形态（`fetchUrl` 的循环走完不抛、或只校验首跳）会让重定向成为绕过白名单的通道。本计划对两条各有一组机械判据。

Output: `classifyImportUrl` / `isWhitelistedHost` / `downloadPackage` / `prepareFromRawFile`、`ai-manager` 的 `url` 分支接线、以及新建套件 `tests/test-skills-import-net.js`（本地 stub server，零实时外网依赖）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-RESEARCH.md
@.planning/phases/51-zip/51-PATTERNS.md
@.planning/phases/51-zip/51-03-PLAN.md
@.planning/phases/51-zip/51-04-PLAN.md
@.planning/phases/51-zip/COVERAGE.md
@ai-skills-manager.js
@ai-manager.js
@search-manager.js
@tests/test-skills-http-api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: URL 分类器 —— 白名单（www 归一化）+ https 强制 + 三形态映射 + 拒绝面</name>
  <files>ai-skills-manager.js, tests/test-skills-import-net.js</files>
  <read_first>
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 2`（`:679-792`）—— **本任务的唯一权威**：四种 `curl` 响应的逐字输出（codeload 直连 200 `application/zip` / `archive/…zip` 302 → codeload / raw 200 `text/plain` / `blob` 带 `x-raw-download`）、分流规则表、ref 形态与错误响应实测（`zip/refs/heads/main` 与 `zip/main` 都吃、404 是 14 B `text/plain`）、技能根定位表（无子路径 20 根 / `tree/main/skills/pdf` 恰 1 根）、`url-resolve.js` 的安全实测四行（`evilgithub.com` / `github.com.evil.com` / `http:` / `www.skills.sh` 的逐字分类结果）；`### 实测 5`（`:878-930`）—— `skills.sh` 的 308 与 `www.skills.sh` 的 404/SPA 形态、以及 `grep skills.sh` 的零命中证据
    - `.planning/phases/51-zip/51-CONTEXT.md`：D-05（白名单六条与逐跳校验）、D-06（三形态与多技能拒绝）、CR-1 / CR-1b（`www.skills.sh` 与 `api.github.com` 的裁决）、CR-7（全包扫描不限定 `skills/*`）、CR-8（多根拒绝文案要讲清 `tree/<ref>/<path>`）
    - `.planning/phases/51-zip/COVERAGE.md`（本阶段的外部来源面矩阵 —— 分类器的每个分支都对应表里的一行 `INTEGRATE` 或 `OPT-OUT`）
    - `ai-skills-manager.js`：文件头的依赖纪律注释（`51-03` 已豁免 `yauzl` / `yaml` —— 本任务再加 `search-manager` 的**惰性** require 说明）、`getAiMemoryManagerLazy` / `getAgentWorkspaceLazy`（惰性 require 的形态）、`IMPORT_SKILL_ERROR`（`unsupported_url` / `download_failed` / `redirect_limit` / `not_a_zip` 四个键已在 `51-03` 定义 ⇒ **只用不增**）
    - `tests/test-skills-import.js`：文件骨架（`node:test` 自跑头、`readSource`、`withTempRoot`）—— 新套件照抄它
  </read_first>
  <action>
    1. **`ai-skills-manager.js` 新增白名单与归一化**：
       - `const HOST_WHITELIST = Object.freeze([''github.com'', ''api.github.com'', ''codeload.github.com'', ''raw.githubusercontent.com'', ''objects.githubusercontent.com'', ''skills.sh'']);`（六条，与 D-05 / COVERAGE.md 逐条对应）。
       - `function isWhitelistedHost(hostname)`：`typeof hostname === ''string''` 且非空 ⇒ `String(hostname).toLowerCase().replace(/^www\./, 空字符串)` 后 `HOST_WHITELIST.includes(<归一化值>)`。**注释必须写清两点**：① 归一化**只剥前导 `www.`**（CR-1 的等价做法，使 `www.skills.sh` 可达），**不**做后缀 / 子串匹配 —— `evilgithub.com` 与 `github.com.evil.com` 都必须是 `false`；② 这是**精确成员判定**，不是 `endsWith`。
       - `const MAX_REDIRECTS = 5;`、`const FETCH_TIMEOUT_MS = 30 * 1000;`（单跳超时；与 `search-manager` 的同名量**各自独立**，注释说明理由：导入是用户交互路径，超时体感与搜索不同）。
    2. **`classifyImportUrl(url)`（纯函数，可单测）** → 返回 `{ kind, target, scopeRel, ref, code?, message? }`，`kind ∈ { ''zipball'', ''raw-skill'', ''unsupported'' }`：
       - 解析失败（`new URL` 抛）⇒ `unsupported_url`（「地址格式不正确」）。
       - `protocol !== ''https:''` ⇒ `unsupported_url`（message 明说「只支持 https 地址」；同时拒绝 `file:` / `data:` / `http:`）。
       - `!isWhitelistedHost(hostname)` ⇒ `unsupported_url`（message 含**归一化后**的主机名，便于用户核对；**不回显整条 URL**）。
       - **`api.github.com`** ⇒ `unsupported_url`，message 逐字：`GitHub API 地址不受支持；请改用仓库或子目录地址（github.com/<组织>/<仓库>/tree/<分支>/<路径>）或 SKILL.md 直链`。注释交叉引用 CR-1b 与 COVERAGE.md 的 `OPT-OUT` 行。
       - **`codeload.github.com`**：路径形如 `/<o>/<r>/zip/...` ⇒ `{ kind:''zipball'', target: <原 URL>, scopeRel: null }`；其它路径 ⇒ `unsupported_url`。
       - **`github.com`**（路径分段逐条判，**顺序即语义**）：
         - `/<o>/<r>`（恰两段，可带尾斜杠）⇒ zipball，`ref` 缺省 `''main''`（`target` 用 `codeload.github.com/<o>/<r>/zip/refs/heads/<ref>`），**并记录 `fallbackRef = ''master''`** 供下载层在 404 时重试一次；
         - `/<o>/<r>/tree/<ref>/<path...>` ⇒ zipball + `ref` + `scopeRel = <path...>`（路径按原样保留，**不做 URL 解码后的二次拼装** —— 用 `URL.pathname` 的既有解码结果，**不得**再手工 `decodeURIComponent` 拼路径）；
         - `/<o>/<r>/blob/<ref>/<path...>` ⇒ 若 `path` 以 `SKILL.md` 结尾 ⇒ raw-skill，`target = raw.githubusercontent.com/<o>/<r>/<ref>/<path>`；若以 `.zip` 结尾 ⇒ zipball（从 raw 取 zip）；其它 ⇒ `unsupported_url`；
         - `/<o>/<r>/archive/**` ⇒ zipball（实测 302 → codeload，逐跳校验会跟随）；
         - 其它形态（`/issues` / `/pull` / `/releases` / `/settings` 等）⇒ `unsupported_url`（message 列出**受支持的三形态**）。
       - **`raw.githubusercontent.com`**：`/<o>/<r>/<ref>/<path...>` ⇒ 以 `SKILL.md` 结尾 ⇒ raw-skill；以 `.zip` 结尾 ⇒ zipball；其它 ⇒ `unsupported_url`（message：「raw 直链只支持 SKILL.md 或 .zip」）。
       - **`objects.githubusercontent.com` / `skills.sh`**（归一化后）⇒ `unsupported_url`，message 与注释都写明「该来源保留为未来兼容，本阶段导入管线**无载荷语义**」（CR-1 裁决的如实表述；**不得**写成「承载产品闭环」—— 那是已被实测推翻的过时理由）。
       - `SKILL.md` 的判定**必须大小写敏感**（与 `locateSkillRoot` 的判据一致），并在注释里点明：用大小写不敏感匹配会让 `/blob/main/x/skill.md` 被当成直链而实际是无载荷语义的 HTML 页。
    3. **`tests/test-skills-import-net.js`（新建套件）** 落**分类器值域矩阵**（≥ 20 例，全部离线、零网络）：三种受支持形态各自的 `kind` / `target` / `scopeRel` / `ref` 逐字断言（含 `zip/refs/heads/<ref>` 的构造结果）；六个拒绝面各一例（`http:` / `evilgithub.com` / `github.com.evil.com` / `api.github.com` / `https://www.skills.sh/anthropics/pdf` / `https://github.com/o/r/issues` / `raw.../x/README.md` 等）；`www.` 归一化的**正**例（`https://www.skills.sh/x` 必须被识别为**白名单内**、进而回 `unsupported_url` 而不是「主机不在白名单」—— 这两条的 message 不同，是本条判据可观测之处）；`SKILL.md` 大小写敏感一例。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js && node --check tests/test-skills-import-net.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const src=stripC(fs.readFileSync("ai-skills-manager.js","utf8"));const bad=[];for(const t of ["const HOST_WHITELIST = Object.freeze(","function isWhitelistedHost(","function classifyImportUrl(","const MAX_REDIRECTS =","const FETCH_TIMEOUT_MS ="])if(src.indexOf(t)===-1)bad.push("缺 "+t);const i=src.indexOf("function isWhitelistedHost(");const b=src.slice(i,src.indexOf("\n}\n",i));if(b.indexOf("endsWith(")!==-1)bad.push("白名单用了 endsWith（实测会放行 evilgithub.com）");if(b.indexOf("replace(/^www\\./")===-1&&b.indexOf("replace(/^www.")===-1)bad.push("白名单未做前导 www. 归一化（www.skills.sh 会在第一跳被自己拒）");if(b.indexOf("includes(")===-1)bad.push("白名单未用精确成员判定");if(!/module\.exports\s*=\s*\{[\s\S]*?\bclassifyImportUrl\b/.test(src))bad.push("classifyImportUrl 未导出");if(!/module\.exports\s*=\s*\{[\s\S]*?\bisWhitelistedHost\b/.test(src))bad.push("isWhitelistedHost 未导出");if(bad.length){console.error("分类器不合格:\n"+bad.join("\n"));process.exit(1)}console.log("分类器 ok（六条白名单 + www 归一化 + 精确成员判定 + https 强制入口 + 两个导出）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `分类器 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import-net.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 20（分类器值域矩阵不允许压成个位数），或进程以非零退出</fails_when>
    <automated>node -e 'const net=require("./tests/test-skills-import-net.js")' 2>/dev/null; node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const t=stripC(fs.readFileSync("tests/test-skills-import-net.js","utf8"));const bad=[];if(t.indexOf("require(\x27node:test\x27)")===-1&&t.indexOf("require(\"node:test\")")===-1)bad.push("新套件必须 require(\x27node:test\x27) 才能以 node tests/<file>.js 自跑（counts-parity 依赖它输出 TAP）");if(t.indexOf("picker")!==-1)bad.push("套件名或内容含 picker（counts-parity 会误插 --test）");if(t.indexOf("fetch(")!==-1)bad.push("分类器用例不得发真实网络请求（CR-12：网络面用本地 stub server）");if(bad.length){console.error("套件形态不合格:\n"+bad.join("\n"));process.exit(1)}console.log("套件形态 ok（node:test 自跑 + 无 picker + 零实时网络）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `套件形态 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`tests/test-skills-import-net.js` 的 `# fail 0` 且 `# tests` ≥ 20。
    - **可失败性（本轮修订期实跑记录，逐字）**：门禁一当前树 `exit 1`，stderr 首行为 `Error: Cannot find module '<repo>/tests/test-skills-import-net.js'` —— 链式 `node --check tests/test-skills-import-net.js` 先失败 ⇒ 其后的 `node -e` 段未执行；把 `node -e` 段单独跑则报 5 项缺失（`const HOST_WHITELIST = Object.freeze(` / `function isWhitelistedHost(` / `function classifyImportUrl(` / `const MAX_REDIRECTS =` / `const FETCH_TIMEOUT_MS =`）。两类失败均属「新代码未落地」类。门禁三当前树 `exit 1`，stderr 为 `ENOENT: no such file or directory, open 'tests/test-skills-import-net.js'`（新套件未落地）。
    - **单点变异（执行者落地后自行跑，各确认转红后复原）**：① 把 `isWhitelistedHost` 的 `includes` 换成 `hostname.endsWith(...)` ⇒ `evilgithub.com` 那一例转红；② 去掉 `www.` 归一化 ⇒ `www.skills.sh` 那一例的 message 从「无载荷语义」变成「主机不在白名单」⇒ 该例转红；③ 把 `SKILL.md` 的判定改成大小写不敏感 ⇒ 对应那例转红。
    - **两条不同 message 可区分**：`https://www.skills.sh/x`（白名单内、无载荷语义）与 `https://evil.example.com/x`（白名单外）**都是 `unsupported_url` 码**但 message 不同 ⇒ 用例必须断言 message，只断言 `code` 无法证明归一化生效。
    - 分类器**零网络**：整个套件（本任务部分）不得出现 `fetch(` / `net.fetch` / 真实域名解析。
    - **负向 token 先剥注释再判（W2，本轮修订；本任务两处）**：门禁一里的 `endsWith(` 判据、门禁三里的 `picker` 与 `fetch(` 两条判据，全部改在 `stripC`（与 `51-03` / `51-04` / `51-06` 同一份五态剥注释器，**保长替换** ⇒ 切片不错位）之后的源上判定。理由：① 本任务第 1 步**逐字要求** `isWhitelistedHost` 的注释写明「这是精确成员判定，不是 `endsWith`」；② 本任务验收条**逐字写着**套件「不得出现 `fetch(`」，而套件头注释正是写这种免责声明的地方（「不含 `picker`」「不使用 `fetch(`」）—— 原文判定会让两条门禁**恒红**。**一对验证（合成实现上实跑）**：① 注释形态（注释里含 `endsWith` / `picker` / `fetch(`）⇒ `分类器 ok` 与 `套件形态 ok`、均 `exit 0`；② 单点变异分别把 `endsWith(` 写进 `isWhitelistedHost` 的代码、把 `fetch(` 写进套件代码 ⇒ 各报「白名单用了 endsWith（实测会放行 evilgithub.com）」与「分类器用例不得发真实网络请求」。
    - **www 归一化判据的转义口径（本轮修订，判据可满足性）**：门禁一那条 `replace(/^www\./` 的字面量此前在计划里被多转义了一层（JS 字符串里成为 `replace(/^www\\./`），即按 `<action>` 逐字实现的源码**永远匹配不上** ⇒ 恒红且属「判据形态不可满足」类。本轮已改成与源码同级的单反斜杠形态。**验证**：合成实现（`.replace(/^www\./, '')`）⇒ `exit 0`；若把归一化整段删掉 ⇒ 报「白名单未做前导 www. 归一化」。
  </acceptance_criteria>
  <done>`classifyImportUrl` 把 GitHub 三形态与 raw 直链映射成确定的 zipball / raw 目标（含 `scopeRel` 与 `ref`），六条白名单按「www 归一化 + 精确成员」判定，`api.github.com` 等无载荷语义的四类来源显式回 `unsupported_url` 并带可读原因。</done>
</task>

<task type="auto">
  <name>Task 2: 下载器（逐跳 / 流式上限 / magic bytes / 失败清理）+ 直链准备器 + url 分支接线</name>
  <files>ai-skills-manager.js, ai-manager.js, tests/test-skills-import-net.js</files>
  <read_first>
    - `search-manager.js`：`:169-181`（11 条私网网段的唯一权威表）、`:195-214`（`isPrivateHost` 的实现与 fail-closed 语义）、`:1668`（它已导出）、`:1586-1645`（`fetchUrl` 的重定向循环 —— **反面参照**：走完循环不抛「重定向过多」、`await res.text()` 一次性读入、`FETCH_DEFAULT_MAX_LENGTH = 12_000` **字符**截断）
    - `favicon-fetcher.js`：`:40-42`（`net.fetch` 的既有选项形状 `{ signal: AbortSignal.timeout(TIMEOUT_MS) }`）与 `main.js:2974` 的同款先例
    - `media-remuxer.js`：`:514-579`（流式写盘 + 失败 `unlinkSync` 清半成品 + `settled` 幂等 —— 本任务的失败清理形态照它）
    - `ai-skills-manager.js`：`51-03` 的 `extractAndValidatePackage` / `locateSkillRoot` / `buildImportPreview` / `importUserSkill`（**本任务只复用，不改**；`prepareFromRawFile` 必须产出与前者**同一个** `pkgRoot` 形状）、`IMPORT_LIMITS`、Task 1 的 `classifyImportUrl`
    - `ai-manager.js`：`51-03` 的 `previewSkillImport`（`kind === ''url''` 分支当前抛 `UNSUPPORTED_URL` —— 本任务把它替换成真实分流）、`this._skillImports` 的字段
    - `tests/test-skills-http-api.js`：`:410-445`（`postStream` 的真实字节流客户端 —— stub server 侧的参照）、`:312-360`（可注入行为的本地 `http.createServer` 夹具形态）
    - `.planning/phases/51-zip/51-VALIDATION.md`：`## Manual-Only Verifications` 的三行（真实 GitHub 端到端 / 403·429 限流 / `skills.sh` 真实可达性）—— 本任务的 stub server **不覆盖**这些，它们各自需要一次性人工证据
  </read_first>
  <action>
    1. **`ai-skills-manager.js` 新增惰性 require**：`function getSearchManagerLazy() { return require(''./search-manager''); }`（形状照同文件既有的两个惰性 require；注释写明理由：`search-manager.js` 顶层 `require(''electron'')`，而本模块必须零 electron 依赖 ⇒ 只在**调用期**接它，且**只**取 `isPrivateHost` 一个判据）。
    2. **`async function downloadPackage(deps, { url, destPath, maxBytes, kind, fallbackRef })`**（`deps = { fetchImpl, isPrivateHost, hostWhitelist }`，**全部有安全默认值**；`fallbackRef` 是可选的「404 时重试一次」用的 ref）：
       - 默认值表达式：`fetchImpl = (u, o) => require(''electron'').net.fetch(u, o)`（惰性，纯 Node 下不会被调用）、`isPrivateHost = getSearchManagerLazy().isPrivateHost`、`hostWhitelist = HOST_WHITELIST`。**判据（网段表）只有一份**：注释逐字写明「私网判据来自 `search-manager.isPrivateHost`，本模块**不**复制网段表」。
       - **逐跳循环**：`for (let hop = 0; hop <= MAX_REDIRECTS; hop++)`：
         - `const u = new URL(current)`；`u.protocol !== ''https:''` ⇒ `UNSUPPORTED_URL`；`!hostWhitelist.includes(normalizeHost(u.hostname))` ⇒ `UNSUPPORTED_URL`；`await isPrivateHost(u.hostname)` 为真 ⇒ `DOWNLOAD_FAILED`（message 逐字「拒绝访问内网地址」+ 主机名）；**每一跳都跑这三条**（不是只跑首跳）。
         - `resp = await fetchImpl(current, { redirect: ''manual'', signal: AbortSignal.timeout(FETCH_TIMEOUT_MS) })`。
         - `3xx`（301/302/307/308）⇒ 读 `location`，`new URL(loc, current)` 解析（**相对跳转也要能解析**），`current = 其 href`，`continue`；
         - `status >= 400` ⇒ 按**真实原因**分类：`403` / `429` ⇒ `DOWNLOAD_FAILED`（message 含「访问被拒 / 触发限流，请稍后重试」，并带上 `x-ratelimit-remaining` / `retry-after` 头**若存在**）；`404` ⇒ `DOWNLOAD_FAILED`（message 含「地址或 ref 不存在」，并在 `kind === ''zipball''` 且存在 `fallbackRef` 时由调用方决定是否重试 —— 见第 4 步）；其它 ⇒ `DOWNLOAD_FAILED`（message 含状态码）。
         - `2xx` ⇒ 跳出循环进入流式写盘。
       - **循环走完仍未拿到 2xx** ⇒ `throw makeImportError(REDIRECT_LIMIT, ''重定向次数超过上限（<N>）'')` —— **不得**带着最后一个 3xx 掉出（那会让用户看到 `HTTP 301` 这种误导性错误；这正是 `fetchUrl` 的既有缺陷，本计划顺手修）。
       - **流式写盘**：`const rs = Readable.fromWeb(resp.body)`；`pipeline(rs, counterWritable, fs.createWriteStream(destPath))` 形态（或等价的手写累加 + `write`）：累加 `size`，`size > maxBytes` ⇒ `rs.destroy()` + 抛 `DOWNLOAD_FAILED`（message 含上限与当前值）；**任何失败路径都必须 `unlinkSync(destPath)` 清半成品**（`try/catch/finally` + `settled` 幂等，形态照 `media-remuxer.js`）。
       - 成功 ⇒ 返回 `{ bytes: size, contentType: resp.headers.get(''content-type'') || 空字符串, finalUrl: current }`。
    3. **`verifyPackageBytes(destPath, kind)`（内部）**：
       - `kind === ''zipball''` ⇒ 读前 4 字节：`50 4B 03 04`（正常 zip）或 `50 4B 05 06`（空包）以外 ⇒ `NOT_A_ZIP`，message **用 `content-type` 补充**（例：`拿到的不是 zip 文件（content-type: text/plain）—— 请核对地址或 ref 是否存在`）。
       - `kind === ''raw-skill''` ⇒ **不做 magic 校验**（实测 raw 的 content-type 是 `text/plain`）；改为「文件非空」+「前 512 字节无 `\x00`」⇒ 否则 `NOT_A_ZIP`（message：「直链返回的不是文本内容」）。
    4. **`async function prepareFromRawFile({ importDir, name, text })`**（**第二个准备器，产出与 `extractAndValidatePackage` 同一个 `pkgRoot` 形状**；**签名里没有依赖注入形参** —— 它只用 Node `fs` 顺序写盘，不需要 `deps`，与 `downloadPackage(deps, …)` 的形态刻意不同）：`fs.mkdirSync(path.join(importDir, ''pkg'', name), { recursive: true })` + 写 `<importDir>/pkg/<name>/SKILL.md`，返回 `{ importDir, pkgRoot: path.join(importDir, ''pkg''), entries: <单文件条目数组，形状与 readSkillPackageEntries 的产物一致> }`。注释写明：它**不绕过**任何后续判据（全树校验 / 技能根定位 / 预览 / 落盘都走同一段代码），`yauzl` 那一步只是**没有档案可解**。
    5. **`ai-manager.js` 的 `previewSkillImport` `kind === ''url''` 分支**替换为真实分流：
       - `const cls = getAiSkillsManagerLazy().classifyImportUrl(url)`；`cls.kind === ''unsupported''` ⇒ 抛 `UNSUPPORTED_URL`（原样带 message）。
       - 建 `importDir`（**唯一一处** `fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))`，与 `51-03` 的 zip 分支**共用同一段建目录代码或同一个私有方法**，不得出现两份 `mkdtempSync` 语义）。
       - `cls.kind === ''zipball''` ⇒ `downloadPackage(undefined, { url: cls.target, destPath: path.join(importDir, ''pkg.zip''), maxBytes: IMPORT_LIMITS.MAX_TOTAL_BYTES, kind: ''zipball'', fallbackRef: cls.fallbackRef })` → `verifyPackageBytes` → `extractAndValidatePackage(env, { importDir, scopeRel: cls.scopeRel })`；**若 404 且 `fallbackRef` 存在** ⇒ 用 fallbackRef 重构 codeload URL **重试恰一次**，仍失败则把原始 404 原因抛给用户。
       - `cls.kind === ''raw-skill''` ⇒ `downloadPackage(undefined, { kind: ''raw-skill'', destPath: path.join(importDir, ''raw.tmp'') })` → `verifyPackageBytes` → 读文本（`fs.readFileSync(destPath, ''utf8'')`）→ `deriveImportName(parseSkillFrontmatter(text), <url 的父目录名>)` 定名 → `prepareFromRawFile({ importDir, name, text })`。
       - **两条分支之后的公共段**（**必须是一段代码，不得复制**）：全树校验（递归 `lstat` + realpath）→ `locateSkillRoot(pkgRoot, scopeRel)` → `collectPreviewStats` → `buildImportPreview` → 写入 `this._skillImports`（`origin: ''url''`）→ 返回 `{ importId, preview }`；失败时删 `importDir`（**不得留残留**）。
       - **生产调用点必须以缺省依赖形态调用**（首参**逐字** `undefined` ⇒ 走 `downloadPackage` 的参数默认值表达式；**不得**传 `deps` 变量、也不得传内联依赖对象）—— 这条由门禁以**正命题**断言（判据三条：① `previewSkillImport` 内**每个** `downloadPackage(` 调用的首参逐字 `undefined` ⇒ `downloadPackage(deps, { … })` 与 `downloadPackage({ … })` 两种可达形态都转红；② 三个可注入键在 `ai-manager.js` 出现 **0** 次；③ 三个键各自只在 `downloadPackage` 的参数默认值表达式里出现一次且接真实实现）。见 `must_haves.truths` 第四条。⚠️ 本步的示例调用形态与本节收尾的纪律句**必须一致**（都写 `undefined`）—— 两处不一致会让执行者按示例落地、而被门禁判红。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js && node --check ai-manager.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const src=stripC(fs.readFileSync("ai-skills-manager.js","utf8"));const bad=[];for(const t of ["function getSearchManagerLazy(","async function downloadPackage(","function verifyPackageBytes(","async function prepareFromRawFile("])if(src.indexOf(t)===-1)bad.push("缺 "+t);const d=src.indexOf("async function downloadPackage(");const b=src.slice(d,src.indexOf("\n}\n",d));for(const t of ["MAX_REDIRECTS","isPrivateHost(","hostWhitelist.includes(","AbortSignal.timeout(","Readable.fromWeb(","unlinkSync("])if(b.indexOf(t)===-1)bad.push("downloadPackage 缺 "+t);if(b.indexOf("REDIRECT_LIMIT")===-1)bad.push("跳数超限未抛 REDIRECT_LIMIT（会带着 3xx 掉出 ⇒ 报 HTTP 301 这种误导性错误）");if(b.indexOf("arrayBuffer()")!==-1)bad.push("downloadPackage 用了 arrayBuffer（判据对象是「不无上限读入内存」）");if(b.indexOf("res.text()")!==-1||b.indexOf("resp.text()")!==-1)bad.push("downloadPackage 用了 text()（Anti-Pattern 7 的破坏性形态）");const sm=src.indexOf("function getSearchManagerLazy(");const smb=src.slice(sm,src.indexOf("\n}\n",sm));if(smb.indexOf("require(\x27./search-manager\x27)")===-1)bad.push("getSearchManagerLazy 未惰性 require search-manager");if(/PRIVATE_IP_RANGES|^const .*RANGES/m.test(src))bad.push("本模块复制了一份私网网段表（判据必须单源）");if(src.indexOf("fetchUrl(")!==-1)bad.push("出现了 fetchUrl(（Anti-Pattern 7：不得复用 fetchUrl 抓二进制）");if(bad.length){console.error("下载器不合格:\n"+bad.join("\n"));process.exit(1)}console.log("下载器 ok（逐跳三校验 + 跳数抛错 + fromWeb 流式 + unlink 清理 + 私网判据单源）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `下载器 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const am=stripC(fs.readFileSync("ai-manager.js","utf8"));const i=am.indexOf("async previewSkillImport(");const b=am.slice(i,am.indexOf("\n  }",i));if(b.indexOf("classifyImportUrl(")===-1)bad.push("url 分支未调 classifyImportUrl");if(b.indexOf("downloadPackage(")===-1)bad.push("url 分支未调 downloadPackage");const hits=[...b.matchAll(/downloadPackage\(/g)].map(m=>m.index);if(!hits.length)bad.push("生产调用点缺 downloadPackage( 调用（缺省形态判据无对象 ⇒ 不得以空集通过）");for(const k of hits){const tail=b.slice(k,k+44);if(!/^downloadPackage\(\s*undefined\s*,/.test(tail))bad.push("生产调用点不是缺省依赖形态（首参必须逐字 undefined，实测形态："+tail.replace(/\n/g," ").slice(0,34)+"）")}const msrc=stripC(fs.readFileSync("ai-skills-manager.js","utf8"));const prod=stripC(fs.readFileSync("ai-manager.js","utf8"));for(const key of ["fetchImpl","isPrivateHost","hostWhitelist"]){const pn=(prod.match(new RegExp("\\b"+key+"\\b","g"))||[]).length;if(pn!==0)bad.push("ai-manager.js（生产侧）出现 "+key+" "+pn+" 处（生产调用点必须走默认值，不得注入）")}if(!/isPrivateHost\s*[:=][^}]*?getSearchManagerLazy\(\)\.isPrivateHost/.test(msrc))bad.push("默认 isPrivateHost 未接 search-manager 的真实判据（可注入缝被改成了放宽值）");if(!/hostWhitelist\s*[:=][^}]*?HOST_WHITELIST/.test(msrc))bad.push("默认 hostWhitelist 未接 HOST_WHITELIST（可注入缝被改成了放宽值）");if(!/fetchImpl\s*[:=][^}]*?net\.fetch/.test(msrc))bad.push("默认 fetchImpl 未接 electron net.fetch（可注入缝被改成了放宽值）");const mk=(am.match(/mkdtempSync\(/g)||[]).length;if(mk!==1)bad.push("mkdtempSync 在 previewSkillImport 内应恰 1 处（两条来源共用同一段建临时根代码），实测 "+mk);if(b.indexOf("prepareFromRawFile(")===-1)bad.push("缺直链 SKILL.md 的准备器调用");if(b.indexOf("extractAndValidatePackage(")===-1)bad.push("缺 zip 分支的准备器调用");const c=(f,tok)=>{const t=stripC(fs.readFileSync(f,"utf8"));return (t.match(new RegExp(tok.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),"g"))||[]).length};const a=c("ai-skills-manager.js","importUserSkill("),bb=c("ai-manager.js","importUserSkill("),z=c("ai-skills-manager.js","yauzl.openPromise(");if(a!==1)bad.push("importUserSkill( 定义处应为 1，实测 "+a);if(bb!==1)bad.push("importUserSkill( 调用处应为 1，实测 "+bb);if(z!==1)bad.push("yauzl.openPromise( 应恰 1 处（本计划不得新增第二个解压入口），实测 "+z);if(bad.length){console.error("url 接线不合格:\n"+bad.join("\n"));process.exit(1)}console.log("url 接线 ok（同一段后段 + 缺省依赖正命题：首参逐字 undefined 且默认值接真实实现 + 单一落盘与单一解压入口保持）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `url 接线 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import-net.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 35（分类器矩阵 ≥ 20 + 本任务新增 ≥ 15），或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`tests/test-skills-import-net.js` 的 `# fail 0` 且 `# tests` ≥ 35。
    - **stub server 覆盖六类**（全部离线）：① 白名单外主机 ⇒ `unsupported_url`；② `http:` ⇒ `unsupported_url`；③ **第二跳**是私网地址 ⇒ `download_failed`（证明逐跳而非首跳）；④ 重定向链超过 `MAX_REDIRECTS` ⇒ `redirect_limit` 且失败信息**不含** `HTTP 30` 形态（正命题：证明「超限抛错」而不是「带着 3xx 掉出」）；⑤ magic bytes 不匹配（stub 回 `text/plain` 的 14 字节）⇒ `not_a_zip` 且 message 含 content-type；⑥ 持续吐字节超过 `maxBytes` ⇒ `download_failed` **且 `<importDir>` 下不残留半成品**（断言 `fs.existsSync(destPath) === false`）。
    - **可失败性**：门禁一当前树报 4 项缺失；门禁二当前树报「url 分支未调 classifyImportUrl」等（`51-03` 的占位分支形态）。执行者落地后自行做两次单点变异：① 把逐跳循环里的三条校验移到循环**外**（只校验首跳）⇒ 第 ③ 类用例转红；② 把 `hop > MAX_REDIRECTS` 的抛错删掉 ⇒ 第 ④ 类用例转红。
    - **注入面判据是正命题、且覆盖两种可达调用形态（第二轮修订的改动点）**：门禁二不再用「不含这三个键」的否命题，而是**逐个** `downloadPackage(` 调用点断言首参**逐字** `undefined`，并配三条正命题（三键在 `ai-manager.js` 出现 0 次 / 三键各在 `downloadPackage` 的参数默认值表达式里恰一次 / 默认值分别接 `getSearchManagerLazy().isPrivateHost`、`HOST_WHITELIST`、`require(''electron'').net.fetch`）。**单点变异（每种形态各验一次，记进 SUMMARY）**：① 首参由 `undefined` 改成 `deps` 变量 ⇒ 门禁二报「生产调用点不是缺省依赖形态」；② 首参改成内联对象（`downloadPackage({ url… }, {…})`）⇒ **同一条**报出 —— 这正是旧判据 `/downloadPackage\([^)]*\{\s*(fetchImpl|isPrivateHost|hostWhitelist)/` 覆盖不到、会假绿的形态；③ 把参数默认值里的 `hostWhitelist = HOST_WHITELIST` 改成 `hostWhitelist = []` ⇒ 门禁二报「默认 hostWhitelist 未接 HOST_WHITELIST」。
    - **负向 token 一律先剥注释再判（W2）**：门禁一与门禁二里凡负向 token（`fetchUrl(` / `arrayBuffer()` / `.text()` / `PRIVATE_IP_RANGES` / `^const .*RANGES`）都改在 `stripC`（与 `51-03` / `51-04` / `51-06` 同一份五态剥注释器，**保长替换** ⇒ 原文与剥离面的下标一一对应、切片不错位）之后的源上判定。**理由（本任务第 2 步强制要求的那条注释）**：实现必须写「私网判据来自 `search-manager.isPrivateHost`，本模块**不**复制网段表」——在原文上判会让注释里出现的 `PRIVATE_IP_RANGES` 命中而**恒红**。**一对验证（两条都要做，记进 SUMMARY）**：① 按第 2 步的措辞把该标识符写进**注释** ⇒ 门禁一在**合成实现**上 `exit 0`（当前树因符号未落地本就为红，故绿轮在合成实现上取证）；② 一次单点变异：把它真的写进**代码**（`const PRIVATE_IP_RANGES = [...]`）⇒ 门禁一报「本模块复制了一份私网网段表」。
    - **`pkgRoot` 形状同一**：两条准备器返回的对象在用例里对 `pkgRoot` 的**相对结构**断言一致（`<importDir>/pkg` 下的技能根目录 + 其中的 `SKILL.md`），且两条路径之后调用的是同一段后段（源码判据：`locateSkillRoot(` 在 `ai-manager.js` 恰 1 处调用）。
    - **上限单源**：下载用的 `maxBytes` 取自 `IMPORT_LIMITS.MAX_TOTAL_BYTES`（不是新字面量）—— 判据见本计划 Task 3 的跨文件断言。
    - **零实时外网**：套件里所有请求都指向 `http://127.0.0.1:<port>` 且注入 `isPrivateHost: async () => false` + `hostWhitelist: [''127.0.0.1'']`（**注入只出现在测试里**，生产调用点用默认值 —— 门禁二已断言）。
  </acceptance_criteria>
  <done>网络包与 zip 上传经**同一条**后段落盘；下载路径对每一跳校验协议 / 白名单 / 私网，跳数超限抛 `redirect_limit`，magic bytes 与流式上限各自成闸且失败清理半成品；直链 SKILL.md 由 `prepareFromRawFile` 产出同一个 `pkgRoot` 形状，不新增任何解压或落盘入口。</done>
</task>

<task type="auto">
  <name>Task 3: 网络面端到端与真实形态证据 + 上限单源跨文件断言 + 过渡态收口</name>
  <files>tests/test-skills-import-net.js, ai-skills-manager.js, tests/test-skills-import.js</files>
  <read_first>
    - `tests/test-skills-import-net.js`：Task 1/Task 2 落地的分类器矩阵与 stub server 夹具（**先读**，本任务只追加）
    - `tests/test-skills-import.js`：`51-03` 的端到端 tracer 用例与 `51-04` 的 `assertRejected` / `PENDING_CODES_NETWORK` 机制（本任务要**缩小**那张豁免清单）
    - `.planning/phases/51-zip/51-VALIDATION.md`：`## Manual-Only Verifications` 的前三行（真实 GitHub 端到端 / 403·429 / `skills.sh` 可达性）—— 本任务把它们各自转成一条**可离线重跑**的形态断言 + 一段一次性人工证据记录
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 5`（`skills.sh` 的 308 `location` 与 `www.skills.sh` 的 404/SPA 形态）、`### 实测 2` 的限流头实测（`x-ratelimit-limit: 60`、`retry-after` 只在触限后出现）
  </read_first>
  <action>
    1. **stub server 端到端两来源**（本任务的核心用例，全部离线）：
       - **zipball 形态**：stub 用 `buildZip`（`tests/helpers/make-malicious-zip.js`）造一个含 `<repo>-<ref>/skills/pdf/SKILL.md` 的包 ⇒ 分类器给 `scopeRel = ''skills/pdf''` ⇒ `previewSkillImport({ kind: ''url'' })` ⇒ 断言**顶层前缀被剥离**（`preview.name` 与树里**不含** `repo-ref/` 段）⇒ `commitSkillImport` ⇒ 断言 `skills/<name>/SKILL.md` 落盘且回读可见（这条用例同时证明「网络来源也走唯一落盘实现」）。
       - **raw 直链形态**：stub 回一个 SKILL.md 文本（`content-type: text/plain`）⇒ 断言走 `prepareFromRawFile` 且**不**做 magic 校验、预览六字段骨架齐备、commit 后落盘。
       - **302 链形态**：stub 先回 302 到自身另一路径再回 zip ⇒ 断言最终拿到包（证明逐跳跟随有效，且白名单内的跳转不被误拒）。
    2. **`skills.sh` 的真实 308 形态断言（离线可重跑）**：用 stub 造「`/` ⇒ 308 + `location: https://www.skills.sh/`」这一**实测形态**（不跟随外部网络），断言：① 归一化后 `www.skills.sh` **在白名单内**（不因 308 在第一跳被自己的逐跳校验拒 —— 这是 CR-1 的验收点）；② 但分类器对它仍回 `unsupported_url`（本阶段无载荷语义）⇒ 两条**同时**断言才能区分「白名单收它」与「有载荷语义」。
       - **一次性人工证据**：执行者跑一次 `curl -sS -o /dev/null -D - -I https://skills.sh/` 并把 `308` + `location: https://www.skills.sh/` 的原始响应**逐字**记进 `51-05-SUMMARY.md`（这是「该 stub 形态与真实站点一致」的唯一证据；**不得**让套件依赖实时外网，CR-12）。
    3. **403 / 429 / 404 的真实原因映射用例**（离线）：stub 分别回 403 / 429（带 `retry-after: 60`）/ 404（14 B `text/plain`）⇒ 断言：403 / 429 ⇒ `download_failed` 且 message 含「限流 / 稍后重试」类可操作指引；404 ⇒ `download_failed` 且 message 含「地址或 ref 不存在」且**不是** `not_a_zip`（顺序证明：状态码分类**先于** magic 校验 —— 这正是「真实原因」的判据）。
    4. **上限单源跨文件断言**（源码级，防止两个常量漂移）：读 `main.js` 文本取 `const MAX_SKILL_PACKAGE_BYTES = 32 * 1024 * 1024;` 的字面量，与 `require(''../ai-skills-manager'').IMPORT_LIMITS.MAX_TOTAL_BYTES` **运行期**比对并断言相等；不相等 ⇒ 报「HTTP body 上限与解压累计上限已漂移（同值不同量，两处必须同步）」。
    5. **`fallbackRef` 的重试恰一次**：stub 让 `refs/heads/main` 回 404、`refs/heads/master` 回 zip ⇒ 断言成功且**请求序列**是 `main` 然后 `master`（stub 记录路径清单来断言**不是**无限重试、也不是先试 master）。
    6. **过渡态收口**：`tests/test-skills-import.js` 的 `PENDING_CODES_NETWORK` 里若含本计划已覆盖的码 ⇒ 从清单移除（`unsupported_url` / `download_failed` / `redirect_limit` / `not_a_zip` 四个码在本计划**全部**有真实用例）—— 判据：本计划结束时 `tests/test-skills-import.js` 的矩阵用例报告**零跳过项**；若仍有跳过项，说明有码未覆盖，必须补用例而**不是**保留清单。
  </action>
  <verify>
    <automated>node tests/test-skills-import-net.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 50（上一任务的 ≥ 35 + 本任务新增 ≥ 15），或进程以非零退出</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)|PENDING_CODES|跳过的码"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或**出现任何跳过项**（网络面四码本计划已全覆盖，清单必须为空），或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/test-skills-import-net.js","utf8");const bad=[];for(const s of ["prepareFromRawFile","302","308","retry-after","403","429","404","MAX_TOTAL_BYTES","master","repo-ref"])if(t.indexOf(s)===-1)bad.push("用例缺 token: "+s);if(t.indexOf("skills.sh")===-1)bad.push("缺 skills.sh 的 308 形态断言");if(t.indexOf("http.createServer")===-1)bad.push("缺本地 stub server（CR-12：不得依赖实时外网）");if(bad.length){console.error("网络面用例不足:\n"+bad.join("\n"));process.exit(1)}console.log("网络面用例 ok（两来源端到端 + 302/308 链 + 403/429/404 + 上限单源 + fallbackRef）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `网络面用例 ok`，或 stderr 列出任意一条缺失 token</fails_when>
    <automated>node -e 'const fs=require("fs");const main=fs.readFileSync("main.js","utf8");const m=main.match(/const MAX_SKILL_PACKAGE_BYTES = (\d+ \* \d+ \* \d+);/);const bad=[];if(!m){bad.push("main.js 缺 MAX_SKILL_PACKAGE_BYTES 字面量（口径失效）")}else{const v=eval(m[1]);const lim=require("./ai-skills-manager").IMPORT_LIMITS.MAX_TOTAL_BYTES;if(v!==lim)bad.push("HTTP body 上限 "+v+" 与解压累计上限 "+lim+" 已漂移（同值不同量，两处必须同步）")}if(bad.length){console.error("上限单源不合格:\n"+bad.join("\n"));process.exit(1)}console.log("上限单源 ok（MAX_SKILL_PACKAGE_BYTES === IMPORT_LIMITS.MAX_TOTAL_BYTES）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `上限单源 ok`，或 stderr 指出两个上限已漂移</fails_when>
  </verify>
  <acceptance_criteria>
    - 四条门禁全绿；`tests/test-skills-import-net.js` 的 `# tests` ≥ 50 且 `# fail 0`；`tests/test-skills-import.js` 的矩阵用例**零跳过项**。
    - **可失败性（落地前实跑一遍）**：四条门禁当前树分别报「缺 token」（新套件未落地）/ 缺 `MAX_SKILL_PACKAGE_BYTES` 字面量（`51-03` 才加）。执行者落地后自行做三次单点变异：① 把 404 分支从「状态码分类」挪到 magic 校验**之后** ⇒ 第 3 类用例的「不是 `not_a_zip`」断言转红；② 把 `fallbackRef` 的重试改成循环（不设上限）⇒ 请求序列断言转红；③ 把 `main.js` 的常量改成 `16 * 1024 * 1024` ⇒ 上限单源断言转红。
    - **两来源同管线的端到端证据**：zipball 与 raw 直链**各有一条**「preview → commit → `skills/<name>/SKILL.md` 落盘 → 回读可见」的用例，两条都走 `importUserSkill`（判据由 `51-03`/`51-04` 的源码扫描持续守住）。
    - **一次性人工证据成文**：`curl -sS -o /dev/null -D - -I https://skills.sh/` 的原始响应（`308` + `location`）逐字进 `51-05-SUMMARY.md`；同时写明「套件不依赖实时外网」。
    - `tests/test-skills-import.js` 的 `PENDING_CODES_NETWORK` **为空**（本计划覆盖了网络面全部四码）—— 这是「每个码至少一例」从过渡态转为完全态的时点。
  </acceptance_criteria>
  <done>网络导入两条来源都端到端跑通（stub server 离线可重跑）：zipball 剥前缀 + 子路径定位 + 落盘 + 回读；raw 直链经准备器进同一管线；302/308 链逐跳跟随；403/429/404 给真实原因且状态码分类先于 magic；上限单源有跨文件断言；网络面四码的过渡豁免清单清空。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户提供的 URL → 主进程出网请求 | URL 是**不可信输入**；它决定我们向哪里发请求（SSRF 面） |
| 远端响应 → 临时文件 → 解压管线 | 远端内容不可信；`content-type` 可伪造（magic bytes 才是权威） |
| 重定向链 → 白名单边界 | 每一跳都可能指向另一个主机（含私网） |
| `net.fetch`（Chromium 网络栈）↔ `dns.lookup`（Node） | 两者可能用不同的解析器 / 代理 ⇒ 被校验的 IP ≠ 被连接的 IP（DNS rebinding） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-31 | Information Disclosure | SSRF：URL 指向内网 / 元数据服务 / 本地端口 | high | mitigate | https-only + 主机白名单（`www.` 归一化 + 精确成员）+ **逐跳** `isPrivateHost` + 跳数上限（超限**抛错**）+ 流式字节上限；用例覆盖「第二跳是私网」与「跳数超限」两类 |
| T-51-32 | Bypass | 重定向绕过白名单（302 到白名单外的域） | high | mitigate | 逐跳三校验（协议 / 白名单 / 私网）在**每一跳**执行；302/308 链用例 + 「第二跳私网被拒」用例 |
| T-51-33 | Spoofing | DNS rebinding（被校验的 IP ≠ 被连接的 IP） | high | accept | **无法在「先解析再请求」模型下根除** ⇒ D-16 要求如实披露（`51-07` 写进产品文档与威胁模型）；缓解 = 白名单把收益压到接近零 + https-only + magic bytes + 流式上限。**不得**写成「已缓解 / 已消除」 |
| T-51-34 | Tampering | `content-type` 伪造 / 误判（OS 识别的 `application/x-zip-compressed`、codeload 404 的 `text/plain`） | medium | mitigate | **magic bytes 为权威**（`50 4B 03 04` / `50 4B 05 06`），`content-type` 只用于把错误说清楚；raw 分支不校验 magic 而校验「非空 + 无 NUL」 |
| T-51-35 | Denial of Service | 无上限流式下载（大文件 / 无限流） | high | mitigate | `Readable.fromWeb` + 累加计数，超 `IMPORT_LIMITS.MAX_TOTAL_BYTES` 即 `destroy` + 抛错；声明长度不可信 ⇒ 判据是**实读字节**；失败必 `unlinkSync` 半成品 |
| T-51-36 | Tampering | 直链 SKILL.md 被写成第二条落盘路径 | high | mitigate | `prepareFromRawFile` 只产出 `pkgRoot`，之后是**同一段**后段；判据：`importUserSkill(` 恰 2 处、`yauzl.openPromise(` 恰 1 处、`locateSkillRoot(` 在 `ai-manager.js` 恰 1 处调用 |
| T-51-37 | Tampering | 为通过测试而在生产路径注入放宽的 `hostWhitelist` / no-op `isPrivateHost` | high | mitigate | 门禁断言生产调用点未传这三个键（默认值表达式里接真实实现）；`prohibitions` 具名登记 |
| T-51-38 | Repudiation | 限流 / 404 被折叠成通用错误 ⇒ 用户不知该怎么办 | medium | mitigate | 403 / 429 / 404 各自的 message（含 `retry-after` 与 x-ratelimit 头**若存在**）；「状态码分类先于 magic 校验」由用例正命题守住 |
| T-51-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本计划**零安装动作**（依赖由 `51-02` 落地并已过 blocking-human 合法性闸） |
</threat_model>

<verification>
- 三个任务的 automated 门禁绿；新建套件 `tests/test-skills-import-net.js` 的 `# fail 0`；`tests/test-skills-import.js` 与 `tests/test-skills-http-api.js` 保持绿。
- **跨计划回归判据**：`importUserSkill(` 全仓恰 2 处、`yauzl.openPromise(` 恰 1 处（本计划不得新增落盘或解压入口）。
- **上限单源**：`main.js` 的 `MAX_SKILL_PACKAGE_BYTES === IMPORT_LIMITS.MAX_TOTAL_BYTES`（跨文件断言）。
- **本计划不覆盖**：真实 GitHub 的端到端成功路径（只在 `51-VALIDATION.md` 的 Manual-Only 表里，需一次性人工跑）、UI（51-06）、文档与账本（51-07）。
- **诚实边界（必须进 51-07 的文档）**：DNS rebinding 残余风险；`skills.sh` / `www.skills.sh` / `objects.githubusercontent.com`「保留为未来兼容，本阶段无消费者」；`api.github.com` 显式拒绝的理由；ref 缺省的 main→master 尝试策略。
</verification>

<success_criteria>
- `github.com` 三形态（仓库 / tree 子路径 / blob）与 `raw.githubusercontent.com` 直链各自转出确定的 zipball 或 raw 目标；zipball 顶层前缀被剥离。
- 网络面六道校验（https / 白名单精确 / 逐跳私网 / 跳数上限 / 流式上限 / magic bytes）各有独立用例，且每一跳都生效。
- 网络来源与 zip 上传经同一条后段落盘（判据仍在）；直链 SKILL.md 不产生第二条落盘路径。
- 403 / 429 / 404 给可操作的真实原因；跳数超限抛 `redirect_limit` 而不是 `HTTP 3xx`。
- 全部网络测试离线可重跑（本地 stub server），真实站点形态另有一次人工证据成文。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-05-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-05）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 模块常量（导出） | `HOST_WHITELIST`（六条）、`MAX_REDIRECTS`（5）、`FETCH_TIMEOUT_MS`（30 s） | `ai-skills-manager.js` |
| 导出函数 | `isWhitelistedHost(hostname)`（www 归一化 + 精确成员）、`classifyImportUrl(url)` | `ai-skills-manager.js` |
| 内部惰性 require | `getSearchManagerLazy()` | `ai-skills-manager.js` |
| 导出函数（异步） | `downloadPackage(deps, { url, destPath, maxBytes, kind, fallbackRef })`、`prepareFromRawFile({ importDir, name, text })`（**无依赖注入形参** —— 与 51-03 的写法、51-05 任务体的调用点三处一致） | `ai-skills-manager.js` |
| 内部函数 | `verifyPackageBytes(destPath, kind)`、`normalizeHost(hostname)` | `ai-skills-manager.js` |
| 类方法分支 | `AIManager.prototype.previewSkillImport` 的 `kind === 'url'` 真实分流（分类 → 下载 → 准备器 → 同一段后段） | `ai-manager.js` |
| 测试套件（**新文件**） | `tests/test-skills-import-net.js`（本地 stub server，零实时外网） | `tests/` |

### 本阶段其余计划将新建（此处仅登记，避免在评审时被当作「应已存在」）

| 计划 | 符号 |
|------|------|
| 51-06 | `.skill-import-*` CSS 段（含 3 条作用域覆盖）、导入弹框完整 DOM、`SKILL_IMPORT_ERROR_TEXT`（闭合白名单表）、`renderSkillImportScan()` / `renderSkillImportTree()` / `renderSkillImportConflict()`、`tests/uat-51-import-modal.js` |
| 51-07 | `docs/product/ai-skills.md` §十三【导入】+ 安全边界定稿、`docs/product/ai-agent-workspace.md` §七 的 SEC-10 行为变更、`AGENTS.md` 导入维护约定与测试行、counts-parity 的 `suites` / `cells` 扩展（12 → 20） |
