---
phase: 51-zip
plan: 06
type: execute
wave: 5
depends_on: [51-03, 51-04, 51-05]
files_modified:
  - src/settings.html
  - src/settings-page.js
  - src/styles/main.css
  - tests/test-skills-import.js
  - tests/uat-51-import-modal.js
autonomous: true
requirements: [USER-05, USER-08]
user_setup: []

estimate:
  # ⚠️ 未经本项目校准：estimate-check --calibrated 报 ratio 1.40 / over_budget=true，
  #    但 confidence=low 且 sample_count=0（本项目尚无校准样本）⇒ 该数字是**量级参考而非精确值**。
  #    不要据此重切已经有效的计划形状（本计划的 3 任务边界由 UI-SPEC 的元素分组决定，不由该估算决定）。
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

key-decisions:
  - '**本计划不设 tracer**：UI 是 `51-03` 已跑通纵切的**远端展示层**，不是新子系统；本计划把 `51-03` T1 的「最小入口 + 极简弹框」扩成 `51-UI-SPEC.md` 的完整契约（两 tab / 六字段预览 / 必勾 / 冲突三选一 / 截断折叠 / 键盘与 AT 契约）。tracer 落在 `51-01` 与 `51-03`'
  - '**UI-SPEC 是唯一权威，逐条照抄、不自行设计**：18 个元素（E1..E18）、`## Copywriting Contract` 的 40 条文案、`## 新增 CSS 声明清单`（整段已定稿）、`## 状态机` 的 9 态、`## 禁用态契约` 的 4 种情形、`## 键盘可达性契约`。本计划的 acceptance_criteria 按元素归并（E1-E6 → T1，E7-E18 → T2，跨元素样式与读数 → T3），**不重述** UI-SPEC 已写死的取值'
  - '**`realm://` CSP 的三条硬约束（错一条就等于没做）**：① 初始隐藏**一律走 CSS 类**（`.ai-modal-overlay { display:none }`），**绝不写 markup 内联 `style`**（`style-src ''self''` 会拦掉，元素反而常驻可见 —— AGENTS.md 明文已付过代价）；② 显隐用 JS CSSOM 的**具体值**（`''flex''` / `''none''`），**不用空字符串回落**；③ **绝不把 `.ai-modal-overlay` 用到 `<dialog>` 上**（UA 的 fit-content + inset 全 0 过约束会把盒子钉在 `top:0;left:0`）'
  - '**本计划唯一的滚动容器是 `.skill-import-preview`**（`flex:1 1 auto; min-height:0; overflow-y:auto` + `tabindex="0"`）：`min-height: 0` 是**必需**的（flex 子项默认 `min-height:auto`，不给 0 则永不收缩、弹框被撑破）；**不得**在预览内部再建第二个滚动容器；目录树 / 脚本清单的「只渲染前 50 项」是**内容截断**不是滚动。它同时是**本阶段唯一的「非控件型」键盘停靠点**（零值 `tabindex` 不改 Tab 相对序、不新增焦点环规则）'
  - '**注入纪律（TD-48-01 仍开未修 ⇒ 本阶段的责任是「不扩大缺口」）**：预览里渲染的**全部**字符串都来自不可信包（文件名 / 目录树路径 / `description` 原文 / `diagnostics` 原文 / `allowed-tools` 值）⇒ **零 `innerHTML` / 零 `insertAdjacentHTML` / 零字符串模板拼 HTML**，一律 `document.createElement` + `textContent` + `el.title = …` / `setAttribute`（DOM 属性赋值不走 HTML 解析，与 `escapeHtml` 只转义 `& < >` 的缺口**无关**）。**类名 / id / dataset 全部是白名单字面量**，包内字符串**不得**参与 class / id / dataset 键值的拼接；扩展名标签只渲染**白名单查表**得到的定长文字。**另加显示前净化**（C0/C1 控制字符 + 双向控制符）——**只在显示层，绝不参与拒绝判据**（判据用原始字节，用净化值判会让 RTL override 类样本逃过检查），且被净化字符数 > 0 时行末追加定长提示「（含 N 个控制字符，已隐藏）」——**禁止静默隐藏**'
  - '**弹框打开期间的失败只进弹框内状态行（`#skillImportStatus`）**，**不得**写区级 hint（被遮罩挡住 = 用户看不到 = 静默失败）；弹框关闭后的成功 / 最终失败才走 `setSkillManageHint`（2 秒无条件复位 + `clearTimeout` 纪律）。读 hint 的测试**必须避开这 2000ms 自动清空**（50 收尾实测的「假绿」形态之一）'
  - '**失败文案按 `code` 查闭合白名单表，前端不解析 `message`**：新增的 `IMPORT_SKILL_ERROR` 每个码必须有对应文案；表是 `src/settings-page.js` 内的**闭合白名单**（表外码统一走兜底文案，不回落 `undefined`）。**必须有一条双向覆盖判据**（表键 ⊇ 常量值 ∧ 常量值 ⊇ 表键 —— 单向会漏「后端多一个码前端没文案」或「表里留了已删的码」）。⚠️ 该表当前**只有本区一个消费者**，所以它留在 `src/settings-page.js`；**出现第二个消费者时必须提升到 `src/skill-picker-model.js`**（50 的同一纪律）'
  - '**禁用态一律用原生 `disabled` 属性**（`btn.disabled = true`），**不使用 `aria-disabled`**：`aria-disabled` **不阻止激活**（键盘在输入框里按 Enter 仍会触发 click / 表单提交）⇒ 只做属性「声明」是**假安全**。**禁用原因必须上屏**：`.btn:disabled { pointer-events: none }` 让 `title` 根本无法触发，且 `disabled` 元素不可聚焦（读屏也读不到）⇒ 原因只能靠 `#skillImportGate` 的可见文本承载，**禁止**只写在 `title` 里'
  - '**不引入新的全局交互机制**（风险隔离，与 ROADMAP 把 51 排最后同一取向）：沿用 50 的「**不做完整焦点陷阱（Tab 循环）**」并因此**省略 `aria-modal`**（`aria-modal="true"` 会向 AT 声明「框外内容不可用」，而本契约不强制焦点留在框内 ⇒ 比不写更糟）；模式选择**复用 `.ai-memory-tabs` / `.ai-memory-tab` 类**但**不复用**其 ARIA 形态（改用 `role="group"` + `aria-label="导入方式"` + `aria-pressed` 分段控件 —— ARIA tabs 模式要求 roving tabindex 与方向键，那是一整套新键盘机制）；「展开全部」是**内容截断开关**，**不得**复用 48 的 `.ai-skill-content-box` 折叠族（会产出「同一区域两个控件、两份状态」的漂移面），也**不引入 `aria-expanded`**'
  - '**两条 backstop 的处置**：E2（弹框外壳 overflow）与 E17（动作区 overflow）都是「由 CSS 值推算的布局结论 + 需要真实数据集在真实窗口下的读数」⇒ 它们以**扁平标量** `{ statement, verification: backstop }` 进 `must_haves.truths`；验证方式是**一条带正命题的属性测试**（`modal.getBoundingClientRect().height <= 0.8 * innerHeight`、`preview.scrollTop` 能增大而页面级滚动不动、`actions.scrollWidth <= actions.clientWidth`），**不是**「看着没问题」。若无该显式证据，正确处置是 `human_needed` —— **绝不允许静默通过**。⚠️ 写该驱动**不得重复 `WR-12` 的「否命题空集真」形态**：判据必须能在「未修复的树上转红」，且证据固化进仓库（`IN-16` / `WR-09` 教训）'
  - '**uat 驱动必须避开已实测的四种「假绿」形态**（50 收尾带出 + 49 的 `WR-12`）：① 时间线采样必须**排除 pre-click / same-tick**（否则恒真）；② 读 hint 要避开 **2000ms 自动清空**，且 danger 类名是 `skill-manage-hint-danger` 而非 `danger`；③ 面板在 `display:none` 时 `getBoundingClientRect` **全 0** ⇒ 「0 vs 0 全等」的假绿；④ 生成给 guest 执行的源码字符串里**不得含反引号**（会提前终止外层模板字面量 —— Phase 50 踩过两次）。另：驱动**依赖全局 `playwright`** ⇒ 必须 `NODE_PATH="$(npm root -g)"` 前缀；基线**动态取**（不硬编码本机 `realm-dev` 数据）'

must_haves:
  truths:
    - 'preview 调用返回 {importId, preview} 且不产生 skills/<name>/；仅 commit 调用后才出现该目录；预览六字段（名称 / description 原文 / 目录树+计数 / 字节数 / 脚本清单 / 扫描结论两栏）在渲染快照中齐备 —— **信息集逐条对齐 D-13**（六字段 + 两条补充：`allowed-tools` 免责展示与「结论是启发式而非安全背书」的诚实边界）'
    - '每个 IMPORT_SKILL_ERROR 码至少一条用例覆盖；回传文案含码 + 可读原因；限额类失败文案含限额名与当前值；无任何失败路径返回空 message 或仅通用错误'
    - '**元素顺序是契约**（4 处声明同序、不得调换）：模式选择 → 当前面板 → 状态行 → 预览 → 必勾 → 冲突 → 禁用原因 → 动作区。自查点为：本计划 DOM 骨架（`#skillImportAck` 在 `#skillImportConflict` 之前）、状态机表的列序、Tab 序、以及「预览 → 必勾 → 冲突 → 禁用原因 → 动作区」的布局序'
    - '**[E1] 标题行** flex（`justify-content:space-between`）+ 按钮 `flex-shrink:0` + `white-space:nowrap`；「技能管理」与「导入技能」均为定长字面量，无不可信插值面'
    - '**[E3] 模式选择**：两个按钮 `flex-shrink:0` + `nowrap`，容器 `gap:4px` 不换行；容器 `role="group"` + `aria-label="导入方式"`，按钮用 `aria-pressed`；**不写 `aria-modal`**；两条文案定长'
    - '**[E4] 本地上传面板空态与在途态**：未选文件时状态行显示「尚未选择 zip 文件」+ 面板内恒显包形态说明；选中文件立即进入在途态（状态行「正在读取技能包…」、入口 `disabled`、**零 spinner / 零骨架屏**）；解析失败时文件选择入口**保持可用**以直接重选（**不**新建「重试」按钮）；已选但预览未就绪一律视为在途态（**不存在「部分预览」**）'
    - '**[E5] 网络地址面板**：输入为空 ⇒「获取预览」`disabled` 且在 `#skillImportGate` 说明「请输入网络地址」；下载中状态行「正在从网络地址下载…」且**取消按钮仍可用**（`AbortController` 真正中止请求，不只关弹框）；URL 非法 / 主机不在白名单 / 非 https / 网络或限流失败 ⇒ 状态行 danger 给码与可读原因，**输入框保留已填内容**以便就地修正；**不存在「部分下载可用于预览」的中间态**（流式上限命中即整包拒绝）'
    - '**[E6] 状态行**：`role="status"` + `aria-live="polite"`（**恒为 polite，绝不 `assertive`**）；它是**同一节点承担可见文本与 AT 播报两职**，播报内容 = 可见文本（**不另写一份仅屏读文本**）；允许换行不截断，文本为空时经 CSSOM 置 `display:none`（不走 markup 内联 style、不用空字符串回落）；预览从「在途」变「就绪」时**不移动焦点**'
    - '**[E7] 名称与落点**：技能名等宽单行截断 + `title` 全文；落点行 `将写入 skills/<name>/` 用 `word-break:break-all` 折行不截断；`<name>` 是不可信字符串，经 DOM API 注入'
    - '**[E8] description 原文**：**不截断、不净化、不语义截断**（`white-space: pre-wrap` + `word-break: break-word`）—— 预览的全部意义就是让用户判断净化是否会误伤'
    - '**[E9] 目录树**：默认仅渲染前 50 项 + **恒显**「共 N 个文件 / M 个目录」+ 截断时追加「（当前显示前 K 项）」+「展开全部 / 收起」按钮；1 项时**不渲染**开关；缩进每层 12px 并在**第 8 层封顶**（最大缩进 96px）；渲染顺序确定（同父下目录在前、同级按路径字符串 **UTF-16 码元序**，**不用 `localeCompare`**）；RTL override 与 C0/C1 控制字符在**显示前**净化（拒绝对策仍用原始字节）'
    - '**[E10] 字节数**：定长数值格式（`toFixed(1)` + B/KB/MB/GB）+ 与限额对照同排（`解压后合计 X · 单文件最大 Y（限额：SKILL.md A / 累计 B）`，**A / B 取主进程回传值，前端零字面量**）；整体可换行不横向溢出'
    - '**[E11] 脚本清单**：0 个脚本时**整块不渲染**（不写「未检测到脚本」）；有则每条以 `--skill-error-text` 标红、块内**恒显**「技能不构成额外权限 —— 这些脚本受既有 bash 三档策略与确认卡片约束」；截断口径与目录树同款（默认 50 + 恒显「共 N 个脚本」+ 展开按钮）；扩展名标签只渲染**白名单查表**的定长文字'
    - '**[E12] 扫描结论两栏**：注入类栏恒渲染（通过时为定长「未命中」）；启发式栏每条以 `--skill-limit-text` 高亮 + 栏头计数「命中 N 条」，0 条时显示定长「未命中技能域启发式模式」；**栏头恒显**「启发式检测只降低概率，不是安全边界。」；**不渲染命中处的原文片段**（不回显被拒内容）'
    - '**[E13] `allowed-tools`**：值折行不截断；免责标注「本字段在当前运行时不被强制，仅供参考。」**必须与值同排出现，不得折叠、不得省略、不得只进 tooltip**；`allowed-tools` 缺失时**整行不渲染**（宁缺勿猜）；解析失败时值渲染为「（无法解析）」但行**仍渲染**（禁止静默失败）'
    - '**[E14] 风险确认区**：复选框恒渲染（命中启发式时必有必勾项）；未勾选 ⇒ 确认按钮 `disabled` 且 `#skillImportGate` 写明「请先勾选『我已了解以上风险』」；**提交失败后勾选状态保持**（不静默复位）；提示文案可换行'
    - '**[E15] 冲突三选一**：与已有用户技能不同名 ⇒ 整块不渲染（不占位）；同名但未选择 ⇒ 三项均未选中、确认 `disabled` 且原因文本写明「请选择覆盖、改名或取消」；三项与改名输入框纵向排列、文案可换行；**输入框在选中「改名」前不渲染**（不占位、不进 Tab 序）；被冲突的 `name` 单行截断 + `title` 全文'
    - '**[E16] 改名输入框**：为空 ⇒ 确认 `disabled` 且原因「请输入新的技能名」；**不预填默认值**（预填会让用户误以为已校验通过）；非法 ⇒ 输入框下方**内联 danger** 文案（取 `validateManagedSkillName` 的**同一拒绝原因**，不写第二份映射）+ 确认 `disabled`；**不设 `maxlength`**（截断会产出静默失败，与「拒绝必须可见」相悖）；**不在输入框内另做字数计数**（第二份上限实现会与校验器漂移）'
    - '**[E17] 动作区**：在途态「确认与取消**双双** `disabled`」（本阶段唯一「连取消都不可用」的窗口，长度 = 本地 `rename` 序列，毫秒级）+ 状态行「正在写入技能，请稍候…」+ `Escape` 被忽略；按钮文案取自**闭合二元表**（默认「导入」，冲突项选中「取消」时切「关闭弹框」；取消按钮恒「取消」）；`.skill-import-actions` **不得**声明 `flex-wrap`（两按钮恒同排）'
    - '**[E18] 区级 inline hint**：成功 `已导入「{name}」` / 最终失败按码查表兜底；走既有 `setSkillManageHint`（2 秒无条件复位 + `clearTimeout`）；可换行不截断；文本为空时 CSSOM `display:none`（不留 8px 残留外边距）'
    - '**[状态机 9 态]** `idle·本地` / `idle·网络` / `在途·读取` / `在途·下载` / `预览就绪` / `提交中` / `提交成功` / `提交失败` / `预览过期` 各自的状态行 / 预览区 / 必勾区 / 冲突区 / 动作区取值按 `51-UI-SPEC.md` 的唯一权威状态表；**两行 idle 的状态行内容不同是刻意的**（本地用 `尚未选择 zip 文件`；网络态留空 + CSSOM `display:none`，由面板内地址说明 + 禁用原因共同承担）；**弹框关闭时复位**（清预览 / 状态行 / 必勾 / 冲突选择 / URL 输入 / 文件输入置空（`input.value` 设为空字符串））'
    - '**[键盘契约]** 初始焦点落在**当前模式面板内第一个可交互元素**（本地 = 选择 zip 文件按钮；网络 = URL 输入框），**不落在确认按钮**；`role="dialog"` + `aria-labelledby="skillImportTitle"`；**无任何正 `tabindex`、无 roving tabindex**，唯一的 `tabindex` 是预览滚动容器的 `tabindex="0"`；Tab 序 = DOM 序；`Escape` 监听挂 `document` 并以「弹框可见」为前置守卫早退（因不做焦点陷阱，焦点可能被 Tab 到框外，局部监听会静默失效），**提交中忽略 Escape**；关闭时按 `isConnected` 判定归还焦点到 `#skillImportOpen`（**不主动把焦点移向任何其它节点**，也不承诺「焦点不落到 body」）；弹框内**不得**出现「在 Tab 序内但不可见 / 零可见高度」的停靠点（显隐一律 `display` 切换，**不得** `max-height:0` + `overflow:hidden`，**不得** `visibility:hidden`）'
    - statement: '复用既有 `.ai-modal` 的 `max-height:80vh` + `flex-direction:column`，新增 `.skill-import-preview { flex:1 1 auto; min-height:0; overflow-y:auto }` 为弹框内**唯一**滚动容器（标题与按钮排不参与滚动）；真实 512 条目包 + 启发式高亮齐备时，在 800px 可用窗口下弹框总高不超过 80vh、页面级滚动不移动、且滚动只发生在预览区内（需真实数据集视觉确认，非 CSS 推算可证）'
      verification: backstop
    - statement: '按钮排 `flex-end` + `gap:8px`、禁用原因文本位于按钮排**上方**且可换行（窄窗口下让位的是文本而不是按钮）；最宽形态（禁用原因文本为「请先勾选『我已了解以上风险』」+ 冲突提示 + 两按钮）在 800px 可用窗口下不横向溢出、按钮不被裁切、文本按 `word-break` 折行（由 CSS 值推算，非实测）'
      verification: backstop
  artifacts:
    - 'src/settings.html：`div.skill-manage-header`（包住既有 `h2.settings-group-title` + `button#skillImportOpen.btn.btn-secondary.btn-sm`）、`input#skillImportFile.skill-import-file-input[type=file][accept=".zip,application/zip"]`、`div#skillImportModal.ai-modal-overlay` 全骨架（`div.ai-modal.skill-import-modal[role=dialog][aria-labelledby=skillImportTitle]` / `div.ai-memory-tabs[role=group][aria-label=导入方式]` / `#skillImportPanelLocal` / `#skillImportPanelUrl` / `p#skillImportStatus[role=status][aria-live=polite]` / `div#skillImportPreview[tabindex=0]` / `div#skillImportAck` / `div#skillImportConflict` / `p#skillImportGate` / `div.skill-import-actions` 两按钮）'
    - 'src/settings-page.js：`/* Phase 51 skill-import region: start/end */` 内的完整实现 —— 状态机单点（`setImportState(state, detail)`）、`renderSkillImportPreview()` / `renderSkillImportTree()` / `renderSkillImportScripts()` / `renderSkillImportScan()` / `renderSkillImportConflict()` / `updateConfirmGate()`、`sanitizeDisplayString()`（显示层净化）、`SKILL_IMPORT_ERROR_TEXT`（闭合白名单表）、`SKILL_IMPORT_CONFIRM_TEXT`（二元表）、`openSkillImportModal()` / `closeSkillImportModal()` / `performSkillImport()` / `fetchSkillImportPreview()` / `cancelSkillImportRequest()`'
    - 'src/styles/main.css：末尾 `/* ===== 设置页「技能导入」（Phase 51） ===== */` 专属段（含 3 条作用域覆盖：`.skill-manage-header` / `.skill-manage-header .settings-group-title` / `.skill-manage-section .ai-memory-tab.active`）'
    - 'tests/uat-51-import-modal.js（**新建 uat 驱动**，需 `NODE_PATH="$(npm root -g)"`）：两条 backstop 的带正命题属性测试 + 键盘契约 + 注入纪律的渲染快照'
    - 'tests/test-skills-import.js 追加：`SKILL_IMPORT_ERROR_TEXT` 与 `IMPORT_SKILL_ERROR` 的**双向覆盖**判据 + 注入纪律的 region-scoped 源码扫描（负命题 + 正命题）'
  key_links:
    - '单源链：`IMPORT_LIMITS` / `LIMITS` → preview 响应的 `limits` → 设置页渲染。设置页**不得**出现 `64` / `32` / `50` / `2000` / `100` / `16` 这类限额字面量（45/46 建立的「端点与前端零字面量」纪律）'
    - '单源链：`IMPORT_SKILL_ERROR`（主进程常量）↔ `SKILL_IMPORT_ERROR_TEXT`（前端闭合表）：**双向覆盖**由一条机械判据守住（表键 ⊇ 常量值 ∧ 常量值 ⊇ 表键）'
    - '单源链：改名校验的拒绝原因**只有一份** —— 前端调 `skillsApi` 得后端 `invalid_name` 的 `message`，**不**在本地复刻 `validateManagedSkillName` 的正则（那会产出第二份实现并漂移）'
    - '注入纪律链：`TD-48-01`（`escapeHtml` 只转义 `& < >`）**仍开未修** ⇒ 本区**零 HTML 字符串模板**，全部走 DOM API（`textContent` / `el.title` / `setAttribute`）；负命题（region 内零 `innerHTML` / `insertAdjacentHTML`）**必须与正命题（region 内出现 `document.createElement(` 与 `.textContent`）同时存在**，否则会退化成「空集真」'
    - '键盘契约链：`Escape` 挂 `document` + 可见性守卫；焦点归还判据是 `#skillImportOpen.isConnected`；弹框内唯一 `tabindex` 是预览滚动容器的 `tabindex="0"`'
  prohibitions:
    - statement: '不得把威胁扫描的「未命中」呈现成「这个技能是安全的」背书（预览 / 状态行 / 区级 hint / 文案表 / 测试命名均计入）'
      status: unresolved
      verification: judgment
      reason: 'UI-SPEC 已把四条诚实边界定为**恒显、不得折叠、不得只进 tooltip**（E12 栏头「启发式检测只降低概率，不是安全边界。」、预览底部「以上结论由启发式检测与限额判据得出，不代表该技能是安全的。」、E13 的 `allowed-tools` 免责、E11 的「技能不构成额外权限」）。任何把它们降级为 tooltip / 折叠 / 省略的改动都是本条禁止的形态'
    - statement: '不得在弹框打开期间把失败反馈写到区级 hint（被遮罩挡住 = 静默失败），也不得为此新建 toast / 弹窗基建'
      status: unresolved
      verification: judgment
      reason: 'UI-SPEC 的失败落点是**两处**：弹框打开期间 ⇒ `#skillImportStatus`（E6）；弹框关闭后 ⇒ E18。写错落点等于用户看不到失败。50 D-06 已把本区的反馈面锁定为 inline hint，新建 toast 会让设置页出现第二套反馈机制'
    - statement: '不得用 `aria-disabled` 代替原生 `disabled`，也不得把禁用原因只写在 `title` 里（或依赖 hover / tooltip）'
      status: unresolved
      verification: judgment
      reason: '`aria-disabled` 不阻止激活（键盘 Enter 仍会触发提交）；`.btn:disabled { pointer-events: none }` 让 `title` 无法触发，且 `disabled` 元素不可聚焦（读屏读不到）⇒ 只有 `#skillImportGate` 的可见文本能承载原因。两条都是「假安全 / 不可见失败」的具体形态'
    - statement: '不得复刻 `validateManagedSkillName` 的规则到前端，也不得为改名输入框加 `maxlength` 或本地字数计数'
      status: unresolved
      verification: judgment
      reason: '规则复刻必然与主进程漂移（两份实现）；`maxlength` 会静默截断用户输入（与「拒绝必须可见」相悖）；字数计数是第二份上限实现。改名的合法性判定**只有**主进程那一份，前端只消费它回传的 `code` 与 `message`'
---

<objective>
把 `51-03` 的最小导入入口扩成 `51-UI-SPEC.md` 的**完整契约**：两 tab 弹框、六字段预览卡片（含目录树与脚本清单的截断折叠、扫描结论两栏、`allowed-tools` 免责标注）、必勾风险确认、冲突三选一、四情形禁用态与可见原因、9 态状态机、键盘与 AT 契约、以及 `realm://` CSP 下的初始隐藏纪律。

本计划是 **D-13（预览卡片的信息集）** 的**唯一用户可见交付面**：D-13 列的六字段（名称 + 落点 / `description` 原文 / 目录树与计数 / 字节数 / 脚本清单标红 / 威胁扫描两栏）与两条补充（`allowed-tools` 带免责标注、诚实边界「结论是启发式而非安全背书」）逐条落到 E7..E13 与四条恒显文案上 —— **不增字段、不减字段**（字段集以 D-13 为权威，取值与形态以 `51-UI-SPEC.md` 为权威）。

Purpose: 判据 1 的用户可见面**就是**这个弹框 —— 用户必须能「看清将写入什么」才谈得上确认。本计划同时是 `TD-48-01`（`escapeHtml` 不转义引号）的**不扩大缺口**责任面：预览里渲染的每一个字符串都来自不可信包。

Output: `src/settings.html` 的弹框骨架、`src/settings-page.js` 的完整渲染与交互（状态机单点 / 闭合文案表 / 显示层净化）、`src/styles/main.css` 的 `.skill-import-*` 专属段、`tests/uat-51-import-modal.js` 的真实渲染驱动、`tests/test-skills-import.js` 的双向覆盖与注入纪律判据。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-UI-SPEC.md
@.planning/phases/51-zip/51-PATTERNS.md
@.planning/phases/51-zip/51-03-PLAN.md
@.planning/phases/51-zip/51-04-PLAN.md
@.planning/phases/51-zip/51-05-PLAN.md
@src/settings.html
@src/settings-page.js
@src/styles/main.css
@src/skill-picker-model.js
@tests/uat-50-b-interactions-perf-layout.js
@tests/test-skills-http-api.js
@tests/test-skills-import.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 弹框骨架 + 9 态状态机 + 键盘与 AT 契约（E1-E6）</name>
  <files>src/settings.html, src/settings-page.js</files>
  <read_first>
    - `.planning/phases/51-zip/51-UI-SPEC.md`：`## 结构与交互契约` 的 `### DOM 骨架`（`:475-536` —— **逐字照抄**，含「元素顺序是契约」与四处自查）、`### 状态机`（`:538-571` —— 9 态唯一权威表 + 两行 idle 的刻意差异 + 关闭复位契约 + 文件输入 `finally { input.value = '' }` 的既有先例行号）、`### 禁用态契约`（`:572-613`）、`### 键盘可达性契约`（`:614-657`）、`### #skillImportStatus 的 AT 播报契约`（`:658-666`）、`## Copywriting Contract`（`:342-457` —— 40 条文案的唯一权威清单与三条纪律）
    - `src/settings.html`：`:536-568`（技能管理区现状：`.settings-group.skill-manage-section` / `skillManageSummary` / `skillManageState` / `skillManageGroups` / `skillManageHint` / **`#skillManageConfirm` 弹框（形态模板）**）、`:197`（`rulesFileInput` —— 唯一的 file input 先例）、`:490-493`（`.ai-memory-tabs` 的既有宿主 —— **本阶段复用类、不复用其 ARIA**）、`:826`（脚本引入区）
    - `src/settings-page.js`：`51-03` T1 落地的导入入口（**先读** —— 本任务把它扩成完整状态机）、`:4784-4803`（region 标记与注入纪律的 JSDoc）、`:5427-5462`（`openSkillUninstallConfirm` / `closeSkillUninstallConfirm` 的焦点归还与 `isConnected` 判据）、`:5692-5715`（遮罩点击关闭 / `Escape` / 事件绑定）、`:1026` / `:4387` / `:5708`（三处既有 `Escape` 处理器 —— **必须确认互不冲突**：三者的守卫都要求各自的弹框 / 面板可见）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### src/settings.html`（`:833-874`）、`### src/settings-page.js`（`:661-829`）—— ⑤ 弹框打开 / 关闭 / Escape / 焦点归还的逐行模板（**并注意它标出的两处不得照抄的地方**：导入弹框要在「在途·下载」时让 Escape 可用、在「提交中」忽略 Escape ⇒ 不能无条件 `close()`）
  </read_first>
  <action>
    1. **`src/settings.html`**：
       - 技能管理区标题行改为 `div.skill-manage-header` 包住既有 `h2.settings-group-title`（文案仍是「技能管理」）+ 右侧 `button#skillImportOpen.btn.btn-secondary.btn-sm`（文案「导入技能」；**不是** `.btn-primary`）。
       - 区内加隐藏 file input：`input#skillImportFile.skill-import-file-input[type=file][accept=".zip,application/zip"]`（隐藏**走类规则**，**绝不写 markup 内联 `style`**）。
       - 在 `#skillManageConfirm` **之后**插入 `div#skillImportModal.ai-modal-overlay`，内层结构**逐字照抄 UI-SPEC 的 DOM 骨架**（顺序即契约）：`div.ai-modal.skill-import-modal[role="dialog"][aria-labelledby="skillImportTitle"]` → `div.ai-modal-header > h3#skillImportTitle`（「导入技能」）→ `div.ai-memory-tabs[role="group"][aria-label="导入方式"]` 两个 `button.ai-memory-tab[aria-pressed]` → `div#skillImportPanelLocal.skill-import-panel.active`（`p.skill-import-note` 包形态说明 + 选择按钮）→ `div#skillImportPanelUrl.skill-import-panel`（`p.skill-import-note` 地址形态说明 + `input#skillImportUrl.text-input[type=url]` + `button#skillImportFetch`）→ `p#skillImportStatus.skill-import-status[role="status"][aria-live="polite"]` → `div#skillImportPreview.skill-import-preview[tabindex="0"]` → `div#skillImportAck` → `div#skillImportConflict` → `p#skillImportGate.skill-import-gate` → `div.skill-import-actions`（`button#skillImportCancel` + `button#skillImportConfirm[disabled]`）。**不新增任何 `<script>`**（零新增前端文件）。
       - 文案一字不改地取 Copywriting 表：`#4`（本地上传说明）/ `#5`（选择 zip 文件）/ `#7`（网络地址说明）/ `#9`（三条在途）/ `#10` / `#11`（字段标签）/ `#12` / `#37`（两按钮）等；`SKILL.md` / `github.com` / `raw.githubusercontent.com` / `allowed-tools` 等静态标识用 `<code>` 包裹（**唯一允许走 markup 直写的字符串**）。
    2. **`src/settings-page.js`（全部实现住 `/* Phase 51 skill-import region: start/end */` 之内）**：
       - **状态机单点** `setImportState(state, detail)`：`state ∈ { ''idle-local'', ''idle-url'', ''reading'', ''downloading'', ''ready'', ''committing'', ''success'', ''failed'', ''expired'' }`；函数体内**唯一**决定「状态行文本 / 预览区 `display` / 必勾区渲染 / 冲突区渲染 / 动作区两个按钮的 `disabled` + `#skillImportGate` 文本」。**渲染端不得在别处再写一份状态判断**（这条是 UI-SPEC 状态机的机械形式；注释里逐字写明「禁止在别处重写 if 链」）。
       - **模式切换** `setImportMode(''local'' | ''url'')`：切 `.skill-import-panel.active` 类 + 两个 `aria-pressed`；切模式时按状态机重置到对应 idle 态；初始焦点随模式落在该面板第一个可交互元素（本地 = `#skillImportPick`；网络 = `#skillImportUrl`）。
       - **打开 / 关闭**：`openSkillImportModal()`（`overlay.style.display = ''flex''` + 初始焦点 + 记录触发元素）、`closeSkillImportModal()`（`display = ''none''` + **复位契约**：清预览 `replaceChildren()` / 状态行空 + `display:none` / 必勾与冲突选择清空 / **URL 输入清空** / **文件输入置空**（`input.value` 设为空字符串；⚠️ `<input>` **没有** `reset()` 方法）+ 焦点归还按 `#skillImportOpen.isConnected` 判定）。
       - **`Escape` 挂 `document`** + 「弹框可见」前置守卫；**`committing` 态忽略 `Escape`**；`downloading` 态 `Escape` = 取消（走 `cancelSkillImportRequest()` 真正 `AbortController.abort()`）。
       - **遮罩点击关闭**（`e.target === overlay`）与 `#skillImportCancel` 点击都走关闭路径；关闭前**尽力**通知后端丢弃预览（`skillsApi(''import'', { mode: ''commit'', importId, conflict: ''cancel'' })`，失败**不阻塞关闭** —— TTL 与崩溃清扫兜底；这条「尽力而为 + 兜底」的诚实边界要写进注释）。
       - **文件选择链路照抄既有先例**：可见按钮转调 `#skillImportFile.click()`；`change` 处理器内 `const file = e.target.files[0]` ⇒ 进入 `reading` 态 ⇒ `skillsApi(''import'', { method: ''POST'', headers: { ''Content-Type'': ''application/zip'' }, body: file })`（**`File` 直接作 body、不 FileReader、不 base64；`Content-Type` 显式给出**，因为 OS 识别出的 `file.type` 可能是 `application/x-zip-compressed`）；`finally { e.target.value = 空字符串 }`（否则同一文件第二次选不触发）。
       - **`#skillImportFetch`** ⇒ `downloading` 态 + `skillsApi(''import'', { method:''POST'', headers:{''Content-Type'':''application/json''}, body: JSON.stringify({ mode:''url'', url }) })`（请求用 `AbortController` 以便取消）。
       - **AT 契约**：`#skillImportStatus` 是可见文本与播报的唯一节点（`role="status"` + `aria-live="polite"`）；预览从在途到就绪**不移动焦点**。
  </action>
  <verify>
    <automated>node --check src/settings-page.js && node -e 'const fs=require("fs");const bad=[];const html=fs.readFileSync("src/settings.html","utf8").replace(/<!\s*--[\s\S]*?--\s*>/g,(m)=>m.replace(/[^\n]/g," "));for(const t of ["class=\"skill-manage-header\"","id=\"skillImportOpen\"","id=\"skillImportFile\"","id=\"skillImportModal\"","id=\"skillImportPanelLocal\"","id=\"skillImportPanelUrl\"","id=\"skillImportUrl\"","id=\"skillImportFetch\"","id=\"skillImportStatus\"","id=\"skillImportPreview\"","id=\"skillImportAck\"","id=\"skillImportConflict\"","id=\"skillImportGate\"","id=\"skillImportCancel\"","id=\"skillImportConfirm\"","aria-labelledby=\"skillImportTitle\""])if(html.indexOf(t)===-1)bad.push("settings.html 缺 "+t);if(!/role="status"[^>]*aria-live="polite"|aria-live="polite"[^>]*role="status"/.test(html))bad.push("状态行缺 role=status + aria-live=polite");if(!/id="skillImportPreview"[^>]*tabindex="0"|tabindex="0"[^>]*id="skillImportPreview"/.test(html))bad.push("预览滚动容器缺 tabindex=0");if(html.indexOf("aria-modal")!==-1)bad.push("出现了 aria-modal（本契约明确省略）");const seg=html.slice(html.indexOf("id=\"skillImportModal\""));const ack=seg.indexOf("id=\"skillImportAck\""),conf=seg.indexOf("id=\"skillImportConflict\"");if(ack===-1||conf===-1||ack>conf)bad.push("元素顺序违反契约：#skillImportAck 必须在 #skillImportConflict 之前");if(bad.length){console.error("弹框骨架不合格:\n"+bad.join("\n"));process.exit(1)}console.log("弹框骨架 ok（18 元素 id 齐备 + AT 契约 + 顺序 + 无 aria-modal）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `弹框骨架 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const raw=fs.readFileSync("src/settings-page.js","utf8");const i=raw.indexOf("/* Phase 51 skill-import region: start */");const j=raw.indexOf("/* Phase 51 skill-import region: end */");if(i===-1||j===-1||j<i)bad.push("缺 /* Phase 51 skill-import region: start/end */ 标记（注入纪律与负向扫描无判据对象）");const r=stripC(i===-1||j===-1?raw:raw.slice(i,j));if(r.indexOf("function setImportState(")===-1)bad.push("缺状态机单点 setImportState(");for(const s of ["idle-local","idle-url","reading","downloading","ready","committing","failed","expired"])if(r.indexOf(s)===-1)bad.push("状态机缺状态 "+s);if(r.indexOf("AbortController")===-1)bad.push("缺 AbortController（下载 / 请求可中断）");if(r.indexOf("input.value = \x27\x27")===-1&&r.indexOf("value = \x27\x27")===-1)bad.push("文件输入未在 finally 里置空（同一文件第二次选不触发）");if(/\.reset\(\)/.test(r))bad.push("用了 input.reset()（<input> 没有该方法，那是 <form> 的）");if(r.indexOf("innerHTML")!==-1||r.indexOf("insertAdjacentHTML")!==-1)bad.push("region 内出现 innerHTML / insertAdjacentHTML（注入纪律）");if(r.indexOf("document.createElement(")===-1||r.indexOf("textContent")===-1)bad.push("region 内缺 DOM 构建的正命题（createElement + textContent）");if(r.indexOf("addEventListener(\x27keydown\x27")===-1&&r.indexOf("keydown")===-1)bad.push("缺 Escape 处理");if(i!==-1&&raw.slice(0,i).indexOf("Phase 50 skill-manage region: start")===-1)bad.push("新 region 必须与 50 的 region 并列存在（不得替换它）");if(bad.length){console.error("状态机与交互不合格:\n"+bad.join("\n"));process.exit(1)}console.log("状态机与交互 ok（9 态单点 + 可中断 + 输入复位 + region 无 innerHTML 且有 DOM 正命题）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `状态机与交互 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const html=fs.readFileSync("src/settings.html","utf8").replace(/<!\s*--[\s\S]*?--\s*>/g,(m)=>m.replace(/[^\n]/g," "));const css=fs.readFileSync("src/styles/main.css","utf8").replace(/\/\*[\s\S]*?\*\//g,(m)=>m.replace(/[^\n]/g," "));const ids=["skillImportOpen","skillImportFile","skillImportModal","skillImportTitle","skillImportPanelLocal","skillImportPanelUrl","skillImportUrl","skillImportFetch","skillImportStatus","skillImportPreview","skillImportAck","skillImportConflict","skillImportGate","skillImportCancel","skillImportConfirm"];for(const id of ids){const k=html.indexOf("id=\""+id+"\"");if(k===-1){bad.push("缺元素 "+id);continue}const s=html.lastIndexOf("<",k),e=html.indexOf(">",k);const tag=html.slice(s,e+1);if(/\sstyle\s*=/.test(tag))bad.push("#"+id+" 的标签带内联 style（realm:// 的 style-src 会拦掉 ⇒ 元素反而常驻可见）")}if(!/\.ai-modal-overlay\s*\{[^}]*display:\s*none/.test(css))bad.push("main.css 缺 .ai-modal-overlay 的 display:none 类规则（初始隐藏的唯一合法承载）");if(bad.length){console.error("CSP 纪律不合格:\n"+bad.join("\n"));process.exit(1)}console.log("CSP 纪律 ok（15 个新增元素零内联 style + 初始隐藏由 .ai-modal-overlay 类规则承担）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `CSP 纪律 ok`，或 stderr 列出任意一条「缺元素」/「#<id> 的标签带内联 style」/「缺 .ai-modal-overlay 的 display:none 类规则」</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿（第三条是**真断言**：15 个新增元素的标签零内联 `style` ∧ `.ai-modal-overlay` 的 `display:none` 类规则存在 —— 两者都有正命题，不再是一个恒真的计数核对项）。
    - **[E1]** 标题行 flex + 按钮 `flex-shrink:0`、`white-space:nowrap`（CSS 在 T3 落，本任务只保证 DOM 与类名）；**[E2]** 弹框外壳 `role="dialog"` + `aria-labelledby`，**无 `aria-modal`**；**[E3]** 两个模式按钮带 `aria-pressed`，容器 `role="group"` + `aria-label`；**[E4]** 空态文案 `尚未选择 zip 文件` 落在**状态行**（不是面板内）；**[E5]** 网络面板的地址说明与 `disabled` 原因共同承担空态；**[E6]** 状态行 `role="status"` + `aria-live="polite"` 且是唯一播报源。
    - **可失败性（落地前实跑一遍）**：三条门禁在本仓当前树上报「缺 `class="skill-manage-header"`」、「缺 region 标记 / 缺 `setImportState(`」与「缺元素 skillImportOpen」（失败项全为「新代码未落地」类）。执行者落地后自行做四次单点变异：① 把 `aria-live="polite"` 改成 `assertive` ⇒ 门禁一转红（若未转红说明正则太宽，须收紧）；② 在 region 内把一处 `textContent` 改成 `innerHTML` ⇒ 门禁二报注入违规；③ 删掉 `finally { input.value = '' }` ⇒ 门禁二报「未置空」；④ 给 `#skillImportModal` 加一个内联 `style="display:none"` ⇒ 门禁三报「#skillImportModal 的标签带内联 style」；把 `.ai-modal-overlay` 的 `display: none` 从 `main.css` 删掉 ⇒ 门禁三报「缺 .ai-modal-overlay 的 display:none 类规则」。
    - **负向 token 先剥注释再判（W2，本轮修订；HTML 用等价机制）**：门禁一里 `aria-modal` 的负向判据改在**剥 HTML 注释**后的 markup 上判定。⚠️ **`stripC` 是 JS 五态词法器、不识别 `<!-- -->`**（它只认 `//` 与 `/* */`），故此处用**等价机制**：`html.replace(/<!\s*--[\s\S]*?--\s*>/g, m => m.replace(/[^\n]/g, " "))` —— **保长替换 + 保留换行**，与 `stripC` 的两条性质一致；该剥离值只作为本 gate 的 `html` 局部变量使用，**不与原文下标混用** ⇒ 无错位风险（正则写成 `<!\s*--` 而非连写的 `<!--`，以免本计划 markdown 自身被渲染器当注释吞掉）。**理由**：本计划要求「**不写** `aria-modal`」，那条说明性 HTML 注释（`<!-- 刻意不写 aria-modal：会向 AT 声明框外不可用 -->`）在原文判定下会让门禁**恒红**。**一对验证（合成 markup 上实跑）**：① 注释形态 ⇒ `弹框骨架 ok`、`exit 0`；② 单点变异（给 dialog 真加 `aria-modal="true"` 属性）⇒ 报「出现了 aria-modal（本契约明确省略）」；③ 当前树上本条门禁的 16 条「缺 `<token>`」红轮信息逐字不变。
      - **同一门禁三（CSP 纪律）也同步改用剥注释面**：`\sstyle\s*=`（负向）与 `.ai-modal-overlay { … display:none }`（正向）都改在**剥 HTML 注释 / 剥 CSS 块注释**（同为保长 + 保留换行）后的文本上判定。理由：该判据用 `html.lastIndexOf("<", k)` 取元素开标签，若一条 HTML 注释里同时出现 `<` 与 `style=`（例如 `<!-- 不要写成 <div style="display:none"> 这种形态 -->`），原文判定会**误报**为元素带内联 style；正向面也顺带修掉「注释里提一句就让规则看起来存在」的假绿。**一对验证**：① 注释形态（注释同时含 `<` 与 `style=`）⇒ `CSP 纪律 ok`、`exit 0`；② 单点变异（给 `#skillImportPreview` 真加 `style="display:none"`）⇒ 报「#skillImportPreview 的标签带内联 style」；③ 当前树上红轮仍为**逐字 15 条「缺元素 `<id>`」**（与本计划验收条记录一致）。
    - **门禁二的红轮证据与绿轮前提（region 标记的定位口径，已在修订中修正）**：门禁二**先在原文（`raw`）上定位** `/* Phase 51 skill-import region: start/end */`，**再对切片执行剥注释（`stripC`）后的 token 判定**；`stripC` 是**保长**替换（每个注释字符换成空格、换行保留）⇒ 原文与剥离面的下标一一对应，切片后剥离不会错位。红轮（修订期已实跑）：在当前树上 `raw.indexOf("/* Phase 51 skill-import region: start */") === -1` ⇒ 门禁二报「缺 region 标记」而非零退出，**且不会**再出现「标记存在却恒报缺失」的假红。绿轮前提：`51-03` 落的最小入口已把 `/* Phase 50 skill-manage region: start/end */` 写在 `settings-page.js` 内（门禁二的并列性判据也在**原文**上做 `raw.slice(0,i)`，故 50 的块注释标记能被找到），本任务只需**新增**一对 `Phase 51` 标记即可转绿。⚠️ **不得**把标记改写成非注释哨兵来绕开本条 —— 7 份计划与 `51-PATTERNS.md` 的 region 扫描判据都按「`/* … */` 块注释标记」的口径写。
    - **修订期实跑证据（红→绿，逐条留下真实输出）**：修订时在**当前树**上对三条门禁各跑一次 —— 门禁一：`exit 1` + stderr 为 16 条「settings.html 缺 `<token>`」（15 个元素 id 各一条 + `aria-labelledby="skillImportTitle"` 一条）+「状态行缺 role=status + aria-live=polite」+「预览滚动容器缺 tabindex=0」+「元素顺序违反契约：#skillImportAck 必须在 #skillImportConflict 之前」；门禁二：`exit 1` + stderr **首条**为「缺 `/* Phase 51 skill-import region: start/end */` 标记（注入纪律与负向扫描无判据对象）」；门禁三：`exit 1` + stderr 为 15 条「缺元素 `<id>`」。**绿轮的可满足性已用合成实现证明**：在临时目录里造一份最小实现（含两对 region 标记、15 个 id、`role=status`+`aria-live=polite`、`tabindex="0"`、`setImportState(` 与 9 个状态串、`AbortController`、`input.value = ''`、`document.createElement(`+`textContent`、`keydown`、四条诚实边界文案、`.ai-modal-overlay { display: none }`），三条门禁**全部 `exit 0`** ⇒ 判据**可被正确实现满足**，而当前树上为红（全部失败项都属于「新代码未落地」类）。真实绿轮的前提：本任务按 `<action>` 落地骨架与状态机（region 标记 / 15 个 id / AT 契约 / 预览 `tabindex="0"` / 不动 `.ai-modal-overlay` 的既有类规则）。
    - **既有 `Escape` 处理器不被破坏**：`settings-page.js` 里 `:1026` / `:4387` / `:5708` 三处既有处理器**逐行未改**（本计划只**新增**一处挂在 `document` 的处理器，且守卫要求「本弹框可见」⇒ 互不冲突）。
    - **CSP 纪律**：`#skillImportModal` 及其内部**零内联 `style`**；初始隐藏由 `.ai-modal-overlay` 的既有 `display:none` 类规则承担。
  </acceptance_criteria>
  <done>导入弹框的 DOM 骨架与 9 态状态机就位：两 tab 可切、状态行兼 AT 播报、必勾与冲突的位置符合契约、`Escape` / 遮罩点击 / 取消 / 关闭复位路径完整、初始焦点与焦点归还按契约执行，且全程无内联 style、无 `innerHTML`。</done>
</task>

<task type="auto">
  <name>Task 2: 预览卡片六字段 + 必勾 + 冲突三选一 + 禁用原因 + 闭合文案表（E7-E18）</name>
  <files>src/settings-page.js, src/settings.html, tests/test-skills-import.js</files>
  <read_first>
    - `.planning/phases/51-zip/51-UI-SPEC.md`：`## Copywriting Contract` 的 `### 全部新增 / 复用的用户可见文案（唯一权威清单）`（`:344-387` —— **40 条逐字**）、`### 确认按钮文案的二元表`（`:389-400`）、`### 失败文案映射表`（`:402-432` —— 按 `code` 查表 + 三条纪律）、`### 空态 / 错误态`（`:434-441`）、`### 文案纪律`（`:443-456`）、`### 折叠 / 截断 / 滚动纪律`（`:697-716`）、`### 长文本与溢出纪律`（`:718-737`）、`### 不可信字符串与注入纪律`（`:667-695`）
    - `.planning/phases/51-zip/51-UI-SPEC.md`：`## UI Considerations` 的 E7..E18 行（`:1027-1038`）与其 Category 表（`:1045-1128`）
    - `src/settings-page.js`：Task 1 落地的状态机（**先读** —— 渲染函数必须只被状态机驱动）、`:5267-5274`（`skillManageErrorText` 的闭合表形态 —— 新表照它写，`BODY_TOO_LARGE` 的既有条目**要保留**）、`:5289-5302`（`setSkillManageHint`）、`:5598-5675`（`renderSkillManagement` 的 DOM-only 渲染范式 —— **注**：`escapeHtml` 在 `:1657`，**只转义 `& < >` 不转义引号** ⇒ 本任务**不得**用它）
    - `ai-skills-manager.js`（**只读，不改**）：`IMPORT_SKILL_ERROR` 的**全部值**（`51-03` 的 20 键 + `51-04` 的 `INVALID_NAME` = 21 个；前端闭合表要与它双向覆盖）、`IMPORT_LIMITS` 的八键（限额数值一律取回传值）
    - `tests/test-skills-import.js`：文件骨架与既有用例（本任务追加两条判据）
  </read_first>
  <action>
    1. **`renderSkillImportPreview(preview)`**（**零 HTML 字符串模板**）逐字段 —— 字段清单**逐条对照 D-13**（D-13 的六字段 + 两条补充，一条不多一条不少）：
       - **[E7]** `技能名`（等宽单行 CSS 截断 + `title` 全文）+ `落点`（`将写入 skills/<name>/`，`<name>` 经 DOM API 赋值 + `word-break:break-all`）。
       - **[E8]** `描述`：**原文**渲染（`white-space:pre-wrap` + `word-break:break-word`），**不截断、不净化**。
       - **[E9]** `目录`：`共 {N} 个文件 / {M} 个目录` **恒显**；默认渲染**前 50 项**（阈值取 `preview.limits.previewListLimit`，**前端零字面量**）+ 截断时追加 `（当前显示前 {K} 项）`；`展开全部 / 收起` 用既有 `.btn.btn-secondary.btn-sm`（**不引入 `aria-expanded`**、**不复用 48 的折叠族**）；渲染顺序：同父目录在前、同级按路径字符串 **UTF-16 码元序**（`a < b ? -1 : a > b ? 1 : 0`，**不用 `localeCompare`**）；缩进 `el.style.paddingLeft = (12 * Math.min(depth, 8)) + ''px''`（**CSSOM 赋值**，第 8 层封顶）。
       - **[E10]** `体积`：`解压后合计 {X} · 单文件最大 {Y}（限额：SKILL.md {A} / 累计 {B}）` —— `{A}`/`{B}` 取 `preview.limits`，`{X}`/`{Y}` 用定长格式（`toFixed(1)` + B/KB/MB/GB）。
       - **[E11]** `脚本`：0 条时**整块不渲染**；有则每条以 `--skill-error-text` 渲染（12px 等宽路径 + `break-all`）+ 恒显「共 {N} 个脚本」+ 恒显「技能不构成额外权限 —— 这些脚本受既有 bash 三档策略与确认卡片约束。」+ 截断口径与 E9 同款。**扩展名标签只渲染白名单查表的定长文字**（包内原始扩展名**不参与**展示文案拼接）。
       - **[E12]** `扫描结论`：两栏 —— 注入类栏恒渲染（通过时定长「未命中」）；启发式栏 0 条时定长「未命中技能域启发式模式」，N 条时每条 `--skill-limit-text` + 栏头「命中 N 条」；**栏头恒显**「启发式检测只降低概率，不是安全边界。」；条目**只渲染模式名 + 被扫字段名（`description` / 正文，定长标识）**，**不渲染命中处原文片段**。
       - **[E13]** `allowed-tools`：`status === ''missing''` ⇒ **整行不渲染**；`''unparsed''` ⇒ 值渲染为「（无法解析）」但行仍渲染；`''ok''` ⇒ 值折行；**免责标注「本字段在当前运行时不被强制，仅供参考。」必须与值同排**（同一容器内、可换行）。
       - **底部恒显诚实边界**「以上结论由启发式检测与限额判据得出，不代表该技能是安全的。」（不得折叠）。
       - **条件渲染**：必勾区（`preview.scan.heuristic.length > 0` 时）**恒在预览之后、冲突之前**；冲突区（`preview.conflict.kind === ''user''` 三选一 / `''managed''` 只给改名 + 取消 + 提示「已存在同名 AI 自建技能「{name}」，导入后它将被永久遮蔽。」）**恒在必勾之后、禁用原因之前**。
    2. **必勾与禁用态**：`#skillImportAck` 的 `input[type=checkbox]` 变化 ⇒ 调 `updateConfirmGate()`；`updateConfirmGate()` 是**唯一**决定 `#skillImportConfirm.disabled` 与 `#skillImportGate.textContent` 的地方，四情形（预览未就绪 / 必勾未勾 / 改名非法或为空 / 冲突未选择）按 UI-SPEC 的表格给可见原因；**一律用原生 `disabled`**（不写 `aria-disabled`）；`committing` 态**确认与取消双双 disabled**。
    3. **文案表（两张，闭合）**：
       - `SKILL_IMPORT_ERROR_TEXT`（`Object.freeze`）：键 = `IMPORT_SKILL_ERROR` 的**全部值**（`import_expired` / `unsupported_url` / `unsupported_zip64` / `seeded_conflict` / `invalid_name` / `conflict_unresolved` / …），值取 Copywriting 失败映射表的逐字文案；**表外码走兜底文案**「导入失败：{后端 error 原文}」（取不到则「导入失败，请重试。」）—— **不回落 `undefined`**。⚠️ **`BODY_TOO_LARGE` 不在本表内**：413 由 `readRawBody` 直接 reject（`code: 'BODY_TOO_LARGE'`，与 `readJsonBody` 同形），它属既有 `skillManageErrorText` 那张表的域；**不得**把它加进 `SKILL_IMPORT_ERROR_TEXT`（双向覆盖判据会报「表里有已不存在的码」），上传超限走上面的兜底文案即可。`limit_exceeded` 一类的值是**后端 `error` 原文**（前端不另造文案）；`multi_skill` / `no_skill` / `seeded_conflict` 三条**逐字取用** #29 / #30 的文案。
       - `SKILL_IMPORT_CONFIRM_TEXT`（`Object.freeze`）：二元表 `{ default: ''导入'', cancel: ''关闭弹框'' }` —— **不得**在渲染代码里再写一份字面量。
    4. **显示层净化 `sanitizeDisplayString(value)`**（**只用于显示，绝不参与拒绝判据**）：移除 C0（`\x00-\x1F`）与 `\x7F`、C1（`\x80-\x9F`）、双向控制符（U+202A–U+202E、U+2066–U+2069、U+200E/U+200F）；返回 `{ text, hidden: N }`；`N > 0` 时调用方在行末追加「（含 {N} 个控制字符，已隐藏）」。**必须**有注释写明「判据用原始字节，净化只在显示层」（用净化值判会让 RTL override 类样本逃过检查）。
    5. **`tests/test-skills-import.js` 追加两条判据**（源码扫描，**先剥注释**，复用该文件既有的剥注释器）：
       - **双向覆盖**：从 `src/settings-page.js` 解析 `SKILL_IMPORT_ERROR_TEXT` 的**键集合**，与 `require(''../ai-skills-manager'').IMPORT_SKILL_ERROR` 的**值集合**做**双向**比对（表键 ⊇ 常量值 ∧ 常量值 ⊇ 表键）；任一方向缺失都指名报出。
       - **注入纪律（负命题 + 正命题）**：在 `/* Phase 51 skill-import region: start/end */` 区间内断言（a）**零** `innerHTML` / `insertAdjacentHTML`；（b）**存在** `document.createElement(` 与 `.textContent`（**正命题 —— 缺它则该判据在「region 为空」时假绿**）；（c）属性上下文**不拼接**：断言 region 内不出现形如 `setAttribute(''title'', ''x="'' +` 或 `` `...class="${` `` 的字符串拼 HTML 形态（用固定 token 判，不猜全文）。
  </action>
  <verify>
    <automated>node --check src/settings-page.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const raw=fs.readFileSync("src/settings-page.js","utf8");const i=raw.indexOf("/* Phase 51 skill-import region: start */");const j=raw.indexOf("/* Phase 51 skill-import region: end */",i);if(i===-1||j===-1||j<i)bad.push("缺 /* Phase 51 skill-import region: start/end */ 标记（六字段渲染函数的判据无对象）");const r=stripC(i===-1||j===-1?raw:raw.slice(i,j));for(const t of ["function renderSkillImportPreview(","function renderSkillImportTree(","function renderSkillImportScripts(","function renderSkillImportScan(","function renderSkillImportConflict(","function updateConfirmGate(","function sanitizeDisplayString(","SKILL_IMPORT_ERROR_TEXT","SKILL_IMPORT_CONFIRM_TEXT"])if(r.indexOf(t)===-1)bad.push("region 内缺 "+t);if(r.indexOf("localeCompare")!==-1)bad.push("用了 localeCompare（排序会随机器 locale 变，破坏预览可复现性）");if(r.indexOf("aria-disabled")!==-1)bad.push("用了 aria-disabled（不阻止激活 ⇒ 假安全）");if(r.indexOf("maxlength")!==-1)bad.push("改名输入框用了 maxlength（会静默截断）");if(r.indexOf("aria-expanded")!==-1)bad.push("用了 aria-expanded（展开全部是内容截断开关，不是折叠容器）");if(r.indexOf("ai-skill-content-box")!==-1)bad.push("复用了 48 的折叠族（会产出两份状态）");if(r.indexOf("已隐藏")===-1)bad.push("缺「（含 N 个控制字符，已隐藏）」的显示层提示（禁止静默隐藏）");if(r.indexOf("不代表该技能是安全的")===-1)bad.push("缺恒显的诚实边界文案");if(r.indexOf("不是安全边界")===-1)bad.push("缺启发式栏的诚实边界文案");if(r.indexOf("不被强制，仅供参考")===-1)bad.push("缺 allowed-tools 免责标注");if(r.indexOf("不构成额外权限")===-1)bad.push("缺脚本清单的「不构成额外权限」说明");if(bad.length){console.error("预览面不合格:\n"+bad.join("\n"));process.exit(1)}console.log("预览面 ok（六字段渲染 + 两张闭合表 + 显示层净化 + 四条诚实边界恒显）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `预览面 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 低于 **90**（可机械判读的**绝对下限**：60 → 72 → 90 是本阶段前序计划 `51-04` 已确立的收口值；本任务只增用例不删，低于 90 即说明既有用例被删改），或进程以非零退出（双向覆盖或注入纪律判据会在此转红）</fails_when>
    <automated>node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const code=stripC(fs.readFileSync("src/settings-page.js","utf8"));const i=code.indexOf("SKILL_IMPORT_ERROR_TEXT");const j=code.indexOf("});",i);const seg=code.slice(i,j);const tableKeys=new Set((seg.match(/[a-z_]{4,}:/g)||[]).map(s=>s.slice(0,-1)));const errs=require("./ai-skills-manager").IMPORT_SKILL_ERROR;if(!errs){console.error("IMPORT_SKILL_ERROR 未导出（口径失效：先落 51-03 的码表）");process.exit(1)}const constVals=Object.values(errs);const missInTable=constVals.filter(v=>!tableKeys.has(v));const extraInTable=[...tableKeys].filter(k=>!constVals.includes(k)&&k!=="Object"&&k!=="freeze");if(missInTable.length)bad.push("常量有码但表里没文案: "+missInTable.join(","));if(extraInTable.length)bad.push("表里有已不存在的码: "+extraInTable.join(","));if(bad.length){console.error("码与文案表未双向覆盖:\n"+bad.join("\n"));process.exit(1)}console.log("双向覆盖 ok（IMPORT_SKILL_ERROR "+constVals.length+" 键 ↔ SKILL_IMPORT_ERROR_TEXT）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `双向覆盖 ok`，或 stderr 列出「有码无文案」/「有文案无码」</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`tests/test-skills-import.js` 的 `# fail 0` 且既有用例零删改。
    - **[E7]-[E18] 逐元素**：E7 名称截断 + `title` 全文 / 落点折行；E8 原文不截断；E9 三件套（前 50 + 恒显计数 + 展开开关）+ 1 项不渲染开关 + 缩进封顶 + UTF-16 码元序 + 显示前净化；E10 定长格式 + 限额取回传值；E11 空块不渲染 + 标红 + 恒显说明 + 白名单扩展名标签；E12 两栏 + 栏头诚实边界 + 不渲染原文片段；E13 三态（缺失不渲染 / 解析失败仍渲染 / 免责同排）；E14 必勾 + 未勾可见原因 + 失败后保持勾选；E15 三选一 + 未选可见原因 + 输入框条件渲染；E16 空值禁用 + 不预填 + 内联 danger + 无 `maxlength`；E17 在途双禁用 + 二元表文案 + 无 `flex-wrap`（CSS 在 T3）；E18 走 `setSkillManageHint`。
    - **可失败性**：门禁一当前树报「region 内缺 renderSkillImportPreview(」等（新代码未落地 —— 修订期实跑 `exit 1`，stderr 首条为「缺 `/* Phase 51 skill-import region: start/end */` 标记（六字段渲染函数的判据无对象）」；用**合成实现**跑同一命令得 `exit 0` + `预览面 ok` ⇒ 该判据可被正确实现满足）；门禁三当前树报「常量有码但表里没文案」（表未落地）。执行者落地后自行做三次单点变异：① 把某一条码的键从 `SKILL_IMPORT_ERROR_TEXT` 删掉 ⇒ 门禁三报「有码但表里没文案」；② 在 region 内加一个 `innerHTML =` ⇒ 注入纪律判据转红（该判据在 `tests/test-skills-import.js` 里，由门禁二承载）；③ 把 `localeCompare` 用在目录树排序上 ⇒ 门禁一转红。
    - **四情形禁用原因都上屏**：`#skillImportGate` 的四个原因文本各有触发路径（每条在 uat 驱动里有一条断言 —— 见 T3）。
    - **诚实边界四条恒显、不折叠、不只进 tooltip**：`不是安全边界` / `不代表该技能是安全的` / `不被强制，仅供参考` / `不构成额外权限` 四条文本都在 region 内且不在任何 `title` 赋值里。
  </acceptance_criteria>
  <done>预览卡片按 UI-SPEC 渲染六字段（**字段集 = D-13 的信息集**；含截断折叠、标红脚本、两栏扫描结论、`allowed-tools` 免责）、必勾与冲突三选一按契约条件渲染、四情形禁用原因上屏、两张闭合文案表就位且与主进程常量双向覆盖、显示层净化带可见提示。</done>
</task>

<task type="auto">
  <name>Task 3: 样式段（3 条作用域覆盖）+ 真实渲染 uat 驱动（两条 backstop 的正命题读数）</name>
  <files>src/styles/main.css, tests/uat-51-import-modal.js</files>
  <read_first>
    - `.planning/phases/51-zip/51-UI-SPEC.md`：`## Color` 的 `### 前置修复 / 作用域覆盖（三条）`（`:311-318`）、`### 补记（四条，不修、不声称已修）`（`:319-327`）、`### 须在真实渲染下取数的读数（UAT 驱动必须断言）`（`:328-341`）、`## 新增 CSS 声明清单`（`:738-980` —— **整段已定稿，逐字照抄**）、`### 与 48 / 49 / 50 既有面的关系`（`:981-996`）
    - `src/styles/main.css`：`:7-64`（两套主题令牌块 —— 新增令牌必须**两处都有**）、`:10389-10415`（`.ai-memory-tabs` / `.ai-memory-tab` / `.ai-memory-tab.active` 的既有取值 —— 作用域覆盖 ① 的目标）、`:3195-3215`（`.settings-group-title` —— 作用域覆盖 ② 的目标）、`:5350-5358`（`.ai-modal-overlay { display:none }` 的既有初始隐藏）、`:1466-1470`（`.btn:disabled`）、文件**末尾**（50 的 `/* ===== 设置页「技能管理」区（Phase 50） ===== */` 专属段 —— 新段追加在它之后）
    - `tests/uat-50-b-interactions-perf-layout.js`（**逐行读**：驱动骨架、`NODE_PATH="$(npm root -g)"` 的文件头说明、`_electron` 启动方式、如何拿设置页窗口、`process.exit` 退出、以及它如何**动态取基线**；本驱动照它的形态写）
    - `.planning/STATE.md` 的「三处「假绿」形态」条目与 `.planning/phases/49-manage-skill-ai/49-REVIEW.md` 的 `WR-12`（**否命题空集真**）
    - `.planning/phases/51-zip/51-VALIDATION.md`：`## Manual-Only Verifications` 的「预览卡片在真实设置页 CSP 下的渲染」行（本驱动把它自动化到可跑的程度）
  </read_first>
  <action>
    1. **`src/styles/main.css` 末尾追加专属段**（段注释 `/* ===== 设置页「技能导入」（Phase 51） ===== */`，**必须追加在 50 的专属段之后**）：逐字照抄 UI-SPEC 的「新增 CSS 声明清单」，含：
       - **三条作用域覆盖**（作用域**全部以 `.skill-manage-section` 或 `.skill-manage-header` 为前缀**，48/49 的实名宿主零变化）：`.skill-manage-header`（flex/space-between/gap 8px/margin-bottom 16px）、`.skill-manage-header .settings-group-title { margin-bottom: 0 }`、`.skill-manage-section .ai-memory-tab.active { color: var(--text-primary) }`（既有 `--accent-color` 对宿主实测暗 3.90 / 亮 3.37 < 4.5:1，几何值一字不改）。
       - 弹框与面板：`.skill-import-modal`（`width: 560px; max-width: 94vw`）、`.skill-import-panel`（`display:none` + `flex-direction:column` + `gap:8px`）/ `.active { display:flex }`、`.skill-import-file-input { display:none }`。
       - 预览：`.skill-import-preview { flex:1 1 auto; min-height:0; overflow-y:auto }`（**唯一滚动容器**）、字段行 / 树行 / 脚本块 / 扫描两栏 / `--skill-error-text` 标红 / `--skill-limit-text` 高亮 / 状态行 / 门控文本 / 动作区（`flex-end` + `gap:8px`，**不得声明 `flex-wrap`**）/ 必勾区 / 冲突区。
       - **新增令牌写进两套主题块**：`--skill-error-text` / `--skill-limit-text`（缺一即在对应主题下失效）；对照度按 UI-SPEC 的核算表（11/12/13/14px 全部属小字 ⇒ ≥ 4.5:1）。
       - **`src/styles/bookmarks-bar.css` 是未被加载的死副本**（AGENTS.md 明文）⇒ **只改 `main.css`**。
    2. **`tests/uat-51-import-modal.js`（新建）**：真实 `_electron` 驱动（`NODE_PATH="$(npm root -g)"`），启动 dev 应用 → 打开设置页 → 切到 AI 分区 → 点「导入技能」，然后跑**带正命题**的属性测试：
       - **[E2 backstop]** 用一个**真实的多条目包**（用 `tests/helpers/make-malicious-zip.js` 的 `buildZip` 现场生成一个含 ≥ 200 条目的合法单技能包，经 IPC / 文件选择器导入 —— **不得硬编码本机数据**）使预览进入就绪态后：断言 `modal.getBoundingClientRect().height <= 0.8 * window.innerHeight`；断言**滚动只发生在预览区内**（对预览容器滚轮 / 键盘 `PageDown` 后 `preview.scrollTop > 0` 而 `document.scrollingElement.scrollTop` **不变**）；断言弹框内**只有一个**可滚动容器（对候选元素算 `scrollHeight > clientHeight` 的集合，断言其只含预览容器）。
       - **[E17 backstop]** 造出**最宽形态**（必勾未勾 ⇒ 原因文本「请先勾选『我已了解以上风险』」+ 冲突提示 + 两按钮）后：断言 `actions.scrollWidth <= actions.clientWidth`；断言两个按钮的 `getBoundingClientRect().width > 0` 且 `right <= window.innerWidth`（不被裁切）；断言禁用原因文本可见（`getBoundingClientRect().height > 0` 且 `textContent` 非空）。
       - **禁用态的四个原因各一条断言**（未选包 / 必勾未勾 / 冲突未选 / 改名非法）+ 「未勾选时确认按钮 `disabled === true` 且**点击无效**」（真实 `click()` 后断言弹框**未关闭**且未发 commit 请求）。
       - **键盘契约**：`Tab` 序与 DOM 序一致（收集各停靠点的 `getBoundingClientRect().top` 升序与 DOM 序比对）；预览容器在 `.active` 时 `tabindex === ''0''` 且能获焦；`Escape` 在 `ready` 态关闭弹框、在 `committing` 态被忽略（用一条**可控的慢路径**造出 `committing` 窗口：把一个 stub 化的 commit 请求挂起 —— 沿用 50 的 `window.skillsApi` stub 形态）。
       - **注入纪律的渲染快照**：造一个包内文件名含 `<img src=x onerror=alert(1)>` 与含 RTL override（U+202E）的文件名 ⇒ 断言预览里该行的 `textContent` **逐字包含**原始字符串（作为**文本**而非元素）、`querySelector('img') === null`、且该行末尾出现「（含 N 个控制字符，已隐藏）」。
       - **四条假绿规避（写进注释并在实现里体现）**：① 时间线采样**排除 pre-click / same-tick**；② 读 hint 用 `#skillImportStatus` 或**在 2000ms 内**读，且 danger 类名是 `skill-manage-hint-danger`；③ 断言前**必须**确认目标面板不在 `display:none`（否则 `getBoundingClientRect` 全 0 ⇒ 「0 vs 0 全等」假绿）；④ 生成给 guest 执行的源码字符串里**不得含反引号**。
       - **证据固化**：把断言结果写入仓库内路径（`tests/fixtures/` 之外的一个 `tests/.uat-out/` 或直接打印到 stdout 并 `process.exit(0/1)`）—— **不得**只写 `/tmp`（`IN-16` / `WR-09` 教训）；若全局 `playwright` 不可用，驱动必须**明确报错退出**（不是静默跳过），并在 SUMMARY 里记明该层证据降级为「本机可重跑」。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const css=fs.readFileSync("src/styles/main.css","utf8");const bad=[];const seg=css.indexOf("/* ===== 设置页「技能导入」（Phase 51） ===== */");if(seg===-1)bad.push("缺 Phase 51 专属段注释");else{const p50=css.indexOf("/* ===== 设置页「技能管理」区（Phase 50） ===== */");if(p50!==-1&&seg<p50)bad.push("Phase 51 段必须在 Phase 50 段之后（追加）");const bare=css.replace(/\/\*[\s\S]*?\*\//g,(m)=>m.replace(/[^\n]/g," "));const need=[".skill-manage-header",".skill-manage-header .settings-group-title",".skill-manage-section .ai-memory-tab.active",".skill-import-modal",".skill-import-panel",".skill-import-panel.active",".skill-import-file-input",".skill-import-preview",".skill-import-status",".skill-import-gate",".skill-import-actions",".skill-import-ack",".skill-import-conflict"];for(const s of need)if(bare.indexOf(s)===-1)bad.push("缺选择器 "+s);const i=bare.indexOf(".skill-import-actions {");if(i===-1)bad.push("缺 .skill-import-actions {（硬禁令无判据对象 ⇒ 空集真）");else{const b=bare.slice(i,bare.indexOf("}",i));if(b.indexOf("flex-wrap")!==-1)bad.push(".skill-import-actions 声明了 flex-wrap（两按钮必须恒同排）")}const pi=bare.indexOf(".skill-import-preview {");if(pi===-1)bad.push("缺 .skill-import-preview {");else{const p=bare.slice(pi,bare.indexOf("}",pi));for(const t of ["flex: 1 1 auto","min-height: 0","overflow-y: auto"])if(p.indexOf(t)===-1)bad.push(".skill-import-preview 缺 "+t+"（唯一滚动容器 + min-height:0 是必需项）")}for(const tok of ["--skill-error-text","--skill-limit-text"]){const n=(bare.match(new RegExp(tok.replace(/[-]/g,"\\-")+"\\s*:","g"))||[]).length;if(n!==2)bad.push(tok+" 定义 "+n+" 处（须两套主题块各一；判定在剥块注释后的 CSS 面上 —— 注释里提到令牌名不得使计数虚增）")}}if(bad.length){console.error("样式段不合格:\n"+bad.join("\n"));process.exit(1)}console.log("样式段 ok（3 条作用域覆盖 + 唯一滚动容器 + 动作区无 flex-wrap + 双主题令牌）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `样式段 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/uat-51-import-modal.js","utf8");const bad=[];if(t.indexOf("NODE_PATH")===-1&&t.indexOf("playwright")===-1)bad.push("驱动未声明全局 playwright 依赖（仓内 require.resolve 失败）");for(const s of ["innerHeight","scrollWidth","clientWidth","scrollTop","tabindex","Escape","getBoundingClientRect"])if(t.indexOf(s)===-1)bad.push("缺读数 token: "+s);if(t.indexOf("make-malicious-zip")===-1&&t.indexOf("helpers/")===-1)bad.push("缺现场生成的真实数据集（不得硬编码本机 realm-dev 数据）");if(t.indexOf("display:none")===-1&&t.indexOf("display === \x27none\x27")===-1)bad.push("缺「面板不在 display:none」的前置守卫（否则 rect 全 0 会产出假绿）");if(t.indexOf("2000")===-1&&t.indexOf("skillImportStatus")===-1)bad.push("缺避开 hint 2000ms 自动清空的处理");if(bad.length){console.error("uat 驱动不合格:\n"+bad.join("\n"));process.exit(1)}console.log("uat 驱动 ok（真实读数 + 动态数据集 + 三处假绿规避）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `uat 驱动 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>NODE_PATH="$(npm root -g)" node tests/uat-51-import-modal.js 2>&1 | tail -20</automated>
    <fails_when>退出码非 0，或输出里出现 `FAIL` / `✗`，或缺少一条总结行（如 `uat-51-import-modal: N passed`）。若因环境无法启动 Electron（无 dev 依赖 / 无显示器）而失败，执行者必须**在 SUMMARY 里如实记录该层证据降级为「本机可重跑」**，并把失败原因（而不是把断言删除）写进 `51-06-SUMMARY.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；uat 驱动在**本机**能跑出通过结论（或如实记录该层证据降级，**不得**为了让门禁变绿而删断言）。
    - **两条 backstop 各有一条可在未修复树上转红的正命题**：执行者至少对 E2 那条做一次变异（把 `.skill-import-preview` 的 `min-height: 0` 删掉 ⇒ 弹框高度断言转红）；对 E17 那条做一次变异（给 `.skill-import-actions` 加 `flex-wrap: wrap` ⇒ 同排 / 不裁切断言转红）。两条变异结果记进 SUMMARY。
    - **令牌计数在剥注释的 CSS 面上判定（W2，本轮修订）**：`--skill-error-text` / `--skill-limit-text` 的「恰 2 处」判据由「原文 `css`」改为该门禁**既有的 CSS 注释剥离面**（`css.replace(/\/\*[\s\S]*?\*\//g, m => m.replace(/[^\n]/g, " "))` —— **保长替换 + 保留换行**）。⚠️ 此处**不用**五态 `stripC`：它是 JS 词法器，会把 CSS 里的 `url(//host/…)` 误判成行注释而污染整个剥离面；CSS 只有 `/* */` 一种注释形态，块注释正则即其等价物（该剥离值只在本 gate 内用，不与原文下标混用）。**理由**：UI-SPEC 的 CSS 段自带「新令牌两套主题块各一」的说明性注释，其中出现 `--skill-error-text:` 会让原文计数虚增到 3 ⇒ 门禁**恒红**。**一对验证（合成 CSS 上实跑）**：① 注释形态（注释里含 `--skill-error-text:` / `--skill-limit-text:`）⇒ `样式段 ok`、`exit 0`；② 单点变异（真加第三处 `--skill-error-text:` 定义）⇒ 报「--skill-error-text 定义 3 处」。
    - **不得重复 `WR-12` 的「否命题空集真」形态**：每条承重判据都带正命题（`height <= 0.8 * innerHeight` 是上界断言，而「滚动只发生在预览区」同时断言 `preview.scrollTop > 0` **且** 页面级滚动不变 ⇒ 有正有反）；断言集合里**不得**只有「不存在违规元素」一类。
    - **证据固化**：断言细节（含两条 backstop 的读数与变异结果）写进 `51-06-SUMMARY.md` 与仓库内产物；**不得**只留 `/tmp`。
    - **样式只改 `main.css`**：`src/styles/bookmarks-bar.css` **零改动**（它是未加载的死副本）。
    - **既有面板零回归**：`.ai-memory-tab` 的宿主（AI 记忆区三个 tab）在几何上零变化（作用域覆盖 ① 只改 `color`），且 `settings.html` 不新增 `<script>`。
  </acceptance_criteria>
  <done>`.skill-import-*` 专属段（含 3 条作用域覆盖与双主题令牌）落地且唯一滚动容器 / 动作区硬禁令有判据对象；uat 驱动在真实设置页里读出两条 backstop 的正命题结论、四条禁用原因、键盘契约与注入纪律的渲染快照，且证据固化进仓库。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 不可信包内容 → 预览 DOM | 文件名 / 目录树路径 / `description` 原文 / `diagnostics` 原文 / `allowed-tools` 值全部来自用户导入的包 |
| 主进程回传的错误 → 用户可见文案 | 前端只按 `code` 查闭合表，**不解析 `message`**（也**不回显被拒内容原文**） |
| 设置页 guest 的 CSP（`style-src 'self'`）→ 渲染 | 内联 `style` 被拦（元素反而常驻可见）；初始隐藏必须走 CSS 类 |
| 键盘 / AT 用户 → 弹框交互 | 禁用原因必须上屏（`disabled` 元素不可聚焦、`title` 无法触发） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-39 | Tampering (XSS) | 预览渲染不可信字符串（含属性上下文） | high | mitigate | region 内**零** `innerHTML` / `insertAdjacentHTML` / 字符串模板拼 HTML；全部 DOM API 赋值（`textContent` / `el.title` / `setAttribute`）；类名 / id / dataset 全为白名单字面量；注入纪律判据 = **负命题 + 正命题**同时存在；uat 驱动用含 `<img onerror>` 的文件名做渲染快照断言 |
| T-51-40 | Spoofing | RTL override / 控制字符让包内路径**视觉欺骗** | high | mitigate | 显示前净化（C0 / C1 / 双向控制符）**只作用于显示层**（判据仍用原始字节）+ 净化数 > 0 时行末可见提示（**禁止静默隐藏**）；uat 驱动断言提示文本出现 |
| T-51-41 | Repudiation | 弹框打开期间的失败被写进被遮罩挡住的区级 hint ⇒ 静默失败 | medium | mitigate | 失败落点两分（弹框内 `#skillImportStatus` / 关闭后 E18）；`prohibitions` 具名登记 |
| T-51-42 | Tampering | 用 `aria-disabled` 假装禁用 ⇒ 键盘 Enter 仍可提交 | medium | mitigate | 一律原生 `disabled`；门禁断言 region 内不出现 `aria-disabled`；uat 断言「未勾选时真实 `click()` 不提交」 |
| T-51-43 | Availability | 禁用原因不可见（只写在 `title` / 依赖 hover） | medium | mitigate | `#skillImportGate` 的可见文本承载原因；uat 逐情形断言该文本非空且 `height > 0` |
| T-51-44 | Tampering | 前端复刻改名校验规则 ⇒ 与主进程漂移 | medium | mitigate | 前端**只**消费后端 `invalid_name` 的 `code` + `message`；`maxlength` 与字数计数被禁止（会静默截断 / 第二份上限） |
| T-51-45 | Repudiation | 诚实边界被降级为 tooltip / 折叠 / 省略 | medium | mitigate | 四条恒显文本各有源码判据（`不是安全边界` / `不代表该技能是安全的` / `不被强制，仅供参考` / `不构成额外权限`）；`prohibitions` 具名登记 |
| T-51-46 | Tampering | CSP 下内联 `style` 被拦 ⇒ 元素常驻可见（`realm://` 的既有事故形态） | medium | mitigate | 初始隐藏走 `.ai-modal-overlay { display:none }` 类规则；显隐用 CSSOM 具体值；门禁核对 `settings.html` 的 `style=` 计数并逐处判定 |
| T-51-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本计划**零安装动作**；新增的 uat 驱动依赖**全局** `playwright`（`AGENTS.md` 的既有做法）⇒ 不引入新的 `dependencies` 条目 |
</threat_model>

<verification>
- 三个任务的 automated 门禁绿；`tests/test-skills-import.js` 与 `tests/test-skills-http-api.js` 保持绿；uat 驱动在本机能跑（或如实记录降级）。
- **本计划不覆盖**：文档与账本（`51-07`）；`allowed-tools` 的执行层门禁（REQUIREMENTS 的 Out of Scope，本阶段只解析 + 免责展示）。
- 人工面（`51-VALIDATION.md` 的 Manual-Only 表）：真实 GitHub 端到端成功路径、32 MiB 上传的真实耗时与内存峰值（本计划只覆盖 UI 侧渲染与布局读数）。
- **诚实边界**：`TD-48-01`（`escapeHtml` 只转义 `& < >`）**仍开未修** —— 本阶段的责任是**不扩大缺口**（新增插值全走 DOM API），**不得**在任何交付物里声称已修；`#skillImportGate` 的禁用原因变化**不在 live region 内**（UI-SPEC 已登记的已知边界，`51-07` 如实写入文档）。
</verification>

<success_criteria>
- 用户在设置页点「导入技能」后能：切两个 tab、选 zip 或填地址、看到六字段预览与两条诚实边界、在命中启发式时必勾确认、在冲突时三选一、在四种禁用情形下看到可见原因，且在提交中无法误关弹框。
- 预览里渲染的任何不可信字符串都不构成注入面（负命题 + 正命题双判据），控制字符 / RTL 被净化且有可见提示。
- 两条 backstop（弹框 overflow / 动作区 overflow）在真实数据集与真实窗口下有带正命题的读数，且证据固化进仓库。
- 失败文案按 `code` 查闭合表且与主进程常量双向覆盖；不解析 `message`、不回显被拒内容原文。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-06-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-06）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| DOM 元素 / 类 | `.skill-manage-header`、`#skillImportOpen`、`#skillImportFile.skill-import-file-input`、`#skillImportModal.ai-modal-overlay`、`.ai-modal.skill-import-modal`、`.ai-memory-tabs[role=group]` 两按钮、`#skillImportPanelLocal` / `#skillImportPanelUrl` / `.skill-import-panel.active`、`#skillImportUrl`、`#skillImportFetch`、`#skillImportPick`、`#skillImportStatus.skill-import-status[role=status][aria-live=polite]`、`#skillImportPreview.skill-import-preview[tabindex="0"]`、`.skill-import-tree` / `.skill-import-tree-row`、`.skill-import-scripts`、`.skill-import-scan` / `.col` / `.col-heuristic`、`#skillImportAck` / `.skill-import-ack-label` / `.skill-import-ack-box`、`#skillImportConflict` / `.skill-import-radio-row`、`#skillImportGate.skill-import-gate`、`.skill-import-actions`、`#skillImportCancel` / `#skillImportConfirm` | `src/settings.html` |
| 前端函数 | `setImportState(state, detail)`、`setImportMode(mode)`、`openSkillImportModal()`、`closeSkillImportModal()`、`renderSkillImportPreview()`、`renderSkillImportTree()`、`renderSkillImportScripts()`、`renderSkillImportScan()`、`renderSkillImportConflict()`、`updateConfirmGate()`、`sanitizeDisplayString()`、`fetchSkillImportPreview()`、`performSkillImport()`、`cancelSkillImportRequest()` | `src/settings-page.js` |
| 闭合文案表 | `SKILL_IMPORT_ERROR_TEXT`（键 = `IMPORT_SKILL_ERROR` 的**全部 21 个值** = `51-03` 的 20 键 + `51-04` 新增的 `INVALID_NAME`；键集合由一条**双向覆盖**用例与常量机械比对）、`SKILL_IMPORT_CONFIRM_TEXT`（二元表） | `src/settings-page.js` |
| region 标记 | `/* Phase 51 skill-import region: start */` … `/* Phase 51 skill-import region: end */` | `src/settings-page.js` |
| CSS 段 / 令牌 | `/* ===== 设置页「技能导入」（Phase 51） ===== */` 段、`--skill-error-text`、`--skill-limit-text`（两套主题块各一份） | `src/styles/main.css` |
| uat 驱动（**新文件**） | `tests/uat-51-import-modal.js` | `tests/` |
| 追加判据 | `SKILL_IMPORT_ERROR_TEXT` ↔ `IMPORT_SKILL_ERROR` 双向覆盖 + region-scoped 注入纪律（负命题 + 正命题） | `tests/test-skills-import.js` |

### 本阶段其余计划将新建（此处仅登记，避免在评审时被当作「应已存在」）

| 计划 | 符号 |
|------|------|
| 51-07 | `docs/product/ai-skills.md` §十三【导入】（能力 / 三来源 / 六类限额 / 错误码表 / 安全边界与已知限制定稿）、`docs/product/ai-agent-workspace.md` §七 的 SEC-10 行为变更条目、`AGENTS.md` 的导入维护约定与测试行、counts-parity 命令的 `suites` / `cells` 扩展（12 → 20） |
