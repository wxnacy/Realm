---
phase: 51-zip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-workspace.js
  - tests/test-agent-workspace.js
autonomous: true
requirements: [SEC-10]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

key-decisions:
  - '**本计划是「沙箱写面」这一子系统的 tracer**：一条端到端纵切 —— `env.writeFile(中间目录是 symlink 的越界路径)` 从**放行**转为**拒绝**，同时**沙箱内自指 symlink 仍放行**、既有 21 例零转红。这条纵切把「判据（`resolveInsideForWrite`）→ 沙箱包装（`guardForWriteResult`）→ 全部写方法 → 真磁盘副作用」一次打通，剩余用例是在它之上的扩充。**阶段的主 tracer（导入纵切）在 `51-03-PLAN.md` T1** —— 两条纵切分属两个互不耦合的子系统（本计划不 import 任何导入面符号）'
  - '**SEC-10 必须与导入路径同批修（D-15 / ROADMAP 判据 4 明文）**：缺口是**同一个**根因（`resolveInside` 对 ENOENT 路径只做词法校验），分两处修会留下既有沙箱这一半。缺口真实位置实测为 `agent-workspace.js:182-191`（`ENOENT` 分支在 `:188`）—— CONTEXT D-15 引的 `:169-175` **已漂移**，不得按旧行号落地'
  - '**必须新增 `guardForWriteResult` 而不是改 `guardResult` 本体**：`guardResult` 被**读面**（`readTextFile` / `readTextLines` / `readBinaryFile` / `fileInfo` / `listDir` / `exists` / `canonicalPath` / `absolutePath`）复用；改本体等于**顺手改读面语义**，而 D-15 明文「**不改** `resolveInside` 对**已存在**路径的既有语义」、SEC-10 的验收判据之一就是「既有放行 / 拒绝集合**零变化**」。两条判据并列存在的形态也让「写面被切过去了」有独立判据对象'
  - '**`resolveInsideForWrite` 必须保留原有词法前缀校验**（双基准 `root` / `realpath(root)`，macOS `/var → /private/var` 必须放行），只在**词法通过之后**追加「最近已存在祖先 realpath」复核。理由：新函数若**替换**而非**叠加**词法判据，会放行「root 自身不存在且 realpath 失败」这类退化形态（`resolveInside` 里 `catch {}` 退化为单基准的既有取向必须逐字沿用）'
  - '**`createTempDir` / `createTempFile` 一并纳入（research 实测 8 的可选项，这里定为必做）**：两者走 `fs.promises.mkdtemp`，**未经 guard**；其产物恒在 `.tmp/` 下但**内部目录若被替换成 symlink 同样可逃逸**。本阶段导入管线会在 `.tmp/` 下大量建目录 ⇒ 不纳入等于给导入留一条侧门。纳入方式 = 对 `mkdtemp` 的**父目录**（`getTmpDir()`）做写面复核，并对返回路径再做一次写面复核'
  - '**`exec` 不在本计划范围**（D-15 明文：命令内容不在这一层校验，交给 bash 三档策略与确认卡片）。本加固**不**声称封闭 `bash` 写盘能力 —— 诚实边界必须写进 `docs/product/ai-agent-workspace.md`（`51-07-PLAN.md` 交付）；`bash` 里「先 `ln -s` 出去再写」会被拒是**本加固的连带行为变更**，属**正确**行为但须成文'
  - '**行号会漂移，判据必须锚在结构上**：本计划所有源码断言锚在**函数名 / 方法名 / token**，只有「窗口」类判据用固定字符窗口；凡引用既有行号处一律写成「（实测于 2026-09-15，漂移后按函数名重新定位）」。既有 21 例中 `tests/test-agent-workspace.js:157-165` 的「bash 建 link 后读 link 被拒」是**读面**用例 ⇒ 加固后**预期不变**，它同时是新增**写面**用例的模板'

must_haves:
  truths:
    - 'resolveInsideForWrite 对中间目录为 symlink 且目标不存在的形态返回 null；沙箱自指 symlink 仍放行；createSandboxEnv 全部写入面已切换到它（源码扫描）；既有用例集合零变化'
    - '`env.writeFile(''<root 内 symlink 指向 root 外>/x'')` 返回 `{ ok: false, error.code: ''permission_denied'' }` 且 **不 throw**（FileSystem 契约：失败编码进 Result），并且**真的没有在 root 外产生文件**（用 `fs.existsSync` 对 root 外的探针路径断言，不是断言「没调用过 fs」）'
    - '`env.renameFile(src, dst)` 的 **destination** 走写面判据：dst 的中间目录是逃逸 symlink ⇒ 拒绝；dst 的父目录链全在 root 内 ⇒ 放行（既有 `renameFile destination 越界拒绝` 用例的语义**不变**）'
    - '沙箱内**自指** symlink（`ln -s references references/self`、`ln -s . loop`）下的写入仍放行 —— realpath 归一化后仍在 root 内 ⇒ 这是「加固没有收紧过头」的正命题'
    - '`resolveInsideForWrite(root, target)` 对 `target` 为**非字符串 / 空串 / 纯空白**返回 `null`（不抛、不 fallback 到 root）；对 `..` 逃逸、绝对路径越界、兄弟前缀目录（`/root-evil` 对 root `/root`）返回 `null`'
    - '`createSandboxEnv` 的**读面**方法（`readTextFile` / `readTextLines` / `readBinaryFile` / `fileInfo` / `listDir` / `exists` / `canonicalPath` / `absolutePath`）**仍走** `resolveInside`（`guardResult`）：源码扫描断言写面用 `guardForWriteResult(`、读面用 `guardResult(`，且两者**并列存在**'
  artifacts:
    - 'agent-workspace.js：`function resolveInsideForWrite(root, target)`（内部，`module.exports` 导出供单测）、`const guardForWriteResult = (p) => …`（内部）、`module.exports.resolveInsideForWrite`'
    - 'agent-workspace.js：`writeFile` / `appendFile` / `renameFile` / `createDir` / `remove` 五个方法体切到 `guardForWriteResult`；`createTempDir` / `createTempFile` 追加写面复核'
    - 'tests/test-agent-workspace.js：新增「写面加固」用例组（逃逸转拒 / 自指放行 / 既有集合零变化 / 真写盘副作用 / 源码契约）'
  key_links:
    - '单一判据链：`resolveInsideForWrite`（唯一新判据）→ `guardForWriteResult`（唯一包装）→ 五个写方法与两个临时目录方法。**不得**在任何方法体内重写一份路径判据（那会产出第二份实现，`SEC-10` 的源码扫描判据按「写面方法体内零 `realpathSync`」检查）'
    - '读面/写面并列：`guardResult`（读面 + 既有语义）与 `guardForWriteResult`（写面）**同时存在**。删掉任一个都会让另一面的既有用例转红 —— 这条并列关系本身是判据（`SEC-10` 的「既有集合零变化」与「写面已切换」两条同时成立才绿）'
    - '本计划与导入管线**零耦合**：`agent-workspace.js` 的新增导出被 `51-03`/`51-04` 间接使用（`env.renameFile` 的落点校验），但本计划不 import 任何导入面符号、不改 `ai-skills-manager.js` ⇒ 可与其他 Wave 1 计划并行'
  prohibitions:
    - statement: '不得借「写面加固」之名放宽或收紧 `resolveInside` 对**已存在**路径的既有放行 / 拒绝集合（含不得把 `ENOENT` 分支从 `resolveInside` 里删掉后改成 fail-closed）'
      status: unresolved
      verification: judgment
      reason: '读面（`readTextFile` 一族）共用同一函数；把 ENOENT 改成 fail-closed 会让「读一个尚不存在的路径」从「词法校验后放行、由底层 fs 报 ENOENT」变成 `permission_denied`，直接改变既有错误码语义（`tests/test-agent-workspace.js:76-82` 的「根内普通路径（存在与不存在）放行」用例会转红）。加固的**增量**必须只落在新的写面函数上'
    - statement: '不得把 `bash` 的写盘能力写成「已被本加固封闭」'
      status: unresolved
      verification: judgment
      reason: '`exec` 的内容不在这一层校验（D-15 明文）⇒ `bash -c "echo x > /etc/y"` 仍可写出 root 外，三档权限与确认卡片才是那道边界。声称已封闭是**虚假安全感**，且会让后续读者据此删掉确认卡片。诚实边界由 `51-07` 写进 `docs/product/ai-agent-workspace.md`'
    - statement: '不得为本加固引入新的依赖（如 realpath 缓存库 / 路径解析库）或改动 `FileError` 的结构'
      status: unresolved
      verification: judgment
      reason: '判据只需要 `path` + `fs` 两个内建；`FileError` 的 `code` 取值是渲染端与测试共同消费的稳定接口，改结构会让 `sandboxErrorCode()` 的折叠面漂移'
---

<objective>
闭合 **P2 / SEC-10**：撤回 `resolveInside` 对「尚不存在」写目标只做词法校验的缺口 —— 中间目录是符号链接时，词法路径看着在沙箱 root 内、实际写盘跟随链接跑出去（实测已复现：`escaped file exists outside? true`）。

Purpose: 这条缺口同时是**导入路径**（恶意 zip 的 symlink entry）与**既有沙箱**（`writeFile` 的 `guard`）的同一根因，ROADMAP 判据 4 明文要求「既有沙箱 `writeFile` 的 ENOENT symlink 缺口**一并加固**」。本计划只做沙箱这一半，导入那一半（zip 树内 symlink 的两路判定 + `mkdtemp` + 最近已存在祖先 realpath）在 `51-03`。

Output: `agent-workspace.js` 的 `resolveInsideForWrite` 与 `guardForWriteResult`、五个写方法与两个临时目录方法的切换、`tests/test-agent-workspace.js` 的写面加固用例组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-RESEARCH.md
@.planning/phases/51-zip/51-PATTERNS.md
@agent-workspace.js
@tests/test-agent-workspace.js
@node_modules/@earendil-works/pi-agent-core/dist/harness/env/nodejs.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 写面加固端到端纵切 —— resolveInsideForWrite → guardForWriteResult → 五个写方法切面</name>
  <files>agent-workspace.js, tests/test-agent-workspace.js</files>
  <read_first>
    - `agent-workspace.js`：`:156-198`（`resolveInside(root, p)` 全段 —— 双基准构造 `rootPrefix` / `realPrefix`、`lexicallyInside` 的判据、`:182-191` 的 symlink 复核块、**`:188` 的 `if (err && err.code === 'ENOENT') return abs;` 就是缺口本体**；注意 `:170-175` 的 `catch {}` 退化单基准取向必须逐字沿用）、`:205-231`（`createSandboxEnv` 的 `root` / `realRoot` 与 `deny` / `guard` / `guardResult` 三个内部函数 —— **新函数必须并列于 `guardResult`**）、`:233-263`（读面方法：`absolutePath` / `joinPath` / `readTextFile` / `readTextLines` / `readBinaryFile` —— 这些**保持** `guardResult`）、`:265-276`（`writeFile` / `appendFile`）、`:277-286`（`renameFile` 的双路径校验，注释逐字写着「destination 逃逸 = 写逃逸」）、`:315-326`（`createDir` / `remove`）、`:327-352`（`createTempDir` 的 `'tmp-' + sanitizeNamePart(prefix)` 与 `createTempFile`）、`:378-391`（`module.exports` —— `resolveInside` 已在列，新函数并列登记）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`#### 实测 8`（`:1047-1154`）—— 缺口逐字定位（CONTEXT 引的行号已漂移）、`createSandboxEnv` 全部写面的逐方法表、既有 21 例的受影响清单（**预期 0 例转红**）、`resolveInsideForWrite` 的**原型实测十例**输出（`link OUT then write: old=ALLOW new=deny`、`link IN (self-referential) then write: old=ALLOW new=ALLOW`）与原型实现骨架（`:1130-1153`）
    - `tests/test-agent-workspace.js`：`:1-40`（文件骨架：`node:test` 自跑头、`withTempRoot(t)`（`mkdtempSync` + `t.after` 清理 + `setWorkspaceDir(null)` 复位）、`makeEnv`）、`:67-83`（`symlink 逃逸拒绝` 与 `根内普通路径（存在与不存在）放行` —— **加固后必须逐字仍绿**）、`:86-100`（`writeFile` 根外拒绝 / 根内成功且父目录自动创建）、`:110-119`（`renameFile destination 越界拒绝`）、`:157-165`（**读面**的 symlink 二段式逃逸 —— 新增写面用例的模板）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### agent-workspace.js —— resolveInsideForWrite`（`:329-424`）—— 缺口本体、双基准配套代码、`guard`/`guardResult` 形态、写面切换清单、临时目录访问器表（**新代码只经访问器拿路径**）
  </read_first>
  <action>
    1. **新增 `resolveInsideForWrite(root, target)`**（住 `agent-workspace.js`，紧邻 `resolveInside` 之后）：
       - 入参守卫：`typeof target !== 'string' || !target.trim()` ⇒ `return null`（不抛、不 fallback）。`path.resolve(root, target)` 抛错时 `return null`。
       - ① **保留**原有词法前缀校验：与 `resolveInside` **同一判据、同一双基准**（`root` / `realpathSync(root)` 的退化取向、`rootPrefix` / `realPrefix` 的 `path.sep` 处理、`abs === root || abs === realRoot` 的等值分支）。**不得**重新发明一套前缀比较。
       - ② **自内向外找最近已存在祖先**：从 `abs` 起，`fs.realpathSync(cur)` 成功则 `path.join(real, ...rest)`（`rest` 是已剥离的尾段，按 `unshift(path.basename(cur))` 累积）后**再走一次 ① 的双基准比价**，通过 ⇒ 返回 `abs`（**返回词法绝对路径，不返回 realpath** —— 调用方后续用 `abs` 做 IO，返回 realpath 会把「root 自己也是 symlink」的合法形态改写成另一条路径）；失败 ⇒ `path.dirname(cur)`，父 = 自身则 `return null`；`err.code !== 'ENOENT'` ⇒ `return null`（**fail-closed**）。
       - 判据对象与返回值语义照抄原型实测形态（`51-RESEARCH.md:1130-1153`），但**不引入** `lexicallyInside` 之外的第三个判据。
    2. **新增内部包装 `guardForWriteResult(p)`**（与 `guardResult` 并列，**不改 `guardResult` 本体**）：`const resolved = resolveInsideForWrite(root, p); return resolved === null ? deny(p) : null;`。注释里写明它与 `guardResult` 的分工（写面 vs 读面）以及「**为什么不能改 `guardResult` 本体**」（读面共用 + SEC-10 的「既有放行 / 拒绝集合零变化」判据）。
    3. **切换全部写面**（逐方法把 `guardResult(` 换成 `guardForWriteResult(`）：`writeFile`（`:265`）、`appendFile`（`:271`）、`renameFile`（`:277-283`，**source 与 destination 两处都要换** —— destination 是写逃逸的正主）、`createDir`（`:315`）、`remove`（`:321`）。
    4. **临时目录方法纳入**（research 实测 8 的「可选，建议一并纳入」在此定为必做）：
       - `createTempDir(prefix)`：在 `fs.promises.mkdtemp(...)` **之前**对 `getTmpDir()` 做 `guardForWriteResult(getTmpDir())`（父目录被替换成 symlink 时拒绝），**之后**对返回的 `dir` 再做一次 `guardForWriteResult(dir)`，两次任一失败返回 `err(new FileError('permission_denied', …))`。
       - `createTempFile(opts)`：同款两段复核（父目录 + 产物路径）。
       - ⚠️ 保持产物命名不变（`'tmp-' + sanitizeNamePart(prefix)` 前缀**不得**改）：它的形状与 `builtin-skills-seeder.sweepSeedResidue` 的清扫正则成对，改名会让崩溃清扫静默失效。在方法体注释里登记这条成对不变式。
    5. **`module.exports` 追加 `resolveInsideForWrite`**（`resolveInside` 已在列，供单测直接调纯函数；包装函数 `guardForWriteResult` **不导出** —— 它只经 `env` 的行为面覆盖）。
    6. **`tests/test-agent-workspace.js` 追加写面加固用例组**（本任务落 4 条最小集，Task 2 补全）：
       - `env.writeFile('<escape-link>/x', ''1'')` 被拒（`result.ok === false && result.error.code === 'permission_denied'`），且 `fs.existsSync(<root 外的目标>) === false`（**真副作用判据**，不是「没调用过 fs」）。
       - `env.writeFile('<self-link>/x', ''1'')` **成功**（自指链接的正命题）。
       - `resolveInsideForWrite` 纯函数的四条值域：空串 / 纯空白 / 兄弟前缀目录 / `..` 逃逸 ⇒ 全 `null`。（**注意**：`setWorkspaceDir` 必须在同用例内注入临时根，否则纯函数以真实 userData 为 root。）
       - **既有 21 例逐字未改且全绿**（本任务不动那些用例的任何一行；若某条红了，是**实现**错而不是用例要改 —— 见 `<acceptance_criteria>`）。
  </action>
  <verify>
    <automated>node --check agent-workspace.js && node -e 'const fs=require("fs");const src=fs.readFileSync("agent-workspace.js","utf8");const bad=[];if(!src.includes("function resolveInsideForWrite(root, target)"))bad.push("缺 function resolveInsideForWrite(root, target)");if(!/const guardForWriteResult\s*=/.test(src))bad.push("缺 guardForWriteResult 内部包装");if(!/module\.exports\s*=\s*\{[\s\S]*?\bresolveInsideForWrite\b/.test(src))bad.push("resolveInsideForWrite 未登记进 module.exports");const i=src.indexOf("function resolveInsideForWrite(root, target)");const body=src.slice(i,src.indexOf("\n}\n",i));if(body.indexOf("realpathSync(")===-1)bad.push("resolveInsideForWrite 未做 realpath 复核（缺口未闭合）");if(body.indexOf("path.dirname(")===-1)bad.push("resolveInsideForWrite 未做「最近已存在祖先」上溯");if(bad.length){console.error("写面判据缺失:\n"+bad.join("\n"));process.exit(1)}console.log("写面判据 ok（resolveInsideForWrite + 包装 + 导出 + realpath 上溯）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `写面判据 ok`，或 stderr 列出任意一条缺失项</fails_when>
    <automated>node -e 'const fs=require("fs");const src=fs.readFileSync("agent-workspace.js","utf8");const bad=[];const win=(name)=>{const i=src.indexOf("async "+name+"(");if(i===-1){bad.push("缺方法 async "+name+"(");return ""}const j=src.indexOf("\n    },",i);return src.slice(i,j===-1?i+1400:j)};for(const m of ["writeFile","appendFile","renameFile","createDir","remove"]){const b=win(m);if(b&&b.indexOf("guardForWriteResult(")===-1)bad.push(m+" 未切到 guardForWriteResult")}for(const m of ["readTextFile","readTextLines","readBinaryFile","fileInfo","listDir","exists"]){const b=win(m);if(b&&b.indexOf("guardResult(")===-1)bad.push(m+" 未保留 guardResult 读面判据")}const rw=src.indexOf("async renameFile(");const rwb=src.slice(rw,src.indexOf("\n    },",rw));const n=(rwb.match(/guardForWriteResult\(/g)||[]).length;if(n!==2)bad.push("renameFile 的写面校验 "+n+" 处（source 与 destination 必须各 1）");if(bad.length){console.error("写面切换不合格:\n"+bad.join("\n"));process.exit(1)}console.log("五个写面已切换 + 六个读面未动（renameFile 双路径各一）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `五个写面已切换`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-agent-workspace.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 行计数低于 21（既有用例被删），或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`node tests/test-agent-workspace.js` 的 `# tests` ≥ 25（21 既有 + 本任务新增 ≥ 4）且 `# fail 0`。
    - **可失败性（必须在当前树上先跑一遍再定稿，执行者落盘前自行复现一次）**：三条门禁的失败项在本仓当前树上全部是「新代码尚未落地」类（缺 `resolveInsideForWrite` / 未切写面）—— **无**「合规实现也修不掉」的断言；`窗口够不到目标格` 与 `断言恒真` 两种自毁形态已按下述方式规避：写面窗口由 `async <name>(` 到 `\n    },` 界定（不用固定字符数），读面判据断言的是**存在** `guardResult(` 而非不存在别的 token；`renameFile` 用**计数恰 2**而非「至少一次」。
    - **单点变异（执行者落地后必须自行跑，各确认转红后复原）**：① 把 `writeFile` 的 `guardForWriteResult(` 改回 `guardResult(` ⇒ 第二条门禁报 `writeFile 未切到 guardForWriteResult`；② 删掉 `resolveInsideForWrite` 里的 `realpathSync(` 那一行 ⇒ 第一条门禁报 `未做 realpath 复核`；③ 把 `renameFile` 的 destination 那一处改回 `guardResult(` ⇒ 报 `renameFile 的写面校验 1 处`。
    - **行为判据（真副作用）**：`env.writeFile('<escape-link>/x')` 被拒 **且** `fs.existsSync(<root 外路径>) === false`；把 `resolveInsideForWrite` 换成 `resolveInside`（即回退加固）会让该用例的 `existsSync` 变 `true` ⇒ 这条断言**能在未修复的树上转红**（research 实测 8 的 `write-guard.js` 已给出该红轮的逐字输出）。
    - **正命题**：自指链接（`ln -s references references/self` 与 `ln -s . loop`）下的写入**成功** —— 只写否命题（「逃逸被拒」）会在「加固把一切 symlink 都拒了」的过度收紧实现上也全绿。
    - 既有 21 例**逐字未改**：以本计划的起点 sha 为基线比对（`BASE=$(cat .git/gsd-plan-head-before-51-01 2>/dev/null || git rev-parse HEAD)`）—— `git diff $BASE -- tests/test-agent-workspace.js` 只应有追加、无删改。⚠️ **裸 `git diff` 对本计划自己的未提交改动是恒空判据**，必须带基线 sha 才是有效判据。
  </acceptance_criteria>
  <done>恶意/逃逸 symlink 下的写盘从「词法放行、实际写到 root 外」转为拒绝；沙箱内自指链接的写入仍放行；五个写方法与两个临时目录方法全部走新判据，读面判据一字未动，既有 21 例全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 写面加固的完整用例组与源码契约（含既有集合零变化的正反证据）</name>
  <files>tests/test-agent-workspace.js</files>
  <read_first>
    - `tests/test-agent-workspace.js`：`:1-40`（骨架与夹具）、`:60-180`（全部既有用例 —— **逐条读**，本任务的「零变化」断言要引用它们，且**不得**修改任何一行）、`:157-165`（`env.exec(''ln -s /etc link-to-etc'')` 的建链形态 —— 写面用例照它建链，但断言的是 `writeFile` 而非 `readTextFile`）
    - `agent-workspace.js`：Task 1 落地后的 `resolveInsideForWrite` / `guardForWriteResult` 与七个已切换方法（**先读再写用例** —— 用例必须覆盖实现真实构造的形态，不得手工构造实现从不构造的对象）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`#### 实测 8` 的 `（c）既有测试受影响的清单`（逐条列出了哪一例预期不变、哪一例是模板）与 `（d）原型实测` 的十例输出表（**用例形态直接对齐这十例的名目**）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### agent-workspace.js` 的 `④ module.exports`（新函数已登记 ⇒ 单测可直接 require 纯函数）与 `### symlink 拒绝的语义（两处镜像）`（`assertNoSymlink` 的递归 `withFileTypes` + `isSymbolicLink()` —— 本任务**不**实现它，只登记语义区别：沙箱写面判的是**落点**、导入包判的是**包内条目**）
  </read_first>
  <action>
    在 `tests/test-agent-workspace.js` 追加一个独立的用例组（建议 `describe(''写面加固（SEC-10）'')` 或既有风格的分组注释），共 **≥ 9 条**，逐条对齐 research 实测 8 的十例名目：
    1. `root 内尚不存在的新文件` ⇒ 允许（`resolveInsideForWrite` 返回非 null）。
    2. `root 内已存在的文件` ⇒ 允许。
    3. `..` 逃逸 ⇒ 拒绝。
    4. 绝对路径越界 ⇒ 拒绝。
    5. **兄弟前缀目录**（root = `<tmp>/root`，目标 = `<tmp>/root-evil/x`）⇒ 拒绝（这是「前缀比较必须带 `path.sep`」的正命题）。
    6. `link OUT then write`（`ln -s <root 外目录> link` 后写 `link/x`）⇒ **拒绝**，且 `fs.existsSync(<root 外目录>/x) === false`。
    7. `link OUT to a FILE then write`（链接指向 root 外**文件**）⇒ 拒绝。
    8. `link IN (self-referential) then write`（`ln -s references references/self` 后写 `references/self/y`）⇒ **允许**。
    9. `link IN to a sibling dir then write`（`ln -s ../other in-link`，`other` 与 `in-link` 同在 root 内）⇒ **允许**。
    10. `link IN loop`（`ln -s . loop` 后写 `loop/z`）⇒ **允许**。
    11. `env.renameFile` 的 **destination** 逃逸（`dst` 的中间目录是 `link OUT`）⇒ 拒绝；source 与 destination **都在** root 内 ⇒ 成功（一条 `renameFile` 双路径的正反例）。
    12. `env.remove('<link OUT>/x')` ⇒ 拒绝（`remove` 是写面，不得被漏掉）。
    13. `env.createDir('<link IN>/newdir')` ⇒ 成功；`env.createDir('<link OUT>/newdir')` ⇒ 拒绝。
    14. **源码契约（正命题 + 并列关系）**：读 `agent-workspace.js` 文本，断言（a）`function resolveInsideForWrite(` 存在；（b）`guardForWriteResult` 的**定义**与**在五个写方法体内的调用**都存在；（c）五个读面方法体内**仍**含 `guardResult(`；（d）`renameFile` 内 `guardForWriteResult(` 恰 2 处。**判据必须先剥注释**（一条解释「本方法已切到写面判据」的注释不得构成违规）—— 复用 `tests/test-skills-http-api.js:58-94` 的剥注释器形态（**逐字符五态扫描**：代码 / 行注释 / 块注释 / 单引号串 / 双引号串 / 模板串；**等长空白化**）。
    15. **「既有集合零变化」的显式对照**：把 Task 1 前后**同一组**探针（十例）的判定结果写成一张期望表（`{ name: ''link OUT then write'', allow: false }` …），逐条断言 —— 使「有哪一例被判反了」有可读失败信息，而不是靠 14 条独立用例猜。
  </action>
  <verify>
    <automated>node --check tests/test-agent-workspace.js && node tests/test-agent-workspace.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>non-zero exit，或输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 34（21 既有 + 本计划新增 ≥ 13）</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/test-agent-workspace.js","utf8");const bad=[];for(const s of ["link OUT then write","link IN (self-referential) then write","sibling","root-evil"])if(t.indexOf(s)===-1)bad.push("缺用例名目: "+s);if(t.indexOf("existsSync")===-1)bad.push("缺真副作用断言（existsSync）");if(t.indexOf("guardForWriteResult(")===-1)bad.push("缺写面源码契约断言");if(t.indexOf("guardResult(")===-1)bad.push("缺读面并列断言");if(bad.length){console.error("用例组不合格:\n"+bad.join("\n"));process.exit(1)}console.log("写面用例组 ok（十例名目 + 真副作用 + 正反源码契约）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `写面用例组 ok`，或 stderr 列出任意一条缺失项</fails_when>
  </verify>
  <acceptance_criteria>
    - `node tests/test-agent-workspace.js` 的 `# fail 0`，`# tests` ≥ 34。
    - 第 6 / 11 / 12 条用例（`link OUT` 的三处写面：`writeFile` / `renameFile` destination / `remove`）**都能在回退加固的树上转红** —— 执行者至少对 `writeFile` 那一条做一次「把实现换回 `resolveInside` ⇒ 转红」的对照并记进 SUMMARY。
    - 第 8 / 9 / 10 条（自指 / 同根内链接 / 自环）**在加固后的树上绿**，且它们的**正命题形态**可用「把 `resolveInsideForWrite` 改成无条件 `return null`」一次变异全部转红（执行者跑一次并记进 SUMMARY）。
    - 第 14 条源码契约是**正命题 + 并列关系**（写面用 `guardForWriteResult` **且** 读面仍用 `guardResult`）：只断言「写面已切换」会在「把读面也一起切了」的过度收紧实现上全绿；只断言「读面未动」会在什么都没做的实现上全绿。两条必须同时在。
    - 用例组的注释里如实登记**未覆盖面**：`exec` 的写盘能力（D-15 明文不在本层校验）与本加固**不**声称的能力边界。
  </acceptance_criteria>
  <done>`tests/test-agent-workspace.js` 新增写面加固用例组，十例判定与「既有集合零变化」有一张可读的期望表，源码契约同时钉住写面切换与读面未动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI 工具（`write` / `edit` / `bash`）→ 沙箱 root 外的文件系统 | 不可信路径经 `ExecutionEnv` 进入真实 `fs`；本计划加固的正是这条边界上「写目标尚不存在」的那一段 |
| 沙箱 root 内的**已存在** symlink → 写盘路径 | 链接本身合法（用户或 bash 建的），但它把写的**实际落点**带到 root 外 |
| `bash` 的任意命令 → 文件系统 | 命令内容**不在**沙箱这一层校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Elevation of Privilege / Tampering | `createSandboxEnv` 的 `writeFile` / `appendFile` / `renameFile` / `createDir` / `remove` | high | mitigate | 全部切到 `guardForWriteResult`（`resolveInsideForWrite`：词法双基准 + 最近已存在祖先 realpath 复核）；`renameFile` 的 source 与 destination 各校验一次；用例组第 6/11/12 条覆盖三个写面各自的逃逸路径 |
| T-51-02 | Elevation of Privilege | `createTempDir` / `createTempFile`（走 `fs.promises.mkdtemp`，`mkdtemp` 本身不经 guard） | high | mitigate | 父目录（`getTmpDir()`）+ 产物路径**两段**复核；产物命名前缀保持不变（与崩溃清扫正则成对） |
| T-51-03 | Denial of Service | `realpathSync` 在父目录链上的上溯（路径极深时） | low | accept | 上溯深度受路径段数物理限制（`MAX_PATH` 级），且仅对写目标调用一次；不引入缓存（缓存会引入失效面，收益不足） |
| T-51-04 | Denial of Service | `canonicalPath` 的既有输出再校验与本次新函数的语义混淆 | low | accept | `canonicalPath` 是**读面**（`:296-305` 已有输出再校验），本计划**不动它**；新函数的注释里交叉引用它的先例，避免后续把两处合并 |
| T-51-05 | Tampering | 既有读面语义被顺手改动（把 `resolveInside` 的 ENOENT 分支改成 fail-closed） | high | mitigate | 新函数**并列**于 `guardResult` 而非改本体；门禁二断言六个读面方法体内仍含 `guardResult(`；门禁用例引用既有 21 例（`根内普通路径（存在与不存在）放行` 会先转红） |
| T-51-06 | Repudiation | 加固被写成「bash 写盘能力已被封闭」 | medium | mitigate | 用例组注释 + `prohibitions` 具名登记；诚实边界由 `51-07` 写进 `docs/product/ai-agent-workspace.md` §七 |
| T-51-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | **本计划零安装动作、零新增依赖**（只用 `path` / `fs` 内建）⇒ 无供应链面 |
</threat_model>

<verification>
- 两个任务的 automated 门禁全绿；`node tests/test-agent-workspace.js` 的 `# tests` ≥ 34 且 `# fail 0`。
- 既有 21 例**逐字未改**：`BASE=$(cat .git/gsd-plan-head-before-51-01 2>/dev/null || git rev-parse HEAD)`，`git diff $BASE -- tests/test-agent-workspace.js` 只有追加 —— SEC-10 的「既有用例集合零变化」是**字面**判据，而它是本计划改动**未提交**时的承重判据 ⇒ 必须带基线 sha（裸 `git diff` 会恒空通过）。
- 行号引用一律标注「实测于 2026-09-15」；本计划新增的判据锚在函数名 / 方法名 / token 上，不锚行号（漂移后仍可用）。
- 人工面（不进本计划）：`bash` 交互下的体感差异（`ln -s` 出去再写被拒）—— 由 `51-07` 的产品文档披露，并在阶段 UAT 里人工走一次。
- 本计划**不**触碰导入面任何符号（`ai-skills-manager.js` 零改动）⇒ `51-03` 之后才能验证「导入落点也受同一判据保护」。
</verification>

<success_criteria>
- `resolveInsideForWrite` 关掉「中间目录为 symlink 且目标不存在」的写逃逸，且**沙箱内自指链接仍放行**。
- 五个写方法与两个临时目录方法全部走新判据；六个读面方法判据一字未动。
- 既有 21 例全绿、零删改；新增 ≥ 13 例覆盖 research 实测 8 的十例名目 + 真副作用 + 正反源码契约。
- 未覆盖面（`exec`）与行为变更（`ln -s` 出去再写被拒）在用例注释与本计划 prohibitions 里具名登记，不在本阶段声称「bash 写盘已封闭」。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-01-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-01）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 内部函数（导出供单测） | `resolveInsideForWrite(root, target)` | `agent-workspace.js` |
| 内部包装函数（不导出） | `guardForWriteResult(p)` | `agent-workspace.js` |
| 被切换的方法 | `writeFile` / `appendFile` / `renameFile`（双路径）/ `createDir` / `remove` | `agent-workspace.js` |
| 追加复核的方法 | `createTempDir(prefix)` / `createTempFile(opts)` | `agent-workspace.js` |
| 测试用例组 | `写面加固（SEC-10）`（≥ 13 例） | `tests/test-agent-workspace.js` |

### 本阶段其余计划将新建（此处仅登记，避免在评审时被当作「应已存在」）

| 计划 | 符号 |
|------|------|
| 51-02 | `dependencies.yauzl`（`^3.4.0`）、`dependencies.yaml`（**精确** `2.9.0`）、`package-lock.json` 的对应条目 |
| 51-03 | `IMPORT_LIMITS`、`IMPORT_SKILL_ERROR`、`SKILL_THREAT_PATTERNS`、`scanSkillThreats()`、`validateEntryName()`、`extractAndValidatePackage()`、`locateSkillRoot()`、`buildImportPreview()`、`importUserSkill()`、`prepareFromRawFile()`、`readRawBody()`（`main.js`）、`MAX_SKILL_PACKAGE_BYTES`（`main.js`）、`POST /api/skills/import`（`main.js`）、`AIManager.prototype.previewSkillImport()`、`AIManager.prototype.commitSkillImport()`、`#skillImportModal` 一族 DOM（`src/settings.html`）、`importSkillApi` 一族前端函数（`src/settings-page.js`）、`tests/test-skills-import.js`、`tests/helpers/make-malicious-zip.js`、`tests/fixtures/skill-corpus/**` |
| 51-04 | `resolveImportConflict()`、`sweepSkillImportResidue()`、`AIManager.prototype.cancelSkillImport()`、`AIManager.prototype.sweepSkillImports()`、`IMPORT_TTL_MS`、`MAX_PENDING_IMPORTS` |
| 51-05 | `classifyImportUrl()`、`downloadPackage()`、`HOST_WHITELIST`、`MAX_REDIRECTS`、`tests/test-skills-import-net.js` |
| 51-06 | `.skill-import-*` CSS 段、`#skillImportOpen` / `#skillImportFile` / `#skillImportUrl` / `#skillImportAck` / `#skillImportConflict` / `#skillImportGate` 等 DOM、`SKILL_IMPORT_ERROR_TEXT` 闭合白名单表、`renderSkillImportPreview()` 一族、`tests/uat-51-import-modal.js` |
| 51-07 | `docs/product/ai-skills.md` §十三【导入】、`docs/product/ai-agent-workspace.md` §七 的 SEC-10 行为变更条目、`AGENTS.md` 的导入维护约定与测试行、counts-parity 命令的 `suites` / `cells` 扩展（12 → 20） |
