---
phase: 51-zip
plan: 04
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - ai-skills-manager.js
  - ai-manager.js
  - main.js
  - tests/test-skills-import.js
autonomous: true
requirements: [USER-05, USER-08, SEC-07]
user_setup: []

estimate:
  # ⚠️ 未经本项目校准：estimate-check --calibrated 报 ratio 1.10 / over_budget=true，
  #    但 confidence=low 且 sample_count=0（本项目尚无校准样本）⇒ 该数字是**量级参考而非精确值**。
  #    不要据此重切已经有效的计划形状（三个任务分别对应「落盘事务 / 句柄生命周期 / 拒绝面矩阵」）。
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

key-decisions:
  - '**本计划不设 tracer**（依 planner-mvp-mode 的 tracer-first 规则）：它不开启新纵切，全部工作都建立在 `51-03` T1 已跑通的落盘路径上（冲突与生命周期都是**同一条路径上的分支与状态**）⇒ 无「薄纵切」可切。tracer 落在 `51-01`（沙箱子系统）与 `51-03`（导入纵切）'
  - '**冲突判定按来源分三档，判定顺序本身就是语义**（D-08）：① 与**内置（seeded）**同名 ⇒ **拒绝导入、不提供覆盖**（文案说明内置名受保护、请改用其它 name）；② `skills/<name>/` 已存在 ⇒ 预览卡片上**三选一：覆盖 / 改名 / 取消**（**绝不静默覆盖**）；③ `managed-skills/<name>/` 已存在 ⇒ **不允许覆盖**（导入只管 `skills/<name>/`，覆盖 managed 不可能是用户意图），按 46 D-06 提示「导入后该 AI 自建技能将被**永久遮蔽**」，允许**改名 / 取消**。⚠️ **不得复用 `resolveManagedTarget`** —— 它的判据方向相反（managed 视角），50 的 `deleteUserSkill` 已明文付过这条代价，本计划**只借形状**'
  - '**覆盖 = 备份 + 两段 rename，失败可回滚**（D-09 实测配方）：`rename(skills/<name> → .tmp/skill-replace-<rand>/<name>)` → `rename(importDir/<rel> → skills/<name>)` → 成功删备份 / 失败把备份 `rename` 回原位并抛。**「先删后移」被明确否决**：任一步失败会**静默丢技能**，而被替换的是**用户自己**的目录（可能手改过、可能带自写 `scripts/`）。⚠️ 备份是**临时的**（成功后即刻删除），**不得**扩张成版本历史 / 恢复按钮（ECO-02 / v1.x）—— 这条已写进 prohibitions'
  - '**改名 = 新建（计入数量闸）；覆盖 = 净增 0（预检时豁免）**（D-11 逐字）：闸的判据**读盘**（`env.listDir` 统计 `skills/` 下的目录数）而非缓存快照（bash 可随时改写磁盘）；改名走 `validateManagedSkillName`（**一份校验器，不写第二份正则**）'
  - '**回读验证失败必须回滚且暴露 diagnostics**（D-10 / P12）：落盘后经 `refreshSkills` 恰一次，确认目标 name 出现在快照里且 `filePath` 指向 `skills/`；不在 ⇒ 覆盖场景恢复备份、新建场景把目录移回 `importDir`，并抛 `READBACK_FAILED` 且 `extra.diagnostics` 带 `diagnostics` **原文（含 path）**。**不提供「部分导入」**'
  - '**`importId` 的生命周期是必须实现的，不是可选的**（D-02）：TTL 到期清理、命中过期句柄回**明确码**（`import_expired`）+ 文案「预览已过期，请重新选择文件」（**不得静默失败**）；并发待确认导入上限（`IMPORT_LIMITS.MAX_PENDING_IMPORTS` = 3）；**崩溃残留清扫**（启动 / 退出 + 每次 preview 时顺带清扫过期项）'
  - '**崩溃清扫必须带陈旧性判据 —— 不得重复 `WR-05` 的历史缺陷**（`47-REVIEW.md` 的 `WR-05`：`sweepSeedResidue` 缺陈旧性判据 ⇒ 并发实例（`dev` 与 `debug` 共享 userData 且无单实例锁）会**互删进行中的 tmp/bak**）。本计划的清扫分两种**互不混淆**的模式：**`own`**（只删**本进程 Map 里**登记的目录 ⇒ 精确、无跨实例风险，用于退出）+ **`stale`**（扫 `.tmp/` 下匹配前缀的残留，**只删 mtime 早于 2 × TTL** 的 ⇒ 用于启动与 preview 时的兜底）'
  - '**前缀常量单源 + 命名形状成对不变式（正命题）**：`skill-import-` 与 `skill-replace-` 两个前缀只在一处定义；`fs.mkdtempSync` 的**实际产物**（`skill-import-` + 6 位随机）与覆盖备份的**实际产物**都必须在清扫判据里被**正向**匹配到（`sweepSeedResidue` 的成对不变式同款要求）。⚠️ **不得**用 `agent-workspace.createTempDir(''skill-import'')` —— 它会再加一层 `tmp-` 前缀（产物变成 `tmp-skill-import-<rand>`），与本计划的清扫正则不成对；本计划用 `fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))`（**经访问器拿路径**，仍落在沙箱 root 内的 `.tmp/` 下，与 `skills/` 同设备）'
  - '**`IMPORT_SKILL_ERROR` 的 20 键已在 `51-03` 一次定义完整** ⇒ 本计划**不重新组织码表**、只在本计划的拒绝面里**按需补一个真实缺失的原因**（本计划实际补的是 `INVALID_NAME: ''invalid_name''` —— 改名非法这一原因在 `51-03` 的码表里没有对应值 ⇒ 最终码表 **21 键**）。新增码必须同批刷新 `51-07` 的码表账本（第十三节码表 + `AGENTS.md` 测试清单），使「后端多一个前端没文案的码」不可能发生（`51-06` 的闭合白名单表由一条**双向覆盖**用例守住，其键集合须为这 21 个值）'
  - '**本计划的 commit 仍不接受任何路径参数**：入参只有 `{ importId, conflict, newName }`。客户端永远不能指定落点（D-02 / ARCHITECTURE Anti-Pattern 1）—— 这条是接口边界而非沙箱能兜住的'

must_haves:
  truths:
    - 'seeded 同名 ⇒ 拒绝导入（skills/ 零变化）；user 同名三选一各自可达；改名走 validateManagedSkillName（非法名被拒）；managed 同名不许覆盖'
    - '每个 IMPORT_SKILL_ERROR 码至少一条用例覆盖；回传文案含码 + 可读原因；限额类失败文案含限额名与当前值；无任何失败路径返回空 message 或仅通用错误'
    - '覆盖事务可回滚：在**第二步** `rename` 注错（目标父目录缺失 / 目标为已存在的非空目录等**真实**失败形态）⇒ 旧技能内容**逐字完好**（不是「assert 没删过」，而是回滚后读回旧目录内容与覆盖前一致）'
    - '回读验证失败即回滚并暴露 `diagnostics` 原文（含 path）：注入一次「落盘后 name 不出现在快照」的真实形态 ⇒ 目录被移回临时区（覆盖场景：备份被恢复），且错误对象的 `extra.diagnostics` 非空'
    - '句柄生命周期三态各自可达：`import_expired`（TTL 到期后 commit）、`import_not_found`（未知 / 已被消费的 importId）、`too_many_pending`（同时挂起的导入达 `MAX_PENDING_IMPORTS`）—— 三条都是**明确码 + 可读原因**，不是静默失败'
    - '崩溃残留清扫不误删进行中的包：把一个 `skill-import-` 残留目录的 mtime 设为**刚刚**（模拟另一实例正在预览）⇒ `sweepSkillImports({ mode: ''stale'' })` **不得**删它；mtime 设为 2 × TTL 之前 ⇒ 必须删它（正反两例）'
    - '数量闸口径：`skills/` 下目录数已达 `MAX_USER_SKILLS` 时 —— **改名**导入被拒（计入），**覆盖**同名导入成功（净增 0 豁免）；两条各有独立用例，且判据是**读盘**（改完磁盘后立即生效，不依赖重扫）'
  artifacts:
    - 'ai-skills-manager.js：`resolveImportConflict(env, name, { seededNames })`（三档判定）、`importUserSkill` 的冲突分支扩展（覆盖两段 rename + 回滚 + 回读失败回滚）、`IMPORT_TMP_PREFIXES`、`isImportResidueName(name)`'
    - 'ai-manager.js：`cancelSkillImport({ importId })`、`sweepSkillImports({ mode })`、`previewSkillImport` 的 TTL / 并发上限接入、`commitSkillImport` 的过期与未知句柄分支、`this._skillImports` 项的 `createdAt` / `dir` / `origin` 消费点'
    - 'main.js：启动时 `sweepSkillImports({ mode: ''stale'' })` 与 `before-quit` 序列里的 `sweepSkillImports({ mode: ''own'' })` 接线（不新开 handler）'
    - 'tests/test-skills-import.js：冲突三档 × 三选项矩阵用例、覆盖回滚用例（真实失败注入）、回读失败回滚用例、TTL / 未知句柄 / 并发上限用例、清扫陈旧性正反用例、数量闸两条口径用例、错误码覆盖矩阵用例'
  key_links:
    - '单一落盘链不变：本计划**只扩** `importUserSkill` 的分支，**不得**新增第二个落盘实现 —— `51-03` 的「`importUserSkill(` 全仓恰 2 处」判据在本计划结束后仍须成立（回归判据）'
    - '状态单源链：`importId` 的 Map 只在 `ai-manager.js`（`this._skillImports`），**判定住 manager、状态住 ai-manager**。`main.js` 的 handler 只转发 `{ importId, conflict, newName }`，**不得**触碰 Map（它是不可序列化的进程内状态）'
    - '读盘判据链：冲突判定 / 数量闸 / 存在性一律 `env.exists` / `env.listDir`（**需要判「是目录」时必须用 `env.fileInfo`** —— `env.exists` 对普通文件也返回 true）'
    - '次数账不变（D-19）：成功出口仍是 `await this.ensureSkillsFresh()` **恰一次** + **调用侧**补播 `windowManager.broadcast(''skills:changed'')` **恰一次**；`syncAgentSystemPrompt()` 函数体逐字未改。本计划新增的失败 / 取消出口**不得**补播（没有变更就没有广播）'
  prohibitions:
    - statement: '不得把覆盖用的临时备份扩张成版本历史 / 恢复机制（含不得为它加「撤销上一次导入」按钮、不得把备份目录留在盘上不删）'
      status: unresolved
      verification: judgment
      reason: 'D-09 明文：备份是**临时**的（成功后即刻删除），不是版本历史机制；版本历史 / 恢复按钮是 ECO-02 / v1.x。且 50 的卸载口径是「无备份」（用户主动要删），把它扩张会让两条口径分裂'
    - statement: '不得静默覆盖任何已有技能，也不得提供「部分导入」（半个技能目录落盘、或落盘失败但目录残留）'
      status: unresolved
      verification: judgment
      reason: '被替换的是**用户自己**的目录；「先删后移」在任一步失败时会静默丢技能（D-09 的理由）。「部分导入」会让加载管线读到半个技能（`description` 缺失 ⇒ SDK 整条丢弃 ⇒ 幽灵技能）。两条都属「失败要真实」的直接推论'
    - statement: '不得把新导入错误码并入 `MANAGE_SKILL_ERROR`（也不得为了让导入复用取值而修改该表）'
      status: unresolved
      verification: judgment
      reason: '`tests/test-manage-skill.js:1589` 把该表锁死为**恰 11 键**、`:1605` 逐字写着「不得再新立第十二键」⇒ 加码会让 49/50 的账本同时漂移。CR-3 已裁决另立 `IMPORT_SKILL_ERROR`'
    - statement: '不得用「先 `remove` 再 `rename`」或「`fs.rmSync` + `fs.renameSync`」绕过沙箱的写面判据来实现覆盖'
      status: unresolved
      verification: judgment
      reason: '`env.renameFile` / `env.remove` 走 51-01 加固后的写面判据（双基准 + 最近已存在祖先 realpath）；绕开沙箱直用 Node `fs` 会把这条保护摘掉，而这正是 P2 门禁的承重面。落点路径必须经 `path.join(getSkillsDir(), name)` + 沙箱原语'
---

<objective>
交付**落盘事务**（冲突三档 + 覆盖的备份 / 回滚 + 回读验证失败即回滚）与**两阶段句柄的完整生命周期**（TTL / 并发上限 / 显式取消 / 崩溃残留清扫），并把 USER-08 的「每条拒绝路径都给真实原因」收成一张**可逐码验收**的矩阵。

Purpose: `51-03` 交付的落盘路径在「同名」与「句柄过期」两条真实路径上还只会拒绝。本计划补上**用户可见的冲突决策面**（三选一）、**破坏性动作的可回滚性**（覆盖的用户可反悔）、以及**跨进程的残留安全**（崩溃后不留垃圾、并发实例不互删）。这三件事都是「用户已显式确认的破坏性动作」的配套，缺一条都会让 P12（静默失败）或 WR-05（互删）重演。

Output: `resolveImportConflict` 与覆盖事务、`importId` 的 TTL / 并发 / 取消 / 清扫、启动与退出接线、以及 `tests/test-skills-import.js` 的冲突矩阵 / 回滚 / 生命周期 / 错误码覆盖用例。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-RESEARCH.md
@.planning/phases/51-zip/51-PATTERNS.md
@.planning/phases/51-zip/51-03-PLAN.md
@ai-skills-manager.js
@ai-manager.js
@main.js
@agent-workspace.js
@tests/test-skills-import.js
@tests/test-manage-skill.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 冲突三档判定 + 覆盖事务（两段 rename + 回滚）+ 回读失败回滚 + 数量闸口径</name>
  <files>ai-skills-manager.js, ai-manager.js, tests/test-skills-import.js</files>
  <read_first>
    - `ai-skills-manager.js`：`51-03` 落地的 `importUserSkill` / `buildImportPreview` / `IMPORT_SKILL_ERROR` / `IMPORT_LIMITS`（**先逐行读** —— 本任务只在既有分支上扩展），`:1677-1703`（`managedSkillPaths` 的路径计算范式 + `countManagedSkills` 的读盘计数范式 —— 导入侧需要同形的 `userSkillPaths(name)` / `countUserSkills(env)`）、`:1727-1760`（`resolveManagedTarget` 的**形状**（判定顺序即语义）—— **不得复用它的判据**）、`:2030-2070`（`deleteUserSkill` 与 `env.remove(..., {recursive:true})`）、`:1782-1873`（`createManagedSkill` 的九步顺序：**先扫描 → 后净化 → 路径 → 撞名（读盘）→ 数量闸 → 字节闸（组装之后、落盘之前）→ 建目录 → 原子写 → 失败只在确实新建时清理**）
    - `agent-workspace.js`：`:265-352`（`writeFile` / `renameFile` / `createDir` / `remove` 的 Result 契约与 51-01 加固后的写面判据）；`.tmp/` 与 `skills/` 同 root 的关系（`getTmpDir()` / `getSkillsDir()` 都从 `getWorkspaceDir()` 派生）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 4`（`:839-876`）—— **本任务的实现配方与红轮证据来源**：`C1 rename-to-absent: OK` / `C2 two-phase: OK content=NEW; backup removed: true` / `C3 step2 failed as expected: ENOENT; rollback restored: OLD-RB` / `C4 lstat isSymlink: true` / `C4b rmSync(recursive) on symlink: link gone / target intact`；`## Code Examples` 例 6（`:1339-1352` 的两段 rename 配方）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### ai-skills-manager.js` 的 ⑮（`:247-276` 的统一目标判定 —— **只借形状**）、⑯（`:277-300` 导入侧的七步顺序）、⑰（`:302-309` 递归删除）、`## Pattern 6 的实测配方`（`:1176-1191`）
    - `tests/test-skills-import.js`：`51-03` 落地的既有用例（夹具 `withTempRoot` / `makeEnv` / `buildZip` 的用法与 seededNames 注入纪律）
  </read_first>
  <action>
    1. **`ai-skills-manager.js` 新增 `async function resolveImportConflict(env, name, { seededNames })`**（**同步零 IO 除外**：它必须读盘）：判定顺序**即语义**，逐档返回可读原因：
       - ① `isSeededName(name, seededNames)` ⇒ `{ kind: ''seeded'', code: SEEDED_CONFLICT, message: ''"<name>" 是内置技能名，受保护，无法导入。请改用其它名称。'' }`（**不提供覆盖**）。
       - ② `skills/<name>` 存在（**用 `env.fileInfo` 判 `kind === ''directory''`** —— `env.exists` 对普通文件也返回 true，用错会把一个同名**文件**误判成可覆盖的目录）⇒ `{ kind: ''user'', message: ''已存在同名用户技能「<name>」。'' }`（前端给三选一）。
       - ③ `managed-skills/<name>` 存在 ⇒ `{ kind: ''managed'', shadowedBy: ''managed'', message: ''已存在同名 AI 自建技能「<name>」，导入后它将被永久遮蔽。'' }`（**只允许改名 / 取消**，不允许覆盖）。
       - ④ 否则 `{ kind: ''none'' }`。
       - 新增 `userSkillPaths(name)`（`{ skillsDir: getAgentWorkspaceLazy().getSkillsDir(), destDir: path.join(skillsDir, name) }` —— **目录名一律用校验后的 name，绝不用包内原始目录名拼路径**）与 `async function countUserSkills(env, skillsDir)`（读盘统计 `kind === ''directory''` 的条目数，失败按 0 计，形状照 `countManagedSkills`）。
    2. **扩 `importUserSkill`**（**仍是唯一落盘实现**）：
       - 参数扩展为 `(env, { srcDir, rootRel, name, conflict, newName }, { seededNames })`；`conflict ∈ { ''overwrite'', ''rename'', ''cancel'' }`（缺省 `undefined` = 未给选择）。
       - **`cancel` 分支**：不落盘，返回 `{ cancelled: true }`（临时目录的清理由调用方 `ai-manager` 负责 —— 保持「manager 只判定、ai-manager 管状态与清理」的分工）。
       - **改名分支**：`resolvedName = newName` 走 `validateManagedSkillName`（**复用那一份**），失败 ⇒ 抛 `INVALID_NAME` 形态的 `IMPORT_SKILL_ERROR`（**码取值必须来自 `IMPORT_SKILL_ERROR`，不得借用 `MANAGE_SKILL_ERROR` 的值** —— 但允许新增一个 `import` 域的 `invalid_name` 键？**不允许**：`IMPORT_SKILL_ERROR` 已在 `51-03` 定义完整，改名非法走 `FRONTMATTER_INVALID` 语义不合 ⇒ 本任务**新增且仅新增** `INVALID_NAME: ''invalid_name''` 一键到 `IMPORT_SKILL_ERROR`，并在 SUMMARY 里点名它是本计划唯一的加码）。
       - **数量闸**（只约束**净增**）：`conflict === ''overwrite''` ⇒ **豁免**（净增 0）；否则 `await countUserSkills(...) >= LIMITS.MAX_USER_SKILLS` ⇒ 抛 `LIMIT_EXCEEDED`，message 含「用户技能数量已达上限」+ 当前值 + 限额值 + 「请先卸载不再需要的技能」。
       - **无冲突 / 改名路径**：`env.renameFile(path.join(srcDir, rootRel), destDir)`（同设备、原子）。**父目录必已存在**（`skills/` 由启动时创建）⇒ **不需要** `createDir` 硬前置（那是 `createManagedSkill` 的场景）；但**失败时不得留半成品**：若 `rename` 抛错且目标目录已被创建（`env.exists(destDir)` 为真）⇒ `env.remove(destDir, { recursive: true })`（**只在本次确实搬动过时执行**）。
       - **覆盖路径（两段 rename + 回滚，逐行照研究实测配方）**：`const bak = path.join(getAgentWorkspaceLazy().getTmpDir(), ''skill-replace-'' + <随机 6 位>)`；① `await env.renameFile(destDir, bak)`；② `try { await env.renameFile(path.join(srcDir, rootRel), destDir) } catch (e) { await env.renameFile(bak, destDir); throw e }`；③ `await env.remove(bak, { recursive: true, force: true })`。⚠️ 三步都用**沙箱原语**（不得用 Node `fs` 绕开写面判据）。备份目录名必须走 `IMPORT_TMP_PREFIXES` 的同一处前缀常量。
       - **回读验证 + 失败回滚**（D-10）：落盘后 `await refreshSkills(env, { disabled, rootDirs: [managedDir, skillsDir] })` **恰一次**，确认 `resolvedName` 出现在 `getSkillsSnapshot()` 且对应条目的 `filePath` 以 `skills/` 开头；不在 ⇒ **覆盖场景** `env.renameFile(bak, destDir)` 恢复备份（并把新目录移到 `importDir` 之外或直接 `remove`）；**新建场景** `env.renameFile(destDir, path.join(srcDir, rootRel))` 移回临时区；随后抛 `READBACK_FAILED`，`extra.diagnostics = <该技能的 diagnostics 原文数组>`（**含 path**）。⚠️ 回读用的 `refreshSkills` 是**本次调用的读盘**，不得写进模块级缓存之外的任何地方（它就是缓存本身）；`ai-manager` 调用侧的 `ensureSkillsFresh()` 次数账**不变**。
       - 返回报告里带 `conflict: ''overwrite'' | ''rename'' | ''none''`（供前端文案与 SUMMARY 对账）。
    3. **`buildImportPreview` 的 `conflict` 字段**改为携带 `resolveImportConflict` 的三档结果（本任务把 `51-03` 的 `{ kind: ''none'' | ''taken'' }` 细化），并**在 seeded 档使 preview 调用直接失败**（`SEEDED_CONFLICT`）—— 与「与内置同名 ⇒ 拒绝导入、不给覆盖选项」一致。
    4. **`ai-manager.js` 的 `commitSkillImport`**：把 `conflict` / `newName` 透传给 `importUserSkill`；`conflict === ''cancel''` ⇒ 清理临时目录 + 从 Map 删除 + 返回 `{ cancelled: true }`（**不**调 `ensureSkillsFresh`、**不**补播 —— 没有变更就没有广播）；成功出口仍保持「重扫恰一次 + 调用侧补播恰一次」。
    5. **`tests/test-skills-import.js` 增补**（全部用**真实 fs 与真实沙箱 env**）：
       - **冲突矩阵**：seeded 同名 ⇒ `SEEDED_CONFLICT` 且 `skills/` **零变化**；user 同名未给 `conflict` ⇒ 显式失败（不静默覆盖）；user 同名 + `overwrite` ⇒ 落盘成功且**内容为新**；user 同名 + `rename` + 合法 `newName` ⇒ 落成**新目录**且原技能**逐字完好**；user 同名 + `rename` + 非法 `newName` ⇒ `INVALID_NAME`（原因取自 `validateManagedSkillName`）；managed 同名 + `overwrite` ⇒ **被拒**（不许覆盖 managed）+ 提示含「永久遮蔽」；managed 同名 + `rename` ⇒ 成功。
       - **覆盖回滚（第二步注错 ⇒ 旧技能完好）**：用**真实失败注入**（不手工构造实现从不构造的对象）—— 首选「让第二步的目标父目录在两次 rename 之间被删掉」或用测试替身 env 把 `renameFile` 的**第二次**调用改成返回 `err(...)`（**失败形态取自沙箱真实的 `permission_denied` / `ENOENT`）**；断言回滚后 `skills/<name>/SKILL.md` 的内容与覆盖前**逐字一致**（先读一份内容副本再比对）。
       - **回读失败回滚**：造「落盘成功但 name 不进快照」的真实形态（例如包内 `SKILL.md` 的 `name` 与目录名不一致 + 组装后超过 64 KiB 被加载管线跳过）⇒ 断言抛 `READBACK_FAILED`、`extra.diagnostics` 非空、且覆盖场景下旧技能内容被恢复。
       - **数量闸两条口径**：把 `skills/` 造到 `MAX_USER_SKILLS` 个目录 ⇒ `overwrite` 成功、`rename` 被拒（`LIMIT_EXCEEDED` 且 message 含当前值与限额）；两条的判据**在磁盘改变后立即生效**（不先重扫）。
       - **同设备断言**：`.tmp/`、`skills/`、`mkdtemp` 三者 `fs.statSync(...).dev` 相同（两段 rename 的 EXDEV 前提，`51-03` 已落一条 —— 本任务为覆盖路径再断言一次备份目录与 `skills/` 同设备）。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js && node --check ai-manager.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const src=stripC(fs.readFileSync("ai-skills-manager.js","utf8"));const bad=[];for(const t of ["async function resolveImportConflict(","function userSkillPaths(","async function countUserSkills(","skill-replace-"])if(src.indexOf(t)===-1)bad.push("缺 "+t);const i=src.indexOf("async function importUserSkill(");const j=src.indexOf("\n}\n",i);const b=src.slice(i,j===-1?i+9000:j);for(const t of ["renameFile(","remove("])if(b.indexOf(t)===-1)bad.push("importUserSkill 未用沙箱原语 "+t);if(b.indexOf("fs.renameSync")!==-1||b.indexOf("fs.rm")!==-1)bad.push("importUserSkill 用了 Node fs 直写（绕过 51-01 的写面判据）");const r1=b.indexOf("renameFile(destDir, bak"),r2=b.indexOf("renameFile(bak, destDir");if(r1===-1||r2===-1)bad.push("覆盖的两段 rename / 回滚分支不齐（r1="+r1+" r2="+r2+"）");if(b.indexOf("refreshSkills(")===-1)bad.push("缺落盘后回读验证（refreshSkills）");if(b.indexOf("READBACK_FAILED")===-1)bad.push("回读失败未抛 READBACK_FAILED");if(b.indexOf("MAX_USER_SKILLS")===-1)bad.push("缺数量闸");if(bad.length){console.error("落盘事务不合格:\n"+bad.join("\n"));process.exit(1)}console.log("落盘事务 ok（三档判定 + 两段 rename + 回滚 + 回读 + 数量闸 + 沙箱原语）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `落盘事务 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const c=(f,tok)=>{const t=stripC(fs.readFileSync(f,"utf8"));return (t.match(new RegExp(tok.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),"g"))||[]).length};const a=c("ai-skills-manager.js","importUserSkill("),b=c("ai-manager.js","importUserSkill("),m=c("main.js","importUserSkill(");if(a!==1)bad.push("ai-skills-manager.js 的 importUserSkill( 应恰 1 处（唯一落盘实现），实测 "+a);if(b!==1)bad.push("ai-manager.js 的 importUserSkill( 应恰 1 处，实测 "+b);if(m!==0)bad.push("main.js 出现 importUserSkill( "+m+" 处");const mgr=stripC(fs.readFileSync("ai-skills-manager.js","utf8"));const mi=mgr.indexOf("const MANAGE_SKILL_ERROR = {");const mj=mgr.indexOf("};",mi);const keys=(mgr.slice(mi,mj).match(/^\s+[A-Z_]+:/gm)||[]).length;if(keys!==11)bad.push("MANAGE_SKILL_ERROR 键数 "+keys+"（必须恰 11，CR-3）");if(/MANAGE_SKILL_ERROR[\s\S]{0,200}(UNSUPPORTED_URL|IMPORT_EXPIRED|READBACK_FAILED)/.test(mgr.slice(mi,mj)))bad.push("导入码被并进 MANAGE_SKILL_ERROR");if(bad.length){console.error("落盘唯一性与码表隔离不合格:\n"+bad.join("\n"));process.exit(1)}console.log("落盘唯一性 ok（定义 1 / 调用 1 / handler 0）+ MANAGE_SKILL_ERROR 仍恰 11 键")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `落盘唯一性 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 60（`51-03` 的 ≥ 45 + 本任务新增 ≥ 15），或进程以非零退出</fails_when>
    <automated>node tests/test-manage-skill.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 低于 55（49 的码表锁被改动会在此转红），或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 四条门禁全绿；`tests/test-manage-skill.js` 的 55 例**零删改**（码表隔离的独立证据）。
    - **可失败性（落地前实跑一遍）**：门禁一当前树在 `resolveImportConflict` 等四项上报缺；门禁二的「`importUserSkill(` 应恰 1 处」当前树报实测 0（`51-03` 之后为 1）；`MANAGE_SKILL_ERROR 键数` 当前树为 **11**（该条现在就是绿的 —— 它是**回归护栏**，把导入码并进去才会转红）。
    - **单点变异（执行者落地后自行跑，各确认转红后复原）**：① 把覆盖路径的 `env.renameFile(bak, destDir)` 回滚分支删掉 ⇒ 回滚用例转红；② 把数量闸的覆盖豁免去掉 ⇒ 「覆盖不计新增」用例转红；③ 把 `env.exists` 换成 `env.fileInfo` 的误用形态（或用 `exists` 判「是目录」）⇒ 造一个同名**文件**在 `skills/` 下的用例转红（这条用例必须存在 —— 否则该误用不可见）。
    - **负向 token 先剥注释再判（W2，本轮修订）**：本计划**三条**门禁里的负向判据全部改在 `stripC`（与 `51-03` / `51-05` / `51-06` 同一份五态剥注释器，**保长替换** ⇒ 切片不错位）之后的源上判定。逐条理由与一对验证（均在合成实现上实跑）：
      - **门禁一**：`fs.renameSync` / `fs.rm` 的判据对象是「用没用 Node `fs` 直写」；本任务的 `<action>` 逐字写着「三步都用**沙箱原语**（不得用 Node `fs` 绕开写面判据）」⇒ 说明性注释在原文判定下会恒红。验证：注释形态（含 `fs.renameSync` / `fs.rmSync` 字样）⇒ `落盘事务 ok`、`exit 0`；单点变异（把 `env.remove(bak, …)` 换成 `fs.rmSync(bak, …)`）⇒ 报「importUserSkill 用了 Node fs 直写（绕过 51-01 的写面判据）」。
      - **门禁二**：`MANAGE_SKILL_ERROR` 表内**注释**提到导入码（例如「导入码是独立命名空间，如 `UNSUPPORTED_URL` / `READBACK_FAILED`」）不得算「并入」；验证：注释形态 ⇒ `落盘唯一性 ok`；单点变异（把 `UNSUPPORTED_URL` 真的加成表内的键）⇒ 报「导入码被并进 MANAGE_SKILL_ERROR」（键数同时报 12 ≠ 11）。
      - **门禁五**：`syncAgentSystemPrompt()` **函数体内**的注释提到 `sweepSkillImports` 不得算「接入」（该函数体逐字不得改，本来也不该有实现改动）；验证：注释形态 ⇒ `生命周期接线 ok`；单点变异（在函数体内真的调 `this.sweepSkillImports(...)`）⇒ 报「清扫被接进 syncAgentSystemPrompt()（该函数体逐字不得改）」。
    - **回滚是「读回旧内容比对」而不是「断言没删过」**：用例先在覆盖前读一份 `SKILL.md` 内容副本，回滚后再读并与副本**逐字比对**。
    - **回读验证的调用次数**：`importUserSkill` 内的 `refreshSkills(` 恰 1 处（源码扫描）+ 成功出口的 `ensureSkillsFresh()` 恰 1 处（`ai-manager.js`）；两条同时成立 ⇒ 不出现「回读用一次、收口又用一次 `syncAgentSystemPrompt`」的**三**次重扫。
  </acceptance_criteria>
  <done>同名冲突按三档处置（seeded 拒绝 / user 三选一 / managed 不许覆盖）；覆盖走「备份 + 两段 rename」，第二步失败可回滚且旧技能逐字完好；回读验证失败即回滚并暴露 diagnostics；数量闸按「覆盖豁免、改名计入」读盘判定。</done>
</task>

<task type="auto">
  <name>Task 2: 句柄生命周期 —— TTL / 并发上限 / 显式取消 / 崩溃残留清扫（含陈旧性判据）</name>
  <files>ai-manager.js, ai-skills-manager.js, main.js, tests/test-skills-import.js</files>
  <read_first>
    - `ai-manager.js`：`51-03` 落地的 `this._skillImports` / `previewSkillImport` / `commitSkillImport`（**逐行读** —— 本任务在既有方法上接生命周期），`:1628-1710`（`setSkillDisabled` / `uninstallUserSkill` 的完整形状与它们的 JSDoc 论证 —— 本任务的成功出口须逐字同构）、`:3228-3246`（`ensureSkillsFresh` —— **它是读侧 / 兜底，不并入写路径份额**）
    - `main.js`：`:843` 起的 `app.whenReady().then(async () => {`（启动序列）、`:4230-4250`（`seedBuiltinSkills()` 与 `aiManager = new AIManager()` 的顺序 —— 清扫的插入点在这之后）、`:4605-4700`（`before-quit` 的既有异步续体与 `will-quit`；**只在该 handler 的既有清理序列里追加一行**，不新开 handler、不改既有的退出确认逻辑）
    - `agent-workspace.js`：`:82-84`（`getTmpDir`）、`:321-336`（`remove` 与 `createTempDir` —— 本任务用 `fs.readdirSync` 读 `.tmp/` 的**名字与 mtime**，用 `env.remove` 删）
    - `builtin-skills-seeder.js`：`:255-266`（`sweepSeedResidue` —— 崩溃清扫的**形状参照**）与 `47-REVIEW.md` 的 `WR-05`（**它缺陈旧性判据 ⇒ 并发实例互删**：本任务必须补上，不得复制该缺陷）
    - `.planning/phases/51-zip/51-PATTERNS.md`：`### 崩溃残留清扫（须补陈旧性判据，不要重复 WR-05）`（`:1090-1102`）、`### 三条「本阶段容易抄错」的点` 第 1 条（`:1197-1209` 的访问器表与**命名形状成对不变式**）
    - `.planning/phases/51-zip/51-CONTEXT.md`：D-02 的「生命周期与清理（必须实现，不是可选）」四条与 Claude''s Discretion 的 TTL / 并发取值
  </read_first>
  <action>
    1. **`ai-skills-manager.js` 新增两个小工具**（单源 + 成对不变式）：
       - `const IMPORT_TMP_PREFIXES = Object.freeze([''skill-import-'', ''skill-replace-'']);`（**前缀只在这一处定义**，`51-03` 的 `mkdtempSync` 与本计划 Task 1 的备份目录**都**从这里取）。
       - `function isImportResidueName(name)` ⇒ 名字以任一前缀开头**且**剩余部分非空（返回布尔）。注释里写清成对不变式：`fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))` 的实际产物是 `skill-import-<6 位随机>`；覆盖备份的实际产物是 `skill-replace-<随机>`；本判据必须**正向**匹配它们（有专门用例）。
    2. **`ai-manager.js` 接入生命周期**：
       - `previewSkillImport` 入口先做 **TTL 清扫 + 并发闸**：① 遍历 `this._skillImports`，`Date.now() - createdAt > IMPORT_LIMITS.IMPORT_TTL_MS` ⇒ 删 Map 项 + `env.remove(dir, { recursive: true, force: true })`；② 若仍 `this._skillImports.size >= IMPORT_LIMITS.MAX_PENDING_IMPORTS` ⇒ 抛 `TOO_MANY_PENDING`（message 说明「同时待确认的导入过多，请先完成或取消上一个预览」）。
       - `commitSkillImport`：`importId` 不在 Map ⇒ 抛 `IMPORT_NOT_FOUND`（「预览不存在或已被使用，请重新选择文件」）；**已过期**（`createdAt` 超 TTL）⇒ 删 Map 项 + 删临时目录 + 抛 `IMPORT_EXPIRED`（「预览已过期，请重新选择文件」）。
       - 新增 `async cancelSkillImport({ importId })`：命中 ⇒ 删 Map 项 + `env.remove(dir, { recursive: true, force: true })` + 返回 `{ cancelled: true }`；不命中 ⇒ 返回 `{ cancelled: false }`（**幂等，不抛** —— 取消路径不应因「已经没了」而报错）。
       - 新增 `async sweepSkillImports({ mode })`，两种模式**语义不同、不得混淆**：
         - `mode: ''own''`：只删 `this._skillImports` 里登记的目录（清空 Map）⇒ **精确、无跨实例风险**（用于退出）。
         - `mode: ''stale''`：`fs.readdirSync(getTmpDir(), { withFileTypes: true })`，对 `kind === ''directory''` 且 `isImportResidueName(name)` 的条目，**仅当** `mtimeMs` 早于 `Date.now() - 2 * IMPORT_TTL_MS` 时 `env.remove(..., { recursive: true, force: true })`（用于启动与兜底；**陈旧性判据是硬要求**）。
         - 两种模式都返回 `{ removed: N }` 供日志与用例断言；**不抛**（清扫失败只记 `console.warn`）。
       - ⚠️ **不得**把清扫接进 `syncAgentSystemPrompt()` 或 `ensureSkillsFresh()`（那两个函数的职责是技能集，不是临时区；且前者函数体逐字不得改）。
    3. **`main.js` 接线**（**只加两行调用，不新开 handler**）：启动序列里 `aiManager = new AIManager()` 之后 `await aiManager.sweepSkillImports({ mode: ''stale'' });`（**必须在 `app.whenReady()` 的异步序体内**，失败不得阻塞启动 ⇒ `.catch` 后 `console.warn`）；`before-quit` 的既有清理序列里追加 `await aiManager.sweepSkillImports({ mode: ''own'' }).catch(() => {});`。
    4. **`ai-manager.js` 的 commit 成功后清理**：`51-03` 已有「成功 ⇒ 删 Map + 删临时目录」；本任务补齐**失败路径**：`importUserSkill` 抛错时**保留**临时目录（用户可以就地重试同一次预览）而**不**删 Map 项 —— 只有 `import_expired` / `import_not_found` / 显式 `cancel` / 成功才清。这条语义要写进 `commitSkillImport` 的 JSDoc（「失败可原地重试」是 UI-SPEC 状态机 `提交失败` 行的实现前提）。
    5. **`tests/test-skills-import.js` 增补**：
       - **TTL 过期**：把 Map 项的 `createdAt` 回拨到 `IMPORT_TTL_MS + 1` 之前（**直接改 Map 项的字段而非 sleep 10 分钟** —— 用例不得依赖真实等待）⇒ `commitSkillImport` 抛 `IMPORT_EXPIRED` 且**临时目录已被删**。
       - **未知句柄**：随机 importId ⇒ `IMPORT_NOT_FOUND`；**同一个 importId 提交两次** ⇒ 第二次 `IMPORT_NOT_FOUND`（一次性句柄的正命题）。
       - **并发上限**：连续 preview `MAX_PENDING_IMPORTS` 次后，第 `MAX_PENDING_IMPORTS + 1` 次抛 `TOO_MANY_PENDING`；先 `cancelSkillImport` 一次后又能 preview（证明闸是「待确认数」而不是「累计数」）。
       - **取消幂等**：`cancelSkillImport` 两次 ⇒ 第二次 `{ cancelled: false }` 而不抛。
       - **清扫陈旧性正反两例**：在 `.tmp/` 下造 `skill-import-abcdef` 与 `skill-replace-abcdef` 两个目录 —— ① mtime 设为**刚刚** ⇒ `sweepSkillImports({ mode: ''stale'' })` 的 `removed === 0` 且两个目录**仍在**（这条就是 WR-05 的回归护栏）；② `fs.utimesSync` 把 mtime 设为 `2 * IMPORT_TTL_MS + 60s` 之前 ⇒ `removed === 2` 且两个目录**已删**。
       - **命名形状成对不变式（正命题）**：断言 `fs.mkdtempSync(path.join(getTmpDir(), ''skill-import-''))` 的**真实产物 basename** 满足 `isImportResidueName(...) === true`；再断言一个真实走覆盖路径产生的备份目录名也满足（用 `readdirSync` 找出 `skill-replace-` 开头的那个）—— 这样「前缀改了但正则没跟着改」会转红。
       - **非残留不误删**：在 `.tmp/` 下造一个 `tmp-something-xyz`（`createTempDir` 的产物形态）与一个普通文件 ⇒ `mode: ''stale''` 不得删它们。
  </action>
  <verify>
    <automated>node --check ai-manager.js && node --check ai-skills-manager.js && node --check main.js && node -e 'const fs=require("fs");const stripC=(x)=>{let o="",i=0,s=0;const N=x.length;while(i<N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s>=3&&s<=5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;continue}if(c==="\x22"){o+=c;s=4;i++;continue}if(c==="\x60"){o+=c;s=5;i++;continue}o+=c;i++}return o};const bad=[];const am=stripC(fs.readFileSync("ai-manager.js","utf8"));for(const t of ["async cancelSkillImport(","async sweepSkillImports(","IMPORT_TTL_MS","MAX_PENDING_IMPORTS","IMPORT_EXPIRED","IMPORT_NOT_FOUND","TOO_MANY_PENDING"])if(am.indexOf(t)===-1)bad.push("ai-manager.js 缺 "+t);const mgr=fs.readFileSync("ai-skills-manager.js","utf8");for(const t of ["IMPORT_TMP_PREFIXES","function isImportResidueName("])if(mgr.indexOf(t)===-1)bad.push("ai-skills-manager.js 缺 "+t);const sw=am.indexOf("async sweepSkillImports(");const swb=am.slice(sw,am.indexOf("\n  }",sw));if(swb.indexOf("mtimeMs")===-1)bad.push("sweepSkillImports 缺陈旧性判据（mtimeMs）—— 这正是 WR-05 的缺陷形态");if(swb.indexOf("2 *")===-1&&swb.indexOf("* 2")===-1)bad.push("陈旧性判据未用 2 × TTL 的余量");if(am.indexOf("mode === \x27own\x27")===-1)bad.push("缺 mode===\x27own\x27 分支（退出时只删本进程登记项）");const main=fs.readFileSync("main.js","utf8");const n=(main.match(/sweepSkillImports\(/g)||[]).length;if(n!==2)bad.push("main.js 的 sweepSkillImports( 应恰 2 处（启动 stale + 退出 own），实测 "+n);const sap=stripC(fs.readFileSync("ai-manager.js","utf8"));const gs=sap.indexOf("async syncAgentSystemPrompt()");const ge=sap.indexOf("\n  }",gs);if(sap.slice(gs,ge).indexOf("sweepSkillImports")!==-1)bad.push("清扫被接进 syncAgentSystemPrompt()（该函数体逐字不得改）");if(bad.length){console.error("生命周期接线不合格:\n"+bad.join("\n"));process.exit(1)}console.log("生命周期接线 ok（TTL/并发/取消/两种清扫模式/mtime 陈旧性/启动与退出各 1 处）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `生命周期接线 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 72（上一任务的 ≥ 60 + 本任务新增 ≥ 12），或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/test-skills-import.js","utf8");const bad=[];for(const s of ["IMPORT_EXPIRED","IMPORT_NOT_FOUND","TOO_MANY_PENDING","cancelSkillImport","utimesSync","removed","isImportResidueName"])if(t.indexOf(s)===-1)bad.push("用例缺 token: "+s);if(bad.length){console.error("生命周期用例不足:\n"+bad.join("\n"));process.exit(1)}console.log("生命周期用例 ok（TTL/未知句柄/并发/取消幂等/清扫陈旧性正反/命名成对）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `生命周期用例 ok`，或 stderr 列出任意一条缺失 token</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`tests/test-skills-import.js` 的 `# fail 0` 且 `# tests` ≥ 72。
    - **WR-05 的回归护栏可失败**：把 `sweepSkillImports` 的 `mtimeMs` 陈旧性判据删掉 ⇒ 「mtime 刚刚 ⇒ 不删」那条用例**必须转红**（执行者跑一次并记进 SUMMARY）—— 这是本计划**唯一**针对历史缺陷的护栏，不得只写不验。
    - **命名形状成对不变式是正命题**：`isImportResidueName` 对**真实产物**断言为 `true`（`mkdtempSync` 的产物 + 真实覆盖路径产生的备份目录名各一例）；只写「不匹配非残留」的否命题不足以发现「前缀改了正则没跟着改」。
    - **用例不依赖真实等待**：TTL 用例通过回拨 `createdAt` / `utimesSync` 实现，套件总时长不因 TTL（10 分钟）或并发闸（3）而显著增长（`node tests/test-skills-import.js` 应在数十秒内跑完 —— 若超时说明用到了 `sleep`，须改回拨形态）。
    - `syncAgentSystemPrompt()` 函数体**逐字未改**（门禁已断言清扫未接进去；同一基线 sha 下该区间零行变化）。
    - `main.js` 只有两处 `sweepSkillImports(`（启动 + 退出），且启动那处带 `.catch`/`try` 保护（清扫失败不得阻塞启动）。
  </acceptance_criteria>
  <done>`importId` 具备完整生命周期：过期给明确码与文案、未知句柄、并发上限、取消幂等、退出删本进程登记项、启动清陈旧残留（带 2 × TTL 的陈旧性判据，不重复 WR-05）；失败路径保留临时目录以支持原地重试。</done>
</task>

<task type="auto">
  <name>Task 3: 拒绝面矩阵收口 —— 每个 IMPORT_SKILL_ERROR 码至少一例 + 限额文案含名与值</name>
  <files>ai-skills-manager.js, ai-manager.js, tests/test-skills-import.js</files>
  <read_first>
    - `ai-skills-manager.js`：`IMPORT_SKILL_ERROR` 的 20 键定义（+ 本计划 Task 1 补的 `INVALID_NAME` = 21）与每一处 `throw makeImportError(` 的调用点（**逐条盘点**：哪个码在哪个阶段被抛）、`IMPORT_LIMITS` 的八键、`validateManagedSkillName` 的**全部**拒绝原因串（改名与导入共用 ⇒ 它的每条 reason 都必须在导入路径上真实可达）
    - `ai-manager.js`：`previewSkillImport` / `commitSkillImport` / `cancelSkillImport` 的全部抛出点
    - `tests/test-skills-import.js`：`51-03` 与 Task 1/2 的既有用例（**先盘点已有覆盖** —— 本任务只补缺口，不重复写等价用例）
    - `.planning/phases/51-zip/51-VALIDATION.md`：`## Per-Task Verification Map` 的 USER-08 行（「每条拒绝路径给定码 + 可读原因（含「命中哪个限额 / 当前值」）；**判据矩阵：每个错误码至少一例**」）
    - `.planning/phases/51-zip/51-CONTEXT.md`：D-11（限额文案必须说明「哪个限额 / 当前值是多少」）、D-10（导入报告结构：成功 `{ name, source, bytes, files, scripts[], scan, warnings[], allowedTools? }` / 失败 `{ code, error, quota?, diagnostics[] }`）
  </read_first>
  <action>
    1. **建立「码 → 用例」的可机械核对矩阵**（不靠人工数）：
       - 在 `tests/test-skills-import.js` 顶部建立一张**期望表** `const EXPECTED_CODES = [...]`，**逐字列出 `IMPORT_SKILL_ERROR` 的全部值**（从被测模块 `require` 进来的对象取值，而不是手抄字符串 —— 手抄会与实现漂移）。
       - 新增一条**矩阵用例**：遍历 `IMPORT_SKILL_ERROR` 的每一个值，断言「至少一条其它用例的失败信息里出现过该码」（实现方式：把每条拒绝用例的断言统一走一个小工具 `assertRejected(promiseOrFn, expectedCode)`，该工具把**已观察到的码**记进模块级 `Set`；矩阵用例在文件**末尾**断言 `EXPECTED_CODES` 的每一项都在该 Set 里，缺哪一项就指名报错）。
       - ⚠️ 允许**显式豁免**极少数在本计划范围内不可达的码（如 `UNSUPPORTED_URL` / `DOWNLOAD_FAILED` / `REDIRECT_LIMIT` / `NOT_A_ZIP` 属网络面，`51-05` 交付）—— 豁免必须写成一张**具名清单** `const PENDING_CODES_NETWORK = [...]` 并在矩阵用例里**显式跳过 + 打印跳过项**（**不得静默过滤**）。**清空这张清单的是 `51-05` Task 3 第 6 步**（网络面四码在 `51-05` 全部有真实用例 ⇒ 由 `51-05` 从 `tests/test-skills-import.js` 里移除它们），`51-05` T3 的门禁**自带**「零跳过项」断言；`51-06` / `51-07` 都**不**承这条断言。
    2. **补齐缺口的拒绝用例**（按 Task 1/2 后的实际覆盖缺口逐条补，至少覆盖）：`INVALID_ZIP`（非 zip 字节 / 截断的 central directory）、`UNSUPPORTED_ZIP64`、`UNDECODABLE_ENTRY`、`UNSAFE_ENTRY`（三类子形态各一：symlink / 非普通文件 / 逃逸族各一 —— 已有则复用）、`LIMIT_EXCEEDED`（**六类限额各一，且逐条断言 message 同时含「限额名」与「当前值」**）、`SKILL_ROOT_COUNT`（0 个 / 多于 1 个各一）、`FRONTMATTER_INVALID`（YAML 语法错 / 顶层数组 / 缺 description / name 缺失且目录名非法 —— 四形态）、`OVERSIZE`、`INJECTION_DETECTED`（硬拒，与 `51-03` T3 的启发式高亮形成对照）、`SEEDED_CONFLICT`、`CONFLICT_UNRESOLVED`、`INVALID_NAME`、`IMPORT_EXPIRED`、`IMPORT_NOT_FOUND`、`TOO_MANY_PENDING`、`READBACK_FAILED`、`UNKNOWN`（造一个沙箱层失败：把 `skills/` 目录 chmod 成不可写 / 传一个失败的 `env` 替身，按可复现性优先选前者并在用例末尾复原权限）。
    3. **失败形态与报告结构对齐 D-10**：断言失败对象的形状是 `{ code, error, quota?, diagnostics[] }` 一类（`error` 非空字符串、`code` 非空、限额类失败 `quota` 带限额名与当前值）；断言**不存在**「`error` 为空字符串或仅为通用文案」的路径（矩阵用例里对每条失败断言 `typeof message === ''string'' && message.trim().length > 0`）。
    4. **`ai-manager.js` 的错误透传**：确保 `previewSkillImport` / `commitSkillImport` / `cancelSkillImport` 抛出的错误对象**原样带 `code`**（`makeImportError` 已把 `code` 挂在 `err.code` 上；`main.js` 的既有两个 `catch` 分支已 `code: err.code || undefined` —— 本任务**只需复核**，不改 `main.js`）。
  </action>
  <verify>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `# tests` 计数低于 90（上一任务的 ≥ 72 + 本任务新增 ≥ 18），或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/test-skills-import.js","utf8");const bad=[];if(t.indexOf("assertRejected(")===-1&&t.indexOf("function assertRejected")===-1)bad.push("缺 assertRejected 统一拒绝断言工具（否则「每码至少一例」无法机械核对）");if(t.indexOf("EXPECTED_CODES")===-1)bad.push("缺 EXPECTED_CODES 期望表");if(t.indexOf("PENDING_CODES_NETWORK")===-1)bad.push("缺网络面待覆盖码的具名豁免清单（不得静默过滤）");if(t.indexOf("quota")===-1)bad.push("限额类失败未断言 quota（限额名 + 当前值）");const mgr=fs.readFileSync("ai-skills-manager.js","utf8");const i=mgr.indexOf("const IMPORT_SKILL_ERROR = Object.freeze({");const j=mgr.indexOf("});",i);if(i===-1)bad.push("缺 IMPORT_SKILL_ERROR 定义");else{const vals=(mgr.slice(i,j).match(/\x27[a-z_]+\x27/g)||[]);if(vals.length<19)bad.push("IMPORT_SKILL_ERROR 值数 "+vals.length+"（应 ≥ 19）")}if(bad.length){console.error("拒绝面矩阵不合格:\n"+bad.join("\n"));process.exit(1)}console.log("拒绝面矩阵 ok（统一断言工具 + 期望表 + 具名豁免 + quota 断言）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `拒绝面矩阵 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-import.js 2>&1 | grep -E "PENDING_CODES|跳过的码" | head -5</automated>
    <fails_when>该行**必须存在且列出跳过项**（若为空 ⇒ 豁免机制未生效，矩阵用例在静默过滤）；跳过项集合必须**恰为**网络面四码（`unsupported_url` / `download_failed` / `redirect_limit` / `not_a_zip`），多一个或少一个都说明盘点有漏</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条门禁全绿；`# tests` ≥ 90 且 `# fail 0`。
    - **矩阵是机械判据**：删掉任意一条拒绝用例 ⇒ 矩阵用例指名报出缺失的码（执行者至少对 `OVERSIZE` 做一次「注释掉该用例 ⇒ 转红」的对照并记进 SUMMARY）。
    - **限额文案六类逐条含「名 + 值」**：`LIMIT_EXCEEDED` 的六个形态各自的 message 同时含限额名（单 entry / 累计 / 条目数 / 压缩比 / 嵌套深度 / `SKILL.md` 字节）与当前值 —— 用一条循环用例断言，而不是六条独立断言（改一处限额名会让它对不上用例里记录的期望名）。
    - **豁免清单是过渡态且明示**：网络面四码在 `PENDING_CODES_NETWORK` 里具名跳过并打印；本任务结束时该清单**非空**（四个码在 `51-04` 范围内确实不可达）。**清空它的时点与责任在 `51-05` Task 3 第 6 步**（四个码在 `51-05` 全部有用例 ⇒ 由 `51-05` 从清单里移除，且 `51-05` T3 的门禁自带「不得出现任何跳过项」的断言）；`51-06` 与 `51-07` **都不**承这条断言 —— 本计划的交付物里也**不得**声称「`51-06`/`51-07` 的收口会断言它为空」。
    - `MANAGE_SKILL_ERROR` 仍恰 11 键、`tests/test-manage-skill.js` 55 例全绿（本任务不得为了复用而改那张表）。
  </acceptance_criteria>
  <done>`IMPORT_SKILL_ERROR` 的每个在本阶段可达的码都有至少一条用例，且「缺了哪个码」由一条机械矩阵用例指名；限额类失败文案逐条含「哪个限额 + 当前值」；失败报告形状与 D-10 一致；网络面四码的豁免是具名且可见的过渡态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端（`realm://` guest）→ commit 端点 | 只允许传 `{ importId, conflict, newName }`；**绝不接受路径**，`importId` 是不透明句柄 |
| 进程内状态（`_skillImports` Map）→ 临时区目录 | Map 是唯一权威；磁盘上的残留是不可信的（崩溃 / 并发实例） |
| 覆盖动作 → 用户已有的技能目录 | 被替换的是**用户自己的**目录（可能手改过），这是本阶段唯一的破坏性写操作 |
| 并发实例（`dev` / `debug` 共享 userData） | 各自持有独立的 Map，但共享同一个 `.tmp/` 与 `skills/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-23 | Tampering | 静默覆盖用户技能（同名时默认覆盖） | high | mitigate | `resolveImportConflict` 三档判定 + `conflict` 缺省时抛 `CONFLICT_UNRESOLVED`（不静默覆盖）+ 六条冲突矩阵用例（含「未给 conflict ⇒ 显式失败」） |
| T-51-24 | Repudiation / Tampering | 覆盖中途失败导致旧技能丢失（「先删后移」） | high | mitigate | 备份 + 两段 `rename` + 第二步失败回滚（实测配方 C1/C2/C3）；回滚用例先读旧内容副本再逐字比对 |
| T-51-25 | Tampering | 覆盖 managed 技能（用户无法从导入 UI 恢复） | medium | mitigate | 第三档判定**不允许覆盖** + 「永久遮蔽」提示 + 只允许改名 / 取消；用例断言 managed + `overwrite` 被拒 |
| T-51-26 | Denial of Service | 并发实例互删进行中的预览（`WR-05` 的历史缺陷形态） | high | mitigate | 两种清扫模式分离（`own` 只删本进程登记项 / `stale` 只删 mtime 早于 2 × TTL 的残留）+ 陈旧性正反两例用例 |
| T-51-27 | Repudiation | `importId` 被重放或长期悬挂（临时目录堆积） | medium | mitigate | 一次性句柄（提交第二次 ⇒ `IMPORT_NOT_FOUND`）+ TTL + 并发上限 + 成功 / 取消 / 过期三条路径都删目录与 Map 项 |
| T-51-28 | Tampering | 失败路径静默（`error` 为空 / 只有通用文案 / 限额不说哪个限额） | high | mitigate | 21 键码表（含本计划补的 `INVALID_NAME`）+ `assertRejected` 统一断言（每条失败必须有非空 message）+ 限额类循环断言「名 + 当前值」+ 机械矩阵用例（缺码即指名） |
| T-51-29 | Elevation of Privilege | 用 Node `fs` 绕过沙箱写面判据实现覆盖 | high | mitigate | 覆盖三步全用 `env.renameFile` / `env.remove`；门禁断言 `importUserSkill` 内出现 `fs.renameSync` / `fs.rm` 即报错 |
| T-51-30 | Denial of Service | 数量闸被覆盖路径绕过（无限增长）或改名路径不计数 | medium | mitigate | 「覆盖豁免 / 改名计入」两条 + 读盘判据（改完磁盘立即生效，不依赖重扫） |
| T-51-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本计划**零安装动作**（依赖由 `51-02` 落地并已过 blocking-human 合法性闸） |
</threat_model>

<verification>
- 三个任务的 automated 门禁绿；`tests/test-skills-import.js` 的 `# tests` ≥ 90 且 `# fail 0`；`tests/test-manage-skill.js` 55 例零删改。
- **回归判据（跨计划）**：`importUserSkill(` 全仓仍恰 2 处（定义 + 调用）—— 本计划只扩分支、不新增落盘实现；`yauzl.openPromise(` 仍恰 1 处。
- **本计划不覆盖**（不得声称已交付）：网络地址来源（51-05）、完整 UI（51-06）、文档与账本（51-07）。
- 人工面：`import_expired` 的真实 10 分钟等待（`51-VALIDATION.md` 未列此项 —— TTL 由回拨 `createdAt` 的用例覆盖，不做真实等待的 UAT）。
- 诚实边界：覆盖的备份**不是**版本历史（成功后即刻删除）；`51-07` 的文档必须如实写明这一点，避免被读成「既然备份了就能恢复」。
</verification>

<success_criteria>
- 同名冲突按三档处置，三选一各自可达且**不做静默覆盖**；改名走同一份校验器。
- 覆盖走「备份 + 两段 rename」，第二步失败旧技能逐字完好；回读验证失败即回滚并暴露 `diagnostics` 原文。
- `importId` 的 TTL / 并发 / 未知 / 取消四态都有明确码与可读原因；崩溃残留清扫带陈旧性判据（正反两例）。
- 每个本阶段可达的 `IMPORT_SKILL_ERROR` 码至少一条用例，缺码由机械矩阵指名；限额文案逐条含「哪个限额 + 当前值」。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-04-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-04）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 导出函数 | `resolveImportConflict(env, name, { seededNames })`、`countUserSkills(env, skillsDir)` | `ai-skills-manager.js` |
| 内部函数 | `userSkillPaths(name)`、`isImportResidueName(name)` | `ai-skills-manager.js` |
| 模块常量（导出） | `IMPORT_TMP_PREFIXES`（`['skill-import-', 'skill-replace-']`） | `ai-skills-manager.js` |
| 码表加码 | `IMPORT_SKILL_ERROR.INVALID_NAME`（**本计划唯一的新增码**，须同批刷新 `51-07` 的两处账本） | `ai-skills-manager.js` |
| 类方法 | `AIManager.prototype.cancelSkillImport({ importId })`、`AIManager.prototype.sweepSkillImports({ mode })` | `ai-manager.js` |
| 接线 | 启动 `sweepSkillImports({ mode: 'stale' })`、`before-quit` 的 `sweepSkillImports({ mode: 'own' })` | `main.js` |
| 测试夹具工具 | `assertRejected(...)`、`EXPECTED_CODES`、`PENDING_CODES_NETWORK` | `tests/test-skills-import.js` |
| 目录形状 | `.tmp/skill-replace-<随机>/`（覆盖的临时备份，成功后即刻删除） | 运行期 `.tmp/` |

### 本阶段其余计划将新建（此处仅登记，避免在评审时被当作「应已存在」）

| 计划 | 符号 |
|------|------|
| 51-05 | `HOST_WHITELIST`、`classifyImportUrl(url)`、`downloadPackage(url, { maxBytes })`、`MAX_REDIRECTS`、`isWhitelistedHost(hostname)`、`tests/test-skills-import-net.js` |
| 51-06 | `.skill-import-*` CSS 段（含 3 条作用域覆盖）、导入弹框的完整 DOM（`#skillImportPanelLocal` / `#skillImportPanelUrl` / `#skillImportAck` / `#skillImportConflict` / `#skillImportGate` / `#skillImportCancel` / `#skillImportConfirm` / `#skillImportFetch` / `#skillImportPick`）、`SKILL_IMPORT_ERROR_TEXT`（闭合白名单表）、`renderSkillImportScan()` / `renderSkillImportTree()` / `renderSkillImportConflict()`、`tests/uat-51-import-modal.js` |
| 51-07 | `docs/product/ai-skills.md` §十三【导入】+ 安全边界定稿、`docs/product/ai-agent-workspace.md` §七 的 SEC-10 行为变更条目、`AGENTS.md` 导入维护约定与测试行、counts-parity 的 `suites` / `cells` 扩展（12 → 20） |
