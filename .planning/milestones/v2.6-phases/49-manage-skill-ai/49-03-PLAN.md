---
phase: 49-manage-skill-ai
plan: 03
type: execute
wave: 3
depends_on: [49-02]
files_modified:
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/phases/49-manage-skill-ai/49-VALIDATION.md
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: medium

key-decisions:
  - '本计划**不改任何源码与测试**：只做文档收口与账本同步。理由：AGENTS.md 的维护约定把【本功能的产品说明权威文档】与【测试清单例数】绑成了同一份契约，而 49-01 / 49-02 的改动面（校验器 / 三动作 / 限额 / 扫描口径 / 拒绝面 / 卡片形态）必须在一处成文，否则下一阶段（50/51）只能靠读代码反推口径'
  - '章节编号取 §十一（现有十节：一能力 / 二双目录 / 三优先级 / 四限额 / 五沙箱边界 / 六已知限制 / 七测试与验证 / 八内置技能 / 九bash 安装档 / 十发现与调用），**新增独立一级小节**而不是塞进 §十：本阶段的能力面（AI 自建技能）与调用面（发现与调用）在概念上并列，且 §十 已经很长（10.1–10.8）。同时 §十的 10.5 三档来源徽标 / 10.6 read 卡片技能化 / 10.7 诚实边界三处需要**补一行交叉指引**指向 §十一（不重复正文）'
  - '诚实边界必须成文（AGENTS.md 的既有口径：bash 白名单与 allowed-tools 都已成文）：本阶段要写进 §十一的三条边界是 —— ① **bash 可绕过 name 格式闸**（AI 经 bash 能造出目录名不合 ^[a-z0-9-]+$ 的技能，它会进 system prompt 但**无法被 /skill:<name> 显式调用**，因为 src/skill-picker-model.js 的 SKILL_NAME_RE 是跨进程单源解析器，不合格式直接返回 null 走【未知命令】）；② **双写路径是设计而非缺陷**（manage_skill 负责技能集级声明式变更，edit / write / bash 负责文件级微调；沙箱层保护技能目录在结构上不可能完整，因为 exec 不校验命令内容）；③ **两个上限闸职责不同**（数量闸管磁盘与重扫成本、prompt 段预算闸管请求成本，二者不互相替代，且不得用后者做创建拒绝）'
  - '测试清单例数一律取 49-01 / 49-02 的**实测输出**（`node tests/<file>.js` 的 `# tests` 值），不按估算填。两处账本（AGENTS.md:267 的 `- 测试：` 行、docs §七）在**同一个任务**里刷，避免两处漂移'
  - '`49-VALIDATION.md` 的 Per-Task Verification Map 在本计划回填：把创建时按需求维度键控的行改为按**已交付的计划 / 任务**键控（`49-01-T1..T3` / `49-02-T1..T3` / `49-03-T1..T3`），并把 `wave_0_complete` 置真、把 `Known-Open Items` 的四个 OQ 逐条写裁决（OQ-1 方案 A / OQ-2 加 LIMITS 第四项 / OQ-3 新建 tests/test-manage-skill.js + test-ai-skills.js 补接线 / OQ-4 静默剥除）'
  - '本计划**不**改 `.planning/STATE.md`（GSD 自身的 state 台账由 plan-phase 的 13b 与 verify-work 维护，手工改会与工具写入竞争）；`STATE.md:246` 的 ⚠️（syncAgentSystemPrompt 无生产调用方）在本阶段完成后应记为 **1/3 完成**，该记账由 SUMMARY 显式披露、由后续 review / verify 阶段落到 STATE.md —— 本计划只在 SUMMARY 里给出建议措辞'
  - '本计划**不**改 `.planning/phases/49-manage-skill-ai/49-UAT.md`（该文件由 /gsd-verify-work 的探针产出 / 回填）；本计划只在 §十一 与 `AGENTS.md` 里写清【哪些面只能人工观察】，并把可跑的命令写进 §七'
  - '测试禁止引用 `npm test`（本项目 package.json 无该脚本）：§七与 AGENTS.md 的测试行一律写 `node tests/<file>.js` / `node --test tests/<file>.js` 的显式形式'

must_haves:
  truths:
    - '`docs/product/ai-skills.md` 新增 §十一【AI 自建技能】，覆盖：三动作语义（create 独占创建 / update 全量覆写正文 / delete 递归删整目录）、工具只接受 name 与 content / description 而**不接受 path**（接口设计即边界）、拒绝面（seeded_protected / user_owned_conflict / already_exists / not_found / limit_exceeded / invalid_name / invalid_description / oversize / unscannable 九码，含各自的触发条件）、两个上限闸的职责分工、扫描与净化口径（description 两组 / content 一组；**先扫描后净化**）、三条诚实边界、改名不支持（= delete + create）、【下一条消息起可用】的时序语义'
    - '§十一 的每条边界都能对应到代码事实（写清 module:line 或符号名），且**不含**任何【已修 / 已闭合】的越界声明：TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-06 与 `STATE.md:246` 的 ⚠️ 一律保持挂账，本阶段只声明完成 1/3'
    - '§十 的 10.5 / 10.6 / 10.7 三处各补一行交叉指引指向 §十一（不复制正文），保证【同一份口径只在一处成文】'
    - '`AGENTS.md` 的 §AI 工作区与 Bash 权限 新增一条 `manage_skill`（AI 自建技能）维护约定：产品说明权威 = `docs/product/ai-skills.md` §十一；改动校验器 / 三动作 / 限额常量 / 扫描与净化口径 / 拒绝面 / 卡片形态时**必须同步该章节与该节测试清单**；并明记四条硬约束（校验器住 `ai-skills-manager.js` 且零 electron 依赖 ⇒ 50/51 复用同一份；seeded 判定按播种登记表而非目录位置；`LIMITS` 只允许加项不得改既有三项数值；三动作一律不加确认卡片、`SKILL_THREAT_PATTERNS` 归 Phase 51）'
    - '`AGENTS.md:267` 的 `- 测试：` 行与 `docs/product/ai-skills.md` §七 的例数**同时**刷为 49-01 / 49-02 的实测值；两处的覆盖面描述各补本阶段新增面（manage_skill 三动作与校验器 / 卡片标记两时点与终态元数据通道 / 幽灵技能与越界护栏）'
    - '`49-VALIDATION.md` 的 Per-Task Verification Map 按已交付的计划 / 任务重键（列数与表头一致），`Status` 保持 `⬜ pending`；frontmatter 的 `wave_0_complete` 置 `true`（Wave 0 交付已在 49-01 / 49-02 落地）；`Known-Open Items` 的四个 OQ 逐条写裁决与落点；`nyquist_compliant` **保持 false**（该字段由 `/gsd-validate-phase` 判定，本计划不自我认证）'
    - '[assumption] 文档中的示例命令必须可直接复制执行（`node tests/test-manage-skill.js` 等），不得出现 `npm test` 或需要先手工构造环境才能跑的命令 —— 可跑性由 §七 的命令在 49-01 / 49-02 已实测通过的同一批命令承担'
  artifacts:
    - 'docs/product/ai-skills.md：新增 §十一【AI 自建技能】（含三动作 / 拒绝面九码 / 两个上限闸职责 / 扫描与净化口径 / 三条诚实边界 / 改名不支持 / 时序语义）+ §十 三处交叉指引 + §七 例数与覆盖面刷新'
    - 'AGENTS.md：§AI 工作区与 Bash 权限 新增 manage_skill 维护约定条目；`:267` 的测试清单行刷新（新增 test-manage-skill.js 条目 + 两处例数）'
    - '.planning/phases/49-manage-skill-ai/49-VALIDATION.md：Per-Task Verification Map 重键 + `wave_0_complete: true` + Known-Open Items 四条裁决'
  key_links:
    - '维护约定链：`AGENTS.md` 的 manage_skill 条目 → 指向 `docs/product/ai-skills.md` §十一 → 指向 `ai-skills-manager.js` 的校验器与三动作 / `ai-manager.js` 的工具项与标记 / `src/skill-picker-model.js` 的白名单表 / `src/renderer.js` 的卡片分支。改任一处 → 同步 §十一 → 同步 AGENTS.md 测试清单'
    - '测试账本链：`node tests/test-manage-skill.js` 的 `# tests` 值 → `AGENTS.md:267` 的新条目 + `docs §七` → 与 `node tests/test-ai-skills.js` 的实测值一起构成同一次实跑的两处文本（两处必须相等，避免漂移）'
    - '回归护栏：`tests/test-builtin-skills-seeder.js` 的【打包排除项配置护栏】与【AGENTS.md 测试行计数】断言 —— 后者在 Node 22 下**基线即红**（D-48-A，与本阶段无关）；本计划改 `AGENTS.md:267` 时**不得**试图去修该测试，也不要让它变得更红（新条目不得写成会被该断言误解析的形态，必要时在 SUMMARY 记明该既有红点）'
  prohibitions:
    - statement: '不得在文档里声称本阶段已闭合 TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-06，也不得声称 `syncAgentSystemPrompt()` 的生产调用方已完整收口'
      status: unresolved
      verification: judgment
      reason: '用户已裁决 TD-48-01 / TD-48-02 延后且不在 49 需求范围内；WR-02 / WR-06 逐字保持挂账；`STATE.md:246` 的 ⚠️ 只在 49/50/51 三者都落地后才算闭合，49 只完成 1/3。文档误声明会让后续阶段据此跳过工作，是**不可逆的账本污染**'
    - statement: '不得在 §十一 里写入实现细节级别的代码片段（函数签名、行号清单、正则字面量），只写行为契约与指向'
      status: unresolved
      verification: judgment
      reason: '产品说明与实现台账是两类文档：前者给使用者与后续阶段读行为，后者是代码本身。§十一 若复制实现细节，会与代码形成第二份需要同步的真相（AGENTS.md 的既有纪律：产品说明改动必须与行为一致，而不是与某几行代码一致）'
---

<objective>
收口 Phase 49 的**文档与账本**：把「AI 自建技能」这件事从代码事实提升为一份可复用的产品说明（`docs/product/ai-skills.md` §十一），把维护约定与测试清单同步进 `AGENTS.md`，并把 `49-VALIDATION.md` 的验证矩阵按已交付的计划 / 任务重键、把四个 plan 期裁决（OQ-1..OQ-4）落成文字。

本计划是三个计划中的第三个，**不改任何源码与测试**。它的价值在于：Phase 50（设置页技能管理区）与 Phase 51（导入管线 + 技能域威胁模式组）**必须复用**本阶段产出的校验器与口径 —— 那两份工作只能通过 §十一 与 `AGENTS.md` 的维护约定读到【口径是什么、边界在哪里、哪些面故意不做】，否则它们会重新推导一遍并写出第二份实现（这正是本阶段排在 50/51 之前的全部理由）。

**关于 tracer：本计划刻意没有 `type="tracer"` 任务。** TRACER_MODE 的意图是让每个计划先打通一条**最短的端到端制品路径**再扩散；本计划的制品本身就是端到端的文档契约（口径 → 维护约定 → 测试账本三处互相指向），没有可纵切的运行时代码路径。强行加一个 tracer 任务只会把 Task 1 拆成两半（先写一节再补其余），反而让【口径一次成文】这件事变难。

Purpose: 让【49 产出的校验器是 50/51 唯一可复用的那一份】这条 ROADMAP 安全门禁在**文档层**也成立 —— 口径在 §十一 成文、约定在 `AGENTS.md` 挂硬、测试账本在两处同值。

Output: `docs/product/ai-skills.md` 的 §十一 与 §十 / §七 的增量；`AGENTS.md` 的维护约定条目与测试清单刷新；`49-VALIDATION.md` 的重键矩阵与四条 OQ 裁决。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-manage-skill-ai/49-CONTEXT.md
@.planning/phases/49-manage-skill-ai/49-RESEARCH.md
@.planning/phases/49-manage-skill-ai/49-UI-SPEC.md
@.planning/phases/49-manage-skill-ai/49-VALIDATION.md
@.planning/phases/49-manage-skill-ai/49-01-PLAN.md
@.planning/phases/49-manage-skill-ai/49-02-PLAN.md
@docs/product/ai-skills.md
@docs/product/ai-agent-workspace.md
@AGENTS.md
@ai-skills-manager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/product/ai-skills.md` 新增 §十一【AI 自建技能】+ §十 三处交叉指引 + §七 例数与覆盖面</name>
  <files>docs/product/ai-skills.md</files>
  <read_first>
    - `docs/product/ai-skills.md`：`:32-105`（§一 能力 / §二 双目录 / §三 优先级 / §四 限额 / §五 沙箱边界 / §六 已知限制 / §七 测试与验证 全段 —— 新章节的**口径必须与它们不矛盾**，尤其是 §二 的双目录语义与 §四 的限额表）、`:252-374`（§十 全段，特别是 10.3 实时读盘口径 / 10.4 边界技能行为表 / 10.5 三档来源徽标 / 10.6 `read` 卡片技能化 / 10.7 诚实边界（含 bash 白名单与 allowed-tools 的成文口径）—— 新章节的三条诚实边界要与它**同一口径**）、`:96-105`（§七 那一行的完整文本与【（N 例，实测）】标注形态）
    - `ai-skills-manager.js`：49-01 落地的 `LIMITS`、六个校验器、`atomicWriteSkillFile`、`resolveManagedTarget`、三个动作函数与 `getSkillPromptIncluded`（写文档时逐条对照行为，不抄实现）
    - `ai-manager.js`：49-01 / 49-02 落地的 `_buildManageSkillTool()`（三动作 / 无 path / sequential / 两条描述文案）、`_resolveManageSkillMarker` / `_resolveManageSkillTerminal` / `_buildManageSkillDecoration`、`getConversationMessages` 的 manage_skill 重建
    - `src/skill-picker-model.js`：`MANAGE_SKILL_ACTION_LABEL` / `MANAGE_SKILL_SHORT_REASON`（九码短原因 —— 文档需给出九码的可读解释而非短原因字面量）
    - `.planning/phases/49-manage-skill-ai/49-CONTEXT.md`：D-05（三条诚实边界与【不新增拦截机制】的理由）、D-10（两个上限闸职责分工）、D-13（时序语义与 1/3 收口）、Claude's Discretion 的【改名不支持】、`<deferred>`（不得提前实现的面）
    - `docs/product/ai-agent-workspace.md`（同族产品文档的章节组织与措辞风格 —— 新章节照它的形态）
  </read_first>
  <action>
    1. 在文件末尾（§十 之后）新增 `## 十一、AI 自建技能（manage_skill）` 一级章节，用与既有章节同款的【行为契约 + 指向】写法（**不写函数签名、不写行号、不写正则字面量**）。至少包含下列小节：
       - **11.1 三动作**：`create`（**独占创建**：目标已存在即拒绝且不落盘）/ `update`（**全量覆写正文**，content 必填且不含 frontmatter；局部增强走【先 read 再全量写】或直接用既有 `edit` 工具）/ `delete`（递归删整目录，含 AI 经 bash 加的 `scripts/` / `references/`）。写明**改名不支持**（name = 目录名 = 唯一权威；要改名 = `delete` + `create`）。
       - **11.2 工具不接受 path**：只接受 `name` 与 `content` / `description`；路径由管理器用 `path.join` 计算。写明这条是**接口设计即边界**（沙箱在这里提供不了保护）。
       - **11.3 九条拒绝原因**：逐条给出可读解释与触发条件（以 `MANAGE_SKILL_SHORT_REASON` 的键为准，但正文写清语义，不照抄短原因字面量）。
       - **11.4 两个上限闸的职责分工**：数量闸（`MAX_MANAGED_SKILLS`，统计**非 seeded 的托管技能数**，读盘）管**磁盘与重扫成本**；prompt 段预算闸（`SKILLS_PROMPT_CHAR_BUDGET`）管**请求成本**。二者不互相替代，且**不得用后者做创建拒绝**（超预算的技能仍可经 `/skill:name` 显式调用）。
       - **11.5 内容扫描与净化**：`description` 跑注入 + 凭据两组（它无条件进每个请求的 system prompt）；`content` **只**跑注入组（技能文档合法地会写配置示例）；**顺序铁律：先扫描、后净化**；净化只作用于 `description`。写明命中的处置是**拒绝写入**（不落盘）。
       - **11.6 三条诚实边界**：① bash 可绕过 name 格式闸（能造出目录名不合 `^[a-z0-9-]+$` 的技能 → 进 system prompt 但**无法被 `/skill:name` 显式调用**）；② 双写路径是设计而非缺陷（`manage_skill` 管技能集级声明式变更，`edit` / `write` / `bash` 管文件级微调；沙箱层保护技能目录在结构上不可能完整，因为 exec 不校验命令内容 —— 不完整的保护是负面价值）；③ 上述扫描是**启发式、降低概率而非安全边界**（安全边界 = 确认卡片 + 硬沙箱）。
       - **11.7 可见性与时序**：成功后技能集与本轮**成功出口**回写 system prompt，**下一条消息起可用**（不是工具返回时立即生效）；工具卡片是**不可变历史记录**（不响应 `skills:changed` 广播）；卡片头部给出来源档位徽标与至多一条标注（失败短原因 / 未进提示词）。
       - **11.8 维护约定与测试**：指向 `AGENTS.md` 的 manage_skill 条目；列出本阶段新增的测试命令与覆盖面。
    2. §十 三处补交叉指引（**各一行，不复制正文**）：
       - 10.5 三档来源徽标末尾补一行：AI 自建技能的卡片同样查这张表（同一 `TIER_BADGE` 单源），详见 §十一。
       - 10.6 `read` 卡片技能化末尾补一行：`manage_skill` 的卡片是同一函数内的另一个分支，复用同一容器类与同一 `TIER_BADGE` 查表，详见 §十一。
       - 10.7 诚实边界末尾补一行：AI 自建技能另有三条边界（bash 绕过 name 格式 / 双写路径 / 扫描是启发式），详见 §十一。
    3. §七（`:96-105`）刷新：
       - `node tests/test-ai-skills.js` 的例数刷为 49-02 的**实测值**，覆盖面描述补【manage_skill 卡片标记两时点与终态元数据通道】；
       - 新增一行 `node tests/test-manage-skill.js`，例数取 49-01 的实测值，覆盖面描述写【manage_skill 三动作 / 校验器值域 / 四类撞名 / seeded 三入口 / 原子性与失败清理 / 字段分离扫描 / 扫描-净化顺序 / 数量闸 / 幽灵技能与越界护栏】；
       - 保持既有的【（N 例，实测）】标注形态；该节其余文字不动。
    4. 全文件不得出现把 TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-06 或 `STATE.md:246` 的 ⚠️ 说成【已修 / 已闭合】的表述；若 §十一 需要提及它们，一律写【保持挂账】与【本阶段完成 1/3】。
    5. **不要**改 §一～§六 与 §八 / §九（本阶段不改加载管线、不改目录语义、不改限额表中的既有三项、不改内置技能与安装档），除非发现与本阶段实现直接冲突的失实表述 —— 若确实发现，只改那一处并在 SUMMARY 记明理由。
  </action>
  <verify>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const bad=[];const i=d.indexOf("## 十一、");if(i&lt;0)bad.push("缺 §十一 章节");else{const sec=d.slice(i);for(const t of ["create","update","delete","独占创建","全量覆写","递归","path","seeded_protected","user_owned_conflict","already_exists","not_found","limit_exceeded","invalid_name","invalid_description","oversize","unscannable","MAX_MANAGED_SKILLS","SKILLS_PROMPT_CHAR_BUDGET","先扫描","后净化","bash","双写","启发式","下一条消息","改名"]){if(!sec.includes(t))bad.push("§十一 缺要点 "+t)}}for(const t of ["10.5","10.6","10.7"]){if(d.indexOf("详见 §十一")&lt;0&amp;&amp;d.indexOf("见 §十一")&lt;0)bad.push("§十 缺交叉指引")}if(!/test-manage-skill\.js/.test(d))bad.push("§七 未新增 test-manage-skill.js 条目");if(/npm test/.test(d))bad.push("文档引用了不存在的 npm test");if(bad.length){console.error("§十一 不合格:\n"+bad.join("\n"));process.exit(1)}console.log("§十一 要点齐备 + §十 交叉指引 + §七 新条目 ok")'</automated>
    <fails_when>非零退出，或出现 '§十一 不合格' 或任一条 `缺 §十一` / `§十一 缺要点` / `§十 缺交叉指引` / `§七 未新增` / `npm test` 行，或标准输出不含 "§十一 要点齐备 + §十 交叉指引 + §七 新条目 ok"</fails_when>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const i=d.indexOf("## 十一、");const sec=i&lt;0?"":d.slice(i);const bad=[];if(/TD-48-01[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))bad.push("文档声称 TD-48-01 已修");if(/TD-48-02[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))bad.push("文档声称 TD-48-02 已修");if(/WR-02[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))bad.push("文档声称 WR-02 已修");if(/WR-06[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))bad.push("文档声称 WR-06 已修");if(/P8[\s\S]{0,60}(全覆盖|已完整收口|6\/6)/.test(d))bad.push("文档声称 P8 失效链已完整收口（本阶段只完成 1/3）");const codes=["seeded_protected","user_owned_conflict","already_exists","not_found","limit_exceeded","invalid_name","invalid_description","oversize","unscannable"];const miss=codes.filter(c=&gt;!sec.includes(c));if(miss.length)bad.push("§十一 缺原因码 "+miss.join("/"));if(!/1\/3|三分之一/.test(d))bad.push("未声明 syncAgentSystemPrompt 的收口只完成 1/3");if(bad.length){console.error("诚实边界与九码不合格:\n"+bad.join("\n"));process.exit(1)}console.log("九码齐备 + 无越界声明 + 1/3 收口语义 ok")'</automated>
    <fails_when>非零退出，或出现 '诚实边界与九码不合格' 或任一条 `文档声称` / `缺原因码` / `未声明 syncAgentSystemPrompt` 行，或标准输出不含 "九码齐备 + 无越界声明 + 1/3 收口语义 ok"</fails_when>
    <automated>node -e 'const fs=require("fs");const d=fs.readFileSync("docs/product/ai-skills.md","utf8");const lines=d.split("\n");const bad=[];const m=lines.filter((l)=&gt;/^.*node (--test )?tests\/test-(ai-skills|manage-skill)\.js.*例/.test(l));if(m.length&lt;2)bad.push("§七 缺【（N 例，实测）】形式的条目（找到 "+m.length+" 条）");for(const l of m){const n=Number((l.match(/(\d+) 例/)||[])[1]||0);if(!(n&gt;0))bad.push("例数未填实测值: "+l.slice(0,80))}if(!/manage-skill/.test(m.join(" ")))bad.push("§七 缺 test-manage-skill.js 的实测例数行");if(bad.length){console.error("测试账本不合格:\n"+bad.join("\n"));process.exit(1)}console.log("§七 两处实测例数 ok（"+m.length+" 条）")'</automated>
    <fails_when>非零退出，或出现 '测试账本不合格' 或任一条 `§七 缺` / `例数未填` 行，或标准输出不含 "§七 两处实测例数 ok"</fails_when>
  </verify>
  <done>
    `docs/product/ai-skills.md` 新增 §十一（三动作 / 不接受 path / 九码拒绝面 / 两个上限闸职责 / 扫描与净化口径 / 三条诚实边界 / 可见性与时序 / 维护约定与测试），§十 的三处补一行交叉指引（不复制正文），§七 的 `test-ai-skills.js` 例数刷新并新增 `test-manage-skill.js` 条目（例数均为实测值）；全文件无越界声明（TD / WR / P8 一律保持挂账并声明 1/3）；不引用不存在的 `npm test`。
  </done>
</task>

<task type="auto">
  <name>Task 2: `AGENTS.md` 维护约定与测试清单同步 + `49-VALIDATION.md` 矩阵重键与四条 OQ 裁决</name>
  <files>AGENTS.md, .planning/phases/49-manage-skill-ai/49-VALIDATION.md</files>
  <read_first>
    - `AGENTS.md`：§AI 工作区与 Bash 权限 全段（`- agent 根目录` / `- 硬沙箱` / `- write/edit 沙箱内自动执行不加确认` / `- Bash 三档权限` / `- 安装档只读豁免的三条实现约束` / `- 维护约定` / `- 白名单存储键` / `- 确认链路` / `- 诚实边界` / `- 测试：` / `- **打包排除项的前提**` / `- **内置技能**` / `- **技能不构成额外权限**` 等条目 —— 新条目要与它们的措辞密度一致）、`:267`（`- 测试：` 行的完整文本）、`:272` 附近（【技能发现与调用的维护约定】条目 —— 新条目与它并列且不重复它管的面）
    - `.planning/phases/49-manage-skill-ai/49-VALIDATION.md`：frontmatter（`status` / `nyquist_compliant` / `wave_0_complete` 三个字段与注释里的生命周期说明）、§Per-Task Verification Map（10 列表头与【回填规则】段落）、§Known-Open Items（OQ-1..OQ-4 四行）、§Wave 0 Requirements（四项）
    - `.planning/phases/49-manage-skill-ai/49-01-PLAN.md` 与 `49-02-PLAN.md`：frontmatter 的 `wave` / `files_modified` / `requirements` 与每个 `<task>` 的 `<name>` —— 矩阵重键的**唯一依据**
    - `tests/test-builtin-skills-seeder.js:2200` 附近（`AGENTS.md` 的【测试：】行计数断言：它在 `node --test` 下**基线即红**（D-48-A），故改 `AGENTS.md:267` 时不得试图顺带修它，也要避免让新条目被该断言误解析）
  </read_first>
  <action>
    **A. `AGENTS.md`：新增 manage_skill 维护约定条目**

    1. 在 §AI 工作区与 Bash 权限 的既有条目序列里（建议紧随【技能不构成额外权限】/【随包静态技能目录的维护约定】一带，与技能域条目相邻）新增一条 `- **AI 自建技能（manage_skill）的维护约定**：`，内容至少含：
       - 产品说明权威 = `docs/product/ai-skills.md` §十一；改动**校验器 / 三动作 / 限额常量 / 扫描与净化口径 / 拒绝面 / 卡片形态**时**必须同步该章节与该节测试清单**；
       - 四条硬约束：① 校验器与三个动作住 `ai-skills-manager.js` 且该模块**零 electron 依赖** ⇒ Phase 50/51 **直接 require 同一份**，不得另写第二份（这是 49 排在 50/51 之前的全部理由）；② seeded 判定按**播种登记表**（`getSeededSkillNames()` 的注入值）而非目录位置；③ `LIMITS` **只允许加项**，不得改既有三项数值（`MAX_SKILL_MD_BYTES` / `MAX_USER_SKILLS` / `SKILLS_PROMPT_CHAR_BUDGET`）；④ 三动作**一律不加确认卡片**（D-01），且 `SKILL_THREAT_PATTERNS` 技能域模式组归 Phase 51（49 只把扫描点接成单点）；
       - 一句时序口径：成功后只调**一次** `syncAgentSystemPrompt()`（其函数体内已含 `refreshSkills()` 重扫；写两次调用会变成三次全量重扫），新技能集在本轮**成功出口**回写、**下一条消息起可用**；
       - 一句测试口径：`node tests/test-manage-skill.js`（manager 级值域矩阵与护栏）+ `tests/test-ai-skills.js` 的 M/L 组（接线与刷新链）；测试**必须**直接注入 `seededNames`，**严禁**经 `getSeededSkillNamesSafe()`（纯 Node 下会降级为 `[]` 导致假绿）。
    2. `:267` 的 `- 测试：` 行刷新：
       - `node tests/test-ai-skills.js` 条目的例数刷为**实测值**，覆盖面描述补【manage_skill 卡片标记两时点与终态元数据通道 / `manage_skill` 刷新链时序】；
       - **新增** `node tests/test-manage-skill.js` 条目（含实测例数与覆盖面描述）；
       - `node --test tests/test-skill-picker-model.js` 条目的例数若因 49-02 的白名单表断言而变化，同批刷新；
       - 该行的其余条目与覆盖面文字**不动**；**不要**为了迁就 `tests/test-builtin-skills-seeder.js:2200` 的计数断言而改变该行的既有形态（该断言在 Node 22 下基线即红，属既有红点 D-48-A）。
    3. 若要新增**打包**相关声明：本阶段不新增任何随包内容（`skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md` 由 Phase 47 已就位）⇒ **不改**【打包排除项的前提】条目。若执行期发现必须改 `package.json` 的 `build.files`（本阶段不应发生），则必须同时更新该条目与 `THIRD_PARTY_NOTICES.md` 并在 SUMMARY 记明。

    **B. `49-VALIDATION.md`：矩阵重键 + 四条 OQ 裁决 + Wave 0 状态**

    4. frontmatter：把 `wave_0_complete: false` 改为 `wave_0_complete: true`（Wave 0 的四个交付项已在 49-01 / 49-02 落地：新建 `tests/test-manage-skill.js`、`tests/test-ai-skills.js` 扩 M/L 组、D-08 可选参的连带断言、无需框架安装）；`status` **保持 `draft`**、`nyquist_compliant` **保持 `false`**（两者由 `/gsd-validate-phase` 判定，本计划不自我认证）。
    5. §Per-Task Verification Map：把创建时按需求维度键控的行**重键**为按已交付的计划 / 任务键控 —— 至少覆盖 `49-01-T1` / `49-01-T2` / `49-01-T3` / `49-02-T1` / `49-02-T2` / `49-02-T3` 六行（Task ID 用 `<plan>-T<n>` 形态；Plan 列 `01` / `02`；Wave 列 `1` / `2`；Requirement 列填该任务覆盖的 MGMT / 判据 / D 编号；Threat Ref 列填 49-01 / 49-02 的 `<threat_model>` 里的 `T-49-01-xx` / `T-49-02-xx` 中相应项；Automated Command 列填该任务 `<verify>` 里真实存在的命令；File Exists 列填 `✅`；Status 一律 `⬜ pending`）。行数与列数必须与表头一致，且**不得**删除原有的【回填规则】段落。
    6. §Known-Open Items：把四个 OQ 的【待裁决】改为裁决结论 + 落点：
       - OQ-1（D-08 字段分离扫描的实现方式）→ **方案 A**：`scanInjectionPatterns(content, { includeCredentials = true })`，content 侧传 `false`；`ai-memory-manager.js` 因此进入本阶段改动文件集（CONTEXT 的【无需改动】表述已被 RESEARCH 的阻塞级发现纠正）；连带回归 = `node --test test/memory/threat-scan.test.js` 全绿。
       - OQ-2（description 上限的常量归属）→ **加入 `LIMITS` 第四项** `MAX_SKILL_DESCRIPTION_CHARS = 1024`，注释声明与 SDK `MAX_DESCRIPTION_LENGTH` 对齐、升版须复核；既有三项数值不动（加项不破坏既有断言）。
       - OQ-3（测试文件组织）→ **新建 `tests/test-manage-skill.js`** 承载 manager 级值域矩阵与护栏；`tests/test-ai-skills.js` 补 **M 组**（卡片标记与接线）与 **L 组**（刷新链时序）两组的**接线类**断言。
       - OQ-4（`content` 含 frontmatter 的处理）→ **静默剥除**首部 YAML 块；工具描述写明【content 只含正文】；不引入新错误码（九码保持闭合）。
    7. §Wave 0 Requirements 的四个复选框：三个已落地的改 `[x]` 并补一句【落地于 49-01 / 49-02 的哪一组断言】，`Framework install` 一项保持 `[x]`（`node:test` 内置）。
    8. **不要**改 §Manual-Only Verifications（该表由 verify 阶段消费；本阶段只需保证它仍准确 —— 若执行期发现它已失实则只改那一处并在 SUMMARY 记明）。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const a=fs.readFileSync("AGENTS.md","utf8");const bad=[];if(!/manage_skill/.test(a))bad.push("AGENTS.md 未新增 manage_skill 维护约定");for(const t of ["ai-skills-manager.js","零 electron","播种登记表","LIMITS","确认卡片","SKILL_THREAT_PATTERNS","syncAgentSystemPrompt","下一条消息","test-manage-skill.js","getSeededSkillNamesSafe"]){if(!a.includes(t))bad.push("AGENTS.md 缺要点 "+t)}const line=a.split("\n").find((l)=&gt;l.startsWith("- 测试："));if(!line)bad.push("缺 测试： 行");else{if(!/test-manage-skill\.js/.test(line))bad.push("测试行缺 test-manage-skill.js 条目");if(!/test-ai-skills\.js[^、]*\d+ 例/.test(line))bad.push("测试行的 ai-skills 例数未填实测值")}if(/^.*- 测试：.*npm test/m.test(a))bad.push("测试行引用了 npm test");if(bad.length){console.error("AGENTS.md 收口不合格:\n"+bad.join("\n"));process.exit(1)}console.log("AGENTS.md 维护约定 + 测试清单 ok")'</automated>
    <fails_when>非零退出，或出现 'AGENTS.md 收口不合格' 或任一条 `未新增 manage_skill` / `缺要点` / `缺 测试：行` / `缺 test-manage-skill.js` / `未填实测值` / `npm test` 行，或标准输出不含 "AGENTS.md 维护约定 + 测试清单 ok"</fails_when>
    <automated>node -e 'const fs=require("fs");const v=fs.readFileSync(".planning/phases/49-manage-skill-ai/49-VALIDATION.md","utf8");const bad=[];if(!/wave_0_complete:\s*true/.test(v))bad.push("wave_0_complete 未置 true");if(!/status:\s*draft/.test(v))bad.push("status 被改成非 draft（本计划不自我认证）");if(!/nyquist_compliant:\s*false/.test(v))bad.push("nyquist_compliant 被改成 true（应由 /gsd-validate-phase 判定）");const hdr=v.split("\n").find((l)=&gt;l.startsWith("| Task ID |"));if(!hdr)bad.push("缺矩阵表头");else{const cols=hdr.split("|").length;for(const id of ["49-01-T1","49-01-T2","49-01-T3","49-02-T1","49-02-T2","49-02-T3"]){const r=v.split("\n").find((l)=&gt;l.startsWith("| "+id+" |"));if(!r)bad.push("矩阵缺 "+id+" 行");else if(r.split("|").length!==cols)bad.push(id+" 行列数 "+r.split("|").length+" 与表头 "+cols+" 不一致")}}for(const k of ["OQ-1","OQ-2","OQ-3","OQ-4"]){const i=v.indexOf(k);if(i&lt;0)bad.push("缺 "+k);else if(/待裁决/.test(v.slice(i,i+400)))bad.push(k+" 仍有【待裁决】字样")}if(bad.length){console.error("VALIDATION 重键不合格:\n"+bad.join("\n"));process.exit(1)}console.log("矩阵重键 + 四条 OQ 裁决 + Wave 0 状态 ok")'</automated>
    <fails_when>非零退出，或出现 'VALIDATION 重键不合格' 或任一条 `wave_0_complete` / `status 被改成` / `nyquist_compliant 被改成` / `缺矩阵表头` / `矩阵缺` / `列数` / `缺 OQ` / `仍有` 行，或标准输出不含 "矩阵重键 + 四条 OQ 裁决 + Wave 0 状态 ok"</fails_when>
    <automated>node tests/test-manage-skill.js 22>&1 | grep -E "^# (tests|pass|fail)"  && node tests/test-ai-skills.js 22>&1 | grep -E "^# (tests|pass|fail)"  && node --test tests/test-skill-picker-model.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（三套件任一失败），或任一套件的 "# fail" 行 &gt; 0，或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `AGENTS.md` 新增了 `manage_skill` 的维护约定条目（产品说明权威指向 §十一 + 四条硬约束 + 时序口径 + 测试口径），`:267` 的测试清单行新增 `test-manage-skill.js` 条目并把例数刷为实测值；`49-VALIDATION.md` 的矩阵按六个已交付任务重键（列数一致、Status 全 pending）、四个 OQ 逐条写入裁决与落点、`wave_0_complete` 置真而 `status` / `nyquist_compliant` 保持待判定；三套测试命令全绿。
  </done>
</task>

<task type="checkpoint">
  <name>Task 3: 端到端人工观察预备（本阶段判据 1 的 LLM 往返面）</name>
  <files>.planning/phases/49-manage-skill-ai/49-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/49-manage-skill-ai/49-VALIDATION.md`：§Manual-Only Verifications（唯一的 Manual-Only 行 —— 本任务把它的步骤细化为**可逐条勾选**的形态）、§时序断言的判定证据设计（三个证据层与【已知判定边界】段落 —— 本任务要如实标注哪一层是自动化证据、哪一层只能人工观察）
    - `.planning/phases/49-manage-skill-ai/49-CONTEXT.md`：D-13（忙时语义与【这是设计而非缺陷】）、D-02（用户感知走卡片）
    - `docs/product/ai-skills.md`：Task 1 新增的 §十一（人工观察的期望现象必须与它一致）
  </read_first>
  <action>
    1. **先把可自动化的部分跑完并留档**（本任务的前置，不新增代码）：
       - `node tests/test-manage-skill.js` / `node tests/test-ai-skills.js` / `node --test tests/test-skill-picker-model.js` 三条命令各跑一次，把 `# tests` / `# pass` / `# fail` 三行原样粘进 SUMMARY（**这是判据 1 的自动化证据层**：脏标记流转 + 出口回写 + 广播恰一次 + `rescanCalls === 2`）。
    2. 在 `49-VALIDATION.md` 的 §Manual-Only Verifications 表里把唯一那一行的【Test Instructions】细化为**逐条可勾选**的步骤，并显式写出每步的**期望现象**（不是【确认正常】）：
       - 前置：`npm run dev`（开发环境；`userData` 为 `realm-dev`，技能目录可手工清空以便从空集开始）；
       - 步骤①：在 AI 聊天里**显式要求**它把某套流程沉淀为技能（例如【把我们刚才确认的提交信息规范记成你的技能，名字用 commit-style】）；
       - 期望①：出现一张 `manage_skill` 卡片，标题为【创建技能「commit-style」】，运行中即可见；完成后头部出现【托管】徽标；展开区看到参数摘要三行、默认折叠的【技能正文（N 字符）】折叠块、结果文本含【下一条消息起可用】；
       - 步骤②：**不重开对话**，直接发下一条消息问【你现在有哪些技能？】；
       - 期望②：应答中出现 `commit-style`（这就是判据 1【下一条消息即对模型可见】的端到端证据 —— 注意断言点在下一条消息，**不是**工具返回时）；
       - 步骤③：手工在 `agent-workspace/managed-skills/` 下造一个与随包内置技能同名的目录（例如 `find-skills/`），再让 AI 对同名技能执行 create / update / delete；
       - 期望③：三种调用各出现一张带【内置不可改删】短原因的失败卡片，且磁盘上的 `managed-skills/find-skills/` 与随包 `skills-builtin/find-skills/` **都未被改动**；
       - 步骤④：打开 `/` 面板；
       - 期望④：`commit-style` 出现在面板里且其来源徽标为【托管】（与卡片上的一致 —— 同一 `TIER_BADGE` 单源）；
       - 步骤⑤（backstop 的视觉确认，对应 1 条 backstop truth）：把 AI 面板拖到最小宽度（对应 `--ai-panel-min-width: 280px`），让一张卡片同时带来源徽标 + 【未进提示词 · 超预算】标注（若不便复现可用一个超长技能名）；
       - 期望⑤：头部保持**单行不换行**、徽标与短原因**完整可读**、头部高度不变；**唯一允许的退化**是技能名被压缩到省略号。若不成立，处理方式是**缩短短原因至 ≤ 4 字**，**不得**改成换行头部或加 system-note（UI-SPEC 明文）。
    3. 在同一表下方补一段【证据分层声明】（如实标注，不夸大）：
       - 层 1（自动化，已在 49-01/49-02 落地）：脏标记流转 / 出口回写 / 广播恰一次 / `rescanCalls === 2` / 卡片标记两时点与重载同形 / 九码拒绝面与三条护栏；
       - 层 2（**仅人工**）：模型是否真的在**下一条消息**里能引用新技能（真实 LLM 往返，不可自动化）、卡片在最小宽度下的可读性（视觉裁决）；
       - 边界声明：步骤③ 的【同名用户技能优先】一类拒绝若不便手工复现，可用 `tests/test-manage-skill.js` 的对应用例作为证据（写入表格的 Instructions 列），**不因人工不便而复述成【已人工验证】**。
    4. 本任务**不**创建 `49-UAT.md`、**不**改 `STATE.md`、**不**跑 `npm run dev`（那是 verify 阶段的人工动作）：本任务只把步骤与期望现象写成可逐条勾选的形态，并在 SUMMARY 里明确【未执行人工观察，步骤已就绪】。
  </action>
  <verify>
    <automated>node tests/test-manage-skill.js 22>&1 | grep -E "^# (tests|pass|fail)"  && node tests/test-ai-skills.js 22>&1 | grep -E "^# (tests|pass|fail)"  && node -e 'const v=require("fs").readFileSync(".planning/phases/49-manage-skill-ai/49-VALIDATION.md","utf8");const bad=[];const i=v.indexOf("## Manual-Only Verifications");if(i&lt;0)bad.push("缺 Manual-Only 章节");else{const seg=v.slice(i,v.indexOf("## Validation Sign-Off",i)&gt;0?v.indexOf("## Validation Sign-Off",i):i+4000);for(const t of ["npm run dev","commit-style","托管","下一条消息","内置不可改删","280","单行","省略号","证据分层"]){if(!seg.includes(t))bad.push("Manual-Only 缺要点 "+t)}if(!/期望/.test(seg))bad.push("未写期望现象");if(/确认正常|看起来/.test(seg))bad.push("出现主观措辞（应为可判定现象）")}if(bad.length){console.error("人工观察预备不合格:\n"+bad.join("\n"));process.exit(1)}console.log("Manual-Only 步骤与期望现象就绪（含证据分层与 backstop 处理方式）")'</automated>
    <fails_when>非零退出（任一测试失败），或出现 '人工观察预备不合格' 或任一条 `缺 Manual-Only` / `Manual-Only 缺要点` / `未写期望现象` / `出现主观措辞` 行，或标准输出不含 "Manual-Only 步骤与期望现象就绪（含证据分层与 backstop 处理方式）"</fails_when>
  </verify>
  <done>
    `49-VALIDATION.md` 的 Manual-Only 行已细化为五步可逐条勾选的步骤（每条都有可判定的**期望现象**，含 280px 最小宽下的 backstop 视觉确认与其处理方式），并补了【证据分层声明】（层 1 自动化已落地 / 层 2 仅人工 / 不便手工复现的一类拒绝以单测为证据，不夸大）；本任务**未**执行人工观察、未创建 UAT 文件、未改 STATE.md，步骤已就绪。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 代码事实 → 产品文档（§十一） | 文档是后续阶段（50/51）**唯一**可读的口径来源；文档与行为不一致会让 50/51 写出第二份实现（本阶段排在它们之前的理由即此） |
| 测试实跑输出 → 两处账本（`AGENTS.md` / `docs §七`） | 例数必须来自同一次实跑；两处不一致会让【测试清单】变成不可信台账 |
| 挂账项 → 文档措辞 | TD / WR / STATE 的 ⚠️ 是**未完成事项**，文档误声明【已修】会让后续阶段据此跳过工作（不可逆的账本污染） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03-01 | Repudiation | 文档声称 TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-06 或 P8 失效链已闭合 | high | mitigate | 源码门禁断言这些标识符附近不出现【已修 / 已闭合 / 全覆盖 / 6/6】字样；§十一 明写【保持挂账】与【本阶段完成 1/3】；SUMMARY 必须披露未闭合项清单 |
| T-49-03-02 | Tampering | 口径分裂（50/51 读了与实现不一致的文档，或各写一份校验器） | high | mitigate | §十一 明写【校验器与三动作住 `ai-skills-manager.js` 且零 electron 依赖 ⇒ 50/51 直接 require 同一份】；`AGENTS.md` 的维护约定把【改口径必须同步该章节】挂成硬约定；§十一 只写行为契约不复制实现细节（避免形成第二份需要同步的真相） |
| T-49-03-03 | Repudiation | 账本漂移（`AGENTS.md` 与 `docs §七` 的例数不一致 / 填的是估算值） | medium | mitigate | 两处账本在**同一任务**里刷且都取实跑 `# tests` 值；验证命令机械断言两处条目存在且例数 > 0；SUMMARY 记录实测值供交叉核对 |
| T-49-03-04 | Repudiation | 把只能人工观察的面写成【已自动化验证】（夸大证据） | medium | mitigate | Manual-Only 表显式写【证据分层声明】（层 1 自动化 / 层 2 仅人工），并声明【未执行人工观察，步骤已就绪】；backstop 那条保持结构化标记（verify 期无可判定证据则路由 human_needed） |
| T-49-03-05 | Denial of Service | 文档/账本改动意外让 `tests/test-builtin-skills-seeder.js:2200` 的既有红点扩散 | low | accept | 该断言在 Node 22 下**基线即红**（D-48-A，与本阶段无关）；本计划**不**试图修它，也不让新条目写成会被它误解析的形态；SUMMARY 记明该既有红点与本阶段无关 |
</threat_model>

<verification>
- `docs/product/ai-skills.md`：§十一 存在且覆盖三动作 / 不接受 path / 九码 / 两个上限闸职责 / 扫描与净化口径 / 三条诚实边界 / 可见性与时序 / 维护约定；§十 三处有指向 §十一 的交叉指引；§七 的 `test-ai-skills.js` 例数刷新且新增 `test-manage-skill.js` 条目（两处均为实测值）；全文件无越界声明、无 `npm test`
- `AGENTS.md`：新增 `manage_skill` 维护约定（产品说明权威 + 四条硬约束 + 时序口径 + 测试口径）；`:267` 测试清单行新增 `test-manage-skill.js` 条目并刷新例数；测试行不引用 `npm test`
- `49-VALIDATION.md`：矩阵按 `49-01-T1..T3` / `49-02-T1..T3` 六行重键且列数一致、Status 全 pending；`wave_0_complete: true`；`status: draft` 与 `nyquist_compliant: false` 保持不变（不自我认证）；OQ-1..OQ-4 四条裁决与落点齐备、无【待裁决】残留
- Manual-Only：五步可勾选步骤 + 可判定期望现象 + 证据分层声明 + 280px backstop 的视觉确认与处理方式（缩短短原因至 ≤ 4 字，不得换成换行头部或 system-note）
- 三套测试命令（`test-manage-skill.js` / `test-ai-skills.js` / `test-skill-picker-model.js`）全绿
- 不改项：任何源码与测试文件（本计划为纯文档 / 账本计划）；`.planning/STATE.md`；`.planning/phases/49-manage-skill-ai/49-UAT.md`；`package.json` 的 `build.files`
- 指纹提示：本计划改 `docs/product/ai-skills.md` / `AGENTS.md` / `49-VALIDATION.md` ⇒ 49 的阶段指纹在本计划提交后会 stale，属**预期**（收尾时重算）
</verification>

<success_criteria>
- Phase 50 / 51 只读 `docs/product/ai-skills.md` §十一 与 `AGENTS.md` 的 manage_skill 条目就能知道【校器在哪、口径是什么、边界在哪、哪些面故意不做】，不必读代码反推
- 两个上限闸的职责分工与【不得用 prompt 段预算做创建拒绝】在文档里成文
- 三条诚实边界（bash 绕过 name 格式闸 / 双写路径是设计 / 扫描是启发式而非安全边界）成文，与 bash 白名单、`allowed-tools` 的既有成文口径同族
- 测试账本两处同值且均为实测值；`49-VALIDATION.md` 的矩阵与 OQ 裁决反映**实际交付**而非计划意图
- 未闭合项（TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-06 / `STATE.md:246` 的 ⚠️）在文档与 SUMMARY 里都被如实标注为保持挂账，本阶段只声明完成 1/3
- 判据 1 的人工观察步骤可逐条勾选，且证据分层如实（哪一层自动化、哪一层只能人工）
</success_criteria>

<artifacts_this_phase_produces>
> **Artifacts this plan produces** —— 本计划新建的文档章节与账本条目（供 plan-review-convergence 的 source-grounding pass 排除）。

**`docs/product/ai-skills.md`（新章节）**
- `## 十一、AI 自建技能（manage_skill）` 与 11.1–11.8 八个小节
- §十 的 10.5 / 10.6 / 10.7 三处交叉指引行
- §七 新增的 `node tests/test-manage-skill.js` 条目

**`AGENTS.md`（新条目）**
- `- **AI 自建技能（manage_skill）的维护约定**：` 条目
- `:267` 测试清单行的 `test-manage-skill.js` 条目与两处例数刷新

**`.planning/phases/49-manage-skill-ai/49-VALIDATION.md`（账本更新）**
- Per-Task Verification Map 的六行 `49-01-T1..T3` / `49-02-T1..T3` 重键
- `wave_0_complete: true`
- Known-Open Items 的四条裁决（OQ-1 方案 A / OQ-2 LIMITS 加项 / OQ-3 新建测试文件 + M/L 组 / OQ-4 静默剥除）
- Manual-Only Verifications 的五步可勾选步骤与证据分层声明
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/49-manage-skill-ai/49-03-SUMMARY.md` when done
</output>
