---
phase: 49-manage-skill-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-memory-manager.js
  - ai-skills-manager.js
  - ai-manager.js
  - tests/test-manage-skill.js
  - tests/test-ai-skills.js
autonomous: true
requirements: [MGMT-01, MGMT-02, MGMT-03, MGMT-04, MGMT-05, MGMT-06]
user_setup: []

estimate:
  tokens: 132000
  raw_tokens: 132000
  tasks: 3
  confidence: low

key-decisions:
  - '本计划是 Phase 49 的 tracer 计划，Task 1 是 tracer 任务：create 的端到端主干（LLM 可见的 schema → 工具 execute → 校验 → 沙箱原子写 → 刷新链）必须先在一条真实链路上跑通并被断言，其余动作（update / delete）与边界矩阵在 Task 2/3 扩展。理由：本阶段的技术风险不在缺少原语而在【误用既有原语】，纵切先把【原语接错】这类错误暴露在最便宜的时点'
  - '写函数与校验器一律住 ai-skills-manager.js，不得放 ai-manager.js（D-11 + ROADMAP 安全门禁）：该模块依赖纪律明文【不得有 electron 依赖】⇒ 可被纯 Node 单测、可被 Phase 50/51 直接 require。放到 ai-manager.js 会让 50/51 无法 require，正是本阶段排序（49 先于 50/51）要防的校验逻辑漂移'
  - 'seededNames 与 env 一律由调用方注入（签名 (env, { …, seededNames })），ai-skills-manager.js 保持零 electron 依赖。理由：builtin-skills-seeder.js 经 agent-workspace 间接依赖 electron（app.isPackaged），直接 require 它会破坏依赖纪律；对齐 48 D-14 的 sourceTierOf(entry, seededNames) 注入模式'
  - 'D-08 的字段分离扫描取 RESEARCH 方案 A：给 ai-memory-manager.scanInjectionPatterns(content, { includeCredentials = true } = {}) 加一个带默认值的可选参（content 侧传 { includeCredentials: false }）。理由：① 改动最小（函数体两行 + 一处默认值）；② 向后兼容 —— 既有 test/memory/threat-scan.test.js 全部单参调用，零影响；③ Phase 51 扩 SKILL_THREAT_PATTERNS 时可沿同一选项面继续扩展，满足 D-08 的【单点扫描 + 51 只扩表】。**否决方案 B（导出两张表）**：会让扫描的调用逻辑分裂成两处，与单点口径有张力。**否决第三种修法**：在 ai-skills-manager.js 里复制一份 INJECTION_PATTERNS（第二份模式表必然独立漂移，P3 的语料库增长机制只更新 memory 侧）。**注意**：CONTEXT.md 的 Integration Points 当前写着 ai-memory-manager.js 无需改动，该表述与代码事实不符，以 RESEARCH 的阻塞级发现为准 —— 本计划把它列为显式改动文件'
  - '撞名判定一律读盘（env.exists），不用缓存快照（D-07 明文）。理由：bash 可随时改写磁盘（P8 第 6 条），缓存只反映上次重扫的时刻，据此判【已存在】会给出错误结论。**并且 createDir 的返回值绝不可用作撞名判定** —— 实测 NodeExecutionEnv.createDir 的 recursive 默认 true ⇒ 已存在目录返回 ok:true（幂等），据此判【不存在】会把已存在的技能静默覆写'
  - '原子写 = 文件级 rename（D-12）：env.createTempFile() → env.writeFile(tmp) → env.createDir(<name>) → env.renameFile(tmp, <dest>)。实测三条硬约束：① createTempFile 的 suffix 经 sanitizeNamePart 剥掉点号（.md → md，无功能影响，但不得写依赖扩展名的断言）；② renameFile 到缺失父目录返回 not_found ⇒ create 必须先 createDir，update 的目标目录必然已存在；③ 每次原子写在 .tmp/ 留下一个空 tmp-XXXXXX/ 目录（.tmp/ 不是技能扫描根，无功能影响）。**失败清理只在 create 路径且本次确实新建了目录时执行**（局部布尔标记）—— update 失败不得删除目标目录，其内容靠 rename 的原子替换天然保持操作前状态'
  - '刷新链 = 成功后只调 syncAgentSystemPrompt() 一次（D-13）。该方法的函数体内已含 refreshSkills() 重扫（ai-manager.js:2835）⇒ 不得照 ROADMAP 字面写 refreshSkills() + syncAgentSystemPrompt() 两行，那是三次全量重扫。工具执行期 isProcessing 恒 true ⇒ 该方法走忙分支（:2843-2846）只置 _skillsPromptDirty 并返回（**紧跟在重扫之后**，故忙时缓存已更新、仅 prompt 未回写）；真正的 prompt 回写与 skills:changed 广播由 48-08 的 _flushDeferredSkillsPrompt() 在本轮成功出口落地（:1091 / :1333，两处都在 isProcessing = false 之后）。**断言点必须是 rescanCalls === 2**（工具路径一次 + 出口补刷一次，不是 3）'
  - 'name 校验器 = SDK validateName 判据集的提前执行，不是发明新规则：RESEARCH 实读 node_modules/@earendil-works/pi-agent-core/dist/harness/skills.js:237-251 确认该函数恰好包含 D-06 的四条（长度 ≤ 64 / ^[a-z0-9-]+$ / 无首尾连字符 / 无连续连字符），且这些判据在 SDK 侧只产 warning **不拒绝**（:220-222）—— 这正是 D-06 的动机：超长或非法 name 会带着 warning 进 system prompt。name 只 trim 首尾空白，**不自动 lowercase**（静默规范化会让判据 2 的【非法 name 被拒绝并说明原因】失去触发面）'
  - 'LIMITS 只加项、不改既有三项数值（D-10 约束 + 46 D-11 单源纪律）：新增 MAX_MANAGED_SKILLS = 50 与 MAX_SKILL_DESCRIPTION_CHARS = 1024（后者注释声明与 SDK harness/skills.js:5 的 MAX_DESCRIPTION_LENGTH 对齐、升版须复核）。既有测试只 includes 三条字面量，加项不转红。MAX_MANAGED_SKILLS 的统计对象 = **非 seeded 的 managed 目录数**（读盘 env.listDir + basename ∉ seededNames）—— 绝不可照抄 MAX_USER_SKILLS 只统计 source === user（ai-skills-manager.js:568-572 的注释理由在 49 落地后不再成立：AI 也能投递 managed）'
  - '两个上限闸的职责分工必须成文：数量闸（MAX_MANAGED_SKILLS）管**磁盘与重扫成本**；prompt 段预算闸（SKILLS_PROMPT_CHAR_BUDGET = 8000）管**请求成本**。二者不互相替代，且**不得用后者做创建拒绝** —— 48 D-12 已明确超预算技能仍可显式调用、本身完全可用'
  - '写入侧的 description 与正文上限预筛必须与加载期闸口同源同值，否则会产生【幽灵技能】：落盘成功但加载管线整条跳过（isDescriptionUnusable 与 createSkillsEnv 的字节闸都在**读侧**丢弃条目），磁盘上留下一个永不生效的文件且用户与 AI 都看不到解释。正文字节用 Buffer.byteLength(content, utf8) 比对 LIMITS.MAX_SKILL_MD_BYTES（加载期按 FileInfo.size 计，单位同为 UTF-8 字节）'
  - '命中即 throw（对齐 ai-memory-manager.write 的语义先例，:234-239）：业务校验失败一律 throw → SDK 转 isError:true toolResult → LLM 可见并自行修正。**不是**返回错误对象。九条机器可读原因码（seeded_protected / user_owned_conflict / already_exists / not_found / limit_exceeded / invalid_name / invalid_description / oversize / unscannable）挂在错误对象的 code 属性上'
  - '净化顺序铁律：先扫描、后净化（D-09）。净化是变形操作、扫描是判定操作；先净化会剥掉零宽字符，让 P3 实测的零宽字符变体绕过检测。净化只作用于 description（压单行 + 剥控制字符与零宽字符）；content 不净化（可能含代码 / 脚本，剥字符会破坏合法内容，且它不进 prompt）'
  - 'content 含 frontmatter 时的处理（RESEARCH A5 裁决）：**静默剥除**首部 YAML 块，工具描述写明 content 只含正文。理由：不剥除会产生双层 --- 头、SDK 解析结果不确定（可能 parse_failed → 幽灵技能）；而 D-07 的九码里没有 invalid_content，不引入新码'
  - '工具 parameters 的 JSON Schema 只是给 LLM 看的提示，服务端必须独立再校验一次（MGMT-03 明文：LLM 参数不可信）。properties 键集合恰为 {action, name, content, description} —— **无 path**，这是判据 2 的验收面（ARCHITECTURE Anti-Pattern 1：沙箱在这里提供不了保护，接口设计才是边界）'
  - '工具描述里写死两条（D-03）：① 仅在用户明确要求把某套流程 / 经验沉淀为技能时调用（落地 FEATURES 的 anti-feature 结论【不做 autolearn 推促】）；② 优先增强已有技能，而非创建近乎重复的新技能（MGMT-06，对齐 oh-my-pi 的 Capture sparingly）。**不写进 REALM_SYSTEM_PROMPT** —— 避免第 1 段改动导致所有既有会话的 provider 前缀缓存重建（48 D-18 已付过一次这种代价）'
  - '测试 seeded 保护必须直接注入 seededNames 数组，**严禁走 getSeededSkillNamesSafe()**：后者在纯 Node 下 require(electron) 返回字符串、app 为 undefined ⇒ TypeError ⇒ 被 catch 降级为 []（ai-manager.js:1532-1540），会让 seeded 保护用例**假绿**。这正是 D-11 的注入式签名设计的目的'
  - '所有验证命令写成 node tests/<file>.js 的显式形式，不得引用 npm test —— 本项目 package.json 无 test 脚本（只有 test:pre-release / test:memory / validate），GSD 的两处 gate 恒解析成不存在的 npm test 是已记录的环境问题'

must_haves:
  truths:
    - 'AI 经 manage_skill 的 create / update / delete 三动作对 managed 技能落盘后，技能集与 Agent system prompt 在**本条消息所属轮次的成功出口**即完成刷新，下一条消息可见（MGMT-01 / MGMT-05 / 判据 1）'
    - '工具 parameters.properties 的键集合**恰为** {action, name, content, description}，action 的 enum 恰为 [create, update, delete]，**不存在 path 属性**；路径由 manager 用 path.join 计算（MGMT-02 / 判据 2 的验收面）'
    - '服务端对 LLM 参数独立二次校验且失败即 throw：【非法 name 七形态】/ description trim 后为空或超 1024 / content trim 后为空或超 64 KiB 一律被拒并给出可读原因（MGMT-02 / MGMT-03）'
    - '原子写经沙箱 env.renameFile 获得 dest 双基准路径校验（越界 dest 被拒为 permission_denied）；create 与 update 共用同一条 tmp → rename 写路径；失败不留半成品目录或半成品文件（MGMT-03 / 判据 4）'
    - 'seeded 判定只消费调用方注入的 seededNames 集合（= builtin-skills-seeder.getSeededSkillNames() 的播种登记表），**不是**按目录位置推断：把 managed-skills/find-skills/ 造出来并注入 seededNames: [find-skills] 后，create / update / delete **三入口**一律被拒且 code === seeded_protected（MGMT-04 / 判据 3）'
    - '「按登记表而非按位置」有反向例：managed-skills/ai-made/ 未列入 seededNames 时 create 允许成功 —— 证明拒绝不是【managed 目录一律拒】（MGMT-04 的判定靶心）'
    - 'create 是独占创建：目标已存在（任何来源）即 isError 且**不落盘**，四类来源给不同原因码 —— seeded → seeded_protected；用户技能 → user_owned_conflict；自己已建的 managed → already_exists；目标不存在时的 update / delete → not_found。撞名判定经 env.exists 读盘，不用缓存快照（D-07）'
    - '工具描述含两条文案（MGMT-06）：【仅在用户明确要求】与【优先增强已有技能，而非创建近乎重复的新技能】；且这两条**不**出现在 REALM_SYSTEM_PROMPT 中（D-03，避免 provider 前缀缓存重建）'
    - '写入范围被接口设计与沙箱双重限定：工具不吃 path；description 跑 INJECTION_PATTERNS + CREDENTIAL_PATTERNS 两组、content **只**跑 INJECTION_PATTERNS（D-08）—— 因此 content: api_key: YOUR_KEY_HERE 这类**合法配置示例放行**，而 description: api_key: real-key-123 被拒'
    - '扫描与净化的顺序为**先扫描、后净化**（D-09）：description 含零宽字符包裹的 ignore all previous instructions 仍被拒 —— 顺序颠倒则放行，故该用例是顺序铁律的可失败证据'
    - 'description 上限与正文字节上限在**写入侧**预筛，且与加载期闸口同源同值（LIMITS 单源）：写侧允许落盘的任何技能都必然能被加载管线收进技能集，不产生【落盘成功但整条被跳过】的幽灵技能'
    - 'MAX_MANAGED_SKILLS 的统计对象是非 seeded 的 managed 目录数（读盘），第 MAX_MANAGED_SKILLS + 1 个创建被拒且 code === limit_exceeded；update / delete 不受该闸限制，seeded 目录不计入（D-10）'
    - '写入不触及 agent-workspace 内其它路径：ai-memory/ 与 attachments/ 在操作前后快照不变；写目标只有 managed-skills/<name>/，绝不写 agent-workspace/skills/（判据 4 + 见 prohibitions）'
    - '[assumption] MGMT-01 idempotency：重复执行同一动作的语义由 D-07 独占创建给出可判定判据 —— 第二次同 name 的 create 被拒（already_exists，且磁盘逐字不变），同内容的 update 幂等覆写（第二次 ok 且目录内文件数恒为 1）。判据来源 = CONTEXT D-07 + RESEARCH E3 的原子替换实测'
    - '[assumption] MGMT-01 concurrency：并发写由 executionMode: sequential（工具层）+ 单进程内串行（工具执行不并发）+ 文件级 rename 的原子替换三者共同保证；三条都是既有机制或本计划显式启用，无新增锁。参考 tests/test-ai-skills.js 的 K 组把 rescanCalls 断言成确定值（2）的既有做法 —— 若存在并发，该断言必然不稳定'
    - '[assumption] MGMT-02 empty：name / description / content 三者一律 trim 首尾空白后判非空；空串与仅空白都归 invalid_name / invalid_description / invalid_description（空正文不新立码，理由见 <assumptions>）'
    - '[assumption] MGMT-02 encoding：name 长度按 JS String.length 且 ≤ 64（与 SDK MAX_NAME_LENGTH 同口径）；description 长度按 JS String.length 且 ≤ 1024（与 SDK MAX_DESCRIPTION_LENGTH 同口径）；content 长度按 Buffer.byteLength(content, utf8) 且 ≤ 65536（与加载期 FileInfo.size 同为 UTF-8 字节）。三处单位不同、互不换算，由常量注释逐条声明'
    - '[assumption] MGMT-03 empty：renameFile 的 dest 为空串或非字符串时不做任何 IO，直接按业务错误拒绝 —— 不把空路径交给沙箱（沙箱会 resolve 到 root 本身）'
    - '[assumption] MGMT-03 encoding：沙箱错误与业务错误分层 —— 沙箱返回 Result 对象（ok / code），manager 把它转成带 code 的业务错误对象再 throw；两者都不裸传底层 message 给 LLM'
    - '[assumption] MGMT-04：seeded 判定不查磁盘存在性、不查目录位置，只查 name ∈ seededNames；因此【用户手工把某个内置技能复制到 managed-skills/ 下】也会被拒（与 D-07 第 4 类同形，工具无法区分）'
    - '[assumption] MGMT-05：新技能集在本轮成功出口写入 agent.state.systemPrompt，因此【下一条消息即对模型可见】是**本轮结束时已回写**的推论，不是【工具返回时已回写】。验收断言点 = _skillsPromptDirty 置位 → 出口回写 + 广播恰一次 + rescanCalls === 2'
    - '[assumption] MGMT-06：工具描述的两条文案不做运行期强制（LLM 是否遵守无法自动裁决）；自动验证面是**文案存在性**（源码扫描），运行期遵从归 UAT 的端到端观察'
  artifacts:
    - 'ai-memory-manager.js：scanInjectionPatterns(content, options) 的可选参 options = { includeCredentials = true }（向后兼容，单参调用行为逐字不变）'
    - 'ai-skills-manager.js：LIMITS.MAX_MANAGED_SKILLS、LIMITS.MAX_SKILL_DESCRIPTION_CHARS 两项新常量'
    - 'ai-skills-manager.js：新导出 validateManagedSkillName(name) / validateManagedSkillDescription(text) / validateManagedSkillContent(text) / sanitizeSkillDescription(text) / scanSkillText(text, { includeCredentials }) / buildSkillFileText({ name, description, content }) —— 全部纯函数，零 electron、零 IO'
    - 'ai-skills-manager.js：新导出 createManagedSkill(env, { name, content, description, seededNames }) / updateManagedSkill(env, { name, content, description, seededNames }) / deleteManagedSkill(env, { name, seededNames }) —— 业务失败一律 throw 带 code 的错误对象'
    - 'ai-skills-manager.js：新内部函数 atomicWriteSkillFile(env, destFile, text)（tmp → 双基准校验 rename 的唯一写路径，create / update 共用）'
    - 'ai-manager.js：_buildRealmTools() 新增 manage_skill 工具项（executionMode sequential、三值 enum、无 path、D-03 两条描述文案），execute 内委托 getAiSkillsManagerLazy() 并在成功后 await this.syncAgentSystemPrompt() 一次'
    - 'ai-manager.js：新私有方法 _buildManageSkillTool()（若拆函数更清晰；也可内联，形状由 executor 定，但工具项的 properties 键集合是硬约束）'
    - 'ai-manager.js：tool_execution_start / tool_execution_end 事件新增 manage_skill 标记（由 49-02 消费；本计划只落 start 侧需要的 action / name 字段与成功结果 details）'
    - 'tests/test-manage-skill.js：新建文件（值域矩阵 + 四类撞名 + seeded 三入口 + 原子性与清理 + 越界 + 字段分离扫描 + 扫描-净化顺序 + 数量闸 + 幽灵技能护栏）'
    - 'tests/test-ai-skills.js：新增断言组（工具项 schema 键集合 / 描述两条文案 / 刷新链 rescanCalls === 2 / 零 electron 依赖回归）'
  key_links:
    - 'create 主干链：LLM → manage_skill{action,name,description,content} → ai-manager.js 工具 execute（参数转发，零判定）→ getAiSkillsManagerLazy().createManagedSkill(this.sandboxEnv, { …, seededNames: this.getSeededSkillNamesSafe() }) → 校验器（name / description / content）→ scanSkillText（description 两组 / content 一组）→ sanitizeSkillDescription（仅 description）→ env.exists 撞名判定（读盘）→ seededNames 判定 → 数量闸（env.listDir 计非 seeded managed）→ createDir → createTempFile → writeFile → renameFile → await this.syncAgentSystemPrompt() → 忙分支置 _skillsPromptDirty → 本轮成功出口 _flushDeferredSkillsPrompt() → 非忙 syncAgentSystemPrompt() → agent.state.systemPrompt 回写 + broadcast(skills:changed)。这条链上没有第二份实现、没有第二套判定'
    - 'seeded 集合链：builtin-skills-seeder.getSeededSkillNames()（扫随包 skills-builtin/ 目录名集合，判据体见 :92-110）→ 由 ai-manager.js 的 getSeededSkillNamesSafe() 注入（:1532-1540，纯 Node 下降级为 []）→ ai-skills-manager.js 的 createManagedSkill / updateManagedSkill / deleteManagedSkill 只消费注入值。因此 ai-skills-manager.js 保持零 electron 依赖，而测试必须直接注入 seededNames 才能不假绿'
    - '扫描单点链：ai-memory-manager.js INJECTION_PATTERNS / CREDENTIAL_PATTERNS（:34-58，未导出）→ scanInjectionPatterns(content, { includeCredentials })（:76-96）→ ai-skills-manager.js 的 scanSkillText 是**唯一调用点**（description 传 includeCredentials: true、content 传 false）→ Phase 51 只需扩表，不加接线'
    - '原子写链：env.createTempFile（重定向 <WS>/.tmp/tmp-XXXXXX/，与 managed-skills/ 同 root 同设备 ⇒ rename 不会 EXDEV）→ env.writeFile(tmp, text) → env.createDir(<managedDir>/<name>) → env.renameFile(tmp, <managedDir>/<name>/SKILL.md)（dest 双基准校验 + 对已存在目标的 POSIX 原子替换）→ 任一步失败则清理 tmp 与（仅 create 且本次新建时）目标目录'
    - '回归护栏：ai-skills-manager.js 的依赖纪律断言（tests/test-ai-skills.js 的零 electron 依赖 describe）、46-04 的五条 syncAgentSystemPrompt() 方法体断言、K 组的 rescanCalls 断言、node --test test/memory/threat-scan.test.js（可选参改动后必须全绿）必须继续成立'
  prohibitions:
    - statement: '绝不能写入或删除 agent-workspace/skills/ 下的用户自建技能；managed-skills/ 是 AI 的唯一可写技能目录'
      status: unresolved
      verification: judgment
      reason: '管理技能的边界规则（O6 / CONTEXT 前置约束：AI 自建 managed 可改可删，只保护 seeded；oh-my-pi 的原文 managed skills ONLY writable skills. NEVER edit user-authored skills.）。写入面必须通过**接口设计**（工具不吃 path，路径由 manager 用 path.join 拼 managed-skills）而非沙箱限定 —— 沙箱的 exec 不校验命令内容，无法表达目录级只读。回归证据：tests/test-manage-skill.js 断言操作前后 agent-workspace/skills/ 与 ai-memory/ / attachments/ 快照逐字不变'
    - statement: '不得为 manage_skill 的任何动作新增确认卡片或插入 system-note'
      status: unresolved
      verification: judgment
      reason: 'D-01 与 D-02 已锁定：三动作一律自动（破坏面已被硬沙箱限定在 AI 专用数据区，且 AI 本就能用 write 工具直写同一路径 —— 单独给 manage_skill 加确认会被轻易绕道，成为只在合作路径上生效的虚假安全感）；用户感知只走工具卡片技能化（49-02）。源码门禁断言 ai-manager.js 的 manage_skill 工具 execute 内不含 requestActionConfirmation 调用'
    - statement: '错误消息不得回显被拒内容原文（description / content 的命中片段）'
      status: unresolved
      verification: judgment
      reason: '遵循读取路径的拒绝语义（扫描器命中即拒绝写入，避免把秘密留在更容易外发的面）与 memory 工具先例：错误消息只给原因码 + 可操作提示 + 限额 / 当前值，不回显被拒文本本身'
    - statement: '不得在 Phase 49 实现 SKILL_THREAT_PATTERNS 技能域模式组（外发 / 凭据路径 / 绕过授权 / 篡改平台写 managed-skills）'
      status: unresolved
      verification: judgment
      reason: '归 Phase 51（CONTEXT 的 Deferred Ideas 明文）；49 只把扫描点接成单点，51 扩表即可。在 49 提前实现会导致 51 再写一份。源码扫描：ai-skills-manager.js / ai-memory-manager.js 内不得出现 SKILL_THREAT_PATTERNS 标识符或 ~/.ssh / .aws / .env 之类的技能域敏感路径表'
    - statement: '不得把校验器或写函数放进 ai-manager.js（含为便于测试而扩大该模块职责）'
      status: unresolved
      verification: judgment
      reason: 'ROADMAP 安全门禁明文：本阶段产出的校验器是 50/51 唯一可复用的那一份；ai-manager.js 顶层有 electron 依赖链，50/51 无法 require 它。此为 Phase 49 排序（先于 50/51）的全部理由'
---

<objective>
交付 Phase 49 的**技能集 AI 写入路径**的权威实现层：一个 `manage_skill` 工具（create / update / delete 三动作）＋一套住在 `ai-skills-manager.js` 的唯一校验器与原子写实现，使 AI 能把一套流程沉淀为自己的 managed 技能、修改、删除它们，且**无法覆盖或删除随包内置技能**。

本计划是 Phase 49 的三个计划中的第一个（tracer），**Task 1 是 tracer 任务**：先把 `create` 的端到端主干（schema → execute → 校验 → 扫描 → 撞名判定 → seeded 判定 → 数量闸 → 沙箱原子写 → 刷新链 → 下一条消息可见）在一条真实链路上跑通并断言，Task 2 才扩展 `update` / `delete` 与完整的边界拒绝矩阵，Task 3 才补齐值域矩阵与两条护栏（幽灵技能 / 越界）。

Purpose: 把「AI 能否安全地自建技能」从 ROADMAP 的五条 Success Criteria 落成**可失败被观察的代码事实**，并且把校验器做成 50/51 可复用的同一份（避免后续写入路径各写一份而漂移）。本计划的全部技术风险集中在**误用既有原语**（`createDir` 幂等不能判撞名、`renameFile` 缺父目录必失败、`remove` 默认非递归、`refreshSkills` 已被 `syncAgentSystemPrompt` 内部调用、`scanInjectionPatterns` 无条件连跑两组），故 Task 1/3 的断言直接钉住这五条。

Output: `ai-memory-manager.js` 的字段分离可选参；`ai-skills-manager.js` 的两个新限额常量 + 六个新导出纯函数 + 三个新导出动作函数 + 一条原子写内部实现；`ai-manager.js` 的 `manage_skill` 工具项与刷新链接线；新建 `tests/test-manage-skill.js` 与在 `tests/test-ai-skills.js` 追加的接线断言。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-manage-skill-ai/49-CONTEXT.md
@.planning/phases/49-manage-skill-ai/49-RESEARCH.md
@.planning/phases/49-manage-skill-ai/49-VALIDATION.md
@ai-skills-manager.js
@ai-memory-manager.js
@ai-manager.js
@agent-workspace.js
@builtin-skills-seeder.js
@tests/test-ai-skills.js
@tests/test-agent-workspace.js
</context>

<assumptions>
> 以下 9 条来自本阶段的 spec-less edge probe（本阶段**无 SPEC 文件** ⇒ `EDGE_ABSENT` / `PROHIB_ABSENT` 同时成立）。
> 探针在 MGMT-01..06 上产出 `applicable 9 / resolved 0 / unresolved 9`（6 条 empty·encoding·idempotency·concurrency + 3 条 unclassified）。
> 按 spec-less fallback 的纪律，它们**不得静默丢弃、也不得 auto-resolve 成 backstop**；此处逐条给出显式裁决与落点，
> 裁决已写入上方 `must_haves.truths` 的 `[assumption]` 行。探针的 `unclassified` 三行（MGMT-04 / MGMT-05 / MGMT-06）
> 各有一条可写的显式判据（分别来自 D-07 的来源判定、D-13 的时序语义、D-03 的文案锁定），故按 explicit 裁决而非假设保留。

1. **MGMT-01 / idempotency（重复执行同一动作）** —— 裁决：不复用 upsert 语义，由 D-07 的**独占创建**给出判据。
   `create` 第二次同 name 必拒（`already_exists`）且磁盘逐字不变；`update` 同内容幂等（第二次 `ok`、目录内文件数恒 1）。
   落点：`createManagedSkill` / `updateManagedSkill`。证据：RESEARCH E3 实测 renameFile 对已存在目标的原子替换成立。

2. **MGMT-01 / concurrency（被中断或并行执行时的保证）** —— 裁决：三条既有机制相加，**不新增锁**。
   ① 工具层 `executionMode: 'sequential'`（对齐 memory 工具先例，防同批次并发写）；
   ② 工具执行本身在单进程内不并发（同一 Agent 轮的 tool call 串行）；
   ③ 文件级 `rename` 是 POSIX 原子替换（E3 实测），故不存在半写状态。
   可失败证据：K 组的 `rescanCalls === 2` 是确定值断言 —— 若真存在并发写，该断言会不稳定。

3. **MGMT-02 / empty（空 / 单元素 / null 输入）** —— 裁决：三者一律 `trim()` 后判非空。
   `name` 空 → `invalid_name`；`description` 空 → `invalid_description`；`content` 空 → `invalid_description`
   （**空正文归 invalid_description 而非 oversize**：UI-SPEC 的非阻断建议 2 要求显式指定，
   而 `oversize` 文案是【正文超限】、语义是长度而非空。九码内的选择比新立第十码更符合 D-07 的闭合白名单纪律）。
   另：`name` 非字符串 / `null` / `undefined` 走同一 `invalid_name` 分支，不做隐式 String() 转换。

4. **MGMT-02 / encoding（长度与相等性口径）** —— 裁决：**不新定义口径**，三处各自沿用既有语义。
   `name` ≤ 64 按 JS `String.length`（对齐 SDK `MAX_NAME_LENGTH`）；
   `description` ≤ 1024 按 JS `String.length`（对齐 SDK `MAX_DESCRIPTION_LENGTH`）；
   `content` ≤ 64 KiB 按 `Buffer.byteLength(content, 'utf8')`（与加载期 `FileInfo.size` 同为 UTF-8 字节）。
   相等性：name 与目录名逐字符全等，不做 NFC/NFD 归一化（归一化归 Phase 51 的查重职责）。
   落点：三个校验函数 + `LIMITS` 的注释（逐条声明对齐关系，升版须复核）。

5. **MGMT-03 / empty** —— 裁决：`renameFile` 的 dest 为空串 / 非字符串时**不做任何 IO**，直接按业务错误拒绝。
   理由：沙箱会把空路径 resolve 到 root 本身，把这种输入交给沙箱等于让它去猜语义。
   落点：`atomicWriteSkillFile` 的入参前置检查。

6. **MGMT-03 / encoding** —— 裁决：**沙箱错误与业务错误分层**。
   沙箱返回 Result 对象（`{ ok, value }` / `{ ok: false, error: { code } }`），manager 把它转成带 `code` 的
   业务错误对象再 throw；两者都不裸传底层 message 给 LLM（prohibition 3）。
   落点：`atomicWriteSkillFile` 的每个失败分支 + 三个动作函数的 throw 形态。

7. **MGMT-04 / unclassified** —— 裁决：**判定靶心 = 登记表而非目录位置**，可写显式判据。
   在 `managed-skills/` 下造 `find-skills/`（位置 = managed）并注入 `seededNames: ['find-skills']` → 三入口全拒；
   反向例：`managed-skills/ai-made/` 未列入 → 允许创建。落点：三个动作函数的 seeded 判定分支。

8. **MGMT-05 / unclassified** —— 裁决：**时序语义可写显式判据**（不是【立即】而是【本轮成功出口】）。
   断言链：工具执行期 `_skillsPromptDirty === true` 且 `agent.state.systemPrompt` **不含**新技能名 →
   调 `prompt()` 成功后脏标记归 `false`、prompt 含新技能名且 `=== buildSystemPrompt()`、
   `skills:changed` 恰广播一次、`rescanCalls === 2`。落点：`ai-manager.js` 的成功出口（48-08 已建，本计划只接线）。

9. **MGMT-06 / unclassified** —— 裁决：**文案存在性可自动验证，运行期遵从归 UAT**。
   自动面 = 源码扫描工具描述含两条文案关键字；运行期面 = UAT 端到端观察 AI 是否真的优先 update 而非重复 create。
   落点：`ai-manager.js` 的工具 description；UAT 步骤见 `49-VALIDATION.md` 的 Manual-Only 表。
</assumptions>

<!-- planner-discipline-allow: 技能名 / 正文 / 描述 / 内置技能 / 托管技能 -->

<tasks>

<task type="tracer">
  <name>Task 1: create 的端到端主干 —— 校验器 → 字段分离扫描 → 独占创建 → 沙箱原子写 → 刷新链（含新建测试文件的 create 脊椎）</name>
  <files>ai-memory-manager.js, ai-skills-manager.js, ai-manager.js, tests/test-manage-skill.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-memory-manager.js`（被改造文件）：`:34-58`（`INJECTION_PATTERNS` / `CREDENTIAL_PATTERNS` 两张表的形状与条目名）、`:76-96`（`scanInjectionPatterns(content)` 的函数体 —— 两组**串行无条件**执行，本任务要加可选参）、`:142-147`（`atomicWrite` 的 tmp + rename **模式先例**；注意它未导出、且是 fs 直写，本任务只照抄其模式不复用它）、`:198-270`（`write()` 的业务校验失败一律 `throw` 语义先例与 `:234-239` 的扫描命中 throw 形态）、`:405-417`（导出面 —— 两张表均未导出，本任务**不**导出它们）
    - `ai-skills-manager.js`（被改造文件）：`:1-61`（文件头的**依赖纪律**：不得有 electron 依赖 / SDK 只走包根动态 import / 不自行剥 frontmatter；`LIMITS` 在 `:32-36`；`EMPTY_CACHE` 在 `:39-46`）、`:140-190`（`createSkillsEnv()`：`readTextFile` 的 SKILL.md 字节闸与 `maxSkillMdBytes` 的取值来源 —— 这是**加载期闸口**，写侧预筛必须与它同源同值）、`:298-330`（`isDescriptionUnusable` 的【description 不可用 → 整条跳过】判据）、`:446-500`（`refreshSkills(env, { disabled, rootDirs })` 的注入式签名与 SDK 动态 import 先例）、`:568-580`（`MAX_USER_SKILLS` 只统计 `source === 'user'` 的既有实现与它的注释理由 —— `MAX_MANAGED_SKILLS` **不得**照抄）、`:660-700`（`getSkillsSnapshot` / `sourceTierOf(entry, seededNames)` —— 集合由调用方注入的模式）、`:707-760`（`toUISkillEntry` / `matchSkillByPath`）、`:802-841`（`readSkillForInvocation` 的实时读盘口径 + 空正文拒绝先例）、`:841-853`（导出面）
    - `ai-manager.js`（被改造文件）：`:94-121`（`getAgentWorkspaceLazy` / `getAiSkillsManagerLazy` / `getBuiltinSkillsSeederLazy` 惰性 require 模板）、`:129`（`getSeededSkillNamesSafe()` 的 JSDoc：**必须**经它、不得直接调 seeder）、`:879`（`this.tools = this._buildRealmTools()`）、`:1512` / `:1532-1540`（`getSeededSkillNamesSafe()` 的 try/catch 降级 —— 纯 Node 下返回 `[]`）、`:3177`（`_buildRealmTools()` 起点）、`:5813-5931`（`memory` / `memory_read` 工具项与数组尾部的 `...this._buildFilesystemTools(),` —— 新工具项插在它**之前**）、`:2831-2859`（`syncAgentSystemPrompt()` 的方法体：`:2835` 的 `refreshSkills` 调用、`:2843-2846` 忙分支、`:2858` 广播）、`:2905-2919`（`_flushDeferredSkillsPrompt()`）、`:1084-1091` / `:1329-1333`（两处成功出口）
    - `agent-workspace.js`（**只读消费，零改动**）：`:162-200`（`resolveInside` 双基准 + realpath 复核）、`:205-360`（`createSandboxEnv()` 的全部 FileSystem 包装：`writeFile` / `renameFile` 的双路径 `guardResult`、`:231` 的 `sanitizeNamePart`（会剥掉点号）、`:338-352` 的 `createTempFile`（重定向到 `getTmpDir()`））
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/env/nodejs.js:524-537`（`renameFile` 用 `fs.promises.rename` ⇒ POSIX 原子替换）、`:592-601`（`createDir` 的 `recursive: options?.recursive ?? true` ⇒ **已存在目录返回 ok:true**）、`:602-611`（`remove` 的 `recursive ?? false` / `force ?? false` 默认值）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/skills.js:4-5`（`MAX_NAME_LENGTH = 64` / `MAX_DESCRIPTION_LENGTH = 1024`）、`:220-224`（这些判据只产 warning 不拒绝）、`:237-251`（`validateName` 的四条 push —— name 校验器的判据来源）
    - `builtin-skills-seeder.js`（**只读消费**）：`:92-110`（`getSeededSkillNames()` 的判定体 —— 判据 3 的播种登记表唯一数据源）、`:60-90`（`resolveBuiltinSkillsSrc` / `resolveManagedSkillsDir`）
    - `tests/test-agent-workspace.js`：文件头夹具（`mkdtempSync` + `t.after` 清理）与 `renameFile` 双路径用例的写法 —— 新建 `tests/test-manage-skill.js` 的夹具骨架照它
    - `tests/test-ai-skills.js`：`:13-73`（`withTempRoot` / `writeSkill` / `readSource` / `functionBody` / `methodBody` 五件套与 `REAL_BUILTIN_SRC`）、`:1127-1205`（依赖纪律与导出面的源码断言组 —— 本任务新增的导出必须让它继续绿）、`:2173-2180`（46-04 的五条 `syncAgentSystemPrompt()` 方法体断言 —— **不得**被本次改动破坏）、`:3160-3190`（K 组的 `rescanCalls === 2` 断言范式）
    - `.planning/phases/49-manage-skill-ai/49-RESEARCH.md`：§实测证据索引 E1–E12、§Common Pitfalls 1–9、§Code Examples 全节、§Open Questions 1–2 的裁决
  </read_first>
  <behavior>
    - `scanInjectionPatterns(text)` 单参调用行为**逐字不变**；`scanInjectionPatterns(text, { includeCredentials: false })` 对 `api_key: YOUR_KEY_HERE` / `Set token: abc123 in the config` 返回 `{ safe: true }`，对 `ignore all previous instructions` 仍返回 `safe: false`（注入组照跑）。
    - `validateManagedSkillName`：`foo` / `a` / `skill-1` / `x`.repeat(64) → 通过；`''`（trim 后空）/ `Foo`（大写）/ `FOO` / `My_Skill` / `foo_bar` / `foo.bar` / `foo bar` / `-foo`（首连字符）/ `foo-`（尾连字符）/ `foo--bar`（连续连字符）/ `x`.repeat(65)（超长）/ `null` / `undefined` / `123`（非字符串）→ 全部 `invalid_name`。
    - `validateManagedSkillDescription('')` / `'   '` → `invalid_description`；`'x'`.repeat(1024) → 通过；`'x'`.repeat(1025) → `invalid_description`。
    - `validateManagedSkillContent('')` / `'   '` → `invalid_description`；`'a'.repeat(65536)`（ASCII，字节数 == 字符数）→ 通过；`'a'.repeat(65537)` → `oversize`；`'中'.repeat(21846)`（3 字节 × 21846 = 65538）→ `oversize`（证明按**字节**而非字符计数）。
    - `sanitizeSkillDescription('a\nb')` → `'a b'`；含零宽字符（`\u200b`）与控制字符（`\u0007`）的输入被剥除；**净化不改变不含这些字符的输入**。
    - `buildSkillFileText({ name: 'foo', description: '描述', content: '正文' })` → frontmatter 恰两行（`name:` 与 `description:`），正文不含任何包裹标签；输入 `content` 首部带 `---\nname: x\ndescription: y\n---\n正文` 时产出**单层** frontmatter（剥除，见 assumptions 的 A5 裁决）。
    - `createManagedSkill(env, { name, content, description, seededNames })`：正常入参 → 返回 `{ name, filePath, promptIncluded }` 形状的对象且磁盘上出现 `managed-skills/<name>/SKILL.md`；`env.exists(managed-skills/<name>)` 为真。
    - 撞名四类：目录已存在且 `name ∈ seededNames` → `seeded_protected`；已存在且同 name 命中 user 来源（`agent-workspace/skills/<name>/`）→ `user_owned_conflict`；已存在且非 seeded、非 user → `already_exists`；三者一律**不落盘**（操作前后 `managed-skills/` 的 `readdirSync` 逐字相等）。
    - 两次连续 `env.createDir(existing)` 都返回 `ok: true`（E2 的回归护栏）—— 测试注释须写明【故撞名判定走 `env.exists`，绝不用 createDir 的返回值】。
    - `renameFile` 到缺失父目录 → `ok: false` 且 `code === 'not_found'`（E4 的回归护栏，说明 `createDir` 是硬前置）。
    - `renameFile` 到 `../../../etc/passwd` 之类的越界 dest → `ok: false` 且 `code === 'permission_denied'`，且目标未产生（E5）。
    - 连续两次写同一 dest（第二次内容不同）→ 两次都 `ok: true`、dest 内容 == 第二次内容、**目录内文件数恒为 1**（E3 的原子替换）。
  </behavior>
  <action>
    **A. `ai-memory-manager.js`：给扫描函数加字段分离可选参（唯一的外部改动）**

    1. 把 `:76` 的签名改为 `function scanInjectionPatterns(content, options = {})`，函数体首行解构 `const includeCredentials = options.includeCredentials !== false;`（默认 `true` = 向后兼容），并把 `:84-90` 的 `CREDENTIAL_PATTERNS` 循环整体包在 `if (includeCredentials) { ... }` 内。注入组循环**不动**、顺序**不动**（先注入后凭据是既有行为）。
    2. 在该函数上方补 JSDoc：① 这是技能域与记忆域**共用**的单点扫描（D-08）；② `includeCredentials` 的语义与唯一的 false 调用方（`ai-skills-manager.js` 的 content 侧 —— 技能文档合法地会写配置示例，凭据组会误伤）；③ **不得**在本函数之外复制两张模式表（第二份表必然独立漂移）。
    3. **不要**把 `INJECTION_PATTERNS` / `CREDENTIAL_PATTERNS` 加入 `module.exports`（方案 A 的前提就是调用逻辑单点）；**不要**改 `atomicWrite`（未导出，本阶段不需要它）。
    4. 回归：`node --test test/memory/threat-scan.test.js` 必须全绿（既有调用全部单参）。

    **B. `ai-skills-manager.js`：限额常量 + 校验器 + 净化 + 扫描单点 + 原子写 + create**

    5. `LIMITS`（`:32-36`）**只加两项、不动既有三项**：
       - `MAX_MANAGED_SKILLS: 50`（注释：AI 自建技能的数量闸，统计对象是**非 seeded 的 managed 技能数**；管磁盘与重扫成本，与 `SKILLS_PROMPT_CHAR_BUDGET` 的请求成本闸职责不同，二者不互相替代）
       - `MAX_SKILL_DESCRIPTION_CHARS: 1024`（注释：与 SDK `node_modules/@earendil-works/pi-agent-core/dist/harness/skills.js:5` 的 `MAX_DESCRIPTION_LENGTH` 对齐，升版须复核；写成第四项是为了让写入侧预筛与加载期闸口**同源**，而不是在写侧硬编码第二份字面量）
    6. 新增六个**纯函数**（零 IO、零 electron、零 SDK import），全部加 JSDoc 并在 `module.exports` 导出（放在 `:841-853` 的导出面里，按 48-01 的分组注释风格）：
       - `validateManagedSkillName(name)` → `{ ok: true }` 或 `{ ok: false, code: 'invalid_name', reason: <中文可读原因> }`。判据四条（长度 ≤ `LIMITS` 之外的 SDK 常量 64 / `^[a-z0-9-]+$` / 无首尾连字符 / 无连续连字符），**只 trim 首尾空白、不 lowercase**；非字符串一律拒。这四条判据就是 **D-06** 的落点：D-06 要求「判据字面 + 三条必要补充」且明写一条**故意的不对称 —— 写入门严、读入门宽**（加载管线对磁盘上已存在的技能保持宽松，严格校验只作用于我们能控制的写入侧），因此本校验器**只被写入侧调用**，绝不在加载管线上加闸（那会推翻 46 D-08 的【不丢弃命名不规范的合法技能】）。
       - `validateManagedSkillDescription(text)` → 同上形状（`invalid_description`）：trim 后非空 + `String.length ≤ LIMITS.MAX_SKILL_DESCRIPTION_CHARS`。
       - `validateManagedSkillContent(text)` → `{ ok: false, code: 'invalid_description' }`（trim 后空）或 `{ ok: false, code: 'oversize' }`（`Buffer.byteLength(text, 'utf8') > LIMITS.MAX_SKILL_MD_BYTES`）或 `{ ok: true }`。
       - `sanitizeSkillDescription(text)` → 压单行（`\s+`→单空格，含换行）+ 剥控制字符与零宽字符；**只作用于 description**。
       - `scanSkillText(text, { includeCredentials = true } = {})` → 委托 `getAiMemoryManagerLazy()` 的 `scanInjectionPatterns`（惰性 require 模板照 `:100-121`），命中时 `throw` 一个 `code: 'unscannable'` 的错误对象（对齐 memory 工具的 throw 语义）；**这是技能域扫描的唯一调用点**。
       - `buildSkillFileText({ name, description, content })` → frontmatter（恰 `name` 与 `description` 两行）＋ 空行 ＋ 正文；`content` 首部若匹配 `^---\s*\n[\s\S]*?\n---\s*\n` 则**先剥除**再拼接（A5 裁决：静默剥除，不引入新码）。
    7. 新增内部函数 `atomicWriteSkillFile(env, destFile, text)`（不导出）：
       - 前置：`destFile` 非空字符串，否则 throw 一个 `code: 'unknown'` / 可读原因的业务错误（不把空路径交给沙箱，见 assumptions 5）。
       - `const tmp = await env.createTempFile({ prefix: 'skill-', suffix: '.md' })` → 失败即 throw（`oversize` 之外的 `unknown`）。
       - `await env.writeFile(tmp.value, text)` → 失败则 `await env.remove(tmp.value, { force: true })` 后 throw。
       - `await env.renameFile(tmp.value, destFile)` → 失败则 `await env.remove(tmp.value, { force: true })` 后 throw。
       - 每个失败分支都把沙箱 Result 的 `error.code` 转成业务错误的 `code`（分层，见 assumptions 6），**不裸传**底层 message。
       - 注释必须写明三条实测约束：`createTempFile` 的 suffix 被 `sanitizeNamePart` 剥掉点号（无功能影响）；`renameFile` 到缺失父目录返回 `not_found` ⇒ 调用方必须先 `createDir`；每次调用在 `.tmp/` 留下一个空 `tmp-XXXXXX/` 目录（`.tmp/` 不是技能扫描根）。
    8. 新增导出 `async function createManagedSkill(env, { name, content, description, seededNames } = {})`，**严格按下列顺序**（顺序本身就是纪律：先纯校验、后 IO、扫描在净化之前）：
       1. 三个校验器（`validateManagedSkillName` / `validateManagedSkillDescription` / `validateManagedSkillContent`）任一不过即 throw 对应 `code`。
       2. `scanSkillText(description, { includeCredentials: true })` → 再 `scanSkillText(content, { includeCredentials: false })`（**先扫描**）。
       3. `const safeDescription = sanitizeSkillDescription(description)`（**后净化**）。
       4. `const managedDir = getAgentWorkspaceLazy().getManagedSkillsDir()`；`const destDir = path.join(managedDir, name)`；`const destFile = path.join(destDir, 'SKILL.md')`。
       5. **撞名判定（读盘）**：`const existsManaged = await env.exists(destDir)`；`const existsUser = await env.exists(path.join(getAgentWorkspaceLazy().getSkillsDir(), name))`。两者任一为真即进入拒绝分支（**不落盘**）：
          - `isSeededName(name, seededNames)` → `seeded_protected`
          - 否则 `existsUser` → `user_owned_conflict`
          - 否则 → `already_exists`
          seeded 判定必须先于 user 判定（内置优先给内置的原因），而 managed 侧的已存在与【用户手放在 managed-skills/ 下的】同形（工具无法区分）→ 同走 `already_exists`。
       6. **数量闸**：`const entries = await env.listDir(managedDir)`（`ok` 为假时按 0 计，不因读目录失败而拒绝创建）；`const managedCount = entries.value.filter((e) => e.isDirectory() && !isSeededName(e.name, seededNames)).length`；`managedCount >= LIMITS.MAX_MANAGED_SKILLS` → throw `limit_exceeded`（原因里带 `LIMITS.MAX_MANAGED_SKILLS` 与可操作提示【先删除不再需要的技能】）。
       7. `const madeDir = !(await env.exists(destDir)).value`；`await env.createDir(destDir)`。**`CreateDir` 的返回值不参与任何判定**（幂等，E2）。
       8. `try { await atomicWriteSkillFile(env, destFile, buildSkillFileText({ name, description: safeDescription, content })) } catch (err) { if (madeDir) await env.remove(destDir, { recursive: true }); throw err; }` —— **清理只在本次确实新建了目录时执行**（Pitfall 9）。
       9. 返回 `{ name, filePath: destFile, description: safeDescription, action: 'create' }`（`promptIncluded` 由 `ai-manager.js` 的工具层判定，见 49-02；本任务返回对象不带该键）。
       - 新增内部 `function isSeededName(name, seededNames)`：`const set = seededNames instanceof Set ? seededNames : new Set(seededNames || []); return set.has(name);`（与 `:684-688` 的 `sourceTierOf` 同款容错形态）。
       - 新增内部 `function makeManageSkillError(code, message, extra = {})`：构造 `const err = new Error(message); err.code = code; Object.assign(err, extra); return err;` —— 九码的**唯一构造点**。

    **C. `ai-manager.js`：工具注册与刷新链接线**

    9. 在 `_buildRealmTools()` 的 `:5926-5931`（`...this._buildFilesystemTools(),` **之前**）插入 `manage_skill` 工具项，形状照 `:5813` 的 `memory` 工具：
       - `name: 'manage_skill'`；`label` 用中文（如【创建/更新/删除技能】）；
       - `description`：**必须**同时含 D-03 的两条文案 —— 【仅在用户明确要求】把某套流程沉淀为技能时调用（不做 autolearn 推促）、【优先增强已有技能，而非创建近乎重复的新技能】（MGMT-06）；并写明动作分工（create 独占创建 / update 全量覆写正文 / delete 递归删除整目录 —— 这条分工即 **D-04** 要求写进工具描述的口径）、`content` 只含正文不含 frontmatter（D-04）、不支持改名（要改名 = delete + create）。
       - `parameters`：`type: 'object'`；`properties` **恰为** `action`（`type: 'string'`，`enum: ['create', 'update', 'delete']`）、`name`、`content`、`description`；`required: ['action', 'name']`。**绝不出现 `path`**；`pattern` / `maxLength` 等约束只作 LLM 提示（服务端独立再校验）。
       - `executionMode: 'sequential'`（对齐 memory 工具；防同批次并发写）。
       - `execute: async (toolCallId, params) => { ... }`：只做参数解析与转发 —— `const action = params && params.action;`、`const name = typeof params.name === 'string' ? params.name.trim() : '';`；按 action 分派到 `getAiSkillsManagerLazy().createManagedSkill(this.sandboxEnv, { name, content, description, seededNames: this.getSeededSkillNamesSafe() })`（update / delete 由 Task 2 补齐；本任务先实现 create 分支，其余 action 抛【暂未实现】之外的**业务错误**由 Task 2 移除）；成功后**恰一次** `await this.syncAgentSystemPrompt();`（**不得**写 `refreshSkills()` + `syncAgentSystemPrompt()` 两行 —— D-13）；返回 `{ content: [{ type: 'text', text: <中文成功文案> }], details: <形状见 Task 2 的终态元数据> }`。
       - **不要**在 execute 内调用 `requestActionConfirmation`（D-01：三动作一律自动；prohibition 2 的源码门禁会断言这一点）。
    10. 新增私有方法 `_buildManageSkillTool()`：把上述工具对象整体搬进去，`_buildRealmTools()` 里以 `this._buildManageSkillTool(),` 调用（保持 `_buildRealmTools()` 的体量可控；形状与 `_buildFilesystemTools()` 同款）。若判定内联更清晰也可内联 —— 但 `properties` 的键集合是硬约束，与形态无关。
    11. `tool_execution_start` 事件（`:1751-1766`）**本任务不改**（`manage_skill` 的标记与终态元数据归 49-02）。

    **D. `tests/test-manage-skill.js`：新建文件，本任务只落 create 脊椎**

    12. 文件头（照 `tests/test-agent-workspace.js` 的夹具风格）：
       - `require('node:test')` / `node:assert/strict`；
       - `const REPO_ROOT = path.join(__dirname, '..');`
       - 夹具 `withTempRoot(t)`：`fs.mkdtempSync(path.join(os.tmpdir(), 'realm-manage-skill-'))` + `t.after` 清理 + `workspace.setWorkspaceDir(root)` + `aiSkills._resetCacheForTest()` + `t.after` 里 `workspace.setWorkspaceDir(null)`；
       - 夹具 `async function makeEnv(root)`：`workspace.ensureWorkspaceDir()` 后 `return workspace.createSandboxEnv({ cwd: root })`；
       - 夹具 `function writeSkill(dir, name, { description, body, frontmatterName } = {})`：造型一个合法 `SKILL.md`（用于造 seeded 目录 / 撞名对象）；
       - 夹具 `const SEEDED = ['find-skills', 'skill-creator'];`（**显式数组常量**，直接注入 —— 严禁走 `getSeededSkillNamesSafe()`，见 key-decisions 最后第 2 条）。
    13. 本任务落的用例组（每例注释里写明它钉住的是哪条实测/纪律）：
       - `createManagedSkill` 主干：合法入参 → `ok` 形状 + `env.exists(managed-skills/<name>/SKILL.md)` 为真 + 文件内容含单层 frontmatter 与正文。
       - 撞名三类的 create 侧：seeded → `seeded_protected`；已存在同名用户技能 → `user_owned_conflict`；已存在非 seeded managed → `already_exists`；三例都断言 `managed-skills/` 的 `readdirSync` 与操作前 `deepStrictEqual`（**不落盘**）。
       - 反向例（MGMT-04 靶心）：`managed-skills/ai-made/` 未列入 `SEEDED` → create 成功。
       - 校验器值域：name 的通过集与七类非法形态；description 的 1024 边界；content 的 65536 / 65537 字节边界（含中文三字节用例）。
       - 扫描分离：`description: 'api_key: real-key-123'` → 拒且 `code === 'unscannable'`；`content: 'api_key: YOUR_KEY_HERE'` → **成功**（合法配置示例放行）。
       - 净化顺序：`description` 含零宽字符包裹的 `ignore all previous instructions` → 拒（若顺序颠倒则放行 ⇒ 该用例会转红）。
       - 沙箱原语回归护栏：两次 `createDir` 都 `ok: true`；`renameFile` 缺父目录 → `not_found`；`renameFile` 越界 dest → `permission_denied`；两次写同一 dest → `ok` 且目录内文件数恒 1。
    14. `tests/test-ai-skills.js` 追加一组源码断言（放既有【依赖纪律与导出面】组附近）：`manage_skill` 工具项存在、`properties` 的**顶层键集合恰为** `{action, name, content, description}`（用深度计数收集 `properties: {` 之后一层内的键，**不要**用会误收嵌套键的朴素正则）、`enum` 恰三值、`ai-manager.js` 的 manage_skill 工具范围内不出现 `path` 参数键、`executionMode: 'sequential'`、描述含两条 D-03 文案、exec 成功后含 `await this.syncAgentSystemPrompt()` 调用且**不含** `refreshSkills(`、execute 内不含 `requestActionConfirmation`。
    15. 跑 `node tests/test-manage-skill.js` 与 `node tests/test-ai-skills.js`，记录实测例数（Task 3 的账本要用）。
  </action>
  <verify>
    <automated>node --check ai-memory-manager.js  && node --check ai-skills-manager.js  && node --check ai-manager.js  && node -e 'const m=require("./ai-memory-manager.js");const a=m.scanInjectionPatterns("api_key: YOUR_KEY_HERE");const b=m.scanInjectionPatterns("api_key: YOUR_KEY_HERE",{includeCredentials:false});const c=m.scanInjectionPatterns("ignore all previous instructions",{includeCredentials:false});const bad=[];if(a.safe!==false)bad.push("单参调用行为变了（凭据组必须照跑）");if(b.safe!==true)bad.push("includeCredentials:false 未放行凭据形态");if(c.safe!==false)bad.push("注入组被误关（includeCredentials:false 只应关凭据组）");if(bad.length){console.error("扫描可选参不合格:\n"+bad.join("\n"));process.exit(1)}console.log("scanInjectionPatterns 字段分离 ok（默认向后兼容）")'</automated>
    <fails_when>非零退出（任一 `node --check` 语法错，或扫描断言失败），或出现 '扫描可选参不合格' 或任一条 `单参调用行为变了` / `includeCredentials:false` 行，或标准输出不含 "scanInjectionPatterns 字段分离 ok（默认向后兼容）"</fails_when>
    <automated>node --test test/memory/threat-scan.test.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（memory 威胁扫描回归失败），或 "# fail" 行 &gt; 0，或输出中无 "# pass" 行</fails_when>
    <automated>node -e 'const fs=require("fs");const src=fs.readFileSync("ai-manager.js","utf8");const bad=[];const i=src.indexOf("name: \x27manage_skill\x27");if(i&lt;0){bad.push("缺少 manage_skill 工具项")}else{const seg=src.slice(i,i+6000);const p=seg.indexOf("parameters:");const pb=seg.slice(p,seg.indexOf("required:",p));if(p&lt;0||pb.length&lt;20)bad.push("无法定位 parameters 块（提取口径失效）");else{for(const k of ["action","name","content","description"]){if(!new RegExp("\\b"+k+"\\s*:").test(pb))bad.push("schema 缺参数 "+k)}if(/\bpath\s*:/.test(pb))bad.push("parameters 出现 path 键（判据 2 失败）")}const e=seg.match(/enum:\s*\[([^\]]*)\]/);if(!e||e[1].replace(/[\s\x27"]/g,"")!=="create,update,delete")bad.push("action enum 不等于三动作")if(!seg.includes("executionMode: \x27sequential\x27"))bad.push("未声明 executionMode sequential")if(!seg.includes("await this.syncAgentSystemPrompt()"))bad.push("成功后未接刷新链（唯一权威入口）")if(/refreshSkills\(/.test(seg))bad.push("工具内直接调 refreshSkills（D-13 禁止，会变成三次重扫）")if(seg.includes("requestActionConfirmation"))bad.push("execute 内出现确认调用（D-01 锁定三动作一律自动）")if(!seg.includes("仅在用户明确要求"))bad.push("描述缺【仅在用户明确要求】文案")if(!seg.includes("优先增强已有技能"))bad.push("描述缺【优先增强已有技能】文案")}if(bad.length){console.error("manage_skill 工具项不合格:\n"+bad.join("\n"));process.exit(1)}console.log("manage_skill 工具项 schema/描述/刷新链 ok")'</automated>
    <fails_when>非零退出，或出现 'manage_skill 工具项不合格' 或任一条 `缺少 manage_skill` / `parameters` / `schema 缺参数` / `出现 path 键` / `enum` / `executionMode` / `未接刷新链` / `refreshSkills` / `确认调用` / `文案` 行，或标准输出不含 "manage_skill 工具项 schema/描述/刷新链 ok"</fails_when>
    <automated>node tests/test-manage-skill.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（create 脊椎用例失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 14（本任务落的用例地板），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `scanInjectionPatterns` 有了向后兼容的 `{ includeCredentials }` 可选参（单参调用行为逐字不变、`test/memory/threat-scan.test.js` 全绿）；`ai-skills-manager.js` 的 `LIMITS` 多出两项且既有三项未动，六个纯函数校验器与净化 / 扫描单点 / `buildSkillFileText` 已导出，`atomicWriteSkillFile` 成为 create 与 update 的唯一写路径，`createManagedSkill` 按【纯校验 → 先扫描 → 后净化 → 读盘撞名判定 → seeded 判定 → 数量闸 → createDir → tmp+rename → 失败按 madeDir 清理】的顺序落地；`ai-manager.js` 的 `manage_skill` 工具项 schema 恰为四参数且无 `path`、`executionMode` 为 sequential、描述含 D-03 两条文案、成功后只调一次 `syncAgentSystemPrompt()` 且不出现确认调用；`tests/test-manage-skill.js` 已建立且 create 脊椎全绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: update / delete 两动作 + 统一的【目标判定】+ 三入口 seeded 拒绝 + 数量闸（create 受限 / update·delete 不受限）</name>
  <files>ai-skills-manager.js, ai-manager.js, tests/test-manage-skill.js</files>
  <read_first>
    - `ai-skills-manager.js`（被改造文件）：Task 1 新增的 `isSeededName` / `makeManageSkillError` / `atomicWriteSkillFile` / `createManagedSkill` 与六项导出（**必须复用，不得各自再写一份判定**）、`:684-688`（`sourceTierOf` 的 Set 容错形态）、`:802-841`（`readSkillForInvocation` 的 `not_found` 判定与空正文拒绝）、`:841-853`（导出面 —— Task 1 已把新符号加进去，本任务继续补）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/env/nodejs.js:602-611`（`remove` 的 `recursive ?? false` / `force ?? false` ⇒ **delete 与失败清理都必须显式传 `{ recursive: true }`**；目标不存在且无 `force` 时返回 `not_found`）
    - `ai-manager.js`（被改造文件）：Task 1 新增的 `_buildManageSkillTool()` 与 create 分支、`:1512` / `:1532-1540`（`getSeededSkillNamesSafe()`）、`:5813-5931`（`memory` 工具的 `details` 回传形状先例）、`:2831-2859`（`syncAgentSystemPrompt()`）
    - `tests/test-manage-skill.js`（Task 1 新建）：`withTempRoot` / `makeEnv` / `writeSkill` / `SEEDED` 四件套 —— 本任务继续用，不新造夹具
    - `tests/test-ai-skills.js`：`:684-770`（`toUISkillEntry` 的 `promptOmitted` / `shadowed` / `disabled` 三字段断言组 —— `promptIncluded` 的判定必须**复用这三个字段**，不得新写一份边际成本计算）
    - `.planning/phases/49-manage-skill-ai/49-CONTEXT.md` 的 D-07（四类撞名与拒绝语义）/ D-10（两个闸的职责分工）/ D-12（原子写与清理）
  </read_first>
  <behavior>
    - `updateManagedSkill(env, { name, content, description, seededNames })`：目标存在且非 seeded、非 user → 成功；正文被**全量覆写**（旧正文消失、新正文就位）；目录内文件数恒为 1（原子替换，不产生第二个文件）；返回对象含 `action: 'update'`。
    - `update` 目标不存在 → `not_found`；目标是 seeded → `seeded_protected`；目标命中同名用户技能 → `user_owned_conflict`；三例都断言目标目录快照与操作前逐字相等（**不落盘、不删目录**）。
    - `update` 失败清理**不删除目标目录**（与 create 的区别）：注入一个必定失败的写（例如把目标目录在写之前 chmod 掉或把 dest 指向一个不存在且不可建的父路径）→ 目标目录仍存在且其 `SKILL.md` 内容与操作前逐字相等。
    - `deleteManagedSkill(env, { name, seededNames })`：目标存在且非 seeded、非 user → 递归删掉整个目录（含 AI 经 bash 加的 `scripts/` / `references/`）；返回对象含 `action: 'delete'`；删后 `env.exists(destDir)` 为假。
    - `delete` 目标不存在 → `not_found`；目标是 seeded → `seeded_protected`（**且 seeded 目录必须仍在**，内容逐字未变）；目标是同名用户技能 → `user_owned_conflict`（**且用户目录必须仍在** —— 这是 prohibition 1 的直接回归证据）。
    - seeded 拒绝在三入口**同形**：同一个 `SEEDED` 数组注入下，create / update / delete 对 `find-skills` 一律 `code === 'seeded_protected'`（判据 3 的三入口覆盖）。
    - 数量闸：预置 `MAX_MANAGED_SKILLS` 个非 seeded managed 目录后，第 `MAX_MANAGED_SKILLS + 1` 个 create 被拒 `limit_exceeded`；同一时刻的 update（改已有技能）与 delete（删已有技能）**仍然成功**（闸只管创建）；seeded 目录**不计入**计数（预置含 2 个 seeded 目录后仍需恰好 `MAX_MANAGED_SKILLS` 个非 seeded 才触发上限）。
    - `update` 的 `content` 首部带 frontmatter 块时，落盘结果是**单层** frontmatter（剥除生效），且 `description` 走同一条扫描 → 净化 → 写入路径。
    - `getSkillPromptIncluded(name, seededNames)`：技能集快照里存在该 name 且该条目 `shadowed !== true && disabled !== true && promptOmitted !== true` → `true`；条目不存在 / shadowed / disabled / promptOmitted → `false`；**判定不触发任何重扫**（纯读快照）。
  </behavior>
  <action>
    **A. `ai-skills-manager.js`：抽出统一的【目标判定】并落两动作**

    1. 新增内部函数 `async function resolveManagedTarget(env, name, seededNames)`（不导出），返回 `{ ok: true, destDir, destFile }` 或 `{ ok: false, code, message }`，判定顺序**固定**：
       - `isSeededName(name, seededNames)` → `{ ok: false, code: 'seeded_protected', message: <内置技能不可覆盖或删除 + 换名 / 只增强自己创建的可读原因> }`（**seeded 优先判定，不查磁盘**：内置身份来自播种登记表而非位置，判据 3）；
       - 同名用户技能 `exists(path.join(getAgentWorkspaceLazy().getSkillsDir(), name))` → `{ ok: false, code: 'user_owned_conflict', message: <同名用户技能优先级更高，改它会被永久遮蔽 / 请换名> }`；
       - `exists(destDir)` 为假 → `{ ok: false, code: 'not_found', message: <技能不存在，无法更新或删除> }`；
       - 否则 `{ ok: true, destDir, destFile }`。
       `createManagedSkill`（Task 1）的撞名分支**必须改为复用同一判定**（create 把 `not_found` 分支映射为【可以创建】而非错误）—— 保证四类撞名在三个动作里**只有一份判据**，这是 CONTEXT【一份校验器 + 一处不对称的调用口径】的具体落地。
    2. 新增导出 `async function updateManagedSkill(env, { name, content, description, seededNames } = {})`，顺序：
       - 语义按 **D-04** 固定：`update` = **全量覆写正文**，`content` 必填 = 完整新正文且**不含 frontmatter**（frontmatter 由 `buildSkillFileText` 生成 `name` + `description` 两行）；**不提供** `old_string` / `new_string` 等局部编辑参数（局部增强走【先 read 再全量写】或直接用既有 `edit` 工具，这条分工写进工具描述）。理由：加局部编辑参数会与沙箱内 `edit` 工具能力完全重叠（`edit` 在 `managed-skills/**` 上可用是既成事实），等于同一能力两份实现；且判据 2 明文【工具只接受 `name` 与 `content` / `description`】，多一个参数会让该判据的验收面（`properties` 键集合）失败。顺序：
       1. `validateManagedSkillName` / `validateManagedSkillDescription` / `validateManagedSkillContent` 三校验（同 create）；
       2. `scanSkillText(description, { includeCredentials: true })` → `scanSkillText(content, { includeCredentials: false })`（先扫描）；
       3. `sanitizeSkillDescription(description)`（后净化）；
       4. `resolveManagedTarget(...)` → 不 ok 即 throw `makeManageSkillError(code, message)`；
       5. `atomicWriteSkillFile(env, destFile, buildSkillFileText({ name, description: safe, content }))` —— **失败直接 throw，不做任何清理**（目标目录本就存在，其内容由 rename 的原子替换保证保持操作前状态）；
       6. 返回 `{ name, filePath: destFile, description: safe, action: 'update' }`。
    3. 新增导出 `async function deleteManagedSkill(env, { name, seededNames } = {})`，顺序：
       1. `validateManagedSkillName(name)`（name 仍须合法，否则 `invalid_name`）；
       2. `resolveManagedTarget(...)` → 不 ok 即 throw；
       3. `const res = await env.remove(destDir, { recursive: true });` → `res.ok !== true` 即 throw `makeManageSkillError('unknown', <删除失败 + 原因>)`；
       4. 返回 `{ name, filePath: destFile, action: 'delete' }`。
       注释必须写明：`remove` 的 `recursive` 默认是 **false**（`nodejs.js:602-611`），必须显式传 `{ recursive: true }`；递归删整目录是对齐 50/51 卸载口径的既定语义（含 AI 经 bash 加的 `scripts/` / `references/`）。
    4. 新增导出 `function getSkillPromptIncluded(name, seededNames)`（同步、零 IO）：`const entry = getSkillsSnapshot().skills.find((e) => e.skill.name === name);` → `return !!(entry && entry.shadowed !== true && entry.disabled !== true && entry.promptOmitted !== true);`。JSDoc 必须写明：**不新写边际成本计算** —— 直接复用加载管线已在 `toUISkillEntry` 上算好的 `promptOmitted` 三字段（48 D-12），因此与 prompt 段的实际归属同源；该判定不触发重扫，也不声称【已进提示词】是最终态（真正的回写在本轮成功出口）。
    5. 把上述四个符号加入 `module.exports`。

    **B. `ai-manager.js`：action 分派补全三动作与成功 details**

    6. `_buildManageSkillTool()` 的 `execute` 改为三分支：`create` / `update` / `delete` 分别调 `getAiSkillsManagerLazy()` 的对应函数（参数：`name` 与 `seededNames: this.getSeededSkillNamesSafe()` 恒传；`content` / `description` 只在 create / update 分支传）。`action` 不在三值白名单内 → throw 一个 `code: 'invalid_name'` 之外的**业务错误**（用 `makeManageSkillError` 不便跨模块，改为在工具层 throw `new Error(<中文：action 必须是 create / update / delete 之一>)` 并带 `code: 'invalid_name'`；理由是九码是**闭合白名单**，不新增第十码，而 action 非法属于参数不合法）。
    7. 成功返回值统一形状：`{ content: [{ type: 'text', text: <该动作的中文成功文案> }], details: { action, name, filePath, description?, promptIncluded? } }`：
       - `promptIncluded` 只对 create / update 计算（`getAiSkillsManagerLazy().getSkillPromptIncluded(name, this.getSeededSkillNamesSafe())`），delete 省略该键；
       - 成功文案**必须**含【下一条消息起可用 / 生效 / 不再可用】的时间语义（UI-SPEC 的 Copywriting Contract：D-13 的忙时语义对用户不可见，不说明会被读成【AI 建的技能没用】）；`promptIncluded === false` 时在末尾追加【该技能暂未进入模型提示词（技能段预算已满），仍可用 /skill:{name} 手动调用。】。
    8. 业务错误**照常向外 throw**（不 catch、不转成返回值）—— 失败态的 `code` / `tier` 转存为该次工具执行的元数据是 49-02 的职责（UI-SPEC 硬约束 2），本计划不实现它。**不要**在 execute 内 try/catch 并改写错误形态。
    9. 成功后仍**恰一次** `await this.syncAgentSystemPrompt();`（三个动作共用同一句，写在分支之后）。

    **C. `tests/test-manage-skill.js`：追加 update / delete / 数量闸用例组**

    10. 追加的用例（每例注释写明钉住的纪律）：
       - update 快乐路径 + 全量覆写（旧正文消失）+ 目录内文件数恒 1 + `action === 'update'`；
       - update 的三类拒绝（seeded / user / not_found）+ 三例的目录快照 `deepStrictEqual`；
       - **update 失败不删目录**：先造一个合法技能 → 人为让写路径失败（把 `destDir` 下的 `SKILL.md` 设为不可写，或在写前把 `destDir` 移除后重建为文件）→ 断言抛错且目标目录仍存在、其 `SKILL.md` 内容与操作前逐字相等；
       - delete 快乐路径（含 `scripts/` 子目录一并删除）+ `action === 'delete'`；
       - delete 三类拒绝（seeded / user / not_found）+ **seeded 与 user 目录必须仍在且内容逐字未变**；
       - seeded 三入口同形（同一 `SEEDED` 注入下 create / update / delete 对 `find-skills` 一律 `seeded_protected`）；
       - 数量闸：预置 `LIMITS.MAX_MANAGED_SKILLS` 个非 seeded managed 目录 + 2 个 seeded 目录 → 下一次 create 被拒 `limit_exceeded`、`code` 正确、提示含上限值；同一环境下 update / delete 成功；再断言 seeded 目录未被计入（否则上限会提前触发）；
       - `getSkillPromptIncluded` 的四个分支（存在且正常 → true；不存在 → false；`disabled` → false；`promptOmitted` → false），用一个 `_resetCacheForTest()` + 手工注入快照的用例覆盖 `promptOmitted` 分支。
    11. 跑 `node tests/test-manage-skill.js`，记录实测例数。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js  && node --check ai-manager.js  && node -e 'const m=require("./ai-skills-manager.js");const bad=[];for(const f of ["createManagedSkill","updateManagedSkill","deleteManagedSkill","getSkillPromptIncluded","validateManagedSkillName","validateManagedSkillDescription","validateManagedSkillContent","sanitizeSkillDescription","scanSkillText","buildSkillFileText"]){if(typeof m[f]!=="function")bad.push("缺导出 "+f)}if(!m.LIMITS||m.LIMITS.MAX_MANAGED_SKILLS!==50)bad.push("LIMITS.MAX_MANAGED_SKILLS 不等于 50")if(!m.LIMITS||m.LIMITS.MAX_SKILL_MD_BYTES!==65536||m.LIMITS.MAX_USER_SKILLS!==50||m.LIMITS.SKILLS_PROMPT_CHAR_BUDGET!==8000)bad.push("既有三项限额被改动（D-10 只允许加项）")const src=require("fs").readFileSync("ai-skills-manager.js","utf8");if(/require\(.electron.\)/.test(src))bad.push("ai-skills-manager.js 出现 electron 依赖（依赖纪律破坏）")if(src.includes("SKILL_THREAT_PATTERNS"))bad.push("提前实现了 Phase 51 的技能域威胁表")if(bad.length){console.error("导出面/限额不合格:\n"+bad.join("\n"));process.exit(1)}console.log("导出面 + LIMITS 加项 + 零 electron 依赖 ok")'</automated>
    <fails_when>非零退出，或出现 '导出面/限额不合格' 或任一条 `缺导出` / `LIMITS` / `既有三项限额被改动` / `electron 依赖` / `技能域威胁表` 行，或标准输出不含 "导出面 + LIMITS 加项 + 零 electron 依赖 ok"</fails_when>
    <automated>node -e 'const src=require("fs").readFileSync("ai-skills-manager.js","utf8");const bad=[];const bodies=(name)=&gt;{const i=src.indexOf("async function "+name+"(");if(i&lt;0)return "";const j=src.indexOf("\n}\n",i);return src.slice(i,j&lt;0?i+4000:j)};const del=bodies("deleteManagedSkill");if(!del.includes("recursive: true"))bad.push("delete 未显式传 recursive: true（remove 默认 false）");if(/env\.remove\([^)]*\)(?![\s\S]*force)/.test(del)===false)&amp;&amp;!del.includes("env.remove("))bad.push("delete 未使用沙箱 remove");const up=bodies("updateManagedSkill");if(up.includes("env.remove("))bad.push("update 失败路径出现目录删除（不得删目标目录）");for(const n of ["createManagedSkill","updateManagedSkill"]){const b=bodies(n);if(!b)bad.push("无法提取 "+n+" 方法体（口径失效）");else{if(!b.includes("validateManagedSkillName"))bad.push(n+" 缺 name 校验");if(b.indexOf("scanSkillText")&lt;0)bad.push(n+" 缺扫描单点")}}if(src.indexOf("resolveManagedTarget")&lt;0)bad.push("缺统一的目标判定函数");if(bad.length){console.error("三动作形态不合格:\n"+bad.join("\n"));process.exit(1)}console.log("三动作形态 ok（统一目标判定 / 扫描单点 / update 不清理目录）")'</automated>
    <fails_when>非零退出，或出现 '三动作形态不合格' 或任一条 `delete 未显式` / `未使用沙箱 remove` / `update 失败路径` / `无法提取` / `缺 name 校验` / `缺扫描单点` / `缺统一的目标判定` 行，或标准输出不含 "三动作形态 ok（统一目标判定 / 扫描单点 / update 不清理目录）"</fails_when>
    <automated>node tests/test-manage-skill.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（update / delete / 数量闸用例失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 28（Task 1 地板 14 + 本任务 ≥14），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `update` 与 `delete` 与 `create` 共用**同一个**目标判定（seeded 优先判登记表 → user 优先 → not_found → 放行），因此四类撞名与 seeded 保护三入口只有一份判据；`delete` 显式 `recursive: true` 递归删整目录，`update` 失败不清任何目录；数量闸只约束 create（以读盘的【非 seeded managed 目录数】为统计对象），update / delete 不受限且 seeded 不计入；`getSkillPromptIncluded` 复用加载管线的 `promptOmitted` / `shadowed` / `disabled` 三字段而不另写边际成本计算；工具层三动作分派与成功 details 形状落地，成功文案含时间语义；`tests/test-manage-skill.js` 的 update / delete / 数量闸用例全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: 端到端行为断言与两条护栏 —— 刷新链时序、幽灵技能同源、越界不触及，并刷测试账本</name>
  <files>tests/test-manage-skill.js, tests/test-ai-skills.js</files>
  <read_first>
    - `tests/test-ai-skills.js`：`:3160-3190`（K 组的 `promptCtx` 夹具与 `rescanCalls === 2` 断言 —— 本任务的刷新链用例**照抄该范式**，不新造夹具风格）、`:3184-3188`（忙时重扫一次 + 出口补刷一次的既有断言口径）、`:2758-2781`（`skillResolveCtx` 的 `Object.create(prototype)` + own-property 包装计数纪律）、`:1249-1254`（`windowManager.broadcast` 打桩收集频道名的写法）、`:1127-1205`（依赖纪律与导出面源码断言组）、`:684-770`（`promptOmitted` 的既有断言）
    - `ai-manager.js`：`:2831-2859`（`syncAgentSystemPrompt()` 方法体四条 —— 本任务只加护栏不改它）、`:2905-2919`（`_flushDeferredSkillsPrompt()`）、`:1084-1091` / `:1329-1333`（两处成功出口）
    - `ai-skills-manager.js`：`:140-190`（`createSkillsEnv` 的加载期字节闸 —— 幽灵技能护栏的判据对象）、`:298-330`（`isDescriptionUnusable`）、`:446-500`（`refreshSkills` 的诊断产出）与 Task 1/2 新增的动作函数
    - `agent-workspace.js`：`:38-114`（`getWorkspaceDir` / `getSkillsDir` / `getManagedSkillsDir` / `getAttachmentsDir` / `getAiMemoryDir` —— 越界护栏的判据对象）
    - `tests/test-manage-skill.js`（Task 1/2 已建）：`withTempRoot` / `makeEnv` / `writeSkill` / `SEEDED`
    - `.planning/phases/49-manage-skill-ai/49-VALIDATION.md`：§关键可验证主张（V1–V9）与 §Wave 0 Requirements —— 本任务的用例是它们的落地
  </read_first>
  <behavior>
    - **刷新链时序（MGMT-05 / 判据 1 的验收面）**：在 `promptCtx` 风格夹具上直接执行 `manage_skill` 的 create 分支后 —— `ctx._skillsPromptDirty === true` 且 `ctx.agent.state.systemPrompt` **不含**新技能名；随后 `aiManager.prototype.prompt.call(ctx, '你好')` 成功后 —— `_skillsPromptDirty === false`、`agent.state.systemPrompt` 含新技能名且 `=== aiManager.buildSystemPrompt()`、`broadcast` 捕获 `['skills:changed']` **恰一次**、`ctx.rescanCalls === 2`（**不是 3**）。
    - **脏标记为假时零成本**：普通消息（不调 manage_skill）走同一出口时 `ctx.rescanCalls === 0`。
    - **幽灵技能护栏（写侧与加载期闸同源）**：① `createManagedSkill` 对 `'a'.repeat(65537)` 的正文直接拒（写侧预筛）；② 手工在 `managed-skills/<name>/SKILL.md` 造一个超限文件后 `refreshSkills` 产 `realm_skill_md_too_large`（或等价的 oversize 诊断）⇒ 两侧判的是同一条上限；③ description 侧同理：写侧拒超 1024，而手工造的超长 description 条目被 `isDescriptionUnusable` 整条跳过。
    - **越界护栏（判据 4）**：任一次 create / update / delete 前后，`agent-workspace/skills/`、`ai-memory/`、`attachments/` 三个目录的递归文件清单与内容快照**逐字相等**。
    - **`.tmp/` 的已知副作用被显式记录**：断言原子写之后 `.tmp/` 下出现一个新目录（而不是断言它被清理）—— 这是 A2 裁决【不清理】的可观察形态，防止后人误以为它被清掉了。
    - **零 electron 依赖的运行时证据**：`node tests/test-manage-skill.js` 在纯 Node 下能 `require` 到 `ai-skills-manager.js` 并跑完全部动作用例（`getSeededSkillNamesSafe()` 从未被调用 —— 若被调用，降级告警 `[Realm AI] 读取随包内置技能名失败` 会出现在输出里）。
  </behavior>
  <action>
    **A. `tests/test-ai-skills.js`：追加刷新链行为用例组（照抄 K 组范式）**

    1. 在 K 组之后新增 `describe('L 组 · Phase 49 manage_skill 的刷新链', ...)`，复用 K 组的 `promptCtx(env, { dirty })` 夹具（若该夹具是 K 组内局部定义，则提到文件级或在新组内复制**同一份**实现 —— 不得写第二套 `Object.create` 拼装，否则两组的隔离语义会漂移）：
       - **L1（靶心）**：`withTempRoot` → `workspace.ensureWorkspaceDir()` → 建 env → `ctx = promptCtx(env)` → 打桩 `broadcast` 收集频道名 → 调 `aiManager.prototype` 上的工具执行入口（若工具 `execute` 是 `_buildManageSkillTool()` 的返回值，则 `const tool = ctx._buildManageSkillTool(); await tool.execute('call-1', { action: 'create', name: 'flush-probe', description: '落地用描述', content: '正文' });`）→ 断言 `ctx._skillsPromptDirty === true` 且 `ctx.agent.state.systemPrompt` 不含 `flush-probe` → `await aiManager.prototype.prompt.call(ctx, '你好')` → 断言 `_skillsPromptDirty === false`、`systemPrompt.includes('flush-probe')`、`systemPrompt === aiManager.buildSystemPrompt()`、广播频道 `deepStrictEqual(['skills:changed'])`、`ctx.rescanCalls === 2`。
       - **L2（零成本）**：同前但不调工具 → `prompt()` 成功后 `ctx.rescanCalls === 0`、`systemPrompt === 'OLD'`。
       - **L3（次数账）**：源码扫描断言 `manage_skill` 的工具范围内 `await this.syncAgentSystemPrompt()` **恰 1 处**、`refreshSkills(` **0 处**（D-13：不要照 ROADMAP 字面写两次调用）。
    2. 若 `promptCtx` 夹具依赖 `this.sandboxEnv` 而工具层需要它，夹具里补 `sandboxEnv = env`；**不要**为了本组去改 `_buildManageSkillTool()` 的可测试性（把工具构造写成可独立调用的方法本身就是既有风格的好处）。

    **B. `tests/test-manage-skill.js`：追加幽灵技能护栏与越界护栏**

    3. 幽灵技能护栏三例：
       - 写侧：`validateManagedSkillContent('a'.repeat(65537))` → `code === 'oversize'`；`validateManagedSkillDescription('x'.repeat(1025))` → `code === 'invalid_description'`；
       - 读侧对照：用 `writeSkill` 手工造一个正文超 64 KiB 的 `SKILL.md` → `refreshSkills(env, { rootDirs: [managedDir, skillsDir] })` 后 `getSkillsSnapshot().diagnostics` 含 oversize 类诊断（字段名以 `:161-183` 的 `droppedNotices` 实际产出为准）；
       - 读侧对照 ②：手工造一个 `description` 超长的条目 → 该条目**不在** `getSkillsSnapshot().skills` 里（`isDescriptionUnusable` 整条跳过）。
       护栏的写法要点：断言的是【两侧判同一条上限】这一**关系**（写侧拒的边界值 == 读侧丢弃的边界），不是两条独立断言各自成立。
    4. 越界护栏：写一个 `snapshotTree(dir)` 小工具（递归 `readdirSync` + `readFileSync` 收集 `{相对路径: 内容}`，目录不存在时返回 `{}`），在 create / update / delete 各自的最外层 try 前后各取一次快照，断言 `skills/` / `ai-memory/` / `attachments/` 三者 `deepStrictEqual`。**注意** `managed-skills/` 自身**必须**变化 —— 所以不要把它纳入这三份快照。
    5. `.tmp/` 副作用登记：原子写之后断言 `fs.readdirSync(workspace.getTmpDir())` 的数量比操作前 **+1**（A2 裁决的可观察形态），并在用例注释里写明【这是已知且接受的累积，`.tmp/` 不是技能扫描根；不得因此加清理代码】。
    6. 在文件末尾加一条【零降级告警】用例：跑完所有用例后无法直接断言 stdout，改为**反向断言**：`require('ai-manager.js')` 的 `getSeededSkillNamesSafe` 若被调用会 `console.warn`；本文件在文件头用 `t.after` 安装一个 `console.warn` 收集器（只在本文件的 describe 级钩子里），断言收集结果中**不含** `读取随包内置技能名失败` —— 这条是【测试没有误走降级路径】的机械护栏（Pitfall 10）。

    **C. 账本（两处，取实测值，不按推算填）**

    7. 记录 `node tests/test-manage-skill.js` 与 `node tests/test-ai-skills.js` 的 `# tests` 实测值。**本计划不改** `AGENTS.md` 与 `docs/product/ai-skills.md`（文档与测试清单的维护约定收口归 49-03），只把实测值写进 SUMMARY 的 `actuals` 与正文。
  </action>
  <verify>
    <automated>node tests/test-manage-skill.js 22>&1 | grep -E "^# (tests|pass|fail|skipped)"</automated>
    <fails_when>非零退出（幽灵技能 / 越界 / 数量闸 / 校验器矩阵任一失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 40（Task 1 地板 14 + Task 2 ≥14 + 本任务 ≥12），或输出中无 "# pass" 行</fails_when>
    <automated>node -e 'const out=require("child_process").execSync("node tests/test-manage-skill.js 22>&1",{encoding:"utf8"});const bad=[];if(/读取随包内置技能名失败/.test(out))bad.push("测试误走了 getSeededSkillNamesSafe 的降级路径（seeded 保护会假绿）");if(!/^# fail 0$/m.test(out.replace(/# fail (\d+)/,"# fail $1"))){const m=out.match(/# fail (\d+)/);if(!m||Number(m[1])!==0)bad.push("测试有失败项")}const src=require("fs").readFileSync("tests/test-manage-skill.js","utf8");for(const t of ["seeded_protected","user_owned_conflict","already_exists","not_found","limit_exceeded","oversize","unscannable","invalid_name","invalid_description"]){if(!src.includes(t))bad.push("缺原因码断言 "+t)}for(const t of ["ai-memory","attachments","getTmpDir","realm_skill_md_too_large","isDescriptionUnusable"]){if(!src.includes(t))bad.push("缺护栏素材 "+t)}if(!src.includes("seededNames"))bad.push("未直接注入 seededNames")if(bad.length){console.error("护栏与值域矩阵不合格:\n"+bad.join("\n"));process.exit(1)}console.log("九码值域 + 幽灵技能 + 越界护栏齐备，且未走降级路径")'</automated>
    <fails_when>非零退出，或出现 '护栏与值域矩阵不合格' 或任一条 `降级路径` / `测试有失败项` / `缺原因码断言` / `缺护栏素材` / `未直接注入` 行，或标准输出不含 "九码值域 + 幽灵技能 + 越界护栏齐备，且未走降级路径"</fails_when>
    <automated>node tests/test-ai-skills.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（L 组或既有 147 例回归失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 150（既有地板 147 + L 组 ≥3），或输出中无 "# pass" 行</fails_when>
    <automated>node --test tests/test-agent-workspace.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（沙箱回归失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 21（既有地板），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    刷新链的时序断言落在行为上：工具执行期脏标记置位且 prompt 不含新技能名 → 本轮成功出口脏标记归假、prompt 含新技能名且等于 `buildSystemPrompt()`、`skills:changed` 恰广播一次、`rescanCalls === 2`；脏标记为假时零重扫；源码扫描证明工具范围内只调一次 `syncAgentSystemPrompt()` 且零 `refreshSkills(`。幽灵技能护栏断言【写侧预筛的边界值 == 读侧丢弃的边界值】这一关系（正文 64 KiB 与 description 1024 两处），越界护栏断言 `skills/` / `ai-memory/` / `attachments/` 在三动作前后逐字不变，`.tmp/` 的累积被显式登记为已知副作用，且全部用例未走 `getSeededSkillNamesSafe()` 的降级路径。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → 工具参数（`manage_skill` 的 `action` / `name` / `content` / `description`） | **不可信输入**：MGMT-03 明文【LLM 参数不可信】，JSON Schema 的 `pattern` / `maxLength` 只是给模型看的提示，服务端必须独立再校验一次 |
| 工具层 → `ai-skills-manager.js`（进程内） | 工具只做参数转发，判定与写入全在 manager；这条边界保证校验器只有一份实现（50/51 可复用） |
| manager → 磁盘（经沙箱 `env`） | 所有路径都由 `path.join(managedDir, name)` 计算，`name` 已过 `^[a-z0-9-]+$` 且路径分隔符不在合法字符集内；写目标另有沙箱 `resolveInside` 双基准 + realpath 复核 |
| 磁盘状态 → 判定（撞名 / seeded / 数量闸） | 一律读盘（`env.exists` / `env.listDir`），**不用缓存快照** —— bash 可随时改盘（P8 第 6 条） |
| 技能 `description` → 每次请求的 system prompt | **零交互注入通道**（P3）：description 无条件进每个请求的 system prompt，故它必须跑注入组 + 凭据组两组扫描 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01-01 | Tampering | 路径穿越 / 写逃逸（模型试图传间接路径或让 `name` 含分隔符） | high | mitigate | 工具**不吃 `path`**（只有 `name`，路径由 manager `path.join` 计算 —— ARCHITECTURE Anti-Pattern 1：沙箱在这里提供不了保护，接口设计才是边界）；`name` 过 `^[a-z0-9-]+$`（不含 `/`、`\`、`.`）且长度 ≤ 64；沙箱 `resolveInside` 双基准 + realpath 复核 + `renameFile` 的 dest 逃逸拒绝（RESEARCH E5 实测 `permission_denied`） |
| T-49-01-02 | Tampering | Prompt injection 持久化（`description` 作为零交互注入通道进每次请求的 system prompt） | high | mitigate | `description` 跑 `INJECTION_PATTERNS` + `CREDENTIAL_PATTERNS` 两组；命中即 `throw`（不落盘）；净化剥控制字符与零宽字符；**顺序铁律：先扫描、后净化**（D-09，零宽变体用例钉死）。`SKILL_THREAT_PATTERNS` 的技能域扩展归 Phase 51 —— 本阶段只把扫描点接成单点 |
| T-49-01-03 | Information Disclosure | 凭据入库即外发（`description` 无条件进每个请求） | high | mitigate | `CREDENTIAL_PATTERNS` 命中即拒（与两层记忆同构的同一论据）；**`content` 只跑注入组**，因为技能文档合法地会写配置示例（`api_key: YOUR_KEY_HERE` 实测会被凭据组命中 —— 跑两组会误伤合法技能创建）。错误消息不回显被拒内容原文（prohibition 3） |
| T-49-01-04 | Tampering / Spoofing | 内置技能被冒名覆盖（AI 建同名技能顶替随包技能） | high | mitigate | seeded 保护按**播种登记表**（`seededNames` 注入）而非目录位置判定，且 **seeded 优先于其它三类判定**；`create` 对任何来源的已存在目标一律拒绝（独占创建，D-07，不是 upsert）；`update` / `delete` 对 seeded 同样拒绝（三入口同形，判据 3） |
| T-49-01-05 | Repudiation | 静默遮蔽（AI 建同名 managed 被 user 技能永久遮蔽 ⇒ 静默无用） | medium | mitigate | D-07 的第 2 类拒绝（`user_owned_conflict`）+ 可读原因【同名用户技能优先级更高，AI 创建的会被永久遮蔽，因此未写入】；加载管线的 `applyShadowing` 仍产出 `realm_shadowed` 诊断作为二线可见性 |
| T-49-01-06 | Denial of Service | 资源耗尽（无限建技能 ⇒ 磁盘与每次 Agent 重建的重扫成本无上限） | high | mitigate | `LIMITS.MAX_MANAGED_SKILLS = 50`，统计对象是**非 seeded 的 managed 目录数**（读盘 `env.listDir`）；到顶即拒 + 可操作提示；`update` / `delete` 不受该闸限制（否则到顶后无法自救）。**不用 prompt 段预算做创建拒绝**（会与 48 D-12 冲突：超预算技能仍可显式调用） |
| T-49-01-07 | Tampering | 失败留下半成品（半成品目录 / 半成品文件被加载管线当成合法技能） | medium | mitigate | 原子写 = 文件级 `rename`（POSIX 对已存在目标原子替换）；`create` 失败的清理**只在本次确实新建了目录时**执行（局部布尔标记 —— 否则会误删不属于本次操作的数据）；`update` 失败不删目标目录（题中之义：其内容由 rename 的原子性保持操作前状态）。回归证据：操作前后 `readdirSync` 的 `deepStrictEqual` |
| T-49-01-08 | Tampering | 幽灵技能（落盘成功但加载管线整条跳过 ⇒ 磁盘上留一个永不生效的文件） | medium | mitigate | 写入侧预筛与加载期闸口**同源同值**：正文字节用 `Buffer.byteLength(content, 'utf8')` 比 `LIMITS.MAX_SKILL_MD_BYTES`（加载期按 `FileInfo.size` 计，同为 UTF-8 字节）；`description` 用 `LIMITS.MAX_SKILL_DESCRIPTION_CHARS`（注释声明与 SDK `MAX_DESCRIPTION_LENGTH` 对齐）。护栏断言的是两侧边界值的**相等关系** |
| T-49-01-09 | Elevation of Privilege | 越界写其它工作区路径（`ai-memory/MEMORY.md`、`attachments/` 快照、用户技能目录） | high | mitigate | 工具不吃 `path` ⇒ 接口层就无法表达其它目标；写入面恒为 `managed-skills/<name>/`（prohibition 1）；越界护栏对 `skills/` / `ai-memory/` / `attachments/` 三目录做操作前后逐字快照比对 |
| T-49-01-10 | Denial of Service | 父目录缺失导致 rename 必失败 / `createDir` 幂等被误当作撞名判据 | medium | mitigate | `create` 先 `createDir`（`renameFile` 到缺失父目录实测返回 `not_found`，故它是硬前置）；撞名判定**一律**走 `env.exists`（`createDir` 对已存在目录实测返回 `ok: true`，用它判撞名会把既有技能静默覆写 —— 回归护栏用例把这条前提钉住） |
</threat_model>

<verification>
- `node --check` 三个被改的 JS 文件全部通过；`node tests/test-manage-skill.js` → `# fail 0`、`# pass ≥ 40`；`node tests/test-ai-skills.js` → `# fail 0`、`# pass ≥ 150`；`node --test test/memory/threat-scan.test.js` → `# fail 0`；`node --test tests/test-agent-workspace.js` → `# fail 0`、`# pass ≥ 21`
- 扫描可选参：`scanInjectionPatterns(text)` 单参行为逐字不变（凭据组照跑）；`{ includeCredentials: false }` 放行 `api_key: YOUR_KEY_HERE` 但仍拦 `ignore all previous instructions`
- 工具项：`properties` 顶层键集合恰为 `{action, name, content, description}`（深度计数收集，不是朴素正则）；`enum` 恰三值；**无 `path` 键**；`executionMode: 'sequential'`；描述含 D-03 两条文案；执行体内 `await this.syncAgentSystemPrompt()` 恰 1 处、`refreshSkills(` 0 处、`requestActionConfirmation` 0 处
- 限额单源：`LIMITS` 既有三项数值不变（65536 / 50 / 8000），新增 `MAX_MANAGED_SKILLS = 50` 与 `MAX_SKILL_DESCRIPTION_CHARS = 1024`；`ai-skills-manager.js` 无 `require('electron')`；无 `SKILL_THREAT_PATTERNS`
- 三动作同形：seeded 三入口一律 `seeded_protected`（登记表判定，含【位置在 managed 但列入 seededNames】的正例与【未列入】的反向例）；四类撞名共用 `resolveManagedTarget` 一份判据；`delete` 显式 `recursive: true`；`update` 失败不删目录
- 刷新链：`rescanCalls === 2`（不是 3）、脏标记流转正确、`skills:changed` 恰广播一次、脏标记为假时零重扫
- 护栏：幽灵技能（写侧边界 == 读侧边界）、越界（三目录逐字不变）、`.tmp/` 累积被登记、测试未走 `getSeededSkillNamesSafe()` 降级路径
- 不动项：`agent-workspace.js` / `builtin-skills-seeder.js` 零 diff；`syncAgentSystemPrompt()` 与 `_flushDeferredSkillsPrompt()` 的方法体逐字未改（46-04 / 48-08 的既有断言继续绿）；`src/renderer.js` / `src/styles/main.css` / `src/skill-picker-model.js` 零 diff（归 49-02）
- 指纹提示：本计划改 3 个源文件 + 1 个新测试文件 + 1 个既有测试文件 ⇒ 49 的阶段指纹在本计划提交后会 stale，属**预期**（收尾时重算）
</verification>

<success_criteria>
- AI 可经 `manage_skill` 的 create / update / delete 对 managed 技能落盘，且三动作共用同一份目标判定与同一条原子写路径（MGMT-01 / MGMT-03）
- 工具 `parameters.properties` 恰为 `{action, name, content, description}` 且无 `path`；服务端对 name / description / content 独立二次校验并给出九码之一（MGMT-02）
- seeded 内置技能在 create / update / delete 三入口一律被拒，判定依据是**播种登记表**（`seededNames` 注入）而非目录位置，并给出可读原因（MGMT-04 / 判据 3）
- 写入经沙箱 `env.renameFile` 获得 dest 双基准路径校验；失败不留半成品；`skills/` / `ai-memory/` / `attachments/` 不被触及（判据 4）
- 成功后只调一次 `syncAgentSystemPrompt()`，新技能集在本轮成功出口回写、下一条消息可见（`rescanCalls === 2`）（MGMT-05 / 判据 1）
- 工具描述含【仅在用户明确要求时调用】与【优先增强已有技能】两条文案，且不写进 `REALM_SYSTEM_PROMPT`（MGMT-06 / D-03）
- 九个 edge-probe 的 unresolved 项全部有显式裁决与落点，无静默丢弃
- 校验器住在 `ai-skills-manager.js` 且零 electron 依赖 ⇒ Phase 50/51 可直接 `require`（ROADMAP 安全门禁）
- 全部验证命令用 `node tests/<file>.js` 显式形式，不引用 `npm test`
</success_criteria>

<artifacts_this_phase_produces>
> **Artifacts this plan produces** —— 本计划新建的符号与文件（供 plan-review-convergence 的 source-grounding pass 排除；新增符号被误报为【引用了不存在的符号】时以本节为准）。

**`ai-memory-manager.js`（改签名，非新符号）**
- `scanInjectionPatterns(content, options)` 的新增第二参 `options = { includeCredentials = true }`

**`ai-skills-manager.js`（新符号）**
- `LIMITS.MAX_MANAGED_SKILLS`、`LIMITS.MAX_SKILL_DESCRIPTION_CHARS`
- `validateManagedSkillName(name)`
- `validateManagedSkillDescription(text)`
- `validateManagedSkillContent(text)`
- `sanitizeSkillDescription(text)`
- `scanSkillText(text, options)`
- `buildSkillFileText({ name, description, content })`
- `isSeededName(name, seededNames)`（模块内部，不导出）
- `makeManageSkillError(code, message, extra)`（模块内部，不导出）
- `atomicWriteSkillFile(env, destFile, text)`（模块内部，不导出）
- `resolveManagedTarget(env, name, seededNames)`（模块内部，不导出）
- `createManagedSkill(env, { name, content, description, seededNames })`（导出）
- `updateManagedSkill(env, { name, content, description, seededNames })`（导出）
- `deleteManagedSkill(env, { name, seededNames })`（导出）
- `getSkillPromptIncluded(name, seededNames)`（导出）

**`ai-manager.js`（新符号）**
- `AIManager.prototype._buildManageSkillTool()`（新私有方法）
- 工具项字面名 `manage_skill`；参数键 `action` / `name` / `content` / `description`

**`tests/test-manage-skill.js`（新文件）**
- 夹具 `withTempRoot(t)` / `makeEnv(root)` / `writeSkill(dir, name, opts)` / `snapshotTree(dir)`
- 常量 `SEEDED = ['find-skills', 'skill-creator']`

**`tests/test-ai-skills.js`（新增断言组与夹具）**
- `L 组 · Phase 49 manage_skill 的刷新链`（L1 靶心 / L2 零成本 / L3 次数账）
- 工具项 schema 源码断言组（顶层键集合 / enum / 无 path / sequential / 描述两条文案 / 刷新链唯一）
- 复用（不自建）的既有夹具：`promptCtx` / `withTempRoot` / `writeSkill` / `readSource` / `methodBody`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/49-manage-skill-ai/49-01-SUMMARY.md` when done
</output>

