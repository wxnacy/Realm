---
phase: 50-api-skills
plan: 04
type: execute
wave: 3
depends_on: [50-01, 50-02]
files_modified:
  - src/settings-page.js
  - src/settings.html
  - src/styles/main.css
  - src/skill-picker-model.js
  - tests/test-skills-management.js
  - tests/test-skill-picker-model.js
autonomous: true
requirements: [USER-01, USER-02, USER-06]
user_setup: []

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

key-decisions:
  - '**「仅显式」的 label 与 title 必须提升为 `src/skill-picker-model.js` 的单源冻结表**（UI-SPEC 硬前置条件）：今天它们是 `src/renderer.js:10599` 面板行里的**内联字面量**，而设置页是本阶段新增的**第二个消费者** ⇒ 再写一份就是 48/49 反复付过代价的「第二份拷贝」（`49-UI-REVIEW` 的 `W1-04` 同形态）。提升形状照 `TIER_BADGE` 同族（`{ label, title }` + `Object.freeze` + 挂到同一个 `api` 对象），**具体命名交执行期**（建议 `EXPLICIT_TAG`）。**面板侧渲染结果逐字不变**（只是从字面量改成引用同一常量）'
  - '⚠️ **该提升会同时打翻三条既有断言，三条的改写方向各不相同，必须一并做**（UI-SPEC 已定稿）：① `tests/test-skill-picker-model.js:1265-1269`（`panelBody.split(''''仅显式'''').length - 1 === 1`）⇒ 面板改引用常量后**字面量不再出现在 `panelBody`** ⇒ 计数变 **0**，应改为「断言单源表的值恰为 `''''仅显式''''`」+「断言面板侧**引用**该单源常量」；② `:1270-1274`（title 字面量计数）⇒ 同上；③ `:1278`（`modelSrc.includes(''''仅显式'''') === false`）⇒ **被新设计直接证伪**（单源表必须把它写在 `skill-picker-model.js` 里），应改为「断言**单源表的值**恰为 `''''仅显式''''`」+「断言它**不在** `STATUS_TEXT` 的值集合里」—— 把「不混放」的原意**从「文件级零命中」改为「表级值域隔离」**（前者在新设计下**字面不可满足**，留着会让测试永久红）'
  - '**卸载确认弹框走 `realm://` 的既有豁免范式**（AGENTS.md「内部页面 CSP」+「弹框居中约定」明文豁免）：`.ai-modal-overlay`（既有 `display: none` 规则）+ `.ai-modal`，打开时 JS CSSOM 置 `display: ''''flex''''`、关闭置 `''''none''''`。**绝不**写 markup 内联 `style="display:none"`；**绝不**把 overlay 类用到 `<dialog>` 元素上（会把盒子钉在 `top:0; left:0`）；**不做**完整焦点陷阱（与设置页既有的 2 个 `.ai-modal-overlay` 弹框同范式 —— 这是**有理由的边界**，不是静默省略）'
  - '**失败 / 拒绝反馈走 inline hint，不新建 toast 基建**（D-06）：设置页在 `realm://` guest 内、`realmAPI` 不可用，主窗口 toast 覆盖不到它；本区**不调用**设置页既有的 `#toast`。**必须补一条既有先例未覆盖的加固**：2 秒复位定时器保存在模块级变量，**每次设置 hint 前先 `clearTimeout` 上一次** —— 连续操作（快速连点两个开关）时陈旧复位**不得**清掉新消息；文本与色调必须**一并设置**（不允许「只改文字不改色」⇒ 否则会出现绿色文案说失败）；空文本时用 **CSSOM** 置 `display: ''''none''''`、有文本置 `''''block''''`（不走 markup 内联 style、不用 `''''''''` 回落）'
  - '**「一次操作只影响一行」是可见承诺，因此重渲染必须还原滚动位置与焦点**（UI-SPEC 契约）：`set-disabled` / `uninstall` 的响应即最新投影 ⇒ 就地重渲染（**零二次请求**）会重建整棵行 DOM ⇒ 必须记录并回写 `.settings-content.scrollTop`，并按 `data-skill-name` 把焦点归还到**同一技能行**的开关（找不到则不移动焦点）；卸载成功行已消失 ⇒ 焦点不移动'
  - '**折叠态是「类 + 属性双写、单一赋值点」**（UI-SPEC 明确）：显隐规则踩在 48 的既有 `.collapsed` 类规则上（`main.css:5872-5874` / `5889-5891`），改成属性驱动会**改动 48/49 两个实名宿主** ⇒ 取舍是「保留既有类 + 强制单点同步」：**一个折叠 = 一个赋值函数**，函数体内**同时**写 `classList.toggle(''''collapsed'''', collapsed)` 与 `setAttribute(''''aria-expanded'''', String(!collapsed))`；全页**禁止**出现第二处单独改其一的地方'
  - '**诊断详情区复用折叠块家族但只取「shell + body」，不取 header**（UI-SPEC 定稿）：本页的开关已在**第一行的诊断徽标**（离用户更近、且必须在行内可见）。再渲染一个 header 会产出「一个区域两个控件、两份状态」（`aria-expanded` 需双向同步）—— 那是纯粹的漂移面，不是复用。详情区默认**折叠**；徽标是 `<button type="button">` + `aria-expanded` + `aria-controls`，chevron 的旋转**以 `aria-expanded` 为唯一状态源**（本页 chevron 是**新类**，没有既有类规则要兼容 ⇒ 可以让属性成为唯一状态源）'
  - '**模块级汇总条相反：用完整家族（有 header）、默认展开**（D-11 + 「禁止静默失败」纪律）：`errors[]` 是**无归属技能**的模块级容器（46 D-07），塞进行内不可能；默认折叠会把异常状态藏起来。header 是它的开关，须带 `role="button"` + `tabindex="0"` + 随态 `aria-expanded` + Enter / Space（`preventDefault` 防 Space 滚动）'
  - '**注入纪律（不扩大 `TD-48-01` 的缺口）**：本页新增的插值面**全部**走 DOM API（`textContent` / `el.title = …` / `setAttribute`）。`escapeHtml`（`src/settings-page.js:1657`）**只转义 `& < >` 不转义引号**且该挂账**仍开**（用户已裁决延后、49 未处置）⇒ 任何属性上下文插值都必须走 `element.title = value` / `setAttribute`（DOM 属性赋值不走 HTML 解析）。**唯一**允许走 markup 直写的是**静态字面量**（区说明里的 `<code>manage_skill</code>` / `<code>/</code>` / `<code>SKILL.md</code>`）'
  - '**状态文案 / 徽标文案 / 限额数值三者零新增**：状态标注取 **`window.SkillPickerModel.pickStatusKey(item)`**（50-01 落在 `src/skill-picker-model.js` 的**单源链函数**，链序 `disabled > shadowed > overLimit > promptOmitted`，**不含 `nameClash`** —— 它依赖本地命令表 `SLASH_COMMANDS`（`src/renderer.js:344-347`，浏览器脚本、guest 拿不到），是 `/` 面板独有维度）再查 `STATUS_TEXT`；**本页不得重写这条 if 链**；徽标查 `TIER_BADGE`；失败文案按 `code` 查**本页的闭合白名单表**（`not_found` / `not_user_owned` / `invalid_name` / `BODY_TOO_LARGE` + 表外兜底），**不解析 `message`**'
  - '**唯一非 4 倍数的新增间距值只有一条**：`.skill-manage-diag-badge` 的 `padding: 1px 4px`（UI-SPEC B 表已登记：11px 文本按钮与三档徽标 / `仅显式` 在首行并列，垂直内边距取 4px 会把 chip 撑到约 23px、超出宿主行高）。其余新增间距一律 4 / 8 / 16'

must_haves:
  truths:
    - '[E2] 模块级汇总条 `#skillManageSummary` 是 `errors[]` 的承载面：0 条时**整条不渲染**（不占位、无空态文案）；常态展开显示「技能加载问题（{N}）」；每条显示级别标签（错误 / 警告 / 提示，闭合白名单、表外级别**不渲染标签**）+ 可读 `message` + `realm_*` 诊断码；超长 `message` 落在既有 `.ai-skill-content-box-body`（`max-height: 260px; overflow-y: auto`）的内层滚动里，不带动整页；自身无异步取数与中间态（随列表响应原子填充）'
    - '[E5] 行首行的动态态：启停开关在途时**立即乐观翻转** `.on` / `aria-checked` 并置 `disabled`（`opacity: .6` / `cursor: progress`），**不做**全局遮罩、**不做**列表 loading 态；失败 ⇒ 回滚 `.on` / `aria-checked`、解除 `disabled`、区级 hint（按 `code` 查表、2 秒复位）；卸载失败同走 hint，列表不因失败改变；长文本只由 `.skill-manage-name` 承担压缩（`min-width: 0` + ellipsis），右对齐簇与徽标 `flex-shrink: 0` + `nowrap` **永不让位**'
    - statement: '[E5 backstop] 在设置页最小可用窗口（800px：行宽约 476px）下，最宽形态的行首行（`诊断 N` + 状态标注「已遮蔽 · 由用户同名技能胜出」+ 启停开关 + 卸载按钮 + 4×8px 间隙 ≈ 330px）保持单行不换行、右对齐簇不被裁切，仅技能名缩到省略号（`title` 仍可读全名）；不出现徽标被挤出首行或头部高度变化'
      verification: backstop
    - '[E7] 诊断详情区：`diagnostics.length === 0` 时**徽标与详情区都不渲染**（不留空元素）；展开后每条显示级别标签 + 可读 `message`（`white-space: pre-wrap` + `word-break: break-word`，不截断）+ `realm_*` 诊断码（等宽 + `nowrap`）；**诊断条目自带的 `path` 不上屏**（沙箱内绝对路径，对用户无意义）；级别上色三档（错误 ⇒ `--skill-error-text`、警告 ⇒ `--skill-limit-text`、提示 ⇒ `--text-secondary`）；无部分就绪态；展开/折叠用 `display: none → block`（既有规则）⇒ 折叠态的详情区**不在 Tab 序内**、**不可能**产出零可见高度的隐形焦点停靠点'
    - '[E8] 卸载确认弹框：确认后按钮置 `disabled` 且弹框**保持打开**直到响应到达；失败 ⇒ **关闭弹框** + hint(danger)（按 `code` 查表），**不再**改变行内任何状态（若 `code === ''''not_found''''` 则**额外**触发一次列表重拉，让该行自然消失）；正文含目录路径 `skills/{name}/`（文本节点，不是 HTML 拼接）；条件追加行只在「列表里存在 `tier !== ''''user''''` 且 `shadowed === true` 且 `shadowedBy === 该技能名`」时渲染（纯数据查找，**查找不到即不渲染**，不重判优先级 / 遮蔽）；打开时初始焦点落在**「取消」**（默认焦点 = 安全选项），`Esc` 关闭，关闭后焦点归还**触发行内的卸载按钮**（该行已不存在则不移动）'
    - '[E9] 区级 inline hint `#skillManageHint`：允许换行（`word-break: break-word`），不截断；失败 hint 的兜底文案承载后端 `error` 原文（长度不可控）；文本为空时不可见（不占位、不留下 8px 残留外边距）。**复位纪律**：2 秒后无条件复位为 `''''''''`；每次设置前先 `clearTimeout` 上一次的定时器'
    - '[D-15/D-06] 行内「卸载」按钮**仅** `tier === ''''user''''` 渲染；`builtin` / `managed` 行不渲染（不给「点了才知道不行」的挫败），限制说明由区说明 ② 承担'
    - '[UI-SPEC 硬前置] 「仅显式」的 label 与 title 提升为 `src/skill-picker-model.js` 的单源冻结表，面板侧改引用同一常量，**面板渲染结果逐字不变**；三条既有断言按各自方向改写完毕（值断言 / 引用形态断言 / **表级值域隔离**）'
    - '[assumption:USER-01 边界] 汇总条与详情区是**两层独立容器**（模块级 `errors[]` vs 每条技能的 `diagnostics[]`），互不复用；两层都不因对方的空 / 非空改变自身渲染条件'
    - '[assumption:USER-02 边界] 连续快速操作不产生状态互串：hint 的复位定时器与折叠态赋值点各自单一、不互相覆盖；在途态只作用于被点的那一行（其他行的开关仍可点）'
    - '[assumption:USER-06 边界] 卸载按钮的**不渲染**只是 UX；服务端拒绝由 50-02 的 manager 判据承担。前端**不得**用「已渲染的按钮存在与否」反过来推断服务端会不会拒绝'
  artifacts:
    - 'src/skill-picker-model.js：新增「仅显式」单源表（`{ label, title }` + `Object.freeze`，命名交执行期）+ 挂进同一个 `api` 对象'
    - 'src/settings-page.js：启停开关的乐观翻转 / 回滚 / 在途态；卸载按钮与确认弹框；`setSkillManageHint` / `resetSkillManageHint`（含 `clearTimeout` 纪律）；诊断徽标 + 详情区 + 汇总条的渲染与折叠；`setCollapsed(box, toggleEl, collapsed)` 单点赋值函数；重渲染的滚动位置与焦点还原（全部包在既有的 region 标记之间）'
    - 'src/settings.html：行内右对齐簇的按钮结构（诊断徽标 / 启停开关 / 卸载按钮）+ 汇总条容器 + 卸载确认弹框（`.ai-modal-overlay` + `.ai-modal`）'
    - 'src/styles/main.css：补齐本段其余规则（`.skill-manage-diag-badge` 及其 chevron / `.skill-manage-diag*` / `.skill-manage-summary` / `.skill-manage-actions` / `.skill-manage-danger-btn` / `.skill-manage-confirm*` / `.skill-manage-state-title*` / `.skill-manage-state-body` / `.skill-manage-actions .ai-switch:disabled`）+ 三条作用域收敛覆盖（`.skill-manage-section .ai-switch:focus-visible` / `.skill-manage-section .slash-picker-tag-explicit` / `.skill-manage-section .ai-skill-content-box-header:hover`）'
    - 'tests/test-skills-management.js：注入纪律（region 内零 HTML 模板）、折叠态单点写入点、跨文件折叠类名与 aria 契约一致性、失败文案按 `code` 查表而不解析 `message`、`.skill-manage-row-main` 无 `flex-wrap`、`.skill-manage-row` 无 `--bg-hover` 底'
    - 'tests/test-skill-picker-model.js：三条既有断言按新设计改写 + 「仅显式」单源表的值断言'
  key_links:
    - '单源链：`src/skill-picker-model.js` 的 `STATUS_TEXT` / `TIER_BADGE` / 新的「仅显式」表 → 设置页（`window.SkillPickerModel`）与 `/` 面板（同一 `api` 对象）三方共用；任何一处新增文案都必须落在这张表上'
    - '折叠块链：48 的既有类规则（`.ai-skill-content-box` / `-header` / `-title` / `-chevron` / `-body` + `.collapsed`）是**唯一**折叠视觉；本页是第三处宿主，**只允许复用 + 三条作用域收敛的覆盖**，**不得**新建第二份折叠实现、**不得**改 48/49 的实名宿主'
    - '写操作后的一致性链：响应体即最新管理投影 → 就地重渲染（零二次请求）→ 还原滚动位置与焦点 → hint 给成功 / 失败反馈。设置页**收不到**任何主进程广播（`windowManager.broadcast` 只到 BrowserWindow 的 webContents）⇒ 这条链是设置页唯一的同步机制'
    - '焦点 / a11y 契约：启停开关 `button` + `role="switch"` + `aria-checked` + 逐行 `aria-label`；诊断徽标 `button` + `aria-expanded` + `aria-controls`；汇总条 header `role="button"` + `tabindex="0"` + `aria-expanded` + Enter / Space；焦点序 = DOM 序，**不引入任何 `tabindex` 正值**'
  prohibitions:
    - statement: '不得在设置页新写一份状态文案 / 来源徽标文案 / 「仅显式」文案 / 限额数值；也不得把「仅显式」的表提升到别处（如 `renderer.js`）'
      status: unresolved
      verification: judgment
      reason: '48 / 49 已反复付过「第二份拷贝」的代价（`49-UI-REVIEW` 的 `W1-04`）。单源提升的**唯一**正确落点是双模式导出的 `src/skill-picker-model.js` —— 它是「纯 Node 可 require + 浏览器可 `window.*`」这一个形态让三方拿到同一份实现的全部机制'
    - statement: '不得新建第二份折叠实现，也不得改动 `.ai-skill-content-box*` 的既有规则本身'
      status: unresolved
      verification: judgment
      reason: '那条规则同时服务 48 的气泡正文块与 49 的工具卡片。改类本体 = 跨阶段视觉决策，须单独立项；本页只允许三条**带 `.skill-manage-section` 前缀**的作用域收敛覆盖（48/49 宿主与 `/` 面板零变化）'
    - statement: '不得在某处单独改 `collapsed` 类或单独改 `aria-expanded`'
      status: unresolved
      verification: judgment
      reason: '显隐规则以类为准、chevron 旋转在本页以属性为准 ⇒ 两个状态源。只有「一个折叠 = 一个赋值函数、同时写两者」才能消除漂移；任何只写其一的路径都会产出「图标翻转了但内容没展开」这类不可复现的 UI 故障'
    - statement: '不得在设置页使用 `innerHTML` / `insertAdjacentHTML` / 字符串模板拼 HTML 渲染任何技能数据（含「拼完再整体赋给 `innerHTML`」）'
      status: unresolved
      verification: judgment
      reason: '`TD-48-01`（`escapeHtml` 只转义 `& < >`、不转义引号）**仍开且未被修**（用户已裁决延后、49 未处置）。本页新增大量属性上下文插值（`title` / `aria-label` / `aria-controls`），走 HTML 字符串就是**扩大**该缺口'
    - statement: '不得给 `.skill-manage-row` 加 `--bg-hover` 底；不得给 `.skill-manage-row-main` 加 `flex-wrap`'
      status: unresolved
      verification: judgment
      reason: '前者会让描述 / 元信息 / 中性状态标注同时跌破 4.5:1，并把「仅显式」的合成底从 `#383838` / `#e2e2e2` 拖到 `#4a4a4a` / `#d0d0d0`（UI-SPEC 表 A / 表 B-2 逐值核算过）；后者会重演 48-UI-REVIEW Pillar 2 的「徽标被挤出首行、与名字读作两行」'
    - statement: '不得为让 `TD-48-01` / `TD-48-02` 看起来已修而改动 `escapeHtml` 或取消分支，也不得在本阶段声称它们已修'
      status: unresolved
      verification: judgment
      reason: '用户已裁决延后。本阶段的责任是**不扩大缺口**。执行期若顺手改了，必须一并核对 48 的面板面并单独记账，**不得**在本阶段声称已修（否则后续阶段据此跳过工作）'
---

<objective>
把设置页「技能管理」区从**只读列表**补成**完整的可操作面**：行内启停开关（即改即存 + 乐观翻转 + 失败回滚）、卸载按钮与二次确认弹框（`realm://` 的 div 遮罩范式）、诊断的两层承载（行内徽标 + 内联可展开详情区；模块级 `errors[]` 走顶部汇总条），以及全部样式与键盘可达性。

Purpose: ROADMAP 判据 2 与判据 3 的**用户可见半边**在此落地；三条只在文档里活着、被后续阶段当成冗余改掉就会静默退化的判断在此被钉住：① 折叠态的「类 + 属性双写、单一赋值点」（第三处宿主最容易漂移成第二套折叠控件）；② 「一次操作只影响一行」的滚动与焦点还原；③ 「仅显式」的单源化 —— 它的代价是**三条既有断言必须一并改写**，漏改第三条会留下一条**字面不可满足**的断言让测试永久红。

Output: 设置页的行内交互与诊断呈现、卸载确认弹框、inline hint 的复位加固、新增 CSS 段的补全与三条作用域收敛覆盖、以及「仅显式」单源表与三条断言的改写。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-api-skills/50-CONTEXT.md
@.planning/phases/50-api-skills/50-RESEARCH.md
@.planning/phases/50-api-skills/50-PATTERNS.md
@.planning/phases/50-api-skills/50-UI-SPEC.md
@.planning/phases/50-api-skills/50-01-PLAN.md
@.planning/phases/50-api-skills/50-02-PLAN.md
@src/settings-page.js
@src/settings.html
@src/styles/main.css
@src/skill-picker-model.js
@src/renderer.js
@tests/test-skill-picker-model.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 「仅显式」label + title 单源提升 + 面板侧改引用 + 三条既有断言按各自动向改写</name>
  <files>src/skill-picker-model.js, src/renderer.js, tests/test-skill-picker-model.js</files>
  <read_first>
    - `src/skill-picker-model.js`：`:345-365`（`TIER_BADGE` —— **同族形状**：`Object.freeze({ … Object.freeze({label, className, title}) })`）、`:452-478`（`api` 对象与双模式导出 —— 新表挂进 `api` 即可，导出机制不变）
    - `src/renderer.js`：`:10595-10602`（**「仅显式」的现有内联字面量**：`''<span class="slash-picker-tag-explicit" title="该技能不进模型提示词，只能手动调用（/skill:名字）">仅显式</span>''` —— 位置紧邻 `explicitTag` 变量；⚠️ 该行**用 `escapeHtml` 拼 HTML 字符串**，这是 48 的既有形态，**本任务不改它的渲染机制**，只把两个字符串换成引用同一常量）
    - `tests/test-skill-picker-model.js`：`:1258-1282`（**三条必须一并改写的断言**，见 key-decisions 的逐条方向）、`:1026-1041`（`STATUS_TEXT` 四键值冻结 —— **本任务不得碰**）、`:940-950`（`MANAGE_SKILL_SHORT_REASON` 恰 9 键 —— **本任务不得碰**）
    - `.planning/phases/50-api-skills/50-UI-SPEC.md`：`### 硬前置条件（单源化：仅显式的 label 与 title）`（含三条断言改写表）、`### 注入纪律`（本任务**不改** `renderer.js` 的拼接机制，只换字符串来源）
  </read_first>
  <action>
    1. 在 `src/skill-picker-model.js` 内新增「仅显式」单源表（**命名交执行期**，建议 `EXPLICIT_TAG`）：与 `TIER_BADGE` **同址、同导出面、同双层 `Object.freeze`** 形态，形状 `{ label, title }` —— `label: ''仅显式''`、`title: ''该技能不进模型提示词，只能手动调用（/skill:名字）''`（**逐字**取自今天的 `renderer.js` 内联值）。JSDoc 写明它是「48-02 新增类 `.slash-picker-tag-explicit` 的 label 与 title 的**唯一权威**」，消费者是 `/` 面板行与设置页行（两个宿主）。把它加进 `api` 对象（导出机制不变）。
    2. `src/renderer.js` 的面板行改为**引用同一常量**：把该行的 `''仅显式''` 与 `title` 字面量替换为常量插值（**渲染结果逐字不变** —— 单测会用值断言锁住）。⚠️ **不得**改这一行的拼接机制（仍是既有 `escapeHtml` 字符串拼接形态；本任务只换来源）。**不得**在该文件里新增第三份副本。
    3. **三条既有断言一并改写**（方向各不相同，逐条照 UI-SPEC 的改写表）：
       - `:1265-1269`：原「`''仅显式''` 在 `panelBody` 内恰出现 1 次」⇒ 面板改引用后**计数变 0**。改为：「断言单源表的值恰为 `''仅显式''`」+「断言面板侧**引用**该单源常量（正则断言该渲染函数体内出现该常量名 / `SkillPickerModel` 引用形态）」。
       - `:1270-1274`：原「title 字面量在 `panelBody` 内恰出现 1 次」⇒ 同上。改为「断言单源表的值恰为那句 title」+「断言面板侧引用同一常量」。
       - `:1278`：原「`modelSrc.includes(''仅显式'') === false`（该文案不与行尾状态标注混放）」⇒ **被新设计直接证伪**（单源表必须写在 `skill-picker-model.js` 里）。改为：「断言**单源表的值**恰为 `''仅显式''`」+「断言它**不在** `STATUS_TEXT` 的值集合里」—— 把「不混放」的原意**从「文件级零命中」改为「表级值域隔离」**。
       - ⚠️ **必须先按新设计逐条实跑一遍再落码**：第三条属「断言恒假」的同族形态，凭「改了就能过」的推断交付会让测试永久红。改完逐条运行并把红 → 绿的过程记进 SUMMARY。
    4. **不得**动 `STATUS_TEXT` 的四键值冻结断言、**不得**动 `MANAGE_SKILL_SHORT_REASON` 的 9 键断言（那是两张不同的表）。
  </action>
  <verify>
    <automated>node --test tests/test-skill-picker-model.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出（第三条断言若按旧设计留着会在此永久红）</fails_when>
    <automated>node -e 'const fs=require("fs");const model=fs.readFileSync("src/skill-picker-model.js","utf8");const rend=fs.readFileSync("src/renderer.js","utf8");const test=fs.readFileSync("tests/test-skill-picker-model.js","utf8");const bad=[];const m=model.match(/(\w+)\s*=\s*Object\.freeze\(\{[\s\S]{0,400}?label:\s*\x27仅显式\x27/);if(!m)bad.push("skill-picker-model.js 缺「仅显式」单源表（label 逐字）");else{const name=m[1];if(!/title:\s*\x27该技能不进模型提示词，只能手动调用（\/skill:名字）\x27/.test(model))bad.push("单源表缺逐字 title");if(!model.includes("module.exports = api")||!new RegExp("\\b"+name+"\\b").test(model.slice(model.indexOf("const api = {"))))bad.push("单源表未挂进 api 对象（双模式导出面）");const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const panel=stripC(rend).slice(rend.indexOf("const explicitTag"),rend.indexOf("const explicitTag")+400);if(panel.includes("仅显式"))bad.push("renderer 面板侧仍写着「仅显式」字面量（应改为引用单源常量；判据已剥注释 —— 注释里提到原字面量不构成违规）");if(!/SkillPickerModel|EXPLICIT_TAG/.test(panel))bad.push("renderer 面板侧未引用单源常量")}if(/modelSrc\.includes\(\x27仅显式\x27\)\s*,\s*false/.test(test)||/assert\.ok\(!modelSrc\.includes\(\x27仅显式\x27\)/.test(test))bad.push("第三条断言仍是「文件级零命中」形态（新设计下字面不可满足 ⇒ 永久红）");if(!/STATUS_TEXT/.test(test))bad.push("缺 STATUS_TEXT 相关断言");if(bad.length){console.error("「仅显式」单源化不合格:\n"+bad.join("\n"));process.exit(1)}console.log("「仅显式」单源化 ok（单源表逐字 + 面板改引用 + 第三条断言改为表级值域隔离）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `「仅显式」单源化 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 「仅显式」的 label 与 title 各只存在于 `src/skill-picker-model.js` 一处；`src/renderer.js` 的面板行改为引用该常量（源码扫描断言「panel 段内**不再出现**「仅显式」字面量」且「出现单源常量引用」）。⚠️ **本轮修订**：该子判据原写作 `panel.includes('仅显式')`（**带引号**的字面量），而现状树里的内联值形如 `…>仅显式</span>'`（**不带**引号）⇒ 该子判据**恒不命中**（惰性判据）。已改为判定**不带引号**的 `仅显式`，并实测三态：① **现状树（尚未提升）⇒ 命中**「面板侧仍写着「仅显式」字面量」（证明它不再惰性）；② 合规形态（面板改引用 + 注释里提到原字面量）⇒ **不**命中；③ 变异（面板内联写着 `仅显式` 同时又引用了常量）⇒ 命中。判据一律**先经词法级剥注释**（`stripC`：单引号 / 双引号 / 模板反引号字符串、正则字面量、行注释、块注释四态逐字符扫描 + 等长空白化）再判。
    - `tests/test-skill-picker-model.js` 的三条断言按新方向改写，且 `node --test` 全绿（**逐条运行验证**，不接受「改了就能过」的推断）。
    - **门禁对「第三条断言的旧形态」的识别判据已核实为生效**（修复前后各测一次）：以**当前** `tests/test-skill-picker-model.js`（其中写的是 `assert.strictEqual(modelSrc.includes('仅显式'), false, …)`）实测 ⇒ 判据命中并报「第三条断言仍是『文件级零命中』形态」；按新设计改成**表级值断言**后再测 ⇒ 不再报（无误报）。⚠️ 修复前该判据写的是 `=== false`（与实际的**逗号**形态不符）⇒ **恒不命中**，是一条惰性判据 —— 这正是它必须改判「逗号形态 / `assert.ok(!…)` 形态」的原因。
    - 面板渲染结果**逐字不变**（值断言：单源表的两个字符串与原内联值逐字相同）。
    - `STATUS_TEXT` 的四键值冻结断言与 `MANAGE_SKILL_SHORT_REASON` 的 9 键断言**保持绿**。
  </acceptance_criteria>
  <done>「仅显式」的可见文字与 title 成为跨进程单源，面板渲染结果不变，三条既有断言按各自方向改写完毕且全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 行内交互 —— 启停开关（乐观翻转 / 回滚 / 在途）+ 卸载按钮与确认弹框 + inline hint 复位加固</name>
  <files>src/settings-page.js, src/settings.html, src/styles/main.css, tests/test-skills-management.js</files>
  <read_first>
    - `src/settings-page.js`：`:2839-2901`（白名单的「读 state → 整存 → POST → 更新 state → 重渲染」四步链 —— **启停开关照抄这条链路的形状**，但反馈面换成 inline hint，本区**不调用** `showToast`）、`:2907-2920`（`renderAiBashWhitelistTags` —— **DOM 构建 + `textContent` 防 XSS（WR-13）** 的范式与「空态 hint 显隐走 CSSOM（CSP）」的既有注释）、`:3878-3885`（`setAiMemoryHint` 的双色类切换形状 —— 本任务另立 `setSkillManageHint` / `resetSkillManageHint`）、`:3815-3816`（`clearTimeout(timer); timer = setTimeout(...)` 的既有配对写法）、`:4787-4815`（`init()`）
    - `src/settings.html`：`:371` / `:402`（设置页既有的 **2 个 `.ai-modal-overlay` 弹框**范式）、`:755` / `:769`（2 个原生 `<dialog class="modal">` —— **本任务不采用这一形态**）
    - `src/styles/main.css`：`:5350-5358`（`.ai-modal-overlay` 的 `display: none` 初始隐藏 —— **CSP 安全的初始隐藏范式**）、`:5376-5403`（`.ai-modal` / `-header` / `-header h3`，含 `gap: 10px`）、`:5098-5130`（`.ai-switch` 与 `.on`）、`:921-936` / `:1349-1353` / `:5638-5641`（`.btn` / `.btn-secondary` / `.btn-sm` 的**生效**取值）、`:1442-1454`（`.btn-danger` —— **本阶段不使用**：白字对 `--danger-color` 实测 3.76:1 不达标）
    - `.planning/phases/50-api-skills/50-UI-SPEC.md`：`### 启停开关（即改即存，D-05）与失败回滚（D-06）`、`### 卸载（D-05 / D-07）`、`### inline hint（D-06）`、`### 卸载确认弹框`、`### 失败文案映射表`、`### 破坏性 CTA 的用词纪律`（**单动词 `卸载` 是刻意的取舍**，不得改成 `卸载技能`）、`### 键盘可达性`
  </read_first>
  <action>
    1. **启停开关**（渲染在 `div.skill-manage-actions` 内的 `button.ai-switch`，**既有类零改动**）：
       - 属性：`role="switch"` + `aria-checked="true|false"`（`true` ⇔ 技能**启用**）+ 状态类 `.on` + 逐行 `aria-label`（按当前状态给 `禁用技能「{name}」` / `启用技能「{name}」`）。
       - 点击（乐观翻转）：**立即**翻转 `.on` 与 `aria-checked`，并置 `disabled`（在途态由 `.skill-manage-actions .ai-switch:disabled { opacity: .6; cursor: progress; }` 承担）；**不发**请求前的任何二次确认（D-05）。
       - 请求：`skillsApi(''set-disabled'', { method: ''POST'', headers: { ''Content-Type'': ''application/json'' }, body: JSON.stringify({ name, disabled: 目标值 }) })` —— **增量载荷 `{name, disabled}`**（全量会在并发下互相覆盖）。
       - 成功：① 用**响应体回传的最新投影**就地重渲染（**零二次请求**）；② hint(success) `已禁用「{name}」` / `已启用「{name}」`；③ 2 秒后无条件复位。
       - 失败：① **回滚** `.on` 与 `aria-checked`；② 解除 `disabled`；③ hint(danger) 按 `code` 查表。
       - **重渲染后的位置保持**：重渲染前后记录并回写 `.settings-content.scrollTop`；按 `data-skill-name` 把焦点归还到**同一技能行**的开关（找不到则**不移动**焦点）。
    2. **卸载按钮与确认弹框**：
       - 入口可见性：**仅** `tier === ''user''` 的行渲染 `button.skill-manage-danger-btn.btn.btn-secondary.btn-sm`（文案**单动词** `卸载`）；`builtin` / `managed` 行**不渲染**按钮（限制说明由区说明 ② 承担）。
       - 危险语义用**文字色**表达：`.skill-manage-danger-btn { color: var(--skill-error-text); }`（**不使用** `.btn-danger`；该规则必须落在 CSS 专属段内 —— 与 `.btn-secondary` 同特异性、靠**后出现**才能覆盖 `color`）。
       - 点击打开确认弹框（`src/settings.html` 里以 `.ai-modal-overlay` + `.ai-modal` 结构落位，文案逐字取 UI-SPEC 的弹框表：标题 `卸载技能「{name}」？` / 正文 `将删除 skills/{name}/ 整个目录（含其中的脚本与引用文件），此操作无法恢复。` / 条件追加行 `同名内置 / 托管技能将在删除后重新可见。` / 按钮 `取消` + `卸载`）。**初始隐藏走 CSS 类**（`.ai-modal-overlay { display: none }` 既有规则）；打开 `overlay.style.display = ''flex''`、关闭 `''none''`（CSSOM，不受 CSP 限制）。**绝不**写 markup 内联 `style="display:none"`；**绝不**把 overlay 类用到 `<dialog>` 上。
       - 条件追加行的判定 = **纯数据查找**：列表里存在 `tier !== ''user''` 且 `shadowed === true` 且 `shadowedBy === 该技能名` 的条目 ⇒ 渲染该行；查找不到**即不渲染**（宁缺勿猜）。这是「同名技能将重新可见」的**唯一**提示面 —— 50-02 已裁决**不**在卸载响应里回传 `shadowNotice` 一类字段（提示必须在**删除前**给出；删完再提示是事后告示，且该字段无第二个消费者）。
       - 确认：按钮置 `disabled`，弹框**保持打开**直到响应到达。成功：关闭弹框 → 用响应体投影重渲染（滚动 / 焦点还原同上；被删行的开关已不存在 ⇒ 焦点不移动）→ hint(success) `已卸载「{name}」`。失败：**关闭弹框** + hint(danger)（按 `code` 查表）+ **不改**行内任何状态；若 `code === ''not_found''` 则**额外**触发一次列表重拉。
       - 焦点管理：打开时初始焦点落在**「取消」**；`Esc` 关闭；关闭后焦点归还**触发行内的卸载按钮**（该行已不存在则不移动）。**不实现完整焦点陷阱**（与设置页既有的 2 个 overlay 弹框同范式 —— 这是**有理由的边界**，须在代码注释里如实写明，不是静默省略）。
    3. **inline hint 加固**（`setSkillManageHint(text, tone)` / `resetSkillManageHint()`，另立于 `setAiMemoryHint`）：
       - 模块级 `let skillManageHintTimer = null;` —— 每次设置前 `clearTimeout(skillManageHintTimer)` 再 `setTimeout(() => { hintEl.textContent = ''''; hintEl.style.display = ''none''; }, 2000)`。
       - **文本与色调一并设置**（不允许只改文字不改色的路径）。
       - 色调类：`.skill-manage-hint-success`（`--skill-success-text`）/ `.skill-manage-hint-danger`（`--skill-error-text`）/ 默认中性（`--text-secondary`）。
       - 空文本 ⇒ **CSSOM** 置 `hintEl.style.display = ''none''`；有文本 ⇒ `''block''`（不走 markup 内联 style、不用 `''''` 回落）。
    4. **失败文案映射表**（本页的闭合白名单，落脚在 `src/settings-page.js`）：`not_found` → `技能已不存在，列表已刷新`（并**必须**触发一次列表重拉）/ `not_user_owned` → `该技能不是你的技能，无法在此卸载（内置技能只可禁用，AI 创建的技能请让 AI 用 manage_skill 删除）` / `invalid_name` → `技能名不合法，无法操作` / `BODY_TOO_LARGE` → `请求体超过上限，操作未执行` / 表外或缺失或 `unknown` → `操作失败：{后端 error 原文}`（取不到则 `操作失败，请重试`）。**前端只按 `code` 查表、不解析 `message`**。注释写明：这张表是**单消费者**新文案，一旦出现第二个消费者必须提升到 `src/skill-picker-model.js`。
    5. **CSS 段补全**（继续追加在 50-01 建立的专属段内）：`.skill-manage-actions`、`.skill-manage-actions .ai-switch:disabled`、`.skill-manage-danger-btn`、`.skill-manage-confirm` / `-body` / `-note` / `-actions`，以及三条**作用域收敛**覆盖：`.skill-manage-section .ai-switch:focus-visible { outline: 2px solid var(--accent-color); outline-offset: 2px; }`、`.skill-manage-section .slash-picker-tag-explicit { color: var(--text-primary); }`、`.skill-manage-section .ai-skill-content-box-header:hover { color: var(--text-primary); }`。⚠️ **不得**新增同义令牌（本阶段唯一新增令牌仍是 `--skill-success-text`，其定义写在两套主题块里、**不在**本段）；**本段所有颜色声明一律 `var(--…)`** —— 不得出现任何硬编码 `#hex` / `rgb()` / `hsl()` 值（门禁按**声明值**扫描）；⚠️ **不得**给 `.skill-manage-row` 加 hover 底、**不得**给 `.skill-manage-row-main` 加 `flex-wrap`。
    6. **`tests/test-skills-management.js` 补交互面护栏**（全部为源码扫描，因为 `realm://` guest DOM 不可在纯 Node 下驱动）：
       - 注入纪律：既有的 region 内零 `innerHTML` / `insertAdjacentHTML` **负向**断言 + `document.createElement` / `textContent` **正向**断言（50-01 已建）保持绿；新增「region 内无 `showToast` 调用」（D-06 把本区锁定为 inline hint）。
       - hint 复位纪律：断言 region 内出现 `clearTimeout(` 且其**前**存在对同一计时器变量的赋值 —— 用**区域切片**断言（找到 `setSkillManageHint` 函数体，断言体内含 `clearTimeout(` 与 `setTimeout(` 且 `clearTimeout` 的出现位置**早于** `setTimeout`）。
       - 失败文案：断言 `code` 表含四个键（`not_found` / `not_user_owned` / `invalid_name` / `BODY_TOO_LARGE`）且**不出现** `JSON.parse(err.message)` / `err.message.match(` 之类「解析 message」的形态。
       - 危险按钮：断言 region 内**不出现** `btn-danger`（改用 `.skill-manage-danger-btn` + `--skill-error-text`）。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const css=fs.readFileSync("src/styles/main.css","utf8");const bad=[];const cssC=css.replace(/\/\*[\s\S]*?\*\//g,(m)=&gt;m.replace(/[^\n]/g," "));const i=css.indexOf("/* ===== 设置页「技能管理」区（Phase 50） ===== */");if(i&lt;0)bad.push("缺 Phase 50 专属段");const seg=i&lt;0?"":cssC.slice(i);for(const sel of [".skill-manage-actions",".skill-manage-danger-btn",".skill-manage-confirm",".skill-manage-confirm-body",".skill-manage-confirm-note",".skill-manage-confirm-actions"])if(!seg.includes(sel))bad.push("专属段缺 "+sel);if(!/\.skill-manage-section \.ai-switch:focus-visible\s*\{[^}]*outline:\s*2px solid var\(--accent-color\)/.test(css))bad.push("缺开关的作用域收敛焦点环");if(!/\.skill-manage-section \.slash-picker-tag-explicit\s*\{[^}]*color:\s*var\(--text-primary\)/.test(css))bad.push("缺「仅显式」的作用域收敛色覆盖（前置修复 ③）");if(!/\.skill-manage-section \.ai-skill-content-box-header:hover\s*\{[^}]*color:\s*var\(--text-primary\)/.test(css))bad.push("缺折叠块 header hover 的作用域收敛覆盖（前置修复 ①）");if(/\.skill-manage-row\s*\{[^}]*background\s*:\s*var\(--bg-hover\)/.test(cssC))bad.push(".skill-manage-row 加了 --bg-hover 底（Color 硬禁令；判据已剥除 CSS 注释）");const n=(cssC.match(/--skill-success-text\s*:/g)||[]).length;if(n!==2)bad.push("--skill-success-text 定义 "+n+" 处（须两处主题块各一）");const danger=(seg.match(/\bbtn-danger\b/g)||[]).length;if(danger&gt;0)bad.push("技能管理区出现 .btn-danger（白字对比度 3.76:1 不达标；判据已剥除 CSS 注释 —— 注释里如实写明「不使用 .btn-danger」不构成违规）");const segN=seg.split("\n").filter((l)=&gt;!/--skill-success-text\s*:/.test(l)).join("\n");const hard=[...segN.matchAll(/:\s*([^;{}]*);/g)].map((m)=&gt;m[1]).filter((v)=&gt;/#|rgb\(|hsl\(/.test(v));if(hard.length)bad.push("Phase 50 段出现硬编码颜色值（本段颜色一律 var(--…)；唯一新增令牌 --skill-success-text 的定义在两套主题块里、不在本段）："+hard.slice(0,3).join(" / "));if(bad.length){console.error("交互样式段不合格:\n"+bad.join("\n"));process.exit(1)}console.log("交互样式段 ok（新增规则齐备 + 三条作用域收敛 + 两条硬禁令 + 零 btn-danger + 颜色全 var）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `交互样式段 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const fs=require("fs");const sp=fs.readFileSync("src/settings-page.js","utf8");const bad=[];const S="/* Phase 50 skill-manage region: start */",E="/* Phase 50 skill-manage region: end */";const a=sp.indexOf(S),b=sp.indexOf(E);if(a&lt;0||b&lt;0||b&lt;=a)bad.push("缺技能管理区 region 标记");const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const r=a&lt;0?"":stripC(sp.slice(a,b+E.length));if(/\binnerHTML\b|insertAdjacentHTML/.test(r))bad.push("region 内出现 innerHTML/insertAdjacentHTML；判据已剥注释 —— 注释里如实写明「本区零 innerHTML」不构成违规");if(!/document\.createElement\(/.test(r))bad.push("region 内未见 DOM 构建（无正命题）");if(/\bshowToast\(/.test(r))bad.push("region 内调用了 showToast（D-06 锁定为 inline hint）；判据已剥注释");if(!/pickStatusKey\(/.test(r))bad.push("设置页未消费 pickStatusKey（D-12 状态链单源，不得在本页重写 if 链）");if(!/[\x27"]卸载[\x27"]/.test(r))bad.push("缺单动词卸载按钮文案（UI-SPEC Copywriting #13 逐字 \x27卸载\x27）");if(!/clearTimeout\(/.test(r))bad.push("缺 clearTimeout 纪律（连续操作会被陈旧复位清掉新消息）");const fnI=r.indexOf("function setSkillManageHint(");if(fnI&lt;0)bad.push("缺 setSkillManageHint");else{const fn=r.slice(fnI,fnI+1200);const ci=fn.indexOf("clearTimeout("),si=fn.indexOf("setTimeout(");if(ci&lt;0||si&lt;0||ci&gt;si)bad.push("clearTimeout 未早于 setTimeout（复位纪律不成立）")}for(const c of ["not_found","not_user_owned","invalid_name","BODY_TOO_LARGE"])if(!r.includes(c))bad.push("失败文案白名单缺 "+c);if(/JSON\.parse\(\s*err\.message|err\.message\.match\(/.test(r))bad.push("出现解析 message 的形态（前端只按 code 查表）；判据已剥注释 —— 注释里举反例不构成违规");if(/\bbtn-danger\b/.test(r))bad.push("region 内使用 .btn-danger（危险语义改用 .skill-manage-danger-btn + --skill-error-text）；判据已剥注释 —— 注释里如实写明「不使用 .btn-danger」不构成违规");if(bad.length){console.error("设置页交互面不合格:\n"+bad.join("\n"));process.exit(1)}console.log("设置页交互面 ok（DOM 构建 / inline hint 复位纪律 / 折叠单点双写 / code 白名单 / 零 btn-danger）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `设置页交互面 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 两条扫描（交互样式段 / 设置页交互面）输出 ok 行；**可失败性（只做过负方向，未做 stub 正方向与单点变异）**：两条门禁落地前对当前树实跑，失败项全部是「新代码尚未落地」类（缺专属段 / 缺 region / 缺新增选择器），无「合规实现也修不掉」的断言。执行者落地后**必须自行**做四次单点变异（另加一次颜色变异）：删掉任一作用域收敛覆盖 ⇒ 红；给 `.skill-manage-row` 加 `--bg-hover` 底 ⇒ 红；把 `clearTimeout` 挪到 `setTimeout` 之后 ⇒ 红；把 `BODY_TOO_LARGE` 从白名单删掉 ⇒ 红；另加一次颜色变异：在本段写一个 `color: #fff;` ⇒ 红（报「硬编码颜色值」）。
    - ⚠️ **W-b 修订（交互样式段门禁）**：`.skill-manage-diag-badge` **已从本任务的专属段选择器清单中移除** —— 该规则由 **50-04-T3 step 6** 交付，把它钉在本任务（T2）的门禁里会让「严格按 T2 的 action 落地」的实现必然报「专属段缺 `.skill-manage-diag-badge`」，与 T2 自己的「失败项无『合规实现也修不掉』类」声明自相矛盾。移除后覆盖不丢：**T3 的样式门禁**已断言该选择器（`padding: 1px 4px` + 无底色 + chevron 属性驱动）。
    - ⚠️ **W1 修订（设置页交互面门禁，本轮 iteration 3）—— 同一类缺陷的第二个实例**：`setCollapsed` 的三条断言（定义**恰 1 处** + 函数体内 `classList.toggle('collapsed'` 与 `setAttribute('aria-expanded'` **双写**）**已从本任务的门禁里整体移除**，并移交 **50-04-T3**（该任务 step 3 才是 `setCollapsed` 的**创建者**；T2 的 action 里没有任何折叠）。移除前，严格按 T2 的 action 落地会必然报「折叠赋值函数定义 **0** 处（须恰 1 处单点写入）」—— 那是一条「合规实现也修不掉」的断言。**同源的第二处一并移交**：T2 原 step 6 要求写「折叠态单点写入点」护栏，且其措辞是「对本任务新增的两个折叠（详情区 / 汇总条）各断言一次调用点」，而这两个折叠都是 **T3** 的交付物 —— 该护栏已移入 T3 的 step 7。**覆盖不丢**：T3 的 region 门禁现同时断言 `setCollapsed` 恰 1 处定义、函数体内两条写入齐备，且带「函数体无法定位」的显式失败分支（避免取值窗口失效时静默通过）。**本类缺陷的穷尽扫描结论见 50-04-T3 的同一注解与本计划文件末的验证小节。**
    - ⚠️ **本轮修订的注释误报面**：两条门禁的负向 token 扫描（region 内 `innerHTML` / `insertAdjacentHTML` / `showToast(` / `.btn-danger` / `err.message` 解析形态；CSS 段内 `.btn-danger` / `--bg-hover` 底）一律**先剥注释再判**（CSS 只剥块注释；JS 经 `stripC` 词法级剥注释 = 四态逐字符扫描 + 等长空白化，字符串与正则字面量内的 `//` 不再被误判为注释），失败文案里已写明「注释中的同一字面量不构成违规」。已实测五组：合规形态（注释里逐一提到这些字面量）⇒ 不转红；四类真实代码变异（真用 `innerHTML` / 真调 `showToast` / 真写 `btn-danger` / 真解析 `err.message`）⇒ 各自转红；CSS 侧 `.btn-danger` 与 `--bg-hover` 同理（合规含解释性注释 ⇒ 不转红，真写声明 ⇒ 转红）。
    - 启停开关的四个态（常态 / 在途 / 成功 / 失败回滚）在代码里都有对应路径；卸载的「确认中保持打开 / 失败关闭 + 不改行内状态 / `not_found` 额外重拉」三条都有对应路径。
    - 确认弹框用 `.ai-modal-overlay` + `.ai-modal`，初始隐藏走 CSS 类、显隐用 CSSOM 具体值；markup 内联 `style="display:none"` 在新区内**零命中**（50-01 的门禁 B 已覆盖，本任务须保持绿）。
    - 区级 hint 有 `clearTimeout` 纪律且 `clearTimeout` 在 `setTimeout` 之前；空文本用 CSSOM 置 `none`。
    - 卸载按钮文案是**单动词** `卸载`（未改成 `卸载技能`）；`builtin` / `managed` 行不渲染该按钮。
  </acceptance_criteria>
  <done>设置页的技能行可启停、可卸载（带二次确认）、失败有 inline hint 且复位纪律加固；「一次操作只影响一行」的滚动 / 焦点保持在代码里有落实；三条作用域收敛覆盖与两条硬禁令被扫描门禁钉住。</done>
</task>

<task type="auto">
  <name>Task 3: 诊断的两层承载 —— 行内徽标 + 内联详情区 + 模块级汇总条（含 a11y 契约）</name>
  <files>src/settings-page.js, src/settings.html, src/styles/main.css, tests/test-skills-management.js</files>
  <read_first>
    - `src/styles/main.css`：`:5827-5891`（`.ai-skill-content-box` 家族**全段逐字**：`-header` 的 `gap: 6px` / `padding: 6px 12px` / hover 底 `--bg-hover` / `:focus-visible` 的 `outline-offset: -2px`；`-body` 的 `padding: 8px 12px 10px` / `max-height: 260px; overflow-y: auto` / `white-space: pre-wrap` / `word-break: break-word`；`.collapsed` 的 chevron 旋转规则与 `:not(.collapsed) .ai-skill-content-box-body { display: block }`）
    - `src/renderer.js`：`:8953`（`renderSkillContentBox` —— ⚠️ **浏览器脚本、无 `module.exports`、跨文件不可调用**。本页**只复用 CSS 类**，DOM 与开关逻辑须自行构建 3~5 行；**不得**为消重去改 `renderer.js`）
    - `src/settings-page.js`：`:3878-3897`（`setAiMemoryHint` / `resetAiMemoryHint` —— 本任务的 hint 已在 Task 2 另立）
    - `.planning/phases/50-api-skills/50-UI-SPEC.md`：`### 诊断详情区（本阶段唯一需要定稿的交互形态）`、`### 模块级汇总条（errors[]，D-11 的顶部承载面）`、`### 折叠态的单一写入点`、`### 列表滚动归属`、`### 键盘可达性`、`### 状态机（#skillManageState 与列表的互斥关系）`、`### 状态机与重渲染的硬约束（汇总）`
    - `.planning/phases/50-api-skills/50-PATTERNS.md`：`### src/settings-page.js` 的**不可用的既有工具**对照表（`renderSkillContentBox` 不可调 → 接受一处小重复 + 必须加一条跨文件源码扫描测试）
  </read_first>
  <action>
    1. **行内诊断徽标**（渲染在 `div.skill-manage-tail` 内、`span.skill-manage-status` **之前**，条件 `diagnostics.length > 0`）：`button.skill-manage-diag-badge[type="button"]`，文案 `诊断 {N}`，内含 `<span class="skill-manage-diag-badge-chevron" aria-hidden="true">▾</span>`；属性 `aria-expanded="false|true"`（**语义与 chevron 的唯一权威**）+ `aria-controls="{详情区 id}"` + `title="展开 / 折叠诊断详情"`。**无底色**（有底色会压低暗色主题对比度）。`button` 原生支持 Enter / Space ⇒ **不需要**自定义 `keydown`。`diagnostics.length === 0` 时**徽标与详情区都不渲染**（不占位、不留空元素）。
    2. **诊断详情区**（`li` 内、第二行之后；条件同上）：`div.ai-skill-content-box.collapsed.skill-manage-diag` + 直接子节点 `div.ai-skill-content-box-body` > `ul.skill-manage-diag-list` > 每条 `li.skill-manage-diag-item.skill-manage-diag-item-{error|warning|info}`，其中 `span.skill-manage-diag-level`（`错误` / `警告` / `提示`，**表外 `level` 不渲染标签**，不得回落到 `undefined` 字面量）+ `span.skill-manage-diag-message`（可读 `message`，`pre-wrap` + `break-word`，**不截断**）+ `code.skill-manage-diag-code`（`realm_*` 码、等宽、`nowrap`）。
       **`path` 字段不上屏**（它是沙箱内绝对路径，对用户无意义且冗余）。
       **默认折叠**（带 `.collapsed`）；显隐由既有规则 `:not(.collapsed) .ai-skill-content-box-body { display: block }` 提供 ⇒ 折叠态**不在 Tab 序内**，不可能产出零可见高度的隐形焦点停靠点。
    3. **折叠态的单一写入点**：新增 `setCollapsed(box, toggleEl, collapsed)`，函数体内**同时**写 `box.classList.toggle(''collapsed'', collapsed)` 与 `toggleEl.setAttribute(''aria-expanded'', String(!collapsed))`。本页两处折叠（诊断详情区 + 汇总条）**共用**它；全页**禁止**第二处单独改 `classList` 或单独改 `aria-expanded`。诊断徽标的 chevron 旋转由**本页新类** `[aria-expanded="false"] .skill-manage-diag-badge-chevron { transform: rotate(-90deg) }` 驱动（以属性为唯一状态源 —— 它是新类，无既有规则要兼容）；汇总条的 chevron 旋转由**既有类规则** `.ai-skill-content-box.collapsed .ai-skill-content-box-chevron` 驱动（**不改** —— 改它会命中 48/49 宿主），故那里只能类 + 属性双写、靠单点赋值消漂移。
    4. **模块级汇总条**（`#skillManageSummary`，`div.ai-skill-content-box.skill-manage-summary`，**完整家族含 header**）：header = `div.ai-skill-content-box-header` > `span.ai-skill-content-box-title`（文案 `技能加载问题（{N}）`）+ `span.ai-skill-content-box-chevron`；header 带 `role="button"` + `tabindex="0"` + 随态 `aria-expanded` + Enter / Space（`preventDefault` 防 Space 滚动）。⚠️ 三个 a11y 属性**必须用 DOM API 的写入形态落码**（门禁按**写形态**判，不按属性名子串判 —— 后者会被一条注释假绿）：`header.setAttribute('role', 'button')`、`header.setAttribute('tabindex', '0')`、折叠态经 `setCollapsed()` 内的 `toggleEl.setAttribute('aria-expanded', String(!collapsed))`。**不得**只在注释里提这三个属性名。**默认展开**（不初始化 `.collapsed` —— 模块级错误是异常状态，「禁止静默失败」）。`errors.length === 0` 时**整条不渲染**。逐条渲染同「详情区」的三项结构（级别标签按级别上色：错误 ⇒ `--skill-error-text`、警告 ⇒ `--skill-limit-text`、提示 ⇒ `--text-secondary`）。
       **与每技能诊断的关系**：两层各自独立、互不复用容器（`errors[]` 是**无归属技能**的模块级容器，46 D-07）。
    5. **状态机接线**（`#skillManageState` 与列表 / 汇总条互斥）：加载中 ⇒ 状态区单行 `正在加载技能…`（无标题 / 无按钮 / 无 spinner / 无骨架屏）、汇总条与列表都不渲染；就绪（总数 > 0）⇒ 状态区不渲染、有 `errors[]` 时渲染汇总条、渲染 `groups[]`；空态 A（`refreshedAt === 0`）/ B（`refreshedAt > 0` 且 0 条）⇒ 各自标题 + 正文 + `重新加载`，**B 态允许同时渲染汇总条**（那正是「为什么一条都没有」的解释面）；空态 C（拉取失败）⇒ 标题 + 后端 `error` 原文 + `重新加载`，汇总条与列表都不渲染。hint 在这四态下都保持空。
       ⚠️ **区在页面上恒存在**（即使加载失败）—— 用户必须始终能看到「技能管理」这个区与其说明。
    6. **CSS 段补全**：`.skill-manage-diag-badge`（含 `gap: 4px` / `padding: 1px 4px`（**B 表登记的唯一非 4 倍数**）/ `border-radius: 3px` / `font-size: 11px` / `color: var(--text-secondary)` / `white-space: nowrap`）、`.skill-manage-diag-badge-chevron` 与 `[aria-expanded="false"]` 的旋转、`.skill-manage-diag-list { gap: 8px }`、`.skill-manage-diag-item { gap: 8px }` 与三档级别上色、`.skill-manage-diag-message` / `.skill-manage-diag-code`、`.skill-manage-summary` 与 `.ai-skill-content-box.skill-manage-summary + .skill-manage-groups { margin-top: 16px }`、`.skill-manage-state-title` / `.skill-manage-state-title-danger` / `.skill-manage-state-body`。**不得**新建第二份折叠实现、**不得**改 `.ai-skill-content-box*` 的既有规则。
    7. **`tests/test-skills-management.js` 补两组**：
       - **跨文件折叠契约一致性（PATTERNS 明文要求）**：断言 `src/settings-page.js` 与 `src/renderer.js` 使用**同一组类名**（`.ai-skill-content-box` / `-header` / `-title` / `-chevron` / `-body` / `.collapsed`）与**同一组 aria 属性**（`role="button"` / `tabindex="0"` / `aria-expanded` / Enter 与 Space 的 `preventDefault`）—— 否则第三处宿主会静默漂移成第二套折叠控件。**必须带正命题**（断言两文件各自**确实出现**这些类名，不是只断言「没有别的」）。
       - **折叠态单点写入点（本任务新建 `setCollapsed`，故这条护栏归本任务，**不得**留在 50-04-T2）**：断言 region 内存在一个同时含 `classList.toggle(''collapsed''` 与 `setAttribute(''aria-expanded''` 的函数；并断言**不存在**第二个只含其一的位置（对本任务新增的两个折叠（诊断详情区 / 汇总条）各断言一次调用点）。⚠️ **为什么归本任务**：`setCollapsed` 由本任务 step 3 **新增**，T2 的 action 里没有任何折叠；把它钉在 T2 的门禁里会让「严格按 T2 的 action 落地」的实现必然报「折叠赋值函数定义 0 处」—— 那是「合规实现也修不掉」的断言（与 `.skill-manage-diag-badge` 同形，见 T2 acceptance 的 W-b / W1 两处修订说明）。
       - **汇总条与详情区的两层独立**：断言 `errors` 与 `diagnostics` 分别绑定到不同的容器（`#skillManageSummary` vs 行内 `.skill-manage-diag`），且渲染函数里 `errors.length === 0` 与 `diagnostics.length === 0` 是**两个独立条件**（不得合并成一个「有没有诊断」的开关）。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const sp=fs.readFileSync("src/settings-page.js","utf8");const rd=fs.readFileSync("src/renderer.js","utf8");const bad=[];const S="/* Phase 50 skill-manage region: start */",E="/* Phase 50 skill-manage region: end */";const a=sp.indexOf(S),b=sp.indexOf(E);const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const r=(a&lt;0||b&lt;0)?"":stripC(sp.slice(a,b+E.length));const rdC=stripC(rd);if(!r)bad.push("缺技能管理区 region");const CLS=["ai-skill-content-box","ai-skill-content-box-header","ai-skill-content-box-title","ai-skill-content-box-chevron","ai-skill-content-box-body","collapsed"];for(const c of CLS){if(!r.includes(c))bad.push("设置页未复用折叠类 "+c);if(!rdC.includes(c))bad.push("renderer 未使用折叠类 "+c)}if(!/setAttribute\(\s*["\x27]role["\x27]\s*,\s*["\x27]button["\x27]\s*\)/.test(r))bad.push("缺 role=button 的**真实写入形态**（DOM API：header.setAttribute(\x27role\x27, \x27button\x27)）");if(!/setAttribute\(\s*["\x27]tabindex["\x27]\s*,\s*["\x27]0["\x27]\s*\)/.test(r))bad.push("缺 tabindex=0 的**真实写入形态**（DOM API：header.setAttribute(\x27tabindex\x27, \x27\x30\x27)）");if(!/setAttribute\(\s*["\x27]aria-expanded["\x27]/.test(r))bad.push("缺 aria-expanded 的真实写入形态（setCollapsed 内的 setAttribute）");const fns=(r.match(/function setCollapsed\s*\(/g)||[]).length;if(fns!==1)bad.push("折叠赋值函数定义 "+fns+" 处（须恰 1 处单点写入）");else{const k2=r.indexOf("function setCollapsed(");const e2=r.indexOf("\n}",k2);const body=k2!==-1&&e2!==-1?r.slice(k2,e2):"";if(!body)bad.push("setCollapsed 函数体无法定位（起止括号缺失 ⇒ 判据窗口失效）");else{if(!/classList\.toggle\(\x27collapsed\x27/.test(body))bad.push("setCollapsed 未写 collapsed 类");if(!/setAttribute\(\x27aria-expanded\x27/.test(body))bad.push("setCollapsed 未写 aria-expanded（折叠态单一写入点：类与属性必须同点双写）")}}if(!/pickStatusKey\(/.test(r))bad.push("设置页未消费 pickStatusKey（D-12 状态链单源，不得在本页重写 if 链）");if(!/refreshedAt\s*===\s*0/.test(r))bad.push("缺空态 A 的判据字段 refreshedAt === 0（D-19 硬要求：不得把它渲染成「无技能」）");if(!/refreshedAt\s*&gt;\s*0/.test(r))bad.push("缺空态 B 的判据字段 refreshedAt &gt; 0（区分「尚未加载」与「确实没有」）");if(!/preventDefault/.test(r))bad.push("缺 Enter/Space 的 preventDefault");if(!/diagnostics\.length\s*&gt;\s*0/.test(r))bad.push("缺 diagnostics 的非空条件");if(!/errors\.length\s*&gt;\s*0/.test(r))bad.push("缺 errors 的非空条件（两层必须独立）");if(!/skillManageSummary/.test(r) && !/skillManageSummary/.test(fs.readFileSync("src/settings.html","utf8")))bad.push("缺模块级汇总条容器");if(!r.includes("技能加载问题（"))bad.push("缺汇总条标题文案（UI-SPEC Copywriting #21：技能加载问题（{N}））");if(/skill-manage-diag[\s\S]{0,80}\.path\b/.test(r))bad.push("诊断 path 字段上屏（契约要求不上屏）；判据已剥注释 —— 注释里如实写明「path 不上屏」不构成违规");if(bad.length){console.error("诊断两层承载不合格:\n"+bad.join("\n"));process.exit(1)}console.log("诊断两层承载 ok（跨文件折叠契约一致 + 两层条件独立 + path 不上屏）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `诊断两层承载 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const css=fs.readFileSync("src/styles/main.css","utf8");const bad=[];const i=css.indexOf("/* ===== 设置页「技能管理」区（Phase 50） ===== */");const seg=css.slice(i).replace(/\/\*[\s\S]*?\*\//g,(m)=&gt;m.replace(/[^\n]/g," "));if(!/\.skill-manage-diag-badge\s*\{[^}]*padding:\s*1px 4px/.test(seg))bad.push("诊断徽标缺 1px 4px（B 表登记值）");if(!/\[aria-expanded="false"\]\s*\.skill-manage-diag-badge-chevron/.test(seg))bad.push("chevron 未以 aria-expanded 为状态源");if(/\.skill-manage-diag-badge\s*\{[^}]*background\s*:/.test(seg))bad.push("诊断徽标声明了底色（会压低暗色主题对比度）；判据已剥除 CSS 注释");if(!/\.skill-manage-diag-item-error\s*\{[^}]*var\(--skill-error-text\)/.test(seg))bad.push("缺错误级上色");if(!/\.skill-manage-diag-item-warning\s*\{[^}]*var\(--skill-limit-text\)/.test(seg))bad.push("缺警告级上色");if(!/^\.ai-skill-content-box-body\s*\{[^}]*max-height:\s*260px/m.test(css))bad.push("既有 .ai-skill-content-box-body 的 max-height: 260px 被改动或缺失（命中 48/49 宿主；判据按**行首锚定**只认裸规则，`.tool-card-content .ai-skill-content-box-body { max-height: none }` 那条既有作用域覆盖不在判据内）");if(bad.length){console.error("诊断样式不合格:\n"+bad.join("\n"));process.exit(1)}console.log("诊断样式 ok（徽标无底色 + chevron 属性驱动 + 三档级别上色 + 未改既有宿主）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `诊断样式 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 三条扫描输出 ok 行；**可失败性（分条如实披露）**：其中**「未改既有宿主」那一条已在本仓当前树上做过正方向实跑 + 变异** —— 现状 ⇒ 无输出；把裸规则 `.ai-skill-content-box-body { … max-height: 260px }` 改成 `100px` 或删掉该声明 ⇒ 转红 ⚠️ **修复说明**：该判据原形态（`/\.ai-skill-content-box-body\s*\{[^}]*max-height:\s*none/`）在**既有树上恒红** —— 它误匹配了 `.tool-card-content .ai-skill-content-box-body { max-height: none }` 这条**既有作用域覆盖**，合规实现也修不掉（只有删掉一条合法规则才能变绿）；现改为**行首锚定**的裸规则**正命题**（断言既有 `max-height: 260px` 仍在）。其余四条只做过**负方向**（失败项为「缺 region / 设置页未复用折叠类 / 缺 `refreshedAt` 空态分支」等新代码未落地类）。执行者落地后**必须自行**做五次变异：把 `diagnostics.length > 0` 与 `errors.length > 0` 合成一个条件 ⇒ 红；给 `.skill-manage-diag-badge` 加底色 ⇒ 红；把 chevron 的旋转状态源从 `aria-expanded` 改成类名 ⇒ 红；把 aria 契约落成注释而非 `setAttribute('role','button')` ⇒ 红（这正是本门禁判**写形态**的理由）；**把 `setCollapsed` 拆成两处、或删掉函数体内两条写入之一 ⇒ 红**（本条自 50-04-T2 移交而来 —— 见 T2 acceptance 的 W1 修订说明：断言必须由**创建该符号的任务**持有，否则「严格按 action 落地」的实现会必然红）。
    - 诊断详情区**无 header**（只 shell + body）、默认折叠；汇总条**有完整家族**、默认展开 —— 两处形态差异与 UI-SPEC 一致且被扫描断言区分。
    - `path` 字段不上屏；`level` 表外不渲染标签。
    - 折叠由 `setCollapsed` 单点双写；扫描断言该函数**恰 1 处定义**且体内同时含类与属性两条写入。
    - ⚠️ **本轮修订**：三条扫描的 region / CSS 内容判据一律**先剥注释再判**（JS 经 `stripC` 词法级剥注释 = 单引号 / 双引号 / 模板反引号字符串、正则字面量、行注释、块注释四态逐字符扫描 + 等长空白化 —— 字符串与正则字面量内的 `//` 不再被误判为注释，真实代码不被吞；CSS 只剥块注释）—— 「`path` 不上屏」「徽标无底色」这类负向判据不再会被一条如实写明该规则的注释顶红。其余判据（折叠类复用、`role`/`tabindex`/`aria-expanded` 的真实写入形态、两层条件独立）为**正向**断言，剥注释只会让它们更严格（注释不再能满足它们）。
    - 跨文件折叠契约断言带**正命题**（两文件都确实出现同一组类名与 aria 属性），不退化成「否命题空集真」（`WR-12` 的教训）。
  </acceptance_criteria>
  <done>诊断的两层承载完整：行内徽标可键盘展开内联详情区（复用既有折叠视觉、aria 契约齐备、path 不上屏），模块级 `errors[]` 走默认展开的顶部汇总条，两层条件互相独立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 磁盘上的技能元数据（名称 / 描述 / 诊断 message / 错误全文）→ 设置页 DOM | 内容**完全不受信**（用户手放的文件、AI 经 `write`/`bash` 写入、导入的第三方技能） |
| 设置页 guest（`realm://`，CSP `style-src ''self''`）→ 渲染与显隐 | markup 内联 style 被 CSP 拦；初始隐藏必须走 CSS 类 |
| 服务端返回的 `{ error, code }` → 用户可见文案 | 文案必须按 `code` 查表；`message` 可能含被拒内容（不得据此拼 UI） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-24 | Tampering | 属性上下文注入（技能名 / 描述含引号时逃逸 `title` / `aria-label` / `aria-controls`） | high | mitigate | 本页新增插值**全部**走 DOM API（`el.title = value` / `setAttribute`）；region 内 `innerHTML` / `insertAdjacentHTML` 零命中（门禁 B，含 `document.createElement` + `textContent` 正命题）。`TD-48-01` **仍开未修** ⇒ 本阶段只保证**不扩大缺口**，且不得声称已修 |
| T-50-25 | Tampering | CSP 绕过（用 markup 内联 `style="display:none"` 做初始隐藏 ⇒ 元素一进页面就可见） | medium | mitigate | 初始隐藏一律走 CSS 类（`.ai-modal-overlay { display: none }` 既有规则）；显隐用 CSSOM 具体值（`''flex''`/`''none''`，不用 `''''` 回落）。门禁 B 断言新区块内无内联 `display:none` |
| T-50-26 | Denial of Service | 陈旧复位定时器清掉新消息（连续操作时 hint 闪烁或消失） | low | mitigate | `setSkillManageHint` 内先 `clearTimeout` 再 `setTimeout`；扫描断言 `clearTimeout` 的位置**早于** `setTimeout` |
| T-50-27 | Tampering | 折叠控件漂移成第二套（类与属性两个状态源不同步 ⇒ UI 假象） | medium | mitigate | `setCollapsed` 单点双写（扫描断言恰 1 处定义 + 体内同时写两者）+ 跨文件契约扫描（设置页与 `renderer.js` 同类名同 aria 属性，带正命题） |
| T-50-28 | Repudiation | 失败文案失实（把「不是你的技能」说成「不存在」；或只改文字不改色调 ⇒ 绿色文案说失败） | medium | mitigate | 失败文案按服务端 `code` 查**闭合白名单**（`not_user_owned` 与 `not_found` 两条独立文案 + 表外兜底）；hint 的文本与色调**一并设置**（扫描断言不存在只改一者的路径） |
| T-50-29 | Information Disclosure | 诊断的 `path` 字段上屏（沙箱内绝对路径，泄漏本机目录结构） | low | mitigate | 详情区与汇总条都只渲染 `level` + `message` + `code` 三项；扫描断言渲染路径不含 `path` |
| T-50-30 | Tampering | 危险按钮的对比度不足被当成「有语义」 | low | mitigate | 不使用 `.btn-danger`（白字对 `--danger-color` 实测 3.76:1 < 4.5）；危险语义由 `--skill-error-text` 文字色 + 中性底承担（四档全部 ≥ 4.5）。扫描断言 region 内零 `btn-danger` |
| T-50-31 | Denial of Service | 重渲染重建整棵行 DOM 导致滚动位置与键盘焦点被重置 | low | mitigate | 记录并回写 `.settings-content.scrollTop`；按 `data-skill-name` 归还焦点到同一技能行；卸载成功行已消失则不移动焦点。这是「一次操作只影响一行」的可见保证 |
| T-50-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本阶段**零新增依赖**，无安装动作 ⇒ 无供应链面 |
</threat_model>

<verification>
- 三个任务的 automated 门禁 全绿，含 `node --test tests/test-skill-picker-model.js`（三条改写后的断言）与 `node tests/test-skills-management.js` 的回归。
- 本计划 5 条门禁**逐条如实披露**（不得整段声称已预跑）：5 条都只在**当前树**上做过负方向实跑，失败项一律为「新代码尚未落地」类。**唯一在落地前取得正/反证据的**是 50-04-T3 的「未改既有宿主」子判据（判既有 CSS：正方向通过 + 内存变异转红，见该任务 acceptance）；其余全部**未做** stub 等价树正方向与单点变异（它们指向**新代码**：新的 region 代码、新的 CSS 段、新的单源表与三条改写的断言）。变异义务已逐条写进各任务 `<acceptance_criteria>`。⚠️ 其中 50-04-T1 的三条断言改写属「断言恒假」同族风险 —— 该任务已要求**先按新设计逐条实跑再落码**，不接受「改了就能过」的推断；其门禁对「旧形态」的识别判据也已核实为**生效**（见 T1 的 acceptance）。**本轮修订（iteration 2）额外落地并实测的证据**：① T1 的「面板侧不再出现字面量」判据原为**惰性**（要求带引号的 `'仅显式'`，而现状树是不带引号的形态）⇒ 已改判不带引号并在**现状树**上实测命中、在合规形态上实测不命中；② T2 / T3 的全部负向 token 扫描先剥注释再判，已用合成 fixtures 实测「合规含解释性注释不转红 / 真实代码变异转红」各四组（JS region）与两组（CSS）；③ T2 的专属段选择器清单移除了 `.skill-manage-diag-badge`（由 T3 门禁覆盖）—— 详见 T2 的 acceptance。
- **`## UI Considerations` 的 lift 说明**：本计划的 `must_haves.truths` 承接 UI-SPEC 探针报告里 **E2 / E5 / E7 / E8 / E9 五组共 18 条 `resolved (explicit)`** 与 **1 条 `resolved (backstop)`**（E5 overflow，以 `{ statement, verification: backstop }` 扁平标量形式承接 —— 该条在 verify 期无显式证据时abstain ⇒ `human_needed`，**绝不静默通过**）；E1 / E3 / E4 / E6 四组 15 条 explicit 由 **50-01** 承接；7 条 `dismissed` 已由 UI-SPEC 逐条带理由关闭，不重复登记。**无静默丢弃**。
- 人工面（不进本计划）：卸窗与焦点观感、窄窗口下行首行不换行的实测（E5 backstop）、100 条技能时的滚动 / 密度 —— 记在 `50-VALIDATION.md` 的 Manual-Only 表。承重判据**必须带正命题**（`WR-12` 的教训）。
- **本轮修订（iteration 3）—— 两件事**：① **剥注释机制更换**（同 50-01 / 50-02 / 50-03）：本计划含 `stripC` 的三条门禁改用**词法级四态扫描**（字符串与正则字面量内的 `//` 不再被误判为注释）；已实测这三条门禁在当前树上**除下述 W1 移交带来的那一项外，判定与换机制前逐字一致**（机制本体未改变任何既有判定）。② **W1 修订 + 同类缺陷的穷尽扫描**：`setCollapsed` 的三条断言连同 T2 step 6 的「折叠态单点写入点」护栏**从 50-04-T2 整体移交 50-04-T3**（创建者持有自己的断言）。**移交已做机械取证**（对同一条门禁在移交前后各跑一次，比对**失败项清单**而非仅末行）：T2 的 region 门禁（`gate#3`）12 项 → **11** 项，**移除**的正是「折叠赋值函数定义 0 处（须恰 1 处单点写入）」；T3 的 region 门禁（`gate#5`）19 项 → **20** 项，**新增**的正是同一项 —— 两侧互为镜像，证明断言确实换了持有者而非被删除；`gate#1` 清单完全一致。**本次已对五个计划的 43 条门禁逐条做「该门禁断言的每个标识符 / 选择器 / 路径，是否由**本任务或更早的任务**交付」的审计**（门禁清单由脚本从五个 PLAN 里**机械提取**，保证不漏读任一条；「标识符 → 交付它的任务」的映射逐条核对；易误判的锚点再用脚本实测其在**当前树**上的存在性），结论：**同类缺陷在本阶段只此两例** —— 前一轮的 `.skill-manage-diag-badge`（已修）与本轮的 `setCollapsed`（本轮修），**无第三例**。审计同时复核了三条易误判的锚点（`const explicitTag` 在 `renderer.js:10598` 实存；`50-04-T3` 门禁复用的 6 个折叠类名在 `renderer.js` 中**今日各至少 1 处**（`ai-skill-content-box` 5 / `-header` 1 / `-title` 1 / `-chevron` 1 / `-body` 1 / `collapsed` 5）；`50-03-T2` 判据的 `res.writeHead(` 基线 14 与 `for (const [key, value] of Object.entries(updates)) {` 锚点实存）。
- counts-parity 在本波仍**预期为红**（`test-skill-picker-model.js` 的例数已变、两个新套件尚未入账本）—— 由 50-05 一次性收口。
</verification>

<success_criteria>
- 行内可启停（乐观翻转 + 失败回滚 + 在途态），可卸载（仅 user 行、二次确认、失败走 inline hint），列表不被操作失败改变。
- 诊断两层承载可见：行内有「诊断 N」徽标可展开内联详情区；模块级 `errors[]` 走默认展开的顶部汇总条；`path` 不上屏。
- 键盘可达性齐备：开关 `role="switch"` + `aria-checked` + 焦点环；徽标 `button` + `aria-expanded` + `aria-controls`；汇总条 header `role="button"` + `tabindex="0"` + Enter / Space（`preventDefault`）。
- 「仅显式」成为跨进程单源，面板渲染结果逐字不变，三条既有断言按新方向改写并全绿。
- 重渲染后滚动位置与焦点保持在代码里落实（`data-skill-name` 定位 + `scrollTop` 回写）。
</success_criteria>

<output>
Create `.planning/phases/50-api-skills/50-04-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号由本计划新建**，不是既有符号的漂移。评审时不得要求它们在 base 上存在。（50-01 已登记的符号不在此重复；本阶段的完整清单以 50-01 的 §「Artifacts this phase produces」为准。）

| 类别 | 符号 | 落点 |
|------|------|------|
| 单源冻结表（**命名交执行期**，建议 `EXPLICIT_TAG`） | 「仅显式」的 `{ label, title }`（挂进 `api` 对象，双模式导出） | `src/skill-picker-model.js` |
| 归一函数 | `setCollapsed(box, toggleEl, collapsed)` —— 折叠态的唯一赋值点（同时写 `.collapsed` 与 `aria-expanded`） | `src/settings-page.js` |
| 前端函数 | `setSkillManageHint(text, tone)` / `resetSkillManageHint()`（含 `clearTimeout` 纪律） | `src/settings-page.js` |
| 前端数据表 | 失败文案的闭合白名单表（键：`not_found` / `not_user_owned` / `invalid_name` / `BODY_TOO_LARGE` + 表外兜底） | `src/settings-page.js` |
| DOM 结构 | 行内右对齐簇新增：`button.skill-manage-diag-badge`（含 `.skill-manage-diag-badge-chevron`）+ `button.ai-switch` + `button.skill-manage-danger-btn` | `src/settings.html` / `src/settings-page.js` |
| DOM 结构 | 诊断详情区 `div.ai-skill-content-box.collapsed.skill-manage-diag`（无 header） | `src/settings-page.js` |
| DOM 结构 | 模块级汇总条 `div.ai-skill-content-box.skill-manage-summary`（**完整家族含 header**，默认展开） | `src/settings.html` / `src/settings-page.js` |
| DOM 结构 | 卸载确认弹框 `.ai-modal-overlay` + `.ai-modal`（复用 `.skill-manage-confirm` 修饰类） | `src/settings.html` |
| CSS 类（新增） | `.skill-manage-actions` / `.skill-manage-actions .ai-switch:disabled` / `.skill-manage-danger-btn` / `.skill-manage-confirm` / `.skill-manage-confirm-body` / `.skill-manage-confirm-note` / `.skill-manage-confirm-actions` / `.skill-manage-diag-badge` / `.skill-manage-diag-badge-chevron` / `.skill-manage-diag` / `.skill-manage-diag-list` / `.skill-manage-diag-item` / `.skill-manage-diag-item-error` / `.skill-manage-diag-item-warning` / `.skill-manage-diag-item-info` / `.skill-manage-diag-level` / `.skill-manage-diag-message` / `.skill-manage-diag-code` / `.skill-manage-summary` / `.skill-manage-state-title` / `.skill-manage-state-title-danger` / `.skill-manage-state-body` | `src/styles/main.css`（专属段内） |
| CSS 作用域收敛覆盖 | `.skill-manage-section .ai-switch:focus-visible` / `.skill-manage-section .slash-picker-tag-explicit` / `.skill-manage-section .ai-skill-content-box-header:hover` | `src/styles/main.css`（专属段内，48/49 宿主零变化） |
| CSS 选择器 | `[aria-expanded="false"] .skill-manage-diag-badge-chevron`（chevron 以 a11y 属性为唯一状态源） | `src/styles/main.css` |
| 测试内容 | `tests/test-skills-management.js` 的交互面组（注入纪律 / hint 复位纪律 / 折叠单点双写 / 跨文件折叠契约 / 失败文案白名单 / 零 `btn-danger`） | `tests/test-skills-management.js` |
| 测试断言（**改写既有**） | `tests/test-skill-picker-model.js:1265-1269` / `:1270-1274` / `:1278` 三条（值断言 / 引用形态断言 / 表级值域隔离） | `tests/test-skill-picker-model.js` |

