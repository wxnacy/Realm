---
phase: 47-bash
plan: 4
type: execute
wave: 3
depends_on: [47-01, 47-02, 47-03]
files_modified:
  - package.json
  - docs/product/ai-skills.md
  - docs/product/ai-agent-workspace.md
  - AGENTS.md
  - tests/test-builtin-skills-seeder.js
autonomous: false
requirements: [SEED-05, DOC-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "打包产物中 `Contents/Resources/app.asar.unpacked/skills-builtin/` 存在，且同时含 `find-skills/SKILL.md` 与 `skill-creator/SKILL.md`（SEED-05 的打包面证据）"
    - "启动该 .app 后 `~/Library/Application Support/realm-nightly/agent-workspace/managed-skills/` 下出现两个技能目录，技能加载器零诊断识别它们 —— **必须在真实打包产物上验证**（`npm run dev` 走 `__dirname` 回落分支，测不到 `app.isPackaged` 那条路径）"
    - "打包产物的 `app.asar` 清单中**不再包含** `.planning/**`（该目录含 `.planning/research/PITFALLS.md` —— 一份逐字记录 `npx skills add <owner/repo@skill> -g -y` 的 RCE 说明书；把它随包分发给用户与 P1 门禁要消除的风险同源）"
    - "打包产物的 `app.asar` 清单中同时**不再包含** `test/**`、`tests/**`、`scripts/**` 与 `*.bak` 备份文件（planning 期实测：最近一次 Nightly 构建的 asar 里有 `test/` 18 项、`tests/` 17 项、`scripts/` 7 项、以及 `main.js.bak` 187,386 B —— 后者是 `make install-nightly` 第 22 行 `cp main.js main.js.bak` 的构建期副产物，等于把一个陈旧整份 `main.js` 随包发给用户）"
    - "**仓库根审查已给出结论**（评审共识项 1 的另一半）：`.github/` **不存在**、`node_modules/.cache/` 不存在且被 electron-builder 默认排除、`dist/` 是 `directories.output` 被默认排除，因此三者**无需**排除项；`docs/**`（36 项）、`AGENTS.md` / `README.md` / `Makefile`、`vendor/**` / `lib/**` / `bin/**` / `cli/**` **保留在包内**，理由逐条写进 47-04-SUMMARY 的审查表（含「`docs/` 经 planning 期 grep 无任何安装语义」这条实测证据）"
    - "`build.files` 的**前提条件**被机械编码：每一项都以 `!` 开头（**不存在正向 allowlist 条目**）—— 这是「`!` 排除在无正向 `files` 列表时才生效」这条 electron-builder 语义的直接断言。任何人日后添加正向条目都会让测试变红，而不是让 5 条（现为 11 条）排除项**静默变成 no-op**（评审共识项 1 的核心风险，`L406@c4c58fe`）"
    - "**没有任何排除项会命中** `skills-builtin/**` 或 `THIRD_PARTY_NOTICES.md`：测试逐条取出排除项并断言其路径段不等于 `skills-builtin` / `THIRD_PARTY_NOTICES.md`（评审共识项 1 要求的补充断言，防止「排除项写宽了把自家交付物一起排掉」）"
    - "`make install-nightly` **不会**覆盖 `package.json` 的 `build.asarUnpack`（research 假设 A2 的实测答案）：该 target 只传 `--config.productName` / `--config.appId` / `--config.mac.icon` 三个**点号合并式**覆盖，不改 `asarUnpack`。**证据（planning 期已取）**：最近一次 Nightly 构建的 `dist/mac-arm64/Realm Nightly.app/Contents/Resources/app.asar.unpacked/node_modules/nodejieba/` 真实存在 —— 即 `package.json` 里唯一那条 `asarUnpack` 在 Nightly 构建下确实生效了"
    - "Nightly 构建的 **asar 清单与 unpacked 目录**两侧都被确认含 `skills-builtin/**`（不只是确认 userData 下的播种结果）：`app.asar.unpacked/skills-builtin/{find-skills,skill-creator}/SKILL.md` 存在（asarUnpack 生效的直接证据），且 `app.asar` 清单里 `skills-builtin/` 与 `THIRD_PARTY_NOTICES.md` 在列（评审共识项 2，`L407@c4c58fe`）"
    - "Nightly 的运行期 userData 路径经实测记录：`~/Library/Application Support/realm-nightly/agent-workspace/managed-skills/`（`app.setName('realm-nightly')` 派生；**不是** `realm-dev` / `realm`），并写进 SUMMARY（评审建议 `L310@c4c58fe`：避免路径写错导致「检查了错的目录却以为通过」）"
    - "`build.files` 的排除项**不波及** `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md`：两者仍在包内（否则 SEED-05 与 P10 同时失效 —— 这是完整 allowlist 方案的主要风险，因此本计划选最小 `!` 排除）"
    - "`docs/product/ai-skills.md` 新增「内置技能」与「bash 包管理器安装档」两章，**追加在 `## 七、测试与验证` 之后**、编号为八与九；既有七节的结构、编号与正文零改动，且章标题在文件中的**出现顺序**为 一…七、八、九（自洽，不出现「六、八、九、七」）"
    - "`docs/product/ai-agent-workspace.md` §四 的第三档判定补上「或包管理器安装表」并新增「强制确认档的两个触发源」小节；§五 的白名单使用建议复核 `brew` 例子；§七 新增第 5 条诚实边界 —— **既有 4 条一字不动，新条序号为「第 5 条」**"
    - "`AGENTS.md` 的「Bash 三档权限」条改写为「强制确认 —— 两个互不包含的触发源」并点名 `PACKAGE_MANAGER_INSTALL_PATTERNS` 的家族清单；测试行的过时计数（「29 例」）改为**本任务实跑 `node --test tests/test-ai-bash-policy.js` 输出中的 `# tests` 值**（判据是「与实跑输出一致」，不是 planning 期基线），并补上新增测试文件"
    - "三份文档口径一致：**沿用「三档权限」框架**（`level` 只有 `allow` / `confirm` 两个取值，`reason` 是档内细分），不出现「四档」说法（decision D-47-02-b / D-47-04-b）"
    - "文档明确写出两条能力边界声明：「**技能不构成额外权限**」与「**`allowed-tools` 当前运行时不被强制，仅供参考**」（DOC-02 字面要求；`ai-skills.md` §五/§六 已有等价表述，本计划负责补齐其在 `ai-agent-workspace.md` 与 `AGENTS.md` 的对应位置）"
    - "文档写明内置技能的自愈式播种语义（每次启动按单技能目录粒度无条件覆盖）、seeded 身份来源（扫随包目录名集合，**非状态文件、非目录位置**）、「不能删改只能禁用」（`settings.aiSkills.disabled`），以及用户定制内置技能的正确通道是 `skills/` 同名遮蔽"
    - "文档写明技能脚本执行的确认成本：`node` / `python3` 命中 `DANGEROUS_INTERPRETERS` → 每次跑技能脚本必弹确认卡片，这是 SKILL-09「零新增权限机制」的必然结果而非缺陷"
  artifacts:
    - "`package.json`：`build.files`（新增字段）含 **11 条 `!` 排除项**（全部为排除、无正向条目）—— 5 条隐藏目录 `!.planning/**`、`!.claude/**`、`!.gsd/**`、`!.wzsh/**`、`!.zcode/**`，3 条开发期目录 `!test/**`、`!tests/**`、`!scripts/**`，1 条备份文件 `!**/*.bak`，2 条根级符号链接 `!CLAUDE.md`、`!CODEBUDDY.md`"
    - "`docs/product/ai-skills.md`：新增 `## 八、内置技能` 与 `## 九、bash 包管理器安装档`；`## 六、已知限制` 补 `REALM_SKILL_CREATOR_PYTHON` 的授权语义一条；`## 七、测试与验证` 补新增测试命令"
    - "`docs/product/ai-agent-workspace.md`：§四 第三档判定 + 新增「强制确认档的两个触发源」小节；§五 白名单使用建议复核；§七 新增第 5 条诚实边界；§八 补新增测试命令"
    - "`AGENTS.md`：`### AI 工作区与 Bash 权限` 小节的「Bash 三档权限」条改写 + 测试行计数修正 + 新增 3 条维护约定（内置技能 / 技能不构成额外权限 + `allowed-tools` 免责 / 随包静态技能目录的维护约定）"
    - "`tests/test-builtin-skills-seeder.js`：新增 DOC-02 静态文本断言分组"
  key_links:
    - "`package.json` 的 `build.asarUnpack: ['skills-builtin/**']` ↔ 打包后 `app.asar.unpacked/skills-builtin/` ↔ 运行时 `process.resourcesPath` 派生路径 —— **三处必须同时成立**，漏任一处即 SEED-05 在正式版静默失败"
    - "`build.files` 的排除项 ↔ `THIRD_PARTY_NOTICES.md` 是否在包内（P10-6 的人工检查点）：排除项必须不波及 repo 根的 `THIRD_PARTY_NOTICES.md` 与 `skills-builtin/**`（有机械断言）"
    - "`build.files` 的**排除语义前提** ↔ 未来维护者：`!` 只在「`files` 里没有正向条目」时保留默认全量包含语义。这条前提写在测试注释、`AGENTS.md` 的维护约定与 47-04-SUMMARY 三处（`package.json` 是 JSON、**不能写注释**，因此不能只靠「文件内注释」）"
    - "三份文档对「三档权限」与「安装档」的表述必须一致 —— 同一事实的三种读者（产品读者 / 安全读者 / 实现读者）"
    - "`settings.aiBashWhitelist` 的语义被 47-02 改变（`npm` 不再能靠白名单免确认）→ `ai-agent-workspace.md` §五 的白名单使用建议必须同步，否则文档会指导用户做出已失效的配置"
  prohibitions:
    - statement: "不得在验证 `/Applications` 下的正式 .app 或执行 `make install` 前跳过 `pgrep` 检查；不得用路径模式 `pkill` / `pkill -f Realm` 清理进程"
      status: unresolved
      verification: judgment
      reason: "AGENTS.md 的打包验证安全纪律：路径模式 pkill 曾误杀生产版实例；`make install` 第一步是 `rm -rf /Applications/Realm.app`，若用户正在运行该 app 会直接删掉运行中的 bundle"
    - statement: "不得用 `npm run dev` 替代打包后实跑验证"
      status: unresolved
      verification: judgment
      reason: "D-12 的强制要求；nodejieba 词典事故（9a1ae11）就是「只在 dev 验证过、打包后启动必崩」的同型教训"
    - statement: "不得在 `build.files` 中添加**任何正向条目**（allowlist）—— 除非同时逐条列入 `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md` 并已实测打包产物"
      status: unresolved
      verification: judgment
      reason: "评审共识项 1（`L406@c4c58fe`）：`!` 排除只在「无正向 allowlist」语义下生效；一旦有人加正向 `files` 列表，全部排除项静默变 no-op，`.planning/**`（含逐字 `npx skills add -g -y`）重新入包，而只断言「排除项存在」的护栏仍会通过。测试里有一条「每一项都必须以 `!` 开头」的机械断言把这条锁死"
    - statement: "不得让任何 `build.files` 排除项的路径段命中 `skills-builtin` 或 `THIRD_PARTY_NOTICES.md`"
      status: unresolved
      verification: judgment
      reason: "评审共识项 1 要求的补充断言：排除项写宽了会把自家交付物一起排掉，SEED-05 与 P10 同时失效，而现有护栏只断言「排除项存在」检不出来"
    - statement: "不得把三份文档改称「四档权限」，也不得改动 `ai-agent-workspace.md` §四 既有标题「bash：三档权限」以外的既有正文（新条序号为「第 5 条」，既有 4 条一字不动）"
      status: unresolved
      verification: judgment
      reason: "D-47-02-b / D-47-04-b：实现上确实是三档；改称四档会牵连标题与 Phase 46 历史文档"
    - statement: "文档不得暗示 `allowed-tools` 会被运行时强制，也不得暗示「技能可以用它限制自己只能用哪些工具」"
      status: unresolved
      verification: judgment
      reason: "DOC-02 与 O3：SDK 的 `Skill` 接口没有该字段，任何制造「按技能授权工具集」印象的表述都是虚假安全感"
    - statement: "文档不得暗示内置技能可以被用户直接改文件长期生效（自愈式播种会在下次启动覆盖）"
      status: unresolved
      verification: judgment
      reason: "D-08/D-09：覆盖前有 warning 诊断但不阻断覆盖；文档必须指明明确定制通道（`skills/` 同名遮蔽）与停用通道（设置页禁用）"
---

<objective>
收口 Phase 47 的两道门禁并同步文档：给打包产物补上 `.planning/**` 的排除项（消除随包分发 RCE 说明书的残余路径）、在真实打包的 .app 上验证内置技能播种（SEED-05 的打包面证据）、把「内置技能」与「bash 包管理器安装档」两章写进三份文档（DOC-02）。

Purpose: 前三个计划各自在源码层面闭合了 P1 / P10 的一部分 —— 但**打包态从未被验证过**，而 SEED-05 的判据就是「打包后正式 .app 中两个内置技能可被正确加载」。同时 47-RESEARCH 的 Pitfall 4 暴露了一条与 P1 同源的残余路径：`build.files` 缺失导致整个 repo（含 `.planning/research/PITFALLS.md` 里逐字的 `npx skills add -g -y`）被打进 asar —— 即使 find-skills 改写完美，分发包里仍有同一份安装指南。文档同步则是 AGENTS.md 强制的维护约定：能力变更不得落后于文档。

Output: `package.json` 的 `build.files` 排除项；三份文档的新章节与修订；`tests/test-builtin-skills-seeder.js` 的 DOC-02 断言；一份打包实跑验证记录（写入 47-04-SUMMARY）。

**关于 tracer 模式的显式偏离**：本计划是「打包复核 + 文档同步」型计划，不引入新的生产代码路径，因此没有 `type="tracer"` 任务。它的端到端证明是 **Task 3 的打包实跑**（唯一能覆盖 `app.isPackaged` 分支的信号），该任务已设为 `autonomous: false` 以强制人工确认 `pgrep` 前置条件。不用「新增切片」充当 tracer，以免制造一个与门禁无关的假纵切。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-bash/47-CONTEXT.md
@.planning/phases/47-bash/47-RESEARCH.md
@.planning/phases/47-bash/47-PATTERNS.md
@.planning/phases/47-bash/47-VALIDATION.md

@package.json
@AGENTS.md
@docs/product/ai-skills.md
@docs/product/ai-agent-workspace.md
@builtin-skills-seeder.js
@ai-bash-policy.js
@tests/test-builtin-skills-seeder.js
@Makefile
</context>

<artifacts_this_phase_produces>
本计划（47-04）新建的符号与命名，供 plan-review-convergence 的 source-grounding 排除新增符号误报：

**`package.json` 变更**
- `build.files` 数组（**新增字段**，当前不存在）：**11 条 `!` 排除项**（全部为排除、无正向条目）—— 5 条隐藏目录 `"!.planning/**"`、`"!.claude/**"`、`"!.gsd/**"`、`"!.wzsh/**"`、`"!.zcode/**"`，3 条开发期目录 `"!test/**"`、`"!tests/**"`、`"!scripts/**"`，1 条备份文件 `"!**/*.bak"`，2 条根级符号链接 `"!CLAUDE.md"`、`"!CODEBUDDY.md"`（与 `must_haves.artifacts` 及 Task 1 step 3 的清单逐字一致）

**文档新增章节 / 小节**
- `docs/product/ai-skills.md`：`## 八、内置技能`、`## 九、bash 包管理器安装档`
- `docs/product/ai-agent-workspace.md`：§四 新增「强制确认档的两个触发源」小节；§七 第 5 条
- `AGENTS.md`：`### AI 工作区与 Bash 权限` 小节内新增 3 条维护约定

**测试文件新增**
- `tests/test-builtin-skills-seeder.js` 内新增 describe 分组（DOC-02 静态文本断言）

**验证记录（非代码产物）**
- 47-04-SUMMARY.md 内的打包实跑记录：`pgrep` 输出、改前/改后 asar 清单核对结果、.app 启动后 `managed-skills/` 的实际内容、两个技能的加载诊断状态

**刻意不产出（decided omission，不是遗漏 —— source-grounding 请据此排除）**
- **不采用 `build.files` 完整 allowlist** —— 选最小 `!` 排除（见 decision D-47-04-a）。
- **不改 `docs/product/ai-agent-workspace.md §四` 的标题**（保持「bash：三档权限（路径不设防，操作必过人）」）。
- **不新增「四档」说法** —— 三份文档统一沿用三档框架 + 第三档两个触发源。
- **不改 `.planning/**` 的内容** —— 本计划只让它们不进分发物。
- **不改 `docs/product/ai-chat-attachments.md` / `navigation-entry-points.md`** —— 与 47 的能力变更无关。
- **不实现 `allowed-tools` 的解析** —— DOC-02 只要求文档声明「当前运行时不被强制」；解析与展示属 Phase 50（O3）。
</artifacts_this_phase_produces>

<decisions>
- **D-47-04-a（Open Question Q4：`.planning/` asar 泄漏）：本阶段修，选最小 `!` 排除方案。** `build.files` 加 **11 条 `!` 排除项**（研究阶段的最小方案是 5 条隐藏目录；本阶段按仓库根实测清单扩到 11 条 —— 追加 `!test/**`、`!tests/**`、`!scripts/**`、`!**/*.bak`、`!CLAUDE.md`、`!CODEBUDDY.md`，权威清单见 Task 1 step 3 与 `must_haves.artifacts`）。理由：① 泄漏物 `.planning/research/PITFALLS.md` 逐字含 `npx skills add <owner/repo@skill> -g -y`，与 P1 要消除的风险**同源** —— 只改写内置技能文本却不堵这条路径，等于门禁留了一个旁路；② 该方案是最小改动面（新增一个 `files` 数组，不动既有默认包含语义）；③ 完整 allowlist 方案需逐条列入 `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md`，漏一条就让 SEED-05 与 P10 同时失效 —— 风险不对称。**风险留痕：** 本决策不阻止任何人日后把 `.planning` 加回 `files`；Phase 48-51 的 CONTEXT 仍会引用 `.planning` 内的研究内容，需在 Phase 51 的文档定稿时复核该排除项仍然存在。
- **D-47-04-b（Open Question Q3 的落地）：三份文档统一沿用「三档权限」框架。** 第三档（强制确认）写成有**两个互不包含的触发源**：危险命令表（本机破坏）与包管理器安装表（网络取第三方代码）。与 47-02 的 D-47-02-b 一致。
- **D-47-04-c（打包验证的安全路径）：优先 `make install-nightly` 而非 `make install`。** 理由：正式版 `/Applications/Realm.app` 需先停用户实例（研究阶段实测该机正在运行 PID 25922 + ~10 helper），而 Nightly 走 `~/Library/Application Support/realm-nightly/` 与独立的 `Realm Nightly.app`，验证 `app.isPackaged` 分支的效力与正式版等价而风险更低。若因故必须验证正式版，先 `pgrep -fl "Realm"` 并请用户自行退出（**不代劳 kill**）。
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: 打包排除项（`.planning/**` 等）与改前/改后 asar 清单取证</name>
  <files>package.json, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - package.json:56-74（`build` 段现状：**无 `files` 字段** → electron-builder 默认全量包含 repo 根，这正是泄漏根因）、:69-73（`asarUnpack` 现含 `node_modules/nodejieba/**` 与 47-01 新增的 `skills-builtin/**`）
    - 47-RESEARCH.md §Pitfall 4（**实测证据**：已构建的 `app.asar` 含 `.planning/` 545 个文件，其中 `.planning/research/PITFALLS.md` 含 `npx skills add -g -y` 的逐字说明；三个方案对比与排除项建议（研究阶段的最小方案只列了 5 条隐藏目录，本阶段按实测清单扩为 11 条））
    - 47-RESEARCH.md §Code Examples 9（`asar` 目录清单解析的工具 —— 用它在改前/改后各跑一次取实证）
    - `dist/builder-effective-config.yaml:11-14`（**planning 期实测证据**：`asarUnpack` 段列出 nodejieba、紧接着 `files: []` —— 证明「未设 `files`」时 electron-builder 走默认全量包含；也是「只放 `!` 排除项」能保留该语义的现场依据）
    - `dist/mac-arm64/Realm Nightly.app/Contents/Resources/app.asar`（**planning 期实测**：总 16,855 条，顶层含 `.planning` 545 / `.claude` 1800 / `.gsd` 7 / `.wzsh` 11 / `.zcode` 1 / `test` 18 / `tests` 17 / `scripts` 7 / `docs` 36 / `main.js.bak` 187,386 B；同目录 `app.asar.unpacked/` 下有 `node_modules/nodejieba` 与 `CLAUDE.md` / `CODEBUDDY.md`）—— 本任务 step 2 的审查表就来自这份清单，改后再取一份对比
    - Makefile:21-30（`install-nightly` target 的实际步骤 —— 第 22 行 `cp main.js main.js.bak` 是 asar 里出现 `main.js.bak` 的根因；第 27 行的 `--config.productName` / `--config.appId` / `--config.mac.icon` 是**点号合并式覆盖**，不改 `asarUnpack`）
    - AGENTS.md §开发/正式环境差异 → 发布前必查（asarUnpack 与 `app.isPackaged` 的三条纪律）
  </read_first>
  <action>
    1. **改前取证**：用 47-RESEARCH.md §Code Examples 9 的 asar 清单解析方式，对当前（或最近一次构建的）`app.asar` 取一份清单，记录三条证据：① `.planning/**` 的条目数（研究实测为 545 个文件）；② `.planning/research/PITFALLS.md` 在列；③ 该文件内确实含 `npx skills add` 逐字文本。这份取证是 D-47-04-a 决策的依据留痕，写入 47-04-SUMMARY。
    2. **仓库根审查（评审共识项 1 的另一半 —— 先审再写排除项）**：用 planning 期已取的实测清单逐目录给结论，把结论表写进 `47-04-SUMMARY.md`（下表是 planning 期实测值，实施时用同法复核一遍即可）：
       | 根条目 | 最近一次 Nightly asar 中的条目数 | 结论 |
       |---|---|---|
       | `.planning/**` | 545（含 `research/PITFALLS.md`） | **排除**（P1 同源泄漏） |
       | `.claude/**` | 1800 | **排除**（agent 配置，20 倍于源码体积） |
       | `.gsd/**` | 7 | **排除**（GSD 缓存；`.gitignore` 已忽略） |
       | `.wzsh/**` | 11 | **排除** |
       | `.zcode/**` | 1 | **排除** |
       | `test/**` | 18 | **排除**（memory 测试脚手架与 fixtures） |
       | `tests/**` | 17 | **排除**（测试套件） |
       | `scripts/**` | 7 | **排除**（构建/生成脚本，含 `postinstall.js`，无运行期 require） |
       | `main.js.bak` | 1（187,386 B） | **排除**（`!**/*.bak`）—— `make install-nightly` 第 22 行 `cp main.js main.js.bak` 的构建期副产物 |
       | `CLAUDE.md` / `CODEBUDDY.md` | 各 1（且被 electron-builder 放进 `app.asar.unpacked/`） | **排除**（根级**符号链接**指向 `AGENTS.md`；打包符号链接语义脆弱） |
       | `.github/**` | — | **不需要**（仓库中不存在；planning 期已 `ls -d .github` 确认） |
       | `node_modules/.cache/` | — | **不需要**（不存在，且 electron-builder 默认排除 `node_modules` 下的 dev 依赖与 `.cache`） |
       | `dist/**` | — | **不需要**（`directories.output`，electron-builder 默认排除） |
       | `docs/**` | 36 | **保留**（planning 期 `grep -rln "npx skills\|npm i \|pip install" docs/` 零命中 → 无安装语义；产品文档随包无害，排除属范围外） |
       | `AGENTS.md` / `README.md` / `Makefile` | 各 1 | **保留**（开发文档；`AGENTS.md` 改写后的「安装档」描述是**策略说明**不是安装处方，且不在任何技能扫描根内） |
       | `vendor/**` / `lib/**` / `bin/**` / `cli/**` / `icons/**` / `src/**` / 根级 `*.js` | 运行期必需 | **保留** |
    3. `package.json` 的 `build` 段新增 `files` 数组，含 **11 条 `!` 排除项**（顺序不重要，但必须全部存在）：`"!.planning/**"`、`"!.claude/**"`、`"!.gsd/**"`、`"!.wzsh/**"`、`"!.zcode/**"`、`"!test/**"`、`"!tests/**"`、`"!scripts/**"`、`"!**/*.bak"`、`"!CLAUDE.md"`、`"!CODEBUDDY.md"`。
       **只加排除项，不加正向 allowlist**（保留既有默认全量包含语义，避免漏列 `skills-builtin/**` / `THIRD_PARTY_NOTICES.md` 而让 SEED-05 与 P10 同时失效）。`package.json` 是 JSON、**不能写注释**，因此这条前提必须落在**三处可见位置**：① `tests/test-builtin-skills-seeder.js` 的护栏断言块注释；② `AGENTS.md` 的维护约定（Task 2 落）；③ `47-04-SUMMARY.md`。三处都要写出「`!` 排除仅在无正向 `files` 条目时生效；日后加正向 allowlist 必须同时列入 `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md`」。
    4. **语法校验（在进入 Task 3 的构建之前必做，评审建议 `L150@c4c58fe`）**：`node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package.json JSON OK')"`。理由：`make install-nightly` 会在 Task 3 消费本任务改过的 `package.json`，JSON 语法错会以一条难懂的 electron-builder 报错出现。
    5. **改后取证**：重新构建取 asar 清单（本任务只需产出清单，不必启动 app —— 启动验证归 Task 3），断言：`.planning/` 条目数为 **0**；`test/` / `tests/` / `scripts/` 条目数为 **0**；清单中**不存在** `main.js.bak`；`skills-builtin/find-skills/SKILL.md` 与 `skills-builtin/skill-creator/SKILL.md` **在列**；`THIRD_PARTY_NOTICES.md` **在列**；`node_modules/nodejieba/**` 与既有 `app.asar.unpacked` 内容（含 `nodejieba`）**不受影响**。
    6. 在 `tests/test-builtin-skills-seeder.js` 加**配置级护栏断言组**（不依赖真实构建，防未来有人删掉排除项或改坏语义）：
       - 断言 11 条排除项全部存在（用一张清单表驱动，**不要**只断言 `files.length` 这类计数，计数会随审查结论变化）；
       - **断言 `build.files` 的每一项都以 `!` 开头**（评审共识项 1 的机械编码：一旦有人加正向 allowlist 条目，这条立刻变红，而不是让排除项静默失效）；
       - **断言没有任何排除项命中 `skills-builtin` 或 `THIRD_PARTY_NOTICES.md`**（逐个排除项取路径段比对，不是简单的 `includes` 全文匹配）；
       - 断言 `build.asarUnpack` 仍同时含 `node_modules/nodejieba/**` 与 `skills-builtin/**`；
       - 注释里写明「这条断言只是配置护栏；**真正的证据是构建后的 asar 清单**，见 47-04-SUMMARY」+ 「`!` 排除仅在无正向 `files` 条目时生效」这条前提。
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const raw=fs.readFileSync('package.json','utf8'); const b=JSON.parse(raw).build; const need=['!.planning/**','!.claude/**','!.gsd/**','!.wzsh/**','!.zcode/**','!test/**','!tests/**','!scripts/**','!**/*.bak','!CLAUDE.md','!CODEBUDDY.md']; const f=b.files||[]; const miss=need.filter(x=>!f.includes(x)); if(miss.length){console.error('missing excludes: '+miss.join(', ')); process.exit(1)} const pos=f.filter(x=>!x.startsWith('!')); if(pos.length){console.error('positive allowlist entries present (excludes become no-ops): '+pos.join(', ')); process.exit(1)} const bad=f.filter(x=>/skills-builtin|THIRD_PARTY_NOTICES/.test(x.replace(/^!/,''))); if(bad.length){console.error('exclude hits own deliverables: '+bad.join(', ')); process.exit(1)} const u=b.asarUnpack||[]; if(!u.includes('skills-builtin/**')||!u.includes('node_modules/nodejieba/**')){console.error('asarUnpack incomplete'); process.exit(1)} console.log('package.json gate OK')"</automated>
    <fails_when>非零退出；或 stdout 不出现 `package.json gate OK`；或 stderr 列出任一 `missing excludes` / `positive allowlist entries present` / `exclude hits own deliverables` / `asarUnpack incomplete`；或 `JSON.parse` 抛错（说明 package.json 语法坏了）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：`package.json` 的 `build.files` 含全部 **11** 条排除项；**每一项都以 `!` 开头**（无正向 allowlist 条目）；`build.asarUnpack` 仍含 `node_modules/nodejieba/**` 与 `skills-builtin/**`
    - 源码断言：**没有任何排除项命中** `skills-builtin` 或 `THIRD_PARTY_NOTICES.md`（逐项路径段比对）
    - 行为断言：`node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` 退出 0（Task 1 与 Task 3 之间的语法护栏）
    - 行为断言：改后构建的 asar 清单中 `.planning/` 条目数为 0（改前 ≥1 且含 `PITFALLS.md`，两次清单都记入 SUMMARY）
    - 行为断言：改后 asar 清单中 `test/` 与 `tests/` 条目数为 0、`scripts/` 条目数为 0、**不存在 `main.js.bak`**（改前分别为 18 / 17 / 7 / 1 —— planning 期实测值，同样记入 SUMMARY）
    - 行为断言：改后 asar 清单中 `skills-builtin/find-skills/SKILL.md`、`skills-builtin/skill-creator/SKILL.md`、`THIRD_PARTY_NOTICES.md` 三者在列
    - 行为断言（**评审建议 `L307@c4c58fe`**）：`.github/` 在改后 asar 清单中**不在列**（该目录本就不存在，这条是把 opcode 的疑问变成一条可留痕的断言而非口头结论）
    - 行为断言：`node_modules/nodejieba/**` 与既有 `app.asar.unpacked` 内容在改后清单中仍然存在（零回归）
    - 记录断言：SUMMARY 含仓库根审查表（每个根条目一行：条目数 + 保留/排除结论 + 理由），且 `.github/` / `node_modules/.cache/` / `dist/` 三行明确写「不需要，原因：…」
    - 行为断言：`node tests/test-builtin-skills-seeder.js` 退出 0（含新增的配置护栏断言组）
  </acceptance_criteria>
  <done>分发包不再携带逐字的 `npx skills add -g -y` 说明书，也不再携带开发期测试、构建脚本、陈旧 `main.js` 备份与 agent 配置：`.planning/**`、`test/**`、`tests/**`、`scripts/**`、`*.bak`、`CLAUDE.md`、`CODEBUDDY.md` 从 asar 清单中消失，而内置技能与归属声明仍在包内；仓库根审查表（每个条目保留/排除 + 理由）留痕在 SUMMARY；11 条排除项的存在与「无正向 allowlist」「不命中自家交付物」三条语义都有测试护栏。</done>
</task>

<task type="auto">
  <name>Task 2: DOC-02 文档同步（`ai-skills.md` 两章 + `ai-agent-workspace.md` 三处 + `AGENTS.md` 维护约定）</name>
  <files>docs/product/ai-skills.md, docs/product/ai-agent-workspace.md, AGENTS.md, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - docs/product/ai-skills.md 全文（既有七节骨架：`:1` 标题与维护约定块、`:28 一、能力`、`:36 二、双目录`、`:51 三、优先级`、`:57 四、限额`、`:71 五、沙箱边界`、`:78 六、已知限制`、`:88 七、测试与验证`；**§五 已含「技能不构成额外权限」，§六 已含 `allowed-tools` 段落** —— DOC-02 不需要重复新增，只需补齐缺口）
    - docs/product/ai-agent-workspace.md §四（`:61-73` 三行表格与「危险命令表」段）、§五（`:75-81` 白名单使用建议）、§七（`:89-94` 四条诚实边界）、§八（测试与验证）
    - AGENTS.md `### AI 工作区与 Bash 权限（agent 根目录 + SDK 内置工具）` 小节全文（含「Bash 三档权限」条与末尾「测试：」行）
    - 47-RESEARCH.md §F-1 / §F-2 / §F-3（三处文档的改动要点逐条给出，含新章位置与要点清单）
    - 47-PATTERNS.md §9 / §10 / §11（三份文档的逐字改动对象与推荐写法）
    - 47-CONTEXT.md D-18；本计划 decision D-47-04-b
  </read_first>
  <action>
    1. `docs/product/ai-skills.md`：**先改文首的「维护约定（重要）」块**（`:3-14`）—— 在既有维护清单里补一条「**修改 `skills-builtin/**` 必须同步核对 `THIRD_PARTY_NOTICES` 的五要素 + 重跑零安装语义扫描**」（评审建议 `L309@c4c58fe`）。**定性说明**：该块**不属于**「既有七节」（一…七是 §标题结构，维护约定是文首引用块），因此这条编辑**不违反**「既有七节零改动」的约束 —— 但要在 SUMMARY 里明确记一句「改了文首维护约定块、七节正文零改动」，避免与下面那条约束看起来冲突。
       然后，在 `## 七、测试与验证` **之后**追加两章（编号顺延为八、九；既有七节的结构、编号与正文零改动）。**为什么追加在末尾而不是插在 §七 之前**：插在 §七 之前会让标题顺序变成「六、八、九、七」（既有 §七 的编号与位置都不动的前提下），读者看到的是编号与位置互相矛盾；追加在末尾则顺序为「一…七、八、九」，编号与位置自洽，且「测试与验证」保持在实现章节之后的收尾位。测试断言里必须显式断言**章标题出现顺序**（用 `indexOf` 比较，而非仅断言标题存在）—— 否则这条顺序约束无法被检出。
       - `## 八、内置技能`：① 随包两个技能（find-skills 改写版 / skill-creator 上游快照）与各自的许可证（MIT / Apache-2.0）及归属声明指向 `THIRD_PARTY_NOTICES.md`；② **自愈式播种语义** —— 每次启动按**单个技能目录**粒度同步、无条件覆盖 `managed-skills/<name>/`、覆盖前若检测到内容差异先产 `warning` 诊断（不静默）；③ **seeded 身份来源** —— 扫随包 `skills-builtin/` 的目录名集合，**不是状态文件、也不是「它在 managed-skills/ 里」**（AI 自建技能同在 managed-skills/，靠目录位置无法区分）；④ **不能删改只能禁用** —— 「停用某内置技能」的正确操作是设置页禁用（`settings.aiSkills.disabled`），手删文件会在下次启动自愈重播；⑤ **用户定制内置技能的正确通道**是复制到 `skills/` 同名目录（同名遮蔽，user 胜出）；⑥ **零安装语义** —— find-skills 只输出候选清单，安装入口在设置页；⑦ skill-creator 的 Python 脚本是**可选路径**，先跑 `check_env.mjs` 拿能力清单。
       - `## 九、bash 包管理器安装档`：① 强制确认档（第三档）的**第二个触发源**，与危险命令表的语义分离（本机破坏 vs 网络取第三方代码）；② 覆盖的包管理器家族清单（npx / npm i·install·ci·exec·add / pnpm·yarn·bun 的 add·dlx·install / pip·pip3 install / python·python3 -m pip install / uv·uvx / brew install·upgrade·reinstall / cargo·go·gem install）；③ **白名单不可越过**（`npm` 不再能靠 `settings.aiBashWhitelist` 免确认）；④ `riskLevel = high` 与专属确认文案；⑤ **只读子命令不在表内**（`npm run` / `test` / `ls` / `view` / `audit` / `outdated`、`brew info` / `list` / `search`、`pip list` / `show` 等）；⑥ **脚本执行的确认成本** —— `node` / `python3` 命中危险解释器表，每次跑技能脚本必弹卡片，这是 SKILL-09「零新增权限机制」的必然结果而非缺陷；⑦ **`npm ci` 也在安装档内** —— 必须写一句「`npm ci` 虽不取新代码，但会执行所有依赖的 `postinstall` 脚本（实质是任意代码执行），因此同样强制确认」（评审 `L181@c4c58fe`：这条论证不能只留在代码注释里，用户侧文档也要有）。
    2. `docs/product/ai-skills.md` 的其他修补：`## 六、已知限制` 补一条 **`REALM_SKILL_CREATOR_PYTHON` 的授权语义**（「该变量由用户自己设置；设置它等于授权 Realm 执行该路径的程序」）；`## 七、测试与验证` 补 `node tests/test-builtin-skills-seeder.js`。
    3. `docs/product/ai-agent-workspace.md`：
       - §四 三行表格的第 3 行「判定」列改为「任一段命中**危险命令表**或**包管理器安装表**」；紧接表格新增「**强制确认档的两个触发源（互不包含）**」小节，逐条列出 install 家族与**不在表内的只读子命令示例**；并写明 `reason` 分别为 `danger` / `install`、均 `riskLevel: high`、**两者进白名单也无效**。
       - §五 白名单使用建议（`:80`）：**保留该建议，只把 `brew` 那半句改精确**。原文是「只加构建类可信命令（`npm run`、`git status`、`brew` 等）」→ 改为「只加构建类可信命令（`npm run`、`git status`、`brew` 等）。注意：白名单**不再覆盖**包管理器安装语义 —— 裸条目 `brew` 仍免确认 `brew` 本身与 `brew info` / `brew list` / `brew search` 等**只读**子命令，但 `brew install` / `brew upgrade` / `brew reinstall` 属强制确认档（第 ③ 档），**加入白名单也无效**；`npm run` 与 `npm i` / `npm install` / `npm ci` / `npm exec` 同理」。**不要删除该建议**（它的核心判断「白名单粒度是前缀不是路径」仍然成立）。
       - §七 新增**第 5 条**诚实边界（既有 4 条一字不动）：「**安装档只审一级 bash 命令** —— 策略引擎看的是用户在卡片上看到的那条命令。若该命令内部再 `spawn` 子进程（如 `check_env.mjs` 内部 `spawnSync` 调 `python3`），二次调用不在策略视野内 —— 由用户对第一条命令的确认承担。同类残余：取值旗标形态与命令替换 / 变量间接构造不被检测，`echo "npm install"` 类字面量会误报。」
       - §八 测试与验证补播种与 install 档的测试命令。
    4. `AGENTS.md` 的 `### AI 工作区与 Bash 权限` 小节：
       - 「**Bash 三档权限**」条改写为「③ 强制确认 —— **两个互不包含的触发源**：**危险段**（`DANGEROUS_PATTERNS` 词边界正则：rm 全系 / sudo / dd / kill / chmod / 重定向覆盖系统路径等 + `DANGEROUS_INTERPRETERS` 管道右侧 sh/node/python 等）与**包管理器安装段**（`PACKAGE_MANAGER_INSTALL_PATTERNS`：npx / npm i·install·ci·exec / pnpm·yarn·bun 的 add·dlx / pip install / uv·uvx / brew install·upgrade / cargo·go·gem install），两者**进白名单也无效**（`reason` 分别为 `danger` / `install`，均 `riskLevel: 'high'`）」。
       - 末尾「测试：」行的过时计数「29 例」**必须改为实跑取到的真实数字**（评审共识项 4 `L409@c4c58fe`：不得沿用文档旧数）。**执行本任务时先跑一次** `node --test tests/test-ai-bash-policy.js 2>&1 | grep "^# tests"`，把输出中的 `# tests` 数值**原样写入** `AGENTS.md` 的「测试：」行 —— **「与实跑输出一致」是本条唯一的权威判据**：行内 bash-policy 的计数必须等于这次实跑的值（本任务属 Wave 3、执行时必然已消费 47-02，故该值**预期 ≥62**），**不得写死任何字面量**；同一行里 `node tests/test-agent-workspace.js` 的 `21 例` 保持不变（无需重跑）。**参照值（仅供比对，不得写入）**：planning 期实测 `node tests/test-agent-workspace.js` = 21 例、`node --test tests/test-ai-bash-policy.js` = 32 例（`# fail 0`）、`node tests/test-ai-skills.js` = 64 例 —— 其中 `32 例` **只是 47-02 落地前的基线**（即该「测试：」行里待替换的旧数 `29 例` 所在位置的历史值），**绝不是在本次任务里要写入的值**。并追加新测试文件条目：`node tests/test-builtin-skills-seeder.js（播种 / 差异诊断 / 零安装语义 / 归属断言，新增）`。
       - 新增第 4 条维护约定（**`build.files` 排除语义的前提**，评审共识项 1 要求的「写进代码附近」的落点之一 —— `package.json` 是 JSON 无注释）：写明「`package.json` 的 `build.files` 只用 `!` 排除项、**没有正向 allowlist**；`!` 仅在无正向 `files` 条目时保留默认全量包含语义。日后若要添加正向 allowlist，必须同时列入 `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md`，否则 SEED-05 与 P10 会同时失效（测试里的 `config` 护栏断言组会变红）」。同时记下 `.planning/**` 被排除的原因（含逐字 `npx skills add -g -y` 的 `PITFALLS.md` 是本阶段 P1 门禁的同源风险）。
       - 新增 3 条维护约定：① **内置技能**（随包 `skills-builtin/`，`asarUnpack` + `app.isPackaged` 路径分支，每次启动自愈式播种，seeded 身份 = 随包目录名集合）；② **技能不构成额外权限** + `allowed-tools` 当前运行时不被强制、**仅供参考**；③ **随包静态技能目录的维护约定**（改动 `skills-builtin/**` 必须同步核对 `THIRD_PARTY_NOTICES` 的五要素 + 跑零安装语义扫描）。
    5. 在 `tests/test-builtin-skills-seeder.js` 新增 DOC-02 断言分组（静态文本断言，照 Phase 46 DOC-01 的 `node -e` 形断言思路）：三份文档分别断言关键章节标题与关键短语存在 —— `ai-skills.md` 含 `## 八、内置技能` / `## 九、` / `包管理器安装` / `自愈` / `settings.aiSkills.disabled`;`ai-agent-workspace.md` 含 `技能不构成额外权限` / `allowed-tools` / `两个触发源` / `第 5 条`（或等价序号标记）；`AGENTS.md` 含 `PACKAGE_MANAGER_INSTALL_PATTERNS` / `test-builtin-skills-seeder.js`。
    6. **口径一致性核对**：三份文档都不得出现「四档」字样（断言 `!/四档/`），且 `ai-agent-workspace.md §四` 的标题保持原文。
    7. **收尾判定口径的记录（防假 gap）**：在 `47-04-SUMMARY.md` 单列一节，把「需求 / CONTEXT 原文仍停在旧写法、已被阶段锁定决策取代」的**三处**如实登记，供 phase 收尾时按锁定决策而非原文字面判读：
       - `REQUIREMENTS.md` SEED-01（仍写 skill-creator「仅保留作者指南部分」）→ 已被 **D-05**（完整保留上游 skill-creator 全部内容）取代；
       - `REQUIREMENTS.md` SEED-02（仍写「版本戳登记表」）→ 已被 **D-08 / D-09 / D-11** 取代（每次启动无条件覆盖 + 覆盖前差异诊断 + 手删自愈重播 + seeded 身份 = 随包目录名集合）；
       - `47-CONTEXT.md` **D-17 的子句**「skill-creator 保留上游原样，标注 unmodified」→ 已被 **D-05 / D-06 / D-07**（完整保留上游内容 + 正文语言分层 + 平台专有内容改写）与 **D-47-03-b**（fail-safe 取 `modified`：Apache-2.0 §4(b) 要求被修改的文件带显著修改声明，把 modified 写成 unmodified 才是潜在违约）取代。**被取代的只是「标注 unmodified」这一个子句 —— D-17 的其余部分（五要素记录、`LICENSE.txt` 随播种落盘）仍完整有效**；`THIRD_PARTY_NOTICES.md` 对两个技能都标 `modified`，该结论已由 47-03 的 prohibition 与 acceptance 锁死（两位评审均已背书）。
       三处都属**显式登记而非静默丢弃**（前两处已在 `47-CONTEXT.md` 的「与需求字面偏离的两处」登记），ROADMAP §Phase 47 判据 1 也已随本次规划收敛改写并由 47-01 Task 2 核对。**本步骤只记录口径，不改 `REQUIREMENTS.md` 与 `47-CONTEXT.md`**（本计划 `files_modified` 不含它们）。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js && node -e "for (const f of ['docs/product/ai-skills.md','docs/product/ai-agent-workspace.md','AGENTS.md']) { const s=require('fs').readFileSync(f,'utf8'); if(/四档/.test(s)) { console.error(f+': 出现「四档」'); process.exit(1); } } console.log('DOC-02 gate OK')"</automated>
    <fails_when>非零退出；或 `node tests/test-builtin-skills-seeder.js` 汇总出现 `# fail` 非 0 计数；或 stdout 不出现 `DOC-02 gate OK`</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：`ai-skills.md` 含 `## 八、内置技能` 与 `## 九、` 两个新章标题，且两章各含上述要点关键词（`自愈` / `seeded` 身份来源 / `settings.aiSkills.disabled` / `skills/` 同名遮蔽 / 家族清单 / `白名单不可越过` / `风险` 相关词）
    - 源码断言（**章序自洽**）：`ai-skills.md` 中 `indexOf('## 七、测试与验证') < indexOf('## 八、内置技能') < indexOf('## 九、')` —— 用位置比较而非仅断言存在（否则「六、八、九、七」这种编号与位置矛盾无法被检出）
    - 源码断言：`ai-skills.md` 的既有七节标题全部仍在（`一、能力` … `七、测试与验证`），且 `## 六、已知限制` 含 `REALM_SKILL_CREATOR_PYTHON`
    - 源码断言：`ai-agent-workspace.md` 含 `两个触发源` 与 `第 5 条`（或等价序号标记），§四 标题仍含「三档权限」，§七 既有 4 条诚实边界的文本未变
    - 源码断言：`AGENTS.md` 含 `PACKAGE_MANAGER_INSTALL_PATTERNS`、`test-builtin-skills-seeder.js`，且「测试：」行不再含 `29 例`
    - 源码断言（**与实跑输出一致 —— 本条唯一的权威判据**）：`AGENTS.md` 的「测试：」行含 `21 例`；并含一个 bash-policy 计数，该计数**必须等于本任务实跑 `node --test tests/test-ai-bash-policy.js` 输出中的 `# tests` 值**（47-04 属 Wave 3、届时已消费 47-02，预期 ≥62）—— 判读方式是把行内数字与本次运行输出**做比对**，而非匹配任何一个预先写死的字面量（`32 例` 是 47-02 落地前的基线，不得据此判过）；且不含 `29 例`
    - 源码断言（**维护约定补齐**）：`AGENTS.md` 含 `build.files` 与「无正向 allowlist」/「`!` 仅在」类表述，且含 `THIRD_PARTY_NOTICES`（维护约定第 4 条落地）
    - 源码断言：`ai-skills.md` 文首维护约定块含 `THIRD_PARTY_NOTICES`（「修改 `skills-builtin/**` 必须同步核对」这条）
    - 源码断言：`ai-skills.md` `## 九、` 章含 `postinstall` 与 `npm ci`（安装档含 `npm ci` 的论证进了用户侧文档）
    - 源码断言（**`brew` 措辞精确化**）：`ai-agent-workspace.md` §五 仍含「只加构建类可信命令」这条建议的原文主干，且新增「白名单**不再覆盖**包管理器安装语义」与 `brew info` / `brew install` 的对照说明
    - 源码断言：三份文档均不含「四档」（口径一致）
    - 源码断言：三份文档均含 `allowed-tools` 与「不被强制」/「仅供参考」的语义（DOC-02 字面要求）
    - 记录断言（**防假 gap**）：SUMMARY 含「需求原文与锁定决策不一致」一节，点名 `REQUIREMENTS.md` SEED-01 被 D-05 取代、SEED-02 被 D-08/D-09/D-11 取代，以及 `47-CONTEXT.md` D-17 的「标注 unmodified」子句被 D-05/D-06/D-07 + D-47-03-b 取代（并写明 D-17 的其余部分仍有效），且写明收尾按锁定决策判读而非原文字面比对
    - 行为断言：`node tests/test-builtin-skills-seeder.js` 退出 0（含新增 DOC-02 断言分组）
  </acceptance_criteria>
  <done>能力变更与文档同步一致：内置技能的自愈式播种、seeded 身份来源、禁用与遮蔽两条正确通道、零安装语义、包管理器安装档的存在与白名单不可越过的语义、以及脚本执行的确认成本，都在产品文档与实现约定里写明；三份文档口径一致（三档框架）且不含虚假安全感表述。</done>
</task>

<task type="auto">
  <name>Task 3: 打包后正式环境实跑验证（SEED-05 收口，`autonomous: false`）</name>
  <files>tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - Makefile（`install` / `install-nightly` 两个 target 的实际步骤 —— 确认 `make install` 第一步是否 `rm -rf /Applications/Realm.app`；确认 `install-nightly` 的三个 `--config.*` 是**点号合并式**覆盖，不改 `asarUnpack`）
    - `dist/builder-effective-config.yaml`（**假设 A2 的证据源**：`asarUnpack` 段 + `files` 段 —— 证明 electron-builder 实际吃到的配置来自 `package.json`）
    - `dist/mac-arm64/Realm Nightly.app/Contents/Resources/app.asar.unpacked/`（**planning 期实测**：含 `node_modules/nodejieba`、`CLAUDE.md`、`CODEBUDDY.md` —— 其中 `nodejieba` 的存在即「Nightly 继承 `asarUnpack`」的直接现场证据）
    - AGENTS.md §构建安装命令 与 §开发/正式环境差异 → 发布前必查（asarUnpack 三条纪律 + Nightly 的独立 appId / userData / 图标）
    - AGENTS.md 的打包验证安全纪律（**先 `pgrep` 查用户实例；清理只按自身 PID；禁止路径模式 `pkill`**）
    - 47-RESEARCH.md §Pitfall 3（**实测证据**：该机正从 `/Applications/Realm.app` 运行 PID 25922 + ~10 helper，而 `make install` 第一步会 `rm -rf` 该目录）、§D-3（`make install` 的验证 4 步）
    - 47-PATTERNS.md Shared Patterns §F（asar 路径分支的三条不变式）
    - builtin-skills-seeder.js 的 `resolveBuiltinSkillsSrc()`（确认它在 `app.isPackaged` 时读 `process.resourcesPath/app.asar.unpacked/skills-builtin`）
    - 47-CONTEXT.md D-12；本计划 decision D-47-04-c
  </read_first>
  <action>
    **本任务 `autonomous: false` —— 存在不可跳过的人工前置检查。**
    1. **前置检查（人工，先做）**：
       a. `pgrep -fl "Realm"` 列出所有 Realm 相关进程，确认是否有用户实例在跑（尤其 `/Applications/Realm.app` 与 `/Applications/Realm Nightly.app`）。
       b. 若有实例在跑：**请用户自行退出**（不要代劳 `kill`，更不得用路径模式 `pkill`）。把 `pgrep` 输出与用户确认记录进 SUMMARY。
       c. 优先走 **Nightly**（decision D-47-04-c）：`make install-nightly`。除非有明确理由要验证正式版，才走 `make install`，且必须先完成 (a)(b)。
    2. **打包验证（评审共识项 2 要求的三个面都要覆盖，`L407@c4c58fe`）**：
       a. 构建并安装（`make install-nightly`）。
       b. **unpacked 面**：确认 `"/Applications/Realm Nightly.app/Contents/Resources/app.asar.unpacked/skills-builtin/"` 下同时存在 `find-skills/SKILL.md` 与 `skill-creator/SKILL.md`（**这是 `asarUnpack` 生效的直接证据**）。
       b2. **asar 清单面（不能只查 unpacked 目录）**：解析 `"/Applications/Realm Nightly.app/Contents/Resources/app.asar"` 的清单，断言 ① `skills-builtin/` 与 `THIRD_PARTY_NOTICES.md` **在列**；② `.planning/`、`test/`、`tests/`、`scripts/` 条目数为 **0** 且**无 `main.js.bak`**。**这是 47-04 Task 1 的改后在真实安装产物上的复核**（Task 1 的构建产物与这里安装的产物是同一次 `make install-nightly` 的产物，但「清单在 dist 里对」与「装到 /Applications 里也对」是两件事）。
       b3. **`asarUnpack` 继承性的显式回答（research 假设 A2）**：`make install-nightly` 只传 `--config.productName` / `--config.appId` / `--config.mac.icon` 三个**点号合并式**覆盖，**不会**改写 `package.json` 的 `build.asarUnpack`。**证据**：① `dist/builder-effective-config.yaml` 里 `asarUnpack` 段来自 `package.json`；② 本次构建后 `app.asar.unpacked/node_modules/nodejieba/` 依然存在（nodejieba 是 `package.json` 里另一条 `asarUnpack`，它生效即证明整段被继承）；③ planning 期在**既有** Nightly 产物上已实测到 `app.asar.unpacked/node_modules/nodejieba` —— 把这三条写进 SUMMARY，把「假设」变成「实测结论」。
       c. **运行期面**：启动该 .app，等待启动完成，检查 **`~/Library/Application Support/realm-nightly/agent-workspace/managed-skills/`**（`app.setName('realm-nightly')` 派生；**不是** `realm-dev` / `realm`）下出现 `find-skills/` 与 `skill-creator/` 两个目录，且各自含 `SKILL.md`（skill-creator 还应含 `LICENSE.txt`、`scripts/`、`agents/`、`references/`、`assets/`、`eval-viewer/`）。**把实际 userData 绝对路径逐字记进 SUMMARY**（评审建议 `L310@c4c58fe`）。
       d. 在该 app 的 AI 助手里问一句能触发技能列表的问题（或直接在 DevTools 里读技能集），确认两个内置技能被加载器识别且**零诊断**（`_cache.diagnostics` 与 `_cache.errors` 为空）；同时确认它们**不占** system prompt（`buildSkillsPrompt()` 为空 —— `disable-model-invocation: true` 生效）。
    3. **幂等与自愈的打包态复核**：重启该 .app 一次，确认播种未产生新的 `realm_builtin_seed_overwritten` 诊断（内容一致 → `detectDiff === 'same'`）；再删掉 `managed-skills/find-skills/` 并第三次启动，确认自愈重播。
    4. 把上述四步的**原始输出与观察结果**逐条写入 `47-04-SUMMARY.md`（含 `pgrep` 输出、目录清单、诊断状态）。这是 SEED-05 与 D-12 的唯一验收面 —— **开发态不可替代**。
    5. 在 `tests/test-builtin-skills-seeder.js` 的注释与断言里记录该任务的人工归属（防后续阶段误以为已自动化覆盖）：明确 `SEED-05` 的打包面证据是**人工实跑**，测试文件不复制其结论。
  </action>
  <verify>
    <automated>ls -1 "/Applications/Realm Nightly.app/Contents/Resources/app.asar.unpacked/skills-builtin/find-skills/SKILL.md" "/Applications/Realm Nightly.app/Contents/Resources/app.asar.unpacked/skills-builtin/skill-creator/SKILL.md" && ls -1 "/Applications/Realm Nightly.app/Contents/Resources/app.asar.unpacked/node_modules/nodejieba" >/dev/null && ls -1 "$HOME/Library/Application Support/realm-nightly/agent-workspace/managed-skills/" && node -e "const fs=require('fs'); const p='/Applications/Realm Nightly.app/Contents/Resources/app.asar'; const fd=fs.openSync(p,'r'); const b=Buffer.alloc(16); fs.readSync(fd,b,0,16,0); const size=b.readUInt32LE(12); const hdr=Buffer.alloc(size); fs.readSync(fd,hdr,0,size,16); const j=JSON.parse(hdr.toString('utf8').replace(/\0+$/,'')); const out=[]; (function walk(n,pre){ for(const [k,v] of Object.entries(n.files||{})){ const pp=pre+'/'+k; if(v.files) walk(v,pp); else out.push(pp);} })(j,''); const cnt=(d)=>out.filter(f=>f.startsWith('/'+d+'/')).length; const bad={planning:cnt('.planning'),test:cnt('test'),tests:cnt('tests'),scripts:cnt('scripts')}; const bak=out.filter(f=>f.endsWith('.bak')).length; const hasSkills=out.some(f=>f.startsWith('/skills-builtin/')); const hasNotices=out.includes('/THIRD_PARTY_NOTICES.md'); console.log('asar manifest:',JSON.stringify({...bad,bak,hasSkills,hasNotices})); if(Object.values(bad).some(v=>v!==0)||bak!==0||!hasSkills||!hasNotices) process.exit(1); console.log('asar manifest gate OK')"</automated>
    <fails_when>任一 `ls` 非零退出（说明 asarUnpack 未生效或播种未发生）；或第二个 `ls` 的输出不同时包含 `find-skills` 与 `skill-creator`；或 `node -e` 非零退出（asar 清单里 `.planning`/`test`/`tests`/`scripts` 条目数不为 0、存在 `*.bak`、或 `skills-builtin/` 与 `THIRD_PARTY_NOTICES.md` 不在列）；或 stdout 不出现 `asar manifest gate OK`；或 SUMMARY 中缺少 `pgrep` 前置检查记录 / Nightly userData 实际路径</fails_when>
  </verify>
  <acceptance_criteria>
    - 人工记录：SUMMARY 含 `pgrep -fl "Realm"` 的输出与「用户是否在运行实例」的确认结论（**未跳过**）
    - 行为断言：`/Applications/Realm Nightly.app/Contents/Resources/app.asar.unpacked/skills-builtin/` 下同时存在两个 `SKILL.md`
    - 行为断言（**asar 清单面，不能只查 unpacked 目录**）：安装产物的 `app.asar` 清单里 `skills-builtin/` 与 `THIRD_PARTY_NOTICES.md` **在列**，且 `.planning/` / `test/` / `tests/` / `scripts/` 条目数为 0、无 `*.bak`
    - 人工记录（**假设 A2 的回答**）：SUMMARY 明确写「`make install-nightly` 继承而非覆盖 `package.json` 的 `build.asarUnpack`」，并给出三条证据（`builder-effective-config.yaml` 的 `asarUnpack` 来源、本次 `app.asar.unpacked/node_modules/nodejieba` 仍在、planning 期在既有 Nightly 产物上的同项实测）
    - 行为断言：`~/Library/Application Support/realm-nightly/agent-workspace/managed-skills/` 下同时存在 `find-skills` 与 `skill-creator` 两个目录，且 `skill-creator/` 内含 `LICENSE.txt` 与 `scripts/check_env.mjs`
    - 人工记录：SUMMARY 含 Nightly 的**实际 userData 绝对路径**（`~/Library/Application Support/realm-nightly/`，不是 `realm-dev` / `realm`）
    - 行为断言：启动后技能集包含两个内置技能，`_cache.diagnostics` 与 `_cache.errors` 均为空数组；`buildSkillsPrompt()` 为空
    - 行为断言：第二次启动不产生新的覆盖诊断（幂等）；手删 `managed-skills/find-skills/` 后第三次启动自愈重播
    - 人工记录：`node skills-builtin/skill-creator/scripts/check_env.mjs` 在打包态 .app 的技能目录内可执行（经确认卡片后），输出可解析 JSON（SKILL-09 的打包态侧证据）
  </acceptance_criteria>
  <done>SEED-05 的打包面判据被真实产物证明：正式打包的 .app 中两个内置技能可被正确加载并播种（**asar 清单面与 unpacked 目录面都查过**），幂等与自愈在打包态成立，`asarUnpack` + `app.isPackaged` 路径分支确实生效（开发态从未测到的那条路径），且 research 假设 A2（`make install-nightly` 是否覆盖 `asarUnpack`）从「假设」升级为「有现场证据的结论」；Nightly 的实际 userData 路径已逐字记录。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 构建产物（`app.asar`）→ 最终用户 | 打包清单决定什么随应用分发；误入的开发期文档等同于随包分发其内容 |
| `app.asar.unpacked/skills-builtin/` → 运行时播种 → `managed-skills/` | 唯一能覆盖 `app.isPackaged` 分支的信号；开发态测不到 |
| 三份文档 → 用户与后续阶段的开发者 | 文档指导用户配置（如白名单）；失效的文档会诱导出失效的配置 |
| `make install*` → `/Applications` | 第一步 `rm -rf` 会删掉目标 bundle；若用户正在运行该应用则直接破坏运行中的实例 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-04-01 | **Information Disclosure / Elevation of Privilege** | `build.files` 缺失导致 `.planning/research/PITFALLS.md`（含逐字 `npx skills add -g -y`）随包分发；同因还带出 `test/**`、`tests/**`、`scripts/**`、陈旧 `main.js.bak`（187,386 B）与 1800 项 agent 配置 | **high** | mitigate | **11 条 `!` 排除项**（planning 期实测清单驱动，非猜测）+ **改前/改后 asar 清单对比取证**（改前 `.planning` 545 / `test` 18 / `tests` 17 / `scripts` 7 / `main.js.bak` 1，改后全为 0）+ 配置级护栏断言组防未来删除。**这是 P1 门禁在分发面的旁路，必须堵** |
| T-47-04-01b | Tampering（未来回归） | 日后有人给 `build.files` 加正向 allowlist → 全部 `!` 排除静默变 no-op、`.planning/**` 重新入包，而「排除项存在」的护栏仍通过 | **high** | mitigate | 测试断言 `build.files` 的**每一项都以 `!` 开头**（正向前提被机械编码）；该前提同时写入 `AGENTS.md` 维护约定与 SUMMARY（`package.json` 是 JSON、无法内联注释，故三处落点） |
| T-47-04-02 | Tampering | `build.files` 排除项波及 `skills-builtin/**` 或 `THIRD_PARTY_NOTICES.md`，使 SEED-05 与 P10 同时失效 | high | mitigate | **选最小 `!` 排除而非完整 allowlist**（保留默认包含语义）；改后清单**显式断言**三者（两个 `SKILL.md` + `THIRD_PARTY_NOTICES.md`）在列；**另加一条「无任何排除项命中 `skills-builtin` / `THIRD_PARTY_NOTICES.md`」的配置级断言**（排除项写宽了会被这条抓住，而不是等到打包后才发现）；`asarUnpack` 与运行时路径两侧成对由 Task 3 的实跑收口 |
| T-47-04-03 | Denial of Service | `make install` 删掉正在运行的 `/Applications/Realm.app` | high | mitigate | Task 3 `autonomous: false` + **不可跳过的人工前置**：先 `pgrep -fl "Realm"` 并由**用户自行退出**；清理只按自身 PID；禁止路径模式 `pkill`；优先 `make install-nightly`（D-47-04-c） |
| T-47-04-04 | Spoofing | 「开发态跑通了」被当作「打包态可用」（nodejieba 事故同型） | high | mitigate | D-12 明文：**不得用 `npm run dev` 替代**；Task 3 的验收面就是打包产物的 `.app`（路径、播种结果、诊断状态三项实测） |
| T-47-04-05 | Repudiation | 文档与实现不一致（文档仍说 `npm` 可白名单免确认） | medium | mitigate | 三份文档同步 + 静态文本断言（关键章标题与短语）+ 口径一致性断言（三份都不得出现「四档」）；`settings.aiBashWhitelist` 语义变化在 §五 显式复核 |
| T-47-04-06 | Tampering | 文档暗示 `allowed-tools` 会被强制（虚假安全感） | medium | mitigate | DOC-02 明文要求带「当前运行时不被强制，仅供参考」；测试断言三份文档均含该语义 |
| T-47-04-SC | Tampering | npm / 依赖安装 | medium | mitigate | **本计划零新增依赖**（只改 `build.files` 与三份文档）；不执行任何包安装命令 → 无包合法性 gate 触发。**注：`make install-nightly` 会触发 electron-builder 构建，但不安装任何新包** |
</threat_model>

<edge_coverage_ledger>
`SPECLESS_FALLBACK=true` / `EDGE_ABSENT=1`：本阶段无 SPEC，edge-probe 把全部 8 条需求判为 `unclassified` / `unresolved`（`applicable = 8`，`resolved = 0`）。按 §A「`unclassified` 一律不得自动 resolve」的纪律，本计划负责的 **2 行**结清如下：**2 = 0（写入 must_haves.truths）+ 2（显式 flagged assumption）**。

| # | requirement_id | probe category | 处置 | 落点 |
|---|----------------|----------------|------|------|
| 1 | SEED-05 | unclassified | **flagged assumption** | 「打包验证在**本机 macOS / 当前架构**上执行一次；未覆盖跨架构（x64 与 arm64）与 Windows 构建 —— `asarUnpack` 的 glob 语义与 `process.resourcesPath` 在两类平台上是否一致，本阶段**未实测**。另：`make install-nightly` 的验证效力被论证为与正式版等价（同一 `app.isPackaged` 分支），但**正式版 .app 本身未实跑**（避免删掉用户运行中的实例，D-47-04-c）」 |
| 2 | DOC-02 | unclassified | **flagged assumption** | 「DOC-02 的验收是**静态文本断言**（章节标题 + 关键词），它证明『文档写了这些』，**不**证明文档表述与实际行为逐条一致 —— 行为一致性由 47-01 / 47-02 / 47-03 的代码断言间接保证，但没有『文档声明 ↔ 代码行为』的自动交叉校验机制。若后续阶段改了行为而未改文档，本阶段的断言仍会通过」 |

> 工具缺口如实记录（来自 orchestrator）：本机安装的 `edge-probe.cjs` 的 `SHAPE_CUES` 映射为空对象，其散文启发式把 8 条需求全部判为 `unclassified`、无法产出类别化探针。因此上表是**文本派生结果，不是本阶段的覆盖上限** —— 真正的验证底座是 `47-VALIDATION.md` 的 P10-6 人工信号与 Task 1-3 的逐条断言。
>
> 至此 8 行 probe 全部结清：47-01 负责 2 行（SEED-02 / SEED-04）、47-02 负责 1 行（SEC-01）、47-03 负责 3 行（SEED-01 / SEED-03 / SKILL-09）、47-04 负责 2 行（SEED-05 / DOC-02）。**0 行 resolved 为 truths、8 行 flagged assumption** —— 符合 §A 对 `unclassified` 的硬性约束（不得自动 resolve）。
</edge_coverage_ledger>

<verification>
1. `node tests/test-builtin-skills-seeder.js` 退出 0（含配置护栏断言与 DOC-02 断言）
2. `node tests/test-ai-skills.js` 退出 0（64 例回归不红）
3. `node tests/test-agent-workspace.js` 退出 0（21 例回归不红）
4. `node --test tests/test-ai-bash-policy.js 2>&1 | grep -E "^# (tests|pass|fail)"` → `# fail 0`
5. `node -e "for (const f of ['docs/product/ai-skills.md','docs/product/ai-agent-workspace.md','AGENTS.md']) { if(/四档/.test(require('fs').readFileSync(f,'utf8'))) process.exit(1) }"` 退出 0（口径一致）
6. 打包实跑：`ls` 两个 `SKILL.md` 于 `app.asar.unpacked/skills-builtin/`；`ls` 两个技能目录于 `realm-nightly/agent-workspace/managed-skills/`
7. asar 清单（**安装产物上复核，不只看 dist**）：`.planning/` / `test/` / `tests/` / `scripts/` 条目数为 0、无 `*.bak`；`skills-builtin/` 与 `THIRD_PARTY_NOTICES.md` 在列
8. `package.json` 语法与配置语义：`node -e "JSON.parse(...)"` 退出 0；`build.files` 的每一项都以 `!` 开头（无正向 allowlist）且无排除项命中 `skills-builtin` / `THIRD_PARTY_NOTICES.md`（Task 1 的 `<verify>` 命令即此判据）
9. `pgrep -fl "Realm"` 的前置检查已执行并记录（**不得跳过**）；SUMMARY 含 Nightly 实际 userData 路径
10. `BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-04")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`）；`git diff "$BASE"..HEAD --stat` 只落在 `package.json` / 三份文档 / 测试文件五处 —— 本计划不碰任何生产代码（**不要用裸 `git diff`**：对已提交的改动它不产生输出，判据会空转；`plan_head_before` 来自 executor 台账，不是 PLAN frontmatter 字段）
</verification>

<success_criteria>
- [ ] `.planning/**` 不再进入分发包；改前/改后两份 asar 清单留痕
- [ ] `test/**` / `tests/**` / `scripts/**` / `*.bak` / 根级符号链接同样不再随包；仓库根审查表（保留/排除 + 理由）写入 SUMMARY
- [ ] `build.files` 的 11 条排除项全为 `!` 形式（**无正向 allowlist** 的前提被机械断言锁死），且无排除项命中 `skills-builtin/**` 或 `THIRD_PARTY_NOTICES.md`
- [ ] research 假设 A2 有明确回答与现场证据：`make install-nightly` **继承** `package.json` 的 `build.asarUnpack`
- [ ] `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md` 仍在包内且仍可解包（asar 清单面 + unpacked 目录面双查）
- [ ] 正式打包的 .app 中两个内置技能可被正确加载并播种（幂等 + 自愈在打包态成立）
- [ ] `ai-skills.md` 新增「内置技能」「bash 包管理器安装档」两章，既有七节零改动
- [ ] `ai-agent-workspace.md` §四/§五/§七 三处修订到位，§七 既有 4 条一字不动
- [ ] `AGENTS.md` 的三档说明与测试行计数更新，3 条维护约定就位
- [ ] 三份文档口径一致（无「四档」），且都含 `allowed-tools` 免责声明
- [ ] 8 行 edge-probe 全部按纪律结清（0 resolved / 8 flagged）
- [ ] 全量回归绿：64 + 21 例 + 策略引擎 `# fail 0`
</success_criteria>

## Review Dispositions Ledger

### Round 1 — c4c58fe

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| `build.files` 的 `!` 排除只在「无正向 allowlist」语义下生效，日后加正向列表会让排除静默失效（共识项 1，`L406@c4c58fe`）| HIGH | Task 1 新增三条机械断言：① 11 条排除项全部存在（清单表驱动，不用计数）；② **`build.files` 每一项都以 `!` 开头**（正向前提被编码，加正向条目立刻变红）；③ **无任何排除项命中 `skills-builtin` / `THIRD_PARTY_NOTICES.md`**。前提同时写入测试注释、`AGENTS.md` 维护约定（Task 2）、SUMMARY 三处（`package.json` 是 JSON、无法内联注释，故三处落点） |
| 仓库根是否还有其它不该随包的目录（opencode 方向，`L301@c4c58fe`）| HIGH | Task 1 step 2：给出**逐条目审查表**（含 planning 期实测条目数），结论为**新增 6 条排除**：`!test/**`（18 项）、`!tests/**`（17 项）、`!scripts/**`（7 项）、`!**/*.bak`（实测 `main.js.bak` 187,386 B）、`!CLAUDE.md`、`!CODEBUDDY.md`（根级符号链接）；`.github/`（不存在）、`node_modules/.cache/`（不存在 + 默认排除）、`dist/`（`directories.output`，默认排除）**写明为何不需要**；`docs/**` 经 grep 证实无安装语义后**保留**并说明理由 |
| 打包态验证必须在实跑记录里确认「Nightly 的 asar 清单含 `skills-builtin/**`」，而非只确认 userData 播种结果（共识项 2，`L407@c4c58fe`）| MEDIUM-HIGH | Task 3 step 2 拆成三个面：**unpacked 面**（b）、**asar 清单面**（b2，解析安装产物的 asar 头并断言），**运行期面**（c/d）；`<verify>` 的自动化命令同时覆盖三者 |
| `make install-nightly` 是否继承 / 覆盖 `package.json` 的 `build.asarUnpack`（research 假设 A2，`L302@c4c58fe` / `L407@c4c58fe`）| MEDIUM-HIGH | Task 3 step 2 b3 + 验收「假设 A2 的回答」：给出**结论与三条现场证据**（`builder-effective-config.yaml` 的 `asarUnpack` 来源；本次 `app.asar.unpacked/node_modules/nodejieba` 仍在；planning 期在既有 Nightly 产物上实测到同一目录）。Makefile 的三个 `--config.*` 是点号合并式覆盖，已在 read_first 与 Task 3 action 里写明 |
| 测试基线数字须以实跑为准（共识项 4，`L409@c4c58fe`）| MEDIUM | Task 2 step 4：`AGENTS.md` 的 `29 例` 必须改为**实跑取到的真实数字**（planning 期基线 workspace 21 例；bash-policy 的 `32 例` 是 47-02 落地前的旧值，只作比对参照、不得写入），且执行时**重跑一次取 47-02 落地后的真实数字**；验收以「**与实跑输出一致**」为唯一权威判据：「测试：」行含 `21 例`、含等于本次 `# tests` 实跑值的 bash-policy 计数（预期 ≥62）、不含 `29 例` —— 已按用户授权修订，不再锁定 `32 例` 字面量（该锁定曾使 <action> 与 <acceptance_criteria> 互斥） |
| `ai-manager.js` 的三分支改动可能遗漏消费点（`L351@c4c58fe`）| HIGH（分歧项）| 已在 **47-02** Task 1 step 11 落为 grep 复核（生产代码调用点唯一 = `ai-manager.js`）。本计划不重复 |
| `docs/product/ai-skills.md` 维护约定块要补「修改 `skills-builtin/**` 必须同步核对 `THIRD_PARTY_NOTICES`」（`L309@c4c58fe`）| LOW | Task 2 step 1 开头：改文首维护约定块（**不属于既有七节**，不违反「七节零改动」），并明确要求在 SUMMARY 里记一句以免与「七节零改动」看起来冲突；验收含 `THIRD_PARTY_NOTICES` 断言 |
| Task 3 SUMMARY 要记录 Nightly 的实际 userData 路径（`L310@c4c58fe`）| LOW | Task 3 step 2 c + 验收：逐字记录 `~/Library/Application Support/realm-nightly/`（并写明**不是** `realm-dev` / `realm`） |
| Task 1 改后取证要显式断言 `test/` / `.github/` 不在 asar 清单中（`L307@c4c58fe`）| LOW | Task 1 验收：新增 `test/` / `tests/` / `scripts/` 条目数为 0、无 `main.js.bak`、`.github/` 不在列三条断言（`.github/` 本就不存在，把口头结论变成可留痕断言） |
| Task 1 与 Task 3 之间要加 `package.json` 语法校验（`L150@c4c58fe`）| LOW | Task 1 新增 step 4（`JSON.parse` 校验）+ 验收条目 + `<verification>` #8；Task 1 的 `<verify>` 命令本身即 `JSON.parse(require('fs').readFileSync('package.json','utf8'))`，语法坏了会先在那里红 |
| `ai-agent-workspace.md` §五 的 `brew` 例子改动要给出确切措辞（`L149@c4c58fe`）| LOW | Task 2 step 3：给出**改前原文与改后逐字文本**（保留「白名单粒度是前缀不是路径」的核心判断，补「白名单不覆盖包管理器安装语义」+ `brew info` 与 `brew install` 对照）。验收含同款断言 |
| `npm ci` 的 `postinstall` 论证应进用户侧文档（`L181@c4c58fe`）| MEDIUM | Task 2 step 1 的 `## 九、` 要点新增 ⑦（`npm ci` 也在安装档 + `postinstall` 理由）；验收新增 `postinstall` / `npm ci` 断言 |
| 打包实跑验证不可自动化、依赖用户先关实例（共识项 2 的另一半）| MEDIUM-HIGH | Task 3 保持 `autonomous: false` + 不可跳过的 `pgrep -fl "Realm"` 人工前置（由用户自行退出、禁止路径模式 `pkill`），并给出可执行的核对命令（`<verify>` 的 `ls` + asar 清单解析 `node -e`） |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 跨架构（x64 / arm64）与 Windows 构建下 `asarUnpack` glob 与 `process.resourcesPath` 的等价性未实测（`L288@c4c58fe` 的 flagged assumption）| 本机为 arm64 macOS，`make install-nightly` 只产 arm64；跨平台验证**不在本阶段范围**，已作为显式 flagged assumption 保留在 `<edge_coverage_ledger>`，未静默丢弃 |
| 「文档声明 ↔ 代码行为」的自动交叉校验机制（`L289@c4c58fe`）| 本阶段的 DOC-02 断言是**静态文本断言**，只能证明「文档写了」；建立行为级交叉校验需要一套新的契约测试框架，属独立工程，超出本阶段。已如实记录为 flagged assumption |
| 三份文档的长期同步维护成本（`L303@c4c58fe`）| 已由 `AGENTS.md` 的强制维护约定（新增第 4 条）+ `ai-skills.md` 文首维护约定块承接；**不做自动检测**（「文档是否与行为一致」无法静态判定），避免制造虚假的自动化承诺 |
| 用 `npm run dev` 之外的开发态替代验证（如 `electron .` 直跑打包目录）| 不采纳：`app.isPackaged` 只有在真实 .app 里才为真，任何开发态替代都测不到那条路径（D-12 的原文要求就是「不得用 `npm run dev` 替代」） |

<output>
Create `.planning/phases/47-bash/47-04-SUMMARY.md` when done
</output>
