---
phase: 46-prompt
plan: 3
type: execute
wave: 3
depends_on: [46-02]
files_modified:
  - ai-skills-manager.js
  - ai-manager.js
  - tests/test-ai-skills.js
autonomous: true
requirements: [SKILL-06, SKILL-07, SKILL-08]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "诊断合并是显式映射，不是字段名想当然：SDK 诊断的严重度字段是 `type`（值恒为 `'warning'`），Realm 形状是 `level`；合并函数按 `type === 'warning' ? 'warning' : 'error'` 映射，并保留 `code` / `message` / `path` / `source`。测试断言「SDK 诊断经合并后 `level === 'warning'` 且 `code` 原样保留」"
    - "诊断容器空态语义：无任何诊断时 `diagnostics` 与 `errors` 均为 `[]`（不是 `undefined` / `null`）—— `getSkillsSnapshot().diagnostics.length` 与 `.errors.length` 在零诊断时安全返回 0，Phase 50 消费者无需空值守卫"
    - "诊断字段口径：`code` 是 ASCII 枚举比对（SDK 的 5 个值 + Realm 的 `realm_` 前缀族），不做本地化、不做大小写归一化；Realm 自建诊断的 `code` 与 SDK 五枚举**零重叠**；限额类诊断额外带 `limit` 与 `currentValue` 两个数值字段"
    - "分层降级第 1 层（单技能失败 = 正常态）：非法 name / 超长 description / YAML 解析失败 / 超限的单个技能被跳过、其余技能照常注入，且 `refreshSkills()` **不抛错**、`_cache.skills` 中其余条目完整"
    - "分层降级第 2 层（整批抛错）：`refreshSkills()` 任一批量错误路径上都**不清空** `_cache.skills` / `_cache.promptBlock`（保留上一次成功快照），并往模块级 `errors[]` 追加一条 `level === 'error'`、`code === 'realm_refresh_failed'` 的诊断；对既有技能集零影响"
    - "目录缺失不静默：`refreshSkills` 对每个扫描根先做 `env.exists()` 断言，缺失时产 `level === 'error'` 诊断（`code === 'realm_skills_dir_missing'`）—— 不依赖 SDK（SDK 对不存在的目录静默 `continue`，零诊断）"
    - "三条限额全部生效且单源：`MAX_SKILL_MD_BYTES`（64 KiB，按字节）在 SDK 解析前拒绝超大 `SKILL.md`；`MAX_USER_SKILLS`（50，按 user 来源条目数）超限时多余条目标 `overLimit` 而不剔除；`SKILLS_PROMPT_CHAR_BUDGET`（8000，按 `string.length`，含 SDK 前言与包裹的固定开销）超限时按 D-10 优先序贪心截断"
    - "超限提示可操作：任一限额触发时，对应诊断必带 `limit` 与 `currentValue` 两个数值字段（SKILL-07 的「哪个限额 / 当前值」），message 亦含具体数值"
    - "prompt 段超预算时不静默消失：段尾（`</available_skills>` 之外）追加一行省略提示 `Note: <omitted> of <eligible> skills omitted to stay within the prompt budget.`，并产 `realm_prompt_budget_exceeded` 诊断（含 `limit` / `currentValue`）；未截断时该行完全不出现"
    - "定序确定：prompt 段的条目顺序由显式全序比较器决定（user 来源 > `disableModelInvocation !== true` > `name` 码点序），**不使用 `localeCompare`**（其结果依赖运行环境 ICU / 区域设置，会让同一技能集跨机器排出不同顺序 → provider 前缀缓存漂移）"
    - "启停不删文件（SKILL-08）：`settings.aiSkills.disabled` 中的 name 对应条目在 `_cache.skills` 里保留并标 `disabled === true`，只是不进 prompt 段；磁盘上的 `SKILL.md` 与目录不被改动或删除"
  artifacts:
    - "ai-skills-manager.js：`toRealmDiag(sdkDiag)`（`type` → `level` 显式映射）、`pushError(errDiag)`（模块级 `errors[]`）、`createBudgetDiag(code, { limit, currentValue, message })`"
    - "ai-skills-manager.js：`bySkillPriority(a, b)` 确定性全序比较器（作用于缓存条目：`a.source` 与 `a.skill.name` / `a.skill.disableModelInvocation`）"
    - "ai-skills-manager.js：`dummyOverhead` / `entryCost(skill)` 差量测量（`formatSkillsForSystemPrompt([dummy]).length` 作固定开销）"
    - "ai-skills-manager.js：缓存条目新增字段 `disabled`（布尔）、`overLimit`（布尔）"
    - "新诊断码：`realm_refresh_failed`、`realm_skills_dir_missing`、`realm_skill_md_too_large`、`realm_user_skill_limit_exceeded`、`realm_prompt_budget_exceeded`（均含 `limit` + `currentValue`，除 `realm_refresh_failed` 与 `realm_skills_dir_missing`）"
    - "ai-manager.js：`refreshSkills` 调用点的 `disabled` 参数已是 46-01 落的注入式读取（`this.configStore.get('settings.aiSkills.disabled', [])`）—— 本计划只需消费，不新增读取点"
    - "tests/test-ai-skills.js：新增 `describe('诊断与限额（SKILL-06/07）')`、`describe('启停状态（SKILL-08）')` 两组断言"
  key_links:
    - "`settings.aiSkills.disabled`（string[] of name）→ `ai-manager` 注入式读取（`:5511-5513` 的 `settings.aiBashWhitelist` 先例）→ `refreshSkills(env, { disabled })` → 条目标 `disabled` → prompt 组装的 `!e.disabled` 过滤 → 文件保持不动"
    - "`createSkillsEnv.readTextFile` 拒绝超限文件 → `droppedNotices` 记 `{ kind: 'oversize_skill_md', limit, currentValue }` → `refreshSkills` 转成带 `limit`/`currentValue` 的 Realm 诊断（SDK 自己那条 `read_failed` 也保留，两条并存：一条是 SDK 的事实记录，一条是 Realm 的可操作提示）"
    - "`formatSkillsForSystemPrompt([dummy]).length` 作 `fixedOverhead` → `entryCost(skill)` 差量 → 按 `bySkillPriority` 贪心填充至 `SKILLS_PROMPT_CHAR_BUDGET` → 段尾省略提示 + 预算诊断（**不复制 SDK 的字符串模板**：差量法只依赖「每条目脚手架固定」，SDK 升级自动跟随）"
  prohibitions:
    - statement: "`SKILL.md` 的字节上限不得在 SDK 解析之后才生效 —— 必须在 `readTextFile` 阶段、YAML 解析发生之前按 `FileInfo.size` 拒绝"
      status: unresolved
      verification: judgment
      reason: "P7 硬要求：先 loadSkills 再按大小过滤意味着 SDK 已经全量读入并 parse 了那个畸形 / 超大 YAML，主进程同步解析 MB 级畸形 YAML 会明显卡顿甚至抛错"
    - statement: "单技能失败不得导致整批 throw，也不得清空技能集；整批失败不得清空上一次成功快照"
      status: unresolved
      verification: judgment
      reason: "D-05 分层降级；技能加载是「部分成功」语义（照 ai-attachments-manager 的 errors[] 部分成功先例），不是 ai-memory-manager 的 fail-closed 语义"
    - statement: "禁用技能不得删除任何磁盘文件，也不得从 `_cache.skills` 移除（否则 Phase 50 无法列出它来重新启用，用户禁用后技能永久消失）"
      status: unresolved
      verification: judgment
      reason: "SKILL-08 明文「不删文件」；与 D-06 的遮蔽保留哲学对称 —— 过滤发生在消费侧（prompt 段组装、未来 /skill: 解析），不发生在数据层"
---

<objective>
把 46-01/46-02 建立的技能集变成「可解释、有上限、可启停」的：诊断统一收敛为 Realm 形状（显式字段映射 + 模块级 `errors[]`），失败分层降级（单技能跳过 / 整批保留旧快照），三条限额在正确的位置生效并给出「哪个限额、当前值」，定序用确定性全序保证跨机一致，启用/禁用只过滤不删文件。

Purpose: 没有这一层，前两个计划的成果是「能跑但不可信」——SDK 的失效面全是 warning（`skills.js` 无一处 throw），不主动透传就等于把「装了但没生效」变成不可自查；没有字节闸，一个 MB 级畸形 YAML 能让主进程卡死；没有确定性定序，同一个技能集在不同机器上会排出不同前缀，provider 前缀缓存命中率不可预测；没有启停的数据层保留，用户禁用一次即永久失去该技能。

Output: `ai-skills-manager.js` 的诊断合并层、分层降级、三限额生效、确定性定序与启停过滤；`tests/test-ai-skills.js` 两组新断言（约 10 例）。

**tracer 说明**：本阶段的唯一 `type="tracer"` 任务在 46-01（Task 1）。本计划在同一模块内细化已跑通的链路（诊断 / 限额 / 定序 / 启停），不引入新的跨层路径。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-prompt/46-CONTEXT.md
@.planning/phases/46-prompt/46-RESEARCH.md
@.planning/phases/46-prompt/46-PATTERNS.md
@.planning/phases/46-prompt/46-VALIDATION.md

@ai-skills-manager.js
@ai-manager.js
@ai-memory-manager.js
@tests/test-ai-skills.js
</context>

<artifacts_this_phase_produces>
本计划（46-03）新增的符号：

- `ai-skills-manager.js`：`toRealmDiag(sdkDiag)`、`pushError(errDiag)`、`createBudgetDiag(code, opts)`、`bySkillPriority(a, b)`、`entryCost(skill)` 与模块级 `fixedOverhead`
- 缓存条目字段：`disabled`、`overLimit`
- 诊断 `code` 取值：`realm_refresh_failed`、`realm_skills_dir_missing`、`realm_skill_md_too_large`、`realm_user_skill_limit_exceeded`、`realm_prompt_budget_exceeded`
- 诊断附加字段（限额族）：`limit`、`currentValue`
- prompt 段省略提示行格式：`Note: <omitted> of <eligible> skills omitted to stay within the prompt budget.`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 诊断合并（`type` → `level` 显式映射）+ 模块级 `errors[]` + 双层降级 + 目录缺失断言</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - ai-skills-manager.js 当前全文（46-01/46-02 后的 `refreshSkills` 管线、`pushEntryDiag`、`_cache` 形状）
    - 46-RESEARCH.md §SDK 契约 C 全文（SDK 诊断真实形状 `{type:'warning', code, message, path}`；四类诊断实测原文；**与 D-07 的字段差异表**：严重度 `type` ↔ `level`、限额附注 SDK 无 ↔ Realm 补 `limit`/`currentValue`、来源 `source` 仅 `loadSourcedSkills` 追加；`description is required` 先于 `return { skill: null }` 产生的语义陷阱）、§Pitfall 4（`level` vs `type` 错位是最易踩的字段名问题）、§Pitfall 6（不存在目录静默 `continue`、零诊断，实测 `{"skills":0,"diags":0}`）、§Code Examples §加载 + 去重 + 遮蔽 的 `toRealmDiag` 形状
    - 46-PATTERNS.md §1c（D-05 两层语义 + 「与 analog 语义相反，注意不要照抄 fail-closed」告警）、§1d（`realm_` 前缀与 SDK 五枚举不重叠）、§S5（部分成功 / 不静默失败）
    - ai-attachments-manager.js:174-185（**部分成功语义**的正确形状：单项失败进 `errors` 数组、不整批 throw）
    - ai-memory-manager.js:8-12（analog 的 fail-closed 语义 —— 本阶段**禁止照抄**）
    - 46-CONTEXT.md D-05 / D-07
    - **勘误提醒**：SDK 严重度字段是 `type`（恒为 `"warning"`），**不是** `level`；`level` 是 Realm 自建诊断的字段名，合并必须显式映射
  </read_first>
  <action>
    1. `ai-skills-manager.js` 新增 `function toRealmDiag(d)`：`{ level: d.type === 'warning' ? 'warning' : 'error', code: d.code, message: d.message, path: d.path, source: d.source }`。JSDoc 必须写明「SDK 的严重度字段名是 `type`（当前恒为 `'warning'`），Realm 形状是 `level`；本函数是**唯一**的映射点，禁止在任何调用点直接读 `d.level`」。透传 `code` / `message` / `path` / `source` 不加工（SDK 的 `message` 天然含限额与当前值，如 `description exceeds 1024 characters (1100)`）。
    2. 新增 `function pushError(errDiag)`：把诊断 push 进 `_cache.errors`（模块级，供 Phase 50 在任意时刻同步读到上次失败原因）。
    3. `refreshSkills` 全流程用 `try { ... } catch (err) { pushError({ level: 'error', code: 'realm_refresh_failed', message: \`技能集刷新失败：${err && err.message}（已保留上一次成功快照）\` }); return _cache; }` 包住。**catch 分支必须一行都不碰 `_cache.skills` / `_cache.promptBlock` / `_cache.diagnostics`**（D-05 第 2 层：保留上一次成功快照，不清空、不静默）。
    4. 成功路径上：`_cache.diagnostics = sdkDiags.map(toRealmDiag).concat(自建诊断…)`，并在**每次成功刷新开始时**把 `_cache.errors` 复位为 `[]`（`errors[]` 只承载「本次刷新失败」；上一次的失败态不应永远挂着）。
    5. 目录缺失断言（补 SDK 的静默缺口）：`refreshSkills` 进入加载前对 `opts.rootDirs` 每一项各做一次 `const ex = await env.exists(p);`，为假时 `pushError({ level: 'error', code: 'realm_skills_dir_missing', message: \`技能目录不存在：${p}（应用启动时应由 ensureWorkspaceDir 创建）\` })`。注意：断言失败也**继续**执行加载（其余目录的技能仍然要有结果），只追加诊断。
    6. 空态契约：`_cache.diagnostics` 与 `_cache.errors` 初始值、复位值、以及「零诊断」时的值一律为 `[]`，不得是 `undefined` / `null`。
    7. `tests/test-ai-skills.js` 新增 `describe('诊断与限额（SKILL-06/07）')` 的**诊断部分**：
       - **第 1 层降级**：同一目录下同时放合法 `skills/alpha/SKILL.md`、YAML 解析失败的 `skills/broken/SKILL.md`、`description` 超 1024 字符的 `skills/longdesc/SKILL.md` → 断言 `refreshSkills()` **正常 resolve（不抛错）**；`alpha` 仍在集合且仍在 `buildSkillsPrompt()` 输出中；`broken` / `longdesc` 不在集合中；`_cache.diagnostics` 分别含 `code === 'parse_failed'` 与 `code === 'invalid_metadata'`，且二者 `level === 'warning'`（**证明 `type` → `level` 映射真的执行了**，不是读到 `undefined`）。
       - **第 2 层降级**：先用好目录完成一次成功刷新（记下 `getSkillsSnapshot().skills.length`），再用同一真实沙箱 env 包一层 stub（`{ ...sandboxEnv, async listDir() { throw new Error('simulated unreadable dir'); } }`）调用 `refreshSkills` → 断言调用**不抛错**、`_cache.skills.length` 与上一次相同（快照保留）、`_cache.errors` 含 `code === 'realm_refresh_failed'` 且 `level === 'error'`。（可行性依据：SDK 的 `loadSkills` / `loadSkillsFromDirInternal` 顶层无 try/catch，`await env.listDir(dir)` 的 rejection 会一路上抛。）
       - **目录缺失**：`refreshSkills` 传一个不存在的目录 → `_cache.errors` 含 `code === 'realm_skills_dir_missing'`（SDK 自身对此零诊断，这条必须由 Realm 产出）。
       - **空态**：`_resetCacheForTest()` 后断言 `getSkillsSnapshot().diagnostics.length === 0` 且 `.errors.length === 0`（不是 `undefined`）。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `诊断与限额（SKILL-06/07）` 分组内任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：SDK 诊断经合并后 `level === 'warning'`（而非 `undefined`），`code` 原样保留 —— 直接证明 `type` → `level` 映射存在
    - 行为断言：单技能失败（YAML 失败 / description 超长）时 `refreshSkills()` 正常 resolve，其余技能仍在集合与 prompt 输出中
    - 行为断言：批量错误（stub env 的 `listDir` 抛错）时 `refreshSkills()` 不抛错、`_cache.skills` 长度不变、`_cache.errors` 含 `realm_refresh_failed`
    - 行为断言：扫描根不存在时 `_cache.errors` 含 `realm_skills_dir_missing`（不依赖 SDK）
    - 行为断言：零诊断时 `diagnostics` 与 `errors` 均为长度 0 的数组（非 `undefined` / `null`）
    - 源码断言：`ai-skills-manager.js` 含 `function toRealmDiag(`，且其映射表达式为 `d.type === 'warning' ? 'warning' : 'error'`
  </acceptance_criteria>
  <done>技能加载的每一个失败面都变成可读、可同步取到的诊断；单个技能坏掉不影响其余技能，整批刷新失败也不会让技能集消失。</done>
</task>

<task type="auto">
  <name>Task 2: 三限额生效 —— 字节闸可操作诊断 / 数量超限保留 / prompt 段预算截断与省略提示</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - ai-skills-manager.js 当前全文（46-01 的 `createSkillsEnv.readTextFile` 字节闸 + `droppedNotices`、Task 1 的 `createBudgetDiag` 落点、`_cache.promptBlock` 组装处）
    - 46-RESEARCH.md §Code Examples §D-10 预算截断全文（`fixedOverhead` / `entryCost` 差量法、贪心填充、段尾省略提示的精确英文句式、`realm_prompt_budget_exceeded` 诊断形状）、§Pattern 4（确定性全序比较器，**不得**用 `localeCompare`，附理由：跨机 ICU 漂移 → 前缀缓存漂移）、§Assumptions Log A1（差量法成立的前提：SDK 每条目脚手架长度固定）、§Open Question 3（8000 是**整段预算**，`fixedOverhead` 计入 —— 因为 `getContextUsage()` 按 `buildSystemPrompt().length / 4` 估 token）、§Open Question 5（`MAX_USER_SKILLS` 超限实现为加载期诊断而非拒绝，数据层不剔除，不删文件）、§Pitfall 5、§Pitfall 7
    - 46-PATTERNS.md §1g（定序比较器：SDK 内部用 `localeCompare`，Realm **不复用**）、§1a（`LIMITS` 单源）、§No Analog Found（差量测量法无先例，必须用差量而非自拼模板）
    - ai-memory-manager.js:19（`BUDGETS` 常量单源先例 —— 端点与前端零字面量）
    - ai-manager.js:2588（`getContextUsage` 用 `Math.ceil(buildSystemPrompt().length / 4)` —— 「整段预算」口径的依据）
    - 46-CONTEXT.md D-10 / D-11 与 <specifics>（两条限额单位不同，勿混为一谈）
  </read_first>
  <action>
    1. **字节闸诊断**：`createSkillsEnv.readTextFile` 在拒绝超限 `SKILL.md` 时，除返回 `FileError('invalid', …)` 外，同时把 `{ path: p, kind: 'oversize_skill_md', limit: opts.maxSkillMdBytes, currentValue: info.value.size }` push 进 `droppedNotices`。`refreshSkills` 把 `kind === 'oversize_skill_md'` 的记录转成 Realm 诊断：`{ level: 'error', code: 'realm_skill_md_too_large', message: \`SKILL.md 超过正文上限：限额 ${limit} 字节，当前 ${currentValue} 字节（${path}）\`, path, limit, currentValue }`。SDK 自身那条 `read_failed` 诊断保留不删（两条并存：一条 SDK 事实记录、一条 Realm 可操作提示）—— **不要**做消息文本解析去合并，靠 `droppedNotices` 的带外记录。
    2. **确定性定序**：新增 `function bySkillPriority(a, b)`，作用于**缓存条目**（`a.source` 与 `a.skill.name` / `a.skill.disableModelInvocation`）：`(a.source === 'user' ? 0 : 1) - (b.source === 'user' ? 0 : 1) || (a.skill.disableModelInvocation === true ? 1 : 0) - (b.skill.disableModelInvocation === true ? 1 : 0) || (a.skill.name < b.skill.name ? -1 : a.skill.name > b.skill.name ? 1 : 0)`。JSDoc 写明「**不得**改用 `localeCompare`：它依赖运行环境 ICU / 区域设置，同一技能集跨机器可能排出不同顺序 → provider 前缀缓存漂移」。该比较器是**全序**（D-08 重写后单目录内 name 唯一、跨目录同名已由遮蔽处理），用它对 `_cache.skills` 做一次稳定排序后写回（顺序即权威顺序，`getSkillsSnapshot()` 的消费者看到的就是它）。**跨计划口径（单一权威，取代 46-02 的旧断言）**：本比较器是 `getSkillsSnapshot().skills` / `buildSkillsPrompt()` 可观测顺序的**唯一**规定者 —— 46-02 已改为不对集合顺序做任何断言（其「输入顺序被原样保留」的旧断言由本条取代，替换为与顺序无关的「被遮蔽者必须位于 `managed-skills/` 下」）；此后任何对集合顺序的断言都只能以本条为权威依据，不得再有第二个计划规定顺序。
    3. **数量上限**：定义「可注入候选」= `_cache.skills.filter((e) => e.source === 'user')` 的长度上限 `LIMITS.MAX_USER_SKILLS`。超出时按 `bySkillPriority` 排序取前 `MAX_USER_SKILLS` 个为可注入，其余 user 条目标 `overLimit = true`。**不剔除、不删文件**。产 `{ level: 'error', code: 'realm_user_skill_limit_exceeded', message: \`用户技能数量超过上限：限额 ${MAX_USER_SKILLS} 个，当前 ${current} 个；超出部分不进 system prompt（请先卸载不用的技能）\`, limit, currentValue }`。
    4. **prompt 段预算（整段口径，Open Q3）**：`const fixedOverhead = formatSkillsForSystemPrompt([{ name: '', description: '', filePath: '' }]).length;` 与 `const entryCost = (skill) => formatSkillsForSystemPrompt([skill]).length - fixedOverhead;`（**差量测量，不自拼字符串** —— 自拼等于复制 SDK 的转义与缩进模板，SDK 升级即漂移）。可注入集合 = `_cache.skills.filter((e) => !e.shadowed && !e.disabled && !e.overLimit && e.skill.disableModelInvocation !== true)`，按 `bySkillPriority` 顺序贪心填充：`used = fixedOverhead`，逐条 `if (used + entryCost(e.skill) > LIMITS.SKILLS_PROMPT_CHAR_BUDGET) break;` 否则纳入并累加。`_cache.promptBlock = kept.length ? formatSkillsForSystemPrompt(kept) : '';`
    5. **省略提示（不静默）**：`const omitted = eligible.length - kept.length;` 当 `omitted > 0` 时在 `</available_skills>` **之外**追加 `\n\nNote: ${omitted} of ${eligible.length} skills omitted to stay within the prompt budget.`（追加在段外保证「未截断时的前缀与截断时逐字节相同」—— 前缀缓存友好），并产 `{ level: 'warning', code: 'realm_prompt_budget_exceeded', message: \`prompt 段预算 ${LIMITS.SKILLS_PROMPT_CHAR_BUDGET} 字符，已省略 ${omitted} 个技能（共 ${eligible.length} 个）\`, limit: LIMITS.SKILLS_PROMPT_CHAR_BUDGET, currentValue: used }`。未截断时该行**完全不出现**。
    6. `tests/test-ai-skills.js` 补 `describe('诊断与限额（SKILL-06/07）')` 的**限额部分**：
       - **字节闸**：写一个超过 `64 * 1024` 字节的 `SKILL.md` → 该技能不进集合；`_cache.diagnostics` 含 `realm_skill_md_too_large` 且 `limit === 64 * 1024`、`currentValue` 等于实际字节数（`fs.statSync(...).size`）。
       - **数量上限**：在 `skills/` 下建 `LIMITS.MAX_USER_SKILLS + 1` 个技能目录（各一个几十字节的 `SKILL.md`）→ 断言 `overLimit === true` 的条目恰有 1 条、全部条目仍在 `_cache.skills` 内（长度 = 51 + managed 数）、诊断含 `realm_user_skill_limit_exceeded` 与 `limit`/`currentValue`、且 `overLimit` 条目不出现在 `buildSkillsPrompt()` 输出中。
       - **预算截断**：把 `LIMITS.SKILLS_PROMPT_CHAR_BUDGET` 通过测试不可改（常量单源）—— 改用「制造足够多 / description 足够长的技能」使整段超过 8000 字符 → 断言 `buildSkillsPrompt()` 长度 ≤ `SKILLS_PROMPT_CHAR_BUDGET + 省略提示行长度`、含 `Note: ` 省略提示且数量与实际被省略条数一致、诊断含 `realm_prompt_budget_exceeded` 与 `limit`/`currentValue`；再断言**未截断**场景下 `Note: ` 子串完全不出现。
       - **定序确定**：同一技能集连续两次 `refreshSkills` → `getSkillsSnapshot().skills.map((e) => e.skill.name)` 深度相等；且断言 user 来源条目全部排在 managed 条目之前。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或限额四类断言（字节闸 / 数量上限 / 预算截断 / 定序确定）中任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：超 `MAX_SKILL_MD_BYTES` 的 `SKILL.md` 不进集合，且诊断带 `limit === 64 * 1024` 与等于实际文件大小的 `currentValue`
    - 行为断言：user 技能数超 `MAX_USER_SKILLS` 时 `overLimit` 条目仍在 `_cache.skills` 内、不进 prompt、诊断带 `limit` / `currentValue`；磁盘文件未被删除
    - 行为断言：prompt 段超 `SKILLS_PROMPT_CHAR_BUDGET` 时只保留预算内最大条数、段尾出现 `Note: ` 省略提示、诊断带 `limit` / `currentValue`；未超时该提示完全不出现
    - 行为断言：连续两次刷新得到相同条目顺序，且 user 来源条目全部在 managed 之前
    - 源码断言：`ai-skills-manager.js` 含 `fixedOverhead` 与 `entryCost`（差量测量），源码中不出现手写 `<available_skills>` 字符串模板
    - 源码断言：`ai-skills-manager.js` 中不存在 `localeCompare`
    - 源码断言：`LIMITS` 三个键在 `ai-skills-manager.js` 之外无字面量复制（`ai-manager.js` 与测试文件均只经 `LIMITS` 引用）
  </acceptance_criteria>
  <done>三条限额都在正确的位置生效（字节闸在解析前、数量上限在注入前、字符预算在组装前），每次超限都给出「哪个限额 / 当前值」，且定序跨机确定。</done>
</task>

<task type="auto">
  <name>Task 3: 启停状态（`settings.aiSkills.disabled`）—— 只过滤不删文件、保留数据层可见</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <reversibility rating="costly">D-09：`settings.aiSkills.disabled` 的键结构一旦落盘，改为 `{source, name}` 复合键需要迁移用户已有设置。</reversibility>
  <read_first>
    - ai-skills-manager.js 当前全文（Task 2 后的 `_cache.skills` 排序位置与 prompt 可注入集合的过滤表达式）
    - 46-CONTEXT.md D-09 全文（键名 `settings.aiSkills.disabled` = `string[]` of name；加载后过滤、**不删文件**；已知边界：同名技能共享禁用状态 —— 先禁用 managed `foo`、后导入 user `foo`，新导入的也会随之禁用；须写进产品文档）
    - 46-RESEARCH.md §Open Question 1（禁用技能**保留在 `skills[]` 内**并标 `disabled: true` 的推荐与理由：若移出，Phase 50 设置页就无法列出它来重新启用，用户禁用后技能永久消失只能手改配置文件；过滤发生在 prompt 段组装与未来 `/skill:` 解析两处）、§Pattern 3（配置注入：manager 保持无 configStore 依赖 → 纯 Node 可测）
    - 46-PATTERNS.md §S3（配置注入先例 `aiBashWhitelist`）
    - ai-manager.js:5505-5515（`this.configStore ? this.configStore.get('settings.aiBashWhitelist', []) : []` 注入式读取先例 —— `disabled` 参数在 46-01 已按同款接好）
    - 46-VALIDATION.md 46-03-04 行
  </read_first>
  <action>
    1. `ai-skills-manager.js`：在管线中**遮蔽判定之后**增加禁用标记：把 `new Set(opts.disabled)` 与每个条目的 `entry.skill.name` 比对，命中则 `entry.disabled = true`（**就地标记，不从数组移除**）。JSDoc 写明「禁用是**消费侧过滤**的下游信号：数据层保留完整条目，过滤只发生在 prompt 段组装处（本阶段）与未来的 `/skill:` 解析处（Phase 48）；文件永不改动或删除（SKILL-08）」。
    2. 顺序说明（写进 JSDoc）：先做名称重写（name ≡ 目录名，禁用比对才有确定键）→ 再做遮蔽（用户能看到来源冲突的真实形态，与禁用状态无关）→ 经 Task 2 的 `bySkillPriority` 稳定排序（可观测顺序的唯一权威）→ 最后打禁用标记（`disabled` 不参与比较器，故排序与标记互不影响）。因此「先禁用 managed `foo`、后导入 user `foo`，新导入的也会随之禁用」这条 D-09 已知边界**自然成立**（禁用键是 name，不区分来源），必须在产品文档「已知限制」如实写明（由 46-04 Task 3 落地）。
    3. `_cache.promptBlock` 的可注入集合过滤表达式（Task 2 落的）确认已含 `!e.disabled` —— 若 Task 2 用的是等价写法，本任务只做校验与注释，不重复实现。
    4. `_cache.digest` 的计算输入必须**覆盖影响 prompt 段的全部因素**：可注入条目的 `(name, source, disabled, overLimit, shadowed, disableModelInvocation)` 与截断结果。理由：启用 / 禁用、遮蔽状态变化都会改变 prompt 段，46-04 的 `syncAgentSystemPrompt()` 把 digest 当作「有没有变」的**快速判定主键**（prompt 逐字符比对作二次确认）；digest 漏掉禁用集合就会让「快速判定」失真 —— 本阶段不会因此漏写（逐字符比对仍是最终裁决），但 digest 会失去作为主键的效力，且会产生无意义的回写与广播。
    5. `tests/test-ai-skills.js` 新增 `describe('启停状态（SKILL-08）')`：
       - 建 `skills/alpha/SKILL.md` → `refreshSkills(env, { disabled: ['alpha'], rootDirs })` → 断言 `alpha` 条目 `disabled === true`、**仍在** `getSkillsSnapshot().skills` 内、**不在** `buildSkillsPrompt()` 输出中。
       - 断言磁盘未被改动：记录 `SKILL.md` 的 `fs.statSync(...).size` 与 `fs.existsSync(...)`，刷新前后一致；且目录仍在。
       - 断言 `disabled: []` 时该技能回到 prompt 输出（禁用可逆，不需重建任何东西）。
       - 断言**同名共享禁用**（D-09 已知边界）：`skills/foo/SKILL.md` 与 `managed-skills/foo/SKILL.md` 同时存在且 `disabled: ['foo']` → 两条都 `disabled === true`（user 那条即使遮蔽了 managed 也一样被禁用）。
       - 断言 digest 变化：`disabled: []` 与 `disabled: ['alpha']` 两次刷新的 `digest` 不相等（保证 46-04 的变更判定能捕捉启停）。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `启停状态（SKILL-08）` 分组内任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`disabled: ['alpha']` 时 `alpha` 条目 `disabled === true`、仍在 `_cache.skills` 内、不进 `buildSkillsPrompt()`
    - 行为断言：刷新前后磁盘文件存在且大小不变（禁用不删、不改文件）
    - 行为断言：`disabled: []` 时该技能回到 prompt 输出（启停可逆）
    - 行为断言：同名技能共享禁用状态（`skills/foo` 与 `managed-skills/foo` 同时被 `disabled: ['foo']` 命中）
    - 行为断言：不同 `disabled` 输入产生不同 `digest`
    - 源码断言：`ai-skills-manager.js` 的数据层不对 `_cache.skills` 做移除式过滤；`disabled` 只出现在消费侧过滤表达式与标记赋值中
  </acceptance_criteria>
  <done>用户可以按 name 停用某个技能：它从 system prompt 消失但文件与数据层条目完好，重新启用即恢复；启停会改变 digest（46-04 的快速判定主键，配套的 prompt 逐字符比对是最终裁决），因此启停一定能被 Agent 回写链路捕捉。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户设置 `realm-config.json` → `refreshSkills({ disabled })` | 用户可控的 name 数组；必须是纯数据、不参与路径拼接 |
| 磁盘上的 `SKILL.md` 体积 / 数量 → 主进程内存与每轮 prompt | 资源耗尽面（DoS 成本 / 延迟） |
| `_cache.diagnostics` / `_cache.errors` → 未来 Phase 50 设置页渲染 | 诊断 message 含文件路径与原始 YAML 错误文本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-03-01 | Denial of Service | 畸形 / 超大 `SKILL.md` 触发主进程 YAML 解析 | high | mitigate | `MAX_SKILL_MD_BYTES`（64 KiB）在 `createSkillsEnv.readTextFile` 按 `FileInfo.size` 前置拒绝，不读盘不解析；诊断带 `limit` + `currentValue` |
| T-46-03-02 | Denial of Service | 技能数量膨胀导致每请求 system prompt 膨胀（成本 / 延迟） | high | mitigate | `MAX_USER_SKILLS`（50）标记 `overLimit` 不注入 + `SKILLS_PROMPT_CHAR_BUDGET`（8000）整段预算贪心截断 + 段尾省略提示 + 预算诊断；两条限额独立生效 |
| T-46-03-03 | Repudiation | 加载失败被静默吞掉，用户以为技能已生效 | high | mitigate | `toRealmDiag` 显式映射 + 模块级 `errors[]` + `realm_skills_dir_missing` 补 SDK 的静默缺口（目录不存在时 SDK 零诊断）；`refreshSkills` 成功时复位 `errors` 但失败时**保留**旧快照并记 error |
| T-46-03-04 | Tampering | 禁用实现成「删除文件」或「从数据层移除」 | medium | mitigate | `disabled` 只做标记 + 消费侧过滤；测试断言文件大小/存在性刷新前后一致、条目仍在 `_cache.skills` 内 |
| T-46-03-05 | Tampering | 诊断 message 原样渲染进设置页造成注入 | low | accept | 本阶段无 UI（Phase 50 才渲染）；届时按 `realm://` 页面约定用 `textContent` 写入。诊断内容为本地文件路径与 SDK 自产文本，无远端拼接 |
| T-46-03-SC | Tampering | npm / 依赖安装 | high | mitigate | 本计划零新增依赖；不执行任何安装命令（`package.json` 原样），无包合法性 gate 触发 |
</threat_model>

<flagged_assumptions>
以下条目来自 `SPECLESS_FALLBACK` 的 edge-probe 文本派生报告（`status: unresolved` / `category: unclassified`），按 §A 不得自动 resolve，显式登记：

- **SKILL-07**（unclassified）：「三限额数值（`MAX_SKILL_MD_BYTES = 64 * 1024` / `MAX_USER_SKILLS = 50` / `SKILLS_PROMPT_CHAR_BUDGET = 8000`）是否匹配真实用量」属 `.planning/STATE.md` 的开放决策 O7。本阶段只保证三个常量**单源生效**、超限**可操作提示**、以及预算口径（整段，含 SDK 固定开销）自洽；**未做真实用量实测校准**。若上线后数据显示 8000 过紧/过松，调整点只有 `LIMITS` 一处。
- **SKILL-08**（unclassified）：「同名技能共享禁用状态」是 D-09 已锁定的已知边界（禁用键是 name、不区分来源）。本阶段按锁定语义实现并断言，并把它写进产品文档「已知限制」；**不实现**按来源区分的复合禁用键（那需要迁移用户已有设置 —— 见 D-09 的 costly 标注）。
- **SKILL-06**（`empty` 探针 → 本轮 **resolve 为 truths**，见本计划 `must_haves.truths` 第 2 条）、**SKILL-06**（`encoding` 探针 → 本轮 **resolve 为 truths**，见第 1、3 条）。
- 完整的 11 行结清表见 `46-01-PLAN.md` 的 `<edge_coverage_ledger>`（等式 11 = 4 truths + 7 flagged assumptions）。
</flagged_assumptions>

<verification>
1. `node tests/test-ai-skills.js` 退出 0（累计约 40 例）
2. `node tests/test-agent-workspace.js` 退出 0（回归）
3. `node tests/test-ai-bash-policy.js` 退出 0（回归 —— 确认本计划对 `ai-manager.js` 的改动未波及 bash 策略接线）
4. `node --check ai-skills-manager.js && node --check ai-manager.js` 通过
5. 源码 gate：`ai-skills-manager.js` 中不存在 `localeCompare`；五个新增 Realm 诊断码均带 `realm_` 前缀；`LIMITS` 三键在其它文件零字面量
</verification>

<success_criteria>
- [ ] SDK 诊断经 `type` → `level` 显式映射后透传，零诊断时两个容器均为 `[]`
- [ ] 单技能失败不抛错、其余技能照常注入；批量失败保留上一次成功快照并记 error
- [ ] 三条限额各自在正确位置生效，每次超限给出「哪个限额 / 当前值」
- [ ] prompt 段定序确定（无 `localeCompare`），跨机不漂移
- [ ] 启用 / 禁用只过滤不删文件，且可逆、被 digest 捕捉
- [ ] 既有 `tests/test-agent-workspace.js` / `tests/test-ai-bash-policy.js` 回归全绿
</success_criteria>

<output>
Create `.planning/phases/46-prompt/46-03-SUMMARY.md` when done
</output>
