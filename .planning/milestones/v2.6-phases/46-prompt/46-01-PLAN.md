---
phase: 46-prompt
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-workspace.js
  - ai-skills-manager.js
  - ai-manager.js
  - tests/test-ai-skills.js
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "应用启动后（`main.js:4040` → `agentWorkspace.ensureWorkspaceDir()`）`agent-workspace/skills/` 与 `agent-workspace/managed-skills/` 均存在，且 `resolveInside(<workspace root>, <两目录内任意文件路径>)` 返回非 null"
    - "AI 的 `read` 工具可成功读取两目录内任意 `SKILL.md`：`createSandboxEnv().readTextFile(<skill md 绝对路径>)` 返回 `{ ok: true }`（消除「模型看到 location 却读不到文件」的静默失效）"
    - "技能集非空时 `buildSystemPrompt()` 含 `<available_skills>` 与 `<name>` / `<description>` / `<location>` 三类标签，且 `<location>` 指向的字符串等于该技能在沙箱内的绝对路径（`Skill.filePath`）"
    - "技能集为空时 `buildSystemPrompt()` 完全不出现 `available_skills`，且相较前三段基线不多出任何空行（D-02：空态整段不追加，不产生空标签）"
    - "`refreshSkills()` 完成后 `buildSkillsPrompt()` / `getSkillsSnapshot()` 在同一同步帧内读到新技能集（零 IO、非 Promise）；`getSkillsSnapshot()` 返回的是浅拷贝视图，就地改其数组不污染模块级权威"
    - "两个扫描根下只认目录：根层放入 `SKILL.md` 时 `skills/<name>/SKILL.md` 仍被加载（不再整组被顶替）；根层带 `description` 的散落 `*.md` 不再产生 name 等于扫描根目录名（`skills` / `managed-skills`）的幽灵技能"
    - "空/单项输入语义（edge probe SKILL-02 `empty`）：传入空技能集时 SDK `formatSkillsForSystemPrompt([])` 返回空串、整段不追加；恰有单项时正常注入单个 `<skill>` 条目且 `<available_skills>` 只出现一次"
    - "长度与相等口径（edge probe SKILL-02 `encoding`）：prompt 段字符预算按 JS `string.length`（UTF-16 code unit，与 Phase 43 `ai-memory-manager.js:19` 的 `BUDGETS` 同口径）计数；`SKILL.md` 正文上限按 `FileInfo.size` 的字节数计数 —— 两条限额单位不同、互不换算"
  artifacts:
    - "agent-workspace.js：`getSkillsDir()` 返回 `path.join(getWorkspaceDir(), 'skills')`、`getManagedSkillsDir()` 返回 `path.join(getWorkspaceDir(), 'managed-skills')`（紧跟 `getAttachmentsDir()` 之后）；`ensureWorkspaceDir()` 追加 2 行幂等 `mkdirSync`；`module.exports` 追加这 2 个访问器"
    - "ai-skills-manager.js（新增）：模块级 `_cache`、`LIMITS`、`createSkillsEnv()`、`refreshSkills()`、`buildSkillsPrompt()`、`getSkillsSnapshot()`、`_resetCacheForTest()`"
    - "ai-manager.js：`getAiSkillsManagerLazy()`、`buildSystemPrompt()` 第 4 段、`init()` 内 Agent 创建前的 `await refreshSkills(...)`、`module.exports.buildSystemPrompt`"
    - "tests/test-ai-skills.js（新增）：`withTempRoot` 脚手架 + 端到端纵切断言 + 反黑屏/反幽灵回归守卫 + 依赖纪律源码扫描"
  key_links:
    - "`main.js:4040 ensureWorkspaceDir()` → 两目录就位 → `ai-manager.js:808 createSandboxEnv()` → `refreshSkills(sandboxEnv, {disabled, rootDirs})` → `_cache.promptBlock` → `buildSystemPrompt()` 第 4 段 → `new Agent({ initialState: { systemPrompt } })`（ai-manager.js:821）"
    - "prompt 段内的 `<location>` 绝对路径 → 模型调 `read` → `createSandboxEnv().readTextFile` → `resolveInside(root, p)` 放行（这就是技能目录必须落在硬沙箱 root 内的根因）"
    - "`createSkillsEnv.listDir` 的收窄只作用于两个扫描根本身；`skills/<name>/references|scripts|assets` 的枚举不受影响（技能自带资源目录仍可被 `read`/`bash` 访问）"
  prohibitions:
    - statement: "不得为 `managed-skills/` 引入第二个沙箱 root、只读挂载，或改动 `resolveInside()` / `createSandboxEnv()` 的任何逻辑（会引入第二套路径判据，与双基准 + realpath 复核漂移）"
      status: unresolved
      verification: judgment
      reason: "本阶段沙箱零改动是硬约束（探针实测沙箱 env 直接可作 loadSkills 的 env）；fallback 召回项，无 wired check，按 flagged-unverified 处理"
    - statement: "不得把 SKILL.md 正文（body）注入 system prompt —— prompt 段只含 name / description / location 三项元数据"
      status: unresolved
      verification: judgment
      reason: "渐进式披露是本里程碑的设计根因；注入正文会让 token 成本随技能数线性增长并触发 REQUIREMENTS.md 的 Out of Scope 条目"
    - statement: "不得自拼 `<available_skills>` XML 字符串，也不得在 renderer 侧重建该段（必须经 `formatSkillsForSystemPrompt`，含其 `escapeXml` 转义）"
      status: unresolved
      verification: judgment
      reason: "自拼等于复制 SDK 的转义与缩进模板，升级即漂移，并引入标签破坏面"
    - statement: "`buildSkillsPrompt()` / `getSkillsSnapshot()` / `buildSystemPrompt()` 不得异步化，也不得触发任何文件 IO（不得在 buildSystemPrompt 内 await 或每轮重新加载技能）"
      status: unresolved
      verification: judgment
      reason: "G-42-4 事故：Agent 创建路径上同步帧内 `this.agent` 恒 null；且 systemPrompt 每轮变会导致 provider 前缀缓存全 miss"
    - statement: "`ai-skills-manager.js` / `ai-manager.js` 不得直接引入 YAML 解析库或 ignore 库，也不得从 SDK 的 harness 子路径导入"
      status: unresolved
      verification: judgment
      reason: "P11/O5：二者只是 SDK 的传递依赖、未列入 package.json dependencies，直接引入后换 pnpm 立即 MODULE_NOT_FOUND；SDK exports map 无 harness 子路径入口"
---

<objective>
立起 Phase 46 的端到端纵切（tracer）：把「双目录进沙箱 → 沙箱 env 加载技能 → 模块级缓存 → system prompt 第 4 段 → 模型可 `read` 正文」这条完整链路一次打通并验证，再把加载面收窄到契约布局 `<dir>/<name>/SKILL.md`，并补齐 SKILL-01/02/03 的 Nyquist 断言。

Purpose: 这条链路是 v2.6 整个里程碑的地基 —— 技能目录不在硬沙箱 root 内，模型看得到 `<location>` 却永远 `read` 不到，是无声失效；`buildSystemPrompt()` 一旦被异步化则重演 G-42-4。先在最薄的一条路径上把架构证明掉，后续三个计划才是「在这条已验证的链路上加语义层」，而不是在未验证的假设上叠代码。

Output: `agent-workspace.js` 两个目录访问器 + 2 行 mkdir；新建 `ai-skills-manager.js` 骨架（缓存 / 加载 / 薄 env / 同步访问器）；`ai-manager.js` 第 4 段与 init 接线；新建 `tests/test-ai-skills.js` 脚手架与端到端断言。

**tracer 说明**：Task 1 是本阶段的唯一 `type="tracer"` 任务 —— 它横跨四个文件、四个层级（路径层 / 加载层 / 缓存层 / prompt 层），并自带一条真实的端到端 `<verify>`。Task 2、3 是从这条已验证切面向外扩展的语义层与断言层。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-prompt/46-CONTEXT.md
@.planning/phases/46-prompt/46-RESEARCH.md
@.planning/phases/46-prompt/46-PATTERNS.md
@.planning/phases/46-prompt/46-VALIDATION.md
@.planning/phases/46-prompt/COVERAGE.md

@agent-workspace.js
@ai-manager.js
@ai-memory-manager.js
@tests/test-agent-workspace.js
</context>

<artifacts_this_phase_produces>
本阶段（46）新建的符号与命名，供 plan-review-convergence 的 source-grounding 排除新增符号误报：

**新文件**
- `ai-skills-manager.js`
- `tests/test-ai-skills.js`
- `docs/product/ai-skills.md`（在 46-04 产出）
- `.planning/phases/46-prompt/COVERAGE.md`（planner 产出）

**`ai-skills-manager.js` 导出（本计划建立全部）**
- 模块级常量 `LIMITS`，键 `MAX_SKILL_MD_BYTES` / `MAX_USER_SKILLS` / `SKILLS_PROMPT_CHAR_BUDGET`
- 模块级状态 `_cache`，字段 `skills` / `promptBlock` / `digest` / `diagnostics` / `errors` / `refreshedAt`
- `createSkillsEnv(sandboxEnv, opts, droppedNotices)`
- `refreshSkills(env, { disabled, rootDirs })`（async）
- `buildSkillsPrompt()`（sync）
- `getSkillsSnapshot()`（sync）
- `_resetCacheForTest()`
- 缓存条目形状：`{ skill: Skill, source: 'user'|'managed' }` + 后续计划追加的 `diagnostics[]` / `shadowed` / `shadowedBy` / `disabled` / `overLimit`

**`agent-workspace.js` 新增导出**
- `getSkillsDir()`、`getManagedSkillsDir()`

**`ai-manager.js` 新增符号**
- `getAiSkillsManagerLazy()`
- `module.exports.buildSystemPrompt`
- （46-04）`syncAgentSystemPrompt()`、实例字段 `_skillsPromptDirty`

**内部约定字符串（本阶段首次出现）**
- 缓存条目 `source` 取值 `'user'` / `'managed'`
- 诊断 `code` 的 Realm 前缀族（在 46-02/46-03 落地）：`realm_name_rewritten` / `realm_shadowed` / `realm_layout_violation` / `realm_prompt_budget_exceeded` / `realm_root_entry_skipped`

**刻意不产出（decided omission，不是遗漏 —— plan-review-convergence 的 source-grounding 请据此排除）**
- `resolveSkill(name)` —— `46-PATTERNS.md` §1h 的导出面清单里列了它（供 Phase 48 的 `/skill:` 解析）。本阶段**不实现、不导出**：`/skill:name` 的解析与注入在 CONTEXT.md 的 scope boundary 与 briefing 的 scope fence 中都明确归 **Phase 48**。Phase 48 的解析器只需在 `getSkillsSnapshot()` 之上加一层 `filter((e) => !e.disabled && !e.shadowed && e.skill.name === name)` —— 所需的过滤位（`disabled` / `shadowed`）由 46-02 与 46-03 在本阶段备齐。**不把注入面提前到 46。**
- `toRealmDiag` / `inContractLayout` / `enforceDirNameAuthority` / `applyShadowing` / `bySkillPriority` / `createSkillsEnv` —— 全部保持**模块内部函数**（不导出）。理由：它们是被 `refreshSkills` 编排的实现细节，导出会给后续阶段留下「绕过权威管线自己拼一遍」的诱因（正是 Anti-Pattern 5 的形态）。测试一律通过公开面（`refreshSkills` / `buildSkillsPrompt` / `getSkillsSnapshot`）断言行为，不做单元级白盒依赖。
- `formatSkillInvocation` 的集成 —— 见 `COVERAGE.md` 的 `OPT-OUT` 行（Phase 48 的 DISC-02）。

</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端最小技能纵切（目录 → 沙箱加载 → 缓存 → prompt 第 4 段 → 可 read）</name>
  <files>agent-workspace.js, ai-skills-manager.js, ai-manager.js, tests/test-ai-skills.js</files>
  <reversibility rating="costly">D-01：技能段固定为第 4 段（末段），前三段全静态 —— 段顺序决定冻结记忆段的前缀缓存边界；改动需同步 buildSystemPrompt() 契约与两处 Agent 创建点，并让既有一切会话的 prompt 前缀缓存重建。</reversibility>
  <read_first>
    - agent-workspace.js:84-100（`getAttachmentsDir` 访问器模板 + `ensureWorkspaceDir` 当前 3 行 mkdir —— 本任务的扩展点）、:30-40（`getWorkspaceDir` 惰性 electron require）、:352-363（模块导出面）、:1-12（文件头落盘数据清单，需补两目录）
    - agent-workspace.js:136-166（`resolveInside`）与 :179-350（`createSandboxEnv`）——**只读，一行不改**（锁定见 prohibitions）；:183-186 是「SDK 从包根动态 import」的既有先例
    - ai-manager.js:94-108（`getAiMemoryManagerLazy` / `getAgentWorkspaceLazy` 惰性 require 三段式 JSDoc —— 新 helper 的逐字模板）、:536-564（`buildWorkspacePrompt` + `buildSystemPrompt`，含 G-42-4 同步契约注释）、:800-835（`init()`：:808 sandboxEnv、:811 tools、:821 `new Agent(`）、:5648-5651（追加式 module.exports 先例）
    - ai-memory-manager.js:1-25 与 :356-403（常量单源 `BUDGETS` + 模块级快照 + 同步只读访问器 —— `_cache` / `buildSkillsPrompt` 的形态先例，含 G-42-4 事故记录原文）
    - tests/test-agent-workspace.js:1-42（文件头 + `withTempRoot` 脚手架 + describe/assert 风格 —— 新测试文件逐字照抄的对象）、:88 附近（`await workspace.createSandboxEnv({ cwd: root })` 的真实沙箱用法）
    - 46-RESEARCH.md §SDK Loader & Prompt API Contract A/B（精确签名与 `Skill` 五字段表，**路径字段是 `filePath` 不是 `location`**）、D（递归语义 + 根层 `SKILL.md` 短路）、F（沙箱 env 零适配）、§Code Examples（两处 Agent 创建点的精确插入位置）
    - 46-PATTERNS.md §1a/1b/1e/1h（常量单源 / 缓存形态 / 包根动态 import / 导出面）、§3a-3b/3e
    - 46-CONTEXT.md D-01/D-02/D-04/D-11 与 <code_context>（「硬沙箱是唯一 root」条）
  </read_first>
  <action>
    1. `agent-workspace.js`：在 `getAttachmentsDir()`（:89-91）之后新增两个访问器，JSDoc 照抄同款两行式：`getSkillsDir()` 返回 `path.join(getWorkspaceDir(), 'skills')`（注明 source='user'，优先级最高）；`getManagedSkillsDir()` 返回 `path.join(getWorkspaceDir(), 'managed-skills')`（注明 source='managed'，被同名 user 技能遮蔽、文件不删）。两者都必须由 `getWorkspaceDir()` 派生 —— 这是「技能目录落在硬沙箱 root 内」的唯一保证。
    2. `agent-workspace.js`：`ensureWorkspaceDir()`（:96-100）在既有三行之后追加 `fs.mkdirSync(getSkillsDir(), { recursive: true });` 与 `fs.mkdirSync(getManagedSkillsDir(), { recursive: true });`。`recursive: true` 幂等，对既有 `.tmp/` / `attachments/` / `ai-memory/` 零影响。头部 JSDoc（:93-95）的目录清单由 3 项改为 5 项；文件头（:1-12）的落盘数据清单补两目录。
    3. `agent-workspace.js`：`module.exports`（:352-363）追加 `getSkillsDir,` 与 `getManagedSkillsDir,`（位置紧跟 `getAttachmentsDir,`）。
    4. 新建 `ai-skills-manager.js`，放仓库根（与 `ai-manager.js` / `ai-memory-manager.js` / `ai-bash-policy.js` 平铺）。模块头 JSDoc 必须写明：「SDK 契约按 `@earendil-works/pi-agent-core@0.84.3` 逐一核对（源码直读 + 本机探针）；**升版后必须复核 `loadSkills` 的递归语义、根层 `includeRootFiles` 硬编码、诊断字段名**」。顶层**不得** `require('electron')`、**不得**顶层 `import` SDK。
    5. 同文件：`const LIMITS = { MAX_SKILL_MD_BYTES: 64 * 1024, MAX_USER_SKILLS: 50, SKILLS_PROMPT_CHAR_BUDGET: 8000 };` —— D-11 的全项目唯一来源，端点与前端零字面量。JSDoc 注明「`MAX_SKILL_MD_BYTES` 按字节（`FileInfo.size`）、`SKILLS_PROMPT_CHAR_BUDGET` 按 `string.length`，两条独立限额」。
    6. 同文件：`let _cache = { skills: [], promptBlock: '', digest: '', diagnostics: [], errors: [], refreshedAt: 0 };`（模块级唯一权威）。
    7. 同文件实现 `async function refreshSkills(env, { disabled = [], rootDirs = [] } = {})`：
       - `const { loadSourcedSkills, formatSkillsForSystemPrompt } = await import('@earendil-works/pi-agent-core');` —— **必须从包根取**（SDK `exports` map 只有 `.` / `./node` / `./session/testing` / `./package.json` 四个入口，harness 子路径不存在）。
       - 调 `loadSourcedSkills(env, [{ path: rootDirs[0], source: 'managed' }, { path: rootDirs[1], source: 'user' }])` —— 顺序不是装饰：managed 先、user 后到，遮蔽判定据此（D-06）。
       - 返回元素的真实形状是**包装对象** `{ skill, source }`（勘误 3：`source` 不在 `Skill` 内），原样存进 `_cache.skills`（保留 `source`）；`_cache.diagnostics` 先存 SDK 原样诊断（形状 `{ type, code, message, path, source }` —— **严重度字段是 `type` 不是 `level`**，勘误 2；本计划的映射在 46-03 完善，此处不得把 `level` 当成 SDK 字段名去读）。
       - `_cache.promptBlock` = `formatSkillsForSystemPrompt(_cache.skills.filter((e) => e.skill.disableModelInvocation !== true).map((e) => e.skill))` —— 空数组时 SDK 返回空串。
       - 计算并写 `_cache.digest`（技能集稳定序列化后的短摘要）、`_cache.refreshedAt = Date.now()`。JSDoc 必须写清 digest 的**唯一消费方与消费口径**：46-04 的 `syncAgentSystemPrompt()` 把它当作「技能集是否变化」的**快速判定主键**（digest 相同**且** `buildSystemPrompt()` 与 `agent.state.systemPrompt` 逐字符相同，才跳过回写与广播）；因此 digest 的输入必须覆盖影响 prompt 段的全部因素（46-03 Task 3 item 4 补齐 `disabled` / 遮蔽 / 截断结果）。本计划只需生成。
       - 返回 `_cache`。
    8. 同文件同步访问器（**零 IO、非 Promise**，G-42-4 同步契约）：
       - `function buildSkillsPrompt() { return _cache.promptBlock; }`
       - `function getSkillsSnapshot() { return { ..._cache, skills: _cache.skills.slice(), diagnostics: _cache.diagnostics.slice(), errors: _cache.errors.slice() }; }` —— 浅拷贝视图，防 Phase 48/50 消费者就地改数组污染权威。
       - `function _resetCacheForTest()` 把 `_cache` 复位为初始形状（照 `ai-attachments-manager.js` 的 `_resetRegistryForTest` 命名先例）。
    9. 同文件 `module.exports = { LIMITS, refreshSkills, buildSkillsPrompt, getSkillsSnapshot, _resetCacheForTest };`
    10. `ai-manager.js`：在 `getAgentWorkspaceLazy()`（:106）之后新增 `function getAiSkillsManagerLazy() { return require('./ai-skills-manager'); }`，JSDoc 照抄 `:98-105` 的三段式（说明「manager 无 electron 依赖，纯 Node 可测」）。
    11. `ai-manager.js`：`buildSystemPrompt()`（:561-564）改为第 4 段**条件追加**：先算 `const base = REALM_SYSTEM_PROMPT + '\n\n' + buildWorkspacePrompt() + '\n\n' + getAiMemoryManagerLazy().buildGlobalSnapshot();`，再 `const skillsBlock = getAiSkillsManagerLazy().buildSkillsPrompt();`，`return skillsBlock ? base + '\n\n' + skillsBlock : base;`。空态**整段不追加**（不产生空标签、不留多余空行）。JSDoc 更新为：「技能段固定在末段（D-01）；前三段全静态，故技能变更不影响冻结记忆段的前缀缓存边界；**三处**都必须经此函数（init / _recreateAgent / 未来调用点），漏一处即部分会话无技能段」。技能段文本**原样**使用 SDK 返回值（D-02），不加中文前缀、不自写中文段。
    12. `ai-manager.js`：`init()` 在 `:808`（`this.sandboxEnv = await getAgentWorkspaceLazy().createSandboxEnv();`）之后、`:821`（`this.agent = new Agent(`）之前插入无条件的
        `await getAiSkillsManagerLazy().refreshSkills(this.sandboxEnv, { disabled: this.configStore ? this.configStore.get('settings.aiSkills.disabled', []) : [], rootDirs: [getAgentWorkspaceLazy().getManagedSkillsDir(), getAgentWorkspaceLazy().getSkillsDir()] });`
        （D-04；`configStore` 注入式读取照 `:5511-5513` 的 `settings.aiBashWhitelist` 先例，manager 侧零 configStore 依赖）。`:811` 的 `_buildRealmTools()` 与 `new Agent(` 块本身零改动。
    13. `ai-manager.js`：`module.exports`（:5648-5651）追加 `module.exports.buildSystemPrompt = buildSystemPrompt;`（VALIDATION.md 标注的 Wave 0 前置；实测当前 `typeof require('./ai-manager').buildSystemPrompt === 'undefined'`，不补则「技能段确实进 prompt」无法断言）。
    14. 新建 `tests/test-ai-skills.js`（`node:test` + `node:assert`，纯 Node，零框架依赖）：
       - 文件头照抄 `tests/test-agent-workspace.js:1-19`（含 `用法: node tests/test-ai-skills.js` 一行）；`require('../agent-workspace')` / `require('../ai-memory-manager')` / `require('../ai-skills-manager')` / `require('../ai-manager')`。
       - `withTempRoot(t)` 照抄 `tests/test-agent-workspace.js:21-32`：`mkdtempSync(path.join(os.tmpdir(), 'realm-skills-test-'))`；`t.after` 内清理临时目录 + `workspace.setWorkspaceDir(null)` + `aiMemoryManager.setBaseDir(null)` + **`aiSkills._resetCacheForTest()`**（模块级 `_cache` 跨用例污染会让后续「空技能集」断言假失败）。
       - 写**一条端到端纵切断言**（本任务的 tracer 验收）：建 `<root>/skills/alpha/SKILL.md`（frontmatter 含 `name: alpha` 与 `description: 端到端纵切测试技能`）→ `await aiSkills.refreshSkills(await workspace.createSandboxEnv(), { rootDirs: [workspace.getManagedSkillsDir(), workspace.getSkillsDir()] })` → 断言 `aiManager.buildSystemPrompt()` 含 `available_skills` 与 `<location>` 两类子串 → 取 `aiSkills.getSkillsSnapshot().skills` 中该条目的 `skill.filePath`，断言 `(await sandboxEnv.readTextFile(filePath)).ok === true`。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总行出现 `# fail` 后跟非 0 计数（如 `# fail 1`）；或输出中完全不出现 `# pass` 行（说明测试文件未被真正执行）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`node tests/test-ai-skills.js` 退出 0，端到端纵切断言通过 —— 断言链路为「建 `skills/alpha/SKILL.md` → `refreshSkills` → `buildSystemPrompt()` 含 `<available_skills>` 与 `<location>` → 该 `filePath` 经真实沙箱 `readTextFile` 返回 `ok: true`」
    - 源码断言：`agent-workspace.js` 含 `function getSkillsDir(` 与 `function getManagedSkillsDir(`，二者返回 `path.join(getWorkspaceDir(), 'skills' / 'managed-skills')`
    - 源码断言：`agent-workspace.js` 的 `ensureWorkspaceDir()` 体内含 4 条以上 `fs.mkdirSync(`，且 `resolveInside` / `createSandboxEnv` 两函数体与该文件在 46 计划开始前逐字节相同（`git diff agent-workspace.js` 只落在访问器 / mkdir / 导出三处）
    - 源码断言：`ai-skills-manager.js` 含 `MAX_SKILL_MD_BYTES: 64 * 1024`、`MAX_USER_SKILLS: 50`、`SKILLS_PROMPT_CHAR_BUDGET: 8000` 三个键；`module.exports` 含 `LIMITS` / `refreshSkills` / `buildSkillsPrompt` / `getSkillsSnapshot`
    - 源码断言：`ai-manager.js` 的 `buildSystemPrompt()` 含条件追加（同时出现 `skillsBlock` 与 `? base +` 分支），且 `module.exports.buildSystemPrompt` 已导出
    - 行为断言：`typeof require('./ai-skills-manager').buildSkillsPrompt() === 'string'`（同步返回字符串而非 Promise）
  </acceptance_criteria>
  <done>一条最薄的技能链路端到端跑通并通过自动化断言：应用启动后双目录存在且在沙箱内可读；放了技能的目录会出现在 system prompt 的 `<available_skills>` 里，其中的 `<location>` 能被沙箱 `read` 打开；空技能集时该段完全不出现。</done>
</task>

<task type="auto">
  <name>Task 2: `createSkillsEnv` 加载面收窄（根层只认目录 + SKILL.md 字节预筛 + 禁静默）</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - agent-workspace.js:207-349（`createSandboxEnv` 的**显式转发纪律** —— 17 个 FileSystem 方法逐项列出、不用 Proxy；`FileError` 三参形态见 :192-196）
    - ai-skills-manager.js 当前全文（Task 1 刚落下的 `refreshSkills` / `_cache` / 导出面）
    - 46-RESEARCH.md §Pattern 1（薄 env 包装的完整形状与三条正确性说明）、§Pattern 2（分层叠加）、§Pitfall 1（根层 `SKILL.md` 静默顶替整组，两态实测 `['alpha']` → `['rooty']`）、§Pitfall 2（根层散落 `*.md` 变幽灵技能，`name` 取扫描根目录名）、§Pitfall 5（超大 `SKILL.md` 被优先解析）、§G（`.ignore` 变通已实测证伪，不要尝试）
    - 46-PATTERNS.md §1f（薄 env 形状与「显式展开不用 Proxy」约束 + 三条拦截范围约束）
    - tests/test-agent-workspace.js:34-83（describe 分组 + 单行断言 + 失败说明消息风格）
  </read_first>
  <action>
    1. `ai-skills-manager.js` 新增 `function createSkillsEnv(sandboxEnv, opts, droppedNotices)`，返回 `{ ...sandboxEnv, async listDir(p, abortSignal) {...}, async readTextFile(p, abortSignal) {...} }`：
       - 用 `...sandboxEnv` 展开（`createSandboxEnv()` 返回对象字面量、非 class 实例，展开安全），**不要用 Proxy**、不要选择性只转发「关键方法」—— 漏包一个方法就是静默落到非沙箱实现，与 `agent-workspace.js:207-349` 逐项列出的纪律同款。
       - `listDir`：`const res = await sandboxEnv.listDir(p, abortSignal);` 若 `res.ok === false` 原样返回（不吞错）；若 `path.resolve(p)` 命中 `opts.rootDirs` 中任意一项（即**两个扫描根本身**），把 `res.value` 收窄为 `filter((e) => e.kind === 'directory')`，并把被滤掉的每个 entry `push` 进 `droppedNotices`（`{ path: e.path, kind: e.kind }`）；不在根上的路径（`skills/<name>/` 内的 `references/` `scripts/` `assets/`）原样返回。
       - `readTextFile`：仅当 `path.basename(p) === 'SKILL.md'` 时先 `const info = await sandboxEnv.fileInfo(p, abortSignal);`，若 `info.ok && info.value.size > opts.maxSkillMdBytes` 则**不读盘、不解析**，直接返回 `err(new FileError('invalid', \`SKILL.md 超过 ${opts.maxSkillMdBytes} 字节上限（当前 ${info.value.size} 字节）\`, String(p)))`；其余路径（含 `.ignore` / `.gitignore`）原样透传给 `sandboxEnv.readTextFile`。`FileError` / `err` 从**包根**动态 import。文案必须同时含「限额值」与「当前值」（SKILL-07 的可操作提示语义）。
    2. `refreshSkills` 改为经 `createSkillsEnv` 构造 env：每轮刷新建一个包装对象（零持久状态）、把 `sandboxEnv` 与 `{ rootDirs, maxSkillMdBytes: LIMITS.MAX_SKILL_MD_BYTES }` 传入，并把 `droppedNotices` 收集后转成 Realm 自建诊断追加进 `_cache.diagnostics`：`code: 'realm_root_entry_skipped'`、`level: 'warning'`、message 含被滤掉的 entry 名 + 「技能必须放在 `<dir>/<name>/SKILL.md`，根层散落文件不会被加载」。**禁止静默**（SKILL-06）——被滤掉必须有诊断。
    3. `tests/test-ai-skills.js` 增两条回归守卫断言（放在新的 `describe('加载面收窄（回归守卫）')` 分组）：
       - **反黑屏**：同一临时 root 先只放 `skills/alpha/SKILL.md` 加载并断言 `alpha` 在集合内；再写入 `<root>/skills/SKILL.md`（frontmatter `name: rooty`）后重新 `refreshSkills`，断言 `alpha` **仍在**集合内。
       - **反幽灵**：写入 `<root>/skills/README.md`（带 `description` frontmatter），断言集合中**不存在** `name === 'skills'` 的条目。
       - 两条断言各自同时断言 `_cache.diagnostics` 里出现了 `realm_root_entry_skipped`（证明是「被滤掉并告知」而不是「碰巧没加载」）。
       - 幂等性：所有用例的临时目录经 `withTempRoot` 建立、`t.after` 清理，不触碰真实 userData。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或反黑屏 / 反幽灵两条守卫中任一条 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：`ai-skills-manager.js` 含 `function createSkillsEnv(`，其返回对象用 `...sandboxEnv` 展开且覆写 `listDir` / `readTextFile` 两个方法，源码中不出现 `new Proxy(` 与 `Proxy(`
    - 行为断言：根层 `skills/SKILL.md` 存在时，`skills/alpha/SKILL.md` 仍被加载（`_cache.skills` 含 `alpha`）
    - 行为断言：根层 `skills/README.md`（带 `description`）不产生 `name === 'skills'` 的技能
    - 行为断言：`createSkillsEnv` 的 `readTextFile` 对超过 `LIMITS.MAX_SKILL_MD_BYTES` 的 `SKILL.md` 返回 `ok === false`，且其 `error.message` 同时含限额数值与当前字节数
    - 行为断言：非目录 entry 被滤掉时 `_cache.diagnostics` 出现 `code === 'realm_root_entry_skipped'` 的条目（无静默丢弃）
    - 行为断言：`skills/<name>/` 内部的子目录枚举不受收窄影响（根层收窄只在 `opts.rootDirs` 命中时生效）
  </acceptance_criteria>
  <done>把「主动防护发生在 SDK 遍历之前」落到代码：根层放错一个文件不再让整个技能集消失，根层散落的 md 不再变成幽灵技能，超大 SKILL.md 在 YAML 解析前就被拒，且每一次过滤都有可读诊断。</done>
</task>

<task type="auto">
  <name>Task 3: SKILL-01/02/03 断言补齐（沙箱归属 + prompt 契约 + 同步零 IO + 依赖纪律）</name>
  <files>tests/test-ai-skills.js</files>
  <read_first>
    - 46-VALIDATION.md §Per-Task Verification Map 中 **Plan 01 / Wave 1** 的六行（`46-01-01` / `46-01-02` / `46-01-03` / `46-01-04` / `46-01-05` / `46-01-08`）的精确断言文本 —— **本任务的断言面就是这六行**。同表其余行不在本任务的落地范围内，不得写进本任务的 verify（会因依赖后续 wave 而转红）：`46-01-06` / `46-01-07` 由 Task 2 落地；`46-02-01` / `46-02-02` 属 46-02 Task 1（深嵌套过滤）与 Task 2（名称权威重写）；`46-04-01` / `46-04-02` 属 46-04 Task 1 / Task 2（后者依赖 46-04 才接线的第二处 `new Agent(`）
    - 46-RESEARCH.md §Validation Architecture（脚手架、纯 Node 可测性前提、Wave 0 Gaps）、§SDK 契约 A（`formatSkillsForSystemPrompt([])` 返回空串的实测结论）
    - ai-skills-manager.js 当前全文（Task 1/2 后的最终形状）与 ai-manager.js:551-564（`buildSystemPrompt` 第 4 段）
    - agent-workspace.js:96-100（`ensureWorkspaceDir` 5 行）与 :352-363（导出面）
    - tests/test-agent-workspace.js:34-83、:118（`assert.strictEqual(..., true, '源文件未被移动')` 的「断言 + 失败说明」风格）
  </read_first>
  <action>
    在 `tests/test-ai-skills.js` 按 46-VALIDATION.md 的分组补齐断言（沿用已建脚手架，不改既有用例）：
    1. `describe('技能目录与沙箱可达（SKILL-01）')`：`workspace.ensureWorkspaceDir()` 后断言 `fs.existsSync(workspace.getSkillsDir())` 与 `fs.existsSync(workspace.getManagedSkillsDir())` 均为 `true`；断言 `workspace.resolveInside(root, path.join(workspace.getSkillsDir(), 'alpha', 'SKILL.md')) !== null`；断言 `(await workspace.createSandboxEnv()).readTextFile(<skill md 绝对路径>)` 的 `ok === true`。
    2. `describe('prompt 段注入（SKILL-02）')`：**空态**（只建两个空目录、`_resetCacheForTest()` 后 `refreshSkills`）断言 `aiManager.buildSystemPrompt()` 不包含 `available_skills` 子串，并断言其长度等于「不含技能段的三段基线长度」（即空态不多出空行）；**有技能态**断言同时包含 `<available_skills>`、`<name>`、`<description>`、`<location>` 四类子串，且 `available_skills` 子串出现次数为 1。
    3. `describe('模块级缓存 + 同步访问器（SKILL-03）')`：`refreshSkills` 之后 `buildSkillsPrompt()` 同步返回非空字符串（`typeof === 'string'`，非 thenable）；断言 `getSkillsSnapshot()` 返回的 `skills` 数组与再次调用返回的数组**不是同一引用**（浅拷贝契约：`!==` 且 `deepStrictEqual`）；断言连续两次 `refreshSkills` 同一目录得到相同 `digest`（确定性——保证后续 46-04 的「无变化不动 prompt」成立）。
    4. `describe('依赖纪律（源码扫描）')`：用 `fs.readFileSync(path.join(__dirname, '..', <file>), 'utf8')` + 正则扫描 `ai-skills-manager.js` 与 `ai-manager.js`，断言二者都不含对 YAML 解析库 / ignore 库的直接 require（正则 `/require\(\s*['"](yaml|ignore)['"]\s*\)/`），也不含 SDK 的 harness 子路径导入（正则 `/pi-agent-core\/harness\//`）；用 `assert.ok(cond, '<失败说明>')` 风格。**执行纪律：不要把上述两个库名或该子路径字面量写进 `ai-skills-manager.js` / `ai-manager.js` 的任何注释**——源码扫描会命中注释而非真实回归。
    5. 全量回归：同一次运行后再跑 `node tests/test-agent-workspace.js`（既有 21 例），确认新增的 2 行 `mkdirSync` 未破坏任何既有断言。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或四类分组中任一 assert 抛出（输出含 `AssertionError`）</fails_when>
    <automated>node tests/test-agent-workspace.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数（既有 21 例回归变红）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`node tests/test-ai-skills.js` 与 `node tests/test-agent-workspace.js` 双双退出 0
    - 行为断言：空技能集时 `buildSystemPrompt()` 不含 `available_skills` 且长度等于三段基线（无多余空行）
    - 行为断言：有技能时 `<available_skills>` / `<name>` / `<description>` / `<location>` 四类子串齐备且 `available_skills` 只出现一次
    - 行为断言：`buildSkillsPrompt()` 返回字符串（非 Promise）；两次 `getSkillsSnapshot()` 的 `skills` 数组非同一引用
    - 源码扫描断言：`ai-skills-manager.js` / `ai-manager.js` 不含对 YAML 解析库或 ignore 库的直接 require，也不含 SDK harness 子路径导入
  </acceptance_criteria>
  <done>SKILL-01 / SKILL-02 / SKILL-03 三条需求各有 ≥2 条可执行的自动化断言锁住，且既有 21 例 agent-workspace 测试保持全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统（用户手放 / 未来导入的技能目录）→ SDK 加载器 | 不可信输入：目录布局、SKILL.md 体积与 frontmatter 形态 |
| `agent-workspace/skills/` `managed-skills/` → 硬沙箱 `resolveInside` | 技能路径必须落在唯一 root 内；越界即静默失效或逃逸 |
| `_cache.promptBlock` → 每次 LLM 请求的 system prompt | 技能 description 无条件进入每一轮请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01-01 | Tampering | `createSkillsEnv.listDir` 收窄逻辑 | medium | mitigate | 收窄只在 `opts.rootDirs` 命中时生效（`path.resolve` 精确比对），技能自带 `references/` `scripts/` `assets/` 不受影响；被滤 entry 一律进 `droppedNotices` → `realm_root_entry_skipped` 诊断，禁静默 |
| T-46-01-02 | Elevation | 技能目录路径来源 | high | mitigate | `getSkillsDir()` / `getManagedSkillsDir()` 只能由 `getWorkspaceDir()` 派生，保证两目录都在唯一硬沙箱 root 内；**不为 `managed-skills/` 加第二个 root 或只读挂载**；`resolveInside` / `createSandboxEnv` 本阶段零 diff（`git diff agent-workspace.js` 只落在访问器/mkdir/导出） |
| T-46-01-03 | Denial of Service | 超大 / 畸形 `SKILL.md` 触发主进程同步 YAML 解析 | high | mitigate | `createSkillsEnv.readTextFile` 在 SDK 解析**之前**按 `FileInfo.size` 拒绝超 `LIMITS.MAX_SKILL_MD_BYTES`（64 KiB）的 `SKILL.md`，返回 `FileError('invalid')` 不读盘；SDK 记为 `read_failed` 诊断并跳过该技能 |
| T-46-01-04 | Tampering | 模型的间接提示注入面 = 技能 description | medium | accept | 本阶段**无写入路径**（导入属 Phase 51），无法在源头扫描 description；可控防线是「让用户看到技能集全貌」（`<available_skills>` + 诊断透传）。诚实边界必须写进 `docs/product/ai-skills.md`「安全边界」节（46-04 Task 3）：description 会进入每次请求的 system prompt，不要导入来源不明的技能 |
| T-46-01-05 | Information Disclosure | `<location>` 暴露工作区绝对路径进 prompt | low | accept | 路径即设计（模型需要它去 `read`）；prompt 只发给用户自己配置的 LLM provider，与既有 `buildWorkspacePrompt()` 暴露工作区根路径同款 |
| T-46-01-SC | Tampering | npm / 依赖安装 | high | mitigate | 本阶段**零新增依赖**（`package.json` dependencies / devDependencies / `build.asarUnpack` 全部原样），不执行任何安装命令 → 无包合法性 gate 触发；`node_modules/` 下的 SDK 为只读契约，绝不写入 `files_modified` |
</threat_model>

<edge_coverage_ledger>
`SPECLESS_FALLBACK=true` / `EDGE_ABSENT=1`：本阶段无 SPEC，edge-probe 报告为**纯文本派生**（安装版 `edge-probe.cjs` 的 `SHAPE_CUES` 全空 → 11 行全部 `category: unclassified` / `status: unresolved`）。按 §A/§C 的无静默丢弃等式 **11 = 4（写入 must_haves.truths）+ 7（显式 flagged assumption）**逐行结清：

| # | requirement_id | probe category | 处置 | 落点 |
|---|----------------|----------------|------|------|
| 1 | SKILL-02 | `empty` | **resolve → truths** | 46-01 `must_haves.truths` 第 7 条（空数组 → `''` → 整段不追加；恰一项 → 单条正常注入） |
| 2 | SKILL-02 | `encoding` | **resolve → truths** | 46-01 `must_haves.truths` 第 8 条（字符预算按 `string.length`；正文上限按字节） |
| 3 | SKILL-06 | `empty` | **resolve → truths** | 46-03 `must_haves.truths`（无诊断时为 `[]` 而非 `undefined`，消费者可安全取长度） |
| 4 | SKILL-06 | `encoding` | **resolve → truths** | 46-03 `must_haves.truths`（SDK `type` → Realm `level` 显式映射表；`code` 为 ASCII 枚举比对） |
| 5 | SKILL-01 | unclassified | **flagged assumption** | 「目录创建只在临时 root 注入路径下断言；未覆盖打包后 `app.isPackaged` 形态下的 userData 路径」 |
| 6 | SKILL-03 | unclassified | **flagged assumption** | 「『两处 Agent 创建点』由源码扫描断言锁定；48-51 若新增第三个创建点，本阶段断言管不到（但扫描会转红作为正向防护）」 |
| 7 | SKILL-04 | unclassified | **flagged assumption** | 「广播端到端到窗口的验证缺消费方（48/50 才实现），本阶段只断言广播被以 `'skills:changed'` 调用」 |
| 8 | SKILL-05 | unclassified | **flagged assumption** | 「遮蔽语义只覆盖 user/managed 两层 tier（locked 设计）；第三来源不在本里程碑」 |
| 9 | SKILL-07 | unclassified | **flagged assumption** | 「三限额数值（64 KiB / 50 / 8000）是否匹配真实用量属 STATE.md 的 O7，本阶段只保证常量单源生效，未做用量实测校准」 |
| 10 | SKILL-08 | unclassified | **flagged assumption** | 「同名技能共享禁用状态（D-09 已锁定的已知边界），本阶段不实现按来源区分的禁用键」 |
| 11 | DOC-01 | unclassified | **flagged assumption** | 「六节骨架粒度由 plan 期决定（CONTEXT.md Claude's Discretion），本阶段取六节 + 占位」 |

> 工具缺口如实记录（来自 orchestrator）：本机安装的 `edge-probe.cjs` 的 `SHAPE_CUES` 映射为空对象，导致其散文启发式把全部需求判为 `unclassified`、无法产出类别化探针。因此上表是文本派生结果，**不是**本阶段的覆盖上限 —— 真正的验证底座是 `46-VALIDATION.md` 的 19 行具体断言。
>
> SKILL-01 / SKILL-07 / SKILL-08 三条虽有 `unclassified` 行，仍被 `46-VALIDATION.md` 的具体断言覆盖（46-01-01/02、46-03-02/03、46-03-04 —— 其中 46-03-02/03 的最终形态在 Plan 03 落地），上表把它们标为假设是遵循 §A「unclassified 一律不得自动 resolve」的纪律，而非表示无验证。
</edge_coverage_ledger>

<verification>
1. `node tests/test-ai-skills.js` 退出 0（本计划三任务的全部自动化断言）
2. `node tests/test-agent-workspace.js` 退出 0（既有 21 例回归不红 —— 验证新增 2 行 mkdir 的幂等性）
3. 源码 gate：`agent-workspace.js` 的 `resolveInside` / `createSandboxEnv` 零 diff；`ai-manager.js` 的两个 `new Agent(` 中**已有一处**（:821）前置了 `refreshSkills`（第二处在 46-04 补）
4. `node --check ai-skills-manager.js && node --check ai-manager.js && node --check agent-workspace.js` 全过
5. `node -e "const p=require('./package.json'); if(p.dependencies['yaml']||p.dependencies['ignore']) process.exit(1)"` 退出 0（零新增依赖）
</verification>

<success_criteria>
- [ ] 双目录在 `ensureWorkspaceDir()` 后自动存在，且在 `resolveInside` 放行范围内，沙箱 `readTextFile` 可读取其中 `SKILL.md`
- [ ] `buildSystemPrompt()` 在技能集非空时含 `<available_skills>`，空态整段不追加
- [ ] `refreshSkills()` 完成后同步访问器立即可读，且不触发 IO
- [ ] 根层 `SKILL.md` 不再顶替整组；根层散落 md 不再变幽灵技能；超限 `SKILL.md` 在解析前被拒
- [ ] 每次「加载面收窄」都有对应诊断，无静默丢弃
- [ ] 既有 `tests/test-agent-workspace.js` 21 例保持全绿
</success_criteria>

<output>
Create `.planning/phases/46-prompt/46-01-SUMMARY.md` when done
</output>
