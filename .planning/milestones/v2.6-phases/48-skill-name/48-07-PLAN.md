---
phase: 48-skill-name
plan: 7
type: execute
wave: 6
depends_on: [48-06]
gap_closure: true
gap_ids: [G-48-12]
files_modified:
  - ai-manager.js
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/WINDOWS.md
  - .planning/phases/48-skill-name/48-VALIDATION.md
  - .planning/phases/48-skill-name/48-UAT.md
autonomous: true
requirements: [DISC-02, DISC-06]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

key-decisions:
  - "口径 = 修复（用户 2026-09-12 裁决），且**在调用侧闭环**：`_resolveSkillInvocation`（ai-manager.js:1421）在 `readSkillForInvocation` 返回 `not_found` 时，**至多**重试一次 —— 先经既有权威入口 `syncAgentSystemPrompt()` 重扫，再当场重新读盘。**不改 `ai-skills-manager.js`**：`readSkillForInvocation` 仍是唯一的显式调用读盘口，其失败码域仍只有 `not_found | disabled`（D-13 推论）"
  - "shadowed 的取值来源 = `_cache.skills` 的 `shadowed` 字段，由 `refreshSkills` 的 ③ `applyShadowing`（46 D-06：managed 先 / user 后、后到者胜出）在同一次重扫里产出；重试读盘时 `readSkillForInvocation` 仍跳过 `shadowed === true` 的条目 → 手打一律作用于胜出者（D-11）。本计划**不写第二套遮蔽判定**（不做「按目录直读 + 自行比较根优先级」）"
  - "disabled 的取值来源 = 注入式清单 `configStore.get('settings.aiSkills.disabled', [])`，由 `syncAgentSystemPrompt()` → `refreshSkills(env, { disabled, rootDirs: [managed, user] })` 的 ⑤ 步骤打成条目级 `disabled` 标记（46 D-09 / D-10：禁用是消费侧过滤，不删文件）；重试若命中禁用清单，`readSkillForInvocation` 在读盘之前返回 `disabled` → `skillError.code === 'skill_disabled'`、**零注入**"
  - "tier 的取值来源 = `sourceTierOf(entry, seededNames)`，输入是重扫产出的 `entry.source`（'user' | 'managed'）与 `getSeededSkillNamesSafe()` 的 seeded 集合（D-14 三档：user / builtin / managed）。`_resolveSkillInvocation` 已有的 `sourceTierOf` 调用点与 seeded 注入方式**逐字不变**，新路径只是让 `result.source` 在 miss 场景下也真的有值"
  - "「为什么不是让 `readSkillForInvocation` 自己按目录回退探测」（G-48-12 missing 第 1 条的另一个分支）：单目录直读会绕过刷新管线的其余判定 —— 契约布局过滤（深嵌套应跳过）、description 不可用跳过、SKILL.md 超 64 KiB 字节闸、`overLimit` / `promptOmitted` 标记（D-12：显式调用是它们的唯一可用路径，不能反过来变成「绕过」）。经 `refreshSkills` 重扫则上述全部判定与三字段同源产出，零重复实现"
  - "重扫的调用形态：复用 `syncAgentSystemPrompt()`（48-06 的 `refreshSkillsForPanel` 同款「只加调用方、函数体逐字未改」）。**代价与语义**：`prompt()` / `promptWithContext()` 在进入 `_resolveSkillInvocation` 之前已置 `this.isProcessing = true`（ai-manager.js:1041 / :1171），故重扫必走 `syncAgentSystemPrompt()` 的忙分支 —— 重扫落地、`_skillsPromptDirty = true`、**不改写 `agent.state.systemPrompt`、不广播**（D-03：忙时改写对本轮无效且让状态机难推理）；prompt 回写与 `skills:changed` 广播在下一次非忙同步点（`refreshSkillsForPanel` / Agent 重建 / `promptWithContext` 的 idle 边界）落地"
  - "有界性：**至多一次**，不得写成循环。真不存在的技能仍是 `skill_not_found`（两个码不变），重复 miss 不会累积重扫；误诊一个技能名的成本 = 一次全量重扫（与打开 `/` 面板同款），该成本写进 docs §10.7 的诚实边界"
  - "**不新增第三套失败文案**、**不改 renderer**：失败仍由重试后的 `result.reason` 经 `skillErrorFromReason`（ai-manager.js:6089）映射，renderer 既有回滚链路（48-06 的跨文件护栏）不动；renderer 侧 `.refreshSkills(` 计数仍恒为 1（P-48-06 自激回路护栏）"
must_haves:
  truths:
    - "G-48-12 靶心：在 `agent-workspace/managed-skills/` 或 `agent-workspace/skills/` 下**运行期**新建技能目录（不打开 `/` 面板、不重启、不重建 Agent、不触发任何其它重扫），直接手打 `/skill:<新名> [args]`：请求到达主进程并由它**当场读盘**后**正常调用** —— 不再出现 system-note「未找到技能「<新名>」，输入 / 查看可用技能」、不再有「主进程无 `发送消息: /skill:…` 日志」这一现象。修复前（UAT test 12 自动驱动实测，2026-09-12）：14ms 内出 note、`state.aiSkills` 全程 `containsNew:false`、主进程零请求；`ai.refreshSkills()` 后原样重发即成功 —— 判别性证明失效点在主进程缓存门"
    - "缓存未命中的重扫走**唯一权威入口**：`syncAgentSystemPrompt()`（其函数体逐字未改，46-04 五条断言与 `:1299`/`:1366` 形态断言继续绿），因而 shadowed（D-11 / 46 D-06）、disabled（D-10 / 46 D-09）、tier（D-14）三字段全部由同一条 `refreshSkills` 管线产出，**新路径无从绕过任何一个**。行为断言：运行期两根同名 → 注入的是 user 版正文、managed 条目在快照里 `shadowed === true`；运行期新增且名入禁用清单 → `skill_disabled` + `skillBlock === ''`"
    - "有界且不改判失败语义：真不存在 → 仍 `skill_not_found`（返回值域仍两个码）；单次调用最多一次重扫；重扫抛错被就地 catch + `console.warn` 后**保留原判定**，不把「未找到」变成异常（也不动 `isProcessing` 的既有复位职责 —— WR-02 的处置仍归 Phase 49 开工前，不在本计划范围）"
    - "已缓存技能的快路径逐字不变：缓存命中时**零重扫**（实测 `rescanDelta === 0`）且改盘后的正文仍当场读到（既有 D 组「调用瞬间读盘」用例不变）；运行期删目录（缓存命中但读盘为空）仍 `not_found`，且只多一次有界重扫"
    - "运行期新增的技能不止「能调用」，在既有失效链里也无新增触发源即可对齐：重扫改变了 digest → 下一次非忙同步点的 `syncAgentSystemPrompt()` 走「改写 + 广播」分支 → renderer 收到 `skills:changed` **只重拉快照**（`.refreshSkills(` 计数恒 1）→ 不构成「广播 → 刷新 → 再广播」回路（P-48-06 不回归）"
    - "测试面盲区消除：`tests/test-ai-skills.js` 新增一组用例覆盖「运行期新增技能目录 → 直接 `/skill:` 调用」形态（正例 2 条 + 快路径不重扫 1 条 + 负例 3 条：disabled / shadowed / 真不存在，+ 源码护栏 1 条），且用**真实** SDK 夹具（真 `createSandboxEnv` + 真 `formatSkillInvocation` + 真 `refreshSkills`）而非自证；`# fail` 为 0 且总例数 ≥ 修复前 132 + 新增 7"
    - "文档与账本收口（AGENTS.md 维护约定 + G-48-12 missing 第 3 条）：`docs/product/ai-skills.md` 的 §10.3（口径）/ §10.7（诚实边界，含成本与忙时语义）/ §七（例数与覆盖面）落地；`AGENTS.md` 技能域测试清单补登记 `node tests/test-ai-skills.js`；`.planning/WINDOWS.md` 的 unrun-verify id 24 在**表行与 JSON 条目两处**同步更正机制描述、**status 保持 `open`**；`48-UAT.md` 以**纯追加**方式更正 G-48-3 括注的错误前提（不手改 Gaps 的 YAML 块）"
    - "诚实边界：本条 truth 是**运行时**行为（重扫 → 缓存 → 读盘 → IPC 往返），node:test 只能钉住调用侧契约与三字段来源；最终证据仍是重跑 `/gsd-verify-work 48` 的自动驱动探针（在 `managed-skills/` 下新建目录后**不打开** `/` 面板直接手打 `/skill:<新名>`），须在 SUMMARY 登记。另：WR-06（已缓存技能在实时读盘路径绕过 64 KiB 字节闸）**保持开放**，本计划不修也不得声称已修"
  artifacts:
    - "`ai-manager.js`：`_resolveSkillInvocation`（:1421-1440）在首次 `readSkillForInvocation` 之后、失败分支之前插入 miss 一次性重试块（`result` 由 `const` 改 `let`；条件 `result && result.ok !== true && result.reason === 'not_found'`；块内 `try { await this.syncAgentSystemPrompt(); } catch (err) { console.warn(...) }` + 重读；失败分支 `if (!result || result.ok !== true)` 的原逻辑、`skillErrorFromReason` 调用与返回形状**逐字不动**）；方法 JSDoc 补一节「缓存未命中的一次性重扫」并逐条写清 shadowed / disabled / tier 来源与禁止循环"
    - "`tests/test-ai-skills.js`：文件末尾追加 `J 组 · G-48-12 运行期新增技能（miss → 权威重扫一次 → 当场读盘）` describe（7 条 `test()`），含夹具 helper `skillResolveCtx(env, { disabled })`（`Object.create(aiManager.prototype)` + 只补 `sandboxEnv` / `isProcessing = true` / `_skillsPromptDirty` / `_skillsPromptDigest` / `agent.state` / `configStore` / `getSeededSkillNamesSafe: () => []`，并用 own-property 包装 `syncAgentSystemPrompt` 计数）"
    - "`docs/product/ai-skills.md`：§10.3 追加「缓存未命中 → 一次性权威重扫」口径条目；§10.7 追加成本 / 忙时语义 / 残余窗口三条诚实边界（并显式声明 WR-06 仍开放）；§七 的 `node tests/test-ai-skills.js` 条目把例数刷成 Task 1 的实测值并在覆盖面里补「运行期新增技能的调用瞬间重扫 + 当场读盘」"
    - "`AGENTS.md`：技能域 `- 测试：` 清单行新增 `node tests/test-ai-skills.js`（技能域全量单测 + 实测例数）—— 该文件此前只在 `docs/product/ai-skills.md` §七 登记、指令文件清单缺项；其余条目与维护约定文字不动"
    - "`.planning/WINDOWS.md`：id 24 的**表行**与 **JSON 条目** 的 `description` 同步更正为「48-07 修复 + 机制更正（原描述的『主进程重扫 → 广播』暗示存在 idle 边界自动重扫链，实际不存在）」；`status` 仍 `open`、frontmatter 的 `open_count` / `total_count` / `fixed_count` 与 `resolved_at` 一律不动（unrun-verify 的收口点是重跑探针，不是本计划）"
    - "`.planning/phases/48-skill-name/48-VALIDATION.md`：Per-Task Verification Map 追加一行 `48-07-T1`（10 列格式与既有行一致，`Status` 列写 `⬜ pending`、`File Exists` 写 `✅（新增）`）"
    - "`.planning/phases/48-skill-name/48-UAT.md`：文件末尾追加 `## 更正（G-48-12 立项时，2026-09-12）` 小节（纯追加，正文外的 markdown 段落），逐条更正 G-48-3 的 root_cause 括注错误前提 + 记录 G-48-12 的修复口径与「status 由 verify-work 重跑后回填」的交接；**Gaps 的 YAML 块一行不动**"
  key_links:
    - "miss 重试链（本计划新增的唯一链路）：`_resolveSkillInvocation` 的 `not_found` → `this.syncAgentSystemPrompt()`（ai-manager.js:2759）→ `refreshSkills(this.sandboxEnv, { disabled: configStore.get('settings.aiSkills.disabled', []), rootDirs: [getManagedSkillsDir(), getSkillsDir()] })`（:2763-2770）→ 缓存三字段就位 → 第二次 `readSkillForInvocation` 当场读盘 → `formatSkillInvocation` 组装 → `{skillBlock, skill:{name, tier, content}}`。**这条链上没有任何一处自行判定 shadowed / disabled / tier**"
    - "忙时语义链（为什么不会中途改 prompt）：`ai-manager.js:1041` / `:1171` 先置 `this.isProcessing = true`，`:1046` / `:1174` 才调 `_resolveSkillInvocation` → 重扫必落 `syncAgentSystemPrompt()` 的忙分支（`:2771-2773`：置脏后 return）→ prompt 回写与 `windowManager.broadcast('skills:changed')`（:2786，全仓唯一广播点）延后到下一次非忙同步点 → 广播 → `renderer.js:4405-4407` 的处理器只调 `pullAiSkillsSnapshot()`（零 IO 读投影）→ 无自激"
    - "失败通道不变：重试后的失败仍由 `result.reason` 经 `skillErrorFromReason`（:6089，两个码 + 两条文案的唯一来源）→ `{skillError}` → IPC 返回 → renderer 既有回滚（`removeSkillFailureBubbles` + `pushSystemNote(result.skillError.message)`）—— 48-06 建立的跨文件单源护栏不受影响"
  prohibitions:
    - statement: "不得改动 `syncAgentSystemPrompt()` 的函数体（含把忙分支的 `this._skillsPromptDirty = true` 换成直接改写 prompt、或在方法体里加 miss 判定）"
      status: unresolved
      verification: judgment
      reason: "`tests/test-ai-skills.js:2173` 起有 46-04 五条方法体断言，`:1299` 有早退→改写→广播形态断言，`:1366` 有两处创建点调用文本逐字一致断言；48-06 也只做了「只加调用方」。忙分支是 D-03 的刻意设计（忙时改写对当前轮无效），调用路径恒处于 `isProcessing = true`，绕过它会得到「本轮 prompt 变了但本轮不生效」的假象"
    - statement: "不得在 `ai-skills-manager.js` 里为 miss 增设第二套磁盘探测 / 自行比较两根优先级的遮蔽判定（例如按目录名直接拼 `rootDirs[i]/<name>/SKILL.md` 读盘并自行判 shadowed）"
      status: unresolved
      verification: judgment
      reason: "`_cache` 是 shadowed / disabled / tier / overLimit / promptOmitted 的唯一权威；单目录直读会绕过契约布局过滤、description 可用性过滤与 64 KiB 字节闸，把「被整条跳过的技能」变成可调用（直接违反 D-13 推论与 D-12 的既有边界）。本计划的全部判定都经 `refreshSkills` 产出"
    - statement: "不得把 miss 重试写成循环（`while` / `for` / 递归 / 反复 await 直到成功），也不得在重试失败后继续尝试"
      status: unresolved
      verification: judgment
      reason: "重试的靶心只有「运行期新增目录」一种情形，一次重扫即可覆盖（B2 判别性补测：一次 `refreshSkills()` 即从必败变为成功）；循环会把误诊技能名的成本放大成主进程无界 IO，且会让「真不存在」的返回时机不可预测"
    - statement: "不得在 renderer 侧新增任何重扫调用点（`src/renderer.js` 的 `.refreshSkills(` 全文件计数恒为 1，仍在 `openSlashPicker` 内），也不得把 miss 判定/重试搬到 renderer"
      status: unresolved
      verification: judgment
      reason: "P-48-06 明确「广播 → 刷新 → 再广播」是禁止回路；`tests/test-ai-skills.js:2377` 起与 `tests/test-skill-picker-model.js` 的 G 组都断言该计数。判定权威在主进程（48-03 / 48-06 已收口），miss 重试必须在主进程调用侧"
    - statement: "不得动 TD-48-01 / TD-48-02 的登记与实现，不得顺手修 WR-01 / WR-02 / WR-03 / WR-04 / WR-05 / WR-06，也不得在任何文档里声称 WR-06 已修"
      status: unresolved
      verification: judgment
      reason: "两条 TD 是用户 2026-09-12 明确延后（Phase 49 开工前第一条）；WR-06 用户裁决「保持开放、随 Phase 49 一并处置」。本计划的唯一目标是关闭 G-48-12；把无关 finding 卷进来会让 gap 收敛轮的判据面失控（也越过本计划的 required 需求 DISC-02 / DISC-06）"
---

<objective>
关闭 UAT gap **G-48-12**：`readSkillForInvocation`（`ai-skills-manager.js:802-804`）在**触盘之前**先用 `_cache.skills` 做存在性门 —— 缓存未命中即 `not_found`，读盘路径根本不执行。而 `_cache` 只由 3 个显式 `refreshSkills()` 调用点刷新（Agent 创建 / `syncAgentSystemPrompt` / `_recreateAgent`），其常规入口只有「打开 `/` 面板」（`ai:refresh-skills`）；全仓无 fs watcher、无定时器、无写工具钩子。于是「运行期新增技能目录（含 AI 经 `write`/`bash` 创建）+ 从未打开过 `/` 面板 + 直接手打 `/skill:<新名>`」走进死路，与用户 2026-09-11 的硬约束「显式调用必须在调用那一刻实时读盘」直接冲突。48-06 已修好 renderer 半边（移除本地否决），主进程半边仍未闭合。

Purpose: 用户 2026-09-12 明确裁决「**修复**」（不是收口文档、不是记技术债）。修复口径见 frontmatter `key-decisions`：在**调用侧**（`_resolveSkillInvocation`）对 `not_found` 做**至多一次**的权威重扫 + 重试，重扫走既有唯一入口 `syncAgentSystemPrompt()`，从而让 shadowed（D-11 / 46 D-06）、disabled（D-10 / 46 D-09）、tier（D-14）三个字段全部来自同一条 `refreshSkills` 管线 —— 既不新增第二套判定，也不让运行期新增的技能绕过遮蔽与禁用。失败码域仍只有 `not_found | disabled`（D-13 推论），`ai-skills-manager.js` 与 `syncAgentSystemPrompt()` 的函数体逐字不改。
Output: `ai-manager.js` 的 miss 一次性重试 + `tests/test-ai-skills.js` 的 J 组 7 条用例（正例 2 + 快路径 1 + 负例 3 + 源码护栏 1）+ 文档与账本收口（`docs/product/ai-skills.md` §10.3 / §10.7 / §七、`AGENTS.md` 测试清单、`.planning/WINDOWS.md` id 24、`48-VALIDATION.md` 矩阵行、`48-UAT.md` 纯追加更正）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-UAT.md
@.planning/phases/48-skill-name/48-REVIEW.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-06-PLAN.md
@.planning/phases/48-skill-name/48-06-SUMMARY.md
@.planning/phases/48-skill-name/48-01-PLAN.md
@.planning/phases/48-skill-name/48-01-SUMMARY.md
@.planning/WINDOWS.md
@docs/product/ai-skills.md
@AGENTS.md
@ai-manager.js
@ai-skills-manager.js
@tests/test-ai-skills.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: miss 一次性权威重扫 + 重试（调用侧闭环）+ J 组测试面补齐</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js:1385-1445`（`_resolveSkillInvocation` 全段：JSDoc 四条口径 + 方法体 `const parsed` → `const result = await skillsManager.readSkillForInvocation(...)`（:1426）→ 失败分支（:1427-1430）→ `formatSkillInvocation` / `buildSkillInvocationBlock` / `sourceTierOf` / 返回三键契约）
    - `ai-manager.js:1029-1050` 与 `:1163-1180`（两处调用点：**先** 置 `this.isProcessing = true`（:1041 / :1171），**后** 调 `_resolveSkillInvocation`（:1046 / :1174）—— 重扫必落忙分支的前提）
    - `ai-manager.js:2740-2790`（`syncAgentSystemPrompt()` 全段：重扫调用（:2763-2770，含 `disabled` 注入表达式与 `rootDirs` 顺序 managed → user）→ 忙分支（:2771-2773）→ digest + 逐字符早退 → 改写 + `broadcast('skills:changed')`（:2786））；`:2795-2810`（`refreshSkillsForPanel` 的「只加调用方」先例）
    - `ai-manager.js:6089-6100`（`skillErrorFromReason`：两个码 + 两条文案的唯一来源）
    - `ai-skills-manager.js:769-834`（`readSkillForInvocation` 全段：缓存存在性门 :803-804、`disabled` 早判 :805、目录路径全等的同一性判据 :820-830、`name` 重写 :833）与 `:446-470`（`refreshSkills(env, { disabled, rootDirs })` 签名）、`:556-566`（③ 遮蔽 / ⑤ disabled 标记的落点）
    - `tests/test-ai-skills.js:34-95`（`withTempRoot` / `writeSkill` / `readSource` / `functionBody` / `methodBody` 五个 helper 的口径 —— 新夹具必须复用它们，不要另造一套）
    - `tests/test-ai-skills.js:1433-1465`（D 组夹具：`sdkFormatSkillInvocation` / `writeRawSkill` / `setupSkillsEnv`）、`:1458-1548`（D 组既有用例：改盘不重扫仍读新正文、SKILL.md 被删 → not_found、目录被换 → not_found、disabled 在读盘前判定、同名遮蔽作用于胜出者 —— 新用例必须与它们**并存且不冲突**）
    - `tests/test-ai-skills.js:1776-1804`（`skillErrorFromReason` 打表：两个码 + 文案 + 值域无第三码 —— 新负例直接断言这两个码）
    - `tests/test-ai-skills.js:2606-2742`（文件末尾 `I 组 · 重载链路技能标记重建` 的位置 —— 新 `J 组` 追加在其后）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `G-48-12`（observed B/B2 + root_cause + missing 四条）与 `### 12.` 整段
  </read_first>
  <action>
    1. `ai-manager.js` 的 `_resolveSkillInvocation`（:1421 起）：把 `const result = await skillsManager.readSkillForInvocation(this.sandboxEnv, parsed.name);` 改成 `let result = ...`，并在它与紧随其后的失败分支之间插入 miss 一次性重试块，语义与形状如下（逐条照做，不要加循环、不要抽新方法）：
       - 条件：`result && result.ok !== true && result.reason === 'not_found'` —— 只在「缓存/读盘判定为不存在」时重试；`disabled` 不重试（缓存已知道它，重扫无意义）。
       - 块内：`try { await this.syncAgentSystemPrompt(); } catch (err) { console.warn('[Realm AI] 技能缓存未命中后的重扫失败（保留原判定）:', err.message); }`，随后 `result = await skillsManager.readSkillForInvocation(this.sandboxEnv, parsed.name);`（重读一次）。
       - 其后 `if (!result || result.ok !== true) { const reason = ...; return { skillBlock: '', skill: null, skillError: skillErrorFromReason(reason, parsed.name) }; }` **逐字不动**（重试后的失败与首次失败走同一条出口）。
       - 注释必须写明四件事：① 这是 G-48-12（UAT test 12 实测：14ms 出 note、主进程无 `发送消息` 日志、`ai.refreshSkills()` 后原样重发即成功）；② 根因是 `readSkillForInvocation` 的存在性门取自 `_cache`，而 `_cache` 只由显式重扫刷新、无 fs watcher / 定时器 / 写工具钩子；③ 重扫经 `syncAgentSystemPrompt()`（唯一加载入口 `refreshSkills` 的调用方、函数体逐字未改），因此 shadowed / disabled / tier 三字段来自同一条管线，**不新增第二套判定**；④ **至多一次、不得改成循环**。
       - 方法 JSDoc 补一小段（接在既有「四条口径」之后）：标题形如「缓存未命中的一次性重扫（G-48-12）」，内容涵盖 ①②③④ 与「忙时只置脏、回写延后」的说明，并指明 shadowed→`applyShadowing`（46 D-06）/ disabled→注入式 `settings.aiSkills.disabled`（46 D-09、D-10）/ tier→`sourceTierOf` + seeded 集合（D-14）。
       - **不得**改动本方法体内其余任何一行（`parsed` 早退、`formatSkillInvocation` 动态 import、`buildSkillInvocationBlock`、`sourceTierOf` 调用、返回三键形状）。
    2. `tests/test-ai-skills.js` 文件末尾（`I 组` describe 之后）追加 `describe('J 组 · G-48-12 运行期新增技能（miss → 权威重扫一次 → 当场读盘）', () => { ... })`，先写夹具 helper，再写 7 条 `test()`：
       - `function skillResolveCtx(env, { disabled = [] } = {})`：`const ctx = Object.create(aiManager.prototype);` 然后只补这些 own property —— `sandboxEnv: env`；`isProcessing: true`（**与真实调用点同值**：`ai-manager.js:1041` / `:1171` 先置位，这同时是「忙时不得改写 prompt」断言的前提，注释写明）；`_skillsPromptDirty = false`；`_skillsPromptDigest = ''`；`agent = { state: { systemPrompt: 'OLD', isStreaming: false } }`；`configStore = { get: (k, d) => (k === 'settings.aiSkills.disabled' ? disabled : d) }`；`getSeededSkillNamesSafe = () => []`（避免纯 Node 下 `builtin-skills-seeder` 的 electron 依赖告警，同时让 tier 判定确定）；`rescanCalls = 0` 并用 own-property 包装计数：`const real = aiManager.prototype.syncAgentSystemPrompt; ctx.syncAgentSystemPrompt = function () { this.rescanCalls += 1; return real.call(this); };`。helper 的 JSDoc 写明「只补读盘/重扫所需状态，不 new AIManager()（不触碰真实 userData）」。
       - 用例 1（**正例 · managed 根**）：`withTempRoot(t)` → `workspace.ensureWorkspaceDir()` → `const env = await setupSkillsEnv(root)`（此刻技能集为空）→ `writeSkill(workspace.getManagedSkillsDir(), 'probe-new', { body: '# probe-new\n\n新技能正文\n' })`（**不重扫**，模拟 AI 经 write/bash 建目录）→ `const res = await aiManager.prototype._resolveSkillInvocation.call(ctx, '/skill:probe-new 你好')`。断言：`res.skillError === undefined`；`res.skill.name === 'probe-new'`；`res.skill.tier === 'managed'`；`res.skill.content` 含 `新技能正文`；`res.skillBlock` 含 `新技能正文`；**`ctx.rescanCalls === 1`**（有界，恰一次）；`ctx._skillsPromptDirty === true` 且 `ctx.agent.state.systemPrompt === 'OLD'`（忙分支：只置脏、不改写）。
       - 用例 2（**正例 · user 根 + 读盘实时性**）：在 `workspace.getSkillsDir()` 下运行期新增 `probe-user` → 同样不重扫直接调用。断言 `res.skillError === undefined`、`res.skill.tier === 'user'`、`ctx.rescanCalls === 1`；随后把该 SKILL.md 正文改成第二版、**再调用一次**，断言读到第二版（说明重试后仍走实时读盘，而不是复用重扫产出的缓存正文）。
       - 用例 3（**快路径零重扫**，保护既有 D 组语义）：先用 `setupSkillsEnv` + 运行期新增 `probe-hit` 成功调用一次（预热缓存与 `ctx.rescanCalls` 基线），记下基线；随后只改盘（不新增目录）、再调用一次，断言 `res.skillBlock` 含新正文且 **`ctx.rescanCalls` 相对基线增量为 0**（缓存命中 → 不触发重扫）。
       - 用例 4（**负例 · disabled 不被绕过**）：`skillResolveCtx(env, { disabled: ['probe-dis'] })` → 运行期新增 `probe-dis`（`managed-skills/` 下）→ 调用 `/skill:probe-dis`。断言 `res.skillError.code === 'skill_disabled'`、`res.skillBlock === ''`、`res.skill === null`（零注入），且 `ctx.rescanCalls === 1`。
       - 用例 5（**负例 · shadowed 不被绕过 / 胜出者语义不变**）：同一个 `ctx` 下运行期在 `managed-skills/probe-dup` 与 `skills/probe-dup` **两处**同时新增同名技能（一个都不在缓存里）→ 调用 `/skill:probe-dup`。断言：`res.skillError === undefined`；`res.skill.tier === 'user'`；`res.skillBlock` 含 user 版正文、**不含** managed 版正文；`aiSkills.getSkillsSnapshot().skills` 里 `probe-dup` 的两条中 `{ source: 'managed', shadowed: true }` 与 `{ source: 'user', shadowed: false }` 同时存在（D-11 / 46 D-06）。
       - 用例 6（**负例 · 真不存在 + 有界**）：用**全新** ctx（基线归零）调用 `/skill:no-such-skill-xyz`。断言 `res.skillError.code === 'skill_not_found'`、`res.skill === null`、`res.skillBlock === ''`，且 `ctx.rescanCalls === 1`（既不 0 次也不 ≥2 次 —— 「有界」是行为断言，不靠读源码）。**断言必须量该 ctx 的增量，不得复用前面用例的 ctx 累加值。**
       - 用例 7（**源码护栏 · 接线与有界性**）：用既有 `methodBody(readSource('ai-manager.js'), '_resolveSkillInvocation')` 取方法体（先断 `length >= 400` 防提取口径失效假绿），断言：含 `syncAgentSystemPrompt(`；`readSkillForInvocation(` 出现**恰 2 次**（首次 + 重试）；含 `console.warn`（重扫失败不静默）；**不含** `while` / `for`（禁循环重试）；**不含** `refreshSkills(`（必须经 `syncAgentSystemPrompt()` 而非自调重扫）；含 `skillErrorFromReason(`（失败出口唯一）。另加一条「修复落在调用侧」的护栏：`methodBody(readSource('ai-skills-manager.js'), 'readSkillForInvocation')` 内仍含 `if (!entry) return { ok: false, reason: 'not_found', name };`（缓存未命中的短路仍在 manager 侧，本计划没有把第二套判定搬进 manager），且该文件仍不含 `syncAgentSystemPrompt`。
       - **既有 helper 无需扩展**：`writeSkill` 的默认行为（`name` = 目录名）已够造出靶心技能，`setupSkillsEnv` / `withTempRoot` / `methodBody` 一律不动；本计划新增的只有调用侧上下文夹具 `skillResolveCtx`，它不修改任何既有 helper → 对既有 132 例零影响（与 48-04 给 `writeSkill` 加 `frontmatterName` 的「默认值 = 既有行为」同款纪律）。
       - **设计已由探针预验证（可直接照做，勿再改口径）**：本机临时工作区 + 真实 `createSandboxEnv` / `refreshSkills` / SDK 实测得 —— 空技能集起步 → 运行期新增目录 → 现状 `_resolveSkillInvocation` 返回 `skill_not_found` 且 `rescanCalls === 0`；插入本计划的重试块后同一输入返回 `{name:'probe-new', tier:'managed'}`、`skillBlock` 含新正文、`rescanCalls === 1`、`_skillsPromptDirty === true`、`agent.state.systemPrompt === 'OLD'`（忙分支）；disabled 负例 → `skill_disabled` + `skillBlock === ''`；同名遮蔽负例 → `tier === 'user'`、user 正文进块、managed 条目 `shadowed === true`；缓存命中改盘 → `rescanDelta === 0`；运行期删目录 → 仍 `not_found`。
    3. 跑 `node tests/test-ai-skills.js`，把**实测总例数**记下来（Task 2 的文档要写它）；若 `< verify >` 里的门槛与实测不符，以实测为准并在 SUMMARY 记明「门槛是为防漏测设的地板，实测值高于地板属正常」。
    4. 在 SUMMARY 里登记：修复前/后的行为对照（`_resolveSkillInvocation` 在 miss 场景的返回）、本机实测的 `rescanCalls` 值（正例 1、负例 1、负例 3）、以及「缓存命中时 `rescanDelta === 0`」这一快路径不变式。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --check ai-manager.js &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（`node --check` 语法错或测试断言失败），或输出中 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 139（修复前实测地板 132 + 新增 7 条），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const src=fs.readFileSync('ai-manager.js','utf8');const i=src.indexOf('  async _resolveSkillInvocation(');const j=src.indexOf('\n  }',i);const b=src.slice(i,j);const bad=[];if(!b.includes('syncAgentSystemPrompt('))bad.push('缺少权威重扫调用');if((b.match(/readSkillForInvocation\(/g)||[]).length!==2)bad.push('readSkillForInvocation 出现 '+(b.match(/readSkillForInvocation\(/g)||[]).length+' 次（须恰 2 次：首次 + 重试）');if(/\bwhile\b|\bfor\s*\(/.test(b))bad.push('出现循环（重试必须至多一次）');if(b.includes('refreshSkills('))bad.push('直接调 refreshSkills（必须经 syncAgentSystemPrompt）');if(!b.includes('console.warn'))bad.push('重扫失败无告警（不得静默）');if(!b.includes('skillErrorFromReason('))bad.push('失败出口缺失');if(b.length&lt;400)bad.push('方法体提取口径失效（长度 '+b.length+'）');if(bad.length){console.error('miss 重试形状不合格:\n'+bad.join('\n'));process.exit(1)}const mgr=fs.readFileSync('ai-skills-manager.js','utf8');const k=mgr.indexOf('async function readSkillForInvocation(');const mb=mgr.slice(k,mgr.indexOf('\n}',k));if(!mb.includes('if (!entry) return { ok: false, reason: \'not_found\', name };')){console.error('manager 侧缓存未命中短路被改动（修复应留在调用侧）');process.exit(2)}if(mgr.includes('syncAgentSystemPrompt')){console.error('manager 侧出现 syncAgentSystemPrompt（跨层依赖违规）');process.exit(3)}console.log('miss retry shape ok (bounded, single authority path)')"</automated>
    <fails_when>非零退出，或出现 'miss 重试形状不合格' / 'manager 侧缓存未命中短路被改动' / 'manager 侧出现 syncAgentSystemPrompt' 任一行，或标准输出不含 "miss retry shape ok (bounded, single authority path)"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const s=require('fs').readFileSync('tests/test-ai-skills.js','utf8');const i=s.indexOf('G-48-12');if(i&lt;0){console.error('缺 G-48-12 用例组');process.exit(1)}const seg=s.slice(i);for(const t of ['skillResolveCtx','rescanCalls','shadowed === true','skill_disabled','skill_not_found']){if(!seg.includes(t)){console.error('新用例组缺断言素材：'+t);process.exit(2)}}console.log('J 组用例素材齐备')"</automated>
    <fails_when>非零退出，或出现 '缺 G-48-12 用例组' / '新用例组缺断言素材' 任一行，或标准输出不含 "J 组用例素材齐备"</fails_when>
  </verify>
  <done>
    `_resolveSkillInvocation` 在 `not_found` 时经 `syncAgentSystemPrompt()`（函数体逐字未改）重扫一次后重试一次，运行期新增的技能目录因此可被 `/skill:<新名>` 当场读盘调用；shadowed / disabled / tier 三字段全部来自 `refreshSkills` 同一条管线，行为由 J 组用例（正例 2 + 快路径 1 + 负例 3 + 源码护栏 1）钉死；`ai-skills-manager.js` 零改动、失败码域仍两个、忙时不改写 prompt 只置脏；本任务的三条 automated 验证门全绿且 `node tests/test-ai-skills.js` 的 `# fail` 为 0。
  </done>
</task>

<task type="auto">
  <name>Task 2: 文档与账本收口（产品文档 / AGENTS.md / WINDOWS id 24 / 验证矩阵 / UAT 更正）</name>
  <files>docs/product/ai-skills.md, AGENTS.md, .planning/WINDOWS.md, .planning/phases/48-skill-name/48-VALIDATION.md, .planning/phases/48-skill-name/48-UAT.md</files>
  <read_first>
    - `docs/product/ai-skills.md:289-304`（§10.3 实时读盘口径，含 48-06 落的「存在性 / 启停判定只在主进程」条目 —— 新条目要接在它之后、口径不冲突）、`:364-383`（§10.7 诚实边界，现有六条 bullet）、`:96-104`（§七 测试与验证：`node tests/test-ai-skills.js`（**132 例，实测**）那一行）
    - `AGENTS.md:267`（技能域 `- 测试：` 清单行的完整文本，含 `test-agent-workspace` / `test-ai-bash-policy` / `test-builtin-skills-seeder` / `test-skill-picker-model` / `test-ai-cancel-state` 五个条目）与 `:272`（「技能发现与调用的维护约定」：改动实时读盘口径**必须同步**该章节**与本节测试清单**）
    - `.planning/WINDOWS.md:1-14`（frontmatter：`open_count: 23` / `waived_count: 0` / `fixed_count: 1` / `total_count: 24`）、`:41`（id 24 的**表行**）、`:320-334`（同一记录在 ````json 块里的**第二份**拷贝 —— 两处必须同步改，否则账本自相矛盾）
    - `.planning/phases/48-skill-name/48-VALIDATION.md:50-56`（Per-Task Verification Map 的 10 列表头与列宽口径）、`:57-80`（既有行样例，含 48-06 落的 `（G-48-3 修订，2026-09-12）` 写法）
    - `.planning/phases/48-skill-name/48-UAT.md:463-475`（文件末尾 `## Deferred Follow-Ups` 的 YAML 块 —— 纯追加落点），以及 `:337-345`（`## Gaps` 的 ```yaml 块开头 —— **不改**）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `G-48-3`（`root_cause` 末句「（AI 经 write/bash 建目录 → 主进程 idle 边界重扫并广播）」与 `missing` 第 4 条）—— 待更正的两处错误前提
    - `tests/test-ai-skills.js` 的实测输出（Task 1 记录的总例数 —— §七 与 `AGENTS.md` 的例数取值来源，**不得写未实测的数字**）
  </read_first>
  <action>
    1. `docs/product/ai-skills.md`：
       - §10.3 在既有「存在性 / 启停判定只在主进程（G-48-3）」条目之后追加一条 bullet（格式与同级 bullet 一致，带 `（G-48-12）` 标记）：**缓存未命中时的调用瞬间重扫**。内容必须覆盖：① 运行期新增的技能目录（含 AI 经 `write` / `bash` 创建）**不需要**先打开 `/` 面板或重启即被 `/skill:<新名>` 调用 —— 显式调用一旦判定为「不存在」，主进程会经 `syncAgentSystemPrompt()` 做**一次**权威重扫（两个技能目录）后再当场读盘；② 重扫只为「把磁盘现状带进缓存」，注入的正文**恒为**重扫后那次读盘的结果（不是缓存里的副本）；③ 重扫产出的 shadowed / disabled / tier 三字段即该次调用的判定依据 —— 不存在「重扫之后另有一套判定」；④ 该重扫**至多一次**，因此误诊一个技能名的代价是「一次全量重扫」（与打开面板同款），不影响失败语义（仍两个码）。
       - §10.7 追加三条 bullet（诚实边界，带 `（G-48-12）` 标记）：① **成本**：每次「缓存未命中」的显式调用多一次全量重扫（打开 `/` 面板本就会做同款重扫）；缓存命中时零重扫（实测不变式）；② **忙时语义**：显式调用路径恒处于「正在处理」（`prompt` / `promptWithContext` 在解析技能之前即置位），因此重扫后**不会**在轮内改写 `agent.state.systemPrompt`、也不广播 —— 只置脏，回写与 `skills:changed` 广播在下一次非忙同步点（打开面板 / Agent 重建 / 带附件或引用的那轮结束）落地；③ **残余窗口**：重扫只在「缓存未命中」与「既有触发点（打开面板、Agent 创建或重建、idle 补刷）」发生 —— 因此「同名技能在**更高优先级目录**运行期新增」与「禁用清单在无重扫期间变更」这两种**缓存命中**情形，会在下一次重扫时才被纠正（46 D-09 的既有口径：禁用清单只在重扫时生效，本阶段不改变这一点）。**同时**在这一节显式写一句：`>64 KiB` 的**已缓存**技能在实时读盘路径上仍绕过字节闸（WR-06）**保持开放**，随 Phase 49 一并处置 —— 本条不得被读成「已修」。
       - §七：把 `node tests/test-ai-skills.js` 那一行的例数刷成 Task 1 实测值（写「（N 例，实测）」形态，N 取实测输出，**不要**按推算填），并在覆盖面描述的末尾补一段「**运行期新增技能的调用瞬间重扫 + 当场读盘（G-48-12）**」。该行其余文字不动。
    2. `AGENTS.md` 技能域 `- 测试：` 清单行：**新增**一个条目 —— `` `node tests/test-ai-skills.js`（技能域全量单测：加载管线 / 诊断 / 限额 / 启停 / prompt 注入 / Agent 回写 / 显式调用解析与实时读盘 / 投影与 tier / `read` 卡片标记 / 重载装饰 / 运行期新增技能的一次性权威重扫，N 例）``，N 取 Task 1 实测值。理由写进 SUMMARY：该文件此前只在 `docs/product/ai-skills.md` §七 登记，指令文件清单缺项；`AGENTS.md:272` 的维护约定要求「改动实时读盘口径必须同步该章节**与本节测试清单**」，故本次补登记是该约定的执行（不是新增规则）。清单行其余条目、`:272` 的约定文字、文件其余部分**不动**。
    3. `.planning/WINDOWS.md`：id 24 记录**两处**同步更正（不得只改一处）：
       - 表行（`:41`）与 JSON 条目（文件末 ````json 块内 `"id": 24`）的 `description` 改为记录「**48-07 已修** + 机制更正」的文本，内容必须包含：① 原文把机制写成「主进程重扫 → 广播 → IPC 往返」，暗示存在「idle 边界自动重扫」链 —— 该链在代码中**不存在**（`_skillsPromptDirty` 只由 `syncAgentSystemPrompt` 自身在忙时置位，不是独立触发源）；② 真正的失效点是主进程 `readSkillForInvocation` 的**缓存存在性门**（缓存未命中即 `not_found`，读盘路径不执行）；③ 48-07 的修法 = 调用侧 miss 一次性权威重扫 + 重试；④ 本条为 unrun-verify，最终证据仍是重跑 `/gsd-verify-work 48` 的自动驱动探针（在 `managed-skills/` 下新建目录后**不打开** `/` 面板直接手打 `/skill:<新名>`）。**status 保持 `open`**（不得 `waive` / 不得 `fixed`）、`resolved_at` 保持空、frontmatter 的 `open_count: 23` / `waived_count: 0` / `fixed_count: 1` / `total_count: 24` 与 `last_updated` **一律不动**（没有状态变化，不重算计数 —— 改了计数就与「仍 open」自相矛盾）。
       - 两处的措辞可以不同（表格是单行、JSON 是字符串），但①②③④四个事实必须都出现；文本里**不得**含 `|` 字符（会破坏表行）与未转义的双引号（会破坏 YAML/JSON）。
    4. `.planning/phases/48-skill-name/48-VALIDATION.md`：在 Per-Task Verification Map 的表末（`| 48-01-T1 | 01 | 1 | （硬约束） | — | … |` 那两行之后、`**A–E 五组断言清单**` 说明段之前）追加**一行**，列数与表头一致（10 列）：Task ID `48-07-T1`、Plan `07`、Wave `6`、Requirement `DISC-02`、Threat Ref `T-48-07-01`、Secure Behavior 写「运行期新增技能目录（未重扫）→ `/skill:<新名>` 成功；miss 时经唯一权威入口 `syncAgentSystemPrompt()` 重扫**一次**再当场读盘（shadowed / disabled / tier 三字段全部来自 `refreshSkills` 管线，不新增第二套判定）；缓存命中时零重扫」，Test Type `unit + 行为`、Automated Command `node tests/test-ai-skills.js`、File Exists `✅（新增）`、Status `⬜ pending`。表头、其余行与说明段不动。
    5. `.planning/phases/48-skill-name/48-UAT.md`：**纯追加**（只允许在文件最末新增内容，不得修改既有任何一行）一个小节：
       - 位置：`## Deferred Follow-Ups` 的 ```yaml 块结束标记**之后**、文件末尾；标题 `## 更正（G-48-12 立项时，2026-09-12）`。
       - 内容三条：① **更正 G-48-3 的 root_cause 括注**：其括注「运行期新增技能（AI 经 write/bash 建目录 → 主进程 idle 边界重扫并广播）」描述的链路**在代码里并不存在** —— `_skillsPromptDirty`（`ai-manager.js:1325-1334`）不是独立触发源，该标记只由 `syncAgentSystemPrompt()` 自身在忙时置位；因此「运行期新增技能」当时并无可依赖的自动重扫。② **真正的失效点与修法**：主进程 `readSkillForInvocation`（`ai-skills-manager.js:802-804`）在触盘**之前**用 `_cache.skills` 做存在性门，缓存未命中即 `not_found`；48-07 在调用侧（`_resolveSkillInvocation`）对 `not_found` 做**至多一次**的 `syncAgentSystemPrompt()` 权威重扫 + 重试，因此 shadowed / disabled / tier 三个字段全部来自 `refreshSkills` 同一条管线（见 `.planning/phases/48-skill-name/48-07-PLAN.md` 的 `key-decisions`）。③ **交接**：本小节不修改上方 Gaps 的 YAML 块（`G-48-12` 的 `status: failed` 保持原样）—— 该 status 由重跑 `/gsd-verify-work 48` 的自动驱动探针（UAT test 12）判定后回填。
       - 这一节是正文 markdown 段落，**不得**写成 ```yaml 块（避免被 gap 解析器当记录读入）。
    6. 不要改 `48-VERIFICATION.md`、不要改 `48-06-PLAN.md` / `48-06-SUMMARY.md`、不要改任何其它阶段的文件。在 SUMMARY 里记明「本计划改 `ai-manager.js` / `tests/test-ai-skills.js` / `docs/product/ai-skills.md` 与四份 48 阶段/账本文本 → 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）」；`AGENTS.md` 仍属项目指令文件、不在 `covered_files` 内（与 48-06 同款记账口径）。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const d=fs.readFileSync('docs/product/ai-skills.md','utf8');const bad=[];for(const t of ['（G-48-12）','缓存未命中']){if(!d.includes(t))bad.push('docs 缺 '+t)}const i3=d.indexOf('### 10.3'),i4=d.indexOf('### 10.4');const s3=d.slice(i3,i4);for(const t of ['syncAgentSystemPrompt','至多一次']){if(!s3.includes(t))bad.push('§10.3 缺 '+t)}const i7=d.indexOf('### 10.7'),i8=d.indexOf('### 10.8');const s7=d.slice(i7,i8);for(const t of ['忙','残余','WR-06','保持开放']){if(!s7.includes(t))bad.push('§10.7 缺 '+t)}const m=d.match(/（\*{0,2}(\d+) 例，实测\*{0,2}）/);if(!m){bad.push('§七 例数标注缺失或非「（N 例，实测）」形态')}else if(Number(m[1])&lt;139){bad.push('§七 例数 '+m[1]+' 未刷成修复后实测值（地板 139）')}if(bad.length){console.error('docs 收口不合格:\n'+bad.join('\n'));process.exit(1)}console.log('docs §10.3/§10.7/§七 收口 ok')"</automated>
    <fails_when>非零退出，或出现 'docs 收口不合格' / 'docs 缺 ' / '§10.3 缺 ' / '§10.7 缺 ' / '§七 例数标注缺失' 任一行，或标准输出不含 "docs §10.3/§10.7/§七 收口 ok"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const w=fs.readFileSync('.planning/WINDOWS.md','utf8');const bad=[];const row=w.split('\n').find(l=&gt;l.startsWith('| 24 |'));if(!row)bad.push('缺 id 24 表行');else for(const t of ['48-07','缓存存在性门'])if(!row.includes(t))bad.push('表行缺 '+t);const i=w.indexOf('json' + String.fromCharCode(10) + '[');const j=w.lastIndexOf(String.fromCharCode(10) + ']');if(i&lt;0||j&lt;i){bad.push('缺 JSON 块')}else{const arr=JSON.parse(w.slice(w.indexOf('[',i), j+2));const e=arr.find(x=&gt;x.id===24);if(!e)bad.push('JSON 块缺 id 24 条目');else{for(const t of ['48-07','缓存存在性门'])if(!e.description.includes(t))bad.push('JSON 条目缺 '+t);if(e.status!=='open')bad.push('JSON 条目 status 被改：'+e.status);if(e.resolved_at!==null)bad.push('JSON 条目 resolved_at 被改')}}const fm=w.split('---')[1];for(const t of ['open_count: 23','total_count: 24']){if(!fm.includes(t))bad.push('frontmatter 计数被改动，缺 '+t)}if(bad.length){console.error('WINDOWS id 24 收口不合格:\n'+bad.join('\n'));process.exit(1)}console.log('WINDOWS id 24 updated in both copies, status still open')"</automated>
    <fails_when>非零退出，或出现 'WINDOWS id 24 收口不合格' 或任一条 `缺 …` / `status 被改` / `计数被改动` 行，或标准输出不含 "WINDOWS id 24 updated in both copies, status still open"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const {execSync}=require('child_process');const bad=[];const a=fs.readFileSync('AGENTS.md','utf8');if(!a.includes('node tests/test-ai-skills.js'))bad.push('AGENTS.md 测试清单未登记 test-ai-skills.js');const v=fs.readFileSync('.planning/phases/48-skill-name/48-VALIDATION.md','utf8');if(!/^\| 48-07-T1 \|/m.test(v))bad.push('48-VALIDATION 缺 48-07-T1 行');const cols=(v.match(/^\| Task ID \|.*$/m)||[''])[0].split('|').length;const r=(v.match(/^\| 48-07-T1 \|.*$/m)||[''])[0].split('|').length;if(cols!==r)bad.push('48-07-T1 行列数 '+r+' 与表头 '+cols+' 不一致');const u=fs.readFileSync('.planning/phases/48-skill-name/48-UAT.md','utf8');if(!u.includes('## 更正（G-48-12 立项时，2026-09-12）'))bad.push('48-UAT 缺更正小节');if(!u.includes('status: failed'))bad.push('G-48-12 的 status 被改动');let head=null;try{head=execSync('git show HEAD:.planning/phases/48-skill-name/48-UAT.md',{encoding:'utf8'})}catch(e){}if(head){const H=head.split('\n'),C=u.split('\n');for(let i=0;i&lt;H.length;i++){if(H[i]!==C[i]){bad.push('48-UAT 非纯追加：第 '+(i+1)+' 行被改动');break}}}if(bad.length){console.error('账本收口不合格:\n'+bad.join('\n'));process.exit(1)}console.log('AGENTS.md + 48-VALIDATION + 48-UAT(append-only) ok')"</automated>
    <fails_when>非零退出，或出现 '账本收口不合格' 或任一条 `未登记` / `缺 …` / `列数 … 不一致` / `status 被改动` / `非纯追加` 行，或标准输出不含 "AGENTS.md + 48-VALIDATION + 48-UAT(append-only) ok"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1 &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（任一测试套件断言失败），或输出中 "# fail" 行 &gt; 0，或（ai-skills）"# pass" 行 &lt; 139、（picker）"# pass" 行 &lt; 93，或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `docs/product/ai-skills.md` 的 §10.3（miss 一次性重扫口径）/ §10.7（成本 / 忙时语义 / 残余窗口 + WR-06 保持开放的显式声明）/ §七（实测例数 + 覆盖面）落地；`AGENTS.md` 技能域测试清单补登记 `node tests/test-ai-skills.js`；`.planning/WINDOWS.md` 的 id 24 在表行与 JSON 条目**两处**同步更正且 status 仍 `open`、计数未动；`48-VALIDATION.md` 新增 10 列对齐的 `48-07-T1` 行；`48-UAT.md` 以纯追加方式更正 G-48-3 的错误前提并交接待回填的 gap status；本任务的四条 automated 验证门全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 磁盘（`managed-skills/` / `skills/`，AI 经 write/bash 可免确认写入）→ 主进程缓存 | 缓存是 `shadowed` / `disabled` / `tier` 的唯一权威；跨过它直读磁盘就等于绕过全部既有判定 |
| renderer → main（`/skill:<name>`） | 判定与文案的唯一权威侧；miss 重试必须留在主进程侧，renderer 不得新增重扫入口 |
| 主进程 → renderer（`skills:changed` 广播） | 广播只能由「真正改写 prompt」的那一次同步触发（全仓唯一广播点），消费侧只重拉零 IO 快照 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-07-01 | Spoofing | `_resolveSkillInvocation` 的 miss 重试块 | high | mitigate | 重试只调用 `syncAgentSystemPrompt()`（唯一加载入口 `refreshSkills` 的调用方），shadowed（46 D-06 / D-11）、disabled（46 D-09 / D-10）、tier（D-14）全部由该管线产出；负例 4 / 5 断言「运行期新增但并入禁用清单 → `skill_disabled` 零注入」「运行期两根同名 → 注入胜出者正文且败者 `shadowed === true`」；源码门禁断言 `_resolveSkillInvocation` 内不出现 `refreshSkills(` 自调 |
| T-48-07-02 | Denial of Service | 未知 / 误诊技能名触发的全量重扫 | medium | accept | 重试**至多一次**（源码门禁禁 `while` / `for`，行为用例断言 `rescanCalls === 1`），且只在显式 `/skill:` 语法且判定为 `not_found` 时触发；单次成本与「打开 `/` 面板」同款（两个技能目录、SKILL.md 受 64 KiB 字节闸约束）。成本与残余窗口写进 `docs/product/ai-skills.md` §10.7 |
| T-48-07-03 | Tampering | 重扫抛错时的判定与状态 | medium | mitigate | 块内 `try/catch` + `console.warn`（禁止静默），失败后**保留原 `not_found` 判定**并把 `result` 交回既有失败出口；不改 `isProcessing` 的既有复位职责（WR-02 的处置不在本计划范围，不得顺手改） |
| T-48-07-04 | Tampering | 实时读盘路径的 64 KiB 字节闸 | medium | accept | WR-06 已由用户裁决「保持开放、随 Phase 49 处置」；重扫路径（miss 场景）**受益**于 `createSkillsEnv` 的 `readTextFile` 字节闸（超限 SKILL.md 不进缓存 → `not_found`），但**已缓存**技能的命中路径仍绕过该闸 —— 本计划不修、文档 §10.7 显式声明「仍开放」，禁止声称已修 |
| T-48-07-05 | Denial of Service | 广播 → 重扫 → 再广播的自激回路 | medium | mitigate | 重扫必走 `syncAgentSystemPrompt()` 的忙分支（调用点先置 `isProcessing = true`）→ 既不改写 prompt 也不广播；广播只在非忙同步点发生，且 renderer 消费侧 `.refreshSkills(` 计数恒 1（既有 G 组断言继续绿） |
</threat_model>

<verification>
- `node --check ai-manager.js` 通过；`node tests/test-ai-skills.js` → `# fail 0`、`# pass ≥ 139`（修复前地板 132 + 新增 7 条）；`node --test tests/test-skill-picker-model.js` → `# fail 0`、`# pass ≥ 93`（renderer 侧护栏未回归）
- 源码形状门禁：`_resolveSkillInvocation` 含 `syncAgentSystemPrompt(` / `console.warn` / `skillErrorFromReason(`，`readSkillForInvocation(` **恰 2 次**，无 `while` / `for`，无 `refreshSkills(`；`ai-skills-manager.js` 仍含缓存未命中短路且不出现 `syncAgentSystemPrompt`
- 三字段来源经行为用例钉死：disabled → `skill_disabled` 零注入；shadowed → 注入 user 版正文 + 快照内 managed 条目 `shadowed === true`；tier → managed / user 两档正例
- 快路径不变式：缓存命中时 `rescanCalls` 增量 0；运行期删目录仍 `not_found`
- 文档与账本：§10.3 / §10.7 / §七 三处落地且 WR-06 的「保持开放」显式在册；`AGENTS.md` 测试清单补登记；`WINDOWS.md` id 24 两处同步更正且 status 仍 `open`、计数未动；`48-VALIDATION.md` 新增行 10 列对齐；`48-UAT.md` 纯追加
- **诚实边界**：「运行期新增技能目录 + 未打开过面板 → 手打 `/skill:<新名>` 可调用」是**运行时**行为（重扫 → 缓存 → 读盘 → IPC 往返），node:test 只覆盖调用侧契约与三字段来源；最终证据是重跑 `/gsd-verify-work 48` 的自动驱动探针（UAT test 12，B/B2 判别性复测形态），须在 SUMMARY 登记；本计划**不**把该 gap 自行标为 resolved
- 指纹提示：本计划改 `ai-manager.js` / `tests/test-ai-skills.js` / `docs/product/ai-skills.md` / `AGENTS.md` / `.planning/WINDOWS.md` / `48-VALIDATION.md` / `48-UAT.md` → 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）；`AGENTS.md` 与 `.planning/WINDOWS.md` 不在 `covered_files` 内，按 48-06 同款口径记账
</verification>

<success_criteria>
- 运行期新增技能目录（不打开 `/` 面板）→ 直接手打 `/skill:<新名> [args]` 正常调用，主进程日志出现 `发送消息: /skill:…`
- miss 重试有界（至多一次）、失败语义不变（仍 `not_found` / `disabled` 两个码）、重扫抛错不静默且不改判定
- shadowed / disabled / tier 全部来自 `refreshSkills` 同一条管线，无第二套判定；`ai-skills-manager.js` 与 `syncAgentSystemPrompt()` 函数体逐字未改
- 缓存命中快路径零重扫；renderer 重扫调用点仍恰一处（不自激）
- 测试面新增 7 条覆盖「运行期新增目录 → 直接 `/skill:` 调用」形态（正例 2 + 快路径 1 + 负例 3 + 源码护栏 1），全套零 fail
- 文档（§10.3 / §10.7 / §七）与账本（`AGENTS.md` 清单 / `WINDOWS.md` id 24 两处 / `48-VALIDATION.md` 矩阵 / `48-UAT.md` 纯追加更正）同步落地，且 WR-06 的开放状态被显式声明、无任何「已修」表述
</success_criteria>

<output>
Create `.planning/phases/48-skill-name/48-07-SUMMARY.md` when done
</output>
