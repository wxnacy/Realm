---
phase: 48-skill-name
plan: 5
type: execute
wave: 4
depends_on: [48-01, 48-02, 48-03]
gap_closure: true
gap_ids: [G-48-6, G-48-4]
files_modified:
  - src/renderer.js
  - src/ai-cancel-state.js
  - src/index.html
  - tests/test-skill-picker-model.js
  - tests/test-ai-cancel-state.js
autonomous: true
requirements: [DISC-02, DISC-03]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "发送 `/skill:<name> <args>` 后**无需任何额外交互**，用户气泡即含技能 pill（`.ai-skill-pill`）与默认折叠的「技能正文（N 字符）」块（`.ai-skill-content-box`）—— 修复前两者只在**下一次整列重绘**（切对话 / 重载 / `/compact`）才出现（UAT test 6 clause 1：发送后立即与整轮跑完均 `hasPill=false` / `hasBox=false`，手动调 `renderAIMessages()` 后立即正确出现）"
    - "AI 正在流式回复（或卡在工具确认卡片）时发起技能调用：新技能调用正常发出、新气泡正常流式回显；**不得**出现新气泡被写成「用户已取消」、其后零增长、停止按钮提前回退（UAT test 4 实测序列：新气泡 t≈7.4s 被写成取消文案，此后 15s 零增长、`.ai-skill-pill` 未出现、无任何 system-note）"
    - "迟到到达的取消事件**只作用于被取消的那条消息**：取消分支按锚点（`state.aiCancelledMessageId`）解算目标，不得读「当前」`state.aiCurrentMessageId` 做归属；新一轮的 `message_update` / `tool_execution_update` 因此不再被丢弃"
    - "停止按钮语义逐字不变：用户点停止时锚点 = 当前轮，取消分支仍把该气泡标为已取消、仍切回发送按钮（`resetRunState` 为真）"
    - "多轮连续调用不串台：`switchConversation` / `createNewConversation` 清空锚点，迟到事件不会落到新对话的消息上"
    - "重发 / 重试路径同样在本次读盘结果回填后立即刷新该气泡的折叠块（不留上一轮的旧正文 / 旧 N）"
    - "断言面真正覆盖「abort × 新消息」时序：纯逻辑用例用 UAT test 4 的三条消息序列（旧 assistant / 新 user / 新 assistant 占位）+ 锚点=旧 id / 当前=新 id 直接判定归属与是否复位轮次状态"
  artifacts:
    - "`src/renderer.js`：新增 `buildUserMessageContent(msg)`（用户气泡构建**单源**：@引用 pill → 技能 pill → 正文 → 内联图片 → 附件徽标 → 技能正文折叠块）与 `refreshUserMessageBubble(messageId)`（只重绘这一条：`replaceChild` 掉旧 `.ai-message-content`，不重建整列、不丢滚动位置与流式光标）；`renderAIMessages` 的 isUser 分支改为调用 `buildUserMessageContent`（不得保留第二份构建逻辑）"
    - "`src/renderer.js`：`state.aiCancelledMessageId`（被取消消息锚点）；`abortAIIfStreaming` / `handleStopAI` 在置 `aiCancelledByUser` 的同处记录锚点；`handleAIStream` 的取消分支改为按锚点解算（含 `resetRunState` 条件复位）；两处对话切换清空锚点"
    - "`src/ai-cancel-state.js`（新，双模式导出 + 零依赖 + 零 DOM）：`resolveCancelAttribution(messages, cancelledId, currentId) → {targetIndex, resetRunState}`"
    - "`src/index.html`：`ai-cancel-state.js` 在 `skill-picker-model.js` 之后、`renderer.js` 之前加载"
    - "`tests/test-ai-cancel-state.js`（新）：A 组纯逻辑表驱动（含 UAT test 4 序列的靶心断言）+ B 组 renderer 接线源码护栏（锚点记录 / 取消分支解算 / 脚本加载顺序 / 两处清空）"
    - "`tests/test-skill-picker-model.js`：气泡顺序断言迁移到 `buildUserMessageContent` + 三处「回填后立即刷新」断言"
    - "文档面**不在本计划**：`docs/product/ai-skills.md` §10.7 的两条行为描述、§10.8 新小节与 §七 测试清单，以及 `AGENTS.md` 的「技能发现与调用」测试清单登记，统一由 48-06 的文档收口任务落地（本轮唯一文档写者，避免同波并发编辑同一文件）。本计划新建的 `tests/test-ai-cancel-state.js` 必须被登记进 `AGENTS.md:267` 的清单 —— `AGENTS.md:272` 的维护约定（改实时读盘口径 / 边界技能行为时「必须同步更新该章节与本节测试清单」）是项目指令级要求，登记动作归 48-06（wave 5，晚于本计划，测试文件此时已存在）"
  key_links:
    - "IPC 返回顺序是 G-48-6 的根因：`renderAIMessages()`（发送起点）早于 `await ai.prompt()` 返回，此后整轮只走 `updateAIStreamingBubble`（只替换 `.ai-message-content` 的 assistant 气泡）—— 因此 `result.skillInvocation` 回填后**必须**主动刷新那一条 user 气泡，否则 pill / 折叠块在本轮永不出现"
    - "G-48-4 的三段链路缺一即回到旧行为：`abortAIIfStreaming`/`handleStopAI` 记录锚点 → 取消分支按锚点解算并只在该锚点仍是当前轮时复位轮次状态 → 落点清空锚点。任一环缺失，迟到 error 就又会按「当前消息 id」归属并清空 `aiCurrentMessageId`，使新轮的流式事件整批被丢弃"
    - "`updateAIStreamingBubble`（`:8179-8193`）与 `refreshUserMessageBubble` 的定向更新互不干扰：前者只作用于 assistant 流式气泡，后者只作用于 user 气泡的 `.ai-message-content`"
  prohibitions:
    - statement: "不得用「回填后直接调 `renderAIMessages()`」充当 G-48-6 的修复（整列 innerHTML 重建会丢滚动位置、丢掉正在流式的气泡节点与 typing 指示器状态）"
      status: unresolved
      verification: judgment
      reason: "UAT 的 `missing` 第一条明确要求「只重绘这一条，避免整列重建丢滚动位置与流式光标」；且该文件既有设计（updateAIStreamingBubble 的注释）就是为了避开 16ms 批次下的整列重建闪烁"
    - statement: "不得删除 `state.aiCancelledByUser` 或改写「用户点停止」的既有语义（既有行为：气泡被标为取消 + 按钮切回发送）—— 本次只把归属对象从「当前消息」换成「被取消的消息」"
      status: unresolved
      verification: judgment
      reason: "该 flag 与停止按钮语义由既有实现在多个入口消费；换归属是隔离新轮所需的最小改动，删旗标会让取消事件与真实错误无法区分（会在聊天区弹出错误条 + 重试按钮）"
    - statement: "本轮**不做** 48-REVIEW.md CR-03 的替代方案「让 `ai:abort` 等 run 结算后再返回」（`ipc-handlers.js:1840-1846` 增加 await）"
      status: unresolved
      verification: judgment
      reason: "锚点方案已能隔离归属，不需要在 renderer↔main 契约上新增等待语义；等待结算会在主进程引入新的挂起路径（run 未结算时 renderer 的新消息被无限期挡住，且需要超时兜底），风险高于收益。该取舍须在 SUMMARY 记明"
---

<objective>
关闭两个 UAT gap：**G-48-6**（技能调用发出后用户气泡的 pill 与「技能正文」折叠块不出现，只在整列重绘后才出现）与 **G-48-4**（流式中 / 卡在工具确认卡片时调用技能 → 新气泡被写成「用户已取消」、15s 零增长、停止按钮提前回退、新一轮输出完全不可见）。

Purpose: 两者都是「技能调用发起**之后**的可见性与状态归属」缺陷 —— 一个在渲染时机（回填元数据后没有渲染），一个在事件归属（迟到取消事件按「当前消息」而不是「被取消的消息」记账）。它们让 `/skill:name` 在真实使用中最常见的两个场景（发完看结果、边生成边改主意）都不可用。
Output: 用户气泡构建单源 + 定向刷新；取消锚点 + 独立纯逻辑模块与其时序用例。文档面（§10.7 / §10.8 / §七）归 48-06 收口。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-UAT.md
@.planning/phases/48-skill-name/48-REVIEW.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-01-SUMMARY.md
@src/renderer.js
@src/skill-picker-model.js
@src/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用户气泡构建单源 + 回填后定向刷新该条气泡（G-48-6）</name>
  <files>src/renderer.js, tests/test-skill-picker-model.js</files>
  <read_first>
    - `src/renderer.js:7967-8120`（`renderAIMessages` 的逐条循环：`skipBubble` 守卫、`isUser` 分支的全部构建步骤、`else if (content)` 的 assistant markdown 分支）
    - `src/renderer.js:8130-8160`（`wrapper.dataset.messageId` 的写入点 —— 定向刷新的定位依据）
    - `src/renderer.js:8179-8193`（`updateAIStreamingBubble` 的定向更新与「找不到 wrapper 则回落整列重绘」容错先例）
    - `src/renderer.js:8835-8865`（`handleSendAIMessage` 消费 `result.skillInvocation` / `result.skillError` 的两段）
    - `src/renderer.js:9905-9925`（`regenerateMessage` 的 `res.skillInvocation` 回填）、`:9980-10000`（错误重试按钮内的同款回填）
    - `tests/test-skill-picker-model.js:358-388`（「气泡渲染：pill + 折叠块…」用例 —— 其中对 `renderAIMessages` isUser 段的顺序断言本任务必须迁移）
    - `docs/product/ai-skills.md:288-322`（§10.3 实时读盘、§10.4 边界行为表 —— 气泡契约的既有表述面）
  </read_first>
  <action>
    1. 抽出 `buildUserMessageContent(msg)`（新函数，放在 `renderAISkillPill` / `renderSkillContentBox` 附近）：把 `renderAIMessages` 的 `isUser` 分支体**逐字搬入**（顺序不得改变）：创建 `class="ai-message-content"` 的容器 → `msg.referencedTabs` 的 pill 行 → `msg.skillInvocation && msg.skillInvocation.name` 时 `renderAISkillPill(...)` → 正文 `textDiv`（`textContent = msg.content || ''`）→ 内联图片 `renderAIAttachmentImages(...)` → 非图片附件徽标行 → `typeof msg.skillInvocation.content === 'string'` 时 `renderSkillContentBox(...)`；返回该容器元素。JSDoc 写明「用户气泡构建**唯一实现**：`renderAIMessages` 与 `refreshUserMessageBubble` 共用，不得复制第二份（顺序即契约：pill 在正文前、折叠块在附件后）」。
    2. `renderAIMessages` 的对应位置改为：`let content = null; if (!skipBubble) { content = document.createElement('div'); content.className = 'ai-message-content'; }` 之后，`if (isUser) { content = buildUserMessageContent(msg); } else if (content) { …既是 assistant 分支，逐字不动… }`。改完后 `renderAIMessages` 函数体内**不得**再出现 `renderAISkillPill(` / `renderSkillContentBox(` / 附件徽标构建。
    3. 新增 `refreshUserMessageBubble(messageId)`（放在 `updateAIStreamingBubble` 之后）：按 id 在 `state.aiMessages` 找到消息，非 `role === 'user'` 直接 return；用与 `updateAIStreamingBubble` 同款选择器取 `[data-message-id="…"]` wrapper，**取不到则回落 `renderAIMessages()` 后 return**（与既有竞态容错一致）；否则用 `buildUserMessageContent(msg)` 生成新节点并 `replaceChild` 掉 wrapper 内既有的 `.ai-message-content`（旧节点不存在时插到 wrapper 首位）。注释写明理由：只重绘这一条 —— 整列重建会丢滚动位置、丢掉正在流式的 assistant 气泡节点与 typing 指示器状态。
    4. 三处调用（均在**本次读盘结果回填之后立即**，不新增 await、不改变回填语义）：
       - `handleSendAIMessage`：`if (result && result.skillInvocation) { …userMsg.skillInvocation = result.skillInvocation; }` 块内回填后调 `refreshUserMessageBubble(userMsgId)`。位置在该块**内**、`result.skillError` 分支**之前**（skillError 时气泡会被 `removeSkillFailureBubbles` 撤掉，无需刷新）。
       - `regenerateMessage`：`if (res && res.skillInvocation) { …resent.skillInvocation = …; }` 块内回填后调 `refreshUserMessageBubble(resentUserId)`。
       - `showAIError` 的重试按钮处理器：`if (res && res.skillInvocation) { …target.skillInvocation = …; }` 块内回填后调 `refreshUserMessageBubble(retryUserMsg.id)`。
    5. `tests/test-skill-picker-model.js`：
       - 把「气泡渲染：pill + 折叠块走 DOM API / textContent」用例里对 `renderAIMessages` isUser 段的相对顺序断言（`pillCall < textCall < boxCall`）**迁移**到 `buildUserMessageContent` 的函数体（用既有的 `functionBody(rendererSrc, '<name>')` helper）；并补两条：`buildUserMessageContent` 定义只出现一次（`(rendererSrc.match(/function buildUserMessageContent\(/g) || []).length === 1`）；`renderAIMessages` 函数体内**不含** `renderAISkillPill(` 与 `renderSkillContentBox(`（已迁出，防第二份实现复活）。
       - 新增用例「回填后立即刷新该条气泡」：在 `handleSendAIMessage` / `regenerateMessage` / 重试处理器三处分别断言 `refreshUserMessageBubble(` 出现在对应 `skillInvocation` 赋值**之后**（用 `indexOf` 相对位置），且三处都在。
       - 新增用例「`refreshUserMessageBubble` 是定向更新」：函数体内含 `replaceChild`、含 `buildUserMessageContent(`、含找不到 wrapper 时回落 `renderAIMessages()` 的分支；且不含 `innerHTML = ''`（不得整列清空重建）。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --check src/renderer.js &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（`node --check` 语法错，或 `node --test` 有任何断言失败），或输出中 "# fail" 行 > 0，或 "# pass" 行 < 93（picker 既有基线地板），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1</automated>
    <fails_when>非零退出，或输出中 "# fail" 行 > 0，或 "# pass" 行 < 129（地板取 48-04 前的既有基线，见 `<verification>` 的跨计划归属说明），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    发送 `/skill:<name> <args>` 后**无需任何额外交互**，用户气泡即含 `.ai-skill-pill` 与 `.ai-skill-content-box`；重发 / 重试路径同样即时刷新该块；用户气泡构建只有一份实现（`buildUserMessageContent`），`renderAIMessages` 内不再有第二份；定向刷新走 `replaceChild`，不整列清空。`node --test tests/test-skill-picker-model.js` 与 `node tests/test-ai-skills.js` 全绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: 取消锚点 + 独立归属纯逻辑模块 + 时序用例（G-48-4）</name>
  <files>src/ai-cancel-state.js, src/index.html, src/renderer.js, tests/test-ai-cancel-state.js</files>
  <read_first>
    - `src/renderer.js:250-262`（state 里 `aiStreaming` / `aiCurrentMessageId` / `aiCancelledByUser` 的声明处）
    - `src/renderer.js:8403-8414`（`handleStopAI`）、`:9044-9055`（`abortAIIfStreaming`）
    - `src/renderer.js:8727-8741`（技能分支的中止 + 就地复位）、`:9315-9332`（`handleAIStream` 的 `error` 取消分支）、`:9244-9262`（`message_update` / `tool_execution_update` 按 `state.aiCurrentMessageId` 查找 —— 复位后整批丢弃的机制）
    - `src/renderer.js:7206-7212` / `:7243-7250`（`switchConversation` / `createNewConversation` 复位 `aiCancelledByUser` 的两处）
    - `ipc-handlers.js:1840-1846`（`ai:abort` 同步返回 —— 竞态窗口的放大器，本轮不改）
    - `src/skill-picker-model.js:1-40` 与文件尾（双模式导出 + 「为什么抽出独立模块」的注释风格，作为新模块的模板）
    - `src/index.html:1018-1023`（`ai-brand-map.js` / `model-family.js` / `skill-picker-model.js` / `renderer.js` 的加载顺序）
    - `tests/test-ai-skills.js:1665-1676`（脚本加载顺序的既有源码扫描断言，本任务按同款为 `ai-cancel-state.js` 补一条）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `G-48-4`（根因五步 + `missing` 四条）与 `48-REVIEW.md` CR-03 的 Fix 段
  </read_first>
  <action>
    1. 新建 `src/ai-cancel-state.js`（零 import / 零 require / 零 DOM，照 `src/skill-picker-model.js` 的 IIFE + 双模式导出：`module.exports = api` 与 `window.AICancelState = api`），只导出一个纯函数 `resolveCancelAttribution(messages, cancelledId, currentId)`，返回 `{ targetIndex, resetRunState }`：
       - `targetIndex` = `messages` 中 `role === 'assistant' && id === cancelledId` 的索引；`cancelledId` 为空串 / null / undefined，或列表中不存在 → `-1`。
       - `resetRunState` = `!!cancelledId && cancelledId === currentId`。**只由锚点等式决定，与 `messages` 的形态无关**（消息被移除、列表为空都不影响该式，这正是不丢按钮语义的原因，见用例 ⑤）。
       - **入参守卫（本函数不得抛错）**：`messages` 非数组（`null` / `undefined` / 字符串 / 对象）一律按**空列表**处理。实现先 `const list = Array.isArray(messages) ? messages : [];` 再 `list.findIndex(...)` —— 直接 `messages.findIndex(...)` 遇到非数组会抛，违反契约（用例 ⑦ 钉住）。
       - 函数头注释必须写清**语义与边界**：锚点 = 「被取消的那条消息」；新一轮已开始（`currentId !== cancelledId`）时**不得**复位轮次状态，否则新轮的 `message_update` / `tool_execution_update`（按 `state.aiCurrentMessageId` 查找）会因它被置 null 而整批丢弃 —— 这正是 UAT test 4 的实测序列（新气泡 t≈7.4s 被写成取消文案、其后 15s 零增长、停止按钮提前回退）。文件头注释写「为什么抽出独立模块」：`src/renderer.js` 是浏览器脚本无 `module.exports`，判定留在里面只能靠人工/Playwright，抽到这里可纯 Node 表驱动覆盖该时序（与 `skill-picker-model.js` 同款理由）。
    2. `src/index.html`：在 `skill-picker-model.js`（`:1021`）之后、`renderer.js`（`:1022`）之前插入 `<script src="ai-cancel-state.js"></script>`（renderer 的错误分支引用该全局，顺序不可调换）。
    3. `src/renderer.js`：
       - state 声明处（`aiCancelledByUser: false` 旁）新增 `aiCancelledMessageId: null`，注释：被取消消息锚点（取消落点后清空）。
       - `abortAIIfStreaming()`：在置 `state.aiCancelledByUser = true` 的**同处**记 `state.aiCancelledMessageId = state.aiCurrentMessageId`；`catch` 分支里 `aiCancelledByUser = false` 处一并清锚点。
       - `handleStopAI()`：同样记录锚点，但**仅在 `state.aiCurrentMessageId` 为真时**才置 `aiCancelledByUser` 与锚点（无进行中轮次时不留空锚点污染下一轮；此时仍照常调 abort）；`catch` 里同样复位两个。
       - `switchConversation` 与 `createNewConversation` 的 `state.aiCancelledByUser = false` 处各补一行清锚点。
       - `handleAIStream` 的 `case 'error'` 取消分支改为：先算 `const attribution = window.AICancelState.resolveCancelAttribution(state.aiMessages, state.aiCancelledMessageId, state.aiCurrentMessageId);`（**必须在任何状态复位之前**，且不得再用「当前消息 id」做查找判据）；随后 `state.aiCancelledByUser = false;`、清锚点 `state.aiCancelledMessageId = null;`；`attribution.targetIndex >= 0` 时把该条 assistant 消息 `content` 写成既有的「用户已取消」文案；**仅当 `attribution.resetRunState` 为真**时才执行 `state.aiStreaming = false; state.aiCurrentMessageId = null; updateSendButtonState(false);`；`needsRender = true` 保持不变（等同现有停止路径的一次整列重绘，本任务不引入新的定向重绘）。注释写明「取消只作用于被取消的那条消息；新一轮已开始时不得越权复位」。
    4. 新建 `tests/test-ai-cancel-state.js`（`node:test` + `node:assert`，模式照 `tests/test-builtin-skills-seeder.js`）：
       - A 组纯逻辑表驱动（直接 `require('../src/ai-cancel-state')`）：① 正常停止（`[assistant#m1]`、锚点 `m1`、当前 `m1`）→ `targetIndex` 命中该条、`resetRunState === true`；② **UAT test 4 实测序列**（`[assistant#m1, user#u2, assistant#m2]`、锚点 `m1`、当前 `m2`）→ `targetIndex` = `m1` 的索引、`resetRunState === false`（本 gap 的靶心断言：旧气泡被标取消、**新轮状态不被复位**）；③ 锚点为空 → `targetIndex === -1` 且 `resetRunState === false`；④ 锚点指向的消息不在列表（切对话后迟到事件）→ `targetIndex === -1`；⑤ 锚点 = 当前轮但消息已被移除 → `targetIndex === -1` 而 `resetRunState === true`（按钮语义不丢）；⑥ `role !== 'assistant'` 的同 id 条目不得被命中；⑦ **入参守卫**（与用例 ⑤ 同口径）：非数组入参（`null` / `undefined` / 字符串）+ 空数组入参一律不抛错，`targetIndex === -1`；`resetRunState` 仍只由锚点等式决定 —— 锚点为空 → `false`，锚点等于当前轮 → `true`（与 ⑤「锚点 = 当前轮但消息已被移除」一致：消息列表形态不该剥夺按钮复位语义）。
       - B 组 renderer 接线源码护栏（读 `src/renderer.js` 原文）：`abortAIIfStreaming` 与 `handleStopAI` 的函数体都含锚点赋值；取消分支区域（从 `case 'error':` 到该分支的 `break;`）含 `resolveCancelAttribution(` 与锚点标识、且**不含**按当前消息 id 的 `m.id === state.aiCurrentMessageId` 查找；`switchConversation` / `createNewConversation` 函数体内各含一行锚点清空；`src/index.html` 中 `ai-cancel-state.js` 出现在 `renderer.js` 之前（形状照 `tests/test-ai-skills.js:1665` 的既有断言）。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --test tests/test-ai-cancel-state.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（文件不存在 / require 失败 / 任一断言失败），或输出中 "# fail" 行 > 0，或 "# pass" 行 < 7（A 组 ①–⑦ 为地板；B 组护栏另计），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --check src/renderer.js &amp;&amp; node --check src/ai-cancel-state.js &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1 &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（任一 `node --check` 语法错，或任一测试套件有断言失败），或输出中 "# fail" 行 > 0，或（picker）"# pass" 行 < 93、（ai-skills）"# pass" 行 < 129，或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    新模块与用例落地：`[旧 assistant, 新 user, 新 assistant 占位]` + 锚点=旧 id / 当前=新 id 的序列判定为「目标 = 旧消息、不复位轮次状态」；renderer 的取消分支按锚点归属且不再读当前 id；停止按钮语义不变（锚点=当前轮时仍复位）；锚点在对话切换两处被清空；脚本加载顺序有断言。四条测试命令全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主进程 AI 事件流 → renderer 状态机 | 事件到达顺序不由 renderer 控制（abort 同步返回 + 迟到 error），状态归属是正确性边界 |
| 不可信技能名 → 气泡 DOM | 技能名来自磁盘目录名（AI 可自建），只能经 DOM API + `textContent` 进气泡 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-05-01 | Tampering | `handleAIStream` 的取消归属状态机 | high | mitigate | 锚点 + 解算函数：取消只作用于被取消消息；`resetRunState` 条件复位防「新一轮被越权复位 → 流式事件整批丢弃」。纯逻辑用例钉死 UAT test 4 序列 |
| T-48-05-02 | Denial of Service | 新一轮技能调用（用户可见输出丢失） | high | mitigate | 同上；另由「三处回填后立即刷新」保证用户至少看到 pill 与折叠块，不出现「点了没反应」 |
| T-48-05-03 | Tampering | `src/index.html` 脚本加载顺序 | medium | mitigate | 新模块必须早于 `renderer.js`；源码扫描断言钉住（缺它会让错误分支引用未定义全局） |
| T-48-05-04 | Information Disclosure | 气泡 pill / 折叠块的技能名与正文 | low | accept | 正文与名称本就是用户自己技能目录的内容，气泡内不渲染任何未展示过的数据；渲染走 `textContent`（既有 T-48-03 口径不变） |
</threat_model>

<verification>
- `node --test tests/test-ai-cancel-state.js` 全绿（含 UAT test 4 序列的靶心断言）
- `node --test tests/test-skill-picker-model.js` 与 `node tests/test-ai-skills.js` 全绿（气泡顺序断言迁移后仍成立；`renderSkillContentBox` 函数体未改）
- `node --check src/renderer.js` / `node --check src/ai-cancel-state.js` 通过
- 文档：本计划**不改** `docs/product/ai-skills.md`、也**不改** `AGENTS.md`；§10.7 两条 + §10.8 新小节 + §七 新命令 + `AGENTS.md:267` 测试清单登记由 48-06 落地（本计划在 SUMMARY 把「需写的四条」逐条登记，供 48-06 对齐）
- **跨计划归属（`tests/test-ai-skills.js`）**：该文件由 **48-04 独占撰写**（同 wave 4），本计划只**执行**它作回归信号、**不编辑**（它不在本计划 `files_modified` 内）。若该命令变红：先核对是否 48-04 尚在写盘（半编辑态），以 48-04 的 SUMMARY 为准复跑；**不得**在本计划内改动该文件来「修绿」—— 两个计划同 wave 写同一文件会互相覆盖。本计划的失败门（`node --check` / `tests/test-skill-picker-model.js` / `tests/test-ai-cancel-state.js`）全部由自有权文件承担，`tests/test-ai-skills.js` 是**附加**回归信号。
- **诚实边界**：用户气泡 DOM 时机（`.ai-skill-pill` / `.ai-skill-content-box` 在发送后立即出现）与「流式中调用技能不被写入取消文案」这两条**运行时**行为，node:test 无法断言（无 DOM / 无事件流宿主）—— 本计划的 automated 只覆盖源码契约与纯逻辑判定；DOM 与端到端面的最终证据是重跑 `/gsd-verify-work 48` 的自动驱动探针（UAT test 4 / test 6 clause 1 / clause 4），须在 SUMMARY 明确登记
- 指纹提示：本计划改 `src/renderer.js`（在 `48-VERIFICATION.md` 的 `covered_files` 内）→ 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）；不得改其它阶段的文件
</verification>

<success_criteria>
- 发送技能调用后无需任何额外交互即见 pill 与折叠块；重发 / 重试路径不留旧正文
- 流式中（含卡在工具确认卡片）调用技能：新调用发出并回显，不出现「用户已取消」错归属、不出现 15s 零增长、停止按钮不提前回退
- 停止按钮语义、`aiCancelledByUser` 的既有用途与两处对话切换复位均无回归
- 用户气泡构建只有一份实现；取消归属判定可被纯 Node 表驱动断言
</success_criteria>

<output>
Create `.planning/phases/48-skill-name/48-05-SUMMARY.md` when done
</output>
