---
phase: 05-容器属性扩展
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - container-manager.js
  - ipc-handlers.js
  - src/index.html
  - src/renderer.js
  - src/styles/main.css
autonomous: true
requirements:
  - ATTR-01
  - ATTR-02
  - ATTR-03
  - ATTR-04
  - ATTR-05

must_haves:
  truths:
    - 用户可以在编辑容器 Modal 中为容器设置手机号、邮箱、备注
    - 容器属性在创建和编辑容器时均可填写和修改
    - 旧版本容器数据升级后自动填充缺失字段的默认值，不会崩溃
    - 扩展属性为空时不触发验证错误
    - 备注字段支持多行输入，最大 500 字符
  artifacts:
    - container-manager.js 中 DEFAULT_CONTAINERS 包含 phone/email/notes 字段
    - container-manager.js 中 getContainers()/getContainer() 包含惰性填充逻辑
    - container-manager.js 中 createContainer()/updateContainer() 处理新属性
    - src/index.html 中 containerModal 包含邮箱、手机号、备注表单字段
    - src/renderer.js 中表单提交收集并验证新属性
    - src/styles/main.css 中包含 textarea 样式
  key_links:
    - renderer.js 表单提交 -> realmAPI.createContainer/updateContainer -> container-manager.js 持久化
    - container-manager.js 惰性填充 -> getContainers() -> renderer.js 编辑 Modal 填充
---

<objective>
扩展容器数据模型，新增手机号（phone）、邮箱（email）、备注（notes）三个可选属性。

Purpose: 让用户可以为容器关联更多元数据，提升多容器管理的实用性。
Output: 修改后的 container-manager.js、ipc-handlers.js、src/index.html、src/renderer.js、src/styles/main.css。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-容器属性扩展/05-CONTEXT.md
@.planning/phases/05-容器属性扩展/05-RESEARCH.md
@.planning/phases/05-容器属性扩展/05-UI-SPEC.md
@container-manager.js
@src/renderer.js
@src/index.html
@src/styles/main.css
@ipc-handlers.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 主进程数据模型扩展 + 惰性填充 + IPC 验证</name>
  <files>container-manager.js, ipc-handlers.js</files>
  <read_first>
    - container-manager.js（当前 DEFAULT_CONTAINERS 定义、getContainers/getContainer/createContainer/updateContainer 函数签名和实现）
    - ipc-handlers.js（validateContainerConfig 和 validateContainerUpdates 函数）
    - .planning/phases/05-容器属性扩展/05-CONTEXT.md（D-01 到 D-11 决策）
  </read_first>
  <behavior>
    - Test: getContainers() 返回的对象包含 phone/email/notes 字段（旧数据缺失时为空字符串）
    - Test: createContainer({ name: 'test', phone: '13800138000', email: 'a@b.com', notes: 'hello' }) 返回的对象包含这些字段
    - Test: updateContainer(id, { phone: '13900139000' }) 正确更新 phone 字段
    - Test: 从 electron-store 读取的旧容器（只有 id/name/color/icon）经 getContainers() 返回时 phone/email/notes 为空字符串
  </behavior>
  <action>
    **container-manager.js 修改（per D-09, D-10, D-11）：**

    1. DEFAULT_CONTAINERS 数组中每个容器对象新增 `phone: '', email: '', notes: ''` 字段

    2. getContainers() 函数：返回对象新增 `phone: c.phone || '', email: c.email || '', notes: c.notes || ''`（惰性填充，per D-09）

    3. getContainer(id) 函数：当前直接返回 containers.get(id) 包含 session 对象。不需要修改此函数（渲染进程不直接调用它获取扩展属性，扩展属性通过 getContainers() 获取）

    4. createContainer() 函数：
       - 参数解构新增 `phone = '', email = '', notes = ''`
       - container 对象新增 `phone: phone || '', email: email || '', notes: notes || ''`

    5. updateContainer() 函数：
       - 参数解构新增 `phone, email, notes`
       - 新增三个条件更新：`if (phone !== undefined) container.phone = phone;` 等
       - 持久化时 savedContainers[index] 新增 `phone: container.phone, email: container.email, notes: container.notes`
       - 返回值对象新增这三个字段

    **ipc-handlers.js 修改（per D-04）：**

    6. validateContainerConfig() 函数：新增对 config.phone、config.email、config.notes 的类型校验（允许 undefined 或 string，不为空时长度限制：phone <= 20, email <= 100, notes <= 500）

    7. validateContainerUpdates() 函数：同上，校验 updates.phone、updates.email、updates.notes

    注意：ipc-handlers.js 中现有的 container:list 返回值会自动包含新字段（因为它调用 containerManager.getContainers()）。
  </action>
  <verify>
    <automated>cd ~/Projects/Realm && node -e "const cm = require('./container-manager'); cm.initContainers(); const cs = cm.getContainers(); console.log('fields:', Object.keys(cs[0]).join(',')); console.log('phone:', cs[0].phone); console.log('email:', cs[0].email); console.log('notes:', cs[0].notes);"</automated>
  </verify>
  <acceptance_criteria>
    - container-manager.js 中 DEFAULT_CONTAINERS 每个对象包含 phone, email, notes 字段且值为空字符串
    - getContainers() 返回的每个对象包含 phone, email, notes 字段
    - createContainer({ name: 'test-phone' }) 返回的对象包含 phone: '', email: '', notes: ''
    - 旧版 electron-store 数据（无 phone/email/notes）经 getContainers() 返回时不会出现 undefined
    - ipc-handlers.js 中 validateContainerConfig 和 validateContainerUpdates 允许 phone/email/notes 为 undefined 或合法字符串
  </acceptance_criteria>
  <done>
    container-manager.js 数据模型已扩展，惰性填充已实现，createContainer/updateContainer 处理新属性，ipc-handlers.js 验证已更新
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 渲染进程表单 UI 扩展 + 验证逻辑 + textarea 样式</name>
  <files>src/index.html, src/renderer.js, src/styles/main.css</files>
  <read_first>
    - src/index.html（containerModal 的 dialog 结构，form-group 布局）
    - src/renderer.js（showCreateContainerModal、showEditContainerModal、containerForm submit handler、loadContainers）
    - src/styles/main.css（现有 .form-group input 样式、.error-message 样式、modal 样式）
    - .planning/phases/05-容器属性扩展/05-UI-SPEC.md（表单字段规格、布局规则、间距）
    - .planning/phases/05-容器属性扩展/05-CONTEXT.md（D-01 到 D-08）
  </read_first>
  <behavior>
    - Test: 打开编辑容器 Modal 时，邮箱、手机号、备注字段可见且为空
    - Test: 填写邮箱为 "test" 并提交，显示 "请输入正确的邮箱地址" 错误
    - Test: 填写手机号为 "123" 并提交，显示 "请输入11位手机号" 错误
    - Test: 填写正确邮箱和手机号后提交成功，数据通过 realmAPI 传递
    - Test: 备注字段支持多行输入，textarea 可见且高度约 80px
  </behavior>
  <action>
    **src/index.html 修改（per D-01, D-02, D-06）：**

    1. 在 containerModal 的 form 中，emoji-picker form-group 之后、form-actions 之前插入以下结构：
       - 一个 `<hr>` 分隔线（用 CSS class `form-divider` 控制样式）
       - 邮箱 form-group：`<input type="email" id="containerEmailInput" placeholder="例如：user@example.com" maxlength="100">` + `<div class="error-message" id="emailError"></div>`
       - 手机号 form-group：`<input type="tel" id="containerPhoneInput" placeholder="例如：13800138000" maxlength="11">` + `<div class="error-message" id="phoneError"></div>`
       - 备注 form-group：`<textarea id="containerNotesInput" placeholder="可选填写备注信息..." maxlength="500" rows="4"></textarea>` + `<div class="error-message" id="notesError"></div>`

    **src/renderer.js 修改（per D-03, D-04, D-05, D-07, D-08）：**

    2. elements 对象新增：
       - `containerEmailInput: document.getElementById('containerEmailInput')`
       - `containerPhoneInput: document.getElementById('containerPhoneInput')`
       - `containerNotesInput: document.getElementById('containerNotesInput')`
       - `emailError: document.getElementById('emailError')`
       - `phoneError: document.getElementById('phoneError')`
       - `notesError: document.getElementById('notesError')`

    3. showCreateContainerModal() 函数：新增重置三个字段值为空字符串，清除三个 error 的 visible class

    4. showEditContainerModal(containerId) 函数：新增填充三个字段值 `container.email || ''`、`container.phone || ''`、`container.notes || ''`，清除三个 error 的 visible class

    5. containerForm submit handler 中，在名称重复验证之后、try 块之前新增验证逻辑：
       - 获取 email/phone/notes 值（trim）
       - 邮箱验证（per D-04, D-05）：如果 email 非空且不包含 '@'，显示 emailError "请输入正确的邮箱地址" 并 return
       - 手机号验证（per D-04, D-05）：如果 phone 非空且不匹配 `/^\d{11}$/`，显示 phoneError "请输入11位手机号" 并 return
       - 备注验证（per D-07）：如果 notes 长度 > 500，显示 notesError "备注不能超过500字" 并 return
       - 空值时不显示任何错误（per D-05）

    6. 容器创建和更新调用中，formData 对象新增 `phone, email, notes` 字段

    **src/styles/main.css 修改：**

    7. 新增 textarea 样式：
       - `textarea` 选择器：继承 input 的背景色、边框、颜色、圆角、padding，resize: vertical，min-height: 80px，font-family: inherit
       - `.form-divider`：border: none, border-top: 1px solid var(--border-color, #3a3a3a), margin: 16px 0
  </action>
  <verify>
    <automated>cd ~/Projects/Realm && grep -c 'containerEmailInput' src/index.html src/renderer.js && grep -c 'containerPhoneInput' src/index.html src/renderer.js && grep -c 'containerNotesInput' src/index.html src/renderer.js && grep -c 'textarea' src/styles/main.css</automated>
  </verify>
  <acceptance_criteria>
    - src/index.html 中 containerModal 的 form 包含 id 为 containerEmailInput、containerPhoneInput、containerNotesInput 的输入元素
    - src/renderer.js 中 elements 对象包含这 6 个新 DOM 引用（3 个 input + 3 个 error）
    - src/renderer.js 中 showEditContainerModal 填充 email/phone/notes 字段值
    - src/renderer.js 中表单提交时验证邮箱 @ 格式、手机号 11 位数字、备注 500 字符限制
    - src/renderer.js 中 createContainer 和 updateContainer 调用包含 phone/email/notes 数据
    - src/styles/main.css 包含 textarea 和 .form-divider 样式规则
  </acceptance_criteria>
  <done>
    编辑容器 Modal 新增邮箱、手机号、备注字段，表单验证逻辑已实现，textarea 样式已添加
  </done>
</task>

</tasks>

<verification>
1. 启动应用 `npm run dev`，打开任意容器的编辑 Modal，确认邮箱、手机号、备注字段可见
2. 不填写任何扩展属性，直接保存，确认无验证错误
3. 填写邮箱为 "invalid" 并提交，确认显示 "请输入正确的邮箱地址"
4. 填写手机号为 "123" 并提交，确认显示 "请输入11位手机号"
5. 填写正确数据后保存，重新打开编辑 Modal，确认数据已持久化
6. 清空所有扩展属性后保存，确认可以正常清空
</verification>

<success_criteria>
- 用户可以在编辑容器 Modal 中为容器设置手机号、邮箱、备注
- 容器属性在创建和编辑容器时均可填写和修改
- 旧版本容器数据升级后自动填充缺失字段的默认值，不会崩溃
- 扩展属性为空时不触发验证错误
- 备注字段支持多行输入，最大 500 字符
</success_criteria>

<output>
创建 `.planning/phases/05-容器属性扩展/05-01-SUMMARY.md` when done
</output>
