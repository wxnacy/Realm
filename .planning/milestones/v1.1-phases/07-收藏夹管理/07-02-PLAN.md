---
phase: 07-收藏夹管理
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/index.html
  - src/renderer.js
  - src/favorites.html
  - src/favorites-page.js
  - src/styles/main.css
autonomous: true
requirements:
  - FAV-01
  - FAV-02
  - FAV-03
  - FAV-04
  - FAV-05
  - FAV-06
must_haves:
  truths:
    - 用户点击星标按钮可以收藏当前页面
    - 已收藏页面星标显示为金色实心星标
    - 用户点击收藏夹按钮可以打开 realm://favorites 收藏列表页面
    - 收藏列表页面显示当前容器的所有收藏
    - 用户可以搜索收藏（标题+URL匹配，高亮显示）
    - 用户可以编辑收藏项的标题（行内编辑）
    - 用户可以删除收藏项（复选框+批量删除）
    - 空状态显示提示文字
  artifacts:
    - 星标按钮（src/index.html）
    - 收藏夹按钮（src/index.html）
    - 收藏编辑面板（src/index.html）
    - realm://favorites 收藏列表页面（src/favorites.html）
    - 收藏页面逻辑（src/favorites-page.js）
    - 收藏相关 CSS 样式（src/styles/main.css）
  key_links:
    - 星标按钮 → renderer.js → favorites:check IPC → 检查收藏状态
    - 星标按钮点击 → renderer.js → favorites:add IPC → 添加收藏
    - 收藏夹按钮 → renderer.js → createTab('realm://favorites') → 打开收藏页面
    - favorites-page.js → /api/favorites/* HTTP API → favorites-manager.js
---

<objective>
创建收藏夹管理的完整前端 UI：工具栏星标按钮（收藏/取消收藏当前页面）、收藏编辑面板（首次收藏时弹出）、收藏夹按钮（打开收藏列表页面）、收藏列表页面（搜索、编辑标题、批量删除）。

Purpose: 为用户提供完整的收藏夹管理界面，实现收藏、查看、搜索、编辑、删除功能
Output: 完整的收藏夹 UI（星标按钮 + 收藏编辑面板 + 收藏列表页面 + CSS 样式）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-收藏夹管理/07-CONTEXT.md
@.planning/phases/07-收藏夹管理/07-RESEARCH.md
@.planning/phases/07-收藏夹管理/07-UI-SPEC.md
@src/index.html
@src/renderer.js
@src/history.html
@src/history-page.js
@src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加工具栏按钮和收藏编辑面板</name>
  <files>src/index.html, src/renderer.js</files>
  <read_first>
    - src/index.html（toolbar-right 区域现有按钮结构）
    - src/renderer.js（historyBtn 事件处理、elements 定义、setupEventListeners 函数、realmUrlToHttp 函数）
    - .planning/phases/07-收藏夹管理/07-CONTEXT.md（D-01: 星标图标, D-02: 编辑面板, D-03: 一键取消, D-04: 按钮位置）
    - .planning/phases/07-收藏夹管理/07-UI-SPEC.md（Bookmark star button, Bookmark edit panel 规范）
  </read_first>
  <action>
修改 src/index.html 和 src/renderer.js，添加星标按钮、收藏夹按钮和收藏编辑面板。

**src/index.html 修改：**

1. 在 toolbar-right 区域，historyBtn 按钮之前添加星标按钮：
```html
<button class="btn-icon" id="bookmarkStarBtn" title="收藏">
  <svg class="star-outline" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
  </svg>
  <svg class="star-filled" width="18" height="18" viewBox="0 0 24 24" fill="#FBBF24" stroke="#FBBF24" stroke-width="2" style="display:none">
    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
  </svg>
</button>
```

2. 在 historyBtn 按钮之后添加收藏夹按钮：
```html
<button class="btn-icon" id="favoritesBtn" title="收藏夹">
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path>
  </svg>
</button>
```

3. 在 body 结束前，toast div 之后添加收藏编辑面板：
```html
<!-- 收藏编辑面板 -->
<div id="bookmarkEditPanel" class="bookmark-edit-panel" style="display:none">
  <div class="bookmark-edit-header">收藏此页面</div>
  <div class="bookmark-edit-field">
    <label for="bookmarkTitleInput">标题</label>
    <input type="text" id="bookmarkTitleInput" class="bookmark-edit-input" placeholder="页面标题">
  </div>
  <div class="bookmark-edit-field">
    <label>网址</label>
    <div id="bookmarkUrlDisplay" class="bookmark-edit-url"></div>
  </div>
  <div class="bookmark-edit-actions">
    <button type="button" class="btn btn-secondary" id="bookmarkCancelBtn">取消</button>
    <button type="button" class="btn btn-primary" id="bookmarkSaveBtn">保存</button>
  </div>
</div>
```

**src/renderer.js 修改：**

1. 在 elements 对象中添加新元素引用：
```javascript
bookmarkStarBtn: document.getElementById('bookmarkStarBtn'),
favoritesBtn: document.getElementById('favoritesBtn'),
bookmarkEditPanel: document.getElementById('bookmarkEditPanel'),
bookmarkTitleInput: document.getElementById('bookmarkTitleInput'),
bookmarkUrlDisplay: document.getElementById('bookmarkUrlDisplay'),
bookmarkCancelBtn: document.getElementById('bookmarkCancelBtn'),
bookmarkSaveBtn: document.getElementById('bookmarkSaveBtn'),
```

2. 在 state 对象中添加收藏状态：
```javascript
isCurrentPageBookmarked: false,
currentBookmarkId: null,
```

3. 添加收藏相关函数：

```javascript
/**
 * 检查当前页面是否已收藏，更新星标状态
 * @param {string} url - 当前页面 URL
 * @param {string} containerId - 当前容器 ID
 */
async function checkBookmarkStatus(url, containerId) {
  if (!url || url.startsWith('realm://') || url === 'about:blank') {
    // 内部页面不显示收藏状态
    updateStarButton(false);
    return;
  }
  try {
    const result = await window.realmAPI.favoritesCheck(containerId, url);
    state.isCurrentPageBookmarked = !!result;
    state.currentBookmarkId = result ? result.id : null;
    updateStarButton(!!result);
  } catch (err) {
    console.error('[Realm Renderer] 检查收藏状态失败:', err);
  }
}

/**
 * 更新星标按钮显示状态
 * @param {boolean} bookmarked - 是否已收藏
 */
function updateStarButton(bookmarked) {
  const outline = elements.bookmarkStarBtn.querySelector('.star-outline');
  const filled = elements.bookmarkStarBtn.querySelector('.star-filled');
  if (bookmarked) {
    outline.style.display = 'none';
    filled.style.display = 'block';
    elements.bookmarkStarBtn.title = '取消收藏';
  } else {
    outline.style.display = 'block';
    filled.style.display = 'none';
    elements.bookmarkStarBtn.title = '收藏';
  }
}

/**
 * 显示收藏编辑面板
 * @param {string} title - 页面标题
 * @param {string} url - 页面 URL
 */
function showBookmarkEditPanel(title, url) {
  elements.bookmarkTitleInput.value = title || '';
  elements.bookmarkUrlDisplay.textContent = url || '';
  elements.bookmarkEditPanel.style.display = 'block';
  elements.bookmarkTitleInput.focus();
  elements.bookmarkTitleInput.select();
}

/**
 * 隐藏收藏编辑面板
 */
function hideBookmarkEditPanel() {
  elements.bookmarkEditPanel.style.display = 'none';
}

/**
 * 保存收藏
 */
async function saveBookmark() {
  const title = elements.bookmarkTitleInput.value.trim();
  const url = elements.bookmarkUrlDisplay.textContent;
  const containerId = state.currentContainer;

  if (!url) return;

  try {
    const result = await window.realmAPI.favoritesAdd({
      containerId,
      url,
      title,
      faviconUrl: '' // favicon 暂不获取
    });

    if (result.error === 'duplicate') {
      showToast('已收藏过该页面', 'error');
    } else {
      state.isCurrentPageBookmarked = true;
      state.currentBookmarkId = result.id;
      updateStarButton(true);
      showToast('已收藏');
    }
  } catch (err) {
    console.error('[Realm Renderer] 收藏失败:', err);
    showToast('收藏失败，请重试', 'error');
  }

  hideBookmarkEditPanel();
}

/**
 * 取消收藏当前页面
 */
async function removeBookmark() {
  const containerId = state.currentContainer;
  const bookmarkId = state.currentBookmarkId;

  if (!bookmarkId) return;

  try {
    await window.realmAPI.favoritesDelete(containerId, bookmarkId);
    state.isCurrentPageBookmarked = false;
    state.currentBookmarkId = null;
    updateStarButton(false);
    showToast('已取消收藏');
  } catch (err) {
    console.error('[Realm Renderer] 取消收藏失败:', err);
  }
}
```

4. 在 setupEventListeners 函数中添加事件监听：

```javascript
// 星标按钮：收藏/取消收藏当前页面
elements.bookmarkStarBtn.addEventListener('click', () => {
  if (state.isCurrentPageBookmarked) {
    // D-03: 已收藏直接取消收藏
    removeBookmark();
  } else {
    // D-02: 未收藏弹出编辑面板
    const activeTab = state.tabs.get(state.activeTabId);
    if (activeTab && activeTab.url) {
      showBookmarkEditPanel(activeTab.title || activeTab.url, activeTab.url);
    }
  }
});

// 收藏编辑面板保存按钮
elements.bookmarkSaveBtn.addEventListener('click', saveBookmark);

// 收藏编辑面板取消按钮
elements.bookmarkCancelBtn.addEventListener('click', hideBookmarkEditPanel);

// 点击编辑面板外部关闭
document.addEventListener('click', (e) => {
  if (elements.bookmarkEditPanel.style.display === 'block' &&
      !elements.bookmarkEditPanel.contains(e.target) &&
      e.target !== elements.bookmarkStarBtn) {
    hideBookmarkEditPanel();
  }
});

// Escape 键关闭编辑面板
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape' && elements.bookmarkEditPanel.style.display === 'block') {
    hideBookmarkEditPanel();
  }
});

// 收藏夹按钮：打开 realm://favorites 收藏列表页面
elements.favoritesBtn.addEventListener('click', () => {
  const containerId = state.currentContainer;

  // 检查当前容器是否已有 realm://favorites 的 Tab 打开
  let existingTabId = null;
  state.tabs.forEach((tab, tabId) => {
    if (tab.url === 'realm://favorites' && tab.containerId === containerId) {
      existingTabId = tabId;
    }
  });

  if (existingTabId) {
    switchTab(existingTabId);
  } else {
    createTab(containerId, 'realm://favorites');
  }
});
```

5. 在 Tab 切换和页面导航时更新收藏状态：
   - 在 switchTab 函数中，切换 Tab 后调用 checkBookmarkStatus
   - 在 webview 的 did-navigate 事件中，导航完成后调用 checkBookmarkStatus
   - 在容器切换事件中，重新检查当前页面收藏状态
  </action>
  <verify>
    <automated>grep -c "bookmarkStarBtn\|favoritesBtn\|bookmarkEditPanel" src/index.html src/renderer.js | paste -sd ',' - | grep -v '^0,0$'</automated>
  </verify>
  <acceptance_criteria>
    - src/index.html 中存在 id="bookmarkStarBtn" 的星标按钮
    - src/index.html 中存在 id="favoritesBtn" 的收藏夹按钮
    - src/index.html 中存在 id="bookmarkEditPanel" 的收藏编辑面板
    - src/renderer.js 中存在 checkBookmarkStatus、updateStarButton、showBookmarkEditPanel、hideBookmarkEditPanel、saveBookmark、removeBookmark 函数
    - 星标按钮点击时，未收藏页面弹出编辑面板，已收藏页面直接取消收藏
    - 收藏夹按钮点击时，打开 realm://favorites 收藏列表页面
  </acceptance_criteria>
  <done>工具栏星标按钮和收藏夹按钮添加完成，收藏编辑面板实现完成</done>
</task>

<task type="auto">
  <name>Task 2: 创建收藏列表页面</name>
  <files>src/favorites.html, src/favorites-page.js, src/styles/main.css</files>
  <read_first>
    - src/history.html（参考结构和布局）
    - src/history-page.js（参考 JavaScript 模式：API 调用、DOM 渲染、事件处理、搜索、批量删除）
    - src/styles/main.css（现有样式，添加收藏相关样式）
    - .planning/phases/07-收藏夹管理/07-CONTEXT.md（D-06: 紧凑列表, D-07: 时间倒序, D-08: 空状态, D-09: 搜索, D-10: 高亮, D-11: 行内编辑, D-12: 批量删除）
    - .planning/phases/07-收藏夹管理/07-UI-SPEC.md（Layout specs, Copywriting, Interaction patterns）
  </read_first>
  <action>
创建 src/favorites.html 和 src/favorites-page.js，参考 history.html 和 history-page.js 的结构和模式。在 src/styles/main.css 中添加收藏相关样式。

**src/favorites.html 创建：**
参考 src/history.html，修改以下内容：
1. title: "收藏夹 - Realm"
2. base href: "/favorites/"
3. 页面标题: "收藏夹"（h1 class="favorites-title"）
4. 无"清空所有历史"按钮（收藏不需要清空全部功能）
5. 搜索框 placeholder: "搜索收藏..."
6. 批量操作栏：与 history.html 相同结构
7. 主内容区域: id="favoritesContent"
8. 批量删除确认弹窗: id="favoritesBatchDeleteModal"
9. Toast: id="toast"
10. script src: "favorites-page.js"

**src/favorites-page.js 创建：**
参考 src/history-page.js，修改以下内容：

1. 文件头注释：说明是 realm://favorites 收藏列表页面
2. state 对象：
   - containerId: 从 URL 参数获取
   - records: 收藏记录数组
   - keyword: 搜索关键词
   - offset/limit: 分页参数
   - loading/hasMore: 加载状态
   - selectedIds: 已选中记录 ID 集合
   - selectAll: 全选状态
3. favoritesApi 函数：调用 /api/favorites/* 端点
4. elements 对象：引用 favorites.html 中的 DOM 元素
5. 渲染函数：
   - renderFavoritesList(): 渲染收藏列表（按 created_at DESC 排序，D-07）
   - renderFavoriteItem(record): 渲染单条收藏项
     - 复选框 + favicon + 标题（可点击编辑，D-11）+ URL + 收藏时间
     - 标题点击进入行内编辑模式
   - renderEmptyState(): 渲染空状态（D-08："暂无收藏，点击工具栏星标按钮收藏当前页面"）
   - renderSearchEmpty(): 渲染搜索无结果状态（"没有找到匹配的收藏"）
6. 搜索功能：
   - debounce 300ms（D-09）
   - 同时匹配 title 和 url
   - 搜索结果高亮匹配文本（D-10，使用 .favorites-highlight 样式）
7. 行内编辑标题（D-11）：
   - 点击标题文本，替换为 input 元素
   - Enter 或点击其他地方保存
   - Escape 取消编辑
   - 保存时调用 /api/favorites/update
8. 批量删除（D-12）：
   - 复选框选择
   - 全选/取消全选
   - 删除选中项（弹出确认对话框）
   - 确认后调用 /api/favorites/delete-batch
9. 滚动加载：与 history-page.js 相同模式

**src/styles/main.css 添加收藏相关样式：**

在 history 相关样式之后添加 favorites 相关样式：

```css
/* ==================== 收藏夹页面样式 ==================== */

.favorites-page {
  max-width: 800px;
  margin: 0 auto;
  padding: 24px;
  flex: 1;
  overflow-y: auto;
}

.favorites-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
}

.favorites-title {
  font-size: 24px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
}

/* 搜索栏 */
.favorites-search-wrapper {
  position: relative;
  margin-bottom: 16px;
}

.favorites-search-icon {
  position: absolute;
  left: 12px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-muted);
  pointer-events: none;
}

.favorites-search {
  width: 100%;
  padding: 10px 12px 10px 36px;
  background-color: var(--bg-tertiary);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  color: var(--text-primary);
  font-size: 14px;
  outline: none;
  transition: border-color 0.2s;
}

.favorites-search:focus {
  border-color: var(--accent-color);
}

/* 批量操作栏 */
.favorites-actions-bar {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 12px;
  background-color: var(--bg-secondary);
  border-radius: 8px;
  margin-bottom: 12px;
}

.favorites-actions-bar.hidden {
  display: none;
}

/* 收藏项 */
.favorite-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border-radius: 8px;
  cursor: default;
  transition: background-color 0.15s;
}

.favorite-item:hover {
  background-color: var(--bg-hover);
}

.favorite-item-checkbox {
  flex-shrink: 0;
  cursor: pointer;
}

.favorite-item-favicon {
  flex-shrink: 0;
  width: 20px;
  height: 20px;
  border-radius: 4px;
  background-color: var(--bg-tertiary);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  color: var(--text-muted);
}

.favorite-item-favicon img {
  width: 16px;
  height: 16px;
  border-radius: 2px;
}

.favorite-item-content {
  flex: 1;
  min-width: 0;
}

.favorite-item-title {
  font-size: 14px;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  cursor: pointer;
}

.favorite-item-title:hover {
  color: var(--accent-color);
}

.favorite-item-url {
  font-size: 13px;
  color: var(--text-secondary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.favorite-item-time {
  flex-shrink: 0;
  font-size: 12px;
  color: var(--text-muted);
  white-space: nowrap;
}

/* 行内编辑模式 */
.favorite-item-title-edit {
  font-size: 14px;
  color: var(--text-primary);
  background-color: var(--bg-tertiary);
  border: 1px solid var(--accent-color);
  border-radius: 4px;
  padding: 2px 6px;
  outline: none;
  width: 100%;
}

/* 搜索高亮 */
.favorites-highlight {
  background-color: rgba(59, 130, 246, 0.3);
  border-radius: 2px;
  padding: 0 2px;
}

/* 空状态 */
.favorites-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 64px 24px;
  color: var(--text-muted);
}

.favorites-empty-icon {
  font-size: 48px;
  margin-bottom: 16px;
}

.favorites-empty-text {
  font-size: 16px;
  margin-bottom: 8px;
}

.favorites-empty-hint {
  font-size: 14px;
}

/* 收藏编辑面板样式 */
.bookmark-edit-panel {
  position: fixed;
  top: calc(var(--toolbar-height, 48px) + 4px);
  right: 12px;
  width: 320px;
  background-color: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: 12px;
  padding: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  z-index: 1001;
}

.bookmark-edit-header {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 16px;
}

.bookmark-edit-field {
  margin-bottom: 12px;
}

.bookmark-edit-field label {
  display: block;
  font-size: 14px;
  font-weight: 500;
  color: var(--text-secondary);
  margin-bottom: 4px;
}

.bookmark-edit-input {
  width: 100%;
  padding: 8px 12px;
  background-color: var(--bg-tertiary);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  color: var(--text-primary);
  font-size: 14px;
  outline: none;
  transition: border-color 0.2s;
}

.bookmark-edit-input:focus {
  border-color: var(--accent-color);
}

.bookmark-edit-url {
  font-size: 13px;
  color: var(--text-muted);
  word-break: break-all;
}

.bookmark-edit-actions {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  margin-top: 16px;
}
```
  </action>
  <verify>
    <automated>test -f src/favorites.html && test -f src/favorites-page.js && grep -c "favorites-page\|favorites-search\|favorite-item\|bookmark-edit" src/styles/main.css</automated>
  </verify>
  <acceptance_criteria>
    - src/favorites.html 文件存在且结构正确
    - src/favorites-page.js 文件存在且包含完整的收藏列表逻辑
    - src/styles/main.css 中存在 .favorites-page、.favorite-item、.bookmark-edit-panel 等样式
    - 收藏列表按 created_at DESC 排序（D-07）
    - 搜索同时匹配 title 和 url，debounce 300ms（D-09）
    - 搜索结果高亮匹配文本（D-10）
    - 标题支持行内编辑（D-11）
    - 支持复选框 + 批量删除（D-12）
    - 空状态显示"暂无收藏，点击工具栏星标按钮收藏当前页面"（D-08）
  </acceptance_criteria>
  <done>收藏列表页面创建完成，支持搜索、行内编辑、批量删除功能</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview guest → HTTP API | favorites-page.js 通过 /api/favorites/* 访问数据 |
| renderer → IPC | renderer.js 通过 favorites:* IPC 通道访问数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure | XSS 注入（收藏标题/URL） | high | mitigate | 使用 textContent 渲染用户数据，搜索高亮使用 escapeHtml（复用 history-page.js 模式） |
| T-07-05 | Tampering | 行内编辑输入验证 | medium | mitigate | 服务端验证标题非空，客户端限制输入长度 |
</threat_model>

<verification>
1. 星标按钮可以收藏当前页面（FAV-01）
2. 已收藏页面星标显示为金色实心（FAV-01）
3. 再次点击星标可以取消收藏（FAV-02）
4. 收藏夹按钮打开 realm://favorites 页面（FAV-03）
5. 收藏列表页面显示当前容器的收藏（FAV-03）
6. 可以编辑收藏项标题（FAV-04）
7. 可以删除收藏项（FAV-05）
8. 可以搜索收藏（FAV-06）
</verification>

<success_criteria>
- 用户可以点击星标按钮收藏当前页面（FAV-01）
- 用户可以取消收藏已收藏页面（FAV-02）
- 用户可以查看收藏列表（FAV-03）
- 用户可以编辑收藏项的标题（FAV-04）
- 用户可以删除收藏项（FAV-05）
- 用户可以搜索收藏（FAV-06）
</success_criteria>

<output>
创建 `.planning/phases/07-收藏夹管理/07-02-SUMMARY.md` 当任务完成时
</output>
