---
phase: 09-共享收藏数据库
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/preload.js
  - src/renderer.js
  - src/favorites-page.js
autonomous: true
requirements:
  - FAV-07
  - FAV-09
  - FAV-10

must_haves:
  truths:
    - preload.js 中收藏相关 IPC 接口不再传递 containerId 参数
    - renderer.js 中 checkBookmarkStatus() 不再接收 containerId 参数，收藏按钮状态检测与容器无关
    - renderer.js 中 saveBookmark() 和 removeBookmark() 不再使用 state.currentContainer
    - favorites-page.js 中 state.containerId 已移除，所有 API 调用不再传递 containerId
    - realmUrlToHttp() 中 favorites 相关调用不再传递 containerId
  artifacts:
    - src/preload.js（IPC 接口移除 containerId）
    - src/renderer.js（收藏按钮逻辑简化）
    - src/favorites-page.js（移除容器感知）
  key_links:
    - renderer.js 调用 window.realmAPI.favoritesCheck(url) 不再传 containerId
    - renderer.js 调用 window.realmAPI.favoritesAdd({url, title, faviconUrl}) 不再传 containerId
    - favorites-page.js API 调用不再传 containerId 查询参数
---

<objective>
更新 preload.js、renderer.js 和 favorites-page.js，移除所有收藏相关的 containerId 参数传递，使前端与全局共享收藏表对齐。

Purpose: 前端层与重构后的后端 API 对齐，所有收藏操作不再依赖容器 ID。
Output: 三个前端文件移除 containerId 依赖，收藏功能跨容器共享。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-共享收藏数据库/09-CONTEXT.md
@.planning/phases/09-共享收藏数据库/09-RESEARCH.md
@.planning/phases/09-共享收藏数据库/09-01-SUMMARY.md
@src/preload.js
@src/renderer.js
@src/favorites-page.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新 preload.js — 收藏 IPC 接口移除 containerId 参数</name>
  <files>src/preload.js</files>
  <behavior>
    - Test: favoritesCheck(url) 调用 ipcRenderer.invoke('favorites:check', { url }) 无 containerId
    - Test: favoritesAdd({ url, title, faviconUrl }) 调用 ipcRenderer.invoke('favorites:add', data) 无 containerId
    - Test: favoritesUpdate(id, title) 调用 ipcRenderer.invoke('favorites:update', { id, title }) 无 containerId
    - Test: favoritesDelete(id) 调用 ipcRenderer.invoke('favorites:delete', { id }) 无 containerId
    - Test: favoritesDeleteBatch(ids) 调用 ipcRenderer.invoke('favorites:delete-batch', { ids }) 无 containerId
    - Test: favoritesList({ offset, limit }) 调用 ipcRenderer.invoke('favorites:list', data) 无 containerId
    - Test: favoritesSearch({ keyword, offset, limit }) 调用 ipcRenderer.invoke('favorites:search', data) 无 containerId
    - Test: favoritesCount() 调用 ipcRenderer.invoke('favorites:count', {}) 无 containerId
  </behavior>
  <action>
修改 src/preload.js 中收藏夹部分（行 333-405），per D-11：

1. **favoritesCheck**（行 341）：签名从 `(containerId, url)` 改为 `(url)`，调用从 `ipcRenderer.invoke('favorites:check', { containerId, url })` 改为 `ipcRenderer.invoke('favorites:check', { url })`。更新 JSDoc：移除 @param containerId。

2. **favoritesAdd**（行 352）：调用不变（data 对象透传），但更新 JSDoc：移除 data.containerId 参数说明。

3. **favoritesUpdate**（行 361）：签名从 `(containerId, id, title)` 改为 `(id, title)`，调用从 `ipcRenderer.invoke('favorites:update', { containerId, id, title })` 改为 `ipcRenderer.invoke('favorites:update', { id, title })`。更新 JSDoc。

4. **favoritesDelete**（行 369）：签名从 `(containerId, id)` 改为 `(id)`，调用从 `ipcRenderer.invoke('favorites:delete', { containerId, id })` 改为 `ipcRenderer.invoke('favorites:delete', { id })`。更新 JSDoc。

5. **favoritesDeleteBatch**（行 377）：签名从 `(containerId, ids)` 改为 `(ids)`，调用从 `ipcRenderer.invoke('favorites:delete-batch', { containerId, ids })` 改为 `ipcRenderer.invoke('favorites:delete-batch', { ids })`。更新 JSDoc。

6. **favoritesList**（行 387）：调用不变（data 对象透传），但更新 JSDoc：移除 data.containerId 参数说明。

7. **favoritesSearch**（行 398）：调用不变（data 对象透传），但更新 JSDoc：移除 data.containerId 参数说明。

8. **favoritesCount**（行 405）：签名从 `(containerId)` 改为 `()`，调用从 `ipcRenderer.invoke('favorites:count', { containerId })` 改为 `ipcRenderer.invoke('favorites:count', {})`。更新 JSDoc。
  </action>
  <verify>
    <automated>cd ~/Projects/Realm && grep -n "containerId" src/preload.js | grep -i favor</automated>
  </verify>
  <done>preload.js 中所有收藏相关 IPC 接口不再包含 containerId 参数</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 renderer.js — 收藏按钮逻辑移除容器依赖</name>
  <files>src/renderer.js</files>
  <behavior>
    - Test: checkBookmarkStatus(url) 不再接收 containerId 参数
    - Test: checkBookmarkStatus 内部调用 window.realmAPI.favoritesCheck(url) 只传 url
    - Test: saveBookmark() 内部不再引用 state.currentContainer
    - Test: saveBookmark() 调用 favoritesUpdate(id, title) 只传 id 和 title
    - Test: saveBookmark() 调用 favoritesAdd({url, title, faviconUrl}) 无 containerId
    - Test: removeBookmark() 内部不再引用 state.currentContainer
    - Test: removeBookmark() 调用 favoritesDelete(bookmarkId) 只传 bookmarkId
    - Test: switchTab() 中调用 checkBookmarkStatus(tab.url) 只传 url
    - Test: bindWebviewEvents did-navigate 中调用 checkBookmarkStatus(displayUrl) 只传 url
    - Test: favoritesBtn click handler 不再检查 tab.containerId 匹配
  </behavior>
  <action>
修改 src/renderer.js 中的收藏相关函数，per D-12：

1. **checkBookmarkStatus()**（行 218-236）：
   - 函数签名从 `checkBookmarkStatus(url, containerId)` 改为 `checkBookmarkStatus(url)`
   - 移除 JSDoc 中的 @param containerId
   - 调用从 `window.realmAPI.favoritesCheck(containerId, url)` 改为 `window.realmAPI.favoritesCheck(url)`

2. **saveBookmark()**（行 297-345）：
   - 移除 `const containerId = state.currentContainer`（行 300）
   - 编辑模式调用从 `window.realmAPI.favoritesUpdate(containerId, state.currentBookmarkId, title)` 改为 `window.realmAPI.favoritesUpdate(state.currentBookmarkId, title)`
   - 新增模式调用从 `window.realmAPI.favoritesAdd({ containerId, url, title, faviconUrl })` 改为 `window.realmAPI.favoritesAdd({ url, title, faviconUrl })`

3. **removeBookmark()**（行 350-377）：
   - 移除 `const containerId = state.currentContainer`（行 351）
   - 调用从 `window.realmAPI.favoritesDelete(containerId, bookmarkId)` 改为 `window.realmAPI.favoritesDelete(bookmarkId)`

4. **switchTab() 中的调用**（行 476）：
   - 从 `checkBookmarkStatus(tab.url, tab.containerId)` 改为 `checkBookmarkStatus(tab.url)`

5. **bindWebviewEvents 中 did-navigate 的调用**（行 667-668）：
   - 从 `checkBookmarkStatus(displayUrl, tab.containerId)` 改为 `checkBookmarkStatus(displayUrl)`

6. **favoritesBtn click handler**（行 2305-2320）：
   - 移除 `tab.containerId === containerId` 的检查条件（行 2312），改为只检查 `tab.url === 'realm://favorites'`
   - 因为收藏页面现在是全局的，不需要按容器区分

7. **realmUrlToHttp() 调用处**（行 606）：
   - 检查 createTab 中对 realm://favorites 的调用，如果传了 containerId 参数，移除。收藏页面 URL 不再需要 ?container=xxx 参数。
  </action>
  <verify>
    <automated>cd ~/Projects/Realm && grep -n "containerId" src/renderer.js | grep -i "bookmark\|favor\|star" | head -10</automated>
  </verify>
  <done>renderer.js 中所有收藏相关函数不再依赖 containerId；checkBookmarkStatus 只接收 url 参数；saveBookmark 和 removeBookmark 不再使用 state.currentContainer</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 更新 favorites-page.js — 移除容器感知逻辑</name>
  <files>src/favorites-page.js</files>
  <behavior>
    - Test: state 对象中不再包含 containerId 属性
    - Test: loadFavorites() 调用 favoritesApi('list', {}, { offset, limit }) 无 containerId
    - Test: loadFavorites() 搜索时调用 favoritesApi('search', {}, { keyword, offset, limit }) 无 containerId
    - Test: loadMore() 调用 favoritesApi 时无 containerId 参数
    - Test: handleBatchDelete() body 中只包含 { ids }，无 containerId
    - Test: startInlineEdit() body 中只包含 { id, title }，无 containerId
  </behavior>
  <action>
修改 src/favorites-page.js，per D-10, D-11：

1. **移除 state.containerId**（行 22）：
   - 删除 `containerId: pageParams.get('container') || 'default'` 这一行
   - state 对象中不再有 containerId 属性

2. **loadFavorites()**（行 363-392）：
   - 搜索调用从 `favoritesApi('search', {}, { containerId: state.containerId, keyword: state.keyword, offset: 0, limit: state.limit })` 改为 `favoritesApi('search', {}, { keyword: state.keyword, offset: 0, limit: state.limit })`
   - 列表调用从 `favoritesApi('list', {}, { containerId: state.containerId, offset: 0, limit: state.limit })` 改为 `favoritesApi('list', {}, { offset: 0, limit: state.limit })`

3. **loadMore()**（行 397-432）：
   - 搜索调用移除 `containerId: state.containerId` 参数
   - 列表调用移除 `containerId: state.containerId` 参数

4. **handleBatchDelete()**（行 439-472）：
   - body 从 `JSON.stringify({ containerId: state.containerId, ids })` 改为 `JSON.stringify({ ids })`

5. **startInlineEdit()**（行 297-356）：
   - body 从 `JSON.stringify({ containerId: state.containerId, id: record.id, title: newTitle })` 改为 `JSON.stringify({ id: record.id, title: newTitle })`

6. **保留 pageParams 和 apiToken**：虽然 containerId 不再使用，但 pageParams 仍然用于读取 token，不要删除 pageParams 相关代码。
  </action>
  <verify>
    <automated>cd ~/Projects/Realm && grep -n "containerId" src/favorites-page.js</automated>
  </verify>
  <done>favorites-page.js 中 state.containerId 已移除；所有 API 调用不再传递 containerId 参数；pageParams 和 apiToken 保留</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → main | IPC 通信，contextBridge 隔离 |
| favorites-page → HTTP API | webview guest 通过 HTTP API 访问数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Information Disclosure | favorites-page URL 参数 | low | accept | 移除 container 参数后 URL 更简洁，token 参数仍保留鉴权 |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | 本阶段无新增依赖 |
</threat_model>

<verification>
## 整体验证

1. 启动应用，在任意容器中收藏一个页面
2. 切换到另一个容器，点击星标按钮，应显示已收藏状态（因为数据全局共享）
3. 打开收藏页面（realm://favorites），应能看到所有容器的收藏
4. 在收藏页面搜索、编辑标题、删除收藏，功能正常
5. 收藏同一 URL，应返回"已收藏过该页面"提示
6. 删除容器后，收藏数据仍然保留
</verification>

<success_criteria>
- preload.js 收藏接口无 containerId 参数
- renderer.js 收藏函数不依赖 state.currentContainer
- favorites-page.js 无 containerId 引用
- 收藏功能跨容器共享正常工作
</success_criteria>

<output>
Create `.planning/phases/09-共享收藏数据库/09-02-SUMMARY.md` when done
</output>
