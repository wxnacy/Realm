---
phase: 09-共享收藏数据库
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - ipc-handlers.js
  - scripts/test-ipc-favorites.js
autonomous: false
gap_closure: true
requirements:
  - FAV-09
  - FAV-10

must_haves:
  truths:
    - 渲染进程经 preload 发起的 8 个 favorites:* IPC 调用全部不再抛出参数错误（星标 check/add/update/delete 四链路恢复）
    - 每个收藏 handler 以 Phase 9 新签名调用 favoritesManager（无 containerId 位置参数，参数个数与 manager 函数形参一致）
    - 用户在容器 A 收藏的页面，切换容器后星标仍显示已收藏（跨容器共享真实可达）
    - history:* 等其他 IPC handler 的 containerId 校验保持不变（仅收藏夹区段被修改）
  artifacts:
    - ipc-handlers.js（收藏夹区段 8 个 handler 移除 containerId 验证与位置参数）
    - scripts/test-ipc-favorites.js（新增：IPC 运行时冒烟测试，无需 Electron/数据库即可回归）
  key_links:
    - preload.js favoritesXxx → ipcMain.handle('favorites:*') → favoritesManager 新签名（参数形状逐通道对齐）
    - renderer.js 星标按钮 → window.realmAPI.favorites* → IPC → 全局 favorites 表
---

<objective>
修复 09-VERIFICATION.md 报告的唯一 BLOCKER gap：ipc-handlers.js 中 8 个 favorites:* IPC handler 仍是 Phase 7 per-container 旧签名，与已更新的 preload.js（不再发送 containerId）和 favorites-manager.js（新签名不含 containerId）双重不兼容，导致星标按钮写入/查询链路 100% 抛出参数错误。

Purpose: 打通「星标按钮 → preload → IPC handler → favoritesManager → 全局 favorites 表」的完整链路，使 FAV-09（收藏全局共享）的写入路径与跨容器星标标记真正可用。
Output: 重构后的 ipc-handlers.js 收藏夹区段 + 一个可重复运行的 Node 冒烟测试（回应 VERIFICATION 流程备注：静态 grep 无法发现 IPC 运行时断裂，需运行时覆盖）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-共享收藏数据库/09-CONTEXT.md
@.planning/phases/09-共享收藏数据库/09-VERIFICATION.md
@ipc-handlers.js
@favorites-manager.js
@src/preload.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 ipc-handlers.js 收藏夹区段 — 8 个 favorites:* handler 移除 containerId（含运行时冒烟测试）</name>
  <files>ipc-handlers.js, scripts/test-ipc-favorites.js</files>
  <read_first>
    - ipc-handlers.js 行 729-868（收藏夹区段：8 个 handler 当前实现 + 上方 history:count 作为"不可触碰"对照）
    - ipc-handlers.js 行 1-34（模块依赖清单 + assertTrustedSender 实现，冒烟测试需要据此打桩）
    - favorites-manager.js 行 236-250（模块导出清单：8 个新签名函数）
    - src/preload.js 行 333-397（8 个 favorites* 接口实际发送的 payload 形状，handler 必须与之匹配）
  </read_first>
  <behavior>
    冒烟测试对 8 个通道逐一断言「不抛异常 + favoritesManager 被以正确签名调用」：
    - favorites:check 收到 { url } → 调用 checkUrl，恰好 1 个参数且值为该 url，返回 null
    - favorites:add 收到 { url, title, faviconUrl } → 调用 addRecord，恰好 1 个参数且为含 url/title/faviconUrl 三字段的对象，返回 { id: 1 }
    - favorites:update 收到 { id, title } → 调用 updateRecord，恰好 2 个参数（id、{ title } 对象），返回 true
    - favorites:delete 收到 { id } → 调用 deleteRecord，恰好 1 个参数且为该 id，返回 true
    - favorites:delete-batch 收到 { ids: [1, 2] } → 调用 deleteRecords，恰好 1 个参数且为该数组，返回 2
    - favorites:list 收到 { offset: 0, limit: 50 } → 调用 listRecords，恰好 1 个参数且为 { offset, limit } 对象，返回 []
    - favorites:search 收到 { keyword, offset, limit } → 调用 searchRecords，恰好 1 个参数且为 { keyword, offset, limit } 对象，返回 []
    - favorites:count 收到 {} → 调用 getCount，恰好 0 个参数，返回 0
    - 全部 8 通道：不抛出任何参数校验错误；调用参数个数断言精确匹配（专门捕捉"多传一个位置参数"的 Phase 7 旧签名残留）
  </behavior>
  <action>
<!-- planner-discipline-allow: containerId -->

本任务修复 VERIFICATION.md gaps.missing 列出的全部 10 项。严格按 TDD 顺序执行：先写冒烟测试并确认 RED，再改 handler 确认 GREEN。

**Step 1（RED）— 新建 scripts/test-ipc-favorites.js 冒烟测试：**

该测试在纯 Node 环境运行（不启动 Electron、不触数据库，规避 better-sqlite3 系统 Node ABI 不匹配问题），通过 Module._load 拦截对 ipc-handlers.js 的依赖打桩：

- electron 桩：ipcMain.handle 将 (channel, fn) 捕获到 handlers 字典；BrowserWindow.fromWebContents 返回 { id: 1 }；dialog 给空对象
- ./window-manager 桩：getMainWindow 返回 { id: 1 }（与 BrowserWindow 返回的 id 一致，使 assertTrustedSender 通过）
- ./favorites-manager 桩：8 个导出函数各自把 [函数名, ...实参] 记录进 calls 数组，并返回预设值（checkUrl→null、addRecord→{id:1}、updateRecord→true、deleteRecord→true、deleteRecords→ids.length、listRecords→[]、searchRecords→[]、getCount→0）
- 其余 6 个 manager 依赖（container-manager、tab-manager、cookie-manager、assignment-rules、shortcut-manager、history-manager）：用 Proxy 桩，任意属性访问返回空操作函数（registerHandlers 顶层会调用 tabManager.setRecycleListener 等，需存在但行为无关）
- 随后 require ipc-handlers.js、调用 registerHandlers()，从 handlers 字典取出 8 个 favorites:* handler，以 event = { sender: {} } 和 <behavior> 中列出的 preload 真实 payload 逐一同步调用
- 每通道断言：未抛异常；calls 中对应记录的实参个数与期望值精确一致；实参内容与期望值深度相等；返回值与预设值一致
- 输出每通道 PASS/FAIL 明细与汇总，任何失败以非零码退出

**Step 2 — 运行 node scripts/test-ipc-favorites.js 确认 RED：** 当前 8 个 handler 的参数校验强制要求容器 ID 字段，preload 新 payload 一律不含该字段，应见 8/8 FAIL（全部抛出校验异常）。此即 VERIFICATION.md 行为抽查中"5/5 通道全部 THROW"的可重复回归版本。

**Step 3（GREEN）— 重构 ipc-handlers.js 收藏夹区段（行 729-865），仅改这 8 个 handler，其余一律不动：**

对每个 handler 做三类修改（per D-08、D-11 的全链路去容器化语义）：

1. 参数校验：删除校验条件中的 containerId 类型检查子句，保留 data 非空且为对象的基线校验（与文件内既有校验风格一致；favorites:count 的 preload payload 为空对象 {}，基线校验必须保留）
2. manager 调用：删除首个位置实参，按下列新签名传参——
   - favorites:check（行 738-744）：checkUrl 单参调用，实参为 data.url || ''
   - favorites:add（行 755-765）：addRecord 单参调用，实参为对象字面量 { url: data.url || '', title: data.title || '', faviconUrl: data.faviconUrl || '' }
   - favorites:update（行 775-781）：updateRecord 双参调用，实参为 data.id 与 { title: data.title }
   - favorites:delete（行 790-796）：deleteRecord 单参调用，实参为 data.id
   - favorites:delete-batch（行 805-811）：deleteRecords 单参调用，实参为 data.ids || []
   - favorites:list（行 821-830）：listRecords 单参调用，实参为 { offset: data.offset || 0, limit: data.limit || 50 }
   - favorites:search（行 841-851）：searchRecords 单参调用，实参为 { keyword: data.keyword || '', offset: data.offset || 0, limit: data.limit || 50 }
   - favorites:count（行 859-865）：getCount 零参调用
3. JSDoc：删除 8 个 docblock 中描述容器 ID 参数的那一行（概念性删除，不要新增任何含容器 ID 的注释）；favorites:count 的摘要文案从"获取容器收藏记录总数"改为"获取收藏记录总数"

**明确禁止：**
- 不得修改 history:* / container:* / tab:* 等收藏夹区段以外的任何 handler——历史记录仍按容器隔离（Phase 6 设计），其 containerId 校验是有意保留的
- 不得修改 favorites-manager.js、preload.js、renderer.js、favorites-page.js、main.js（VERIFICATION 已验证这些层 PASS）
- 不得弱化 assertTrustedSender 调用（每个 handler 首行必须保留）

**Step 4 — 再跑 node scripts/test-ipc-favorites.js 确认 GREEN：** 8/8 PASS，退出码 0。

冒烟测试文件纳入 git（它是回应 VERIFICATION 流程备注的持久运行时回归护栏，不是一次性脚本）。
  </action>
  <verify>
    <automated>cd ~/Projects/Realm && node scripts/test-ipc-favorites.js && sed -n '/==================== 收藏夹/,$p' ipc-handlers.js | grep -c containerId | grep -x 0</automated>
  </verify>
  <done>
    冒烟测试 8/8 PASS（每通道不抛异常 + manager 调用签名精确匹配新签名）；收藏夹区段（"==================== 收藏夹" 注释行至文件尾）containerId 出现次数为 0；history:* 等其他区段未受影响（history 仍保留 containerId 属正常）；ipc-handlers.js 语法可被 Node 正常解析（冒烟测试本身即加载证明）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 真实星标流程 UAT — 收藏 → 持久化 → 跨容器标记 → 重复提示 → 取消</name>
  <read_first>
    - .planning/phases/09-共享收藏数据库/09-VERIFICATION.md（gaps.missing 最后一项：UAT 需补充真实星标流程用例）
  </read_first>
  <files>无文件修改（人工 UAT 检查点）</files>
  <action>暂停自动化执行，向用户展示 how-to-verify 中的 9 步验证清单，等待用户完成真实星标流程测试并给出 resume-signal。若用户报告失败，记录失败步骤与 Console 报错，回到 Task 1 排查，不得擅自标记通过。</action>
  <what-built>
    Task 1 已修复 8 个 favorites:* IPC handler 并通过运行时冒烟测试。此前 UAT 8/8 通过中 7 项为 grep 静态检查、1 项人工 Cold Start 不含星标操作，从未覆盖 IPC 运行时链路（VERIFICATION.md 流程备注 ①）。本检查点补齐真实星标全流程人工验证。
  </what-built>
  <how-to-verify>
    1. 完全退出应用后运行 npm run dev 启动
    2. 打开开发者工具 Console 备用（此前报错 renderer.js:234 / renderer.js:332「无效的参数」应不再出现）
    3. 在任意容器（如 default）打开一个网页（如 https://example.com），点击工具栏星标按钮 → 预期：弹出「收藏成功」toast（而非「收藏失败，请重试」），星标变为已收藏态
    4. 刷新该页面 → 预期：星标保持已收藏态（favorites:check 链路恢复）
    5. 切换到另一个容器（如 work），打开同一 URL → 预期：星标直接显示已收藏（跨容器共享，FAV-09/FAV-10 用户可感证据）
    6. 在该已收藏页面再次点击星标尝试重复收藏 → 预期：提示「已收藏过该页面」（SC 3 重复提示可达性恢复）
    7. 点击星标选择取消收藏/移除 → 预期：星标恢复未收藏态，切回第一个容器刷新后同样未收藏
    8. 再收藏一次该页面，打开 realm://favorites → 预期：列表显示该收藏；在列表页编辑标题、搜索、删除均正常（HTTP 路径此前已 PASS，回归确认）
    9. 全程观察 Console：不再出现「检查收藏状态失败」或「收藏失败」+「无效的参数」报错
  </how-to-verify>
  <verify>用户在真实应用中完成 how-to-verify 全部 9 步并输入 "approved"</verify>
  <done>星标按钮收藏/状态回显/重复提示/取消收藏全链路在真实应用中人工确认可用；Console 无「无效的参数」报错；跨容器星标状态一致</done>
  <resume-signal>输入 "approved" 表示全流程通过；或描述具体失败的步骤与现象</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | 类型 | 说明 |
|----------|------|------|
| ipc-handlers.js | 修改 | 收藏夹区段 8 个 favorites:* handler 移除 containerId 校验与位置参数，JSDoc 同步清理 |
| scripts/test-ipc-favorites.js | 新增 | IPC 运行时冒烟测试：依赖打桩 + 8 通道 payload/签名断言，纯 Node 可重复运行，作为 IPC 层持久回归护栏 |
| .planning/phases/09-共享收藏数据库/09-03-SUMMARY.md | 新增 | 本计划执行记录 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → main (IPC) | 渲染进程经 contextBridge 调用 favorites:* 通道，不可信输入在此越界 |
| main process → SQLite | favoritesManager 直接操作全局 favorites 表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Spoofing | favorites:* IPC 通道调用方伪造 | medium | mitigate | 每个 handler 首行保留 assertTrustedSender（CR-4 已有控制：仅接受主窗口 webContents），本计划明确禁止弱化；冒烟测试桩已覆盖该路径通过场景 |
| T-09-04 | Tampering | 收藏 payload 字段注入（url/title 拼接 SQL） | low | accept | favoritesManager 全部使用 better-sqlite3 预编译参数化语句，无字符串拼接；校验放宽仅涉及移除容器 ID 字段检查，不影响注入面 |
| T-09-05 | Tampering | 参数校验放宽导致异常输入直达 manager | low | accept | 保留 data 为对象的基线校验；manager 层对重复 URL（UNIQUE 约束）与空数组批量删除均有防御；最严重后果为单条垃圾收藏记录，用户可自行删除 |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | 本计划无新增依赖（冒烟测试仅用 Node 内置模块） |
</threat_model>

<verification>
## 整体验证（对应 gap closure 三层要求）

1. **静态检查**：sed 提取 ipc-handlers.js 收藏夹区段（"==================== 收藏夹" 注释行至文件尾），containerId 出现次数为 0；同时文件级 grep 确认 history:* 区段 containerId 保留（>0），证明改动范围精确
2. **运行时检查**：node scripts/test-ipc-favorites.js 退出码 0，8/8 通道断言通过（含 favoritesManager.checkUrl('https://example.com') 形态调用返回 null 不抛异常）
3. **UAT 重跑**：Task 2 人工检查点完成真实星标流程（收藏 → toast 成功 → 星标已收藏 → 刷新持久化 → 跨容器同状态 → 重复提示 → 取消收藏）
</verification>

<success_criteria>
- ipc-handlers.js 收藏夹区段不再引用 containerId（验证层 + 位置参数层双重清理）
- 8 个 handler 调用 favoritesManager 的实参个数与内容与新签名精确一致（冒烟测试断言）
- 星标按钮 check/add/update/delete 四链路在真实应用中端到端可用（UAT 人工确认）
- renderer.js 不再出现「无效的参数」IPC 报错
- SC 1（写入路径）、SC 2（跨容器标记）、SC 3（重复提示可达）由 FAILED 转为 PASS
</success_criteria>

<output>
Create `.planning/phases/09-共享收藏数据库/09-03-SUMMARY.md` when done
</output>
