---
phase: 21-ai-agent-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - src/index.html
  - src/renderer.js
autonomous: true
gap_closure: true
requirements:
  - AI-03
must_haves:
  truths:
    - "面板头部设置按钮可打开 AI 设置页面"
  artifacts:
    - "src/index.html 中 #aiPanelHeader 的 .ai-panel-header-actions 包含 #aiSettingsBtn 按钮"
    - "src/renderer.js 中 openAISettings() 函数可正确跳转到设置页面 AI 分区"
  key_links:
    - "#aiSettingsBtn 点击 → openAISettings() → openSettingsTab('ai') → realm://settings?tab=ai"
---

<objective>
修复 Phase 21 验证发现的 gap：AI 面板头部设置按钮缺失。

Purpose: 补全 AI 面板与设置页面的集成入口，使用户可从 AI 面板直接跳转到 AI 设置分区。
Output: 在 AI 面板头部添加设置按钮，修复 openAISettings() 函数使其正确跳转到设置页面。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ai-agent-ui/21-VERIFICATION.md
@.planning/phases/21-ai-agent-ui/21-CONTEXT.md
@.planning/phases/21-ai-agent-ui/21-01-SUMMARY.md
@.planning/phases/21-ai-agent-ui/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加设置按钮到 AI 面板头部 HTML</name>
  <files>src/index.html</files>
  <action>
    在 src/index.html 的 #aiPanelHeader 的 .ai-panel-header-actions 容器中，在关闭按钮 (#aiPanelCloseBtn) 之前添加设置按钮。

    具体位置：第 423-429 行的 .ai-panel-header-actions div 内，在 #aiPanelCloseBtn 按钮之前插入设置按钮。

    添加的按钮应使用与关闭按钮相同的 .btn-icon 类和 ai-panel 命名约定，ID 为 aiSettingsBtn，标题为 "AI 设置"，使用齿轮 SVG 图标（复用 #settingsBtn 的齿轮图标路径）。

    按钮结构参考现有 .btn-icon 模式（如第 121-126 行的 #settingsBtn），使用 16x16 viewBox。
  </action>
  <verify>
    <automated>grep -c 'id="aiSettingsBtn"' /Users/wxnacy/Projects/Realm/src/index.html</automated>
  </verify>
  <done>src/index.html 中 #aiPanelHeader 的 .ai-panel-header-actions 包含 #aiSettingsBtn 设置按钮，位于关闭按钮之前</done>
</task>

<task type="auto">
  <name>Task 2: 修复 openAISettings 函数跳转逻辑</name>
  <files>src/renderer.js</files>
  <action>
    修改 src/renderer.js 中的 openAISettings() 函数（当前位于第 3662-3666 行）。

    当前实现依赖不存在的 window.realmAPI.openSettings API，应改为直接调用已有的 openSettingsTab('ai') 函数。

    修改后的函数体：
    - 调用 openSettingsTab('ai') 跳转到设置页面的 AI 助手分区
    - 移除对 window.realmAPI.openSettings 的依赖检查

    这是正确的实现方式，因为：
    1. openSettingsTab() 已在 renderer.js 中定义（第 1111 行），支持 tab 参数跳转到指定分区
    2. 无需新增 IPC 通道和 preload API，减少代码复杂度
    3. 与 Cmd+, 快捷键打开设置的入口保持一致（第 1057 行调用 openSettingsTab()）
  </action>
  <verify>
    <automated>grep -A 3 "function openAISettings" /Users/wxnacy/Projects/Realm/src/renderer.js</automated>
  </verify>
  <done>openAISettings() 函数调用 openSettingsTab('ai') 跳转到设置页面 AI 分区，不再依赖不存在的 realmAPI.openSettings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → renderer | 内部函数调用，无安全边界跨越 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| (none) | - | - | - | - | 本次修改仅涉及 UI 元素添加和内部函数调用修复，无新增安全风险 |
</threat_model>

<verification>
## 整体验证

1. AI 面板头部显示设置按钮（齿轮图标）
2. 点击设置按钮跳转到 realm://settings?tab=ai
3. 设置按钮不影响面板头部布局和其他按钮功能
</verification>

<success_criteria>
- #aiSettingsBtn 元素存在于 AI 面板头部
- 点击设置按钮可正确打开设置页面并定位到 AI 助手分区
- openAISettings() 函数不再依赖不存在的 API
</success_criteria>

<output>
创建 `.planning/phases/21-ai-agent-ui/21-03-SUMMARY.md` when done
</output>
