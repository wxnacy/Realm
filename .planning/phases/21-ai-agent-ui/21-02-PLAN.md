---
phase: 21
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/renderer.js
  - src/styles/main.css
autonomous: true
requirements:
  - AI-03
must_haves:
  truths:
    - 工具执行状态以可折叠卡片形式显示
    - 点击工具卡片可展开/折叠查看参数和结果
    - 多工具调用纵向堆叠显示
    - 每条消息支持复制和重新生成操作
    - 面板左侧边缘可拖拽调整宽度
    - 面板宽度和开关状态持久化
  artifacts:
    - src/renderer.js 包含工具卡片、复制/重新生成、拖拽调整逻辑
    - src/styles/main.css 包含工具卡片、操作按钮、拖拽手柄样式
  key_links:
    - ai:events-batch tool_execution_update → renderToolCard()
    - #aiPanelResizeHandle → initPanelResize()
    - electron-store → 持久化面板宽度和开关状态
---

<objective>
实现 AI 聊天面板的增强功能，包括工具执行卡片、消息操作、拖拽调整宽度和设置集成。

Purpose: 完善 AI 聊天面板的交互体验，使用户可以查看工具执行状态、复制/重新生成消息、调整面板大小。
Output: 功能完整的 AI 聊天面板，支持所有 D-01 到 D-17 决策。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ai-agent-ui/21-CONTEXT.md
@.planning/phases/21-ai-agent-ui/21-RESEARCH.md
@.planning/phases/21-ai-agent-ui/21-UI-SPEC.md
@src/renderer.js
@src/styles/main.css
@.planning/phases/21-ai-agent-ui/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现工具执行卡片</name>
  <files>src/renderer.js, src/styles/main.css</files>
  <read_first>
    - src/renderer.js
    - src/styles/main.css
    - .planning/phases/21-ai-agent-ui/21-CONTEXT.md (D-10, D-11, D-12, D-13)
    - .planning/phases/21-ai-agent-ui/21-UI-SPEC.md (工具执行卡片章节)
  </read_first>
  <action>
    1. 在 src/renderer.js 的 AI 助手区域添加工具卡片相关函数：

    2. 实现 renderToolCard(toolExecution) 函数（per D-10, D-11, D-12, D-13）：
       - 创建 .tool-card 容器
       - 折叠状态（默认）：
         * 高度 36px，背景 --ai-tool-card-bg，圆角 8px
         * 显示状态图标（旋转/成功/失败）+ 工具名称 + 状态文字
         * 执行中：旋转动画（@keyframes spin）
         * 完成：绿色勾图标（--success-color）
         * 失败：红色叉图标（--danger-color）
       - 点击折叠区域切换展开/折叠状态
       - 展开内容：
         * 参数区域：JSON 格式显示，monospace 12px
         * 结果区域：格式化输出或 JSON
         * 使用 textContent 设置内容（防止 XSS）
       - 动画：max-height 过渡，200ms ease

    3. 更新 handleAIStream() 函数处理 tool_execution_update 事件：
       - 在 state.aiMessages 中找到对应消息
       - 添加/更新 toolExecutions 数组
       - 调用 renderToolCards(messageId) 更新显示
       - 多工具调用纵向堆叠，间距 8px

    4. 实现 renderToolCards(messageId) 函数：
       - 获取消息元素中的 .tool-cards-container
       - 清空并重新渲染所有工具卡片
       - 每个工具卡片独立显示状态

    5. 在 src/styles/main.css 中添加工具卡片样式：
       - 添加 .tool-card 样式（36px 高度，背景 #252525，圆角 8px）
       - 添加 .tool-card-header 样式（点击区域，cursor: pointer）
       - 添加 .tool-card-content 样式（展开内容，默认 max-height: 0）
       - 添加 .tool-card.expanded 样式（max-height: 500px 或 auto）
       - 添加工具图标样式（.tool-icon-spin 旋转动画，.tool-icon-success 绿色，.tool-icon-error 红色）
       - 添加工具参数/结果区域样式（monospace 字体，12px）
       - 添加多工具堆叠间距（.tool-cards-container gap: 8px）
  </action>
  <verify>
    <automated>grep -c "renderToolCard" src/renderer.js && grep -c "tool-card" src/styles/main.css</automated>
  </verify>
  <acceptance_criteria>
    - renderToolCard() 函数可渲染折叠/展开状态的工具卡片
    - 工具卡片显示状态图标（旋转/成功/失败）和工具名称
    - 点击工具卡片可展开/折叠查看参数和结果
    - 多工具调用纵向堆叠显示
    - tool_execution_update 事件正确处理并更新显示
    - 工具参数和结果使用 textContent 设置（防 XSS）
  </acceptance_criteria>
  <done>工具执行卡片功能完成，用户可查看工具执行状态和详情</done>
</task>

<task type="auto">
  <name>Task 2: 实现消息操作和拖拽调整宽度</name>
  <files>src/renderer.js, src/styles/main.css</files>
  <read_first>
    - src/renderer.js
    - src/styles/main.css
    - .planning/phases/21-ai-agent-ui/21-CONTEXT.md (D-09, D-17)
    - .planning/phases/21-ai-agent-ui/21-UI-SPEC.md (面板拖拽调整手柄章节)
  </read_first>
  <action>
    1. 在 src/renderer.js 的 AI 助手区域添加消息操作函数：

    2. 实现 copyMessage(messageId) 函数（per D-09）：
       - 从 state.aiMessages 中找到对应消息
       - 复制消息内容到剪贴板（使用 navigator.clipboard.writeText）
       - 显示提示"已复制到剪贴板"（2 秒后自动消失）
       - 提示样式：底部居中，背景 --bg-tertiary，圆角 4px

    3. 实现 regenerateMessage(messageId) 函数（per D-09）：
       - 找到消息在 state.aiMessages 中的位置
       - 删除该消息及其之后的所有消息
       - 获取被删除的 AI 消息之前的用户消息
       - 重新发送该用户消息（调用 window.realmAPI.ai.prompt）
       - 设置 state.aiStreaming = true
       - 调用 renderAIMessages() 更新显示

    4. 更新 renderAIMessages() 函数，为每条消息添加操作按钮：
       - AI 消息：显示"复制"和"重新生成"按钮
       - 用户消息：显示"复制"按钮
       - 按钮样式：12px，--text-muted，悬停时 --text-primary
       - 按钮位置：消息气泡下方，时间戳旁边

    5. 在 src/styles/main.css 中添加消息操作按钮样式：
       - 添加 .message-actions 容器样式（flex，gap: 8px）
       - 添加 .message-action-btn 样式（12px，--text-muted，cursor: pointer）
       - 添加 .message-action-btn:hover 样式（--text-primary）
       - 添加复制成功提示样式（.copy-toast，底部居中，动画淡入淡出）

    6. 实现 initPanelResize() 函数（per D-17）：
       - 获取 #aiPanelResizeHandle 元素
       - 监听 mousedown 事件：记录起始位置和面板宽度，设置 body cursor 为 col-resize
       - 监听 mousemove 事件：计算新宽度（向左拖拽增大），限制在 280-600px 范围内，实时更新面板宽度
       - 监听 mouseup 事件：恢复 cursor，持久化宽度到 electron-store（通过 window.realmAPI.setSetting('ai.panelWidth', width)）

    7. 在 src/styles/main.css 中添加拖拽手柄样式：
       - 添加 #aiPanelResizeHandle 样式（绝对定位，左侧 6px 宽度，cursor: col-resize）
       - 添加悬停高亮效果（background-color 变化，150ms ease）

    8. 在页面加载完成后调用 initPanelResize() 初始化拖拽功能

    9. 更新 toggleAIPanel() 函数，加载持久化的面板宽度：
       - 在打开面板时，从 electron-store 读取 ai.panelWidth（通过 `const settings = await window.realmAPI.getSettings(); const width = settings['ai.panelWidth'];`）
       - 如果有值，设置 #aiPanel 的宽度为该值
       - 持久化面板开关状态（已在 21-01 实现）
  </action>
  <verify>
    <automated>grep -c "copyMessage" src/renderer.js && grep -c "initPanelResize" src/renderer.js</automated>
  </verify>
  <acceptance_criteria>
    - copyMessage() 函数可复制消息内容到剪贴板
    - regenerateMessage() 函数可重新生成 AI 消息
    - 消息气泡下方显示操作按钮（复制/重新生成）
    - initPanelResize() 函数可实现拖拽调整面板宽度
    - 面板宽度限制在 280-600px 范围内
    - 面板宽度和开关状态持久化到 electron-store
    - 拖拽手柄悬停时显示高亮效果
  </acceptance_criteria>
  <done>消息操作和拖拽调整宽度功能完成，用户可复制/重新生成消息、调整面板大小</done>
</task>

<task type="auto">
  <name>Task 3: 完善 AI 面板设置集成和空状态</name>
  <files>src/renderer.js, src/styles/main.css</files>
  <read_first>
    - src/renderer.js
    - src/styles/main.css
    - .planning/phases/21-ai-agent-ui/21-CONTEXT.md
    - .planning/phases/21-ai-agent-ui/21-UI-SPEC.md (文案合约章节)
  </read_first>
  <action>
    1. 在 src/renderer.js 的 AI 助手区域添加空状态渲染：

    2. 实现 renderAIEmptyState() 函数：
       - 当 state.aiMessages 为空时显示空状态
       - 标题："准备好聊天了吗？"（16px, 600, --text-primary）
       - 正文："输入消息开始与 AI 助手对话。我可以帮你导航网页、搜索历史、管理收藏。"（14px, --text-secondary）
       - 居中显示，上下间距 32px

    3. 更新 renderAIMessages() 函数：
       - 如果 state.aiMessages 为空，调用 renderAIEmptyState()
       - 否则渲染消息列表

    4. 实现 showAIError(error) 函数：
       - 在消息列表中显示错误消息
       - 错误文案："出现问题 — {错误描述}。请重试或检查 AI 设置。"
       - 错误样式：红色文字（--danger-color）
       - 添加"重试"按钮，点击后重新发送上一条用户消息

    5. 更新 handleAIStream() 函数处理 error 事件：
       - 调用 showAIError(event.error)
       - 设置 state.aiStreaming = false

    6. 实现 openAISettings() 函数：
       - 点击面板头部设置按钮时调用
       - 打开设置页面并滚动到"AI 助手"分区（Phase 20 已实现）
       - 使用 window.realmAPI.openSettings('ai') 或类似 API

    7. 在 setupEventListeners() 中添加设置按钮事件监听：
       - #aiSettingsBtn 点击：openAISettings()

    8. 在 src/styles/main.css 中添加空状态和错误样式：
       - 添加 .ai-empty-state 样式（居中，padding: 32px）
       - 添加 .ai-empty-state-title 样式（16px, 600, --text-primary）
       - 添加 .ai-empty-state-text 样式（14px, --text-secondary）
       - 添加 .ai-error-message 样式（--danger-color，红色文字）
       - 添加 .ai-retry-btn 样式（按钮样式）

    9. 添加面板打开/关闭过渡动画（per UI-SPEC.md）：
       - 更新 #aiPanel 样式：添加 transition: width 250ms ease-in-out, opacity 250ms ease-in-out
       - 更新 #aiPanel.hidden 样式：width: 0, opacity: 0, overflow: hidden

    10. 添加消息气泡入场动画（per UI-SPEC.md）：
        - 添加 @keyframes messageSlideIn（from opacity: 0, translateY: 8px to opacity: 1, translateY: 0）
        - 添加 .message-bubble 样式：animation: messageSlideIn 200ms ease

    11. 添加流式光标闪烁动画（per UI-SPEC.md）：
        - 添加 @keyframes blink（0%, 100% opacity: 1; 50% opacity: 0）
        - 添加 .streaming-cursor 样式：2px 宽、14px 高、--accent-color、animation: blink 1s infinite
  </action>
  <verify>
    <automated>grep -c "renderAIEmptyState" src/renderer.js && grep -c "ai-empty-state" src/styles/main.css</automated>
  </verify>
  <acceptance_criteria>
    - 空状态显示正确的标题和正文
    - 错误消息正确显示，包含重试按钮
    - 设置按钮可打开 AI 设置页面
    - 面板打开/关闭有平滑过渡动画
    - 消息气泡有入场动画
    - 流式光标有闪烁动画
  </acceptance_criteria>
  <done>AI 面板设置集成和动画效果完成，用户体验完善</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI Response → DOM | AI 返回的 Markdown 内容渲染到 DOM |
| Tool Result → DOM | 工具执行结果渲染到 DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering | Tool Result Rendering | medium | mitigate | 工具结果使用 textContent 设置，不使用 innerHTML |
| T-21-04 | Tampering | Clipboard API | low | accept | 复制功能使用 navigator.clipboard API，浏览器原生安全机制 |

</threat_model>

<verification>
## 整体验证

1. 工具卡片验证：
   - 发送会触发工具调用的消息（如"打开百度"）
   - 验证工具卡片以折叠形式显示
   - 点击工具卡片展开，查看参数和结果
   - 验证多工具调用纵向堆叠

2. 消息操作验证：
   - 点击 AI 消息的"复制"按钮，验证剪贴板内容
   - 点击"重新生成"按钮，验证消息重新生成
   - 验证复制成功提示显示

3. 拖拽调整验证：
   - 鼠标悬停在面板左边缘，光标变为 col-resize
   - 拖拽调整面板宽度，验证宽度变化
   - 验证宽度限制在 280-600px 范围内
   - 关闭并重新打开面板，验证宽度持久化

4. 空状态和错误验证：
   - 打开空面板，验证空状态显示
   - 模拟错误（如断网），验证错误消息显示
   - 点击"重试"按钮，验证重新发送

5. 动画验证：
   - 打开/关闭面板，验证平滑过渡
   - 发送消息，验证气泡入场动画
   - 接收流式响应，验证光标闪烁
</verification>

<success_criteria>
- 工具执行状态以可折叠卡片形式正确显示
- 消息支持复制和重新生成操作
- 面板可拖拽调整宽度，状态持久化
- 空状态和错误状态正确显示
- 所有动画效果正常工作
- 设置按钮可打开 AI 设置页面
</success_criteria>

<output>
创建 `.planning/phases/21-ai-agent-ui/21-02-SUMMARY.md` 当完成时
</output>
