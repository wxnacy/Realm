---
phase: 25-script-tab
plan: 07
type: execute
wave: 4
depends_on:
  - 25-06
files_modified:
  - ai-manager.js
  - main.js
  - src/renderer.js
autonomous: true
gap_closure: true
gap_ids:
  - G-25-SC1
  - G-25-SC2
  - G-25-SC3
requirements:
  - SCRIPT-01
  - SCRIPT-02
  - SCRIPT-03
estimate: 40min
must_haves:
  truths:
    - "用户在 AI 聊天中输入自然语言描述，AI 生成可执行脚本并展示预览卡片（含完整步骤列表）"
    - "脚本执行前经 validateScriptForSteps 白名单校验，危险操作被拦截并返回错误"
    - "用户确认脚本后可通过预览卡片的执行按钮触发脚本执行，执行结果实时反馈"
  artifacts:
    - path: ai-manager.js
      must_exist: "validateScriptForSteps 通过 module.exports 导出；generate_script 工具接受 steps 参数"
    - path: main.js
      must_exist: "script:execute handler 调用 validateScriptForSteps 并拦截 unsafe 脚本"
    - path: src/renderer.js
      must_exist: "renderToolCards 包含 generate_script 分支调用 renderScriptPreviewCard；错误面板使用 textContent 而非 innerHTML"
---

## Gap 修复目标

**修复范围:** 脚本半边三个未达成目标（SC#1/2/3）+ CR-01 XSS 修复

**根因:** generate_script 工具返回 steps:[] 空骨架，AI 产出的完整步骤无结构化回流通道；renderToolCards 缺少 generate_script 分支；script:execute 未调用白名单校验。

**修复方向:** 让 AI 调用 generate_script 时传入 steps 参数 → 工具返回含完整步骤的结果 → renderToolCards 识别 generate_script 并调用 renderScriptPreviewCard → 卡片接线后执行入口可达 → 顺带修复 CR-01 innerHTML XSS。

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-script-tab/25-VERIFICATION.md
@.planning/phases/25-script-tab/25-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ai-manager.js — generate_script 接受 steps + 导出 validateScriptForSteps</name>
  <files>ai-manager.js</files>
  <read_first>
    - ai-manager.js:2113-2190（generate_script 工具定义与 execute 函数）
    - ai-manager.js:193-250（validateScriptForSteps 函数定义）
    - ai-manager.js:2580-2583（module.exports）
  </read_first>
  <action>
1. 修改 generate_script 工具的 parameters，添加 `steps` 字段：
   - 类型: array
   - description: "脚本步骤数组，每步包含 action/target/options/waitFor"
   - items: { type: 'object', properties: { action: {type:'string'}, target: {type:'string'}, options: {type:'object'}, waitFor: {type:'number'} } }
   - 不设为 required（向后兼容：AI 可先不传 steps 让工具返回骨架，也可直接传入完整步骤）

2. 修改 generate_script execute 函数（ai-manager.js:2132 起）：
   - 从 params 解构 `steps`：`const { description, containerId: requestedContainerId, steps: inputSteps } = params;`
   - 将脚本骨架的 `steps: []` 改为 `steps: Array.isArray(inputSteps) ? inputSteps : []`
   - 这样 AI 传入 steps 时，返回的结果包含完整步骤；不传时仍返回空骨架（向后兼容）

3. 在 module.exports 部分（ai-manager.js:2583 之后）添加：
   `module.exports.validateScriptForSteps = validateScriptForSteps;`
  </action>
  <behavior>
    - Test: generate_script({description:'test', steps:[{action:'click',target:'按钮'}]}) 返回的 script.steps.length === 1
    - Test: generate_script({description:'test'}) 返回的 script.steps.length === 0（向后兼容）
    - Test: require('./ai-manager').validateScriptForSteps 是 function 类型
  </behavior>
  <verify>
    <automated>node -e "const m=require('./ai-manager'); console.log(typeof m.validateScriptForSteps)" && grep -c "steps: Array.isArray" ai-manager.js</automated>
  </verify>
  <acceptance_criteria>
    - validateScriptForSteps 通过 module.exports 导出
    - generate_script parameters 包含 steps 字段
    - execute 函数使用 `steps: Array.isArray(inputSteps) ? inputSteps : []` 构造脚本
  </acceptance_criteria>
  <done>generate_script 工具接受 steps 参数并回传到结果；validateScriptForSteps 已导出</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.js — script:execute 强制白名单校验</name>
  <files>main.js</files>
  <read_first>
    - main.js:1741-1776（script:execute handler）
    - ai-manager.js:193-250（validateScriptForSteps 逻辑参考）
  </read_first>
  <action>
1. 在 main.js 顶部的 require 区域，将 `const { executeScript } = require('./ai-manager')` 改为同时解构 validateScriptForSteps：
   `const { executeScript, validateScriptForSteps } = require('./ai-manager');`
   注意：main.js:1756 有局部 require，需同步修改为解构导入或在此处也解构。

2. 在 script:execute handler 中（main.js:1743 格式校验之后、获取 webContentsId 之前），插入白名单校验：
   ```javascript
   // 白名单静态分析（SCRIPT-03）
   const validation = validateScriptForSteps(script);
   if (!validation.safe) {
     console.warn(`[Realm] 脚本安全检查未通过: ${validation.reason}`);
     return { success: false, error: `脚本安全检查未通过: ${validation.reason}`, blocked: true };
   }
   ```

3. 删除 main.js:1756 的局部 `const { executeScript } = require('./ai-manager')`，因为已在文件顶部导入。
  </action>
  <behavior>
    - Test: script:execute 传入包含 eval 的 step → 返回 {success:false, blocked:true}
    - Test: script:execute 传入白名单内 steps → 正常执行
  </behavior>
  <verify>
    <automated>grep -c "validateScriptForSteps(script)" main.js</automated>
  </verify>
  <acceptance_criteria>
    - main.js 从 ai-manager.js 导入 validateScriptForSteps
    - script:execute handler 在执行前调用 validateScriptForSteps
    - unsafe 脚本返回 {success:false, blocked:true, error:'...'}
  </acceptance_criteria>
  <done>script:execute 执行前强制白名单校验，危险脚本被拦截</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: renderer.js — renderToolCards 补 generate_script 分支 + CR-01 XSS 修复</name>
  <files>src/renderer.js</files>
  <read_first>
    - src/renderer.js:3900-3960（renderToolCards 函数）
    - src/renderer.js:4698-4790（renderScriptPreviewCard 函数）
    - src/renderer.js:5268-5282（CR-01 innerHTML XSS 位置）
  </read_first>
  <action>
1. 在 renderToolCards 函数中（src/renderer.js:3956 之后、默认工具卡片之前），添加 generate_script 分支。参照 suggest_tab_groups/apply_tab_groups 的信封解包模式：

   在 `if ((toolExec.name === 'suggest_tab_groups' || toolExec.name === 'apply_tab_groups')` 块之后、`// 默认工具卡片` 注释之前，插入：

   ```javascript
   // 特殊工具卡片：generate_script 完成后渲染脚本预览卡片
   if (toolExec.name === 'generate_script' && toolExec.status === 'completed' && toolExec.result) {
     try {
       let resultData = typeof toolExec.result === 'string'
         ? JSON.parse(toolExec.result)
         : toolExec.result;
       // 工具结果信封解包
       if (resultData && !resultData.steps && Array.isArray(resultData.content)) {
         const textBlock = resultData.content.find(
           block => block && block.type === 'text' && typeof block.text === 'string'
         );
         if (textBlock) {
           resultData = JSON.parse(textBlock.text);
         }
       }
       if (resultData && Array.isArray(resultData.steps) && resultData.steps.length > 0) {
         const scriptCard = renderScriptPreviewCard(resultData);
         if (scriptCard) {
           container.appendChild(scriptCard);
           return;
         }
       }
     } catch (err) {
       console.error('[Realm Renderer] 解析脚本工具结果失败:', err.message);
     }
   }
   ```

   逻辑说明：
   - 仅当 toolExec.name === 'generate_script' 且 status === 'completed' 且有 result 时触发
   - 先尝试直接解析 result，若无 steps 字段则从 content 信封解包（与分组卡片一致的模式）
   - 解析到 steps 数组且非空时调用 renderScriptPreviewCard
   - renderScriptPreviewCard 内部已有完整的执行按钮、取消按钮、添加步骤按钮绑定

2. 修复 CR-01 XSS（src/renderer.js:5274-5279）：
   将 innerHTML 替换为安全的 DOM 构造：
   ```javascript
   // 创建错误面板（使用 textContent 防止 XSS — CR-01 修复）
   const errorMessage = document.createElement('div');
   errorMessage.className = 'step-error-message';
   errorMessage.textContent = error || '操作执行失败';

   const errorActions = document.createElement('div');
   errorActions.className = 'step-error-actions';

   const retryBtn = document.createElement('button');
   retryBtn.className = 'step-retry-btn';
   retryBtn.textContent = '重试';

   const skipBtn = document.createElement('button');
   skipBtn.className = 'step-skip-btn';
   skipBtn.textContent = '跳过';

   const abortBtn = document.createElement('button');
   abortBtn.className = 'step-abort-btn';
   abortBtn.textContent = '终止';

   errorActions.appendChild(retryBtn);
   errorActions.appendChild(skipBtn);
   errorActions.appendChild(abortBtn);

   const errorPanel = document.createElement('div');
   errorPanel.className = 'step-error-panel';
   errorPanel.appendChild(errorMessage);
   errorPanel.appendChild(errorActions);
   ```

   注意：后续 retryBtn/skipBtn/abortBtn 的事件绑定代码中，原来的 `errorPanel.querySelector('.step-retry-btn')` 等选择器需改为直接使用 retryBtn/skipBtn/abortBtn 变量。
  </action>
  <behavior>
    - Test: renderToolCards 遇到 generate_script completed result 含 steps → 调用 renderScriptPreviewCard
    - Test: renderToolCards 遇到 generate_script result steps 为空数组 → 走默认卡片（不渲染预览）
    - Test: 错误面板使用 textContent 设置错误信息，不包含 innerHTML
  </behavior>
  <verify>
    <automated>grep -c "toolExec.name === 'generate_script'" src/renderer.js && grep -c "textContent" src/renderer.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - renderToolCards 包含 generate_script 分支（信封解包 + steps 判定 → renderScriptPreviewCard）
    - 错误面板（step-error-panel）使用 textContent 替代 innerHTML
    - renderScriptPreviewCard 不再是死代码（有真实调用路径）
  </acceptance_criteria>
  <done>generate_script 结果可渲染脚本预览卡片；CR-01 innerHTML XSS 已修复</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI → generate_script | AI 传入 steps 数组，可能包含恶意内容 |
| script:execute → CDP | 脚本步骤通过 CDP 在网页中执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07-01 | Tampering | generate_script steps 参数 | high | mitigate | validateScriptForSteps 白名单校验 action + 危险模式扫描 target/options |
| T-25-07-02 | Information Disclosure | innerHTML XSS (CR-01) | high | mitigate | 替换 innerHTML 为 textContent + DOM API 构造 |
| T-25-07-03 | Elevation of Privilege | script:execute 绕过白名单 | critical | mitigate | main.js handler 强制调用 validateScriptForSteps，unsafe 返回 blocked |
</threat_model>

<verification>
## 整体验证

1. **语法检查**: `node --check ai-manager.js main.js src/renderer.js` 全部通过
2. **导出验证**: `node -e "const m=require('./ai-manager'); console.log(typeof m.validateScriptForSteps)"` 输出 `function`
3. **白名单调用验证**: `grep -c "validateScriptForSteps(script)" main.js` 输出 >= 1
4. **渲染分支验证**: `grep -c "toolExec.name === 'generate_script'" src/renderer.js` 输出 >= 1
5. **XSS 修复验证**: `grep -c "innerHTML" src/renderer.js` 确认 step-error-panel 区域不再使用 innerHTML
</verification>

<success_criteria>
- generate_script 工具接受 steps 参数，返回含完整步骤的结果
- validateScriptForSteps 从 ai-manager.js 正确导出
- main.js script:execute 执行前调用 validateScriptForSteps，unsafe 脚本被拦截
- renderToolCards 识别 generate_script 结果并调用 renderScriptPreviewCard 渲染预览卡片
- 脚本预览卡片的执行按钮可正常触发 script:execute IPC
- CR-01 innerHTML XSS 已修复为 textContent
</success_criteria>

<output>
创建 `.planning/phases/25-script-tab/25-07-SUMMARY.md` 当任务完成时
</output>
