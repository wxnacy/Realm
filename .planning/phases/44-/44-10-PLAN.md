---
phase: 44-player-video-cache-and-local-media-library
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/preload.js
  - src/renderer.js
  - src/styles/main.css
  - src/index.html
autonomous: true
gap_closure: true
gap_ids: [G-44-5]
requirements: [D-26]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "convert/record 任务进入 completed/failed 终态时，主窗口出现应用内 toast 终态提示（不依赖系统通知——ad-hoc 签名下 macOS 静默拒授系统通知是环境级根因），文案与系统通知契约一致（G-44-5）"
    - "带产物的终态 toast 可点击，点击经既有 shell.showItemInFolder 链路（download:show-in-folder IPC）在 Finder 定位产物"
    - "系统通知代码保留不删（showTaskNotification 链路原样）——正式签名后系统通知恢复可用，应用内 toast 与其并存"
    - "既有 showToast 调用方零回归（第三参为可选扩展，缺省行为不变）"
  artifacts:
    - src/preload.js mediaAPI 新增 onMediaTaskChanged（media-task:changed 通道在主窗口 renderer 的暴露面）
    - src/renderer.js showToast 扩展可选 opts（onClick/duration）+ initMediaTaskToast（终态过滤 + 文案映射 + 点击定位）+ :4206 调用点接线
    - src/styles/main.css 新增 .toast-clickable 样式
    - src/index.html main.css 版本查询参数 v=5 → v=6（缓存失效）
  key_links:
    - main.js persistMediaTasks 状态 diff → windowManager.broadcast('media-task:changed', task) → preload mediaAPI.onMediaTaskChanged → renderer initMediaTaskToast（主窗口 toast）
    - toast click → window.downloadAPI.showInFolder(task.outputPath) → ipc 'download:show-in-folder' → shell.showItemInFolder
  prohibitions:
    - statement: 禁止删除或改动 main.js showTaskNotification 系统通知链路（正式签名后恢复可用；本 plan 不改 main.js）
      status: locked
      verification: main.js 无 diff（git diff --stat main.js 为空）
---

<objective>
修复 G-44-5：convert/record 终态无任何提示——根因为环境级（Nightly/dev 均 ad-hoc 签名，macOS 对未正式签名 app 静默拒授通知授权，系统从未弹过授权框），非代码缺陷。按 UAT 推荐方案实现**应用内 toast 终态提示**（主窗口 renderer）：复用既有 media-task:changed 广播通道（主进程 persistMediaTasks 状态 diff 已在发），主窗口监听后弹 toast；点击定位沿用既有 download:showItemInFolder IPC 链路。系统通知代码保留不删（正式签名后可用）。

Purpose: 用户在 ad-hoc 签名环境下对转换/录制终态完全无感知；toast 不依赖系统授权，当前环境即可用。
Output: preload 通道暴露 + renderer toast 监听/文案/点击定位 + 样式与缓存版本号。

**范围裁量（记录）**：toast 落主窗口 renderer——主窗口常驻（用户发起转换/录制后通常回到浏览），播放器窗口转瞬即逝且录制失败已有 showError 即时反馈（player.js onRecordStateChanged 分支），不在播放器重复建设 toast 基建。范围外（债务不动）：WR-A/B/C/05/01-04、IN-01-04 未点名项；（根治）Developer ID 正式签名+公证后系统通知恢复，属发布工程事项。

**tracer 说明**：gap-closure 小计划，修复本身即最小端到端路径（广播→preload→renderer→toast→点击定位），无独立 tracer 收益。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-UAT.md
@.planning/phases/44-/44-UI-SPEC.md

@src/preload.js
@src/renderer.js
@src/styles/main.css
@src/index.html
</context>

<artifacts_this_phase_produces>
- src/preload.js：mediaAPI 新增 `onMediaTaskChanged(callback)`（ipcRenderer.on('media-task:changed') 透传，返回取消监听函数——与 onMediaTaskCountChanged 同款写法）
- src/renderer.js：showToast(message, type, opts = {}) 第三可选参（opts.onClick 点击回调 + opts.duration 自动消失毫秒数，缺省 3000 不变；点击时加 toast-clickable 类）；新增 initMediaTaskToast()（过滤 status ∈ {completed, failed}，文案映射与 showTaskNotification 标题契约一致，completed/有 outputPath 时挂 onClick → window.downloadAPI.showInFolder）；:4206 initMediaTaskBadge() 调用点旁接线
- src/styles/main.css：`.toast.toast-clickable { cursor: pointer; }`（追加在 :1445 .toast 段附近）
- src/index.html：main.css?v=5 → main.css?v=6
- 无新模块、无新依赖、main.js 零改动
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: preload mediaAPI 暴露 onMediaTaskChanged（media-task:changed 主窗口可达）</name>
  <files>src/preload.js</files>
  <read_first>
    - src/preload.js:1467-1544（mediaAPI 命名空间现状：onMediaTaskCountChanged :1537-1541 监听 media-task:count-changed 的完整写法——新增条目紧随其后，同款 handler/removeListener 模式）
    - src/preload.js:1692-1703（playerAPI.onRecordStateChanged——media-task:changed 通道已被 playerAPI 消费的先例，过滤 type=record；本次主窗口需要全类型故在 mediaAPI 暴露不过滤版本）
    - main.js:2114-2126（persistMediaTasks 状态 diff → windowManager.broadcast('media-task:changed', task)——广播源，确认 completed/failed 终态必经此路径且仅状态变化时触发一次）
  </read_first>
  <action>
    在 mediaAPI 命名空间内、onMediaTaskCountChanged 之后新增条目 `onMediaTaskChanged`：签名 `(callback) => { const handler = (event, data) => callback(data); ipcRenderer.on('media-task:changed', handler); return () => ipcRenderer.removeListener('media-task:changed', handler); }`，JSDoc 注明「监听媒体任务状态变化（主窗口终态 toast 数据源，G-44-5）：参数为任务对象快照（含 id/type/status/title/outputPath/error），不过滤类型——调用方自行过滤」。
    不做类型/状态过滤（与 playerAPI.onRecordStateChanged 的 record 过滤分工：主窗口 toast 需要 convert + record 两类终态）。
    preload 其余部分零改动（realmAPI/playerAPI/downloadAPI 均不动）。
  </action>
  <verify>
    <automated>node --check src/preload.js && [ "$(grep -c 'onMediaTaskChanged' src/preload.js)" -ge 1 ] && grep -q "media-task:changed" src/preload.js</automated>
    <fails_when>node --check 语法错误非零退出；或 preload.js 无 onMediaTaskChanged 条目（计数 0 → gate 非零）；或 media-task:changed 通道监听缺失（grep 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：mediaAPI 内存在 onMediaTaskChanged，监听 media-task:changed 并返回取消监听函数（与 onMediaTaskCountChanged 同款契约）
    - 行为语义：主窗口 renderer 可经 window.mediaAPI.onMediaTaskChanged 收到全部 media-task:changed 广播（record + convert）
    - 回归：preload 其余命名空间零改动
  </acceptance_criteria>
  <done>media-task:changed 通道在主窗口 renderer 可订阅（preload 暴露面补齐）。</done>
</task>

<task type="auto">
  <name>Task 2: renderer 终态 toast（showToast 扩展 + initMediaTaskToast + 点击定位）</name>
  <files>src/renderer.js, src/styles/main.css, src/index.html</files>
  <read_first>
    - src/renderer.js:4566-4592（showToast 现状：.toast/.toast-{type}/visible 动画/3 秒自动消失——扩展点；textContent 写入已天然防注入）
    - src/renderer.js:4200-4210（initMediaTaskBadge() 调用点——initMediaTaskToast() 接线位置）与 :10538-10557（initMediaTaskBadge 定义，参考其监听注册风格）
    - src/preload.js:1735（downloadAPI.showInFolder → 'download:show-in-folder'）与 ipc-handlers.js:1118-1124（该 IPC 的 assertTrustedSender + 字符串校验——点击定位复用的既有链路）
    - main.js:2065-2097（showTaskNotification 文案契约：completed convert「MP4 转换完成：{文件名}」/ completed record「录制已保存」/ failed 用 task.error（44-05 起已带「MP4 转换失败：」前缀，勿重复拼接））
    - src/styles/main.css:1445-1475（.toast 段——toast-clickable 追加位置）与 src/index.html:11（main.css?v=5 缓存版本号）
    - .planning/phases/44-/44-UAT.md G-44-5（truth 与推荐方案）
  </read_first>
  <action>
    1. showToast（:4569）签名扩展为 `function showToast(message, type, opts = {})`：`const { onClick, duration = 3000 } = opts || {};`——自动消失 setTimeout 用 duration 替换硬编码 3000；onClick 为函数时给 toast 挂 click 监听（点击执行 onClick）并加 'toast-clickable' 类。缺省行为逐字节不变（既有全部调用点不传第三参）。
    2. 新增 initMediaTaskToast()（放在 initMediaTaskBadge 定义附近，:10538 区）：`if (!(window.mediaAPI && window.mediaAPI.onMediaTaskChanged)) return;` 注册监听；回调内仅处理 `task && (task.status === 'completed' || task.status === 'failed')`：
       - 文案映射（与 showTaskNotification 标题契约一致）：completed convert → 「MP4 转换完成：{path.basename 式文件名}」（从 task.outputPath 取 basename，无 outputPath 回落 task.title）；completed record → 「录制已保存」；failed → task.error || '任务失败'（error 已带前缀，不重复拼接）。
       - type：failed → 'error'，completed → 'success'。
       - onClick：task.outputPath 存在时 `() => { if (window.downloadAPI && window.downloadAPI.showInFolder) window.downloadAPI.showInFolder(task.outputPath); }`，无 outputPath 不挂点击。
       - duration 用 5000（终态提示停留久于普通 3s toast）。
    3. 接线：:4206 `initMediaTaskBadge();` 旁调用 `initMediaTaskToast();`（同处初始化，仅主窗口执行一次）。
    4. src/styles/main.css .toast 段（:1445 附近）追加 `.toast.toast-clickable { cursor: pointer; }`；src/index.html:11 版本参数 v=5 → v=6（main.css 变更对 file:// 主窗口的缓存失效，沿既有 ?v=N 惯例）。
    5. 不改 main.js（系统通知链路原样保留；锁定见 prohibitions）。
  </action>
  <verify>
    <automated>node --check src/renderer.js && grep -q 'function initMediaTaskToast' src/renderer.js && grep -q 'initMediaTaskToast();' src/renderer.js && grep -q 'onMediaTaskChanged' src/renderer.js && grep -q 'toast-clickable' src/styles/main.css && grep -q 'main.css?v=6' src/index.html && ! grep -q 'main.css?v=5' src/index.html</automated>
    <fails_when>node --check 语法错误非零退出；或 initMediaTaskToast 定义/调用/监听注册任一缺失（对应 grep 非零）；或 main.css 无 toast-clickable 规则；或 index.html 版本号未升到 v=6 或仍残留 v=5（缓存未失效）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：showToast 第三参为可选（既有调用点零改动、缺省 3000ms 行为不变）
    - 源码断言：initMediaTaskToast 过滤 completed/failed 终态、文案映射与 showTaskNotification 契约一致、failed 不重复拼接「MP4 转换失败：」前缀
    - 行为断言：有 outputPath 的终态 toast 可点击并经 download:show-in-folder IPC 定位 Finder；无 outputPath 仅提示
    - 回归：showToast 全部既有调用方行为不变；main.js 零改动（git diff 确认）
  </acceptance_criteria>
  <done>convert/record 终态在主窗口弹应用内 toast（与系统通知文案一致），点击可定位产物；系统通知代码保留，正式签名后两者并存。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主进程 media-task:changed 广播 → 主窗口 renderer | 任务快照（title/error/outputPath）进入 renderer 展示面 |
| renderer toast click → download:show-in-folder IPC | renderer 传入任意字符串路径请求 Finder 定位 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G10-01 | Tampering | task.error/title 注入 toast 文本 | low | mitigate | showToast 用 textContent 写入（无 innerHTML），纯文本展示无注入面 |
| T-44-G10-02 | Elevation | toast click 携带任意路径调 showInFolder | low | mitigate | 复用既有 download:show-in-folder 通道（assertTrustedSender + 字符串校验，ipc-handlers.js:1118），路径来源为主进程注册表的任务产物而非远端输入；与既有下载定位同信任级 |
| T-44-G10-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包，不触发包合法性 gate |

无新增 high 威胁（toast 为纯展示 + 既有受信 IPC 复用）。
</threat_model>

<verification>
1. `node --check src/preload.js && node --check src/renderer.js` 全过
2. `node tests/test-media-task-registry.js`（26/26）与 `node tests/test-media-remuxer.js` 回归不红（主进程零改动的间接确认——git diff --stat main.js 为空）
3. 源码 gate：preload onMediaTaskChanged / renderer initMediaTaskToast + 调用点 / main.css toast-clickable / index.html v=6
4. 人工复测（UAT 第 5 项）：发起转换（或录制→停止）→ 主窗口右上角出现「MP4 转换完成：{文件名}」toast，点击在 Finder 定位产物；失败任务弹 error toast
</verification>

<success_criteria>
- G-44-5 truth 达成：convert/record 终态有可见的应用内提示（不依赖系统通知授权），点击定位产物链路可用
- 系统通知链路保留（正式签名后恢复即得双通道）
- showToast 既有调用方零回归；main.js 零改动
</success_criteria>

<output>
Create .planning/phases/44-/44-10-SUMMARY.md when done
</output>
