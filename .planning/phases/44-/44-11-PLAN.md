---
phase: 44-player-video-cache-and-local-media-library
plan: 11
type: execute
wave: 2
depends_on: ["44-09"]
files_modified:
  - media-remuxer.js
  - main.js
  - tests/test-media-remuxer.js
autonomous: true
gap_closure: true
gap_ids: [G-44-4b]
requirements: [D-04, D-24]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "convertToMp4 对不可转封装格式显式 reject（err.reason: unsupported_container / encrypted_stream），不再静默产出 0 字节文件并伪装 completed（G-44-4b）"
    - "fMP4 分片（moof/ftyp box 开头、无 0x47 同步字节，实测 B 站直播 miniav1 流）被前置嗅探拒绝，reason=unsupported_container"
    - "AES-128 密文分片（高熵、无同步字节，实测 hn.bfvvs.com 缓存）被拒绝，reason=encrypted_stream"
    - "转封装结束校验产物字节数 >0，0 字节产物按失败处理（reason=empty_output）并清理半成品"
    - "任务页失败文案覆盖三个新 reason（CONVERT_FAIL_TEXT 映射），用户看到明确原因而非「未知原因」"
    - "正常 MPEG-TS 分片的转封装路径零回归（嗅探对 0x47 开头分片放行；shouldCancel 取消检查仍先于嗅探——44-08 取消契约不变）"
  artifacts:
    - media-remuxer.js 新增 sniffContainerFormat（首分片前 64KB 嗅探：0x47 同步字节 / fMP4 box 特征 / 高熵密文判定）
    - media-remuxer.js convertToMp4 首分片 push 前嗅探检查点 + stream.end 回调产物字节数校验（empty_output）
    - main.js CONVERT_FAIL_TEXT 新增 unsupported_container / encrypted_stream / empty_output 三条文案
    - tests/test-media-remuxer.js 新增嗅探/零产物契约单测（fMP4 拒转、高熵拒转、TS 放行回归、取消优先序）
  key_links:
    - startConvertTask / startConvertFromRecordTask → convertToMp4 → fail(reason) → CONVERT_FAIL_TEXT[reason] → failTask 文案 → 任务页 task-error 展示
    - 录制/缓存分片落盘（.ts 命名）→ convertToMp4 嗅探拒绝 → 任务 failed（而非 completed + 0 字节产物）
  prohibitions:
    - statement: 禁止在本阶段实现 AES-128 解密或 fMP4 init+frag 拼接（用户判定为可选增强，记债务；本阶段只做显式拒转）
      status: locked
      verification: media-remuxer.js 无解密/拼接实现（嗅探仅 reject，不含 crypto 解密或 box 级重组逻辑）
    - statement: 44-08 shouldCancel 取消契约不得回归（取消检查先于嗅探，取消测试占位分片无需真实 TS 载荷）
      status: locked
      verification: tests/test-media-remuxer.js 既有 shouldCancel 用例不改 fixture 且全绿
---

<objective>
修复 G-44-4b（major，合并 UAT 第 6 项同根因）：停止录制/缓存完成后转出的 mp4 是 0 字节。实测三类分片两种非 TS 格式——① B 站直播 miniav1 流为 HLS+fMP4（moof box 开头无 0x47），mux.js Transmuxer 仅支持 MPEG-TS，push 不报错也不触发 data 事件 → convertToMp4 静默 resolve 产出 0 字节；② hn.bfvvs.com 缓存分片为 AES-128 密文（高熵无同步字节）同样 0 字节；③ convertToMp4 无格式嗅探与零产物校验，两种不可转场景均伪装成 completed。修复 = 转封装前置格式嗅探（0x47 / fMP4 box / 高熵三判定）+ 不可转格式显式 reject + 产物字节数终检 + 任务页明确失败文案。AES-128 解密与 fMP4 拼接为可选增强，本阶段不做（用户判定，记债务）。

Purpose: 让「不能转」变成「明确说不能转」，杜绝 0 字节假产物；失败原因直达任务页。
Output: 嗅探器 + 拒转/终检契约 + 文案映射 + 单测。

**已知债务（本 plan 显式记录）**：① AES-128 EXT-X-KEY 解密支持；② fMP4 init+frag 拼接支持——二者为可选增强，随后续需求单独规划（G-44-4b missing 第 3 项明示「短期至少拒转提示」已满足）。WR-A/B/C/05/01-04、IN-01-04 未点名项维持债务。

**tracer 说明**：gap-closure 小计划；修复即最小端到端路径（嗅探→拒转→文案→任务页），verify 由单测 + 源码 gate 承担。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-UAT.md
@.planning/phases/44-/44-08-PLAN.md

@media-remuxer.js
@main.js
@tests/test-media-remuxer.js
</context>

<artifacts_this_phase_produces>
- media-remuxer.js：模块级 `sniffContainerFormat(filePath)`（返回 null=TS 放行 或 remuxError；常量 SNIFF_READ_BYTES=64KB、ENTROPY_SAMPLE_BYTES=4096、ENTROPY_DISTINCT_THRESHOLD）；convertToMp4 循环内首分片（index 0）push 前嗅探检查点（fail 复用清理）；stream.end 回调产物 statSync 字节数校验（empty_output → unlink + reject）；JSDoc 失败原因清单扩展 unsupported_container/encrypted_stream/empty_output
- main.js：CONVERT_FAIL_TEXT 新增三条（unsupported_container: '分片格式暂不支持转换（仅支持 MPEG-TS）' / encrypted_stream: '分片为加密数据，暂不支持转换' / empty_output: '转换产物为空'）
- tests/test-media-remuxer.js：「convertToMp4 格式嗅探（G-44-4b）」describe：fMP4 拒转 / 高熵拒转 / 0x47 放行进入既有路径 / 取消先于嗅探
- 无新模块、无新依赖（crypto 仅测试用）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 转封装前置格式嗅探 + 零产物终检（unsupported_container / encrypted_stream / empty_output）</name>
  <files>media-remuxer.js</files>
  <read_first>
    - media-remuxer.js 全文（:98-191 convertToMp4：入参解构 :98、pre-check 直接 reject 区 :100-117、fail 清理 :126-133、data/error 监听 :139-157、顺序 push 循环 :159-183——含 shouldCancel 检查点 :167-170（44-08 契约：取消先于一切分片消费）、stream.end 回调 :185-189）、:64-74 remuxError、:87-97 JSDoc 失败原因清单
    - main.js:3002-3011（CONVERT_FAIL_TEXT 现状）与 :3131-3143（.catch 文案映射——reason → failTask 文案的消费端）
    - .planning/phases/44-/44-UAT.md G-44-4b（root_cause 三类分片实测数据：moof 开头无 0x47、AES-128 高熵、静默 0 字节）
  </read_first>
  <action>
    1. 新增模块级嗅探函数 `sniffContainerFormat(filePath)`（置于 remuxError 之后、convertToMp4 之前），JSDoc 说明「仅判别首分片容器格式；录制/缓存同一清单的分片格式齐一，嗅探首片即可代表全流」：
       - 打开 fd 读首部最多 64KB（fs.openSync + fs.readSync + fs.closeSync，try/finally 关 fd；文件小于 64KB 按实际长度）。
       - 判定序：① 首字节 0x47 → 返回 null（MPEG-TS，放行；不做 188 步进强校验——首包对齐存在变体，保持宽松避免误杀）；② 缓冲区内可检索到 fMP4 box 特征 ASCII（'ftyp' / 'styp' / 'moof' / 'moov' / 'sidx' 任一，Buffer.indexOf）→ 返回 remuxError('unsupported_container', 'fMP4 分片暂不支持转封装（仅支持 MPEG-TS）')；③ 取前 4096 字节统计不同字节值个数，≥240（256 值域近满覆盖=高熵）→ 返回 remuxError('encrypted_stream', '分片疑似加密数据（如 AES-128），暂不支持转封装')；④ 其余 → 返回 remuxError('unsupported_container', '未知分片格式，仅支持 MPEG-TS')。
    2. convertToMp4 循环（:159-183）改造为带索引迭代（for...of 改 for (let i = 0; i < segmentPaths.length; i++) 或 entries()）：在 shouldCancel 检查点（:167-170，保持其先于嗅探——44-08 取消契约与占位分片测试依赖此序）之后、`transmuxer.push(` 之前插入首片嗅探检查点：`if (i === 0) { const sniffErr = sniffContainerFormat(tsFile); if (sniffErr) { fail(sniffErr); return; } }`——fail 复用既有清理（destroy stream + unlink 半成品），错误对象直接携带 reason/detail。循环内其余逻辑（push/flush/processed/onProgress）不动。
    3. stream.end 回调（:185-189）插入产物终检：settled 守卫后先 `fs.statSync(outputPath).size`（try/catch 异常按 0 处理），size === 0 时：置 settled、尝试 unlinkSync 清理空产物、reject(remuxError('empty_output', '转封装产物为空（无有效媒体数据）'))；size > 0 才走原 resolve（settled 置位与 resolve 保持原序）。
    4. JSDoc（:87-97）：失败原因清单追加 unsupported_container（分片非 MPEG-TS 容器，如 fMP4）/ encrypted_stream（分片疑似 AES-128 加密数据）/ empty_output（转封装无有效输出）；@param 区补一行嗅探语义说明（首分片判定、0x47 放行）。
    5. 注释纪律：嗅探判定注释按 ①②③④ 分行写实义（同步字节/box 特征/熵判定），**不要写「0 字节」「零字节」字样于 will-be-rejected 语义注释中**（下方 gate 以 empty_output 正向断言，避免反义混淆）。
    6. 不实现解密或 fMP4 拼接（锁定见 prohibitions）；不改动 fail()、data/error 监听、单实例/流式写盘结构断言对象。
  </action>
  <verify>
    <automated>node --check media-remuxer.js && grep -q 'function sniffContainerFormat' media-remuxer.js && grep -q "unsupported_container" media-remuxer.js && grep -q "encrypted_stream" media-remuxer.js && grep -q "empty_output" media-remuxer.js && node -e "require('./media-remuxer'); console.log('pure-node ok')"</automated>
    <fails_when>node --check 语法错误非零退出；或嗅探函数/三个新 reason 任一缺失（对应 grep 非零）；或 pure-node ok 未输出（引入了 electron/原生依赖——去 Electron 化硬约束被破坏）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（Task 2 单测承载）：moof/ftyp 分片 → reject err.reason='unsupported_container'；高熵分片 → 'encrypted_stream'；0x47 首字节分片 → 嗅探放行进入原转封装路径
    - 源码断言：嗅探检查点位于 shouldCancel 检查之后、transmuxer.push 之前（取消契约优先序）
    - 源码断言：stream.end 回调含产物 statSync 字节数校验，0 字节走 reject + unlink 清理
    - 结构回归：data 监听先于 push、单实例、流式写盘的既有结构断言对象未被移动破坏
  </acceptance_criteria>
  <done>不可转格式（fMP4/密文）显式 reject 且半成品被清理；0 字节产物按 empty_output 失败处理；TS 路径与取消契约零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 嗅探契约单测 + CONVERT_FAIL_TEXT 文案映射</name>
  <files>tests/test-media-remuxer.js, main.js</files>
  <read_first>
    - tests/test-media-remuxer.js 全文结构（node:test describe/test 惯例、mkdtemp 临时目录写法、convertToMp4 契约 describe 内「shouldCancel 即拒转」用例 A/B 的占位分片写法——占位文件内容任意，因取消检查先于读文件；本任务新增用例沿用同目录习惯但需真实嗅探载荷）、:1-11 文件头注释（覆盖清单需补嗅探条目）
    - main.js:3002-3011（CONVERT_FAIL_TEXT 对象字面量——三条新文案的插入位置）与 :3140-3142（reasonText 映射消费——确认无硬编码 reason 清单需同步）
    - .planning/phases/44-/44-08-PLAN.md Task 2（shouldCancel 用例 A/B 的原始契约——fixture 与断言不得改动）
  </read_first>
  <behavior>
    - Test 1: 首分片以 fMP4 box 开头（手工构造：4 字节 size + 'ftyp' + 'isom' + 填充）→ convertToMp4 reject err.reason === 'unsupported_container'，且 outputPath 无残留文件
    - Test 2: 首分片为高熵数据（crypto.randomBytes(4096) 后强制首字节为非 0x47 的固定值，消除随机撞 0x47 的偶发）→ reject err.reason === 'encrypted_stream'
    - Test 3: 首分片首字节 0x47（Buffer.alloc(188, 0x47)）→ 嗅探放行，错误语义落到既有路径（转封装失败 transmux_failed——伪 TS 无法产出有效 fMP4，但绝不能是 unsupported_container/encrypted_stream，证明嗅探未误杀 TS）
    - Test 4: shouldCancel=true + 任意内容占位分片 → 仍 err.reason === 'cancelled'（取消先于嗅探——44-08 契约回归）
  </behavior>
  <action>
    1. tests/test-media-remuxer.js convertToMp4 契约 describe 之后新增 describe「convertToMp4 格式嗅探（G-44-4b）」，按 behavior 四用例实现：mkdtemp 临时目录写分片文件，assert.rejects 断言 err.reason 与 outputPath 清理；文件头注释（:1-11）覆盖清单补嗅探条目。
    2. main.js CONVERT_FAIL_TEXT（:3002-3011）追加三条映射：unsupported_container → '分片格式暂不支持转换（仅支持 MPEG-TS）'；encrypted_stream → '分片为加密数据，暂不支持转换'；empty_output → '转换产物为空'。映射消费端（:3140 reasonText）与 convert-resume 端点无需改动（通用映射已覆盖）。
    3. 跑全量 remuxer 测试确认 44-08 既有用例（含占位分片取消用例）全绿。
  </action>
  <verify>
    <automated>node --check tests/test-media-remuxer.js && node --check main.js && node tests/test-media-remuxer.js && grep -q "unsupported_container: '" main.js && grep -q "encrypted_stream: '" main.js && grep -q "empty_output: '" main.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 tests/test-media-remuxer.js 非零退出（fail ≥ 1，含新增嗅探用例或 44-08 既有用例回归）；或 main.js CONVERT_FAIL_TEXT 缺任一新条目（对应 grep 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（单测）：behavior 四用例全部成立（fMP4/高熵拒转 + TS 放行 + 取消优先序）
    - 行为断言（单测）：拒转后 outputPath 无文件残留（fail 清理复用生效）
    - 源码断言：CONVERT_FAIL_TEXT 覆盖三个新 reason（任务页文案不再落「未知原因」）
    - 回归：test-media-remuxer.js 全量（既有 15+ 用例 + 新增）全绿；test-media-task-registry.js 全绿
  </acceptance_criteria>
  <done>fMP4/密文/零产物三类失败在任务页有明确中文文案；嗅探契约被单测锁定；既有取消/结构契约零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源站/录制分片文件（不可信内容）→ convertToMp4 嗅探 | 远端流分片内容进入主进程同步读取路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G11-01 | DoS | 嗅探对超大/畸形分片同步读 64KB | low | mitigate | 读取上限固定 64KB（fs.readSync 定长），无循环拼接无内存放大；分片尺寸本受录制/缓存固有上限约束 |
| T-44-G11-02 | Tampering | 伪装成 TS（0x47 开头）的恶意载荷绕过嗅探进入 mux.js | low | accept | mux.js 纯 JS 解析无原生内存风险；解析异常由 transmuxer 'error' 监听兜底（既有 fail 路径）；本 plan 目标是拒不可转格式而非内容安全审计 |
| T-44-G11-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包（测试用 crypto 为 Node 内置），不触发包合法性 gate |

无新增 high 威胁（嗅探缩小了进入 mux.js 的输入集，攻击面净减少）。
</threat_model>

<verification>
1. `node --check media-remuxer.js && node --check main.js` 全过
2. `node tests/test-media-remuxer.js`（既有 + 新增嗅探四用例）与 `node tests/test-media-task-registry.js` 全绿；`node tests/test-media-cache.js` 回归不红
3. `node -e "require('./media-remuxer'); console.log('pure-node ok')"` 输出正常（去 Electron 化不变）
4. 人工复测（UAT 第 4/6 项）：B 站直播（fMP4 流）录制停止 → 任务页显示「MP4 转换失败：分片格式暂不支持转换（仅支持 MPEG-TS）」且无 0 字节产物；正常 TS 直播源录制 → 转换产物可播放（原路径回归）
</verification>

<success_criteria>
- G-44-4b truth 达成：不可转格式显式报错（unsupported_container/encrypted_stream/empty_output），0 字节假产物不再出现（无法转换时拒绝而非静默）
- 任务页失败文案明确（三个新 reason 均有中文映射）
- 正常 TS 转封装路径、44-08 取消契约、结构断言零回归
- AES-128 解密与 fMP4 拼接未实现（锁定为可选增强债务）
</success_criteria>

<output>
Create .planning/phases/44-/44-11-SUMMARY.md when done
</output>
