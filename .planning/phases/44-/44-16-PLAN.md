---
phase: 44-player-video-cache-and-local-media-library
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - media-record-engine.js
  - tests/test-media-record-duration.js
  - src/player.js
  - src/player.css
autonomous: true
gap_closure: true
gap_ids: [G-44-4, G-44-6]
requirements: [D-21, D-16]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "录制中红点 hover 的时长按本地挂钟平滑推进（每秒 +1s），与分片落盘节奏解耦——0 分片落盘时也在走表（G-44-4）"
    - "终态/已落盘任务的时长口径不变：meta.json totalDuration 仍为分片 EXTINF 累计（duration 缺失兜底 targetDuration），续转/转封装顺序索引不受影响（G-44-4 missing 明示口径保持）"
    - "缓存抽屉条目 meta 行为「时钟图标 + 时间」并排常显：图标只替代「最近观看」四个字，时间文本始终可见（非仅 hover）；title 保留完整「最近观看 时间」作补充提示（G-44-6）"
  artifacts:
    - media-record-engine.js startRecord 的 st 增加 startedAt 时间戳 + getRecordStatus（:296）/ getActiveRecordings（:330）两处同式改为时钟差值
    - tests/test-media-record-duration.js（Wave 0 风格新单测，纯 Node 可跑）
    - src/player.js:994-1001 watchedIcon span 内图标后追加时间文本节点
    - src/player.css:636 注释同步 + .drawer-item-watched 图标与文本间距
  key_links:
    - st.startedAt（startRecord 时刻 Date.now()）→ getRecordStatus/getActiveRecordings 时钟差值 → player.js:898 tooltip 直显（UI 层零改动跟随新数据源）
    - meta.json totalDuration（writeMeta :167-180 EXTINF 累计）↔ 运行中 durationSeconds——两口径彻底分离，互不渗透
  prohibitions:
    - statement: 禁止改 writeMeta/totalDuration 的 EXTINF 累计口径与 meta.json 结构（META_VERSION 不升）——终态任务页时长、续转、转封装都消费它
      status: locked
      verification: writeMeta 函数体零 diff；44-05/44-07 消费端测试全绿
    - statement: 禁止动「关闭播放器窗口时」设置项与 D-19 关窗确认链路（UAT test 8 pass，不要动它）
      status: locked
      verification: settings.html / src/settings-page.js / ipc-handlers.js 关窗确认分支零 diff
---

<objective>
修复 G-44-4（minor）：红点 hover 时长抖动——根因是 getRecordStatus 的 durationSeconds = 分片数 × targetDuration（media-record-engine.js:296，量化到分片落盘事件，粒度 ≥2s 且受网络抖动不均）。改为基于 startRecord 时刻的本地时钟差值平滑推进；getActiveRecordings（:330）同式同步改（D-19 关窗确认、红点同步共用）；meta.json/终态 EXTINF 累计口径逐字节不变。
修复 G-44-6（major）：commit 44a24ef（44-12/G-44-8）把「最近观看 {时间}」整段收进 tooltip 是需求歧义偏差——用户真实意图是图标只替代「最近观看」四个字、时间文本常显。修复 = renderDrawer（src/player.js:983-1001）保留时钟图标 span 并在其后追加时间文本节点；title 保留完整提示；player.css:636 注释同步。

Purpose: 时长显示是录制功能唯一的实时反馈面；抽屉 meta 行时间常显是观看历史的核心信息。
Output: 时钟差值 durationSeconds（含新单测）+ 抽屉「图标 + 时间」并排渲染。

**tracer 说明**：gap-closure 小计划，无独立 tracer 收益。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/debug/record-duration-timer-lag.md
@.planning/debug/watch-time-icon-replaced.md
@.planning/phases/44-/44-04-SUMMARY.md
@media-record-engine.js
@src/player.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 运行中时长改本地时钟差值（G-44-4，含新单测）</name>
  <files>media-record-engine.js, tests/test-media-record-duration.js</files>
  <behavior>
    - 测试 1（时钟驱动）：createRecordEngine 注入永不 resolve 的 fetchPage（0 分片落盘）+ 极简 parsePlaylist → startRecord 后约 1.3s 调 getRecordStatus：durationSeconds ≥ 1 且 ≤ 2（挂钟推进，与分片数无关）；再等约 1.1s 复查：durationSeconds 严格递增
    - 测试 2（两处同式）：同一任务 getActiveRecordings()[0].durationSeconds 与 getRecordStatus(id).durationSeconds 差值 ≤ 1s
    - 测试 3（终态口径不变）：fetchPage 返回固定 TS buffer + parsePlaylist 返回 duration=3 的分片，录制数秒后 stopRecord → meta.json 的 totalDuration = 分片 EXTINF 累计（非挂钟值），segments 数与落盘分片一致
    - 测试 4（缺 duration 兜底）：分片无 EXTINF 时 meta totalDuration 用 targetDuration 兜底累计（既有语义回归保护）
  </behavior>
  <action>
先写 tests/test-media-record-duration.js（风格对齐 tests/test-media-task-registry.js：node assert + 临时 recordRoot 目录 + 注入 fetchPage/parsePlaylist/taskManager），跑一遍确认红。然后实现：
① startRecord 的 st（media-record-engine.js:102-118）增加 startedAt: Date.now() 字段；
② getRecordStatus 运行中分支（:296）与 getActiveRecordings 映射（:330）的分片数×targetDuration 乘积式，统一替换为 Math.max(0, Math.round((Date.now() - st.startedAt) / 1000))——Math.max(0) 容忍时钟微调；
③ 同步两处 JSDoc（:282/:319）：运行中 durationSeconds 语义改为「本地挂钟差值（平滑显示用）」，并注明与 meta.json totalDuration（EXTINF 累计）口径分离。
禁止改 writeMeta/totalDuration/stopRecord/failTask（终态口径 locked，见 prohibitions）；player.js:898 tooltip 直显该值，UI 层零改动。
完成后基线回归：node tests/test-media-task-registry.js 与 node tests/test-media-cache.js 全绿。
  </action>
  <verify>
    <automated>node tests/test-media-record-duration.js && node tests/test-media-task-registry.js && ! grep -n "recorded.size \* st.targetDuration" media-record-engine.js</automated>
  </verify>
  <done>新单测 4 例全绿；运行中时长挂钟平滑推进；0 分片也在走表；终态 EXTINF 口径与注册表既有测试无回归。</done>
</task>

<task type="auto">
  <name>Task 2: 抽屉 meta 行「时钟图标 + 时间」并排常显（G-44-6）</name>
  <files>src/player.js, src/player.css</files>
  <action>
改 src/player.js:994-1001（renderDrawer meta 行）：保留 watchedIcon span（class drawer-item-watched）及其 SVG 时钟图标，在图标节点之后、同一 span 内追加时间文本节点（document.createTextNode(watched)，watched 即 :993 formatWatchedTime 产物）——图标 + 时间并排常显。title 保留完整提示 `最近观看 ${watched}`（hover 补充说明语义不变）。
改 src/player.css:636 注释为「G-44-6：时钟图标 + 时间并排常显（图标只替代「最近观看」文字，时间文本可见）」，并给 .drawer-item-watched 增加 gap: 4px（inline-flex 已就位，间距用 gap 不用 margin）。
遵循项目规范：renderer 侧 DOM API 操作，无 fetch/Node 全局；改完 node --check。
禁止：不动 meta 行前段（缓存大小 · 完整度 / 未缓存）、不动删除确认链路（pendingDeleteTitle/checkbox 联动）、不动抽屉其他区块。
  </action>
  <verify>
    <automated>node --check src/player.js && grep -n "createTextNode(watched)" src/player.js && grep -n "gap: 4px" src/player.css</automated>
  </verify>
  <done>抽屉条目 meta 行时钟图标与时间文本并排常显，title 完整提示保留，注释与实现一致。</done>
</task>

</tasks>

<verification>
1. `node tests/test-media-record-duration.js` 4 例全绿；`node tests/test-media-task-registry.js && node tests/test-media-cache.js` 基线无回归。
2. `grep -n "recorded.size \* st.targetDuration" media-record-engine.js` 零命中（两处乘积式均已替换）；`git diff media-record-engine.js` 中 writeMeta 函数体零改动。
3. `node --check src/player.js && node --check src/player.css 2>/dev/null || true`；src/player.js diff 仅限 meta 行 watchedIcon 区块。
4. human-check：npm run dev 真机——录制中 hover 红点时长每秒平滑 +1（含弱网点播源 0 分片场景走表）；打开缓存抽屉确认「图标 + 时间」并排常显、hover 有完整提示。
</verification>

<success_criteria>
运行中录制时长由本地时钟驱动平滑推进且终态 EXTINF 口径不变（两口径分离有单测护栏）；抽屉 meta 行恢复用户澄清的真实意图（图标替代标签文字、时间常显）。
</success_criteria>

<output>
Create `.planning/phases/44-/44-16-SUMMARY.md` when done
</output>
