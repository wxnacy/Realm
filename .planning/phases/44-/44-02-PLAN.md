---
phase: 44-player-video-cache-and-local-media-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - media-m3u8-parser.js
  - media-task-manager.js
  - tests/test-m3u8-playlist-parser.js
  - tests/test-media-task-registry.js
autonomous: true
requirements: [D-18, D-22, D-25]
estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - 直播/VOD m3u8 清单可被纯函数解析出 MEDIA-SEQUENCE、TARGETDURATION、分片序列号列表与 ENDLIST 标记（录制引擎的唯一解析来源）
    - 媒体任务注册表状态机 running→completed/failed/cancelled/interrupted 合法流转、非法流转被拒（D-25）
    - 注册表经注入回调持久化，重启恢复时 running 任务标记 interrupted 且已落盘产物可追溯（D-18/D-25）
    - record 任务 completed 时触发 convert 接力回调钩子（D-22 的派生点）
    - 两个模块顶层无 electron/原生 require，测试纯 Node 可跑（44-VALIDATION Wave 0 关键前置）
  artifacts:
    - media-m3u8-parser.js（parsePlaylist/resolveUri 纯函数）
    - media-task-manager.js（createMediaTaskManager({ persist, now })，registerTask/updateProgress/completeTask/failTask/cancelTask/listTasks/restoreTasks/onTaskCompleted/hasActiveTasks/isVideoActive）
    - tests/test-m3u8-playlist-parser.js
    - tests/test-media-task-registry.js
  key_links:
    - media-m3u8-parser → 44-04 media-record-engine（轮询追分片的序列号去重依据）
    - media-task-manager → 44-03 main.js 集成（persist 落 userData/media-tasks.json、isVideoActive 注入 media-cache-manager 构造参数实现 D-07 淘汰豁免）与 44-04 record/convert 任务
  prohibitions:
    - statement: 不引 hls-parser 等清单解析依赖，手写行级解析（RESEARCH Don't Hand-Roll 反向约束）
      status: locked
      verification: package.json 无新增依赖；media-m3u8-parser.js 仅用 split/regex
---

<objective>
交付两个去 Electron 化的纯逻辑模块：m3u8 行级解析器（直播录制引擎的解析地基）与 media-task-manager 统一任务注册表（D-25 状态机 + 持久化 + record→convert 接力钩子），并按 44-VALIDATION Wave 0 要求补齐对应单测。

Purpose: 录制引擎（44-04）、任务中心（44-03）、转封装（44-05）全部消费这两个模块；先以纯模块 + 单测定形契约，上层集成才有稳定依赖。
Output: media-m3u8-parser.js、media-task-manager.js、tests/test-m3u8-playlist-parser.js、tests/test-media-task-registry.js。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-/44-CONTEXT.md
@.planning/phases/44-/44-RESEARCH.md
@.planning/phases/44-/44-PATTERNS.md
@.planning/phases/44-/44-VALIDATION.md

@main.js
@download-manager.js
@tests/test-ai-bash-policy.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-m3u8-parser.js`：`parsePlaylist(text)` → `{ mediaSequence, targetDuration, ended, isLive, segments: [{ uri, seq, duration }] }`；`resolveUri(uri, baseUrl)` → 绝对 URL
- `media-task-manager.js`：`createMediaTaskManager({ persist, now })` → `{ registerTask, updateProgress, completeTask, failTask, cancelTask, markInterrupted, listTasks, hasActiveTasks, isVideoActive, onTaskCompleted }`；任务对象形状 `{ id, type: 'record'|'convert', title, containerId, playbackKey?, status: 'running'|'completed'|'failed'|'cancelled'|'interrupted', progress, outputPath, error, createdAt, updatedAt }`（D-25；playbackKey 由 record 任务传入，支撑 D-07 逐视频豁免）
- `tests/test-m3u8-playlist-parser.js`、`tests/test-media-task-registry.js`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: media-m3u8-parser.js — 行级清单解析器 + 单测</name>
  <files>media-m3u8-parser.js, tests/test-m3u8-playlist-parser.js</files>
  <read_first>
    - main.js:234-265（rewriteM3u8ForProxy——行级 split + 正则解析的既有先例，风格照此）
    - .planning/phases/44-/44-RESEARCH.md（「直播录制轮询」Code Example：MEDIA-SEQUENCE/ENDLIST/TARGETDURATION 提取逻辑）
    - tests/test-ai-bash-policy.js（node:test + describe/test 结构）
    - .planning/phases/44-/44-VALIDATION.md（44-W0-parser 覆盖要求）
  </read_first>
  <behavior>
    - 输入含 `#EXT-X-MEDIA-SEQUENCE:100` 与 3 条分片行 → segments 为 [{seq:100},{seq:101},{seq:102}]（RESEARCH 示例的 mediaSeq + i 语义）
    - 含 `#EXT-X-ENDLIST` → ended=true、isLive=false；不含 → ended=false、isLive=true
    - `#EXT-X-TARGETDURATION:6` → targetDuration=6；缺失 → 缺省 6
    - `#EXT-X-KEY:...URI="k.key"` 等标签行与空行不产生分片；`#` 开头注释行忽略
    - `#EXTINF:5.0` 后随分片行 → 该分片 duration=5.0
    - resolveUri('seg1.ts','https://h/live.m3u8') → 'https://h/seg1.ts'；绝对 URL 原样返回
  </behavior>
  <action>
  纯函数模块（无 IO、无 require 依赖）：`parsePlaylist(text)` 用 text.split('\n') 行处理——`#EXT-X-MEDIA-SEQUENCE:(\d+)` 取基线序号、`#EXT-X-TARGETDURATION:(\d+)` 取时长、`EXT-X-ENDLIST` 出现即 ended、非 `#` 开头的非空行计为分片并按 mediaSequence + index 编 seq；`#EXTINF:([\d.]+)` 紧邻前缀归属下一条分片。不引任何 npm 解析库（RESEARCH 定稿：录制只需四个标签，手写 ~40 行）。导出 resolveUri 供录制引擎把相对分片 URI 转绝对 URL。测试文件照 test-ai-bash-policy.js 结构组织 describe 分组。
  </action>
  <verify>
    <automated>node tests/test-m3u8-playlist-parser.js</automated>
    <fails_when>非零退出码，或 node:test 汇总出现 "fail 1" 及以上，或 "pass 0"</fails_when>
  </verify>
  <done>tests/test-m3u8-playlist-parser.js 全绿：序列号/分片列举/ENDLIST/targetDuration/EXTINF/KEY 行/resolveUri 均有断言。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: media-task-manager.js — 任务注册表状态机 + 持久化 + 接力钩子 + 单测</name>
  <files>media-task-manager.js, tests/test-media-task-registry.js</files>
  <read_first>
    - download-manager.js:27-31（活跃任务 Map 结构）、download-manager.js:311-383（状态终态分派 + 清理 + 通知的既有结构——注册表终态语义参照）
    - .planning/phases/44-/44-RESEARCH.md（D-25 注册表字段定义、Pattern 4 的 hasActiveTasks 用途）
    - .planning/phases/44-/44-PATTERNS.md（media-task-manager Analog 节）
    - tests/test-ai-bash-policy.js（测试结构）
    - .planning/phases/44-/44-VALIDATION.md（44-W0-registry 覆盖要求）
  </read_first>
  <behavior>
    - registerTask({type:'record', title, containerId}) → 返回带唯一 id 的 running 任务，persist 回调被调用
    - running → completed/failed/cancelled/interrupted 均合法；对非 running 任务再流转 → 抛错（非法流转拒绝）
    - updateProgress(id, progress) 更新并触发 persist
    - failTask/cancelTask 附带 error 原因字段
    - restoreTasks(savedJson)：saved 中 running 任务恢复后标记 interrupted（D-18 崩溃重启「已中断」，outputPath 保留供续转）
    - completeTask(id, { outputPath }) 后 onTaskCompleted 回调收到完整任务对象（D-22 record→convert 接力挂点）
    - hasActiveTasks()：存在 running 任务时 true
    - isVideoActive(playbackKey)：存在 running 任务且其 playbackKey 与入参相同则 true；任务进入终态后变 false（D-07 逐视频淘汰豁免的查询源，44-03 接线进 media-cache-manager）
    - registerTask 入参支持 playbackKey 字段（record 任务由引擎按 origin+pathname 语义传入，与 44-01 playbackKey 一致），任务对象保留该字段
    - listTasks() 返回按 updatedAt 降序的快照（不暴露内部 Map 引用）
  </behavior>
  <action>
  工厂函数 `createMediaTaskManager({ persist, now })`——**去 Electron 化**：persist 是注入的同步/异步回调（44-03 集成层负责写 userData/media-tasks.json），now 注入时间源；顶层无 electron require（PATTERNS/VALIDATION 的硬约束）。内部 Map 存运行时任务；registerTask 生成 id（crypto.randomUUID），任务对象保留入参中的 playbackKey 字段（record 任务由 44-04 引擎传入，用于 D-07 逐视频豁免查询）；每次变更后调 persist(listTasks())。onTaskCompleted 是注册回调（可多个），completeTask 时遍历触发——record→convert 接力由 44-05 集成层监听实现，注册表只提供钩子。`isVideoActive(playbackKey)` 遍历 running 任务比对 playbackKey 字段返回布尔——44-03 集成层把它注入 media-cache-manager 构造参数实现 D-07 淘汰豁免（media-task-manager 不得反向依赖 media-cache-manager）。markInterrupted 用于 restoreTasks 恢复语义。并发上限与同 URL 去重属 44-04 录制引擎职责，不放在注册表（单一职责）。测试注入内存 persist 收集调用断言，并覆盖 isVideoActive 的 running/终态两态。
  </action>
  <verify>
    <automated>node tests/test-media-task-registry.js && node -e "require('./media-task-manager'); require('./media-m3u8-parser'); console.log('pure-node ok')</automated>
    <fails_when>非零退出码，或 node:test 汇总出现 "fail 1" 及以上，或 "pass 0"；或 pure-node ok 未输出（模块顶层依赖了 electron/原生模块）</fails_when>
  </verify>
  <done>tests/test-media-task-registry.js 全绿：状态机全流转、非法流转拒绝、persist 调用、restore interrupted、completed 接力回调、hasActiveTasks 均有断言；两模块纯 Node 可加载。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 持久化 JSON ↔ 注册表 | restore 数据若被篡改可注入伪造任务字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-05 | Tampering | media-tasks.json 恢复 | low | accept | 文件位于三环境隔离的 userData 内，仅本机可写；restore 时对字段做类型白名单校验（id/title/status 枚举/数值），未知字段丢弃 |
| T-44-06 | DoS | 超量任务注册 | low | accept | 录制有并发上限（44-04），convert 由用户显式触发；注册表不设硬上限风险可忽略 |
| T-44-SC | Tampering | npm 安装（本计划无新增依赖） | high | mitigate | 本计划零新依赖；Phase 44 唯一新依赖 mux.js 的 legitimacy 审计在 44-05（verdict OK） |
</threat_model>

<verification>
- `node tests/test-m3u8-playlist-parser.js && node tests/test-media-task-registry.js` 全绿
- `node -e "require('./media-task-manager')"` 纯 Node 环境可加载（无 electron 泄漏）
</verification>

<success_criteria>
- parsePlaylist 对 live/VOD/缺标签/注释行/相对 URI 全部语义正确
- 注册表状态机与 D-25 字段形状一致，持久化与恢复语义满足 D-18「崩溃重启标记已中断」
- 两模块零 Electron 依赖，可被 44-03/04/05 直接复用
</success_criteria>

<output>
Create `.planning/phases/44-/44-02-SUMMARY.md` when done
</output>
