---
phase: 44-player-video-cache-and-local-media-library
plan: 12
type: execute
wave: 2
depends_on: ["44-09", "44-10"]
files_modified:
  - player-history-manager.js
  - ipc-handlers.js
  - src/preload.js
  - src/player.html
  - src/player.js
  - src/player.css
  - tests/test-player-history.js
autonomous: true
gap_closure: true
gap_ids: [G-44-8]
requirements: [D-11, D-14, D-15, D-16]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "抽屉删除确认框含「同时删除条目（含观看历史）」checkbox，默认不勾：不勾 = 仅删缓存目录（现行为，观看历史条目保留为未缓存）；勾选 = 确认时连 player_history 中该 playbackKey 的记录一并删除（G-44-8 missing 第 1 项）"
    - "确认文案随勾选状态联动：默认「删除缓存「{标题}」？观看历史保留，下次播放将重新缓存」（UI-SPEC Copywriting 契约原文）；勾选后切换为同时删除条目的明确警示文案"
    - "抽屉元信息「最近观看」文字替换为时钟图标，完整时间文字保留在 title tooltip（G-44-8 missing 第 3 项）"
    - "playerHistory 具备按 playbackKey 删除能力（deleteByKey），主进程在 deleteEntry 参数为 true 时于缓存删除成功后联动调用"
  artifacts:
    - player-history-manager.js 新增 deleteByKey(playbackKey)（参数化 DELETE，返回是否删除）+ module.exports 导出
    - ipc-handlers.js player:cache:delete 扩展第二参 deleteEntry（boolean，缺省 false——向后兼容）
    - src/preload.js deleteCacheEntry 签名扩展 (videoId, deleteEntry)
    - src/player.html drawer-delete-dialog 新增 checkbox 行（原生 dialog + showModal 不变，AGENTS.md 弹框居中约定不破坏）
    - src/player.js openDeleteConfirm 复位/联动文案 + 确认处理器透传勾选态；renderDrawer meta 行时钟图标
    - src/player.css .drawer-delete-option 与 .drawer-item-watched 样式
    - tests/test-player-history.js 新建（upsert/getByKey/deleteByKey 闭环，node:test + 临时 db）
  key_links:
    - player.js drawerDeleteEntry.checked → playerAPI.deleteCacheEntry(key, checked) → ipc 'player:cache:delete' → mediaCache.deleteEntry + playerHistory.deleteByKey
    - 删除成功 → renderDrawer() 重渲染（条目消失或转「未缓存」）
  prohibitions:
    - statement: 默认（不勾选）行为必须与现状完全一致——仅删缓存目录、观看历史保留（D-14/D-15/D-16 双层语义不因新增勾选而改变缺省值）
      status: locked
      verification: ipc-handlers.js deleteEntry 参数缺省 falsy 时不触达 playerHistory（源码 gate）；test-player-history 回归 + 既有语义断言
    - statement: playerHistory.deleteByKey 必须 SQL 参数化（禁止字符串拼接 playbackKey 进 DELETE 语句）
      status: locked
      verification: player-history-manager.js deleteByKey 使用 db.prepare('DELETE ... WHERE playback_key = ?').run(...)
---

<objective>
修复 G-44-8：抽屉删除确认框加「同时删除条目（含观看历史）」checkbox（默认不勾，勾选后连 playerHistory 按 playbackKey 删除），「最近观看」meta 文字换时钟图标（title tooltip 保留时间）。非缺陷修复而是 UAT 中用户锁定的产品决策变更：现有删除语义（仅删缓存、历史保留）保持为默认，新增可选的条目级删除；「最近观看」由纯文字改为图标以压缩 meta 行宽度。

Purpose: 用户删除缓存后条目残留为「未缓存」造成困惑，需要显式的连条目删除路径；meta 行文字过长。
Output: playerHistory.deleteByKey + IPC 扩展 + 确认框 checkbox + 文案联动 + 时钟图标 + 单测。

**前置核查结论（规划期已确认）**：player-history-manager.js 现有方法为 init/upsertProgress/getByKey/listRecent，**无**按 key 删除方法 → 按 UAT missing 预案新增 deleteByKey。
**已知债务**：WR-A/B/C/05/01-04、IN-01-04 未点名项维持债务。**tracer 说明**：gap-closure 小计划，无独立 tracer 收益。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-UAT.md
@.planning/phases/44-/44-UI-SPEC.md

@player-history-manager.js
@ipc-handlers.js
@src/preload.js
@src/player.html
@src/player.js
@src/player.css
</context>

<artifacts_this_phase_produces>
- player-history-manager.js：`deleteByKey(playbackKey)`（db.prepare 参数化 DELETE，返回 boolean；未初始化/非法入参返回 false）+ exports
- ipc-handlers.js：player:cache:delete 处理器签名 `(event, videoId, deleteEntry)`；deleteEntry 严格等于 true 且缓存删除成功时对原始 playbackKey 入参（非 16 位 hex）调 playerHistory.deleteByKey，返回值补 `historyDeleted` 字段
- src/preload.js：`deleteCacheEntry: (videoId, deleteEntry) => ipcRenderer.invoke('player:cache:delete', videoId, deleteEntry)`
- src/player.html：drawer-delete-dialog 内 `.drawer-delete-option` label + `#drawer-delete-entry` checkbox
- src/player.js：`drawerDeleteEntry` 元素引用；openDeleteConfirm 复位 unchecked + 默认文案；checkbox change 联动文案；确认处理器 `deleteCacheEntry(key, drawerDeleteEntry.checked)`；renderDrawer meta 行 `.drawer-item-watched` 时钟图标（title=「最近观看 {时间}」）
- src/player.css：.drawer-delete-option（13px 行样式）与 .drawer-item-watched svg 尺寸规则
- tests/test-player-history.js：新文件，node:test——upsert→getByKey→deleteByKey→getByKey null 闭环 + 重复删除/非法入参
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: playerHistory.deleteByKey + player:cache:delete 扩展 deleteEntry 参数 + 单测</name>
  <files>player-history-manager.js, ipc-handlers.js, src/preload.js, tests/test-player-history.js</files>
  <read_first>
    - player-history-manager.js 全文（:28-58 init/dbPath、:74-99 upsertProgress 参数化写法、:106-115 getByKey、:122-131 listRecent、:133 exports——deleteByKey 插在 getByKey 与 listRecent 之间或其后，风格对齐）
    - ipc-handlers.js:2305-2328（player:cache:delete 现状：videoId/playbackKey 双入参兼容换算 :2313-2321、hex 白名单 :2322-2324、mediaCache 未初始化 503 语义 :2325、末行 return mediaCache.deleteEntry(videoId)）；:2252-2303（player:drawer:list——条目 playbackKey 即 player_history.playback_key，删历史用它）
    - src/preload.js:1628-1636（playerAPI.deleteCacheEntry 现签名）
    - media-cache-manager.js:567 起（deleteEntry(videoId) 返回形状——确认 {success} 结构供联动判定）
    - tests/test-media-cache.js（node:test + 临时目录惯例参照）
  </read_first>
  <behavior>
    - Test 1: upsertProgress 两条记录 → deleteByKey(key1) 返回 true → getByKey(key1) 为 null 且 getByKey(key2) 仍在
    - Test 2: deleteByKey 对不存在的 key 返回 false（changed 计数 0）
    - Test 3: 未初始化（不调 init 直接调 deleteByKey——需独立进程态或 init 前调用）或非法入参（空串/非字符串）返回 false 不抛异常
    - Test 4: 删除后再 upsertProgress 同 key 可重新写入（删后重建语义成立）
  </behavior>
  <action>
    1. player-history-manager.js 新增 `deleteByKey(playbackKey)`：入参校验（非字符串或空串返回 false）；`db.prepare('DELETE FROM player_history WHERE playback_key = ?').run(playbackKey)` 取 run 返回的 changes 判断（changes > 0 → true）；try/catch 错误日志后返回 false。**必须参数化**（锁定见 prohibitions）。JSDoc 注明 G-44-8 用途（抽屉勾选连条目删除）。exports 补 deleteByKey。
    2. ipc-handlers.js player:cache:delete（:2311-2328）：签名改 `(event, videoId, deleteEntry)`；在换算前保存 `const originalKey = videoId;`；`const r = mediaCache.deleteEntry(videoId);` 之后——`if (deleteEntry === true && r && r.success && playerHistory && typeof originalKey === 'string' && !/^[a-f0-9]{16}$/.test(originalKey))` 时调 `const historyDeleted = playerHistory.deleteByKey(originalKey)`（hex 入参无 playbackKey 语义，跳过历史删除——抽屉恒传 playbackKey，此为防御分支），返回 `{ ...r, historyDeleted: !!historyDeleted }`；其余路径原样返回 r。JSDoc 补 @param deleteEntry 说明默认 false 保持 D-16 语义。
    3. src/preload.js:1635 deleteCacheEntry 签名扩展为 `(videoId, deleteEntry)` 透传 invoke 第二参；JSDoc 补参数说明（缺省/false = 仅删缓存，true = 连观看历史条目删除）。
    4. 新建 tests/test-player-history.js：node:test 风格，mkdtemp 临时 db 路径调 init（better-sqlite3 在纯 node 可加载——同项目其他 manager 测试先例），按 behavior 四用例实现；文件头注释注明覆盖 G-44-8。
  </action>
  <verify>
    <automated>node --check player-history-manager.js && node --check ipc-handlers.js && node --check src/preload.js && node tests/test-player-history.js && grep -q "deleteByKey" player-history-manager.js && grep -q "deleteEntry === true" ipc-handlers.js</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 tests/test-player-history.js 非零退出（fail ≥ 1，含 behavior 四用例任一失败）；或 deleteByKey 未实现（grep 非零）；或 ipc 层无 deleteEntry 严格判定（grep 非零——缺省语义回归风险）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（单测）：behavior 四用例全部成立（删除/幂等/防御/重建）
    - 源码断言：deleteByKey SQL 参数化（prepare + ? 占位）
    - 源码断言：deleteEntry 缺省（undefined/false）时 playerHistory 零触达——D-16 默认语义逐字节不变
    - 源码断言：hex videoId 入参不触发历史删除（防御分支）；preload 签名向后兼容（旧单参调用仍工作）
  </acceptance_criteria>
  <done>playerHistory 具备按 playbackKey 删除能力并被 IPC 勾选参数联动调用；默认仅删缓存语义不变；单测闭环。</done>
</task>

<task type="auto">
  <name>Task 2: 确认框 checkbox + 文案联动 + 「最近观看」时钟图标</name>
  <files>src/player.html, src/player.js, src/player.css</files>
  <read_first>
    - src/player.html:128-136（drawer-delete-dialog 现状：text + actions 两行——checkbox label 插在 text 与 actions 之间）
    - src/player.js:106-109（drawerDeleteDialog/drawerDeleteText 引用区——drawerDeleteEntry 引用加同区）、:925-928（pendingDeleteKey 状态）、:1061-1094（openDeleteConfirm 默认文案 = UI-SPEC Copywriting 契约原文、cancel/confirm 处理器、cancel 事件清理）、:978-990（renderDrawer meta 行：parts.join(' · ') + 「最近观看 {时间}」文字拼接点 :987-988）
    - src/player.css:625-640（.drawer-item-meta 字号/颜色——.drawer-item-watched 对齐）与 :728-776（#drawer-delete-dialog/.drawer-delete-* 样式段——.drawer-delete-option 插入位置）
    - .planning/phases/44-/44-UAT.md G-44-8（truth/missing 三项）
  </read_first>
  <action>
    1. src/player.html drawer-delete-dialog（:130-136）：`.drawer-delete-text` 之后、`.drawer-delete-actions` 之前插入 `<label class="drawer-delete-option"><input type="checkbox" id="drawer-delete-entry"> 同时删除条目（含观看历史）</label>`——原生 dialog + showModal 结构不动（居中约定不受影响，无新增遮罩类）。
    2. src/player.js：元素引用区（:108-109 旁）新增 `const drawerDeleteEntry = document.getElementById('drawer-delete-entry');`。openDeleteConfirm（:1066-1071）：开头 `drawerDeleteEntry.checked = false;`（每次打开复位默认不勾）并设默认文案（原文案不变）；给 checkbox 挂 change 联动（绑定一次即可，放 openDeleteConfirm 外的初始化区）：勾选 → `删除缓存「{标题}」？将同时删除观看历史条目，不可恢复`；取消勾选 → 回默认文案。注意联动文案需要当前 label——openDeleteConfirm 把 item.title 暂存到 pendingDeleteKey 旁的模块级变量（如 pendingDeleteTitle）供 change 监听读取；dialog cancel/关闭路径一并清空该变量。确认处理器（:1078-1089）：`window.playerAPI.deleteCacheEntry(key, drawerDeleteEntry.checked)`（第二参透传勾选态）；其余（close/renderDrawer/异常处理）不动。
    3. renderDrawer meta 行（:978-990）：删除 `parts.push('最近观看 ' + watched)` 式文字拼接（:987-988 的 watched 分支），改为 watched 非空时在 meta 上 append 一个 `span.drawer-item-watched` 元素——innerHTML 为时钟 SVG（12px 视觉，stroke currentColor，如 circle + 指针两针），`title = '最近观看 ' + watched`（完整时间保留 tooltip，G-44-8 missing 第 3 项）。meta 其余部分（缓存大小/完整度/未缓存）保持 textContent 拼接不变。
    4. src/player.css：追加 `.drawer-delete-option`（13px、与 .drawer-delete-text 协调的颜色、flex 行、checkbox 与文字间距 6px）与 `.drawer-item-watched`（内联 flex、svg 12px、vertical-align middle、颜色继承 .drawer-item-meta 的次级色）。
    5. 回归保护：删除按钮/条目点击/转换按钮 gating/抽屉开合逻辑零改动；确认框仍为原生 dialog（不引入 div 遮罩）。
  </action>
  <verify>
    <automated>node --check src/player.js && grep -q 'drawer-delete-entry' src/player.html && grep -q 'drawerDeleteEntry.checked' src/player.js && grep -q 'drawer-item-watched' src/player.js && grep -q 'drawer-item-watched' src/player.css && grep -q 'drawer-delete-option' src/player.css</automated>
    <fails_when>node --check 语法错误非零退出；或 checkbox 元素/勾选态透传/时钟图标/两组样式任一缺失（对应 grep 非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：checkbox 默认 unchecked（openDeleteConfirm 复位），确认时第二参透传 checked 值
    - 源码断言：文案随勾选联动且默认文案与 UI-SPEC Copywriting 契约逐字一致（「观看历史保留，下次播放将重新缓存」）
    - 源码断言：meta 行无「最近观看」文字拼接残留，时钟图标 title 含完整时间
    - 行为断言（真机/UAT 承载）：不勾选删除后条目保留为「未缓存」；勾选删除后条目从抽屉消失；dialog 居中不回归
  </acceptance_criteria>
  <done>删除确认框支持条目级删除勾选（默认不勾）且文案联动；「最近观看」为时钟图标 + tooltip；居中与既有交互零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player window renderer → player:cache:delete IPC | renderer 传入 playbackKey/deleteEntry 请求删除本地数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G12-01 | Tampering | playbackKey 进 SQL 删除语句 | low | mitigate | deleteByKey 参数化 prepare（锁定 prohibition）；playbackKey 本经 assertPlayerSender 信任边界且为 origin+pathname 派生 |
| T-44-G12-02 | Elevation | deleteEntry=true 被伪造触发误删历史 | low | mitigate | IPC 严格 `=== true` 判定 + 仅缓存删除成功后联动 + hex 入参防御跳过；误删面限本机播放历史（非敏感凭据层） |
| T-44-G12-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包（better-sqlite3 已有），不触发包合法性 gate |

无新增 high 威胁（删除面限于既有双层存储，参数化与严格布尔判定封闭注入/误删路径）。
</threat_model>

<verification>
1. `node --check player-history-manager.js && node --check ipc-handlers.js && node --check src/preload.js && node --check src/player.js` 全过
2. `node tests/test-player-history.js` 全绿；`node tests/test-media-cache.js` / `node tests/test-media-task-registry.js` 回归不红
3. 源码 gate：deleteEntry 严格判定 / checkbox 默认复位 / 文案联动 / 时钟图标 + 样式
4. 人工复测（UAT 第 8 项）：抽屉删除默认仅删缓存（条目变「未缓存」）；勾选「同时删除条目」确认后条目消失（重开抽屉不再出现）；meta 行显示时钟图标、hover 显示「最近观看 时间」；确认框居中
</verification>

<success_criteria>
- G-44-8 truth 达成：勾选删除条目时观看历史记录一并删除；默认行为与现状一致（仅删缓存）；「最近观看」图标化
- deleteByKey 参数化 + IPC 严格布尔门 + 默认语义锁定三重护栏成立
- player-history / cache / task-registry 既有测试零回归
</success_criteria>

<output>
Create .planning/phases/44-/44-12-SUMMARY.md when done
</output>
