---
phase: 44-player-video-cache-and-local-media-library
plan: 04
type: execute
wave: 3
depends_on: ["44-01", "44-02", "44-03"]
files_modified:
  - media-record-engine.js
  - ipc-handlers.js
  - main.js
  - src/player.js
  - src/player.css
  - src/player.html
  - src/preload.js
autonomous: true
requirements: [D-14, D-15, D-16, D-18, D-19, D-20, D-21, D-23]
estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - 播放器工具栏录制按钮发起显式后台录制任务：并发上限、同 URL（playbackKey）已在录则拒绝（D-18）
    - 录制与播放完全解耦：播放/暂停/刷新/切视频不影响录制，红点消失但任务页仍 running（D-20）
    - 录制从直播边缘开始（首轮清单只记基线不落盘），按 MEDIA-SEQUENCE 去重追新分片到独立录制目录，不受缓存容量淘汰影响（D-21/D-23）
    - 网络错误重试 N 次后停录标 failed，已落盘分片保留可转（D-18）
    - 关播放器窗口弹一次「继续后台录制 / 停止并保存」，记住选择作为默认；应用退出有活跃任务弹确认（D-19）
    - 播放器内红点闪烁可点击停止，hover 显示「已录 12:34 · 156MB」格式 tooltip（D-21/UI-SPEC）
    - 抽屉面板（320px 右滑）展示缓存库 + 观看历史条目：标题 ellipsis、accent 续播进度条、元信息「缓存大小 · 完整度%」、最近观看优先排序、hover 删除按钮（D-14/D-15/D-16/UI-SPEC Component 3/4）
    - 抽屉与任务页空态渲染 UI-SPEC 文案；标题长文本单行 ellipsis（UI-SPEC empty/long-text）
  artifacts:
    - media-record-engine.js（createRecordEngine({ fetchPage, parser, taskManager, recordRoot, maxConcurrent, maxRetries })）
    - ipc-handlers.js：player:record/start、player:record/stop、close 拦截录制确认（在 44-01 的 close 进度兜底上扩展）
    - main.js：before-quit 活跃任务确认（D-19 插入既有 quit 协程）
    - src/player.html/player.css/player.js：录制按钮、红点、抽屉面板 UI
    - src/preload.js：playerAPI.startRecord/stopRecord/getRecordStatus/onRecordStateChanged
  key_links:
    - player.js record 按钮 → player:record/start → record-engine.startRecord → taskManager.registerTask(record)
    - taskManager 终态广播 → 播放器红点状态 + 任务页 + 角标（44-03 链路复用）
    - record-engine 轮询 → media-m3u8-parser（44-02）→ 分片落盘 userData/media-records/
  prohibitions:
    - statement: 录制轮询不得依赖/复用 hls.js 实例，主进程独立实现（D-20 + RESEARCH 反模式）
      status: locked
      verification: media-record-engine.js 无对 renderer/hls 的引用，仅用注入 fetchPage + media-m3u8-parser
    - statement: 录制目录独立于缓存库，不参与容量淘汰（D-23）
      status: locked
      verification: recordRoot 默认 userData/media-records，media-cache-manager 淘汰遍历仅限 cacheRoot
---

<objective>
交付直播录制全链路：主进程 media-record-engine（m3u8 轮询追分片，D-18/D-20/D-21/D-23）、播放器录制 UI（按钮 + 红点 + 抽屉面板，D-14~D-16/D-21）、关窗与退出两级确认（D-19）。

Purpose: 录制是本阶段唯一「显式后台任务」型能力；任务中心（44-03）在此获得第一类真实任务。
Output: media-record-engine.js、播放器录制/抽屉 UI、record IPC、before-quit 确认。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-/44-CONTEXT.md
@.planning/phases/44-/44-RESEARCH.md
@.planning/phases/44-/44-PATTERNS.md
@.planning/phases/44-/44-UI-SPEC.md
@.planning/phases/44-/44-VALIDATION.md

@main.js
@ipc-handlers.js
@src/player.js
@src/player.css
@src/player.html
@src/preload.js
@media-m3u8-parser.js
@media-task-manager.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-record-engine.js`：`createRecordEngine({ fetchPage, parsePlaylist, taskManager, recordRoot, maxConcurrent, maxRetries })` → `{ startRecord, stopRecord, getRecordStatus }`；startRecord 返回 `{ ok, taskId? , reason? }`（同 URL 去重拒绝）
- ipc-handlers.js：`player:record/start`、`player:record/stop` 通道；playerWindow close 拦截的录制确认分支 + `settings.recordCloseAction` 记忆
- main.js：before-quit 协程内 `hasActiveTasks` 确认分支
- src/player.js：`startRecording/stopRecording`、红点状态同步、抽屉渲染 `renderDrawer`/`openDrawer/closeDrawer`
- src/player.css：`--player-` token 下的红点闪烁 @keyframes、抽屉面板样式
- src/preload.js：`playerAPI.startRecord/stopRecord/getRecordStatus/onRecordStateChanged`
- configStore 键：`settings.recordCloseAction`（'keep-recording'|'stop-save'|null）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: media-record-engine.js — 直播 m3u8 轮询追分片引擎</name>
  <files>media-record-engine.js</files>
  <read_first>
    - media-m3u8-parser.js（44-02 产出：parsePlaylist/resolveUri 契约）
    - media-task-manager.js（44-02 产出：registerTask/updateProgress/failTask/completeTask/cancelTask）
    - main.js:277-364（handleProxyRequest 的 ses.fetch + referer/cookie 语义——record 引擎的 fetchPage 注入实现要复用容器 session 回源，参照此结构在 main.js 集成时包装）
    - .planning/phases/44-/44-RESEARCH.md（「直播录制轮询」Code Example、Pitfall 2 live 滑动窗口语义、D-18/D-21/D-23）
    - .planning/phases/44-/44-PATTERNS.md（No Analog Found：本文件无同数据流先例，以 RESEARCH 示例为准）
  </read_first>
  <action>
  工厂 `createRecordEngine({ fetchPage, parsePlaylist, taskManager, recordRoot, maxConcurrent, maxRetries })`，**去 Electron 化**（fetchPage 为注入的 `(url, { referer, containerId }) => Promise<Buffer>`，集成层在 main.js 用容器 session 包装；顶层无 electron require）：
  1. `startRecord({ url, title, containerId, referer })`：playbackKey（origin+pathname，与 44-01 语义一致）去重——已在录返回 `{ ok:false, reason:'already_recording' }`（D-18）；活跃任务数达 maxConcurrent（默认 2）返回 `{ ok:false, reason:'concurrency_limit' }`；通过则 taskManager.registerTask({ type:'record', ... }) 并启动轮询循环。
  2. 轮询循环：拉清单（fetchPage → parsePlaylist）→ **首轮只记录基线**（把现有分片 seq 写入 seen 集合，不落盘——D-21 从直播边缘开始不回溯）→ 此后新 seq（mediaSequence 起依序且不在 seen）逐个 fetchPage 下载，写 `<recordRoot>/<taskId>/segments/<seq 补零>.ts`，同时 resolveUri 处理相对 URI（D-20 按 URL+媒体序列号去重落盘）；轮询间隔 = targetDuration（缺省 6s）；ended=true 时（VOD 意外转正）把剩余未录分片录完后 completeTask。
  3. 容错：清单/分片拉取失败连续 maxRetries（默认 5）次 → failTask(error='network')，已落盘分片与索引保留（D-18）；stopRecord(taskId) → 停轮询、写分片索引 meta.json（title、segments 列表、总时长、总大小）→ completeTask({ outputPath: recordRoot/<taskId> })（D-22 接力钩子由 44-05 消费）。
  4. 每次落盘后 taskManager.updateProgress（已录分片数/估算大小）；红点 hover 数据（已录时长≈分片数×targetDuration、已录大小）经 getRecordStatus(taskId) 暴露（D-21）。
  </action>
  <verify>
    <automated>node --check media-record-engine.js && node tests/test-m3u8-playlist-parser.js && node tests/test-media-task-registry.js && node -e "require('./media-record-engine'); console.log('pure-node ok')"</automated>
    <fails_when>node --check 语法错误非零退出；或任一既有测试套件非零退出（fail ≥1）；或 pure-node ok 未输出（引擎顶层依赖 electron）</fails_when>
  </verify>
  <done>引擎纯 Node 可加载；首轮基线不落盘、序列号去重、重试 N 次停录、stop→completeTask、并发上限与同 URL 去重逻辑就位；既有 parser/registry 测试不回归。</done>
</task>

<task type="auto">
  <name>Task 2: 播放器录制 UI + 抽屉面板 + record IPC 与关窗确认</name>
  <files>src/player.html, src/player.css, src/player.js, src/preload.js, ipc-handlers.js</files>
  <read_first>
    - src/player.html 全文（工具栏 .controls-right 结构、CSP、UMD script 段）
    - src/player.css（.control-btn 32×32、speed-menu rgba(0,0,0,0.85) 弹层、z-index 95 层级、--player- token 区）
    - src/player.js:256-308（模式判定）、:580-700（状态管理惯例）
    - ipc-handlers.js:1996-2067（media:play 与 playerWindow 生命周期）、:2085（assertPlayerSender）
    - .planning/phases/44-/44-01-PLAN.md Task 3（player:drawer:list / player:cache:delete 契约——本任务只做 UI 消费）
    - .planning/phases/44-/44-UI-SPEC.md（Component 1/2/3/4、红点语义 #EF4444 非 accent、Spacing/Typography/Project Conventions 5/6）
    - .planning/phases/44-/44-RESEARCH.md（D-19/D-20/D-21、Pitfall 6）
  </read_first>
  <action>
  1. player.html：工具栏 `.controls-right` 加录制按钮（32×32 `.control-btn`，内联 SVG 描边圆点，tooltip「开始录制」；录制中隐藏图标让位红点——UI-SPEC Component 1）与抽屉列表按钮。
  2. player.css（--player- token，恒暗层，不引 main.css）：红点 10px 圆点 `#EF4444` + @keyframes opacity 闪烁约 1s 循环 + `-webkit-app-region: no-drag` + 可点击停止；hover tooltip「已录 mm:ss · xxxMB」（tabular-nums）；抽屉面板 320px 右滑、rgba(0,0,0,0.85) 底、z-index 95 与 speed-menu 同层、打开时控制栏保持可见；条目：标题 14px/600 单行 ellipsis、续播进度条 3px accent 填充、元信息 13px「缓存大小 · 完整度%」+ 最近观看时间、hover 显删除按钮（aria-label「删除缓存」——checker 建议 ②）；空态「暂无观看记录」+ body 文案。
  3. player.js：录制按钮点击 → playerAPI.startRecord({ url: state.currentUrl, title }) → 已在录/超限 reason 提示；onRecordStateChanged 同步红点显隐与按钮态；红点点击 → stopRecord；红点 hover 定时 getRecordStatus 刷新 tooltip；**播放/暂停/刷新/切视频不触碰录制状态**（红点仅按任务状态渲染，切走视频红点消失但不清任务——D-20）。抽屉：列表按钮开关；渲染 playerAPI.getDrawerList()（44-01 契约：双层合并、lastWatched 降序——D-15）；条目点击 → 关抽屉并 openUrl 语义加载该视频（原 URL，续播自动生效）；删除按钮 → 原生 `<dialog>` 确认（显式 margin:auto + ::backdrop——AGENTS.md 弹框居中约定；文案「删除缓存「{标题}」？观看历史保留，下次播放将重新缓存」）→ playerAPI.deleteCacheEntry；空态渲染 UI-SPEC 文案；长标题 ellipsis（UI-SPEC long-text backstop）。
  4. preload.js playerAPI：`startRecord/stopRecord/getRecordStatus`（invoke）+ `onRecordStateChanged(cb)`（监听 media-task:changed 里 type=record 的任务）。
  5. ipc-handlers.js：`player:record/start`（assertPlayerSender；url/title/containerId 校验参照 :2001-2003 惯例；调 recordEngine.startRecord，fetchPage 用容器 session 包装 ses.fetch——参照 main.js:277 结构）；`player:record/stop`；**close 拦截扩展**（在 44-01 的 close 进度兜底之后）：有活跃录制任务时——`settings.recordCloseAction` 已记忆则直接执行；否则 dialog.showMessageBox（「窗口关闭后继续后台录制 / 停止并保存」+ checkbox「记住我的选择」，选择写 configStore 'settings.recordCloseAction'——D-19 关窗弹一次记住默认）；「停止并保存」路径调 stopRecord。
  </action>
  <verify>
    <automated>node --check src/player.js && node --check src/preload.js && node --check ipc-handlers.js && grep -n "record/start" ipc-handlers.js && grep -n "startRecord" src/preload.js src/player.js && grep -n "recordCloseAction" ipc-handlers.js && grep -n "margin: auto" src/player.css</automated>
    <fails_when>任一 node --check 语法错误非零退出；或任一 grep 无匹配退出码 1（record 通道/preload 桥/关窗记忆/抽屉 dialog 居中规则缺失）</fails_when>
  </verify>
  <done>录制按钮/红点/抽屉面板按 UI-SPEC 渲染；record/start|stop 链路通；关窗确认记忆默认选择；播放状态变化不影响录制任务。</done>
</task>

<task type="auto">
  <name>Task 3: before-quit 活跃任务退出确认（D-19）</name>
  <files>main.js</files>
  <read_first>
    - main.js:3537-3604（before-quit 既有协程：cookiesSaved/quitting 重入守卫、preventDefault、setImmediate(app.quit) 跳出异步续体的注释）
    - .planning/phases/44-/44-RESEARCH.md（Pattern 4：插入位置与既有模式对照）
  </read_first>
  <action>
  在既有 before-quit 协程内（Cookie 保存之前）插入：`mediaTaskManager.hasActiveTasks() && !quitConfirmed` 时 preventDefault → `dialog.showMessageBox`（「有正在进行的录制任务，退出将中断录制。确定退出吗？」，UI-SPEC Copywriting）→ 确认则置 quitConfirmed 并 setImmediate(() => app.quit())（严格复用既有跳出模式，:3538 注释「before-quit 不会等待 async handler 返回」是硬约束）；取消则 return 不退出。退出流程照常继续，活跃任务随进程终止、下次启动由 44-03 的 restoreTasks 标记 interrupted（D-18 崩溃语义兜底）。
  </action>
  <verify>
    <automated>node --check main.js && grep -n "hasActiveTasks" main.js && grep -c "setImmediate(() => app.quit())" main.js | grep -v '^0$'</automated>
    <fails_when>node --check 语法错误非零退出；或 hasActiveTasks 无匹配（grep 退出码 1）</fails_when>
  </verify>
  <done>应用退出遇活跃录制任务弹确认；确认后沿用既有 quit 协程完成 Cookie 保存与退出；取消则留在应用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端直播源 → 录制轮询 → 录制目录 | 不可信清单/分片内容落盘本机 |
| 播放器 renderer → record IPC | renderer 被攻破时可发起录制/写入请求 |
| 录制产物命名 → 文件系统 | 标题来自远端页面，可能含路径字符 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-11 | Tampering | 录制分片文件名（seq 补零数字） | low | mitigate | 文件名仅 `<seq 补零>.ts` 纯数字，目录为 taskId（uuid），无远端输入入路径（产物命名 sanitize 在 44-05 T1） |
| T-44-12 | Elevation | 被攻破 renderer 滥用 record IPC | medium | mitigate | player:record/* 均 assertPlayerSender；containerId 沿用 /^[\w-]+$/ 校验惯例 |
| T-44-13 | DoS | 恶意清单 targetDuration 极小致轮询风暴 | medium | mitigate | 轮询间隔下限钳制（≥2s）；连续失败 maxRetries 停录 |
| T-44-14 | DoS | 录制无限增长占满磁盘 | medium | mitigate | 录制目录独立但受 UI 显示已录大小（D-21 tooltip）+ 用户显式停止；容量治理属缓存库范围（D-23 定稿） |
| T-44-SC | Tampering | npm 安装（本计划无新增依赖） | high | mitigate | 本计划零新依赖；mux.js legitimacy 审计在 44-05（verdict OK） |
</threat_model>

<verification>
- `node --check` 全部改动 JS 通过；parser/registry 既有测试不回归
- `node tests/test-unified-navigation.js` 全绿（录制/抽屉不新增绕过 openUrl 的导航）
- 手工（UAT）：录制→红点→停止→任务页可见；关窗/退出确认；抽屉增删条目与续播
</verification>

<success_criteria>
- 录制任务在主进程独立运行，与播放状态完全解耦（D-20）
- 首轮基线不回溯、序列号去重、独立录制目录（D-21/D-23）
- 关窗弹一次记住默认 + 退出确认（D-19）
- 抽屉/红点/按钮符合 UI-SPEC 规格（红点 destructive 色、条目信息、空态、ellipsis）
</success_criteria>

<output>
Create `.planning/phases/44-/44-04-SUMMARY.md` when done
</output>
