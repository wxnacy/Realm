---
phase: 44-player-video-cache-and-local-media-library
plan: 15
type: execute
wave: 2
depends_on: ["44-14"]
files_modified:
  - package.json
  - package-lock.json
  - main.js
  - ua-ch-manager.js
autonomous: true
gap_closure: true
gap_ids: [G-44-2]
requirements: [D-13, D-19]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Electron 升级到 43-x-y 补丁线最新 stable（规划时点为 43.4.11，Chromium 150.0.7871.224；执行日以 releases.electronjs.org 实测为准）——获取上游补丁线全部修复，含 G-44-2 崩溃类的可能修复"
    - "4 处 UA/CH 常量与真实内核版本保持一致：降维 UA（Chrome/150.0.0.0 冻结 build 号）不变；fullVersion/fullVersionList/品牌表从旧内核全版本号同步到新内核实测值（docs/debug/google-login-ua-cover-done.md Round 5 清单）"
    - "原生模块（nodejieba / better-sqlite3）针对新 Electron 重编后可 require；FTS5/nodejieba 分词与下载/凭据加密功能正常"
    - "打包发布验证：make install 装出 .app 后实际启动一次无原生崩溃（AGENTS.md 发布前必查）"
  artifacts:
    - package.json devDependencies.electron → 新版本 + package-lock.json 同步
    - main.js:755-765 品牌表/headers 注入值 + 注释（全版本号同步）
    - ua-ch-manager.js:69-83 fullVersion/fullVersionList 常量 + 注释（全版本号同步）
  key_links:
    - ua-ch-manager.js CHROME_UA 与 main.js CHROME_UA 均为降维形式 Chrome/150.0.0.0（build 号冻结）→ Chromium 150 → 150 补丁线升级不改这两个字符串
    - main.js:763/765 Sec-CH-UA 注入值 ↔ ua-ch-manager.js:79-83 常量 ↔ process.versions.chrome 实测内核——三处必须同值
  prohibitions:
    - statement: 禁止跨大版本升级到 44.0.0（Chromium 152）——品牌表 seed/GREASE 需按 152 源码重算、回归面远大于补丁线，作为后续独立评估
      status: locked
      verification: package.json electron 版本号为 43.x.y
    - statement: 禁止改指纹伪装其余逻辑（品牌表顺序/GREASE 算法、CDP 覆盖链路、navigator.userAgentData 注入）——只同步版本号常量
      status: locked
      verification: git diff 仅含版本号字面量与对应注释行
---

<objective>
G-44-2 第二步：Electron 同分支补丁线升级 43.3.0 → 43.4.11（43-x-y 最新 stable，Chromium 150.0.7871.224）。取舍与推荐（对用户「升级是首选但影响面大」的回应）：跨大版本（44.0.0/Chromium 152）影响面确然大——品牌表须按新 Chromium 重算、Google 登录指纹区（已停止排查）与打包链路全量回归；而补丁线升级保持 Chromium 150 主版本，4 处常量中 2 处降维 UA 根本不用动、2 处全版本号仅改尾缀，原生模块同 ABI 重编，影响面收敛到「npm install + 重编 + 打包启动验证」。上游是否有针对性修复未确证（electron#43297 closed need-info，43.4.x changelog 未见明确对应项）——故升级定位为「获取补丁线全部修复的启发式手段」，与 44-14 的确定性规避叠加成防御纵深，两者都做。

Purpose: blocker 级上游 UAF；补丁线升级以最小影响面承接上游可能修复。
Output: Electron 43.4.11 + 常量同步 + 重编 + 打包启动验证。

**tracer 说明**：gap-closure 小计划，无独立 tracer 收益。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/debug/stop-record-sigsegv.md
@docs/debug/google-login-ua-cover-done.md
@.planning/phases/44-/44-14-SUMMARY.md
</context>

<tasks>

<task type="auto" precondition="执行日 releases.electronjs.org 上 43-x-y 分支最新 stable 版本号及其 Chromium 内核版本已确认（规划时点：43.4.11 / 150.0.7871.224；若实测小版本不同，后续常量按实测值同步）">
  <name>Task 1: 升级 Electron 并重编原生模块</name>
  <files>package.json, package-lock.json</files>
  <action>
npm install --save-dev electron@&lt;43-x-y 最新 stable&gt;（规划时点 43.4.11；package.json 写精确版本语义不变，npm 默认 ^ 前缀可保留）。安装后强制重编原生模块：npm rebuild nodejieba better-sqlite3（Electron ABI 对应版本变化时必须重编；若 rebuild 后 require 仍报 NODE_MODULE_VERSION 不匹配，改用 electron-rebuild）。验证 nodejieba 词典加载与 better-sqlite3 FTS5 正常（favorites/history 相关既有测试覆盖：node tests/test-favorites-folders.js 与 node tests/test-player-history.js 须全绿）。
禁止顺手升级其他依赖；禁止触碰 build/asarUnpack 配置（依赖清单未变）。
若执行日 43-x-y 最新版本号与本前置声明不符：以实测为准继续，常量同步按实测 Chromium 内核版本执行（Task 2）。
  </action>
  <verify>
    <automated>npm ls electron && node -e "console.log(require('electron'))" >/dev/null 2>&1; node tests/test-favorites-folders.js && node tests/test-player-history.js</automated>
  </verify>
  <done>package.json 锁定 43.x.y 补丁线最新；nodejieba/better-sqlite3 在新 Electron 下加载正常；favorites/history 既有测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 同步 UA/CH 全版本号常量并验证发布</name>
  <files>main.js, ua-ch-manager.js</files>
  <action>
按 docs/debug/google-login-ua-cover-done.md Round 5 清单核对 4 处常量：
① main.js:108 CHROME_UA 与 ② ua-ch-manager.js:36 —— 降维形式（build 号 150.0.0.0 冻结），Chromium 150 补丁线内不变，确认零 diff；
③ main.js:755-765 —— Sec-CH-UA 品牌表/headers 注入值中的全版本号（旧内核尾缀 .212，见 verify 的 grep 模式）替换为新内核实测全版本号，同段注释同步；
④ ua-ch-manager.js:69-83 —— fullVersion 与 fullVersionList 两元素的全版本号同步替换，注释（引用内核 tag 的说明行）同步。
同步完成后在 dev 环境启动一次，主进程日志确认 process.versions.chrome 与常量一致；用 DevTools 在 webview guest 内核对 navigator.userAgentData.getHighEntropyValues(['fullVersionList']) 返回值与新常量一致。
发布验证（AGENTS.md 发布前必查）：make install 装出 /Applications/Realm.app 后实际启动、打开一个网页、打开播放器播放一段视频，确认无原生崩溃（启动闪退排查看 ~/Library/Logs/DiagnosticReports/Realm-*.ips）。
禁止：改品牌表顺序/GREASE 算法、CDP 覆盖链路、navigator.userAgentData 注入逻辑——只改版本号字面量与对应注释。
  </action>
  <verify>
    <automated>node --check main.js && node --check ua-ch-manager.js && [ "$(grep -c '150\.0\.7871\.212' main.js ua-ch-manager.js | awk -F: '{s+=$NF} END {print s}')" = "0" ] && grep -c "150.0.7871.224" ua-ch-manager.js</automated>
    <human-check>make install 后实际启动 /Applications/Realm.app：网页浏览 + 播放器播放各一次，无闪退、无崩溃报告生成。</human-check>
  </verify>
  <done>main.js/ua-ch-manager.js 无旧内核全版本号残留；降维 UA 两处零改动；打包版实机启动验证通过。</done>
</task>

</tasks>

<verification>
1. `npm ls electron` 显示 43.x.y 补丁线最新；`node tests/test-favorites-folders.js && node tests/test-player-history.js && node tests/test-media-task-registry.js` 全绿。
2. `grep -rn '150\.0\.7871\.212' main.js ua-ch-manager.js` 零命中；降维 UA 字符串（Chrome/150.0.0.0）两处零 diff。
3. git diff 范围审计：仅 package.json/package-lock.json/main.js/ua-ch-manager.js 四文件，main.js diff 只含常量值+注释与（44-14 已完成的）诊断日志区。
4. human-check：make install 实机启动验证（Task 2 内嵌）。
</verification>

<success_criteria>
Electron 43 补丁线最新版本落地，4 处 UA/CH 常量与真实内核一致（指纹伪装语义不变式保持），原生模块重编可用，打包版实机验证无崩溃；与 44-14 规避叠加形成 G-44-2 双层防御。
</success_criteria>

<output>
Create `.planning/phases/44-/44-15-SUMMARY.md` when done
</output>
