---
phase: 44-player-video-cache-and-local-media-library
plan: 08
type: execute
wave: 7
depends_on: ["44-07"]
files_modified:
  - main.js
  - media-remuxer.js
  - tests/test-media-remuxer.js
autonomous: true
gap_closure: true
gap_ids: [CR-04]
requirements: [D-18, D-25, D-26]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "任务页「停止」对 running record 任务真实生效：/api/tasks/cancel 桥接 recordEngine.stopRecord(taskId)——引擎轮询停止、meta.json 落盘、任务经 completeTask 到 completed（D-22 record→convert 接力照常），不再只改注册表状态让录制无限继续落盘"
    - "record 停止语义统一：任务页停止 == 播放器红点停止（同一 stopRecord 原语），停止后产物/分片保留可续转/可转"
    - "cancel 对 running convert 任务真实停止转码：convertToMp4 经 shouldCancel 协作式取消（每分片迭代开始前检查），取消后 reject reason='cancelled'、半成品 mp4 被清理（fail 路径 unlink 复用）、任务落 cancelled 而非 failed/completed"
    - "取消入口不回归鉴权与非法流转语义：/api/tasks/cancel 仍 token 鉴权；非 running / 不存在任务维持既有 400/404 行为"
  artifacts:
    - main.js：cancel 路由的 record/convert 分支（record → recordEngine.stopRecord；convert → convertCancelTokens 触发）；模块级 `const convertCancelTokens = new Map()`（taskId → () => void）
    - media-remuxer.js：convertToMp4 输入新增 `shouldCancel` 选项（函数，每分片迭代前检查）+ 'cancelled' 原因（复用 fail 清理半成品）
    - tests/test-media-remuxer.js：新增「shouldCancel 即拒转 reason=cancelled 且产物清理」单测
  key_links:
    - /api/tasks/cancel → (record) recordEngine.stopRecord → writeMeta + completeTask → mediaTaskManager persist diff → 广播（任务页/角标刷新）→ onTaskCompleted → startConvertFromRecordTask（D-22 接力）
    - /api/tasks/cancel → (convert) convertCancelTokens.get(taskId)() → convertToMp4 循环 shouldCancel → fail('cancelled') → unlink 半成品 → startConvertTask .catch 分支 cancelTask
  prohibitions:
    - statement: 禁止对 running record/convert 任务只调 cancelTask 就返回（CR-04 根因回归——状态标 cancelled 而引擎/转码继续跑）
      status: locked
      verification: main.js cancel 路由内 running record 分支含 recordEngine.stopRecord 调用、running convert 分支含 convertCancelTokens 触发（源码 gate）
---

<objective>
修复 CR-04：任务页「停止」对运行中录制无效——/api/tasks/cancel（main.js:2121-2138）只调 mediaTaskManager.cancelTask 改注册表状态，media-record-engine 的 active Map / pollLoop 与注册表之间无取消联动，轮询循环继续拉清单、下分片、无限落盘（updateProgress 因任务已终态 throw 被吞）；convert 任务同病（cancel 后 convertToMp4 继续转完）。引擎停止仅经 player:record/stop IPC（ipc-handlers.js:2388）可达——任务页停止路径缺桥接。

Purpose: 把「停止」从状态标记变成真实停止：record 走 recordEngine.stopRecord（与红点停止同原语——引擎写 meta + completeTask + D-22 接力，任务页停止语义 = 停止并保存）；convert 给 convertToMp4 加协作式取消信号。改动全在 main.js / media-remuxer.js 集成层。
Output: cancel 路由桥接 + convert 取消信号 + 取消单测。

**与 44-06/44-07 的 wave 关系**：本 plan 亦改 main.js，wave 顺延为 7（depends_on 44-07），避免同文件并行编辑冲突。

**范围外说明（WR-05 appQuitting 永不复位）**：WR-05 属 D-19 关窗确认 truth（VERIFICATION 标 PRESENT_BEHAVIOR_UNVERIFIED，非 failed）的关联缺陷，且修复点在 ipc-handlers.js（不在本次 files_modified 白名单）；与 5 个 failed truths 无直接映射，不纳入本次 gap-closure（与 WR-01/02/03/04、IN-* 同列为已知债务，UAT 复测 truth #12 时一并观察）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-/44-VERIFICATION.md
@.planning/phases/44-/44-REVIEW.md
@.planning/phases/44-/44-03-PLAN.md
@.planning/phases/44-/44-05-PLAN.md

@main.js
@media-remuxer.js
@media-record-engine.js
@tests/test-media-remuxer.js
</context>

<artifacts_this_phase_produces>
本计划新增符号/契约：
- main.js：模块级 `const convertCancelTokens = new Map(); // convert taskId -> () => void（CR-04 取消令牌）`；handleTasksApi cancel 路由新增 record/convert 分支；startConvertTask 内取消令牌注册/注销（shouldCancel 闭包 + finally 清理）
- media-remuxer.js：convertToMp4({ ..., shouldCancel }) 新可选入参；失败原因集新增 'cancelled'（JSDoc 同步）；循环内取消检查点（fail 复用：destroy stream + unlink 半成品）
- tests/test-media-remuxer.js：新增 shouldCancel 单测
- media-task-manager.js / media-record-engine.js：**零改动**（停止原语已在引擎，取消桥接在集成层完成——CR-04 missing 的「引擎 completeTask 后无需再 cancelTask」语义）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: /api/tasks/cancel 桥接 recordEngine.stopRecord（任务页停止真实停录）</name>
  <files>main.js</files>
  <read_first>
    - main.js:2104-2138（handleTasksApi 与 cancel 端点现状：token 鉴权 :2106-2109、cancel 路由 :2121-2138 仅 cancelTask）
    - main.js:155-158（模块级 mediaTaskManager/persist 状态声明区——convertCancelTokens Map 加在此区附近）与 :2878-2895（recordEngine 实例化，cancel 路由同作用域可引用）
    - media-record-engine.js stopRecord（:265-279：active.get → not_found 返回 / st.stopped=true / active.delete / writeMeta / completeTask({outputPath})；catch 非法流转返回 {ok:false,reason}）与 pollLoop（:198-257：循环体 st.stopped 检查点 :217/:252、fail 路径 :244-249）
    - media-task-manager.js cancelTask（:177-182 非法流转 throw）与 completeTask（:151-166：终态 + outputPath + completedHooks——D-22 record→convert 接力触发点）
    - main.js:3088-3096（onTaskCompleted 接力：record completed → startConvertFromRecordTask——stopRecord 完成后自动弹框派生 convert，任务页停止与红点停止同 UX）
    - .planning/phases/44-/44-REVIEW.md CR-04（行号 106-125）与 44-VERIFICATION.md gaps 第 5 条（missing：running record 先调 recordEngine.stopRecord(taskId)，引擎 completeTask 后无需再 cancelTask，not_found 时才 cancelTask）
  </read_first>
  <action>
    改造 handleTasksApi 的 cancel 路由（main.js:2121-2138）为按任务类型分派的真实停止。保持入口校验不变（body taskId 字符串校验 :2127-2130、注册表未初始化 503 :2123-2126、token 鉴权），其后的逻辑替换为：
    1. `const existing = (mediaTaskManager.listTasks() || []).find((t) => t.id === taskId);`——不存在则 sendJson 404（{ success:false, error:'任务不存在' }）return（比旧实现把 not_found 也丢给 cancelTask 的 400 更精确）。
    2. `existing.status !== 'running'` → 维持旧语义：调 cancelTask 让它抛非法流转 → 400（{ success:false, error: err.message }）——对非 running 的取消请求行为不回归。
    3. `existing.type === 'record'`（running）：
    - `const r = await recordEngine.stopRecord(taskId);`——引擎停轮询 + 写 meta.json + completeTask（completed 带 outputPath，D-22 接力/任务页刷新随之发生）
    - `r.ok` → sendJson 200（{ success:true, task: r.task, stopped:'record-engine' }）return
    - 引擎 not_found（{ok:false, reason:'not_found'}——任务刚被引擎侧自然完成/失败，或并发竞态）：回读 `const fresh = (mediaTaskManager.listTasks() || []).find((t) => t.id === taskId);`，fresh 仍 running 时才走 `cancelTask(taskId, '用户取消')` 兜底（try/catch → 400），否则 sendJson 200（{ success:true, task: fresh || existing, note:'already-stopped' }）return——避免把已终态任务二次流转成 400 噪音
    - 引擎返回其他错误（completeTask 非法流转等）→ 按 err.message 400 返回
    4. `existing.type === 'convert'`（running）：`const stop = convertCancelTokens.get(taskId); if (typeof stop === 'function') { try { stop(); } catch { /* 忽略 */ } }` → sendJson 200（{ success:true, task: existing, note:'stop-signalled' }）return——实际终态由 Task 2 的 convertToMp4 取消检查后异步落定（cancelTask 在 .catch 分支），此处只发信号不等转码循环。
    5. 其他类型回落 `mediaTaskManager.cancelTask(taskId, '用户取消')` 原语义。
    6. 注释纪律：路由上方补块注释说明 CR-04 根因与分派语义（record=引擎停止原语、convert=协作式信号、状态标记仅在引擎/转码已停后落定）；**不要在注释里出现「cancel 只改状态/停不下来」这类会与下方 gate 冲突的反义表述**——gate 以 stopRecord/convertCancelTokens 出现为正。
  </action>
  <verify>
    <automated>node --check main.js && grep -n "recordEngine.stopRecord(taskId)" main.js && grep -n "convertCancelTokens" main.js && node tests/test-media-task-registry.js</automated>
    <fails_when>node --check 语法错误非零退出；或 grep 未匹配 recordEngine.stopRecord(taskId)（grep 退出码 1——record cancel 未桥接引擎）；或 grep 未匹配 convertCancelTokens（convert cancel 分支缺失）；或 test-media-task-registry.js 非零退出（回归不红）</fails_when>
  </verify>
  <acceptance_criteria>
    - 源码断言：cancel 路由内对 running record 任务存在 `recordEngine.stopRecord(taskId)` 调用（真停止而非仅状态标记）
    - 源码断言：cancel 路由内存在 convertCancelTokens 的分派分支（convert 取消信号入口）
    - 源码断言：引擎 not_found 时仅在任务仍 running 才 cancelTask 兜底；任务已终态则返回 200 当前态（不二次流转 400）
    - 回归：非 running/不存在任务维持 400/404；media-task-registry 26/26 全绿（注册表未改）
  </acceptance_criteria>
  <done>任务页对 running record 点停止 = 引擎真实停录（meta.json 落盘 + completed + D-22 接力），不再无限落盘；convert 停止信号入口就位。</done>
</task>

<task type="auto">
  <name>Task 2: convert 协作式取消信号（convertToMp4 shouldCancel + 取消令牌接线 + 单测）</name>
  <files>media-remuxer.js, main.js, tests/test-media-remuxer.js</files>
  <read_first>
    - media-remuxer.js convertToMp4 全文（:96-182：Promise 结构、fail() :124-131 已含 destroy stream + unlinkSync(outputPath) 半成品清理、顺序 push 循环 :159-174、settled 守卫、JSDoc :86-95 失败原因清单）
    - main.js startConvertTask（:2941-3011：registerTask :2967-2976 → convertToMp4 调用 :2982-2995（onProgress 节流）→ .then completeTask :2996-3001 → .catch 失败文案 :3002-3009）与模块级声明区 :155-158
    - tests/test-media-remuxer.js convertToMp4 契约 describe（:83-124：结构断言 onIdx<pushIdx、单实例、流式写盘；discontinuity/no_segments/segment_missing 拒转写法；tmp 目录 mkdtemp 习惯）
    - .planning/phases/44-/44-REVIEW.md CR-04 的 convert 部分（:125：convertToMp4 增加取消信号——aborted 标志 → onProgress 里检查并提前 reject + 清理半成品）
  </read_first>
  <action>
    1. media-remuxer.js convertToMp4（:96-102 解构处）：入参对象解构增加 `shouldCancel`（可选函数）；JSDoc 补 @param：`@param {Function} [input.shouldCancel] - 每分片迭代开始前调用的取消检查，返回 true 即中止（reason='cancelled'，半成品清理同失败路径）`；失败原因清单文案补 'cancelled'（用户取消转换）。
    2. convertToMp4 循环（:159-174）在 `if (settled) return;` 之后、`transmuxer.push(...)` 之前插入取消检查——一行条件：`typeof shouldCancel === 'function' && shouldCancel()` 成立时调 `fail(remuxError('cancelled', '用户取消转换'))` 并 return（fail 的拒绝体即循环提前退出）。fail() 复用既有清理（destroy stream + unlinkSync(outputPath)，:124-131）——半成品 mp4 不留盘；settled 守卫防与 stream.end 竞态。取消粒度 = 一个分片的处理时间（协作式，主进程事件循环内同步循环无法被异步打断，此为最小可打断单元，可接受）。
    3. main.js 模块级（:155-158 附近）新增 `const convertCancelTokens = new Map(); // convert taskId -> () => void（CR-04 取消信号注册表）`。
    4. startConvertTask（:2977-3010）：taskId 取得后、convertToMp4 调用前注册令牌——`let convertCancelled = false; const cancelSignal = () => { convertCancelled = true; }; convertCancelTokens.set(taskId, cancelSignal);`，并把 `shouldCancel: () => convertCancelled` 传入 convertToMp4 入参。
    5. convertToMp4 的 promise 链（:2996-3009）：.catch 开头加取消分支——`if (err && err.reason === 'cancelled') { try { mediaTaskManager.cancelTask(taskId, '用户取消'); } catch (e2) { console.warn(...); } return; }`（取消落 cancelled 而非 failed；CONVERT_FAIL_TEXT 无 'cancelled' 条目，拦截在文案映射之前）；.then（completeTask）与 .catch 之后追加 `.finally(() => { try { convertCancelTokens.delete(taskId); } catch { /* 忽略 */ } })` 注销令牌（防 Map 泄漏；终态竞态——循环已越过最后检查点时 .then 的 completeTask 正常收尾，任务 completed 而非 cancelled，接受该竞态窗口）。
    6. tests/test-media-remuxer.js「convertToMp4 契约」describe 新增单测：
    - 用例 A「shouldCancel=true 即拒转 reason=cancelled 且产物清理」：mkdtemp 建临时目录，写一个占位分片文件（内容任意——取消检查在首个 readFileSync 之前触发，无需真实 TS 载荷），`await assert.rejects(() => remuxer.convertToMp4({ segmentPaths: [占位文件], outputPath: 临时.mp4, shouldCancel: () => true }), (err) => err.reason === 'cancelled')`，随后断言 outputPath 不存在（半成品清理）
    - 用例 B「shouldCancel=false/缺省 行为不变」：沿用既有拒转契约（如 segment_missing 用 shouldCancel: () => false 仍 reason=segment_missing）——确认新入参不改变默认路径
  </action>
  <verify>
    <automated>node --check media-remuxer.js && node --check main.js && node tests/test-media-remuxer.js && grep -c "shouldCancel" media-remuxer.js | grep -qv '^0$'</automated>
    <fails_when>任一 node --check 语法错误非零退出；或 test-media-remuxer.js 非零退出（fail ≥ 1，含新增取消用例失败）；或 media-remuxer.js 无 shouldCancel（计数 0 → grep -qv 失败）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（单测）：shouldCancel 返回 true 时 convertToMp4 reject reason='cancelled'，且 outputPath 半成品文件已被删除（unlink 清理生效）
    - 行为断言（单测）：shouldCancel 缺省/恒 false 时既有拒转/完成路径不变（回归）
    - 源码断言：startConvertTask 注册/注销 convertCancelTokens（set/finally delete）且 .catch 对 reason='cancelled' 走 cancelTask 而非 failTask
    - 结构回归：data 监听先于 push、Transmuxer 单实例、流式写盘断言（既有结构测试）不因插入取消检查而破坏
  </acceptance_criteria>
  <done>convert 任务取消真实停止转码并清理半成品、落 cancelled；令牌注册/注销无泄漏；单测覆盖取消与回归路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| realm://tasks 页面 → /api/tasks/* | guest 页面经 token 鉴权 HTTP 请求主进程执行任务控制（停止录制/转码） |
| 本地产物写路径 | 取消/失败时对半成品 mp4 的 unlink（outputPath 为 showSaveDialog 用户所选路径） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-G08-01 | Spoofing | cancel 端点 token 鉴权旁路 | low | mitigate | 端点保持 /api/tasks/* 统一 REALM_TOKEN 鉴权（:2106-2109），分派改造不新增无鉴权路径 |
| T-44-G08-02 | DoS | 恶意反复 stop 请求 → 非法流转噪音 | low | mitigate | stopRecord not_found + 任务已终态时返回 200 当前态（幂等）；仅仍 running 才 cancelTask 兜底，不制造二次流转异常 |
| T-44-G08-03 | Tampering | 取消触发半成品 unlink 的路径面 | low | mitigate | 产物路径仅来自 showSaveDialog 返回（startConvertTask savePath）+ getUniqueFilePath，非远端输入；unlink 复用 remuxer fail 既有清理（T-44-17 相关面不变） |
| T-44-G08-04 | DoS | convertCancelTokens Map 无界增长 | low | mitigate | .finally 注销令牌；取消/完成/失败路径均收敛，单任务单令牌 |
| T-44-G08-SC | Tampering | npm/依赖（本计划零新增依赖） | high | mitigate | 无新增包，不触发包合法性 gate |

无新增 high 威胁（取消/停止均为受鉴权端点对既有原语的桥接，无新注入面；半成品清理路径与失败清理共用既有 T-44-17 边界）。
</threat_model>

<verification>
1. `node --check main.js && node --check media-remuxer.js` 全过
2. `node tests/test-media-remuxer.js`（15 既有 + 新增取消用例）与 `node tests/test-media-task-registry.js`（26/26）全绿；`node tests/test-unified-navigation.js` 回归（本 plan 无新导航入口，跑通确认）
3. 源码 gate：cancel 路由含 recordEngine.stopRecord(taskId) 与 convertCancelTokens 分派；startConvertTask 含 shouldCancel 传入与令牌 finally 注销
4. 人工复测（VERIFICATION human_verification 相应项）：任务页对 running 直播录制点停止 → 红点/任务状态即刻停录、meta.json 落盘、弹出 D-22 转换选目录；长任务转码中点停止 → 任务落「已取消」、产物目录无半成品残留
</verification>

<success_criteria>
- CR-04 truth 达成：任务页「停止」对运行中录制生效——引擎停止轮询与落盘（不再无限写盘），状态与引擎一致（completed + 可接力/可续转）
- convert 任务取消真实停止转码并清理半成品，任务终态为 cancelled
- cancel 端点鉴权/非法流转语义零回归；media-task-manager/record-engine 模块零改动（停止原语单一来源在引擎）
</success_criteria>

<output>
Create .planning/phases/44-/44-08-SUMMARY.md when done
</output>
