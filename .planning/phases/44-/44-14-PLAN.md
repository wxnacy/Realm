---
phase: 44-player-video-cache-and-local-media-library
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - ipc-handlers.js
  - src/renderer.js
  - main.js
autonomous: true
gap_closure: true
gap_ids: [G-44-2]
requirements: [D-13, D-19]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "播放器窗口关闭不再在键盘 ACK 可能于途时同步 destroy：close 序列终末先 hide()、延迟约 300ms 才真正 close()（G-44-2 Realm 侧规避——关闭瞬时不再有同步销毁窗口）"
    - "tab 关闭时 webview guest 的销毁同样延迟：先隐藏、约 300ms 后从 DOM 移除（关闭瞬时不再同步销毁 guest webContents）"
    - "dev/debug 环境主进程对每个被销毁的 webContents 打印 id + 最近 URL——用户下次复现闪退时可从日志确认被销毁的具体对象（G-44-2 missing 第 2 项）"
    - "录制/停止 JS 链路零改动：stopRecord→writeMeta→completeTask→persistMediaTasks→onTaskCompleted 行为与 UAT 已验证版本逐字节一致"
    - "关窗两级确认（recordCloseAction ask/keep-recording/stop-save，UAT test 8 已 pass）行为不变"
  artifacts:
    - ipc-handlers.js beginCloseSequence 的 finish()：hide → 延迟 close（含 isDestroyed 守卫）
    - src/renderer.js 新增 deferredRemoveWebview 助手 + closeTab/批量关 tab/容器删除链路的 webview 移除改走助手
    - main.js dev-only webContents destroyed 诊断日志（web-contents-created 内挂 did-navigate 记录最近 URL + destroyed 打印）
  key_links:
    - playerWindow 'close' handler 的 playerClosing 旁路标记 → 延迟 close() 期间该标记已置位，二次 close 事件直接放行不重入确认序列
    - renderer deferredRemoveWebview → guest webContents 延迟销毁 → 在途键盘 ACK 送达仍存活的 InspectableWebContents
  prohibitions:
    - statement: 禁止修改录制引擎/任务注册表的停止语义（诊断已反证录制链路无辜，崩溃在上游 UAF，改链路属无效修复）
      status: locked
      verification: media-record-engine.js / media-task-manager.js 零 diff
    - statement: 禁止动「关闭播放器窗口时」设置项（settings.html/settings-page.js recordCloseAction）——UAT test 8 pass，不要动它
      status: locked
      verification: settings.html / src/settings-page.js 零 diff；ipc-handlers.js 确认分支逻辑零 diff（只改 finish() 内的放行方式）
---

<objective>
G-44-2（blocker）Realm 侧规避：根因是 Electron 43.3.0 上游 use-after-free——键盘事件 ACK 回包经 InputRouterImpl::KeyboardEventHandled 委派给 electron::InspectableWebContents::HandleKeyboardEvent 时该包装已释放（.ips 全帧符号化确证，录制/停止 JS 链路 5 组真机复现零崩溃无辜）。触发条件 = 键盘 ACK 在途时对应 webContents 被同步销毁。本计划不碰录制链路，只收窄两处已知销毁时序窗口：① 播放器窗口关闭序列（ipc-handlers.js D-13/D-19，ipc-handlers.js:2092-2170）；② renderer 侧 tab 关闭的 webview guest 销毁（src/renderer.js closeTab 链路）。规避方式统一为「先隐藏、延迟销毁」，给在途 ACK 留出送达时间。同时在 dev 环境留存被销毁 webContents 的诊断日志（UAT missing 第 2 项）。

与 44-15 的分工：本计划是确定性收窄（无论上游是否已修都生效，防御纵深）；44-15 做同分支补丁线升级（43.3.0 → 43.4.11）。两者都做。

Purpose: blocker 级闪退；升级验证是启发式的（heisenbug 无法确定性复现），规避是唯一可结构性论证收窄的修复。
Output: 延迟销毁的两处落点 + dev 诊断日志。

**tracer 说明**：gap-closure 小计划，无独立 tracer 收益（UAT test 8 等 pass 项即既有端到端路径，不改即不破坏）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/stop-record-sigsegv.md
@.planning/phases/44-/44-04-SUMMARY.md
@ipc-handlers.js
@src/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 播放器窗口关闭序列延迟销毁（ipc-handlers.js）</name>
  <files>ipc-handlers.js</files>
  <action>
改 ipc-handlers.js:2096-2109 beginCloseSequence 的 finish()：终末放行不再直接调用 playerWindow.close()，改为——① 先 playerWindow.hide()（try/catch 包裹，isDestroyed 守卫）；② playerClosing = true（必须在定时器回调内置位，保证延迟触发的 close 事件被 'close' handler :2093 的旁路判断直接放行、不重入确认序列）；③ setTimeout 约 300ms（模块级常量 PLAYER_CLOSE_DESTROY_DELAY_MS = 300）后执行 playerWindow.close()，回调内含 isDestroyed() 守卫与 try/catch。延迟期间若窗口已被用户再次操作销毁，静默跳过。
在 finish() 的 hide 之前追加一条 console.log 诊断：打印本次销毁的 webContents id 与 getURL()（try/catch 包裹取 URL，销毁前后取值可能抛错）——这是 G-44-2 missing 第 2 项「留存被销毁 webContents 日志」在主侧关窗路径的落点。
禁止改动：'close' handler 的确认分支（activeRecords/recordCloseAction/dialog 逻辑，UAT test 8 pass 区域）、500ms ack 超时机制、'closed' handler 的清理赋值。只改 finish() 内「如何放行」。
遵循 AGENTS.md「Electron 修复须验证执行环境」：本文件是主进程（有 Node 全局），无 renderer 沙箱约束。
  </action>
  <verify>
    <automated>node --check ipc-handlers.js && grep -n "PLAYER_CLOSE_DESTROY_DELAY_MS" ipc-handlers.js && grep -n "hide()" ipc-handlers.js</automated>
  </verify>
  <done>关窗序列 = 确认/ack → hide → 300ms → destroy；playerClosing 旁路语义保持；关窗路径打印 webContents id+URL 诊断日志。</done>
</task>

<task type="auto">
  <name>Task 2: renderer webview guest 延迟销毁（src/renderer.js）</name>
  <files>src/renderer.js</files>
  <action>
在 src/renderer.js 新增助手 deferredRemoveWebview(webviewEl, delayMs = 300)：① webviewEl.style.display = 'none'（CSSOM 方式，立即从布局摘除、用户无感知）；② setTimeout 后 webviewEl.remove()（remove 与 closeTab 现状 :1058 一致——移除 webview 元素即销毁 guest webContents）。回调内包 try/catch。
将 tab 关闭链路中「移除 webview 元素」的调用点改走助手：closeTab（:1046-1058 的 tab.element.remove()——注意只延迟 webview 元素，tab 条 element.remove() 保持同步，避免 UI 残影）、:1127 附近的批量关闭路径、:1345-1349 destroyWebview 一族。容器删除（:4529 注释「closeTab 内含 destroyWebview」）经 closeTab 间接受益，无需单独改。:1811 等非「用户关闭 tab」语境的移除先读上下文判断：若属错误恢复/重建类（销毁不发生在输入交互瞬间）保持原样不改。
判定标准（写进代码注释）：凡是用户交互瞬间触发的 guest webContents 销毁都走延迟；程序化静默销毁（错误恢复）不延迟。
禁止：不改 openUrl 导航流水线、不改 will-navigate/setWindowOpenHandler 拦截链路、不改 webview 创建逻辑。改完跑 tests/test-unified-navigation.js 确认 32 项断言全绿。
遵循 AGENTS.md「Electron 修复须验证执行环境」：renderer 无 __dirname/Node 全局，助手只用 DOM API 与 setTimeout。
  </action>
  <verify>
    <automated>node --check src/renderer.js && node tests/test-unified-navigation.js && grep -n "deferredRemoveWebview" src/renderer.js</automated>
  </verify>
  <done>tab 关闭时 guest webContents 销毁延迟约 300ms 且先行隐藏；导航回归 32 项断言全绿；非关闭语境的销毁路径未被波及。</done>
</task>

<task type="auto">
  <name>Task 3: dev 环境 webContents 销毁诊断日志（main.js）</name>
  <files>main.js</files>
  <action>
在 main.js 的 app.on('web-contents-created') 订阅（若已有该订阅则并入，不重复注册）内：仅当 process.env.NODE_ENV 为 development 或 debug 时，为每个 webContents 挂——① 'did-navigate' 与 'dom-ready' 记录最近 URL 到局部变量；② 'destroyed' 一次性打印 `[Realm] webContents destroyed id=… url=…`（URL 取局部变量，不在 destroyed 回调里调 getURL——销毁后取值抛错）。生产/Nightly 环境零输出。
这是 G-44-2 missing 第 2 项的全局落点：用户下次复现闪退时，崩溃前最后一条 destroyed 日志即被销毁的具体 webContents（候选：ERR_FAILED 的 mp4 webview guest 或播放器窗口本体）。
禁止：不在该订阅里加任何行为逻辑（UA 覆盖、预加载注入等既有逻辑不动）；44-15 将改 main.js 的 UA/CH 常量区（:108、:710-765），本任务远离那些行，避免语义纠缠。
  </action>
  <verify>
    <automated>node --check main.js && grep -n "webContents destroyed" main.js</automated>
  </verify>
  <done>dev/debug 下每个 webContents 销毁都留下 id+URL 日志；生产环境无新增输出。</done>
</task>

</tasks>

<verification>
1. `node --check ipc-handlers.js && node --check src/renderer.js && node --check main.js` 全过。
2. `node tests/test-unified-navigation.js` 全绿（renderer 导航行为无回归）。
3. git diff 确认：media-record-engine.js、media-task-manager.js、settings.html、src/settings-page.js、src/preload.js 零改动（prohibitions）。
4. human-check：npm run dev 真机走 5 轮「播放器开始录制 → 点击停止 → 关闭播放器窗口（含 Cmd+W）」，应用全程存活无 SIGSEGV；关窗时终端可见 webContents destroyed 诊断日志。
</verification>

<success_criteria>
两处已知销毁时序窗口（播放器窗口关闭、tab 关闭的 guest 销毁）均改为先隐藏后延迟销毁；dev 诊断日志可留存被销毁对象；录制链路与关窗确认设置项行为与 UAT pass 版本完全一致。
</success_criteria>

<output>
Create `.planning/phases/44-/44-14-SUMMARY.md` when done
</output>
