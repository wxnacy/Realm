---
phase: 43-ai-mvp
plan: 03
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - main.js
  - src/settings.html
  - src/settings-page.js
autonomous: true
requirements: [MEM-06]
tags: [ai-memory, http-api, settings-ui, realm-webview]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "用户在 realm://settings「AI 记忆」子区域可查看并编辑三层记忆（用户画像/全局记忆/容器记忆 tab），点「保存记忆」才写盘（D-12 显式保存）"
    - "字数统计显示「已用 / 上限」格式（用户画像 1375、全局与容器 2200，数值来自端点响应而非前端硬编码）；超限时计数变红 + 保存按钮 disabled + hint「超出字符上限，请精简内容后再保存」（D-12 + UI-SPEC overflow resolution）"
    - "用户画像/全局 tab 显示「保存后将在新会话生效」hint，容器 tab 显示「容器记忆即时生效，AI 通过 memory_read 按需读取」（D-04 冻结语义 + UI-SPEC 生效 hint）"
    - "容器 tab 空态显示「该容器暂无记忆，AI 首次写入后在此显示」，textarea 置空可编辑；用户画像/全局首次为空显示「暂无内容，AI 写入记忆后在此显示」（UI-SPEC empty resolutions）"
    - "容器下拉数据源实时取容器列表（复用 fetchContainers），删除容器后列表联动（UI-SPEC zero-one-many resolution）"
    - "保存成功显示「已保存」（2 秒后消失）；失败显示「保存失败：{原因}，请重试」且 textarea 内容不丢失（UI-SPEC error resolution）"
    - "POST /api/ai-memory 对不存在容器的 container scope 返回 400「容器不存在」；GET 返回空态（RESEARCH Open Question 2 落地行为）"
  artifacts:
    - main.js /api/ai-memory GET + POST 路由（REALM_TOKEN 鉴权 + scope 白名单）
    - src/settings.html「AI 记忆」settings-group（3 tab + 容器下拉 + textarea + 状态行 + 保存按钮）
    - src/settings-page.js aiMemoryApi 包装 + AI 记忆分区逻辑
  key_links:
    - "settings-page aiMemoryApi → main.js /api/ai-memory → aiMemoryManager.readScope/writeScope（webview 禁 realmAPI，走 HTTP + token，D-06）"
    - "端点响应 budget 字段 → 前端字数统计（预算数值单源：BUDGETS 只在 ai-memory-manager.js，前端不得另写 1375/2200）"
  prohibitions:
    - statement: "设置页人工编辑保存路径不得经过威胁扫描（D-11：用户本人操作；writeScope 天然不扫描，端点必须走 writeScope 而非 write）"
      status: enforced
      verification: "源断言：main.js /api/ai-memory POST 只调 writeScope"
    - statement: "settings.html markup 内不得出现 style=\"display:none\" 内联初始隐藏（realm://settings CSP style-src 'self' 会拦截，元素会闪现——Pitfall 6）"
      status: enforced
      verification: "grep 反向断言 settings.html 新增区块无内联 display 初始隐藏"
    - statement: "前端不得硬编码预算数值 1375/2200（必须消费端点返回的 budget 字段——默认值唯一来源惯例）"
      status: enforced
      verification: "grep 反向断言 settings-page.js AI 记忆段无 1375/2200 字面量"
---

<objective>
交付设置页「AI 记忆」编辑入口：main.js 本地 HTTP `/api/ai-memory` 端点（D-06）+ settings.html/settings-page.js 的三 tab 编辑分区（D-12），让用户能透明查看、手动整理 AI 记忆——透明可查是产品隐私承诺的一部分（AI-SPEC §1b）。

Purpose: 记忆腐烂（阶段无自动整理）只能靠用户手动编辑对抗；同时该分区是预算饱和度、投毒残留的目检窗口（AI-SPEC §7 每周 dogfooding 依赖它）。
Output: `/api/ai-memory` GET/POST 路由；「AI 记忆」子区域完整交互（tab 切换/取数/保存/字数/空态/错误）。

**Artifacts this phase produces (本 plan 新增符号):**
- main.js：`handleAiMemoryApi(req, res, reqUrl)`（挂本地 HTTP 服务器分发，与 settings/search-config/ai 平级）
- GET /api/ai-memory?scope=user|global|container:&lt;id&gt; → `{ content, used, budget }`；POST /api/ai-memory body `{ scope, content }` → `{ success: true }`
- src/settings-page.js：`aiMemoryApi(route, options, query)` 包装函数 + AI 记忆分区初始化/切换/保存逻辑
- src/settings.html：`ai-memory-section`（settings-group 形态，含 aiMemoryTabs / aiMemoryContainerSelect / aiMemoryTextarea / aiMemoryCount / aiMemoryHint / aiMemorySave 元素 id）
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-ai-mvp/43-CONTEXT.md
@.planning/phases/43-ai-mvp/43-UI-SPEC.md
@.planning/phases/43-ai-mvp/43-RESEARCH.md
main.js
src/settings.html
src/settings-page.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/ai-memory HTTP 端点 — token 鉴权 + scope 白名单 + 预算双保险</name>
  <files>main.js</files>
  <read_first>
    - main.js（grep 定位：REALM_TOKEN 定义、本地 HTTP 服务器的路由分发结构、handleHistoryApi 的 token 鉴权段、/api/ai/providers 路由族、readJsonBody/sendJson 工具函数、错误响应 catch 形状——RESEARCH 标注的 L102/767/1382-1484 行号可能漂移，以 grep 为准）
    - ai-memory-manager.js（Plan 43-01 交付的 readScope/writeScope/BUDGETS 实际签名）
    - .planning/phases/43-ai-mvp/43-RESEARCH.md Pattern 3（HTTP 端点 + token 鉴权实录）+ Open Question 2（容器 scope 行为建议）
  </read_first>
  <action>
  在 main.js 本地 HTTP 服务器的路由分发处新增平级 handler `handleAiMemoryApi`（与 handleSettingsApi / search-config / ai handlers 并存，挂载位置以现有分发结构为准，RESEARCH A3：两种挂法均有先例）：
  - 鉴权：入口即查 `reqUrl.searchParams.get('token') !== REALM_TOKEN` → `sendJson(res, 403, { error: 'Forbidden' })` return（handleHistoryApi 逐字模式，不新写鉴权）
  - GET：解析 `scope` 查询参数（白名单三值：`user` / `global` / `container:<id>`，前缀精确匹配 `container:`，其余 400）→ `aiMemoryManager.readScope(scope)` 取原文（不存在返回空字符串——空态语义与 memory_read 一致）→ `sendJson(res, 200, { content, used: content.length, budget })`，budget 按 scope 从 BUDGETS 取（user 1375 / 其余 2200；经 manager 导出的 BUDGETS 读取，main.js 不写数值字面量）。container scope 对已删容器：GET 返回空态（RESEARCH Open Question 2 建议）
  - POST：`readJsonBody(req)` 取 `{ scope, content }` → scope 白名单同上；container scope 校验容器存在（复用 main.js 现有容器查询途径，如 getContainers 或既有 IPC handler 背后的函数），不存在 → `sendJson(res, 400, { error: '容器不存在' })`（防手改 URL 写孤儿记忆文件）；`content` 为空或非字符串 → 400；调 `aiMemoryManager.writeScope(scope, content)`——**必须走 writeScope（人工编辑路径，不经威胁扫描，D-11）**，超预算时 writeScope throw → catch 里 `sendJson(res, 400, { error: err.message })`（统一错误形状，前端透传显示）
  - aiMemoryManager 未初始化守卫照抄 /api/ai/providers 的 `if (!aiManager)` 空态模式（返回空 content + 0 used 而非 500）
  - 本端点零新依赖、零新鉴权方案；manager 引用沿用 main.js 现有对各 manager 的引用方式
  </action>
  <verify>
    <automated>node --check main.js && grep -n "ai-memory" main.js && grep -n "writeScope" main.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check main.js` 通过
    - main.js 含 `handleAiMemoryApi`（或同职责函数），其入口第一个业务判断是 REALM_TOKEN 不匹配 → 403（grep -A2 源断言）
    - GET 分支返回形状含 `content`、`used`、`budget` 三个键（源断言 `sendJson(res, 200, { content` 或等价对象字面量）
    - POST 分支：container scope 校验容器存在且不存在时 sendJson 400 含「容器不存在」；写入调用为 `writeScope`（grep 断言 main.js 内无对 `write(` 的直接调用于本 handler——prohibition）
    - main.js 内无 1375/2200 预算数值字面量（budget 从 manager BUDGETS 导出读取）
  </acceptance_criteria>
  <done>
  /api/ai-memory GET/POST 上线：token 鉴权、scope 白名单、容器存在性校验、预算服务端双保险、人工路径零扫描；与既有 /api/* 路由族形状一致。
  </done>
</task>

<task type="auto">
  <name>Task 2: 设置页「AI 记忆」分区 — 三 tab 编辑 + 字数统计 + 显式保存</name>
  <files>src/settings.html, src/settings-page.js</files>
  <read_first>
    - src/settings.html（grep 定位 settings-ai-assistant section 与 search-config-section——新分区插在其后；观察 settings-group/settings-group-header/settings-group-content 类结构与既有初始隐藏的 CSS 类做法）
    - src/settings-page.js（grep 定位：apiToken 获取、settingsApi/searchConfigApi fetch 包装、fetchContainers、tab 或分区初始化模式、字数统计同类先例如有）
    - .planning/phases/43-ai-mvp/43-UI-SPEC.md（全文：子区域结构、Copywriting Contract、Color/Typography/Spacing、UI Considerations 12 条 explicit + 2 条 backstop）
    - .planning/phases/43-ai-mvp/43-PATTERNS.md「src/settings.html」「src/settings-page.js」两段（可复制原文）
    - src/styles/main.css（settings-group / btn / text-input / settings-select / ai-form-hint 既有类——本阶段不新增颜色变量）
  </read_first>
  <action>
  ① src/settings.html：在 settings-ai-assistant section 内、search-config-section 之后新增 `<div class="settings-group ai-memory-section">`，结构照 UI-SPEC「AI 记忆子区域结构」：
  - settings-group-header：`<h2 class="settings-group-title">AI 记忆</h2>`
  - settings-group-content 内：三 tab 栏（按钮组，「用户画像」/「全局记忆」/「容器记忆」，data-scope 属性分别 user/global/container）；容器下拉 `<select id="aiMemoryContainerSelect" class="settings-select">`（仅容器 tab 可见）；`<textarea id="aiMemoryTextarea" class="text-input ai-memory-textarea">`（等宽字体、min-height 200px、CSS overflow-y auto 内部滚动——UI-SPEC backstop 长文本不破版）；状态行（`<span id="aiMemoryCount">` 字数 + `<span id="aiMemoryHint" class="ai-form-hint">`）；`<button id="aiMemorySave" class="btn btn-primary">保存记忆</button>`
  - **CSP 铁律**：所有初始隐藏（容器下拉、非激活 tab 面板如适用）走 CSS 类规则，markup 零 `style="display:none"`（Pitfall 6）；textarea 隐藏/显示用 CSS 类 `.hidden { display: none }` 或等效类切换
  - main.css 或 settings 内联样式表按 UI-SPEC 补少量布局样式：textarea monospace + min-height 200px + overflow-y auto、tab 激活态用 var(--accent-color) 下划线、字数超限的 danger 变色类（var(--danger-color)）、成功反馈类（var(--success-color)）——全部经 CSS 变量，不新增颜色 token；样式加在 main.css 的设置页段落（注意 bookmarks-bar.css 是死副本的教训：settings 页样式看 settings.html 实际引用的样式表）

  ② src/settings-page.js：
  - 新增 `aiMemoryApi(route, options, query)` fetch 包装：照 searchConfigApi 形状——URLSearchParams 带 apiToken、非 ok 时 try/catch 解析后端 error 详情透传 throw（保存失败的超限/容器不存在提示依赖这个透传）
  - 分区初始化：fetchContainers() 填充容器下拉（零一新端点，RESEARCH Open Question 1）；默认激活「全局记忆」tab
  - tab 切换：JS CSSOM 切换显隐（`el.style.display = 'none'/'block'` 具体值，不依赖 '' 回落）；切 tab 或切容器即拉取 GET `/api/ai-memory?scope=...` 填充 textarea + 状态行（used/budget 来自响应，前端不硬编码 1375/2200——prohibition）
  - 字数统计：input 事件实时更新 `{textarea.value.length} / {budget}`；`textarea.value.length > budget` 时计数加 danger 类 + 保存按钮 disabled + hint「超出字符上限，请精简内容后再保存」（UI-SPEC overflow resolution）；恢复时反向清除
  - 保存：显式按钮触发（D-12，无自动保存）→ 取数与保存期间 textarea 与按钮 disabled + hint「加载中…」（UI-SPEC backstop loading 态）→ POST `/api/ai-memory` body `{ scope, content }` → 成功 hint「已保存」（success 色，2 秒后消失，不弹框）；失败 hint「保存失败：{原因}，请重试」（danger 色，textarea 内容不丢失——catch 后不清空）
  - 生效 hint：user/global tab「保存后将在新会话生效」；container tab「容器记忆即时生效，AI 通过 memory_read 按需读取」（D-04 + UI-SPEC 生效 hint copy）
  - 空态：GET 返回空 content 时 textarea 置空可编辑 + hint「该容器暂无记忆，AI 首次写入后在此显示」（容器 tab）或「暂无内容，AI 写入记忆后在此显示」（user/global tab）
  - 容器下拉数据源实时：每次进入容器 tab 重新 fetchContainers（删除容器后联动，UI-SPEC zero-one-many resolution）
  </action>
  <verify>
    <automated>node --check src/settings-page.js && grep -q 'ai-memory-section' src/settings.html && [ "$(sed -n '/class="settings-group ai-memory-section"/,/<\/section>/p' src/settings.html | grep -c 'style="display')" -eq 0 ] && grep -n "aiMemoryApi" src/settings-page.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/settings-page.js` 通过
    - settings.html 新增 ai-memory-section 且位于 search-config-section 之后（grep -A3 源断言）；**ai-memory-section 区块内**无内联 display 初始隐藏（verify 按区块行区间提取后计数 = 0；settings.html 存量另有 2 处内联 display——eye-off 图标、searchEnvVarHint——属范围外存量问题，它们本就被 realm://settings CSP 拦截失效，实现时记入 REVIEW.md 技术债，本任务不修改）
    - settings-page.js 含 aiMemoryApi 函数，其 fetch 目标为 `/api/ai-memory`；含「保存后将在新会话生效」「容器记忆即时生效」「超出字符上限，请精简内容后再保存」「该容器暂无记忆」「已保存」「保存失败」全部 UI 文案字面量（grep 逐一断言）
    - settings-page.js AI 记忆段无 1375/2200 字面量（budget 消费端点响应字段）
    - 保存期间 disabled 态逻辑存在（源断言 textarea.disabled = true 或等价赋值出现在取数/保存函数内）
  </acceptance_criteria>
  <done>
  realm://settings 的「AI 记忆」分区可查看/编辑三层记忆：tab 切换取数、实时字数、超限阻断、显式保存、成功/失败/空态/加载态文案齐全，全部符合 UI-SPEC 视觉与文案契约。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview (realm://settings) → main.js HTTP 端点 | 本地 webview 携 token 请求编辑接口；同机其他进程/页面也可能探测该端口 |
| HTTP POST → 记忆文件 | 请求 body 的 content 直接写盘（人工路径，无 AI 扫描） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-07 | Elevation of Privilege | /api/ai-memory 端点（localhost 端口扫描读改记忆文件） | medium | mitigate | REALM_TOKEN 查询参数鉴权（Task 1，handleHistoryApi 同款，7+ 既有路由同方案）；403 拒绝 |
| T-43-08 | Tampering | POST container scope 写孤儿记忆（手改 URL 对已删容器写入） | medium | mitigate | POST 校验容器存在 → 400「容器不存在」（Task 1，RESEARCH Open Question 2 落地） |
| T-43-09 | Tampering | HTTP 端点绕过 AI 写入护栏大量注入内容 | low | accept | 端点仅本机 token 持有者可用（即用户本人）；D-11 拍板人工编辑不经扫描是设计语义而非漏洞；预算校验仍然生效防误操作膨胀 |
</threat_model>

<verification>
- `node --check main.js src/settings-page.js` 通过
- 端点鉴权/scope 白名单/容器存在校验/writeScope-only 源断言齐备
- UI 文案与 UI-SPEC Copywriting Contract 逐条 grep 命中
- 反向断言：无内联初始隐藏、无前端硬编码预算
- Manual（VALIDATION.md Manual-Only 表）：打开 realm://settings → AI 记忆分区 → 编辑保存回读（CSP/真实渲染环境）
</verification>

<success_criteria>
用户可在设置页透明查看与整理 AI 的全部记忆：三层 tab、实时字数与超限阻断、显式保存、生效时机明示、空态与错误反馈完整；端点与既有 /api/* 族安全形状一致。
</success_criteria>

<output>
Create `.planning/phases/43-ai-mvp/43-03-SUMMARY.md` when done
</output>
