---
phase: 42-ai-pi-agent
plan: 04
type: execute
wave: 2
depends_on:
  - 42-03
files_modified:
  - ai-conversations-manager.js
  - ai-manager.js
  - ipc-handlers.js
  - src/renderer.js
autonomous: true
requirements:
  - CONV-01
  - CONV-04
gap_closure: true
gap_ids: [G-42-3, G-42-4]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "重启后打开历史对话，AI 消息以 markdown 渲染，工具调用显示为工具卡片，而非原始 JSON（G-42-3）"
    - "已存为原始 JSON 块数组的历史行兼容读取，旧对话同样正常显示（G-42-3）"
    - "切换对话后继续提问，AI 能衔接之前上下文（历史消息进入 LLM 上下文，G-42-4）"
    - "工具调用结果随对话恢复为上下文一部分（per D-14）"
  artifacts:
    - "ai-conversations-manager.js — saveMessages 内容归一化写入 + getMessages 显示形状 + getAgentMessages 注入形状"
    - "ai-manager.js — switchConversation 改 async，await _recreateAgent 后注入 AgentMessage"
    - "ipc-handlers.js — ai:switch-conversation await 异步切换"
  key_links:
    "关键连接":
      - "saveMessages 归一化 ↔ getMessages/getAgentMessages 双形状读出"
      - "getAgentMessages ↔ agent.state.messages 注入 ↔ convertToLlm 过滤"
      - "ai:switch-conversation 返回 messages ↔ renderer renderAIMessages toolExecutions 渲染"
---

<objective>
修复消息格式与上下文恢复（G-42-3、G-42-4）：消息持久化管线归一化 pi-agent-core 内容块数组（消除 AI 回复渲染为原始 JSON），恢复工具调用显示；switchConversation 改异步等 Agent 重建完成后注入转换过的历史消息，使切换对话后上下文真正衔接。

Purpose: UAT Test 3/4 失败 — 历史对话 AI 回复显示原始 JSON 无 markdown 无工具卡片；切换对话后 UI 有历史但 Agent 上下文全新。根因：AssistantMessage.content 恒为块数组被 JSON.stringify 原样落库；switchConversation 同步方法未 await 异步 _recreateAgent，注入守卫恒 false。
Output: 归一化存储/双形状读取管线 + 真正的上下文注入
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-ai-pi-agent/42-UAT.md
@.planning/phases/42-ai-pi-agent/42-01-SUMMARY.md
@.planning/phases/42-ai-pi-agent/42-03-SUMMARY.md
@.planning/debug/ai-reply-renders-raw-json.md
@.planning/debug/switch-conversation-context-lost.md
@ai-conversations-manager.js
@ai-manager.js
@ipc-handlers.js
@src/renderer.js
@node_modules/@earendil-works/pi-agent-core/dist/types.d.ts
@node_modules/@earendil-works/pi-ai/dist/types.d.ts

**前置事实（诊断已确认，直接采用）**：
- pi-ai AssistantMessage.content 恒为 (TextContent | ThinkingContent | ToolCall)[] 块数组；ToolCall 块形如 { type:'toolCall', id, name, arguments }；UserMessage.content 为 string；ToolResultMessage 为 { role:'toolResult', toolCallId, toolName, content: 块数组, details }。
- SDK 消息无顶层 toolCalls/toolResults 字段（tool_calls 列恒 null），工具调用只存在于 assistant content 块内。
- AgentMessage 无顶层 id 字段（42-03 已将 saveMessages 改为全量替换事务，本计划在其上叠加内容归一化）。
- pi-ai 各 API 适配器（如 openai-completions.js convertMessages）构建请求只读 message.role 与 content 块，不读 usage/stopReason/api 等 provenance 字段——重建的 AgentMessage 用占位 provenance 是安全的。
- ai-manager.js 的 convertToLlm 只过滤 role ∈ user/assistant/toolResult；transformContext（_compactContext）只做 slice 裁剪；agent.state.messages setter 复制顶层数组。

**工作区未提交改动（必须保留，不得回退）**：同 42-03 所列（ai-manager.js / ipc-handlers.js / src/renderer.js），修复提交时一并纳入。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 消息存储管线归一化 — 写入侧内容提取 + 读取侧双形状（显示/注入）+ 旧 JSON 行兼容</name>
  <files>ai-conversations-manager.js</files>
  <read_first>
    - ai-conversations-manager.js (saveMessages 264-304 现 42-03 全量替换版、getMessages 311-328)
    - src/renderer.js renderToolCard 8109 起与 tool_execution_update 分支 7916-7949（toolExecutions 形状 {id, name, status, params, result, error}）
    - 42-UAT.md G-42-3 (root_cause + missing 三项)
  </read_first>
  <action>
1. 写入侧归一化（per G-42-3 missing「写入侧 saveMessages 归一化」）：在 saveMessages 事务的消息映射中按角色处理 content——
   - role 'user'：content 为 string 原样存 content 列；为数组则提取 type 'text' 块的 text 拼接。
   - role 'assistant'：content 数组中 type 'text' 块拼接为显示文本存 content 列；type 'toolCall' 块逐个取 { id, name, arguments } 序列化为 JSON 数组存 tool_calls 列（camelCase 键与 SDK 一致）；thinking 块不落盘（实时链路 _extractText 也不展示 thinking，保持一致）。
   - role 'toolResult'：content 数组 text 块拼接存 content 列；将 { toolCallId: msg.toolCallId, toolName: msg.toolName, isError: !!msg.isError, details: msg.details（undefined 时省略）} 序列化存 tool_results 列。
   - msg.toolCalls / msg.toolResults / page_snapshots 既有兜底读取保留；100KB 截断逻辑继续作用于 tool_calls/tool_results 列。
   - 本任务不改变 42-03 的全量替换事务结构，只替换每条消息的列值计算。

2. 新增模块级解析辅助 parseStoredContent(contentStr, toolCallsFallback)：统一处理「显示文本 + 工具调用列表」提取——
   - contentStr 为合法 JSON 且 JSON.parse 结果为数组 → 旧格式行（G-42-3 根因落库的原始块数组）：text 块拼显示文本，toolCall 块并入工具调用列表（向后兼容，per G-42-3 missing「已存为 JSON 的历史行需兼容读取」）。
   - 否则 contentStr 即显示文本；工具调用列表来自入参（tool_calls 列反序列化结果，容错 JSON.parse 失败返回空数组）。

3. getMessages 改为返回 renderer 显示形状（per G-42-3 missing「getMessages 检测 JSON 块数组，提取 type:'text' 为显示文本，toolCall 块映射为渲染器工具卡片结构」）：
   - 按现有排序读全部行；每行先经 parseStoredContent 得到显示文本与工具调用列表。
   - role 'user' 行 → { id, role:'user', content: 显示文本, timestamp: created_at }。
   - role 'assistant' 行 → { id, role:'assistant', content: 显示文本, toolExecutions: [...], timestamp }。toolExecutions 元素形状对齐 renderer renderToolCard 入参 { id, name, status, params, result, error }：id/name 取工具调用块；params 取 arguments；初始 status 'completed'。
   - role 'toolResult' 行不单独输出：按 toolCallId 匹配前面最近的 assistant 行的 toolExecutions 元素，回填 result（该行显示文本）、status（isError 为真 'failed' 否则 'completed'）、error（isError 文本）；找不到归属 assistant 行时丢弃该行。这样恢复视图中工具调用呈现为父 AI 消息内的工具卡片，与实时链路一致，且不出现独立的 toolResult 气泡。
   - 保持函数签名与导出名不变（现有调用方 ai:switch-conversation 经 getConversationMessages 透传给 renderer，renderer 7801 起 result.messages 直接进 state.aiMessages，显示形状即插即用）。

4. 新增导出 getAgentMessages(conversationId)：返回 pi-agent-core AgentMessage 形状数组（per G-42-4 missing「注入前把库行转换为 pi-agent-core AgentMessage 格式」），与 getMessages 共用 parseStoredContent 与行读取——
   - user 行 → { role:'user', content: 显示文本, timestamp: created_at }。
   - assistant 行 → { role:'assistant', content: [有文本时 {type:'text', text: 显示文本}, ...工具调用块原样 {type:'toolCall', id, name, arguments}], api: 'unknown', provider: 行 provider 或空串, model: 空, usage: { input:0, output:0, cacheRead:0, cacheWrite:0 }, stopReason:'stop', timestamp: created_at }。provenance 占位安全性依据见 context「前置事实」（请求构建只读 role+content）。
   - toolResult 行 → { role:'toolResult', toolCallId, toolName, content: [ {type:'text', text: 显示文本} ], isError（有则带）, details（tool_results 列解析出且有则带）, timestamp: created_at }。
   - 顺序即行序（created_at, rowid），保证 assistant toolCall 与其后 toolResult 相邻，满足 provider API 的配对约束（convertToLlm 按 role 过滤后仍保序）。
   - 旧 JSON 行同样经 parseStoredContent 兼容，toolCall 块原样保留进 content（含 thinking 块时过滤掉再注入）。

5. module.exports 增加 getAgentMessages。JSDoc 注明两种读出形状的用途（显示 vs 上下文注入）。
  </action>
  <verify>
    <automated>node --check ai-conversations-manager.js && node -e "const cm = require('./ai-conversations-manager'); console.log(typeof cm.getMessages, typeof cm.getAgentMessages)" && grep -c "parseStoredContent" ai-conversations-manager.js</automated>
  </verify>
  <acceptance_criteria>
    - saveMessages 按 role 归一化：assistant 行 content 列为纯文本、tool_calls 列为 [{id,name,arguments}]；toolResult 行 tool_results 列含 toolCallId/toolName
    - getMessages 返回显示形状：无独立 toolResult 行，assistant 行带 toolExecutions
    - 旧 JSON 块数组行经 parseStoredContent 兼容读出
    - getAgentMessages 返回 user/assistant/toolResult 三角色 AgentMessage 数组
    - node --check 通过
  </acceptance_criteria>
  <done>存储管线归一化完成，历史行读取不再产生原始 JSON 文本，注入形状与显示形状分离就绪</done>
</task>

<task type="auto">
  <name>Task 2: switchConversation 异步化 + Agent 上下文注入 + IPC await</name>
  <files>ai-manager.js, ipc-handlers.js, src/renderer.js</files>
  <read_first>
    - ai-manager.js (switchConversation 1540-1584、_recreateAgent 1750-1791、_cleanupCurrentAgent 1733-1743)
    - ipc-handlers.js (ai:switch-conversation 1780-1792，含工作区未提交的 messages 回传)
    - 42-UAT.md G-42-4 (root_cause：1567 未 await，注入守卫在同步帧恒 false)
  </read_first>
  <action>
1. ai-manager.js switchConversation 改为 async（per G-42-4 missing「switchConversation 改为 async 并 await this._recreateAgent() 后再注入历史」）：
   - 保留开头参数校验、目标对话存在性校验、切换前 saveCurrentConversation（当前对话守卫）、_cleanupCurrentAgent() 原顺序。
   - 将 this._recreateAgent() 调用改为 await this._recreateAgent()——根因是同步帧内 this.agent 仍为 null，1570 的注入守卫 if (this.agent && messages.length > 0) 恒 false。
   - 历史加载改为 const rows = conversationStore.getMessages(conversationId) 换用 conversationStore.getAgentMessages(conversationId)（42-04 Task 1 产出），注入 this.agent.state.messages = agentMessages（守卫 if (this.agent && agentMessages.length > 0) 保留，覆盖模型缺失重建失败的分支）。
   - 注入后追加一条 console.log 标注注入条数，便于验证。JSDoc @returns 补 Promise；调用链注释同步（a-f 步骤描述改为 await 语义）。
   - 注意 conversationMeta 更新与返回 targetConversation 保持不变。

2. ai-manager.js 内部同步调用点排查：newConversation() → createNewConversation() 链路里 _recreateAgent 同样存在未 await 的问题，但 createNewConversation 无注入需求、Agent 会在下轮 prompt 前就绪（prompt 前有 isProcessing 之外的 agent 判空），本计划仅要求：createNewConversation 内 this._recreateAgent() 改为不阻塞的 void this._recreateAgent().catch(...) 或保持现状——不做扩大化重构；唯一硬性要求是 switchConversation 的 await。若排查发现 prompt() 会在 _recreateAgent 完成前以 !this.agent 早退（显式新建后立即发送的竞态），在 prompt()/promptWithContext() 的未初始化分支加一次 await this._recreateAgent() 重试而非直接报错——仅当确认该竞态真实存在时实施，否则不动。

3. ipc-handlers.js ai:switch-conversation：const conversation = await aiManager.switchConversation(conversationId)（per G-42-4 missing「IPC 处理器对应 await」）；messages 改取 conversationStore 显示形状——保持现有 aiManager.getConversationMessages(conversationId) 调用（内部即 getMessages，42-04 Task 1 已改为显示形状），返回 { conversation, messages } 结构不变。处理器本就 async，只需补 await。

4. src/renderer.js switchConversation（6779-6814）：现有 result.messages 赋 state.aiMessages + renderAIMessages() 的链路对显示形状即插即用，仅需核对两点并修正：
   - toolResult 行已不再出现在 messages 中（Task 1 保证），renderer 无需按 role 过滤——如现有代码对未知 role 有兜底渲染则删除该兜底，避免未来又出现独立工具结果气泡。
   - 恢复的 assistant 行含 toolExecutions，renderAIMessages 7453 的工具卡片分支直接生效；确认 state.aiStreaming 为 false 时操作按钮分支（7463）对恢复消息行为正常（msg.id 来自行 id，非空成立）。
   - 不改 switchConversation 的其他交互（高亮、关面板、清空列表）。
  </action>
  <verify>
    <automated>node --check ai-manager.js && node --check ipc-handlers.js && node --check src/renderer.js && grep -c "await this._recreateAgent()" ai-manager.js && grep -c "getAgentMessages" ai-conversations-manager.js ai-manager.js</automated>
  </verify>
  <acceptance_criteria>
    - switchConversation 为 async 且在注入前 await _recreateAgent()
    - 注入数据来自 getAgentMessages（AgentMessage 形状），非原始库行
    - ai:switch-conversation await 异步切换并返回显示形状 messages
    - renderer 恢复链路对显示形状即插即用，无未知 role 兜底渲染
    - node --check 全部通过
  </acceptance_criteria>
  <done>切换对话后 agent.state.messages 承载真实历史（含工具调用/结果），继续提问时 LLM 上下文衔接；恢复视图 markdown + 工具卡片正常</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB→Agent context | 库行 JSON.parse 重建 AgentMessage，损坏数据进入 LLM 上下文管线 |
| DB→renderer | getMessages 显示形状经 IPC 回传渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-08 | Tampering | parseStoredContent / tool_calls 列 JSON.parse | medium | mitigate | 所有 JSON.parse 包 try/catch，损坏行降级为纯文本/空工具列表，不抛异常中断切换 |
| T-42-09 | Information Disclosure | 恢复的上下文注入 LLM | low | accept | 注入的是本机用户自身历史对话，无跨信任数据；工具结果 100KB 截断沿用 |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | 无新增依赖 |
</threat_model>

<verification>
1. 构造含工具调用的对话（让 AI 调用任一工具），完全退出重启，打开该对话：AI 气泡为 markdown 渲染，工具调用显示为带参数/结果/状态的工具卡片，无原始 JSON 文本
2. 用 42-03 之前遗留的旧库（或手工把一行 content 改回 JSON 块数组字符串）验证旧格式行兼容读出
3. 切换到另一对话再切回，发送「我们刚才聊到哪里」类问题，AI 回答应引用先前内容（上下文已注入）；主进程日志可见注入条数
4. 切换对话后立即发消息（重建竞态路径），不出现「AI 未初始化」类早退
</verification>

<success_criteria>
- 历史对话 AI 消息 markdown + 工具卡片渲染，旧 JSON 行兼容（G-42-3）
- 切换对话后上下文真实恢复，工具结果按 D-14 参与上下文（G-42-4）
- 双形状读取管线（显示/注入）边界清晰，renderer 与 Agent 各取所需
</success_criteria>

<output>
创建 `.planning/phases/42-ai-pi-agent/42-04-SUMMARY.md` when done
</output>
