---
phase: 42-ai-pi-agent
plan: 06
type: execute
wave: 4
depends_on: ["42-05"]
files_modified:
  - ai-conversations-manager.js
  - src/renderer.js
autonomous: true
gap_closure: true
gap_ids: [G-42-8]
requirements: [CONV-01]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 重启打开或切换到含工具调用的对话时，AI 最终文本气泡在上方、工具卡片在下方，同属一条消息（与实时路径一致）
    - 历史恢复渲染无空消息气泡残留（含被中断回合遗留的纯工具行也只渲染工具卡片）
    - 实时流式路径行为不变：流式占位气泡与 typing 指示器保留
    - LLM 上下文注入形状不变：getAgentMessages 输出与 CR-01 配对修复语义不受影响（D-14）
  artifacts:
    - ai-conversations-manager.js getMessages 同回合 assistant 行合并逻辑（仅显示形状）
    - src/renderer.js renderAIMessages 空 content 气泡守卫（流式占位豁免，不以工具卡片存在为前提）
  key_links:
    - getMessages 显示形状 → ai:switch-conversation → state.aiMessages → renderAIMessages（历史恢复链路）
    - getMessages 与 getAgentMessages 共用 parseStoredContent/行读取，但显示/注入形状互不渗漏
---

<objective>
修复 G-42-8（第 2 轮 UAT 测试 3）：含工具调用的对话在历史恢复路径（重启打开 / 切换对话）渲染时，工具卡片上方出现空消息气泡、AI 最终文本跑到工具卡片下方。

根因（见 .planning/debug/tool-card-empty-bubble.md，已诊断闭环）：pi-agent-core 每次供应商响应落一条 assistant 行，工具回合为三行 assistant(''+tool_calls) → toolResult → assistant(最终文本)；getMessages 行级 1:1 映射产出 content:'' 显示消息，renderAIMessages 无空内容守卫且 CSS 给气泡背景+padding，空气泡可见。存储行序正确，仅恢复侧读出分组缺失。

Purpose: 恢复视图与实时视图同构（文本在上、卡片在下、无空气泡），完成 UAT truth「AI 文本气泡与工具卡片顺序正确，无空消息气泡残留」。
Output: getMessages 同回合合并（根因修复）+ renderAIMessages 空气泡守卫（防御修复），自动化冒烟证明显示/注入双形状互不影响。
</objective>

<execution_context>
@/Users/wxnacy/.zcode/gsd-core/workflows/execute-plan.md
@/Users/wxnacy/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/tool-card-empty-bubble.md
@.planning/phases/42-ai-pi-agent/42-UAT.md
@.planning/phases/42-ai-pi-agent/42-04-SUMMARY.md

@ai-conversations-manager.js
@src/renderer.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getMessages 同回合 assistant 行合并（G-42-8 根因修复）</name>
  <files>ai-conversations-manager.js</files>
  <behavior>
    /tmp 冒烟测试断言（better-sqlite3 真库 + electron stub，见 verify）：
    - Test 1 标准工具回合（user → assistant(toolCall) → toolResult → assistant(text)）：getMessages 返回 2 条显示消息（1 user + 1 assistant），assistant 消息 content=最终文本、toolExecutions 恰 1 张卡片（id/name/params/status:'completed'/result 已回填），不产出 content:'' 的显示消息
    - Test 2 多轮工具回合（toolCall A → toolResult A → toolCall B → toolResult B → 最终文本）：合并为 1 条 assistant 消息，content=最终文本、toolExecutions 2 张卡片且各自 result 正确回填
    - Test 3 纯文本对话（user → assistant(text)）：仍为 2 条消息，无行为变化（回归）
    - Test 4 同回合两条都含文本（assistant(text+toolCall) → toolResult → assistant(text)）：保持 2 条显示消息，第一条 content 非空且带卡片、第二条为最终文本——任何情况下不产生 content:'' 消息
    - Test 5 中断回合尾部纯工具行（toolCall 后无后续文本行）：getMessages 输出 1 条 content:'' 但带卡片的 assistant 消息（此残留由 Task 2 守卫承接，读出层保留行语义）
    - Test 6 注入形状不变：同一批行 getAgentMessages 输出 3 条 AgentMessage（assistant(toolCall) → toolResult → assistant(text)），toolCall/toolResult 配对完整、顺序不变（CR-01 形状、D-14 语义）
    - Test 7 合并消息 id/timestamp：取该回合首行的 id 与 created_at（工具卡片与操作按钮锚点稳定）
  </behavior>
  <action>
    修改 ai-conversations-manager.js 的 getMessages（约 536-596 行，仅此函数；getAgentMessages、saveMessages、normalizeMessageColumns、parseStoredContent、readMessageRows 一律不动——上下文注入依赖行级结构（CR-01/D-14），存储管线 42-04 已验证勿动）。

    在 assistant 行处理分支内实现同回合合并（替代现在的无条件 push）：
    - 先按现有逻辑解析出 text 与 toolCalls，取 display 末元素 last
    - 合并规则（仅当 last 存在且 last.role === 'assistant'，天然不跨 user 行）：
      1. 当前行为纯工具行（text 为空且 toolCalls 非空）→ 无论 last.content 是否为空，把 toolCalls 追加进 last.toolExecutions，不 push 新消息
      2. 当前行有文本且 last.content 为空 → 采纳文本：last.content = text，不 push 新消息（工具卡片已在 last 上，toolResult 回填逻辑不受影响——回填按 toolCallId 在 display 里反向扫描，合并后仍命中）
      3. 当前行有文本且 last.content 非空 → 维持现状 push 新消息（两条都含文本的罕见情形保守不合并，避免改变既有显示语义）
    - 合并进 last 时保持 last 的 id 与 timestamp 不变（首行锚点）
    - toolResult 回填分支、user 分支、JSDoc 同步更新（注明「同回合相邻 assistant 行合并为一条显示消息：content=最终文本、toolExecutions=该回合全部卡片」，引用 G-42-8）

    边界自查：合并只发生在「相邻 assistant 显示消息」之间；两条文本行、跨 user 行、toolResult 无归属丢弃等既有行为全部保持。
  </action>
  <verify>
    <automated>node --check ai-conversations-manager.js && node /tmp/realm-g42-8-smoke.js</automated>
  </verify>
  <done>
    冒烟测试 7 项断言全过：标准工具回合与多轮工具回合合并为单条消息（文本在上、卡片数据完整回填）、纯文本对话无回归、无任何 content:'' 空显示消息产生（中断尾部残留除外）、getAgentMessages 输出逐字段不变。电子 stub 方式：测试脚本内 Module._load 拦截 require('electron') 返回 { app: { getPath: () => 临时目录 } }，initDatabase 落临时目录后走 createConversation/saveMessages/getMessages 真实管线（better-sqlite3 从项目 node_modules 解析，与 42-04 冒烟同法）。
  </done>
</task>

<task type="auto">
  <name>Task 2: renderAIMessages 空 content 气泡守卫（G-42-8 防御修复）</name>
  <files>src/renderer.js</files>
  <action>
    修改 src/renderer.js 的 renderAIMessages（约 7394-7492 行）：在创建 .ai-message-content 气泡 div 之前加守卫——assistant 消息且 msg.content 为空，且不处于「流式末条占位」状态时，跳过气泡 div 的创建与 appendChild（无论有无工具卡片都跳过：有卡片时仅渲染工具卡片容器与消息操作按钮；无卡片的空行不留任何可见气泡）。

    关键约束：
    - 流式占位豁免：state.aiStreaming && isLast 时（实时路径的空 content 占位气泡承载 typing 指示器与流式文本覆盖）必须保留气泡——豁免条件必须先于守卫判断；守卫服务历史恢复路径的一切空 content assistant 行：含 Task 1 保留的中断回合尾部纯工具行，也包括 content 为空且无任何工具调用的 assistant 行（getAgentMessages 在 ai-conversations-manager.js 约 641-642 行防御性跳过同类形状，证明其可能落库）——守卫不得以 toolExecutions 非空为前提
    - 执行注记：下方静态验证 gate 以字面子串 state.aiStreaming && isLast 做 grep——请保持该字面写法，不要改写为逻辑等价的 De Morgan 形式（如 !state.aiStreaming || !isLast），否则 verify 会误判失败
    - 守卫分支内不得引用 content 变量（不创建即不引用）；既有 typing 指示器分支、代码高亮、markdown 渲染逻辑全部保持原位
    - 消息操作按钮（createMessageActions）与 wrapper.dataset.messageId 在守卫分支照常渲染
    - JSDoc 补一句守卫语义（G-42-8：空 content 的 assistant 消息仅渲染工具卡片，不产生空气泡）

    不要改 main.css（呈现层背景+padding 是正常气泡样式，空气泡消失由 DOM 层守卫保证）；不要动 renderToolCard、createMessageActions 及流式更新函数。
  </action>
  <verify>
    <automated>node --check src/renderer.js && [ "$(sed -n '/^function renderAIMessages/,/^}/p' src/renderer.js | grep -c 'state.aiStreaming && isLast')" -ge 1 ] && [ "$(sed -n '/^function renderAIMessages/,/^}/p' src/renderer.js | grep -c '!msg.content')" -ge 1 ]</automated>
  </verify>
  <done>
    语法检查通过；renderAIMessages 函数体内存在以 msg.content 为空为键的守卫分支（不要求工具卡片存在），且守卫显式豁免流式末条占位（state.aiStreaming && isLast，字面形式）；历史恢复渲染不再出现空气泡 div——无论该空行有无工具卡片。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB 行 → 显示层 | ai-conversations.db 存储行经 getMessages 合并后进入 innerHTML 渲染路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-09 | Tampering | getMessages 合并输出 → renderAIMessages innerHTML | low | mitigate | 合并不改变内容来源与消毒路径：最终文本仍经 renderAIMarkdown + DOMPurify（T-21-01），工具卡片仍走 renderToolCard 结构化渲染；冒烟 Test 1-4 锁定 content 只来自既有 text 列解析，无新注入面 |
| T-42-10 | DoS | 同回合合并放大单条显示消息体积 | low | accept | 合并上限即一个 AI 回合的行数（数行），tool_calls/tool_results 列已有 100KB 截断（serializeToolData），无额外风险 |
</threat_model>

<verification>
1. `node --check ai-conversations-manager.js && node --check src/renderer.js` 语法通过
2. `node /tmp/realm-g42-8-smoke.js` 7 项断言全过（显示形状合并正确 + 注入形状不变）
3. 静态门：renderAIMessages 内存在空 content 守卫（不以工具卡片存在为前提）且豁免流式末条占位
4. 人工复测（UAT，执行后由用户操作）：让 AI 调用任一工具 → 完全退出重启 → 打开该对话：AI 文本气泡在上方、工具卡片在下方同属一条消息，无空气泡；切换对话路径同样正确；实时对话（发送/流式/typing 指示器）行为与修复前一致
</verification>

<success_criteria>
- G-42-8 truth 达成：「含工具调用的回复中，AI 文本气泡与工具卡片顺序正确（文本在上、工具卡片在下），无空消息气泡残留」——历史恢复路径（重启打开 + 切换对话）与实时路径同构
- getAgentMessages 注入形状零变化（冒烟 Test 6 断言），CR-01 孤儿 toolCall 合成与 D-14 工具结果上下文语义不受影响
- 存储层零变化（saveMessages / normalizeMessageColumns / 表结构不动），旧格式行兼容读出保持
- 实时流式路径无回归（守卫豁免流式占位）
</success_criteria>

<output>
Create .planning/phases/42-ai-pi-agent/42-06-SUMMARY.md when done
</output>
