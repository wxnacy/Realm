---
phase: 42-ai-pi-agent
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-conversations-manager.js
  - ai-manager.js
  - ipc-handlers.js
  - src/renderer.js
  - .planning/phases/42-ai-pi-agent/42-CONTEXT.md
autonomous: true
requirements:
  - CONV-02
  - CONV-03
gap_closure: true
gap_ids: [G-42-1, G-42-2]

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "应用启动不自动创建对话；无对话时对话历史显示「暂无对话」空状态（G-42-1）"
    - "删除当前/最后一个对话后到达空状态，主进程与 renderer 均不自动补建（G-42-1）"
    - "发送首条消息时惰性创建对话，标题自动取首条用户消息前 30 字符（D-04, G-42-2）"
    - "AI 回复完成后，prompt 响应回传对话 id，renderer 采纳并刷新列表，记录立即可见（G-42-2）"
    - "对话列表元信息显示真实 message_count，不再恒为 0 条（G-42-2）"
    - "重复保存消息不再产生重复行（全量替换事务，G-42-2 附带缺陷）"
  artifacts:
    - "ai-manager.js — 惰性对话生命周期（init 不建行、_ensureConversation、deleteConversation 不补建）"
    - "ai-conversations-manager.js — getConversations 含 message_count + saveMessages 全量替换"
    - "ipc-handlers.js — ai:prompt / ai:prompt-with-context 回传 conversationId"
    - "src/renderer.js — 采纳对话 id、回复后刷新列表、删除流程无自动补建"
    - ".planning/phases/42-ai-pi-agent/42-CONTEXT.md — D-06 决策修订"
  key_links:
    - "prompt 响应 conversationId ↔ renderer state.currentConversationId 采纳"
    - "message_count JOIN ↔ renderConvList 元信息显示"
    - "删除当前对话 ↔ 主进程 currentConversationId 置 null ↔ 空状态可达"
---

<objective>
修复对话生命周期缺陷（G-42-1、G-42-2）：对话创建从「启动急切创建」改为「首条消息惰性创建」，删除不补建，发送链路回传对话 id，列表元信息真实化，saveMessages 消除重复插入。

Purpose: UAT Test 1/2 失败 — 启动自动产生 0 消息「新对话」垃圾行，真实对话记录要点「新对话」才出现。根因是创建时机错误（init 急创建 + 删除补建 + renderer 重复创建）与发送链路无对话交互。
Output: 惰性对话生命周期 + 列表数据真实化 + D-06 决策修订
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-ai-pi-agent/42-UAT.md
@.planning/phases/42-ai-pi-agent/42-CONTEXT.md
@.planning/phases/42-ai-pi-agent/42-01-SUMMARY.md
@.planning/debug/startup-empty-conversation.md
@.planning/debug/conversation-list-not-refreshed.md
@ai-manager.js
@ai-conversations-manager.js
@ipc-handlers.js
@src/renderer.js

**工作区未提交改动（必须保留，不得回退）**：ai-manager.js（_escapeXml 修复、saveCurrentConversation 注释、getConversationMessages 方法）、ipc-handlers.js（ai:configure 校验改为 provider、ai:switch-conversation 返回 messages）、src/renderer.js（createNewConversation 读取 result.conversation.id、移除 ai.newConversation 重复调用）。本计划的修改建立在这些改动之上，修复提交时应将它们一并纳入（或先单独提交）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: ai-conversations-manager.js — message_count + saveMessages 全量替换事务</name>
  <files>ai-conversations-manager.js</files>
  <read_first>
    - ai-conversations-manager.js (getConversations 176-183、saveMessages 264-304 现状)
    - 42-UAT.md G-42-2 (root_cause ②③：列表元信息缺失 + saveMessages id 不稳定重复 INSERT)
  </read_first>
  <action>
1. getConversations（per G-42-2 missing「getConversations 增加 message_count」）：改用 LEFT JOIN + COUNT 聚合，每行附加 message_count 字段。SQL 形态：SELECT c.id, c.title, c.model, c.provider, c.token_total, c.created_at, c.updated_at, COUNT(m.id) AS message_count FROM conversations c LEFT JOIN messages m ON m.conversation_id = c.id GROUP BY c.id ORDER BY c.updated_at DESC LIMIT ?。保留 limit 参数语义。

2. saveMessages 消除重复 INSERT（per G-42-2 根因：pi-ai 的 AgentMessage 无顶层 id 字段，现实现 msg.id || generateId() 每次保存生成新随机 id，INSERT OR REPLACE 变成纯 INSERT，6 条消息存出 14 行）：改为「全量替换」事务——在同一个 db.transaction 内先 DELETE FROM messages WHERE conversation_id = ?，再按数组顺序插入全部消息。saveCurrentConversation 每次都传入完整 transcript，全量替换语义正确且天然反映消息删减。插入时 created_at 取 msg.createdAt || msg.created_at || msg.timestamp || Date.now()（AgentMessage 有 timestamp 字段）。

3. 读侧排序稳定性配套：getMessages（及 Task 涉及的其他读函数）ORDER BY created_at ASC, rowid ASC——同一毫秒产生的多条消息靠插入顺序（rowid）稳定排序，全量替换后 rowid 即 transcript 顺序。

4. 保留现有 100KB 工具结果截断逻辑不变（per RESEARCH Pitfall 3，本计划不改消息内容格式——那是 42-04 的范围）。
  </action>
  <verify>
    <automated>node --check ai-conversations-manager.js && node -e "const cm = require('./ai-conversations-manager'); console.log(typeof cm.getConversations, typeof cm.saveMessages)" && grep -c "message_count" ai-conversations-manager.js</automated>
  </verify>
  <acceptance_criteria>
    - getConversations 返回行包含 message_count 字段
    - saveMessages 事务内先 DELETE 后 INSERT，同表不再累积重复行
    - getMessages 排序含 rowid 稳定 tiebreak
    - node --check 通过
  </acceptance_criteria>
  <done>对话列表查询携带真实消息数；重复保存同一 transcript 不再产生重复行</done>
</task>

<task type="auto">
  <name>Task 2: ai-manager.js — 惰性对话生命周期 + D-04 自动命名</name>
  <files>ai-manager.js</files>
  <read_first>
    - ai-manager.js (init 739-748 急创建块、prompt 821-877、promptWithContext 889-933、deleteConversation 1673-1688、getState 1840-1852)
    - 42-UAT.md G-42-1 (init 急创建 + 删除补建双根因) / G-42-2 (发送链路无建行、D-04 未实现)
    - 42-CONTEXT.md D-04 (标题默认截取第一条用户消息前 30 个字符)
  </read_first>
  <action>
1. init()：删除「创建默认对话」整块（739-748 行，per G-42-1 missing「对话创建改为惰性」）。currentConversationId 保持初始 null，仅 conversationStore.initDatabase() 保留。Agent 实例创建逻辑（709-723）不动——Agent 存在与对话行解耦。

2. 新增私有方法 _ensureConversation(userMessageText)：per G-42-2 missing「发送路径首条消息惰性创建（或认领）对话」与 D-04 自动命名。
   a. 若 this.currentConversationId 为空（含启动后首次发送、删除最后一个对话后再次发送）：调用 conversationStore.createConversation({ title: 派生标题, model: this.activeModelId, provider: this.activeProvider })，设置 this.currentConversationId 与 this.conversationMeta。
   b. 若已有时：查 conversationStore.getConversation(id)，若其 title 仍为默认值「新对话」（用户尚未显式重命名），调用 updateConversation 更新标题为派生标题——显式「新对话」按钮建的行在首条消息落地时也获得真实标题。
   c. 派生标题：userMessageText.trim().substring(0, 30)（per D-04）；空串时保持「新对话」。
   d. 返回对话 id。

3. prompt() 与 promptWithContext()：在初始化检查与 isProcessing 检查之后、agent.prompt() 调用之前调用 this._ensureConversation(原始用户消息文本)——promptWithContext 必须传 _buildMessageWithContext 之前的原始 message，避免标题混入 XML 引用块。两方法成功路径返回 this.currentConversationId（供 IPC 回传 renderer），失败/早退路径返回 null。JSDoc 同步更新 @returns。

4. deleteConversation()：删除「删除当前对话时先 createNewConversation」的自动补建分支（1678-1681，per G-42-1 missing「删除当前/最后一个对话不再自动补建，允许到达空状态」）。替换为：若 conversationId === this.currentConversationId，先 this._cleanupCurrentAgent() 并置 this.currentConversationId = null、this.conversationMeta = {}，再执行 conversationStore.deleteConversation。删除非当前对话行为不变。

5. getState()：返回对象增加 conversationId: this.currentConversationId || null 字段（per G-42-2 根因③「renderer 永远不知道主进程的 currentConversationId」，作为 prompt 响应之外的兜底通道）。

注意：createNewConversation()（显式「新对话」按钮链路）保留不动；newConversation() 委托保留。
  </action>
  <verify>
    <automated>node --check ai-manager.js && grep -c "_ensureConversation" ai-manager.js && awk '/deleteConversation\(conversationId\) \{/,/^  \}/' ai-manager.js | grep -c "createNewConversation" </automated>
  </verify>
  <acceptance_criteria>
    - init() 不再调用 conversationStore.createConversation，启动零对话行
    - _ensureConversation 实现惰性创建 + 默认标题自动改名两条路径
    - prompt / promptWithContext 成功路径返回 conversationId
    - deleteConversation 删除当前对话时清理 Agent 并置空 currentConversationId，不补建
    - getState 含 conversationId
  </acceptance_criteria>
  <done>对话行仅在首条消息或显式新建时产生；删除最后一个对话后系统到达空状态；标题遵循 D-04</done>
</task>

<task type="auto">
  <name>Task 3: IPC 回传对话 id + renderer 采纳与删除流程修正 + D-06 修订</name>
  <files>ipc-handlers.js, src/renderer.js, .planning/phases/42-ai-pi-agent/42-CONTEXT.md</files>
  <read_first>
    - ipc-handlers.js (ai:prompt 1640-1650、ai:prompt-with-context 1659-1669)
    - src/renderer.js (handleSendAIMessage 7715-7776、deleteConversation 7028-7044、loadConversations 6665-6676)
    - 42-CONTEXT.md D-06 现文本
  </read_first>
  <action>
1. ipc-handlers.js（per G-42-2 missing「把对话 id 回传给 renderer（如通过 prompt 响应）」）：
   - ai:prompt 处理器：const conversationId = await aiManager.prompt(message); 返回 { success: true, conversationId }。
   - ai:prompt-with-context 处理器：同样采纳 promptWithContext 返回值，返回 { success: true, conversationId }。

2. src/renderer.js handleSendAIMessage（per G-42-2 truth「AI 回复完成后，对话历史列表立即能看到新对话记录」）：
   - 接住两个 IPC 的返回值：有 @ 引用分支 await window.realmAPI.ai.promptWithContext({...})、无引用分支 await window.realmAPI.ai.prompt(text)，取 result.conversationId 非空时更新 state.currentConversationId。
   - await 完成后（本轮 run 结束）调用 loadConversations() 刷新列表——下拉打开时记录/标题/message_count 立即可见，关闭时下次打开由 toggleConvDropdown 的既有 loadConversations 兜底。放 try 块内 prompt 调用之后，不阻塞既有错误处理路径。
   - 不改 streaming 事件处理逻辑。

3. src/renderer.js deleteConversation（per G-42-1 missing「移除 renderer 删除流程中的重复 createNewConversation」+「删除当前/最后一个对话不再自动补建」）：
   - 移除「如果删除的是当前对话，自动创建新对话」分支（7032-7034 的 await createNewConversation()）。
   - 替换为：删除成功后若 conversationId === state.currentConversationId，置 state.currentConversationId = null、state.aiMessages = []、清空 elements.aiMessageList.innerHTML（对齐主进程的置空语义），随后无条件 loadConversations()——空库时 renderConvList 渲染「暂无对话」空状态。
   - createNewConversation() 函数本体保留（显式「新对话」按钮 handleNewConversation 仍使用）。

4. .planning/phases/42-ai-pi-agent/42-CONTEXT.md 修订 D-06（per G-42-1 missing「修订 42-CONTEXT.md 的 D-06 决策」）：用 Edit 就地改写 D-06 条目，保留 D-06 编号，标注修订日期与依据。新语义：对话创建为惰性——应用启动与打开 AI 面板均不自动建行；对话在首条用户消息发送时惰性创建（标题按 D-04 自动取首条消息前 30 字符）或由用户显式点「新对话」创建；删除当前对话不自动补建，允许到达「暂无对话」空状态。原文「每次打开 AI 面板自动新建对话」删除线标注并附一句修订说明（UAT G-42-1：0 消息垃圾行违反用户预期）。同步把 <decisions> 内 D-06 引用处（如有）改一致；不动其他决策。
  </action>
  <verify>
    <automated>node --check ipc-handlers.js && node --check src/renderer.js && grep -c "conversationId" ipc-handlers.js && grep -c "conversationId" .planning/phases/42-ai-pi-agent/42-CONTEXT.md && awk '/^async function deleteConversation/,/^}/' src/renderer.js | grep -c "createNewConversation"</automated>
  </verify>
  <acceptance_criteria>
    - ai:prompt 与 ai:prompt-with-context 响应含 conversationId
    - renderer 采纳 conversationId 并在回复完成后刷新列表
    - renderer 删除流程不再调用 createNewConversation（该函数仅剩显式新建按钮一处调用）
    - 42-CONTEXT.md D-06 为惰性创建语义且保留修订标注
    - node --check 全部通过
  </acceptance_criteria>
  <done>发送消息后记录立即可见且高亮正确；删除当前对话到达空状态；D-06 决策文档与现实一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→IPC | prompt 响应新增 conversationId 字段跨信任边界回传 |
| user input→DB | 用户消息文本派生对话标题写入 conversations 表 |
| UI→user | 对话标题回显到列表 DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-06 | Tampering | 标题派生（_ensureConversation） | low | mitigate | 标题截断 30 字符；落库走参数化查询；列表渲染已有 textContent 路径，无 HTML 注入面 |
| T-42-07 | Spoofing | ai:prompt / ai:prompt-with-context | medium | mitigate | 沿用 assertTrustedSender 既有校验，不新增未受信调用方 |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | 无新增依赖 |
</threat_model>

<verification>
1. npm run dev 启动后打开 AI 面板 → 对话历史显示「暂无对话」，数据库无 0 消息「新对话」行
2. 发送一条消息 → AI 回复完成后打开对话历史，记录立即可见：标题为消息前 30 字符、消息数 ≥ 2、当前高亮
3. 右键删除当前对话 → 列表回到空状态，重启应用不再出现垃圾行
4. 检查 realm-test/realm-dev 的 ai-conversations.db：连续两轮对话后 messages 表行数与 transcript 一致，无重复
</verification>

<success_criteria>
- 启动零对话行，空状态可达（G-42-1）
- 首条消息惰性建行 + D-04 自动命名 + id 回传 + message_count 真实化（G-42-2）
- saveMessages 全量替换，无重复行
- D-06 决策已修订并保留修订标注
- 工作区既有未提交改动被保留并随修复一并提交
</success_criteria>

<output>
创建 `.planning/phases/42-ai-pi-agent/42-03-SUMMARY.md` when done
</output>
