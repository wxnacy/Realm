---
phase: 48-skill-name
plan: 6
type: execute
wave: 5
depends_on: [48-04, 48-05]
gap_closure: true
gap_ids: [G-48-3]
files_modified:
  - src/renderer.js
  - tests/test-skill-picker-model.js
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/phases/48-skill-name/48-01-PLAN.md
  - .planning/phases/48-skill-name/48-02-PLAN.md
  - .planning/phases/48-skill-name/48-VALIDATION.md
autonomous: true
requirements: [DISC-02, DISC-06]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "运行期新增技能（AI 经 `write`/`bash` 建目录 → 主进程 idle 边界重扫并广播）+ **从未打开过 `/` 面板** → 手打 `/skill:<新名>`：请求真的发到主进程、由主进程在**调用那一刻**实时读盘并成功调用 —— 修复前渲染端凭可能陈旧的 `state.aiSkills` 快照否决，输入框被清空、主进程从未收到请求（违反用户 2026-09-11「显式调用必须实时读盘」硬约束）"
    - "不存在 / 已禁用技能的用户可见行为**逐字不变**：仍是一条 system-note、仍无残留气泡 —— 文案与判定**单源在主进程**（`skillErrorFromReason`），渲染端只呈现、不复制文案（D-13 / D-10 的用户可见契约不变，改的只是判定发生的位置）"
    - "渲染端不再存在任何本地否决分支：技能分支只剩「按 kind 分流 → 记录 ref → 中止上一轮 → 就地复位流式状态」四步"
    - "`skills:changed` 广播到达即重拉快照（不再因面板关闭早退），且**绝不**触发 `refreshSkills()`（渲染端 `.refreshSkills(` 全文件仍只有 `openSlashPicker` 一处 —— 自激回路 P-48-06 不回归）；启动预热保留，理由改为「面板首帧零延迟、不闪空态」"
    - "跨文件护栏成立：两条失败文案在 `ai-manager.js` 的 `skillErrorFromReason` 里仍在、渲染端不再出现它们；删除任一侧的实现都会让测试变红（防两侧同时消失后无人发现）"
    - "计划文本与实现一致：`48-01-PLAN.md` Task 3 与 `48-02-PLAN.md` 中「要求渲染端本地预检」的明文改为新口径并留删除痕迹；`48-VALIDATION.md` 的对应矩阵行改述为新判据面（DISC-07 手打入口面由主进程 D 组断言 + 渲染端无否决的源码断言共同承担）"
    - "项目指令文件的维护约定被实际执行：`AGENTS.md:267` 的技能域测试清单登记本阶段新增的 `node --test tests/test-ai-cancel-state.js`，并同步 `node --test tests/test-skill-picker-model.js` 条目的覆盖面描述；`AGENTS.md:272`（改实时读盘口径 / 边界技能行为时「必须同步更新该章节与本节测试清单」）因此不再是只改产品文档、漏改指令文件的状态"
  artifacts:
    - "`src/renderer.js`：`handleSendAIMessage` 斜杠分支删除两段本地否决（以 `const known = (Array.isArray(state.aiSkills) ? state.aiSkills : [])` 开头的那段及其后的 `known.disabled` 段），`kind === 'skill'` 分流与「未知命令」`else` 分支逐字保留；`skills:changed` 监听去掉面板关闭早退，改无条件 `pullAiSkillsSnapshot()`；`state.aiSkills` / `aiSkillsDigest` **保留**（面板的数据源）"
    - "`tests/test-skill-picker-model.js`：`:302-325` 用例改写为「技能分支只按 kind 分流、无本地否决」；`:416-440` 去掉早退断言、改为断言无条件重拉；`:442-449` 启动预热断言保留"
    - "`tests/test-ai-skills.js`：`:2158-2178` 两条 F 组用例改写（位置保留、文案改为「渲染端不得出现」+ 跨文件断言主进程仍是唯一来源）；`:2260-2272` G 组广播处理器用例去掉早退断言"
    - "`docs/product/ai-skills.md`（**本计划是 gap 收敛轮的唯一文档写者**）：§10.3 补「存在性/启停判定只在主进程、渲染端不得用快照否决」；§10.7 补两条（广播到达即重拉快照 / 流式中调用技能不写取消文案且新气泡即时呈现）；§10.4 表下补两条注（目录名权威在显式调用路径的落地 / 两条失败文案的唯一来源是主进程）；新增 §10.8「用户气泡契约（技能调用）」；§七 测试清单补 `node --test tests/test-ai-cancel-state.js` 并回填 `node tests/test-ai-skills.js` 的实测例数（取 48-04 SUMMARY 的值）"
    - "`.planning/phases/48-skill-name/48-01-PLAN.md` / `48-02-PLAN.md` / `48-VALIDATION.md`：按 G-48-3 修订明文，带日期与 gap id 标记"
    - "`AGENTS.md`（**项目指令文件**，`CLAUDE.md` / `CODEBUDDY.md` 是指向它的符号链接）：`:267` 的技能域测试清单补 `node --test tests/test-ai-cancel-state.js` 条目、并更新 `node --test tests/test-skill-picker-model.js` 条目的覆盖面描述；其余条目与 `:272` 的维护约定文字**不动**"
  key_links:
    - "失败通道唯一化：主进程 `skillErrorFromReason` → `_resolveSkillInvocation` 的 `{skillError}` → IPC 返回 → 渲染端 `removeSkillFailureBubbles(userMsgId)` + `pushSystemNote(result.skillError.message)`（`:8851-8859`）—— 本计划后它是**唯一**判定与文案通道，渲染端不再有前置闸门"
    - "快照链路：`pullAiSkillsSnapshot()` 只调 `realmAPI.ai.getSkills()`（零 IO 同步投影）；广播处理器绝不调 `refreshSkills()`（后者会触发主进程重扫并再广播 → 自激）。本计划把「仅在面板打开时重拉」放宽为「无条件重拉」，两侧约束（只读快照 / 不触发重扫）都不动"
  prohibitions:
    - statement: "不得为保住「零气泡零 IPC」而把本地否决改成「先查快照、未命中再问主进程」之类的折中（例如仅当快照为空时才发主进程）—— 该折中同样让陈旧快照参与授权，未命中分支仍会吞掉运行期新增技能"
      status: unresolved
      verification: judgment
      reason: "UAT 裁决（2026-09-12，方案 A）明确要求移除本地否决、改由主进程 `skillError` 走既有回滚；用户可见契约（文案 + 零残留气泡）由主进程文案 + `removeSkillFailureBubbles` 完整承担，不需要渲染端参与判定"
    - statement: "不得删除 `state.aiSkills` / `state.aiSkillsDigest`，也不得改动 `openSlashPicker` 的 stale-while-revalidate 链路（同步快照渲染 + 后台 `refreshSkills`）"
      status: unresolved
      verification: judgment
      reason: "两者仍是 `/` 面板的数据源（D-17 / P8 门禁触发点）；本计划只解除「快照对发送路径的否决权」，不改变面板的刷新语义"
    - statement: "不得改 `48-VERIFICATION.md` 本身，也不得改任何其它阶段的文件（`48-01-PLAN.md` / `48-02-PLAN.md` / `48-VALIDATION.md` 属本阶段、按 UAT 的 `missing` 第 3 条更新；`AGENTS.md` 是项目指令文件的文档收口对象、不属「其它阶段文件」，见 Task 2 第 6 步）"
      status: unresolved
      verification: judgment
      reason: "改本阶段 PLAN 会让 48 的 `covered_digest` stale（预期，收尾时重算）；改 48-VERIFICATION.md 等于自证，改其它阶段会让 44–47 的验证指纹 stale"
---

<objective>
关闭 UAT gap **G-48-3**：渲染端在发送路径用可能陈旧的 `state.aiSkills` 快照做**本地否决**，使「运行期新增技能 + 从未打开过 `/` 面板」这一完全正常的序列走进死路（判「未找到技能」且清空输入框，主进程从未收到请求、其唯一被要求的「调用那一刻实时读盘」没有机会执行），与用户 2026-09-11 的硬约束直接冲突。

Purpose: 该预检是**纯冗余** —— 主进程对两种失败产出的文案与渲染端本地分支一字不差（`ai-manager.js:6087-6098`），删掉后用户可见行为零变化，但「实时读盘」恢复为唯一判定路径。属对 48-01 Task 3 ② / 48-02 明文要求的计划偏离，由 UAT 裁决（2026-09-12，方案 A）背书，故计划文本须同步修订。
Output: 渲染端移除本地否决 + 广播无条件重拉快照 + 两套测试断言同步 + 文档与计划文本修订。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-UAT.md
@.planning/phases/48-skill-name/48-REVIEW.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-01-PLAN.md
@.planning/phases/48-skill-name/48-02-PLAN.md
@.planning/phases/48-skill-name/48-VALIDATION.md
@src/renderer.js
@ai-manager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除渲染端本地否决 + 广播无条件重拉快照 + 两套断言同步</name>
  <files>src/renderer.js, tests/test-skill-picker-model.js, tests/test-ai-skills.js</files>
  <read_first>
    - `src/renderer.js:8700-8745`（`handleSendAIMessage` 的斜杠分支：本地命令命中 → 技能分支 → 未知命令 else；`skillRef` 与 `bubbleContent` 的用法）
    - `src/renderer.js:4397-4406`（`skills:changed` 监听 + 启动预热，含现有注释的两条理由）
    - `src/renderer.js:8848-8860`（`skillError` 回滚链路：`removeSkillFailureBubbles` + `pushSystemNote(result.skillError.message)` + 复位流式状态 —— 删除本地否决后唯一剩下的失败通道）
    - `ai-manager.js:6086-6100`（`skillErrorFromReason`：两个错误码 + 两条文案的**唯一来源**）
    - `tests/test-skill-picker-model.js:275-325`（C 组 renderer 护栏：`functionBody` / `asyncFunctionBody` helper 与「技能预检分支」用例）、`:416-449`（广播监听与启动预热两条用例）
    - `tests/test-ai-skills.js:2148-2179`（F 组两条用例）、`:2258-2272`（G 组广播处理器用例）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `G-48-3`（root_cause 三段 + `missing` 四条）
  </read_first>
  <action>
    1. `src/renderer.js` 的 `handleSendAIMessage` 斜杠分支：删除技能分支里的两段本地否决（第一段以 `const known = (Array.isArray(state.aiSkills) ? state.aiSkills : [])` 开头、含清空输入框与系统提示后 `return`；第二段为紧随其后的 `known.disabled === true` 分支）—— 保留的代码只剩：`if (ref && ref.kind === 'skill') { skillRef = ref; await abortAIIfStreaming(); state.aiStreaming = false; state.aiCurrentMessageId = null; updateSendButtonState(false); }`，其后 `else` 的未知命令分支与再往后的 `if (state.aiStreaming) return;` 全部逐字不动（`kind === 'skill'` 分流与「技能是第二命令源、绝不并入 SLASH_COMMANDS」的既有结论不变）。
    2. 同一处的注释改写为：不做本地否决（48 G-48-3）—— 快照可能陈旧（运行期新增技能、另一窗口刚启用/禁用），而「调用那一刻实时读盘」是硬约束；存在性/启停一律由主进程当场裁定，失败经响应里的 `skillError` 走既有回滚链路呈现（文案与判定单源在主进程）。
    3. `skills:changed` 监听（`:4398-4405`）：删去面板关闭时早退的那一行（`:4401`），处理器体直接调 `pullAiSkillsSnapshot()`；把**既有的两行注释（`:4398-4399`，位于 `onIpcMessage('skills:changed'` 行**之上**）就地改写为新口径：广播到达即重拉快照（快照是 `/` 面板首帧的同步数据源，陈旧会让面板闪一下空态或显示上次投影），**仍绝不**触发 `refreshSkills()`（「广播 → 刷新 → 再广播」会自激，P-48-06 约束不变）。启动预热那一行（`:4405`）保留，其注释的旧理由（预检误判）改写为「面板首帧用内存快照同步渲染，预热让首次打开不出现空态闪烁」。
       **注释位置是硬约束，不是风格偏好**：`tests/test-skill-picker-model.js:419-421` 与 `tests/test-ai-skills.js:2262-2264` 各取 `rendererSrc.slice(idx, idx + 300)`（`idx` = `onIpcMessage('skills:changed'` 的下标）并断言该片段内**不含** `refreshSkills` 字面量 —— 这两条断言在 §4 / §5 中被明确**保留**（不得删除、不得改写成别的判据）。因此本任务的改写**不得**把含该字面量的解释性注释写进该行之后的 300 字符窗口（即处理器体内，以及紧随其后的启动预热注释）—— 解释一律留在 `onIpcMessage('skills:changed'` 行**之上**，处理器体内只留执行语句；若确有必要在附近写「为什么不重扫」的理由，写在**监听器挂载点之上**。
    4. `tests/test-skill-picker-model.js`：
       - 把 `:302-325` 的「技能预检分支：拒绝条件只有 disabled === true…」用例改写为「技能分支：只按 kind 分流，无本地否决（48 G-48-3）」。取区域用 `body.slice(body.indexOf("kind === 'skill'"), body.indexOf('} else {', guardIdx))`（不要用固定长度窗口，避免把 `else` 分支的未知命令提示卷进来），过滤掉以 `//` 开头的行后断言：含 `abortAIIfStreaming(`；**不含** `state.aiSkills`；**不含** `pushSystemNote(`；**不含** `disableModelInvocation`；**不含** 「已遮蔽」；并跨文件断言 `ai-manager.js` 仍含 `skillErrorFromReason`（判定与文案的唯一来源）。
       - 把 `:416-440` 的「skills:changed 监听…」用例中的「面板关闭时必须早退」断言删掉，改为断言 `pullAiSkillsSnapshot()` 在处理器体内直接出现（无前置早退），并保留原有的两条：处理器内不含 `refreshSkills`、`renderer` 全文件 `.refreshSkills(` 计数为 1。
       - `:442-449` 的启动预热用例（同一挂载点至少两次 `pullAiSkillsSnapshot()`）**保留不动**（预热仍在）。
    5. `tests/test-ai-skills.js`：
       - `:2158-2170` 用例改写为「技能分支位置在 `aiStreaming` 守卫之前 + 无本地否决」：保留 `body.indexOf("kind === 'skill'")` 早于 `body.indexOf('if (state.aiStreaming) return;')` 的位置断言与「未知命令 」文案断言；把原先断言渲染端含两条失败文案的两行改为断言**不含**（字面量照抄 `ai-manager.js` 的 `skillErrorFromReason` 内现成字符串，不新写文案），并保留一条「`unknown command` 路径仍走本地提示」的正向断言。
       - `:2172-2178` 用例改写为「`state.aiSkills` / `aiSkillsDigest` 仍建立（面板数据源），但发送路径不得读它」：前两条 `rendererSrc.includes('aiSkills:')` / `('aiSkillsDigest:')` 保留；第三条 `body.includes('state.aiSkills')` 的**正**断言反转为**负**断言。
       - `:2260-2272` G 组广播处理器用例：删掉「面板关闭时必须早退」断言，其余（含 `refreshSkills` 零命中与全文件计数 1）保留。
       - 跨文件单源断言：断言 `ai-manager.js` 的 `skillErrorFromReason` 内**仍含**两条文案字面量（把它钉成「两侧不可能同时被删除」的护栏 —— 这是删除本地否决后防 UX 静默丢失的关键断言）。
    6. 改完后自检（写进 SUMMARY）：`grep -n "state.aiSkills" src/renderer.js` 的命中应只落在面板侧（`openSlashPicker` / `renderSlashPickerList` / `pullAiSkillsSnapshot` 与 state 声明），**不落在** `handleSendAIMessage` 内。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --check src/renderer.js &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（`node --check` 语法错，或 `node --test` 有任何断言失败），或输出中 "# fail" 行 > 0，或 "# pass" 行 < 93（picker 既有基线地板），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1 &amp;&amp; node --test tests/test-ai-cancel-state.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（任一测试套件有断言失败），或输出中 "# fail" 行 > 0，或（ai-skills）"# pass" 行 < 132（48-04 完成后的地板）、（ai-cancel-state）"# pass" 行 < 7，或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const s=require('fs').readFileSync('src/renderer.js','utf8');const a=s.indexOf('async function handleSendAIMessage(');const b=s.indexOf('if (state.aiStreaming) return;', a);const body=s.slice(a,b);const bad=['state.aiSkills','known.disabled'].filter(x=&gt;body.includes(x));if(bad.length){console.error('local veto left in send path:',bad.join(','));process.exit(1)}if(!body.includes('abortAIIfStreaming(')){console.error('abort call missing');process.exit(2)}console.log('send path has no local veto')"</automated>
    <fails_when>非零退出，或出现 'local veto left in send path: …' / 'abort call missing' 任一行，或标准输出不含 "send path has no local veto"</fails_when>
  </verify>
  <done>
    发送 `/skill:<name>` 时渲染端不再做任何存在性/启停判定：请求一律发到主进程，由它当场读盘裁定；不存在 / 已禁用的用户可见结果与修复前逐字相同（同一条 system-note、零残留气泡）。`skills:changed` 到达即重拉快照且仍不触发重扫。四条测试命令全绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: 文档收口（产品文档全量 + 计划/验证文本修订注记）</name>
  <files>docs/product/ai-skills.md, AGENTS.md, .planning/phases/48-skill-name/48-01-PLAN.md, .planning/phases/48-skill-name/48-02-PLAN.md, .planning/phases/48-skill-name/48-VALIDATION.md</files>
  <read_first>
    - `docs/product/ai-skills.md:288-322`（§10.3 实时读盘口径 / §10.4 边界技能行为表与表下注）、`:350-361`（§10.7 诚实边界）
    - `.planning/phases/48-skill-name/48-01-PLAN.md:181`（`readSkillForInvocation` 的实现要求）、`:332-333`（技能预检两条行为行）、`:341-347`（Task 3 ①②：`state.aiSkills` 与两段本地分支的明文要求）、`:361`（`skills:changed` 早退要求）、`:370`、`:403-405`（要求断言预检分支含禁用判据的验收文本）
    - `.planning/phases/48-skill-name/48-02-PLAN.md:35`、`:59`、`:61`、`:123`、`:202`（「走 48-01 已接线的技能预检」措辞）
    - `.planning/phases/48-skill-name/48-VALIDATION.md:75`（renderer 预检拒绝条件的矩阵行）
    - `.planning/phases/48-skill-name/48-04-SUMMARY.md`（`node tests/test-ai-skills.js` 的实测例数 —— §七 补注的取值来源）、`48-05-SUMMARY.md`（登记的**四条**待写文档面，含 `AGENTS.md` 测试清单登记）
    - `AGENTS.md:260-272`（技能域「测试」清单行 `:267` 与被它引用的维护约定行 `:272` —— 本计划是唯一同步点；`CLAUDE.md` / `CODEBUDDY.md` 是指向该文件的符号链接，改它就是改项目指令文件本身）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `G-48-3` → `missing` 第 3 条（要求同步计划与文档明文）
  </read_first>
  <action>
    1. `docs/product/ai-skills.md`（本轮 gap 的**全部**文档面在此收口；48-04 / 48-05 不改本文件）：
       - §10.3 实时读盘口径 追加一条（G-48-3）：显式调用的**存在性 / 启停判定只在主进程** —— 渲染端不得用缓存快照做前置否决（快照可能陈旧：运行期新增技能、另一窗口刚启用或禁用）；因此主进程的读盘在任何情况下都真的被执行。仍然明确「注入之后该正文就是普通历史消息，跨轮不重读」的既有口径不变。
       - §10.7 诚实边界 追加两条：① （G-48-3）技能集快照在 `skills:changed` 广播到达时**无条件**重拉（不再仅在 `/` 面板打开时）；处理器只调零 IO 的读取投影，绝不触发主进程重扫（自激回路约束不变）—— 并把该节里「仅在面板打开时重拉快照并原地重渲染」的旧表述改为新口径，两处不得并存。② （G-48-4 + G-48-6）流式回复进行中（或正卡在工具确认卡片上）发起技能调用：上一轮先被中止（该轮气泡按用户取消处理），**新一轮消息照常发出并正常流式回显**；迟到到达的取消信号只作用于**被取消的那条消息**。技能调用发出后，用户气泡的三件套**即时**呈现，**不需要**切换对话 / 重载页面来触发。
       - §10.4 表下注（「两条必须分清的易混语义」列表之后）追加两条：① （G-48-2）名称以目录名权威在**显式调用路径**上的落地 —— 面板与手打都用目录名，frontmatter `name` 与目录名不一致的技能按目录名调用，注入块的 `name` 属性与 provenance 行同样取目录名（防冒名）。② （G-48-3）两条失败提示的**唯一来源是主进程**（错误码与文案同一处产出），渲染端只呈现、不复制文案、不做本地否决 —— 因此「点了没反应」不可能出现，运行期新增的技能当场可调用。表内两行用户可见文案**不改**。
       - 新增 §10.8「用户气泡契约（技能调用）」（G-48-6）：pill（`技能` 微标 + 技能名，技能名经 DOM `textContent` 注入）+ 正文 = **args 原文**（不含 `/skill:` 前缀）+ 默认折叠的「技能正文（N 字符）」块（N = JS `String.length`，与 prompt 段预算同口径，不是字节数）；折叠块默认折叠、点击表头切换；发送后**即时**呈现；重发 / 重试路径以本次读盘结果覆盖该块（不留上一轮旧值）。
       - §七 测试清单（`:96-104`）：补一行 `node --test tests/test-ai-cancel-state.js`（取消归属与用户气泡时序的纯逻辑用例 + renderer 接线护栏）；并在 `:98` 的 `node tests/test-ai-skills.js` 条目上**新增**例数标注 —— ⚠ 该行**当前没有任何例数**（`grep -n "129" docs/product/ai-skills.md` 零命中；129 是本机实测的当前通过数，不是文档里已存在的数字），所以本步是**补注**而非「回填 / 不沿用旧值」：从 48-04 SUMMARY 的实测输出取值，写成形如「（N 例，实测）」的括注即可，并在 SUMMARY 里记下「该条原本无条件例数、本例数为新增」这一事实。**不要**为了让某处数字对齐而改测试或改其它文档行。
    2. `48-01-PLAN.md`：Task 1 的 `readSkillForInvocation` 实现要求（`:181` 内「`fresh = skills.find(s => s.name === name) || skills[0]`」与「`fresh.name !== name` → `not_found`」两句）按 **G-48-2** 的新判据修订：同一性改由**目录路径全等**判定，命中后注入用的 `name` 重写为目录名；同样带 `（G-48-2 修订，2026-09-12）` 标记与一行「（原文：…，已被 G-48-2 取代）」留档（与 48-04 的实现逐字对齐）。
    3. `48-01-PLAN.md`：Task 3 的 ①② 与 ③/⑤ 验收文本按新口径修订，每处修订都带 `（G-48-3 修订，2026-09-12）` 标记，并在被取代的旧句后以一行「（原文：…，已被 G-48-3 取代）」留档（保留审计痕迹，不静默改写历史）：
       - `:332-333` 两条行为行：原「本地查快照 → 未命中/已禁用 → 清空输入框 + 系统提示 + 零 IPC」改为「技能分支不做本地否决；存在性/启停由主进程当场读盘裁定，失败经响应 `skillError` 回滚气泡并以主进程文案出 system-note（用户可见结果不变）」。
       - `:341` 的 `state.aiSkills` 新增要求与 `:343-347` 的实现步骤 ②：删去「在 `state.aiSkills` 按 name 查」与两段分支的要求，改为「`state.aiSkills` 仅服务 `/` 面板；发送路径零读取」。
       - `:361` 的 `skills:changed` 要求：把「处理器内 `if (!state.slashPickerOpen) return;`」改为「无条件重拉快照」。
       - `:370` / `:403-405` 的测试验收文本：把「断言预检分支的拒绝条件只有 `disabled === true`」改为「断言技能分支**无**本地否决（不读快照、无系统提示调用、不因 `disableModelInvocation` 拒绝），且两条失败文案的唯一来源在主进程（跨文件断言）」。
    4. `48-02-PLAN.md`：**目标行必须先实测重新推导，不得直接沿用任何固定行号清单**（行号会因上文编辑而位移，且旧清单含不含该措辞的行）。做法：先跑 `grep -n "技能预检" .planning/phases/48-skill-name/48-02-PLAN.md`，把**每一处命中**都按下一段改述。当前实测命中 **5 处**（供交叉核对，非绑定清单）：`:35`（`must_haves.truths`「走 48-01 已接线的技能预检与主进程权威解析」）、`:61`（`key_links`「48-01 已接线的斜杠技能预检」）、`:202`（`<read_first>`「48-01 已插技能预检，本任务只调用不改」）三处是「已接线」措辞；`:238`（「漏了它渲染端读到的全局是 `undefined`，技能预检与本面板都不能工作」）与 `:435`（`<verification>`「缺此面板与技能预检都失效」）两处是渲染端预检移除后**已失效**的表述。
       - 改法：每处「技能预检」改为「技能调用分支（G-48-3 后渲染端无本地否决，存在性由主进程当场裁定）」，其余文字不动；并在每处改写**紧邻**留一行「（原文：…，已被 G-48-3 取代）」以保留审计痕迹（留档行的引文含旧措辞，故必须与取代标记**同行** —— 下面的门据此判定「旧措辞不得以未归档形态存活」）。
       - **明确排除**（旧清单里这两项是空的，改它们等于空改/误改）：`:59`（`buildPickerItems(state.aiSkills, …)` 的 `key_link`）与 `:123`（`<precondition>` 段）—— 两处**不含**「技能预检」措辞。若 `grep` 实测与上述 5 处不一致，**以 `grep` 为准**。
    5. `48-VALIDATION.md:75` 那一行的「验收点」描述改述为：「技能分支无本地否决（不读快照、不复制失败文案）；`disableModelInvocation` 不参与任何拒绝条件 —— 手打入口面由主进程 `readSkillForInvocation` 的 D 组断言 + 本行渲染端源码断言共同承担」；覆盖命令列保持不变（仍是 `node --test tests/test-skill-picker-model.js`）。
    6. `AGENTS.md`（项目指令文件；`CLAUDE.md` / `CODEBUDDY.md` 是指向它的符号链接 —— 漏改这里等于漏改项目指令）：
       - 在 `:267` 的技能域测试清单行**追加**一个条目：`` `node --test tests/test-ai-cancel-state.js`（取消归属与用户气泡时序的纯逻辑用例 + renderer 接线护栏）`` —— 该测试文件由 48-05 新建，登记动作归本计划（wave 5，晚于 48-05，文件此时已存在）。
       - 同一条清单里把 `node --test tests/test-skill-picker-model.js` 的覆盖面描述补齐为它的既有描述 + 「/ 气泡构建单源与回填后定向刷新」；其余条目与文字**不动**。
       - `:272` 的维护约定文字**不改**（它是被执行的规则，不是待更新的旧表述）；本步是对它的**执行**。理由写进 SUMMARY：`docs/product/ai-skills.md` 的「发现与调用」章节与本节测试清单必须成对同步，只改其一即为违反该约定。
    7. 不要改 `48-VERIFICATION.md`、不要改任何其它阶段的文件；在本计划的 SUMMARY 写明「`48-01-PLAN` / `48-02-PLAN` / `AGENTS.md` 被修订 → 48 的 `covered_digest` 会 stale，收尾时重算」。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const P='.planning/phases/48-skill-name/';const files=['48-01-PLAN.md','48-02-PLAN.md','48-VALIDATION.md'];const miss=files.filter(f=&gt;!fs.readFileSync(P+f,'utf8').includes('G-48-3'));if(miss.length){console.error('缺 G-48-3 修订标记：'+miss.join(','));process.exit(1)}console.log('G-48-3 markers present in all three')"</automated>
    <fails_when>非零退出，或出现 '缺 G-48-3 修订标记：…' 任一行，或标准输出不含 "G-48-3 markers present in all three"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/48-skill-name/48-02-PLAN.md','utf8');const l=s.split('\n').filter(x=&gt;x.includes('技能预检'));const bad=l.filter(x=&gt;!x.includes('已被 G-48-3 取代'));if(bad.length){console.error('48-02 仍有 '+bad.length+' 处未归档的旧措辞：\n'+bad.join('\n'));process.exit(1)}if(!s.includes('技能调用分支')){console.error('48-02 未见新措辞「技能调用分支」');process.exit(2)}console.log('48-02 wording revised, archived mentions='+l.length)"</automated>
    <fails_when>非零退出，或出现 '仍有 … 处未归档的旧措辞' / '未见新措辞' 任一行，或标准输出不含 "48-02 wording revised"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const d=fs.readFileSync('docs/product/ai-skills.md','utf8');const miss=['ai-cancel-state','用户气泡契约','无条件'].filter(x=&gt;!d.includes(x));if(miss.length){console.error('docs/product/ai-skills.md 缺：'+miss.join(','));process.exit(1)}if(d.includes('仅在面板打开时')){console.error('§10.7 旧限定仍在（须改为无条件重拉，两处不得并存）');process.exit(2)}const a=fs.readFileSync('AGENTS.md','utf8');if(!a.includes('tests/test-ai-cancel-state.js')){console.error('AGENTS.md 测试清单未登记 test-ai-cancel-state.js');process.exit(3)}console.log('docs + AGENTS.md test list synced')"</automated>
    <fails_when>非零退出，或出现 'docs/product/ai-skills.md 缺：…' / '§10.7 旧限定仍在' / 'AGENTS.md 测试清单未登记 …' 任一行，或标准输出不含 "docs + AGENTS.md test list synced"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1 &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1</automated>
    <fails_when>非零退出（任一测试套件有断言失败），或输出中 "# fail" 行 > 0，或（ai-skills）"# pass" 行 < 132、（picker）"# pass" 行 < 93，或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `docs/product/ai-skills.md` 的 §10.3 / §10.7（两条）/ §10.4 表下注（两条）/ §10.8（新）/ §七 全部落地，旧表述不再并存；`48-01-PLAN.md` / `48-02-PLAN.md` / `48-VALIDATION.md` 的旧要求已按 G-48-3 修订并留删除痕迹；`AGENTS.md:267` 的测试清单已登记 `tests/test-ai-cancel-state.js`（`:272` 维护约定的执行）；四条测试命令全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer 缓存快照 → 调用是否发生 | 快照是**可陈旧的读侧缓存**，不构成授权依据；越界使用会让「磁盘上真实存在的技能」被本地判死 |
| renderer → main（`/skill:<name>`） | 判定与文案的唯一权威侧；渲染端只发语法文本与呈现结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06-01 | Denial of Service | `handleSendAIMessage` 的本地否决 | high | mitigate | 删除两段本地否决；存在性/启停一律由主进程当场读盘裁定。Task 1 的源码探针与两套测试断言钉死「发送路径零快照读取」 |
| T-48-06-02 | Tampering | 失败文案与判定入口 | medium | mitigate | 跨文件护栏：主进程 `skillErrorFromReason` 必须仍含两条文案，渲染端不得含 —— 任一侧被删都变红，防 UX 静默丢失 |
| T-48-06-03 | Spoofing | 移除渲染端预检后被绕过的可能性 | low | accept | 预检是**冗余**的（主进程文案逐字相同），不承担任何安全语义；真正的防冒名判定在 `readSkillForInvocation`（由 48-04 加固，见 T-48-04-01） |
| T-48-06-04 | Information Disclosure | `skills:changed` 无条件重拉快照 | low | accept | `realmAPI.ai.getSkills()` 只返回收窄投影（无正文 / 无 filePath / 无诊断，46 D-06 已锁定），零 IO 且不出主窗口；无新增暴露面 |
</threat_model>

<verification>
- `node --test tests/test-skill-picker-model.js`、`node tests/test-ai-skills.js`、`node --test tests/test-ai-cancel-state.js` 全绿（含改写后的断言）
- `node --check src/renderer.js` 通过；发送路径零 `state.aiSkills` 读取（内联探针）
- 文档：§10.3 / §10.7 / §10.4 三处旧表述已改写、不并存；计划文本三处修订带 `G-48-3` 标记；`AGENTS.md:267` 测试清单已登记 `tests/test-ai-cancel-state.js`（`AGENTS.md:272` 的维护约定同时要求产品文档章节与本节测试清单，两者都已同步）
- **诚实边界**：「运行期新增技能 + 未打开过面板 → 手打可调用」是**运行时**行为（主进程重扫 + 广播 + IPC 往返），node:test 只覆盖源码契约；最终证据是重跑 `/gsd-verify-work 48` 的自动驱动探针（在 `managed-skills/` 下新建目录后**不打开** `/` 面板，直接手打 `/skill:<新名>`），须在 SUMMARY 登记
- 指纹提示：本计划改 `src/renderer.js`、`tests/*`、`docs/product/ai-skills.md` 与三份 48 阶段计划/验证文本（均在本阶段内）→ 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）。另改 `AGENTS.md` —— 它**不在** `covered_files` 内，属项目指令文件、由 `AGENTS.md:272` 的维护约定要求同步，按惯例不纳入阶段指纹（SUMMARY 需记明这一条，避免收尾时被误判为越界改动）
</verification>

<success_criteria>
- 手打 `/skill:<name>`：渲染端零预检，主进程当场读盘；运行期新增技能无需先打开面板即可调用
- 不存在 / 已禁用：提示文案与残留气泡行为与修复前逐字一致（用户可见零变化）
- 渲染端零失败文案复制、零快照否决；`skills:changed` 无条件重拉且仍不自激
- 计划文本、验证矩阵与实现不再互相矛盾（三处修订均带 gap id 标记，且每处旧措辞都以「已被 G-48-3 取代」归档而非静默存活）
- 项目指令文件同步：`AGENTS.md:267` 测试清单登记 48-05 新建的 `tests/test-ai-cancel-state.js`（`AGENTS.md:272` 的维护约定要求「更新该章节**与本节测试清单**」，漏登记即只完成一半）
</success_criteria>

<output>
Create `.planning/phases/48-skill-name/48-06-SUMMARY.md` when done
</output>
