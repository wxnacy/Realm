---
phase: 48-skill-name
plan: 8
type: execute
wave: 7
depends_on: [48-07]
gap_closure: true
gap_ids: [G-48-18, G-48-19]
files_modified:
  - ai-manager.js
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/phases/48-skill-name/48-VALIDATION.md
autonomous: true
requirements: [DISC-02, DISC-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

key-decisions:
  - "**两条 gap 合成一个计划（48-08）**，不拆 48-08 / 48-09：两条的改动面**同一段代码**（`_resolveSkillInvocation` 的 JSDoc 与 miss 重试块 —— G-48-18 要收口的是它 JSDoc 第 4 条的「回写延后到哪里」，G-48-19 要收口的是同一 JSDoc 第 3 条的「失败语义承诺」）、**同一批文件**（`ai-manager.js` + `tests/test-ai-skills.js`）、**同一本测试账本**（`AGENTS.md` / `docs §七` 的例数只能写一次终值）。拆成两个计划只会得到「同波冲突 → 被迫串行 → 同一段 JSDoc 被改两遍 → 例数账本被改两遍」"
  - "**G-48-18 取路线 A（补 flush）**，用户 2026-09-12 裁决已载明理由：纯文本流是 `/skill:` 的**默认**通道，路线 B（只收紧措辞）等于把「最终一致」降级为「打开面板才一致」；Phase 49 的 `manage_skill` 会让 AI 自建技能常规化，依赖模型自己感知新技能"
  - "**唯一实现 `_flushDeferredSkillsPrompt()`，两处共用**（`prompt()` 成功出口 + `promptWithContext()` 成功出口）。这是约束 1（不得新增第二套实现）的落地形态：`_skillsPromptDirty` 的检脏 / 复位 / 同步调用 / 失败恢复四行**只允许存在于这一个方法体内**，两个调用点各只留一行 `await this._flushDeferredSkillsPrompt();`"
  - "**补刷首行必须是「检脏早退」**：`_skillsPromptDirty` 为假时立即返回、不做任何 IO。若漏掉这一步，**每一条普通消息**都会在成功出口多付一次全量重扫（两个技能目录 + 逐技能读 `SKILL.md`）—— 这是本计划引入的**最大性能回归面**，故它既是源码门禁（Gate B）也是行为用例（K 组「早退零成本」，断言 `rescanCalls === 0`）"
  - "**只在成功出口补刷**：`prompt()` 的错误出口（重试耗尽后的 `catch`）与 `promptWithContext()` 的 `catch` 一律不补，`_cleanupCurrentAgent()` 也不补 —— 这是 48-07 / `ai-manager.js:1323-1324` 注释的既有设计（避免同一次运行双刷），本计划**保留**并把它变成可失败的门（K 组「错误出口不补刷」+「清理不补刷」）"
  - "**G-48-19 的结构取「两个各自独立的 try」**：重扫在自己的 try 内；重试读盘在自己的 try 内。重扫抛错时**不执行**重试读盘（用一个局部标志在 try 成功后才允许重读）—— 因为重扫仅可能由 `refreshSkills` 抛错（调用点恒 `isProcessing = true`，`syncAgentSystemPrompt` 在忙分支之前只有这一处可抛），而 `refreshSkills` 整批失败会回滚 `skills` / `promptBlock` / `diagnostics` 三件套 → 缓存未变 → 重读必然得到与首次**同形**的判定。**取舍已注明**：若日后调用点改成非忙，重扫可能「缓存已更新但后续步骤抛错」，此时跳过重读会退回 `not_found` —— 代价是一次调用退化（下一次调用缓存已命中即恢复），有界且自愈，换取「保留原判定」在字面上成立"
  - "**`err` 取值一律用 `err && err.message ? err.message : String(err)`**（与 `ai-skills-manager.js:634` 同形）。`throw null` / `throw 'x'` 是合法的 JS 抛出形态，裸读 `err.message` 会在 catch 体内二次抛 `TypeError` —— 恰好把「绝不升级为异常」反转成「升级为异常」。本计划只在**新写/改写的这两处 catch** 上对齐该形态，不回头批量改仓内其它 `err.message` 读点（扩大面且与本 gap 无关）"
  - "**两条告警文案必须可判别**：一条含「重扫失败」、一条含「重试读盘失败」。这既是「不静默」的可见性要求，也是行为用例能把两条 catch 分开断言的前提（否则实现把两次失败并成一条也能过）"
  - "**不新增第三套失败文案**：`skillErrorFromReason` 仍是 `not_found | disabled` 两码与两条文案的**唯一**来源，`_resolveSkillInvocation` 只调它；`disabled` 不触发重试（缓存已知道它，重扫无意义）"
  - "**本次只把「新增的调用点」纳入 try，不顺手闭合 WR-02**：`ai-manager.js:1046` / `:1174` 两处裸调、以及 `_resolveSkillInvocation` 内**首次** `readSkillForInvocation` 与 `formatSkillInvocation` 动态 import 仍不在 try 内 —— 那是 WR-02 的登记范围，属于「上一轮仍开的 Warning」，用户本次**未**裁定。这一点必须写进 SUMMARY，禁止被读成「WR-02 已修」"
  - "**不改 renderer**：G-48-18 的全部改动面在主进程 `ai-manager.js`（+ 文档）。renderer 侧 `.refreshSkills(` 全文件计数仍恒为 1（P-48-06 自激回路护栏）—— 广播的**消费**侧 48-06 已改好且有无条件重拉护栏，本计划不触"
  - "**不动 `ai-skills-manager.js`、不动 `syncAgentSystemPrompt()` 的函数体**：缓存存在性门与三字段产出仍是唯一权威；本计划只改调用侧的补刷与失败兜底，外加它自己的 JSDoc 与注释"
  - "**测试账本单一入口**：`AGENTS.md:267` 的 `- 测试：` 行与 `docs/product/ai-skills.md:98`（§七）的例数都取 Task 1/2 的**实测输出**（不按推算填）；`node tests/test-builtin-skills-seeder.js:2200` 只机械校验 bash-policy 的计数，ai-skills 的计数此前**无机械护栏**，故本计划的 Gate H 自建一条交叉校验（实跑输出 vs 两处文本）"

must_haves:
  truths:
    - "G-48-18 靶心（纯文本流也最终落地）：`/skill:<运行期新增名>` **无 @ 引用与附件**（走 `ai.prompt`）成功后，延迟的 system prompt 回写与 `skills:changed` 广播**在本轮结束时执行一次** —— 断言 `_skillsPromptDirty` 归 `false`、`agent.state.systemPrompt === buildSystemPrompt()` 且含该新技能的 name/description、`skills:changed` 恰广播一次"
    - "G-48-18 的补齐面：`prompt()` 与 `promptWithContext()` 的成功出口**共用同一个补刷实现**（源码门禁：`_flushDeferredSkillsPrompt` 方法体内含检脏 / 复位 / `await this.syncAgentSystemPrompt()` / 失败恢复置脏四要素且顺序正确；全文件 `await this._flushDeferredSkillsPrompt()` **恰 2 处**；`promptWithContext` 内**不得**残留任何一处 `this._skillsPromptDirty` 读写 —— 否则就是第二份实现）"
    - "G-48-18 的零成本不变式：`_skillsPromptDirty` 为假时补刷**立即返回、零重扫**（行为断言 `rescanCalls === 0`）—— 保证「每条普通消息都多付一次全量重扫」不会发生；且补刷只在**成功**出口执行（`promptWithContext` 的 `catch` 与 `_cleanupCurrentAgent()` 均不补：脏标记原样保留、prompt 逐字未变、零广播）"
    - "G-48-19 靶心（重扫抛错保留原判定）：`syncAgentSystemPrompt()` 抛错时 `_resolveSkillInvocation` 仍返回 `{ skillError: { code: 'skill_not_found' } }` —— **不抛异常**（`await` 正常 resolve）、**不变成第三码**、`skill === null`、`skillBlock === ''`；且不因「重扫失败」再去重复一次必然同形的读盘（`readSkillForInvocation` 调用数 = 1）"
    - "G-48-19 的原始值形态：重扫抛 `null` / 抛原始字符串时，catch 体自身**不得二次抛错**（`err && err.message ? err.message : String(err)` 形态），调用仍然 resolve 且判定不变 —— 这条正是「绝不升级为异常」被反转的那个点"
    - "G-48-19 的可判别告警与重试读盘兜底：重扫成功但**重试读盘**抛错时，判定沿用原值、异常不逃逸，且告警文案与「重扫失败」那条**可判别**（行为用例分别断言两条告警文本）"
    - "不回归：失败码域仍只有 `not_found | disabled` 两个（`skillErrorFromReason` 唯一来源）；shadowed / disabled / tier 仍全部来自 `refreshSkills` 同一条管线（既有 J 组负例继续绿）；`ai-skills-manager.js` 零 diff；`syncAgentSystemPrompt()` 函数体逐字未改（46-04 五条断言继续绿）；renderer 侧 `.refreshSkills(` 计数仍恒 1"
    - "文档收口：`docs/product/ai-skills.md` §10.7 里「延迟回写与广播何时落地」的枚举与措辞改为与新实现一致（任一轮**成功出口**，含纯文本轮 / 打开面板 / Agent 创建或重建），并显式声明「补刷失败会恢复脏标记、在下一次成功出口重试」；`ai-manager.js` 文件内不再残留把纯文本轮排除在外的旧措辞；§七 与 `AGENTS.md:267` 的例数同步为实测值"
    - "诚实边界（不得被读成已端到端证成）：G-48-18 的可观察结果分两半 —— ①「本轮结束时补刷落地」由 K 组行为用例在**真实 `syncAgentSystemPrompt()`** 上钉死（不是 mock 判定），这是本计划的主证据；②「已打开的 `/` 面板随后可见 / 模型按 description 自动匹配可见」是**组合面**（广播 → renderer 无条件重拉快照（48-06 已落地并有护栏）→ 面板原地重渲染（48-02 已测）+ SDK 提示词模板给出 `read` 指令），其运行时终证可由下一轮 `/gsd-verify-work 48` 的自动驱动探针复核（形态：先打开 `/` 面板 → 在 `managed-skills/` 下运行期新建目录 → **不重开面板**直接手打 `/skill:<新名>` → 断言面板已出现该行且主进程有 `发送消息: /skill:<新名>` 日志）。同时：WR-06「已缓存技能在实时读盘路径仍绕过 64 KiB 字节闸」**保持开放**（随 Phase 49），WR-02 的两处裸调**仍开**（见 key-decisions），两者都不得在任何文档/摘要里被声称已修"
  artifacts:
    - "`ai-manager.js`：新增私有方法 `async _flushDeferredSkillsPrompt()`（唯一实现：检脏早退 → 复位 → `await this.syncAgentSystemPrompt()` → catch 内恢复脏标记 + 可读告警）；`promptWithContext()` 的旧内联补刷块（`:1323-1334`）整体替换为一次方法调用；`prompt()` 的成功出口（`this.isProcessing = false;` 之后、`return` 之前）插入一次方法调用；`_resolveSkillInvocation()` 的 miss 重试块改为「重扫与重试读盘各有独立 try」+ 可判别告警 + 安全 err 取值；四处注释/JSDoc 的落地时机措辞收口"
    - "`tests/test-ai-skills.js`：新增 `K 组 · G-48-18 延迟补刷在纯文本流上落地`（5 条：靶心行为 / 早退零成本 / 错误出口不补刷 / 清理不补刷 / 单源源码护栏），含夹具 `promptCtx(env, { dirty })`；既有 `J 组` 追加 3 条行为用例（重扫抛 Error 形态 / 重扫抛原始值形态 / 重试读盘抛原始值形态）；把原 `:1318-1327` 那条断言「`promptWithContext` 内含内联补刷块」的既有源码用例**改写**为断言单源实现（该断言的旧形态在新实现下必然红，是重构的配套改动而非放宽护栏）"
    - "`docs/product/ai-skills.md`：§10.7 的落地时机枚举与措辞收口（含补刷失败的恢复语义）；§七 的例数与覆盖面刷成实测值；§10.3 / §10.8 逐条核对后**如无失实即不改动**（不得为凑数而改）"
    - "`AGENTS.md`：`:267` 的 `- 测试：` 行里 `node tests/test-ai-skills.js` 条目的例数与覆盖面刷成实测值"
    - "`.planning/phases/48-skill-name/48-VALIDATION.md`：Per-Task Verification Map 追加 `48-08-T1` / `48-08-T2` / `48-08-T3` 三行（列数与表头一致，`Status` 一律 `⬜ pending`）"
  key_links:
    - "G-48-18 的落地链：`prompt()` 成功出口 → `await this._flushDeferredSkillsPrompt()` → 检脏 → `await this.syncAgentSystemPrompt()` → `refreshSkills(this.sandboxEnv, { disabled: configStore.get('settings.aiSkills.disabled', []), rootDirs: [getManagedSkillsDir(), getSkillsDir()] })` → 非忙（此刻 `isProcessing` 已置回 false）→ digest 与逐字符比对 → 若变则 `this.agent.state.systemPrompt = buildSystemPrompt()` + `windowManager.broadcast('skills:changed')`（全仓唯一广播点）。**这条链上没有第二份实现、没有第二套判定**"
    - "G-48-18 的「为何此前不落地」链：`_skillsPromptDirty` 全仓读写点原先只有 4 处（init / `promptWithContext` 的唯一消费点 / 该点内的失败恢复 / `syncAgentSystemPrompt()` 忙分支的唯一置脏点），而 renderer 只在「有 @ 引用或附件」时才走 `promptWithContext`，常规 `/skill:<name>` 与 `regenerateMessage` / 错误重试都走 `ai.prompt` → 纯文本流上脏标记无限期保持真。本计划补上 `prompt()` 一侧的落地口，并把「唯一实现」抽出来供两处共用"
    - "G-48-19 的失败链：miss → `try { await this.syncAgentSystemPrompt() } catch { 告警 + 沿用原判定（不执行重读）}` → `if (!result || result.ok !== true)` → `skillErrorFromReason(reason, parsed.name)` → `{ skillError }` → IPC 返回 → renderer 既有回滚 + system-note（48-06 的跨文件单源护栏不动）。**任何一环都不新增文案、不新增返回码**"
    - "回归护栏：`tests/test-ai-skills.js` 的 J 组源码护栏（`_resolveSkillInvocation` 内 `readSkillForInvocation(` 恰 2 次、无循环关键字、无管理器刷新入口调用、含 `syncAgentSystemPrompt(` / `console.warn` / `skillErrorFromReason(`）与 46-04 五条 `syncAgentSystemPrompt()` 方法体断言、`:1299` 早退→改写→广播形态断言、`:1366` 两处创建点调用文本一致断言**全部继续绿**"
  prohibitions:
    - statement: "不得写出第二份补刷实现（在 `prompt()` 或 `promptWithContext()` 里再内联一份检脏 / 复位 / 同步调用 / 失败恢复）"
      status: unresolved
      verification: judgment
      reason: "约束 1（单一权威来源）：两份实现必然漂移，且「失败恢复脏标记」这条最容易只写在其中一份。源码门禁断言全文件的方法调用恰 2 处、且 `promptWithContext` 内不残留任何一处脏标记读写；行为用例断言两处都真的落地"
    - statement: "不得把补刷放在错误出口、也不得放进 `_cleanupCurrentAgent()`"
      status: unresolved
      verification: judgment
      reason: "48-07 / `ai-manager.js:1323-1324` 注释明文的既有设计：错误路径与清理路径补刷会造成同一次运行双刷（且清理时往往连 Agent 都已置空，补刷只会静默失败后再置脏）。行为用例把这一点钉成门"
    - statement: "不得把补刷写成「无条件调用」（丢掉检脏早退），也不得在补刷内部新增第二条重扫触发源或绕过 `syncAgentSystemPrompt()`"
      status: unresolved
      verification: judgment
      reason: "无条件调用会让每条普通消息多付一次全量重扫（两个技能目录 + 逐技能读 `SKILL.md`）—— 远超本增量要解决的问题；绕过唯一权威入口会让 shadowed / disabled / tier 三字段出现第二套来源（违反约束 1）"
    - statement: "不得把 miss 重试改写成循环 / 递归 / 反复重试，也不得新增第三个失败码或第三条失败文案"
      status: unresolved
      verification: judgment
      reason: "48-07 已把「至多一次」钉进源码门禁与行为断言（`rescanCalls === 1`），本计划不得松动；`skillErrorFromReason` 是两码两文案的唯一来源（DISC-06），`resolved.skillError` 的返回形状与 renderer 回滚链路均依赖它"
    - statement: "不得顺手闭合 WR-02（不给 `ai-manager.js:1046` / `:1174` 两处裸调、也不给 `_resolveSkillInvocation` 内首次读盘与动态 import 包 try/catch），也不得修 WR-01 / WR-04 / WR-05 / WR-06 / TD-48-01 / TD-48-02"
      status: unresolved
      verification: judgment
      reason: "WR-02 与两条 TD 是用户明确裁定的「仍开 / 延后（Phase 49 开工前第一条）」；WR-06 用户裁决保持开放。本计划只把**本次新增的那个调用点**纳入 try —— 这是 G-48-19 的题中之义；多包一处的代价是判据面失控，且会让「WR-02 仍开」这条登记与实现脱节"
    - statement: "不得改 `ai-skills-manager.js`（含为便于测试而扩大导出）、不得改 `syncAgentSystemPrompt()` 的函数体、不得改 `src/renderer.js` 或任何 renderer 测试"
      status: unresolved
      verification: judgment
      reason: "缓存存在性门与三字段产出是唯一权威、46-04 五条方法体断言是硬约束、renderer 的 `.refreshSkills(` 计数恒 1 是 P-48-06 自激回路护栏。本 gap 的失效点在主进程补刷口，不在这三处"
---

<objective>
闭合 UAT round 3 test 18 裁决出的两条 gap —— 它们都是上个 gap 计划 **48-07** 的增量放大 / 暴露，核验报告（`48-REVIEW.md`）判为 Warning 级、不阻断 UAT，用户 2026-09-12 明确裁定**本阶段闭合、不延后到 Phase 49**。

**G-48-18（WR-07）**：`_skillsPromptDirty` 的读写点全仓只有 4 处（init / 唯一消费点 `promptWithContext()` 的成功路径 / 该点内的失败恢复 / `syncAgentSystemPrompt()` 忙分支的唯一置脏点）。`prompt()` 的成功出口没有对应补刷，而 renderer 只在「有 @ 引用或附件」时才走 `promptWithContext`，常规 `/skill:<name>`、重新生成、错误重试都走 `ai.prompt`。48-07 把「置脏」从罕见情形（流式中打开面板）变成**常规**情形（任一次 miss），于是「缓存已含新技能、system prompt 不含」的持续不一致成了默认态：`_skillsPromptDirty` 无限期保持真、模型自动匹配（D-18 的 `read` + description 路径）对运行期新增技能不可见、`skills:changed` 永不广播（其他窗口 / 已打开的 `/` 面板投影陈旧）。用户裁定**路线 A（补 flush）**。

**G-48-19（WR-08）**：48-07 自述的第三条不变式（「重扫抛错被就地 catch + 告警后**保留原判定**，不把「未找到」升级成异常」）**只被断言、未被强制** —— 重试读盘在 `try` 之外（新增的调用点同样落在 try 外，与刚写下的不变式不符）；catch 之后仍赋值 → 是「替换」而非「保留」；catch 体内裸读 `err.message`，`throw null` / 抛原始值会二次抛 `TypeError`（恰好把「绝不升级为异常」反转）；且该分支**零行为用例**，唯一护栏是 `body.includes('console.warn')` 这种**字符串存在性**断言。用户裁定**修复 + 补行为用例**。

Purpose: 把「延迟回写与广播」从「只有带引用/附件的那一轮、或打开面板才落地」变成**每一轮成功出口都落地**（抽唯一实现、两处共用、检脏早退、只在成功出口补）；把「重扫/重试抛错」从**注释里的承诺**变成**结构上成立且被行为用例钉死的性质**（两条可判别告警 + 安全 err 取值 + 不重复读盘 + 判定不变）。两条都不扩大交付面：不改 `ai-skills-manager.js`、不改 `syncAgentSystemPrompt()` 函数体、不改 renderer、不闭合 WR-02、不碰沙箱与 Bash 权限面。

Output: `ai-manager.js` 的唯一补刷实现 + 两处共用 + 收紧后的 miss 重试块；`tests/test-ai-skills.js` 的 K 组 5 条 + J 组 3 条新用例与 1 条旧护栏改写；`docs/product/ai-skills.md` §10.7 落地时机收口 + §七 例数；`AGENTS.md` 测试清单例数；`48-VALIDATION.md` 三行矩阵。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-UAT.md
@.planning/phases/48-skill-name/48-REVIEW.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-07-PLAN.md
@.planning/phases/48-skill-name/48-07-SUMMARY.md
@.planning/phases/48-skill-name/48-VALIDATION.md
@docs/product/ai-skills.md
@AGENTS.md
@ai-manager.js
@tests/test-ai-skills.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 延迟补刷抽成唯一实现 + 纯文本流成功出口落地（G-48-18 纵切，含 K 组行为用例）</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js:1002-1110`（`prompt()` 全段：`isInitialized` / `agent` 保证 → `isProcessing` 并发闸（:1031-1039）→ 置位（:1041）→ `_resolveSkillInvocation`（:1046）→ 技能失败早退（:1047-1055）→ `_ensureConversation`（:1059-1070）→ 重试循环（:1077-1109）：成功出口 `:1084-1088`（`this.isProcessing = false;` → `return {...}`）、末次失败出口 `:1090-1104`）
    - `ai-manager.js:1135-1352`（`promptWithContext()` 全段：与 `prompt()` 同形的头部 → `:1185-1193` 附件解析 → `:1199-1210` `_ensureConversation` → `:1214-1339` 主 try（`:1321` `this.isProcessing = false;` → `:1323-1334` 现有内联补刷块 → `:1336-1339` `return`）→ `:1340-1351` catch）
    - `ai-manager.js:705-715`（`_skillsPromptDirty` 的 init 与它上方那段 JSDoc —— 该 JSDoc 描述「只置脏、在 promptWithContext() 的某处补刷一次」，是本次要收口的措辞之一）
    - `ai-manager.js:2768-2780`（`_cleanupCurrentAgent()`：只做 abort + 置空 + `isProcessing = false`，**不碰** `_skillsPromptDirty` —— 本任务不改它，只加护栏）
    - `ai-manager.js:2783-2851`（`syncAgentSystemPrompt()` 的 JSDoc（`:2793-2800` 含另一处要收口的措辞，它在方法体**之外**，改动不违反 46-04 断言）+ 方法体 `:2802-2830`（`refreshSkills` → 忙分支置脏并 return → digest/逐字符早退 → 改写 `agent.state.systemPrompt` → `windowManager.broadcast('skills:changed')`）；`:2848-2851` `refreshSkillsForPanel`）——**方法体一字不改**
    - `ai-manager.js:1407-1492`（`_resolveSkillInvocation` 全段：JSDoc 四条口径（第 3 条在 `:1427-1429`、第 4 条在 `:1430-1433`，两处措辞都在本计划要收口的范围内）+ 方法体 `:1440-1492`）
    - `tests/test-ai-skills.js:1236-1338`（`回写` 那一组的既有用例：`:1236-1297` 行为用例（含 `wm.broadcast` 打桩手法）、`:1299-1316` `syncAgentSystemPrompt` 源码形态、**`:1318-1327` 那条断言 `promptWithContext` 内含内联补刷块的源码用例**（本任务必须改写它）、`:1329-1338` digest 初始化）
    - `tests/test-ai-skills.js:68-94`（`readSource` / `functionBody` / `methodBody` 三个 helper 的提取口径：`methodBody` 从 `async <name>(` 到下一个行首两空格 `}`）、`:34-66`（`withTempRoot` / `writeSkill` 的签名与默认值）、`:1451-1456`（`setupSkillsEnv`）、`:2758-2781`（`skillResolveCtx` / `invokeSkill`：本任务的 `promptCtx` 要与它们**同款纪律**——`Object.create(aiManager.prototype)` + 只补必需字段 + own-property 包装计数 + 不 `new AIManager()`）
    - `tests/test-ai-skills.js:1776-1795`（`skillErrorFromReason` 打表：两个码 + 两条文案 + 值域无第三码）、`:2173-2180`（46-04 五条 `syncAgentSystemPrompt()` 方法体断言）、`:2832-2852`（J 组「快路径零重扫」用例的基线计数写法）
  </read_first>
  <action>
    **A. `ai-manager.js` 抽出唯一补刷实现**

    1. 在 `syncAgentSystemPrompt()` **之后**、`refreshSkillsForPanel()` 之前（或紧随 `refreshSkillsForPanel()` 之后，两者皆可）新增私有方法 `async _flushDeferredSkillsPrompt()`，方法体**恰为**下列四件事（顺序不可调换）：
       - 第一行检脏早退：`if (!this._skillsPromptDirty) return;`
       - 复位：`this._skillsPromptDirty = false;`
       - 同步：`await this.syncAgentSystemPrompt();`（在 `try` 内）
       - 失败恢复：`catch (err) { this._skillsPromptDirty = true; console.warn('[Realm AI] 延迟刷新技能 prompt 失败（将于下次成功出口重试）:', err && err.message ? err.message : String(err)); }`
       JSDoc 必须写明四件事：① 这是延迟补刷的**唯一实现**（`prompt()` 与 `promptWithContext()` 的成功出口共用，禁止第二份）；② 首行检脏早退是**零成本**保证 —— 无待补刷时不做任何 IO（否则每条消息都会多付一次全量重扫）；③ 只由**成功**出口调用（错误出口与 `_cleanupCurrentAgent()` 不调，避免同一次运行双刷）；④ 失败恢复脏标记的语义 = 下一次成功出口重试，绝不静默丢弃。

    **B. 两个成功出口改用共用实现**

    2. `promptWithContext()`：把 `:1325-1334` 那段内联块（检脏判断 / 复位 / `try { await this.syncAgentSystemPrompt(); }` / `catch` 内恢复置脏 + 告警）整体**替换为一行** `await this._flushDeferredSkillsPrompt();`，并把 `:1323-1324` 的注释改写成「唯一补刷实现见 `_flushDeferredSkillsPrompt()`；成功出口是唯一的补刷点（错误出口与 `_cleanupCurrentAgent()` 不补）」。**该处的脏标记读写语句与同步调用语句不得保留任何一处**（保留即为第二份实现 —— 源码门禁会按方法体区域断言）。
    3. `prompt()`：在成功出口的 `this.isProcessing = false;`（:1084）**之后**、`return {...}`（:1085）**之前**插入 `await this._flushDeferredSkillsPrompt();`，上方加两行注释：本行是延迟补刷在纯文本通道上的落地点（G-48-18：renderer 只在有 @ 引用或附件时才走 `promptWithContext`，常规 `/skill:<name>` / 重新生成 / 错误重试都走本方法）；补刷自身检脏早退，故普通消息零成本。**末次失败的 catch（:1090-1104）内不得插入任何补刷**。

    **C. 落地时机措辞收口（四处）**

    4. 把这四处对「延迟补刷何时落地」的描述改写成与新实现一致 —— 落地时机 = **任一轮成功出口**（`prompt()` 与 `promptWithContext()` 各一处，**含纯文本轮**）/ 打开面板（`refreshSkillsForPanel`）/ Agent 创建或重建：
       - `_skillsPromptDirty` 的 init JSDoc（`:707-712`）：原描述把补刷点限定在 `promptWithContext()` 内，改为「两个成功出口共用同一个补刷实现」。
       - `promptWithContext()` 的补刷注释（`:1323-1330`）：随 B 步一并改写（含原先写在 catch 里的「下一次某某时机重试」表述）。
       - `_resolveSkillInvocation()` 的 JSDoc 第 4 条（`:1430-1433`）：把落地时机枚举改为与上面一致的四项清单。
       - `syncAgentSystemPrompt()` 的 JSDoc 第三处早退说明（`:2795-2796`）：同样改为「由两个成功出口的共用补刷实现消费」。
       **收口要求**：`ai-manager.js` 内**不再残留**把纯文本轮排除在外的旧措辞（核心是原先那四处「只由 `promptWithContext()` 的某个时机补刷」的说法）。**不要**改动 `syncAgentSystemPrompt()` 的方法体（46-04 五条断言是硬约束），也不要改 `_resolveSkillInvocation()` 方法体内除注释外的任何一行。

    **D. 改写那条已经过时的既有护栏（必须做，不是放宽）**

    5. `tests/test-ai-skills.js:1318-1327` 那条源码用例断言「`promptWithContext` 内含 `this._skillsPromptDirty` 读写 + `await this.syncAgentSystemPrompt()` 且顺序正确」。B 步之后该形态**必然不存在** → 该用例会红。把它**改写**为「延迟补刷单源」护栏，语义等价且更强：取 `methodBody(src, '_flushDeferredSkillsPrompt')` 断言四要素与顺序（检脏在复位之前、失败恢复置脏在同步调用之后）+ 断言 `prompt()` 方法体与 `promptWithContext()` 方法体**各含一次** `await this._flushDeferredSkillsPrompt()` + 断言 `promptWithContext()` 方法体**不含**脏标记读写（`this._skillsPromptDirty`）→ 全文件 `await this._flushDeferredSkillsPrompt()` 恰 2 处。用例标题与注释里写明「这是 G-48-18 的配套改写：旧形态（内联块）已被单源实现取代，不是放宽护栏」。

    **E. K 组：G-48-18 的行为用例（新增 5 条）**

    6. 在文件末尾（既有 J 组 describe 之后）新增 `describe('K 组 · G-48-18 延迟补刷在纯文本流上落地', () => { ... })`，先写夹具再写 5 条 `test()`：

       **夹具 `promptCtx(env, { dirty = false } = {})`**（与 `skillResolveCtx` 同款纪律：不 `new AIManager()`，不触碰真实 userData / 对话库）：
       - `Object.create(aiManager.prototype)`，只补这些 own property：
         - `isInitialized = true`；`sandboxEnv = env`；`isProcessing = false`；`_skillsPromptDirty = dirty`；`_skillsPromptDigest = ''`；`currentConversationId = 'conv-flush'`
         - `agent = { state: { systemPrompt: 'OLD', isStreaming: false }, prompt: async () => {}, waitForIdle: async () => {}, abort: () => {} }`（`prompt` 打桩成成功返回 —— 本组验的是**出口**行为，不是 SDK；`abort` 供 `_cleanupCurrentAgent` 用）
         - `configStore = { get: (k, d) => d }`；`getSeededSkillNamesSafe = () => []`
         - `_ensureConversation = () => 'conv-flush'`（**own property 覆写**真实实现，隔离对话数据库；注释写明这不是为测试而设的假状态，而是隔离真实副作用）
         - `_sendEventsBatch = () => {}`（屏蔽错误广播噪声）
         - `rescanCalls = 0`，并用 own-property 包装计数：`const real = aiManager.prototype.syncAgentSystemPrompt; ctx.syncAgentSystemPrompt = function () { this.rescanCalls += 1; return real.call(this); };`（**包装而非重写** —— `syncAgentSystemPrompt()` 的方法体逐字未改这条硬约束因此不被测试绕过，与 J 组同款）

       **K1（靶心 · 行为）**「纯文本 `/skill:` 成功后延迟补刷落地」：`withTempRoot(t)` → `workspace.ensureWorkspaceDir()` → `const env = await setupSkillsEnv(root)`（此刻技能集为空）→ `writeSkill(workspace.getSkillsDir(), 'flush-probe', { body: '# flush-probe\n\n落地用正文\n' })`（**不重扫**）→ 用 `require('../window-manager')` 的 `broadcast` 打桩收集频道名（照 `:1249-1254` 的写法，`t.after` 复原）→ `const res = await aiManager.prototype.prompt.call(ctx, '/skill:flush-probe 你好')`。断言：`res.skillError === undefined`；`res.skillInvocation.name === 'flush-probe'`；`ctx._skillsPromptDirty === false`（**纯文本流也落地**）；`ctx.agent.state.systemPrompt.includes('flush-probe')`；`ctx.agent.state.systemPrompt === aiManager.buildSystemPrompt()`；广播频道数组 `deepStrictEqual(['skills:changed'])`（恰一次）；`ctx.rescanCalls === 2`（忙时重扫一次 + 出口补刷一次；**不是 3，证明没有双刷**）。

       **K2（早退零成本 · 行为）**「脏标记为假时普通消息不多付重扫」：同前建 env，**不新增技能**；`const ctx = promptCtx(env)`（`dirty` 默认 false）；`const res = await aiManager.prototype.prompt.call(ctx, '你好')`。断言：`res.skillError === undefined`；`res.skillInvocation === null`；`ctx.rescanCalls === 0`（补刷检脏早退，零重扫）；`ctx.agent.state.systemPrompt === 'OLD'`。

       **K3（错误出口不补刷 · 行为）**：同前建 env；`const ctx = promptCtx(env, { dirty: true })`；`ctx.agent.prompt = async () => { throw new Error('boom'); };`；同样打桩 `broadcast`；`const res = await aiManager.prototype.promptWithContext.call(ctx, '你好', [], [])`（空引用 + 空附件走的就是普通分支，且 `promptWithContext` 的 catch **没有**重试循环，所以这条用例是毫秒级的）。断言：`res.skillInvocation === null`；`ctx._skillsPromptDirty === true`（**错误出口不补刷**，避免同一次运行双刷）；`ctx.agent.state.systemPrompt === 'OLD'`；广播数组 `deepStrictEqual([])`；`ctx.rescanCalls === 0`。

       **K4（清理不补刷 · 行为）**：`const ctx = promptCtx({}, { dirty: true })`；`aiManager.prototype._cleanupCurrentAgent.call(ctx)`。断言：`ctx._skillsPromptDirty === true`（`_cleanupCurrentAgent()` 不碰该标记 —— 既有设计）；`ctx.rescanCalls === 0`；`ctx.agent === null`（清理语义未变）。

       **K5（源码护栏 · 单源）**：用 `readSource('ai-manager.js')` + `methodBody` 断言：`_flushDeferredSkillsPrompt` 方法体长度 `>= 150`（防提取口径失效假绿）；含 `if (!this._skillsPromptDirty)` 且其下标**小于** `this._skillsPromptDirty = false` 的下标（检脏早于复位）；含 `await this.syncAgentSystemPrompt()`；含 `this._skillsPromptDirty = true` 且其下标**大于**同步调用的下标（失败恢复在同步之后）；`methodBody(src,'prompt')` 含 `await this._flushDeferredSkillsPrompt()`；`methodBody(src,'promptWithContext')` 含该调用且**不含** `this._skillsPromptDirty`；全文件 `await this._flushDeferredSkillsPrompt()` 匹配数**恰 2**。

    7. 跑 `node tests/test-ai-skills.js`，记录**实测总例数**（Task 3 的两处账本要写它）；若与估算不符，以实测为准并在 SUMMARY 记明「门槛是地板，实测高于地板属正常」。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --check ai-manager.js &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（`node --check` 语法错或测试断言失败），或输出中 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 144（修复前实测 139 − 1 条改写 + 5 条新增），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const src=fs.readFileSync('ai-manager.js','utf8');const mb=(n)=&gt;{let i=src.indexOf('async '+n+'(');if(i&lt;0)i=src.indexOf('function '+n+'(');if(i&lt;0)i=src.indexOf('\n  '+n+'(');if(i&lt;0)return '';return src.slice(i,src.indexOf('\n  }',i))};const bad=[];const f=mb('_flushDeferredSkillsPrompt');if(f.length&lt;150)bad.push('补刷方法体提取口径失效（长度 '+f.length+'）');const g=f.indexOf('if (!this._skillsPromptDirty)');const r=f.indexOf('this._skillsPromptDirty = false');const s=f.indexOf('await this.syncAgentSystemPrompt()');const t=f.indexOf('this._skillsPromptDirty = true');if(g&lt;0)bad.push('补刷缺少检脏判断（会变成每条消息都重扫）');if(r&lt;0)bad.push('补刷未先复位脏标记');if(g&gt;=0&amp;&amp;r&gt;=0&amp;&amp;g&gt;r)bad.push('检脏判断必须位于复位之前（早退零成本）');if(s&lt;0)bad.push('补刷未复用 syncAgentSystemPrompt()');if(t&lt;0)bad.push('补刷失败未恢复脏标记');if(t&gt;=0&amp;&amp;s&gt;=0&amp;&amp;t&lt;s)bad.push('脏标记恢复必须位于同步调用之后');const plain=mb('prompt');const wctx=mb('promptWithContext');if(!plain.includes('await this._flushDeferredSkillsPrompt()'))bad.push('prompt() 成功出口未共用补刷');if(!wctx.includes('await this._flushDeferredSkillsPrompt()'))bad.push('promptWithContext() 未改用共用实现');if(wctx.includes('this._skillsPromptDirty'))bad.push('promptWithContext 仍残留内联补刷块（第二份实现）');const n=(src.match(/await this\._flushDeferredSkillsPrompt\(\)/g)||[]).length;if(n!==2)bad.push('补刷调用点 '+n+' 处（须恰 2 处）');if(src.includes('idle 边界'))bad.push('仍残留把纯文本轮排除在外的旧落地时机措辞');if(bad.length){console.error('补刷单源不合格:\n'+bad.join('\n'));process.exit(1)}console.log('deferred flush single-source ok (2 call sites, dirty-gated)')"</automated>
    <fails_when>非零退出，或出现 '补刷单源不合格' 或任一条 `补刷缺少` / `未先复位` / `必须位于` / `未复用` / `未恢复` / `未共用` / `未改用` / `仍残留` / `调用点` 行，或标准输出不含 "deferred flush single-source ok (2 call sites, dirty-gated)"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const s=require('fs').readFileSync('tests/test-ai-skills.js','utf8');const i=s.indexOf('K 组');const bad=[];if(i&lt;0)bad.push('缺 K 组用例组');else{const seg=s.slice(i);for(const t of ['promptCtx','_flushDeferredSkillsPrompt','skills:changed','rescanCalls','_cleanupCurrentAgent','纯文本','早退','错误出口','单源']){if(!seg.includes(t))bad.push('K 组缺断言素材：'+t)}}if(bad.length){console.error('K 组不合格:\n'+bad.join('\n'));process.exit(1)}console.log('K 组用例素材齐备')"</automated>
    <fails_when>非零退出，或出现 'K 组不合格' / '缺 K 组用例组' / 'K 组缺断言素材' 任一行，或标准输出不含 "K 组用例素材齐备"</fails_when>
  </verify>
  <done>
    延迟补刷有唯一实现 `_flushDeferredSkillsPrompt()`（检脏早退 → 复位 → `await this.syncAgentSystemPrompt()` → 失败恢复置脏 + 安全 err 取值），`prompt()` 与 `promptWithContext()` 的**成功**出口各共用一次（全文件恰 2 处调用，`promptWithContext` 内无残留内联块）；纯文本 `/skill:<运行期新增名>` 成功一轮结束后 `_skillsPromptDirty` 归 `false`、`agent.state.systemPrompt` 已含该技能、`skills:changed` 恰广播一次；脏标记为假时零重扫（早退零成本）；错误出口与 `_cleanupCurrentAgent()` 仍不补刷；四处落地时机措辞收口且文件内不再残留旧措辞；`tests/test-ai-skills.js` 的 K 组 5 条用例全绿、原 `:1318-1327` 的旧形态护栏已改写为单源护栏，`# fail` 为 0。
  </done>
</task>

<task type="auto">
  <name>Task 2: 收紧重扫/重试的失败分支 —— 保留原判定、不逃逸、告警可判别（G-48-19，含 J 组行为用例）</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js:1440-1492`（`_resolveSkillInvocation()` 当前形态：`:1445` 首次 `readSkillForInvocation`（`let result`）→ `:1460-1468` miss 重试块（当前是「一个 try 包重扫，重试读盘落在 try **之外**」；catch 内裸读 `err.message`）→ `:1470-1473` 失败出口（`skillErrorFromReason`）→ `:1476-1481` 动态 import + `buildSkillInvocationBlock` → `:1488-1491` 三键返回）
    - `ai-manager.js:1416-1434`（同一方法的 JSDoc 四条口径 —— 第 3 条 `:1427-1429` 就是那条「重扫抛错被就地 catch + 告警后**保留原判定**，不把「未找到」升级成异常」的**承诺**，本任务必须让它与实现字面一致）
    - `ai-manager.js:1046` / `:1174`（两处**裸调** `_resolveSkillInvocation` —— 仍在开的 **WR-02**，本任务**不**改它们，只把本次新增的那个调用点纳入 try）
    - `ai-manager.js:2802-2830`（`syncAgentSystemPrompt()` 方法体：调用点恒 `isProcessing = true` ⇒ 只有 `refreshSkills(...)` 会抛；而 `refreshSkills` 整批失败会回滚 `skills` / `promptBlock` / `diagnostics` 三件套 —— 「重扫失败 ⇒ 缓存未变 ⇒ 重读必然同形」的依据在此）
    - `ai-skills-manager.js:625-640`（`refreshSkills` 的失败回滚段与 `:634` 的 `err && err.message ? err.message : String(err)` —— 本任务要与之同形）
    - `ai-skills-manager.js:790-835`（`readSkillForInvocation`：缓存存在性门、`disabled` 早判、目录路径全等的同一性判据、动态 import 的抛出点（**在它自己的 try 之外**）—— 这是「重试读盘也可能抛」的依据）
    - `tests/test-ai-skills.js:2758-2781`（J 组夹具 `skillResolveCtx` / `invokeSkill`，本任务直接复用，不新造）与 `:2804-2830`（正例 1/2 的写法与断言口径，新用例要与之并列）、`:2894-2925`（负例 6 与源码护栏 —— 护栏里 `readSkillForInvocation(` 恰 2 次的断言**必须继续成立**）
    - `tests/test-ai-skills.js:1776-1795`（两个码 + 两条文案的打表：新用例只断言这两个码，不得出现第三码）
  </read_first>
  <action>
    **A. `ai-manager.js`：把 miss 重试块改成「两个各自独立的 try」**

    1. 保持触发条件不变（`result && result.ok !== true && result.reason === 'not_found'`），把块内结构改为：
       - **第一段 try**：`await this.syncAgentSystemPrompt();` —— 成功后把一个局部标志置真（例如 `let rescanned = false;` 在 try 前声明、`rescanned = true;` 作为 try 的最后一句）。`catch (err)` 内：告警文案含**「重扫失败」**且说明「沿用原判定」，取值形态为 `err && err.message ? err.message : String(err)`。**catch 之后不执行重试读盘**（缓存未刷新，重读必然得到与首次同形的判定；这一取舍与本任务的「保留原判定」字面要求同源）。
       - **第二段**：仅在上一段成功（标志为真）时进入，内层再包一层 `try`：`result = await skillsManager.readSkillForInvocation(this.sandboxEnv, parsed.name);`；`catch (err)` 内：告警文案含**「重试读盘失败」**（与第一条**可判别**）、同样说明「沿用原判定」、取值形态同上一段，**不赋 `result`、不逃逸**。
       - 两条告警都用 `console.warn`（禁止静默）。
    2. 块上方注释逐条写明：① 本次为什么要在两处各自兜底（重试读盘是 48-07 新增的调用点，它原先落在 try 外，与刚写下的不变式不符）；② 重扫抛错为何可以跳过重读（`refreshSkills` 整批失败会回滚缓存三件套 ⇒ 重读必然同形）；③ **至多一次、不得写成循环**（写成任何循环关键字都不行）；④ 异常的后果链：逃逸会让 `isProcessing` 永不复位，从而让后续消息被「AI 正在处理上一条消息」拒绝直到重建 Agent。
    3. 把 JSDoc 第 3 条（`:1427-1429`）的承诺改成与实现**逐条对应**的写法：重试至多一次 / 真不存在仍是 `not_found`（返回值域仍只有两个码）/ **重扫抛错或重试读盘抛错都就地 catch + 告警后沿用原判定**（异常不逃逸、不升级成第三码）。**不得**在该 JSDoc 里出现任何实现做不到的表述（例如声称「绝不重复读盘」却仍然重读）。
    4. **不要**改本方法体内其余任何一行：`parseSkillInvocationText` 早退、首次读盘、`if (!result || result.ok !== true)` 失败出口、`skillErrorFromReason` 调用、`formatSkillInvocation` 动态 import、`buildSkillInvocationBlock`、`sourceTierOf`、三键返回形状。**不要**给 `:1046` / `:1174` 两处裸调或首次读盘/动态 import 加 try（WR-02 不在本次裁定范围）。

    **B. `tests/test-ai-skills.js`：J 组追加 3 条行为用例（复用 `skillResolveCtx` / `invokeSkill`，不新造夹具）**

    5. **J8「负例 · 重扫抛错（Error 形态）：就地 catch、沿用原判定、不升级为异常、不重复读盘」**：
       - 建 env（`withTempRoot` + `workspace.ensureWorkspaceDir()` + `await setupSkillsEnv(root)`）→ `const ctx = skillResolveCtx(env)`；
       - 覆写 `ctx.syncAgentSystemPrompt = function () { this.rescanCalls += 1; throw new Error('boom'); };`（**必须同时自增 `rescanCalls`** —— `skillResolveCtx` 的计数包装被整体覆写后否则会失真）；
       - 用 `t.after` 复原的方式给 `aiSkills.readSkillForInvocation` 包一层计数器（模块对象即 `getAiSkillsManagerLazy()` 返回的对象，包装后 `_resolveSkillInvocation` 取到的就是它）；
       - 用 `t.after` 复原的方式捕获 `console.warn`（收集文本）；
       - `const res = await invokeSkill(ctx, '/skill:no-such-skill-xyz')`。断言：**调用正常 resolve**（本用例不抛即通过）；`res.skillError.code === 'skill_not_found'`（原判定，不是异常、不是第三码）；`res.skill === null`；`res.skillBlock === ''`；`ctx.rescanCalls === 1`（重扫尝试恰一次）；读盘计数 `=== 1`（**重扫失败后不重复读盘**）；捕获到的告警里**至少一条含「重扫失败」**。
    6. **J9「负例 · 重扫抛错（原始值形态）：告警取值不得二次抛错」**：同一个 ctx 上分别以 `throw null` 与 `throw 'x'` 覆写 `ctx.syncAgentSystemPrompt`（各自自增计数），各调一次 `invokeSkill(ctx, '/skill:no-such-skill-xyz')`。断言：两次都正常 resolve 且 `res.skillError.code === 'skill_not_found'`；**不得抛 `TypeError`**（若 catch 内裸读 `err.message`，本用例必红 —— 这正是「绝不升级为异常」被反转的那个点）。用例里以注释写明：`throw null` / 抛原始值是合法 JS 形态，取值形态必须与 `ai-skills-manager.js:634` 同形。
    7. **J10「负例 · 重试读盘抛错：沿用原判定、不逃逸，且告警可与「重扫失败」判别」**：
       - `const ctx = skillResolveCtx(env)`（`syncAgentSystemPrompt` 走**真实**实现 → 重扫成功；忙分支会把脏标记置真，不影响本用例）；
       - 包装 `aiSkills.readSkillForInvocation`：**第 2 次调用抛 `null`**（第 1 次走真实实现）—— 一次同时覆盖「重试读盘失败兜底」与「非对象抛出值不二次抛错」两个形态；`t.after` 复原；捕获 `console.warn`；
       - `const res = await invokeSkill(ctx, '/skill:no-such-skill-xyz')`。断言：正常 resolve 且 `res.skillError.code === 'skill_not_found'`；`ctx.rescanCalls === 1`；读盘计数 `=== 2`（首次 + 至多一次重试）；捕获到的告警里**至少一条含「重试读盘失败」**（与 J8 的「重扫失败」**可判别** —— 两条 catch 没被合并成一条）。
    8. **不要**改动 J 组既有的 7 条用例与源码护栏的断言语义（尤其 `_resolveSkillInvocation` 内 `readSkillForInvocation(` **恰 2 次**、无循环关键字、无管理器刷新入口调用、含 `syncAgentSystemPrompt(` / `console.warn` / `skillErrorFromReason(` —— 本次重构后它们必须继续成立）。
    9. 跑 `node tests/test-ai-skills.js` 并记录实测总例数。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --check ai-manager.js &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（语法错或断言失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 146（Task 1 后地板 144 + J 组新增 ≥2 条），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const src=fs.readFileSync('ai-manager.js','utf8');const i=src.indexOf('  async _resolveSkillInvocation(');const b=src.slice(i,src.indexOf('\n  }',i));const bad=[];if(b.length&lt;400)bad.push('方法体提取口径失效（长度 '+b.length+'）');const tries=(b.match(/\btry\s*\{/g)||[]).length;if(tries&lt;2)bad.push('重扫与重试读盘未各自处于独立 try（实际 '+tries+' 个）');if(!b.includes('重扫失败'))bad.push('缺「重扫失败」告警文案');if(!b.includes('重试读盘失败'))bad.push('缺「重试读盘失败」告警文案（两条告警须可判别）');const warns=b.match(/console\.warn\([\s\S]*?\)\s*;/g)||[];if(warns.length&lt;2)bad.push('告警调用少于 2 处（实际 '+warns.length+'）');for(const w of warns){if(!w.includes('String(err)'))bad.push('告警未用 String(err) 兜底：'+w.slice(0,60))}if(!b.includes('syncAgentSystemPrompt('))bad.push('缺权威重扫调用');const rd=(b.match(/readSkillForInvocation\(/g)||[]).length;if(rd!==2)bad.push('读盘口出现 '+rd+' 次（须恰 2 次：首次 + 至多一次重试）');if(!b.includes('skillErrorFromReason('))bad.push('失败出口缺失');if(/\bwhile\b|\bfor\s*\(/.test(b))bad.push('出现循环关键字（重试必须至多一次）');if(b.includes('refreshSkills('))bad.push('直接调用管理器刷新入口（必须经 syncAgentSystemPrompt）');if(!b.includes('rescanned'))bad.push('缺少「重扫是否成功」的局部标志（重扫失败后不得再读盘）');if(bad.length){console.error('重扫失败分支不合格:\n'+bad.join('\n'));process.exit(1)}console.log('retry-block shape ok (two guarded tries, distinguishable warns)')"</automated>
    <fails_when>非零退出，或出现 '重扫失败分支不合格' 或任一条 `方法体提取` / `未各自处于独立 try` / `缺「` / `告警` / `读盘口出现` / `失败出口缺失` / `循环关键字` / `刷新入口` / `局部标志` 行，或标准输出不含 "retry-block shape ok (two guarded tries, distinguishable warns)"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const s=require('fs').readFileSync('tests/test-ai-skills.js','utf8');const i=s.indexOf('J 组');const bad=[];if(i&lt;0)bad.push('缺 J 组');else{const seg=s.slice(i);for(const t of ['throw null',\"throw 'x'\",'重扫失败','重试读盘失败','skillResolveCtx','readSkillForInvocation']){if(!seg.includes(t))bad.push('J 组缺用例素材：'+t)}}if(bad.length){console.error('J 组新增用例不合格:\n'+bad.join('\n'));process.exit(1)}console.log('J 组新增行为用例素材齐备')"</automated>
    <fails_when>非零退出，或出现 'J 组新增用例不合格' / '缺 J 组' / 'J 组缺用例素材' 任一行，或标准输出不含 "J 组新增行为用例素材齐备"</fails_when>
  </verify>
  <done>
    miss 重试块的重扫与重试读盘各有独立 `try`：重扫抛错（含 `throw null` / 抛原始值）时判定沿用原值、不逃逸、不升级成第三码、也不重复读盘；重扫成功但重试读盘抛错时同样沿用原判定；两条告警文案可判别且都用 `err && err.message ? err.message : String(err)` 取值；JSDoc 第 3 条的承诺与实现字面一致；既有 J 组 7 条用例与源码护栏语义未变，新增 3 条行为用例全绿，`# fail` 为 0。
  </done>
</task>

<task type="auto">
  <name>Task 3: 文档与账本收口（§10.7 落地时机 / §七 与 AGENTS.md 例数 / 48-VALIDATION 矩阵）</name>
  <files>docs/product/ai-skills.md, AGENTS.md, .planning/phases/48-skill-name/48-VALIDATION.md</files>
  <read_first>
    - `docs/product/ai-skills.md:374-407`（§10.7 诚实边界全段：`:390-392` 的 G-48-4+G-48-6 条、**`:393-400` 的「成本」与「忙时语义」两条**（后者的落地时机清单是本次要改的）、`：401-404` 的「残余窗口」条（首句枚举里同样残留旧措辞）、`:405-407` 的 WR-06 条）
    - `docs/product/ai-skills.md:96-104`（§七「测试与验证」：`:98` 那一行的完整文本与「（N 例，实测）」标注形态）
    - `docs/product/ai-skills.md:304-313`（§10.3「缓存未命中的调用瞬间重扫」三条 —— 本任务**只核对、不改**，除非发现与本计划的实现直接冲突的失实表述）、`:409-422`（§10.8 用户气泡契约 —— 同上，只核对不改）
    - `AGENTS.md:267`（技能域 `- 测试：` 清单行的完整文本；其中 `node tests/test-ai-skills.js` 条目的例数要刷成实测值）与 `:272`（「技能发现与调用的维护约定」：改动实时读盘 / 忙时语义口径**必须同步**权威章节**与本节测试清单**）
    - `.planning/phases/48-skill-name/48-VALIDATION.md:50-56`（Per-Task Verification Map 的 10 列表头）与 `:57-90`（既有行样例，含 48-07 落的 `48-07-T1` 行）
    - Task 1 / Task 2 的**实测输出**（`node tests/test-ai-skills.js` 的 `# tests` 值 —— 两处账本例数的唯一来源，**不得按推算填**）
    - `.planning/phases/48-skill-name/48-REVIEW.md` 的 `WR-07` / `WR-08` 两节（本次两条修复各自要收口的那句措辞的原始出处）
  </read_first>
  <action>
    **A. `docs/product/ai-skills.md`**

    1. §10.7「忙时语义」条（`:396-400`）：把「prompt 回写与 `skills:changed` 广播延后到下一次非忙同步点（……）落地」里的落地时机清单改为与新实现一致 —— **任一轮成功出口**（`prompt()` 与 `promptWithContext()` 各一处，**含纯文本轮**）/ 打开面板 / Agent 创建或重建。同条内需补一句补刷的**失败恢复语义**：补刷本身失败时会恢复「待回写」标记，在下一次成功出口重试，绝不静默丢弃。
    2. §10.7「残余窗口」条（`:401-404`）：首句对「重扫只在哪些触发点发生」的枚举里同样残留旧措辞（把纯文本轮排除在外的那个表述），改为与实际触发点一致（「缓存未命中」+ 打开面板 / Agent 创建或重建 / **任一轮成功出口的补刷**）。该条后半段（两种**缓存命中**情形要等下一次重扫才被纠正、46 D-09 的既有口径）**保持不变**。
    3. §10.7 末尾的 WR-06 条（`:405-407`）**原样保留**（「未被修复、随 Phase 49、不得据此声称已修」的声明不得动，也不得删）。
    4. §七（`:98`）：把 `node tests/test-ai-skills.js` 那一行的例数刷成 Task 2 的**实测值**（保持「（N 例，实测）」形态，N 取实跑输出），并在该行覆盖面描述的末尾补上本次新增面 —— 「延迟补刷在纯文本流的成功出口落地（G-48-18）」与「重扫 / 重试读盘抛错沿用原判定（G-48-19）」。该行其余文字不动。
    5. §10.3 与 §10.8：**逐条核对**是否因本次改动失实；**如无失实即不改动**（不得为凑数而改；若确实发现与本计划实现直接冲突的表述，只改那一处并在 SUMMARY 记明理由）。
    6. 全文件不得出现把 WR-02 / WR-06 / TD-48-01 / TD-48-02 说成「已修」的表述（本次只新增了 WR-08 与 WR-07 的收口）。

    **B. `AGENTS.md`**

    7. `:267` 的 `- 测试：` 清单行里 `node tests/test-ai-skills.js` 条目的例数刷成 Task 2 的实测值，覆盖面描述补上「延迟补刷在成功出口落地 / 重扫或重试读盘抛错沿用原判定」。该清单行的其余条目、`:272` 的维护约定文字与文件其余部分一律不动。

    **C. `.planning/phases/48-skill-name/48-VALIDATION.md`**

    8. Per-Task Verification Map 表末追加**三行**（列数与表头一致，10 列；`Status` 一律 `⬜ pending`，`File Exists` 写 `✅（已有）`，`Automated Command` 写 `node tests/test-ai-skills.js`，`Plan` 列写 `08`，`Wave` 列写 `7`）：
       - `48-08-T1` / Requirement `DISC-02` / Threat Ref `T-48-08-02` / Test Type `unit + 行为` / Secure Behavior：延迟补刷唯一实现 + 两处成功出口共用 + 检脏早退零重扫 + 错误出口与清理不补刷。
       - `48-08-T2` / Requirement `DISC-06` / Threat Ref `T-48-08-04` / Test Type `unit + 行为` / Secure Behavior：重扫抛错（含抛原始值）沿用原判定、不逃逸、不升级第三码；重试读盘抛错同样兜底；两条告警可判别。
       - `48-08-T3` / Requirement `DOC-02`（若表内既有行使用该 ID 风格；否则沿用 `DISC-02`）/ Threat Ref `—` / Test Type `文档 + 结构` / Secure Behavior：§10.7 落地时机与失败恢复语义收口、§七 与 `AGENTS.md` 例数与实跑一致、矩阵行齐备。
       表头、既有行与说明段不动。

    **D. 刻意不做的事（写进 SUMMARY）**

    9. **不改 `.planning/WINDOWS.md`**：本计划的两条 truth 面都是**主进程纯逻辑**，K 组 / J 组行为用例在真实 `syncAgentSystemPrompt()` 上闭环 —— 不新增 unrun-verify 条目（与 48-07 同款记账口径：不制造与「计数未动」自相矛盾的账本变更）。
    10. **不改 `.planning/phases/48-skill-name/48-UAT.md`**：`G-48-18` / `G-48-19` 的 `status: failed` 由重跑 `/gsd-verify-work 48` 的自动驱动探针判定后回填（48-07 已确立的交接口径）。
    11. **SUMMARY 必须显式披露**（避免被读成已端到端证成）：① G-48-18 的可观察结果分两半 —— 「本轮结束时补刷落地」由 K 组在真实 `syncAgentSystemPrompt()` 上钉死（主证据）；「已打开面板 / 模型自动匹配随后可见」是**组合面**（广播 → renderer 无条件重拉快照 48-06 已落地并有护栏 → 面板原地重渲染 48-02 已测 + SDK 提示词模板的 `read` 指令），其运行时终证可由下一轮 `/gsd-verify-work 48` 的自动驱动探针复核（形态：先打开 `/` 面板 → 运行期在 `managed-skills/` 下新建目录 → **不重开面板**直接手打 `/skill:<新名>` → 断言面板已出现该行且主进程有对应 `发送消息` 日志）；② WR-02（`ai-manager.js:1046` / `:1174` 两处裸调 + 首次读盘与动态 import）**仍开** —— 本次只把**新增的调用点**纳入 try；③ WR-06 **保持开放**随 Phase 49；④ 本计划改了 `ai-manager.js` / `tests/test-ai-skills.js` / `docs/product/ai-skills.md` / `AGENTS.md` / `48-VALIDATION.md`，故 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）；`AGENTS.md` 不在 `covered_files` 内，按 48-06 同款口径记账。
    12. **SUMMARY frontmatter 必须带 `gap_ids: [G-48-18, G-48-19]`**（供 `/gsd-verify-work` 对账已闭合的 gap），并记录两条实测例数（`# tests` 值）。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const d=fs.readFileSync('docs/product/ai-skills.md','utf8');const bad=[];const i7=d.indexOf('### 10.7');const i8=d.indexOf('### 10.8');const s7=d.slice(i7,i8);for(const t of ['成功出口','skills:changed','残余窗口','WR-06','保持开放']){if(!s7.includes(t))bad.push('§10.7 缺：'+t)}if(s7.includes('idle 边界'))bad.push('§10.7 仍残留把纯文本轮排除在外的旧落地时机措辞');if(s7.indexOf('WR-06')&lt;0||s7.indexOf('保持开放')&lt;0)bad.push('WR-06 的开放声明缺失');const line=d.split('\n').find((l)=&gt;l.includes('test-ai-skills.js')&amp;&amp;l.includes('例，实测'));if(!line)bad.push('§七 缺「（N 例，实测）」标注行');else{const n=Number((line.match(/(\d+) 例，实测/)||[])[1]||0);if(!(n&gt;=146))bad.push('§七 例数 '+n+' 低于地板 146');if(!line.includes('G-48-18')||!line.includes('G-48-19'))bad.push('§七 覆盖面未补本次两条修复')}if(bad.length){console.error('docs 收口不合格:\n'+bad.join('\n'));process.exit(1)}console.log('docs §10.7 落地时机 + §七 例数/覆盖面 收口 ok')"</automated>
    <fails_when>非零退出，或出现 'docs 收口不合格' 或任一条 `§10.7 缺` / `仍残留` / `开放声明缺失` / `§七 缺` / `例数` / `覆盖面未补` 行，或标准输出不含 "docs §10.7 落地时机 + §七 例数/覆盖面 收口 ok"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const fs=require('fs');const {execSync}=require('child_process');const bad=[];const out=execSync('node tests/test-ai-skills.js 2&gt;&amp;1',{encoding:'utf8'});const n=Number(((out.split('\n').find((l)=&gt;l.startsWith('# tests '))||'').match(/(\d+)/)||[])[1]||0);if(!n)bad.push('无法解析 test-ai-skills 的实测例数');const line=fs.readFileSync('AGENTS.md','utf8').split('\n').find((l)=&gt;l.startsWith('- 测试：'));if(!line)bad.push('AGENTS.md 缺「- 测试：」行');else{if(!line.includes('tests/test-ai-skills.js'))bad.push('AGENTS.md 测试清单缺 test-ai-skills.js 条目');else if(!line.includes(n+' 例'))bad.push('AGENTS.md 的 ai-skills 例数与实跑值 '+n+' 不一致');if(!line.includes('G-48-18')||!line.includes('G-48-19'))bad.push('AGENTS.md 覆盖面未补本次两条修复')}const v=fs.readFileSync('.planning/phases/48-skill-name/48-VALIDATION.md','utf8').split('\n');const hdr=v.find((l)=&gt;l.startsWith('| Task ID |'));if(!hdr)bad.push('48-VALIDATION 缺矩阵表头');else{const cols=hdr.split('|').length;for(const id of ['48-08-T1','48-08-T2','48-08-T3']){const r=v.find((l)=&gt;l.startsWith('| '+id+' |'));if(!r)bad.push('48-VALIDATION 缺 '+id+' 行');else if(r.split('|').length!==cols)bad.push(id+' 行列数 '+r.split('|').length+' 与表头 '+cols+' 不一致')}}if(bad.length){console.error('账本收口不合格:\n'+bad.join('\n'));process.exit(1)}console.log('AGENTS.md + docs §七 + 48-VALIDATION 例数/行数一致（'+n+' 例）')"</automated>
    <fails_when>非零退出，或出现 '账本收口不合格' 或任一条 `无法解析` / `缺 ` / `不一致` / `覆盖面未补` 行，或标准输出不含 "AGENTS.md + docs §七 + 48-VALIDATION 例数/行数一致"</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1 | grep -E "^# (tests|pass|fail)" &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（任一测试套件断言失败），或 "# fail" 行 &gt; 0，或（ai-skills）"# pass" 行 &lt; 146、（picker）"# pass" 行 &lt; 95，或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `docs/product/ai-skills.md` §10.7 的落地时机清单与失败恢复语义已与新实现一致、文件内不再残留把纯文本轮排除在外的旧措辞、WR-06 的「保持开放」声明原样在册；§七 与 `AGENTS.md:267` 的例数等于实跑值且覆盖面补上本次两条修复；§10.3 / §10.8 经核对后（如无失实）未改动；`48-VALIDATION.md` 新增 10 列对齐的 `48-08-T1` / `48-08-T2` / `48-08-T3` 三行；SUMMARY 显式披露组合面边界、WR-02 / WR-06 仍开、covered_digest 预期 stale，并带 `gap_ids: [G-48-18, G-48-19]`；本任务的三条 automated 验证门全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main（`ai:prompt` / `ai:prompt-with-context`） | 只有带上 @ 引用或附件的消息才走后者；两条通道共用同一份技能解析与同一份延迟补刷实现 —— 分流差异正是 G-48-18 的成因 |
| main → 磁盘（`managed-skills/` / `skills/`） | 补刷会重扫这两个目录（逐技能读 `SKILL.md`）；调用点恒处于忙碌态，故补刷只在轮结束后发生 |
| main → renderer（`skills:changed` 广播） | 广播只能由「真正改写 `agent.state.systemPrompt`」的那一次同步触发（全仓唯一广播点）；消费侧只重拉零 IO 快照 |
| 「编辑器/模型经 write·bash 建目录」→ 缓存 | 写入免确认落在硬沙箱 root 内，缓存是 `shadowed` / `disabled` / `tier` 的唯一权威 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-08-01 | Spoofing | `_flushDeferredSkillsPrompt()` 与两个调用点 | medium | mitigate | 补刷只调 **唯一权威入口** `syncAgentSystemPrompt()`（其函数体逐字未改），shadowed（46 D-06 / D-11）、disabled（46 D-09 / D-10）、tier（D-14）仍由同一条 `refreshSkills` 管线产出；源码门禁断言全文件补刷调用恰 2 处、`promptWithContext` 内不残留任何脏标记读写（杜绝第二份实现与第二套判定）；既有 J 组负例（disabled / shadowed）继续作为行为证据 |
| T-48-08-02 | Denial of Service | 补刷引入的额外全量重扫 | high | mitigate | 补刷首行**检脏早退**（无待补刷时零 IO）：源码门禁断言检脏判断位于复位之前，行为用例 K2 断言 `rescanCalls === 0`；补刷只在成功出口执行一次（K1 断言 `rescanCalls === 2` 而非 3，排除双刷）—— 否则「每条普通消息多付一次全量重扫」会成为新常态 |
| T-48-08-03 | Repudiation | 补刷失败被静默吞掉 | medium | mitigate | 失败时恢复「待回写」标记（不丢变更）+ `console.warn` 明确文案；行为契约由 K3 钉住「错误出口保留脏标记」这一半，恢复语义由源码门禁（失败恢复置脏位于同步调用之后）钉住 |
| T-48-08-04 | Tampering | miss 重扫 / 重试读盘的异常处理 | high | mitigate | 两个各自独立的 `try`：重扫抛错与重试读盘抛错都就地 `catch` + 可判别告警 + **沿用原判定**（不赋 `result`、不逃逸、不升级为第三码）；err 取值一律 `err && err.message ? err.message : String(err)`（对齐 `ai-skills-manager.js:634`），并用 `throw null` / 抛原始值的用例钉死「catch 体自身不得二次抛错」；失败码域仍只有 `not_found | disabled`（`skillErrorFromReason` 唯一来源） |
| T-48-08-05 | Denial of Service | 异常逃逸导致 `isProcessing` 永不复位 | medium | mitigate（部分） | 本计划把**新增的那个调用点**（重试读盘）纳入独立 `try`，消除本增量新引入的逃逸口。**诚实边界**：`ai-manager.js:1046` / `:1174` 两处裸调、首次 `readSkillForInvocation` 与 `formatSkillInvocation` 动态 import 仍不在 try 内 —— 即 **WR-02 仍开**，用户未裁定，本计划不修也不得声称已修（SUMMARY 必须披露） |
| T-48-08-06 | Tampering | 已缓存技能的 64 KiB 字节闸旁路 | medium | accept | WR-06 用户裁决「保持开放、随 Phase 49 处置」；本计划不改读写盘路径，`docs/product/ai-skills.md` §10.7 的 WR-06 条原样保留（不得删、不得写成已修） |
</threat_model>

<verification>
- `node --check ai-manager.js` 通过；`node tests/test-ai-skills.js` → `# fail 0`、`# pass ≥ 146`；`node --test tests/test-skill-picker-model.js` → `# fail 0`、`# pass ≥ 95`
- 补刷单源门禁：`_flushDeferredSkillsPrompt` 方法体含「检脏（早于复位）→ 复位 → `await this.syncAgentSystemPrompt()` → 失败恢复置脏（晚于同步）」四要素；全文件 `await this._flushDeferredSkillsPrompt()` **恰 2 处**；`promptWithContext` 内不含 `this._skillsPromptDirty`
- 行为门禁（K 组）：纯文本 `/skill:<运行期新增名>` 成功后脏标记归 `false` + `systemPrompt === buildSystemPrompt()` 且含该技能 + `skills:changed` 恰广播一次 + `rescanCalls === 2`；脏标记为假时 `rescanCalls === 0`；`promptWithContext` 的 `catch` 与 `_cleanupCurrentAgent()` 均不补刷
- 失败分支门禁（J 组新增）：重扫抛 Error / 抛 `null` / 抛原始字符串 → 均正常 resolve 且 `skillError.code === 'skill_not_found'`、不重复读盘、告警含「重扫失败」；重试读盘抛 `null` → 沿用原判定、告警含「重试读盘失败」；两条告警可判别
- 不回归：既有 J 组 7 条 + 46-04 五条 `syncAgentSystemPrompt()` 方法体断言 + `:1299` 早退→改写→广播形态 + `:1366` 两处创建点调用文本一致 全部继续绿；`ai-skills-manager.js` 零 diff；`src/renderer.js` 零 diff（`.refreshSkills(` 计数仍恒 1）
- 文档账本：§10.7 落地时机与失败恢复语义收口且无旧措辞残留、WR-06 条原样在册；§七 与 `AGENTS.md:267` 的例数 = 实跑值（Gate H 交叉校验）；`48-VALIDATION.md` 三行 10 列对齐
- **诚实边界**：G-48-18 的「已打开面板 / 模型自动匹配随后可见」是组合面（广播 → renderer 无条件重拉（48-06）→ 面板重渲染（48-02）+ SDK 提示词模板的 `read` 指令），其运行时终证可由下一轮 `/gsd-verify-work 48` 的自动驱动探针复核，形态与判据写进 SUMMARY；本计划**不**把两条 gap 自行标为 resolved、**不**改 `48-UAT.md`
- 不动项：`ai-skills-manager.js` / `src/renderer.js` / `syncAgentSystemPrompt()` 方法体 / `.planning/WINDOWS.md` / `48-UAT.md` / TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-04 / WR-05 / WR-06
- 指纹提示：本计划改 4 个源/文档文件 + `48-VALIDATION.md` → 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）
</verification>

<success_criteria>
- 纯文本 `/skill:<运行期新增名>`（无 @ 引用与附件）成功一轮结束后：`_skillsPromptDirty` 归 `false`、`agent.state.systemPrompt` 已含运行期新增技能、`skills:changed` 恰广播一次 —— 由 K 组在**真实** `syncAgentSystemPrompt()` 上行为断言
- 延迟补刷只有一个实现、两个成功出口共用（全文件恰 2 处调用）；首行检脏早退 ⇒ 普通消息零重扫；错误出口与 `_cleanupCurrentAgent()` 仍不补刷（不双刷）
- `syncAgentSystemPrompt()` 抛错时 `_resolveSkillInvocation` 返回仍为 `{ skillError: { code: 'skill_not_found' } }`；`throw null` / 抛原始值不得二次抛错；重试读盘抛错同样不逃逸；两条告警可判别
- 失败码域仍只有 `not_found | disabled`；shadowed / disabled / tier 仍全部来自 `refreshSkills` 同一条管线；`ai-skills-manager.js` 与 `syncAgentSystemPrompt()` 函数体零 diff；renderer 零改动
- 既有 139 例零回归，新增用例全绿（K 组 5 + J 组 3，另改写 1 条旧护栏），全套 `# fail 0`
- `docs/product/ai-skills.md` §10.7 的落地时机/失败恢复语义与实现一致、旧措辞为 0 命中、WR-06 开放声明在册；§七 与 `AGENTS.md:267` 的例数等于实跑值；`48-VALIDATION.md` 三行齐备
- SUMMARY 显式披露：组合面（面板/模型自动匹配）的运行时终证归下一轮 `/gsd-verify-work 48` 探针；WR-02 与 WR-06 仍开且未被声称已修；`covered_digest` 预期 stale；`gap_ids: [G-48-18, G-48-19]`
</success_criteria>

<output>
Create `.planning/phases/48-skill-name/48-08-SUMMARY.md` when done
</output>
