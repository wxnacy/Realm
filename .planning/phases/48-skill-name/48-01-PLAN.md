---
phase: 48-skill-name
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skill-picker-model.js
  - tests/test-skill-picker-model.js
  - ai-skills-manager.js
  - ai-manager.js
  - ipc-handlers.js
  - src/preload.js
  - src/renderer.js
  - src/index.html
  - src/styles/main.css
  - tests/test-ai-skills.js
autonomous: true
requirements: [DISC-02, DISC-03, DISC-04, DISC-06, DISC-07]
user_setup: []

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "用户以 `/skill:name [args]`（或裸 `/name [args]`）发送时，主进程解析出 name 与 args，当场从磁盘读取技能正文，并按 `formatSkillInvocation(skill, provenance + '\\n\\n' + args)` 组装成 `<skill …>…</skill>` 块（D-05 / DISC-02）"
    - "技能块置增强消息最前，其余四段相对顺序不变：`[skillBlock, visionNotice, markerBlock, visionBlock, contextBlock]`；无技能调用时输出与改动前逐字符相同（D-07）"
    - "技能调用落入对话历史（`agent.prompt` 收到增强文本）并触发 LLM 回复，与本地 `SLASH_COMMANDS` handler 语义区分；`_ensureConversation` 收到的仍是原始语法文本，故对话标题不退化（D-19 / DISC-03）"
    - "技能调用响应回传 `skillInvocation`；重开对话后 `getConversationMessages` 把 `<skill>` 块还原为同一形状 —— user 行 `content` **逐字符等于**当时收到的 args（SDK 追加的 provenance 行必须被剥离，不得留在正文里）、`skillInvocation.content` 为块体；气泡不出现原始 XML，也不出现被隐藏的 provenance 行（D-09 / D-06 / P-48-05）"
    - "技能不存在 / 已禁用时，响应回传结构化 `skillError.code`（`skill_not_found` / `skill_disabled`）与中文 `message`，主进程不调用 `agent.prompt`（DISC-06 / D-13 推论：被 `refreshSkills()` 整条跳过的技能与「不存在」同形处理，不另立错误码）"
    - "`disableModelInvocation === true` 的技能：name **不出现**在 `buildSkillsPrompt()` 输出中（不进 system prompt），但 `readSkillForInvocation` 仍返回 `{ok:true}`（可经 `/skill:` 显式调用）；且 `disableModelInvocation` 与 `disabled` 是两个互不蕴含的 flag（同时为真时禁用面胜出）（DISC-07 clause ①②③）"
    - "`readSkillForInvocation` 只对 `_cache.skills` 中 `shadowed !== true` 且 `disabled !== true` 的条目读盘；磁盘 SKILL.md 被删 / 被改成不可解析 / 目录被换成别的技能 → 一律 `{ok:false, reason:'not_found'}`，绝不冒名注入（D-13 推论）"
    - "`formatSkillInvocation` 的 `content` 缺失守卫：读盘得到空串或 `content` 非 string 时组装被拒，绝不产出字面量 `undefined`（P-48-03）"
    - "`getSkillsForUI(seededNames)` 返回收窄投影：每个条目含 `name/description/tier/disableModelInvocation/disabled/shadowed/shadowedBy/overLimit/promptOmitted`，且**不含** `content` / `filePath` / `diagnostics`（D-14 / DISC-04 / 事实 5）"
    - "三档 `tier` 判定只消费既有数据：`source==='user'` → `user`；`name ∈ seededNames` → `builtin`；其余 `managed` → `managed`；seededNames 由 `ai-manager` 注入，`ai-skills-manager.js` 保持零 electron 依赖（D-14）"
    - "`promptOmitted` 只打在 ⑦ 预算循环中被丢弃的条目上（`eligible.slice(kept.length)`），`shadowed` / `disabled` / `overLimit` / `disableModelInvocation===true` 条目**永不**为 true（P-48-08 / D-12）"
    - "[UI] 气泡 pill 与技能正文折叠块无 loading / error 独立状态（内容随消息对象一次到位，是已入库消息的纯渲染）"
    - "[UI] 技能正文折叠块 overflow 由 body 的 `max-height: 260px` + `overflow-y: auto` 承接，header 固定不随内容滚动"
    - "[UI] 气泡技能 pill `max-width: 180px` + 名称 ellipsis，`技能` 微标 `flex-shrink: 0` 优先保留；折叠块 body `white-space: pre-wrap` + `word-break: break-word`，长行强制折行、无横向滚动"
    - "[UI] system-note 文本反馈沿用既有 `.ai-system-note` 单行居中形态与宽度行为，不改（会波及 `/clear` / `/compact` 既有反馈）"
    - "[UI] system-note 文案长度天然可控（技能 `name` 受 `^[a-z0-9-]+$` 与目录名约束，`description` 不进任何 system-note 文案），无需新增截断规则"
    - "[assumption] DISC-02 empty：`skill.content` 为空串或仅空白 → `readSkillForInvocation` 返回 `not_found`，组装被拒；绝不产出字面量 `undefined`（`48-RESEARCH.md`「五条必须先知道的硬事实」事实 1 的实测输出：缺 `content` 只产字面量 `undefined`、不抛错）"
    - "[assumption] DISC-02 encoding：长度口径沿用既有常量语义不新定义 —— `skill.content` 的字符数按 JS `String.length`（与 `LIMITS.SKILLS_PROMPT_CHAR_BUDGET = 8000` 同口径，也是 UI-SPEC「技能正文（N 字符）」的 N）；`LIMITS.MAX_SKILL_MD_BYTES = 64 * 1024` 按 `FileInfo.size` 的字节数。相等性判定按逐字符全等（`fresh.name !== name`），不做 Unicode 归一化"
    - "[assumption] DISC-03：只有注入成功才入库 —— `skillError` 路径零 user 气泡、零 assistant 占位、零对话行写入；解析失败不得留下半截历史"
    - "[assumption] DISC-07：`disableModelInvocation` 与 `disabled` 是两个**互不蕴含**的独立 flag —— 前者可经 `/skill:` 显式调用且**永不** `promptOmitted`；后者两个入口（面板与手打）都拒绝"
  artifacts:
    - src/skill-picker-model.js
    - tests/test-skill-picker-model.js
    - ai-skills-manager.js
    - ai-manager.js
    - ipc-handlers.js
    - src/preload.js
    - src/renderer.js
    - src/styles/main.css
  key_links:
    - "renderer `handleSendAIMessage` 斜杠分支 → `realmAPI.ai.prompt` / `promptWithContext`（发完整语法文本，D-19）→ ai-manager `_resolveSkillInvocation` → `ai-skills-manager.readSkillForInvocation`（实时读盘）"
    - "`ai-manager.getSkillsForUI()` → `builtin-skills-seeder.getSeededSkillNames()`（注入 `seededNames`）→ `ai-skills-manager.getSkillsForUI(seededNames)`（收窄投影）→ `ai:get-skills` / `ai:refresh-skills` → `realmAPI.ai.getSkills` / `refreshSkills`"
    - "`ai-manager.refreshSkillsForPanel()` → `syncAgentSystemPrompt()`（不改其函数体）→ `windowManager.broadcast('skills:changed')` → renderer `onIpcMessage('skills:changed')` **只重拉快照**"
    - "`ai-manager.getConversationMessages()` → `<skill>` 块装饰 + `matchSkillByPath(location)` 还原 tier → 重载路径对象形状与 live 路径逐字一致"
    - "`ai-manager._setupEventBroadcasting()` 的 `tool_execution_start` → `skill_invocation` 字段 → renderer `toolExecution.skillInvocation`"
  prohibitions:
    - statement: "本阶段**零新增技能写路径** —— 不提供任何创建 / 修改 / 删除技能文件的能力，`refreshSkills()` 与新增的面板刷新入口全部只读；技能写路径归 Phase 49（`manage_skill` 三动作）/ 50（启停卸载）/ 51（导入）"
      status: unresolved
      verification: judgment
      reason: "T-48-02（high）的唯一缓解形态是「不提供能力」而非「过滤输入」；一旦本阶段长出任何写入口，`syncAgentSystemPrompt()` 的失效链与沙箱边界都要重新论证。fallback 召回项，无 wired check，按 flagged-unverified 处理"
    - statement: "不得把技能条目并入 `SLASH_COMMANDS` —— 技能必须作为第二命令源经 `kind` 判别字段分流（并入即被本地 handler 吞掉，表现为「点了没反应」）"
      status: unresolved
      verification: judgment
      reason: "D-01 / D-04 的双命令源模型：本地命令优先靠 `kind` 判别实现，并入注册表会让技能行走本地 handler 而被静默吞掉"
    - statement: "不得在 renderer 端重算任何技能集状态 —— 优先级 / 遮蔽 / 限额 / 预算省略 / 来源档位只能消费主进程投影字段"
      status: unresolved
      verification: judgment
      reason: "46 D-06 的单一数据权威（UI-SPEC 自绘边界明文禁止渲染端重实现）；两份判定必然漂移，且 renderer 拿不到随包技能目录与技能目录的权威路径"
    - statement: "不得以 prompt 冻结快照或对话历史里的旧正文替代调用瞬间的实时读盘；也不得在装配失败时回退到缓存正文"
      status: unresolved
      verification: judgment
      reason: "用户 2026-09-11 在 46-UAT 明确要求（`STATE.md:223`）；46-UAT Test 3 的「切回老对话看不到磁盘改动」正是复读旧正文的表征，回退缓存会让该约束形同不存在"
---

<objective>
打通 `/skill:name [args]` 显式调用的**主进程端到端纵切**：解析（renderer 与 main 共用一份纯函数规则）→ 实时读盘 → `<skill>` 块组装 → 注入 Agent → 落入对话历史 → 重开对话正确还原气泡；同时把技能集收窄投影与三档 `tier` 判定落到主进程唯一权威，并新增两个面板 IPC 通道与 `promptOmitted` 条目字段。

Purpose: DISC-02/03/04/06/07 的全部主进程半边。这是本阶段唯一「非 UI 可见」但最先必须证明的架构路径 —— 它压住 P-48-01（args 吞字符）、P-48-02（缺 `content` 静默产出 `undefined`）、P-48-07（seeded 注入）与用户 2026-09-11 的「调用瞬间实时读盘」硬约束。面板、徽标、`read` 卡片特殊化作为后续计划的扩展面。

Output: `src/skill-picker-model.js`（新增，双模式导出）+ 两个新 IPC 通道 + `ai-skills-manager.js` 四个新导出与一个条目字段 + `ai-manager.js` 的解析/组装/返回契约/重载装饰 + renderer 调用路径接线（预检 / 气泡 / 错误回退）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-RESEARCH.md
@.planning/phases/48-skill-name/48-PATTERNS.md
@.planning/phases/48-skill-name/48-UI-SPEC.md
@.planning/phases/48-skill-name/48-VALIDATION.md
@ai-skills-manager.js
@ai-manager.js
@ipc-handlers.js
@src/preload.js
@src/renderer.js
@tests/test-ai-skills.js
</context>

<assumptions>
> 以下 3 条是 `48-VALIDATION.md` 的 edge probe 报告里 **`unresolved`（unclassified）** 的边界问题，
> 按 spec-less fallback 降级为**显式 planner 裁决**（不静默丢弃、不 auto-resolve 成 backstop）。
> 裁决已写入上方 `must_haves.truths` 的 `[assumption]` 行，此处给出完整论证与落点。

1. **DISC-02 / empty** —— 「技能正文为空」的裁决。
   `formatSkillInvocation` 对缺失 `content` **不抛错、静默产出字面量 `undefined`**
   （`48-RESEARCH.md` 事实 1 的实测输出）。因此「空」必须**在组装之前**被拒绝，
   而不是交给 SDK 兜底。裁决：`readSkillForInvocation` 在 `typeof fresh.content !== 'string' || fresh.content === ''`
   时返回 `{ok:false, reason:'not_found'}`。落点：`ai-skills-manager.js` 的新增读盘函数。

2. **DISC-02 / encoding** —— 「长度/相等性口径由谁定义」的裁决。
   **不新定义任何口径**，全部沿用既有常量语义：
   - 字符数 = JS `String.length`（UTF-16 code unit）。这既是 `LIMITS.SKILLS_PROMPT_CHAR_BUDGET`
     的口径（既有断言锁定），也是 UI-SPEC「技能正文（N 字符）」的 N。（注意：UTF-16 code unit
     与「字素簇」在 emoji 上会不同；本阶段**明确采用前者**，因为它是既有常量的既有语义。）
   - 字节数 = `FileInfo.size`（UTF-8 字节），只用于 `LIMITS.MAX_SKILL_MD_BYTES` 的既有判定。
   - 名字相等性 = **逐字符全等**（`fresh.name !== name` → `not_found`），不做 NFC/NFD 归一化
     —— 归一化属 Phase 51 的 entry 名查重职责（SEC-03），不属本阶段。

3. **DISC-03** —— 「进历史」的失败边界裁决。
   只有**注入成功**才入库：`skillError` 路径下主进程不调用 `agent.prompt`，
   renderer 移除刚推送的 user 气泡与未产出的 assistant 占位，复位
   `state.aiStreaming` / `aiCurrentMessageId` / 发送按钮，`pushSystemNote(message)`。
   不允许「先建气泡再回滚失败」之外的任何半截历史（不新建对话行）。
</assumptions>

<!-- planner-discipline-allow: 技能调用 / args 吞字符 / 未找到技能 / 已被禁用 -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 端到端 `/skill:name` 主干 —— 解析 → 实时读盘 → 组装 → 注入 → 重载还原</name>
  <files>src/skill-picker-model.js, tests/test-skill-picker-model.js, src/index.html, ai-skills-manager.js, ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-skills-manager.js`（被改造文件）：`:1-61`（文件头的依赖纪律 + `LIMITS` + 模块级 `_cache` 形状注释）、`:298-374`（`isDescriptionUnusable` / `enforceDirNameAuthority` / `applyShadowing`）、`:440-500`（`refreshSkills(env, {disabled, rootDirs})` 的注入式签名与 SDK 动态 import 先例）、`:637-668`（`buildSkillsPrompt` / `getSkillsSnapshot` / 导出面）
    - `ai-manager.js`（被改造文件）：`:94-121`（`getAgentWorkspaceLazy` / `getAiSkillsManagerLazy` 惰性 require 模板）、`:575-600`（`buildWorkspacePrompt` / `buildSystemPrompt`）、`:977-1060`（`prompt()` 全段，特别是 `:1021` 的 `_ensureConversation(message)` 与 `:1039` 的 `agent.prompt(message)`）、`:1089-1160`（`promptWithContext()` 的 `titleSource` 派生）、`:1223-1260`（增强消息组装 + 附件元数据挂载）、`:1305-1341`（`_ensureConversation` / `_deriveConversationTitle`）、`:2116-2130`（`saveCurrentConversation`）、`:2461-2480`（`getConversationMessages`）、`:5789-5794`（导出面）
    - `src/renderer.js`：`:978-1000` 与 `:8663-8770`（斜杠拦截与 IPC 分流条件）、`:9810-9824`（`executeActiveSlashCommand` 的 `rest` 取值缺陷）、`:8029-8100`（`renderAIMessages` 的 user 分支与 pill 先例）、`:10834-10838`（`escapeHtml`）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/skills.js:8-11`（`formatSkillInvocation` 模板权威）与 `:226-233`（`loadSkillFromFile` 返回形状）；`:88-103`（`loadSkills(env, dir)` 单目录加载语义）
    - `ai-conversations-manager.js:570-663`（`getMessages` 的显示形状 + `<context-summary>` 剥壳特判先例）
    - `tests/test-ai-skills.js:13-73`（`withTempRoot` / `writeSkill` / `readSource` / `functionBody` / `methodBody` 五件套）、`:75-110`（tracer 用例骨架）、`:1127-1200`（依赖纪律与导出面源码断言组）
    - `48-RESEARCH.md` §「五条必须先知道的硬事实」全节、§2.1–§2.7、§2.8 的八行矩阵、§「代码骨架」
    - `48-PATTERNS.md` §`ai-skills-manager.js` ①③⑤⑥⑦⑧⑨、§`ai-manager.js` ①④⑤⑥⑦⑧⑩、§`src/skill-picker-model.js`
  </read_first>
  <behavior>
    - `extractArgs('/skill:find 帮我找 X')` → `'帮我找 X'`；`extractArgs('/fin 帮我找 X')` → `'帮我找 X'`；`extractArgs('/cle x')` → `'x'`；`extractArgs('/compact 重点保留登录')` → `'重点保留登录'`（七组表逐行）。
    - `parseSkillRef('/skill:foo', ['clear','compact'])` → `{kind:'skill', name:'foo', args:''}`；`'/foo a b'` → `{kind:'skill', name:'foo', args:'a b'}`；`'/clear'`（`clear` 是本地命令）→ `{kind:'command', name:'clear'}`。
    - `parseSkillRef('/foobar', ['foo'])` → `null`（严格前缀 + 空白边界，不得命中 `foo`）；`'/skill:'` → `null`；`'/skill:Foo'` → `null`（违反 `^[a-z0-9-]+$`）；`'/Skill:foo'` → 按裸名处理且不命中 `foo`。
    - `parseSkillInvocationText` 与 `parseSkillRef` 对同一组输入产出相同 `{name, args}`（跨进程契约防漂移）。
    - `readSkillForInvocation(env, name)`：命中 → `{ok:true, skill, source}`；`disabled` → `{ok:false, reason:'disabled'}`；`shadowed` → `not_found`；磁盘文件删除 / `content` 为空串或仅空白 / `fresh.name !== name` → `not_found`。
    - `readSkillForInvocation` 对 `disableModelInvocation: true` / `overLimit` / `promptOmitted` 的条目**仍返回 `{ok:true}`**（D-12「超限可显式调用」+ DISC-07「仅显式可显式调用」）；两个 flag 的独立性：仅 `disableModelInvocation: true` → `{ok:true}`，`disableModelInvocation: true` 且 `disabled: true` → `{ok:false, reason:'disabled'}`（禁用面胜出）。
    - 实时性：写技能 → 调用 → 改盘 → **再次**调用读到新正文。
    - 组装逐字节：`buildSkillInvocationBlock` 输出 === `formatSkillInvocation(skill, provenance + '\n\n' + args)`。
    - `_deriveConversationTitle('/skill:find-skills')` → `'/skill:find-skills'`（不退化为「新对话」）。
    - 重载装饰：把带 `<skill>` 块的 user 行塞进临时对话库 → `getConversationMessages` 还原出 `{content: args, skillInvocation:{name, tier, content}}`；其中 `content` **逐字符等于** args（有 args / 无 args 两例：无 args 时 `content === ''`），块体 `skillInvocation.content` 逐字符等于 live 路径读到的 `skill.content`。
  </behavior>
  <action>
    按 D-19 / D-05 / D-13 落地主进程端到端纵切。三件事：

    **① 新建 `src/skill-picker-model.js`（双模式导出，项目内零先例）** —— 纯逻辑、零 import（不依赖 electron / SDK / DOM），使 `node --test tests/test-skill-picker-model.js` 无需任何桩。用裸 IIFE + 双挂载：`if (typeof module !== 'undefined' && module.exports) module.exports = api; if (typeof window !== 'undefined') window.SkillPickerModel = api;`。导出 `SKILL_PREFIX = 'skill:'`、`extractArgs(inputValue)`、`parseSkillRef(text, commandNames)`、`parseSkillInvocationText(text)`。`extractArgs` 必须用 token 取值法（`value.slice(1).split(/\s/)[0]` 得 token，再 `value.slice(1 + token.length).trim()`），**不得**沿用 `value.slice(1 + cmd.name.length).replace(/^\S*/, '')`（该式在 `/skill:<前缀>` 形态下吞掉 args 开头，见 `48-RESEARCH.md` 事实 2 的七组实测表）。`parseSkillRef`：本地命令优先（对 `commandNames` 做严格前缀 + 空白边界查询 `text === '/' + name || text.startsWith('/' + name + ' ')`）→ `/skill:` 拆分（`name` 须过 `/^[a-z0-9-]+$/`，否则返回 `null`）→ 裸名（token 整体，天然满足严格边界）→ 两边不命中返回 `null`。`parseSkillInvocationText` 与 `parseSkillRef` 共用同一份 token/边界实现，**不得**复制第二份。

    **② `ai-skills-manager.js` 新增**（严格遵守文件头 `:18-21` 的依赖纪律：零 electron、SDK 只走包根动态 import、不自行剥 frontmatter）：
    - `readSkillForInvocation(env, name)` —— 异步。先在 `_cache.skills` 里 `find(e => e.skill.name === name && e.shadowed !== true)`；未命中 → `{ok:false, reason:'not_found', name}`；`entry.disabled === true` → `{ok:false, reason:'disabled', name}`（在读盘**之前**判，不做无用 IO）。随后 `const { loadSkills } = await import('@earendil-works/pi-agent-core')`，`dir = path.dirname(entry.skill.filePath)`，`const { skills } = await loadSkills(env, dir)`。**不做**任何 frontmatter 自解析。新鲜度与冒名校验：`fresh = skills.find(s => s.name === name) || skills[0]`；`!fresh || typeof fresh.content !== 'string' || fresh.content.trim() === ''` → `not_found`（空串**与仅空白**一律在组装之前拒绝 —— 与「[assumption] DISC-02 empty」的裁决一致）；`fresh.name !== name` → `not_found`（目录被换成别的技能，绝不冒名注入）。成功 → `{ok:true, skill:fresh, source:entry.source}`。**不做**「读失败回退缓存快照」。
    - `sourceTierOf(entry, seededNames)` —— 纯函数同步：`entry.source === 'user'` → `'user'`；`seededNames` 归一为 `Set` 后 `has(entry.skill.name)` → `'builtin'`；否则 `'managed'`。
    - `toUISkillEntry(entry, seededNames)` —— 收窄投影，**只**含 `name` / `description` / `tier` / `disableModelInvocation` / `disabled` / `shadowed` / `shadowedBy`（仅 shadowed 时）/ `overLimit` / `promptOmitted`。**不得**含 `content` / `filePath` / `diagnostics`。字段名用 `tier` 而非 `source`（数据层的 `source` 只有 `'user' | 'managed'`，语义与取值域都不同）。
    - `matchSkillByPath(absPath)` —— 纯函数同步，遍历 `_cache.skills` 对 `path.resolve(e.skill.filePath)` 做**规范化全等比较**，命中返回 `{name, tier}`（tier 的 seeded 集合由调用方经可选第二参注入）。**不按目录前缀猜、不重新扫盘**。
    - `getSkillsForUI(seededNames)` —— 返回 `{ skills: _cache.skills.map(e => toUISkillEntry(e, seededNames)), refreshedAt: _cache.refreshedAt, digest: _cache.digest }`。零 IO 同步。
    - ⑦ 预算循环尾部（`ai-skills-manager.js:595` 的 `}` 之后、`:610` 的 `_cache.promptBlock = block;` 之前）新增一行：`for (const e of eligible.slice(kept.length)) e.promptOmitted = true;`。判定边界必须与 ⑦ 的 `eligible` 过滤条件（`:585-587`）**完全一致**。
    - 导出面追加 `sourceTierOf` / `toUISkillEntry` / `matchSkillByPath` / `getSkillsForUI` / `readSkillForInvocation`；并同步更新 `:48-61` 的条目形状权威注释（补 `promptOmitted`）。

    **③ `ai-manager.js` 新增/改动**：
    - `getBuiltinSkillsSeederLazy()` —— 与 `:106-121` 同款惰性 require 模板。
    - `getSeededSkillNamesSafe()` —— 包 try/catch：调用 `getBuiltinSkillsSeederLazy().getSeededSkillNames()`，throw 时 `console.warn` 并按空集合降级（纯 Node 下 `require('electron')` 返回字符串 → `app` 为 undefined → TypeError，见 `48-RESEARCH.md` 事实 4）。**不得**在 renderer 兜底重判 tier。
    - `getSkillsForUI()` —— 同步，`getAiSkillsManagerLazy().getSkillsForUI(this.getSeededSkillNamesSafe())`。
    - `_resolveSkillInvocation(rawMessage)` —— 私有异步，`prompt()` / `promptWithContext()` **共用**（不得出现两份解析）。流程：`parseSkillInvocationText(rawMessage)` → 未命中返回 `{skillBlock:'', skill:null}`；命中 → `await <skillsManager>.readSkillForInvocation(this.sandboxEnv, name)` → `ok:false` 时把 `reason` 映射为 `{skillError:{code:'skill_not_found'|'skill_disabled', message}}`（`not_found` → `未找到技能「${name}」，输入 / 查看可用技能`；`disabled` → `技能「${name}」已被禁用，可在 设置 → AI → 技能管理 重新启用`）。**只映射这两码** —— `readSkillForInvocation` 的返回值域就是 `not_found | disabled`（SDK `loadSkills` 读盘失败只产诊断 + 空技能集、不抛错），D-13 推论也要求「被整条跳过的技能」与「不存在」同形，**不得**再声明第三个码；`ok:true` 时 `buildSkillInvocationBlock` 组装并回传 `{name, tier: sourceTierOf-ish, content}`。`tier` 用 `matchSkillByPath` 同款 tier 计算（seededNames 注入）。
    - `buildSkillInvocationBlock({skill, name}, args)` —— 模块级纯函数：`provenance = '用户显式调用了技能「' + name + '」'`；`instructions = [provenance, args].filter(Boolean).join('\n\n')`；`return formatSkillInvocation(skill, instructions)`（`formatSkillInvocation` 由调用处动态 import 后注入或作为参数传入，避免顶层 SDK import）。
    - `prompt(message)`：在 `:1021` 之前 `const resolved = await this._resolveSkillInvocation(message);`；`resolved.skillError` 时**不调用** `agent.prompt`，直接返回错误契约。`this._ensureConversation(message)` **保持原样**（D-19）。`:1039` 改为 `const enhanced = resolved.skillBlock || message; await this.agent.prompt(enhanced);`（普通消息 `enhanced === message`，逐字节不变）。
    - `promptWithContext(...)`：`titleSource` 计算保持原样；`:1229` 的数组改为 `[skillBlock, visionNotice, markerBlock, visionBlock, contextBlock]`（其余四段相对顺序**逐字节不变**）。
    - 返回契约：所有成功返回路径统一为 `{ conversationId, skillInvocation }`，错误路径为 `{ conversationId, skillInvocation: null, skillError }`。`skillInvocation` 形状 `{ name, tier, content, contentLength }`。
    - `getConversationMessages(conversationId)` —— 对 `conversationStore.getMessages()` 的显示形状做一次装饰：user 行若 `content` 匹配 `<skill name="([^"]+)" location="([^"]+)">\n[\s\S]*?\n</skill>(?:\n\n([\s\S]*))?$`，还原为 `{ content: <args>, skillInvocation: { name, tier: matchSkillByPath(location)?.tier, content: <块体> } }`。三处口径必须精确（实际输出形态是 `<skill …>\nReferences are relative to <dir>.\n\n<正文>\n</skill>`，其后由 `buildSkillInvocationBlock` 追加 `\n\n` + provenance 行 +（有 args 时）`\n\n` + args）：
      - **块体** = 首行 `References are relative to …` 与其后空行**之后**、最后一个 `\n</skill>` **之前**的正文（**固定模板锚定，不得用「最后一个空行」之类的位置猜测**）；
      - **`content`（气泡正文）= args 本身** —— 尾段（捕获组 3）必须先去掉 SDK 追加的 provenance 行（`'用户显式调用了技能「' + name + '」'`）：尾段以该固定模板行开头时取其后（`\n\n` 之后）的部分，无 args 时应得空串；尾段缺失或不为该固定模板时按原样保留。**禁止**把 provenance 行留在 `content` 里 —— 重开对话会把它当用户消息正文渲染出来，违反 D-06 与 UI-SPEC §用户气泡契约（正文 = args 原文）；
      - **两条路径的 `content` 必须逐字符相同**（live 路径的 user 消息对象正文同为 args）—— 否则同一条消息在重启前后渲染不一致。完整语法文本仍按 D-19 发给主进程用于解析与标题派生：那是**另一个变量**，与气泡正文不是同一个字段；
      - `matchSkillByPath` 找不到 → 省略 tier、气泡照常渲染（**不因元数据缺失丢消息**）。形态照抄 `ai-conversations-manager.js:639-651` 的 `<context-summary>` 特判。
    - `module.exports.parseSkillInvocationText = parseSkillInvocationText;` / `module.exports.buildSkillInvocationBlock = buildSkillInvocationBlock;`（供测试直接打表）。

    **④ `src/index.html` —— 脚本加载接线（本计划内闭环，不留到下个计划）**
    在 `model-family.js` 之后、`renderer.js` **之前**插入 `<script src="skill-picker-model.js"></script>`。**顺序不可调换**：`renderer.js` 顶层即引用 `window.SkillPickerModel`，缺该行或次序颠倒会让本计划 Task 3 的技能预检与 48-02 的面板同时读到 `undefined`（本波形因此不可端到端运行 —— 除本行的两条 `node --test` 之外，wave 1 的护栏断言都是源码扫描，必须由本行补齐 `src/index.html` 才能真正跑起来）。

    **⑤ 测试**：
    - 新建 `tests/test-skill-picker-model.js`（`node:test` + `node:assert`，单文件脚本式，模式照 `tests/test-builtin-skills-seeder.js`）：A 组 —— `extractArgs` 七组表逐行照抄 `48-RESEARCH.md` 事实 2 的表；`parseSkillRef` 正例 / 边界反例（`/foobar` `/foo-bar` `/` `/skill:` `/skill:Foo` `/Skill:foo`）；本地命令优先；`parseSkillRef` 与 `require('../ai-manager').parseSkillInvocationText` 的规则一致性（跨进程契约防漂移）。C 组 renderer 半边：`readSkillForInvocation` 的 `disabled` / `not_found` 分支在 picker model 侧的对应判定入口（未找到 → 未找到文案；disabled → 禁用文案）。
    - 扩展 `tests/test-ai-skills.js`：D 组 —— 实时读盘（改盘后二次调用读到新正文）；组装逐字节；拼接顺序 `[skillBlock, visionNotice, markerBlock, visionBlock, contextBlock]`（`indexOf` 相对次序断言 + 无技能时输出与改动前逐字符相同）；`content` 为空串或仅空白 → 组装被拒（不得出现 `\nundefined\n`）；`_deriveConversationTitle` 打表；重载装饰对象形状与 live 路径一致；源码断言 `this._ensureConversation(message)` 出现且 `this._ensureConversation(enhanced` **不出现**。
    - D 组**必须覆盖 DISC-07 三条子句，各有一条能失败的断言**（同一条 `disableModelInvocation: true` 的技能一次构造、三条断言）：① 其 `name` **不出现**在 `buildSkillsPrompt()` 返回值里（不进 system prompt —— 该条目的 `eligible` 过滤已排除 `disableModelInvocation === true`）；② `readSkillForInvocation` 对其返回 `{ok:true}`（可显式调用）；③ 投影中该条目 `disableModelInvocation === true` 且 `promptOmitted !== true`。再加 flag 独立性一例：`{disableModelInvocation: true, disabled: true}` → `{ok:false, reason:'disabled'}`（禁用面胜出），与仅 `disableModelInvocation: true` 的 `{ok:true}` 成对。
    - 重载装饰**值断言**（不止键集合）：构造一条含 args 与一条无 args 的 `<skill>` 块 user 行，断言还原出的 `content` **逐字符等于**构造时写入的 args（无 args 时为 `''`），且 `skillInvocation.content` 逐字符等于 live 路径读到的 `skill.content`。
    - 所有触达 tier 的断言**必须**经 `seeder.setBuiltinDepsForTest({ srcDir: <真实 skills-builtin 绝对路径> })` 注入，并在 `t.after` 复位（`builtin-skills-seeder._resetForTest()`）；tier 断言优先直接把 seededNames 数组注入投影函数（不依赖真实随包目录）。
    - 用 `withTempRoot(t)` + `writeSkill` 建立临时 workspace；`ai-skills-manager._resetCacheForTest()` 必须在 `t.after` 内调用。
  </action>
  <verify>
    <automated>node --test tests/test-skill-picker-model.js</automated>
    <fails_when>非零退出，或输出中出现 "# fail" 行且其值 > 0，或 "# pass" 行缺失</fails_when>
    <automated>node --test tests/test-ai-skills.js</automated>
    <fails_when>非零退出，或 "# fail" 行 > 0，或 "# pass" 行 < 64（既有基线地板）</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('src/index.html','utf8'); const m=s.indexOf('skill-picker-model.js'); const r=s.indexOf('renderer.js\"'); if(m<0) throw new Error('skill-picker-model.js not loaded'); if(r<0||m>r) throw new Error('skill-picker-model.js must load before renderer.js'); console.log('script-order-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "script-order-ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/test-skill-picker-model.js` 通过，且其中至少 11 条断言（七组 `extractArgs` 表 + `parseSkillRef` 正反例 + 规则一致性）为 `# pass`。
    - `node --test tests/test-ai-skills.js` 输出 `# fail 0` 且 `# pass` ≥ 64。
    - `require('../src/skill-picker-model.js')` 与 `window.SkillPickerModel` 是**同一个 api 对象引用**（源文件中 `module.exports = api` 与 `window.SkillPickerModel = api` 共用同一 `const api`）。
    - 源码断言：`ai-manager.js` 内出现 `this._ensureConversation(message)`，且 `grep 'this\._ensureConversation(enhanced' ai-manager.js` 命中数为 **0**。
    - 断言 `ai-manager.js` 中 `agent.prompt(` 的实参在技能路径下为增强文本（源码含 `const enhanced = ` 且紧跟 `await this.agent.prompt(enhanced`）；普通消息分支的 `enhanced === message` 语义由「无技能时输出逐字符相同」的行为断言覆盖。
    - 断言 `readSkillForInvocation` 在 `SKILL.md` 被删后返回 `{ok:false, reason:'not_found'}`，且注入字符串中**不出现** `\nundefined\n`。
    - 断言 `getConversationMessages` 输出的 user 行 `skillInvocation` 对象键集合与 live 路径**消息对象**的键集合**逐字相等**（`{name, tier, content}` 三键；响应级 `contentLength` 不回填进消息对象），且 user 行 `content` **逐字符等于**当时写入的 args（含无 args 时 `''`）。
    - 断言 DISC-07 三条子句各有断言：`disableModelInvocation === true` 的 name 不出现于 `buildSkillsPrompt()`；该条目 `readSkillForInvocation` 返回 `{ok:true}`；投影中 `disableModelInvocation === true` 且 `promptOmitted !== true`。另断言 `{disableModelInvocation: true, disabled: true}` → `{ok:false, reason:'disabled'}`。
    - 断言 `src/index.html` 中 `skill-picker-model.js` 出现在 `renderer.js` **之前**（wave 1 内可端到端运行的前提）。
    - 新增断言组内**零** `setBuiltinDepsForTest` 之外的真实 `skills-builtin/` 依赖（tier 断言全部走注入数组）。
  </acceptance_criteria>
  <done>`/skill:name` 主进程端到端成立：解析正确（七组 args 表全对）、正文实时读盘且空/冒名被拒、`<skill>` 块逐字节等于 SDK 输出、注入后的历史可被 `getConversationMessages` 还原为与 live 路径同形的 `{content: args, skillInvocation:{name, tier, content}}`；`tests/test-ai-skills.js` 的 64 例基线不回归。</done>
</task>

<task type="auto">
  <name>Task 2: 技能集投影 + `promptOmitted` + 两个 IPC 通道 + D-18 措辞</name>
  <files>ai-manager.js, ipc-handlers.js, src/preload.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js`（被改造文件）：`:479-500`（`REALM_SYSTEM_PROMPT` 第 1 段与「你的能力：」）、`:575-600`（`buildSystemPrompt()` 的技能段拼装 —— **零改动**）、`:840-900`（`init()` 内的 `refreshSkills` 注入与 `sandboxEnv` 赋值）、`:2514-2542`（`syncAgentSystemPrompt()` —— **逐字保持**）、`:2461-2480`（`getConversationMessages`）
    - `ipc-handlers.js`：`:1673-1720`（`ai:prompt` / `ai:prompt-with-context` 的三件套写法：`assertTrustedSender` → `aiManager` 判空 → 返回体）
    - `src/preload.js`：`:969-995`（`ai:` 命名空间的 JSDoc + invoke 成对写法）、`:1055-1075`（无载荷通道模板）
    - `builtin-skills-seeder.js:60-107`（`resolveBuiltinSkillsSrc` / `getSeededSkillNames` 的实现与 throw 点）、`:514-546`（`setBuiltinDepsForTest` / `_resetForTest` / 导出面）
    - `tests/test-ai-skills.js:1199-1320`（`Agent prompt 回写（SKILL-04）` 与 `P8 失效链机制断言` 两组的既有源码扫描形态 —— 本任务的「`syncAgentSystemPrompt` 函数体未重构 + 新增生产调用方」断言照此形态新增）
    - `ai-skills-manager.js:48-61`（条目形状注释，Task 1 已补 `promptOmitted`）
    - `48-RESEARCH.md` §1.3（tier 组合点论证）、§1.4（通道命名与形状）、§1.7（`syncAgentSystemPrompt` 的硬约束与早退边界）、§3.4（D-18 措辞落点与三条硬边界）
    - `48-UI-SPEC.md` §Copywriting Contract（面板文案不在本任务，但 D-10/D-13 的两条 system-note 文案在 Task 1 已定，此处只核对一致性）
  </read_first>
  <action>
    **① `ai-manager.js` —— 面板数据源与刷新入口（只加调用方，不改 `syncAgentSystemPrompt()` 函数体）**：
    - 新增 `getSkillsForUI()`（Task 1 已建，确认导出与 seeded 注入路径一致）。
    - 新增 `async refreshSkillsForPanel()`：`await this.syncAgentSystemPrompt(); return this.getSkillsForUI();`。**必须**逐字保留 `syncAgentSystemPrompt()` 的函数体（`tests/test-ai-skills.js:1278-1295` 是方法体源码扫描断言：`refreshSkills(` / `this.agent.state.systemPrompt = next` / `windowManager.broadcast('skills:changed')` / `this._skillsPromptDirty = true` / `snap.digest === this._skillsPromptDigest` 五条必须继续绿）。在函数 JSDoc 中写明早退边界：`if (!this.agent || !this.sandboxEnv) return;` → AI 未初始化时不刷新，面板拿到旧投影；此时**没有任何可用的读盘环境**，降级为不刷新是唯一诚实行为。
    - `REALM_SYSTEM_PROMPT` 第 1 段（`:479`）与 `你的能力：` 之间插入**一个独立段落**（模板字面量内换行 + 空行），语义必须同时包含三点：① 技能（Skill）与工具（Tool）是两个不同概念；② 技能是「按需读取的指令文档 / 工作流」，正文需经 `read` 打开其 `location` 才能看到；③ 未经显式调用时模型不会自动获得技能正文，提示词里只有 name / description / location。**三条硬边界**：不得触碰技能段（`buildSystemPrompt()` 的 `skillsBlock` 仍必须逐字符等于 `buildSkillsPrompt()` 返回值，46 D-02）；只改第一段（静态前缀的一次性缓存代价）；措辞不得声称技能是工具、也不得写成「技能可自动注入」。
    - `getConversationMessages` 的 assistant 行 `toolExecutions` 重建**不在本任务**（归 48-03）。

    **② `ipc-handlers.js` 新增两个通道**（同款三件套）：
    ```
    ipcMain.handle('ai:get-skills', async (event) => { assertTrustedSender(event); if (!aiManager) throw new Error('AI Manager 未初始化'); return aiManager.getSkillsForUI(); });
    ipcMain.handle('ai:refresh-skills', async (event) => { assertTrustedSender(event); if (!aiManager) throw new Error('AI Manager 未初始化'); return aiManager.refreshSkillsForPanel(); });
    ```
    两通道都是**无载荷** invoke（D-17 的 stale-while-revalidate 要求「立即渲染」与「后台刷新」是两次独立调用，合成一次会让首次渲染等异步重扫）。均**不得**走 `/api/skills/*`（主窗口 `file://` 不能 fetch 本地 HTTP —— Phase 38 事故）。

    **③ `ipc-handlers.js` 扩展两个既有通道的返回体**：`:1688` / `:1713` 由 `const conversationId = await ...` 改为 `const res = await ...`，返回 `{ success: true, conversationId: res.conversationId, skillInvocation: res.skillInvocation, skillError: res.skillError }`。保持抛错语义不变（`!message` / `!data.message` 校验逐字不变）。

    **④ `src/preload.js`** 在 `ai: {` 命名空间（`:969` 起）下新增两个「JSDoc + 箭头 invoke」成对项（模板照 `:1059-1069`）：`getSkills: () => ipcRenderer.invoke('ai:get-skills')` 与 `refreshSkills: () => ipcRenderer.invoke('ai:refresh-skills')`。`skills:changed` **无需**新增 preload 面 —— `onIpcMessage`（`:718-726`）是通用监听通道，`windowManager.broadcast('skills:changed')` 直接经它到达 renderer。

    **⑤ 测试（扩展 `tests/test-ai-skills.js`，D 组续）**：
    - tier 三档：`source==='user'` → `user`；`name ∈ seededNames` → `builtin`；非 seeded managed → `managed`。seeded 集合**由测试注入数组**，不依赖真实 `skills-builtin/`。
    - 投影收窄：`getSkillsForUI(seededNames)` 的每个条目**不含** `content` / `filePath` / `diagnostics`（`assert.ok(!('content' in e))` 三连）。
    - `promptOmitted`：构造 3 个超预算技能 + 1 个 `disableModelInvocation`，断言后者 `promptOmitted !== true`、被丢弃的 `eligible` 条目为 true、`shadowed` / `disabled` / `overLimit` 条目均不为 true。
    - 重载装饰：见 Task 1（若 Task 1 已覆盖，本任务补「tier 由 `matchSkillByPath(location)` 还原、找不到时省略 tier 且气泡照常渲染」一条）。
    - 源码扫描（E 组）：`REALM_SYSTEM_PROMPT` 含「技能」且含「工具」；`buildSystemPrompt()` 的技能段 `=== getAiSkillsManagerLazy().buildSkillsPrompt()` 的返回值（既有 `:1184` 断言继续绿）；`ai-manager.js` 内存在 `await this.syncAgentSystemPrompt()` 的生产调用方（出现在 `refreshSkillsForPanel` 方法体内），且 `ipc-handlers.js` 内 `ai:refresh-skills` 注册处引用该方法名。
    - 源码扫描：`ipc-handlers.js` 内 `ai:get-skills` 与 `ai:refresh-skills` 两个 handle 的下一行均为 `assertTrustedSender(event)`。
  </action>
  <verify>
    <automated>node --test tests/test-ai-skills.js</automated>
    <fails_when>非零退出，或 "# fail" 行 > 0，或 "# pass" 行 < 64</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('ipc-handlers.js','utf8'); for (const ch of ['ai:get-skills','ai:refresh-skills']) { const i=s.indexOf(\"'\"+ch+\"'\"); if (i<0) throw new Error('missing channel '+ch); const seg=s.slice(i, i+240); if (!seg.includes('assertTrustedSender(event)')) throw new Error('no assertTrustedSender for '+ch); } console.log('channels-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "channels-ok"</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('src/preload.js','utf8'); for (const m of ['getSkills: () => ipcRenderer.invoke('ai:get-skills')','refreshSkills: () => ipcRenderer.invoke('ai:refresh-skills')']) { if (!s.includes(m)) throw new Error('missing preload method: '+m); } console.log('preload-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "preload-ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/test-ai-skills.js` 输出 `# fail 0`，且 `P8 失效链机制断言` 组（既有 `:1278-1296` 五条方法体源码扫描）全部继续为 `# pass`。
    - 断言 `getSkillsForUI()` 返回的条目键集合**不含** `content` / `filePath` / `diagnostics`（三条 `assert.ok(!(k in entry))`）。
    - 断言 `promptOmitted` 在 `disableModelInvocation === true` 的条目上恒为 `false`/`undefined`（`!== true`）。
    - 断言 `REALM_SYSTEM_PROMPT` 文本含子串「技能」与「工具」。
    - 断言 `buildSystemPrompt()` 输出中技能段与 `require('../ai-skills-manager').buildSkillsPrompt()` 返回值逐字符相等。
    - 源码断言 `ipc-handlers.js` 的 `ai:get-skills` / `ai:refresh-skills` 两个 handle 内均有 `assertTrustedSender(event)`、且均在 `aiManager` 判空后返回。
    - 源码断言 `src/preload.js` 含 `getSkills: () => ipcRenderer.invoke('ai:get-skills')` 与 `refreshSkills: () => ipcRenderer.invoke('ai:refresh-skills')`。
    - 断言 `src/renderer.js` **不**出现 `fetch('/api/skills`（Phase 38 事故护栏，本任务为后续 renderer 接线立护栏）。
  </acceptance_criteria>
  <done>面板数据源与刷新入口就绪：`realmAPI.ai.getSkills()` 同步返回收窄投影（零正文）、`realmAPI.ai.refreshSkills()` 经 `syncAgentSystemPrompt()` 完成「重扫 → 回写 → 广播」；`promptOmitted` 按 D-12 精准打标且不与「仅显式」混淆；`REALM_SYSTEM_PROMPT` 第 1 段含 D-18 措辞而技能段逐字符未变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: renderer 调用路径接线 —— 预检 / 气泡 / 结果接线 / 错误回退 / 快照同步</name>
  <files>src/renderer.js, src/styles/main.css, tests/test-skill-picker-model.js, tests/test-ai-skills.js</files>
  <read_first>
    - `src/renderer.js`（被改造文件）：`:279-340`（`state` 的 slashPicker 三字段 + `SLASH_COMMANDS` 注册表 —— **零改动**）、`:8029-8130`（`renderAIMessages` 的 user 分支：`referencedTabs` pill 行 + 正文 + 附件）、`:7965-7997`（`/compact` 的 `.ai-summary-box` 折叠框渲染 —— **技能正文折叠块的逐值照抄对象**）、`:8663-8900`（`handleSendAIMessage` 全段：斜杠拦截 / 消息对象构造 / IPC 分流 / `result` 消费）、`:8794-8813`（`pushSystemNote` / `abortAIIfStreaming`）、`:9070-9100`（`case 'error'` 里「占位气泡无内容则 splice 掉」先例）、`:4206-4212`（广播监听唯一既有形状）、`:4380-4395`（`handleAIStream()` 挂载点）
    - `src/styles/main.css`：`:7-43`（两个主题块 —— 新增令牌必须两处都写）、`:5713-5725`（`.ai-system-note`）、`:5732-5785`（`.ai-summary-box` 家族）、`:6921-6953`（`.ai-message-ref-pill` 家族）、`:8404-8421`（`.whitelist-tag` 徽标先例）、`:8441`（`color-mix` 既有用法）
    - `48-UI-SPEC.md` §`用户气泡契约`（技能 pill + 折叠块的结构与逐值契约）、§`刷新契约`（自激回路防护）、§Color（令牌取值与对比度）、§Spacing Scale Exceptions B 表（垂直 `1px` 例外）
    - `48-RESEARCH.md` §2.2（renderer 预检的必要性：流式事件早于 IPC 响应）、§2.5（返回契约与失败协议 + 占位气泡回滚先例）、§2.6（气泡与折叠块）、§1.7（广播 payload 保持无参 + 只重拉快照）
    - `48-PATTERNS.md` §`src/renderer.js` ⑤⑥⑦⑫
    - `tests/test-skill-picker-model.js`（Task 1 新建 —— 本任务在其上补 C 组）
  </read_first>
  <behavior>
    - 输入 `/skill:foo`：`state.aiSkills` 无 `foo` → `pushSystemNote('未找到技能「foo」，输入 / 查看可用技能')`，**零** user 气泡、**零** assistant 占位、不调用任何 IPC。
    - 输入 `/skill:foo` 且 `foo` 在 `state.aiSkills` 中 `disabled === true` → `pushSystemNote('技能「foo」已被禁用，可在 设置 → AI → 技能管理 重新启用')`，同样零气泡零 IPC。
    - 输入 `/clear`（本地命令）且同时存在名为 `clear` 的技能 → 走本地 `SLASH_COMMANDS` handler（本地命令优先，逐字节不变）。
    - 命中技能：user 消息对象带 `skillInvocation` 元数据（响应回传后回填），气泡渲染出「技能」微标 pill + args 正文，附件之后有默认折叠的「技能正文（N 字符）」块。
    - 主进程回传 `skillError` → 移除刚推送的 user 气泡与 assistant 占位、复位 `state.aiStreaming` / `state.aiCurrentMessageId` / 发送按钮、`pushSystemNote(skillError.message)`。
    - `skills:changed` 广播 → 仅重拉 `realmAPI.ai.getSkills()`，`digest` 相同则直接 return；**绝不**调用 `refreshSkills()`。
  </behavior>
  <action>
    **① `src/renderer.js` 状态新增**：`state.aiSkills`（主进程投影缓存，**含已禁用条目**，供区分「未找到」与「已禁用」）、`state.aiSkillsDigest`。`SLASH_COMMANDS` **零改动**（不得把技能 push 进来）。

    **② `handleSendAIMessage()` 的斜杠分支（`:8673-8691`）插入技能预检**，位置在「本地命令命中」之后、「未知命令 system-note」之前，且**必须保持在 `if (state.aiStreaming) return;`（`:8691`）之前**（D-08 的位置约束；否则流式中调用技能会被静默丢弃）：
    1. `SLASH_COMMANDS` 严格前缀 + 空白边界命中 → 既有 handler（**逐字节不变**）
    2. `window.SkillPickerModel.parseSkillRef(text, SLASH_COMMANDS.map(c => c.name))` 命中 `kind === 'skill'` → 在 `state.aiSkills` 按 name 查（含 disabled 条目）：未找到 → `pushSystemNote('未找到技能「' + name + '」，输入 / 查看可用技能')` + `return`；`disabled === true` → `pushSystemNote('技能「' + name + '」已被禁用，可在 设置 → AI → 技能管理 重新启用')` + `return`；`shadowed === true` **不可达**（name 唯一性，46 D-08 —— 查找必然取到胜出者），**不得**为此加错误分支；否则继续落入正常发送流程（**不 return**）
    3. 两边都不命中 → 既有未知命令文案，`/ 查看可用技能与命令`
    技能命中时：`await abortAIIfStreaming()`（复用，零新增中止逻辑，与 `/clear` / `/compact` 同款）；把技能元数据候选记到局部变量；`text` 保持**完整语法文本**原样进入 `state.aiMessages` 与 IPC（D-19 —— **不得**在 renderer 拼增强文本、不得把技能正文塞进 `message`）。

    **③ 用户消息对象（`:8713-8731`）挂 `skillInvocation`**：形状 `{name, tier, content}`，由 IPC 响应回传后回填（发给主进程的方向**不带**该字段）。**不改表结构**（D-16）。

    **④ 结果消费（`:8749-8790`）**：`result = await window.realmAPI.ai.prompt(text)` / `promptWithContext({...})` 的方向与分流条件**逐字不变**（技能调用沿用既有分流：有 `@` 引用或附件走 `promptWithContext`，否则走 `prompt`）。新增：`result.skillInvocation` 存在 → 回填 ③；`result.skillError` 存在 → 移除 agent 消息列表里刚 push 的 user 与 assistant 占位（照 `:9088-9099` 的 splice 先例，**不用** `showAIError`）、复位 `state.aiStreaming` / `state.aiCurrentMessageId` / `updateSendButtonState(false)`、`pushSystemNote(result.skillError.message)`（文案统一由 main 给，renderer 只呈现）。

    **⑤ 气泡渲染（`renderAIMessages` 的 `isUser` 分支，`:8029` 起）**：
    - 在 `referencedTabs` pill 行**之后**、正文之前插入 `.ai-message-refs` 容器 + `.ai-message-ref-pill.ai-skill-pill`（`msg.skillInvocation` 存在时）。pill 内 `.ai-skill-pill-badge` 文本「技能」、`.ai-message-ref-title` 为技能名（等宽字体）。**一律用 DOM API + `textContent`，不得 `innerHTML`**（`renderToolCard` 的 T-21-03 缓解同款）。微标 `title` 与来源徽标 `title` 同源文案。
    - 附件之后追加 `.ai-skill-content-box`（默认带 `collapsed` 类）：header 文本 `技能正文（N 字符）`，N = `msg.skillInvocation.content.length`（JS `String.length` 口径）；chevron 折叠时 `rotate(-90deg)`；header 点击 `classList.toggle('collapsed')`；body `textContent = msg.skillInvocation.content`（**不含** `<skill>` 外层标签 / 不含 provenance 行 / 不含 args）。展开状态**不持久化**。逐值照抄 `.ai-summary-box` 家族（`:7965-7997`）。

    **⑥ `skills:changed` 监听 + 启动预热**：在 `:4389` 的 `handleAIStream();` 同处新增 `window.realmAPI.onIpcMessage('skills:changed', ...)`；处理器内 `if (!state.slashPickerOpen) return;` 后**只**调 `realmAPI.ai.getSkills()`，`digest === state.aiSkillsDigest` 时直接 return，否则原地 `renderSlashPickerList()`。**绝不**调 `realmAPI.ai.refreshSkills()`（自激回路：广播 → 刷新 → 再广播）。启动时（同一挂载点）fire-and-forget 调一次 `realmAPI.ai.getSkills()` 预热 `state.aiSkills`，否则从未开过面板的用户手打 `/skill:` 会被误判为「未找到」。

    **⑦ `src/styles/main.css`**：
    - 新增令牌 `--skill-source-user` / `--skill-source-builtin` / `--skill-source-managed` / `--skill-limit-text`，**两个主题块（`:7-23` 与 `:25-42`）各写一份**，取值照 `48-UI-SPEC.md` §Color 的表。
    - 新增 `.ai-skill-pill-badge`（`display:inline-flex; align-items:center; flex-shrink:0; padding:1px 4px; margin-right:4px; border-radius:3px; font-size:11px; font-weight:400; line-height:1.4; color:inherit; background:rgba(0,0,0,0.25)` —— 底必须用 `rgba(0,0,0,0.25)`（与所属 pill 同底），**不得**用 `var(--bg-hover)`）。
    - 新增 `.ai-skill-content-box` / `-header` / `-chevron` / `-body`，逐值照抄 `.ai-summary-box` 家族（`:5732-5785`），**仅两处覆盖**：`max-width: 100%`、`margin: 4px 0 0`。`gap: 6px` / `padding: 6px 12px` 为照抄值（已登记于 `48-UI-SPEC.md` Spacing Exceptions B 表第 2 条）。
    - 新增值一律取 4 的倍数；唯一例外是 `.ai-skill-pill-badge` 的垂直 `padding: 1px`（Spacing Exceptions B 表第 1 条）。

    **⑧ 测试**：
    - `tests/test-skill-picker-model.js` 补 C 组 renderer 半边：`parseSkillRef` 在本地命令同名时返回 `kind:'command'`；状态分支选择（未找到 → 未找到文案；`disabled` → 禁用文案；`shadowed` 条目在按 name 查找时被跳过，**不产生**「已遮蔽」错误分支）；**`disableModelInvocation === true` 的条目不被预检拒绝**（DISC-07 clause ③ 的手打入口面 —— 源码断言预检分支体含 `disabled` 判定且**不含** `disableModelInvocation`，即该 flag 不参与拒绝条件）。
    - `tests/test-ai-skills.js` 补源码扫描：`src/renderer.js` 的 `SLASH_COMMANDS` 数组字面量仍是 `clear` / `compact` 两条（`assert.strictEqual` 计数）；`src/renderer.js` 不出现 `fetch('/api/skills`；`src/renderer.js` / `src/preload.js` / `ipc-handlers.js` 内**零**出现限额字面量（`64 * 1024` / `65536` / `MAX_USER_SKILLS = 50` / `8000`）；`renderer.js` 的 `onIpcMessage('skills:changed'` 处理器体内**不**出现 `refreshSkills`。
  </action>
  <verify>
    <automated>node --test tests/test-ai-skills.js tests/test-skill-picker-model.js</automated>
    <fails_when>非零退出，或输出中出现 "# fail" 行且其值 > 0，或 "# pass" 行 < 64</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('src/renderer.js','utf8'); const i=s.indexOf(\"onIpcMessage('skills:changed'\"); if(i<0) throw new Error('no skills:changed listener'); const seg=s.slice(i, i+400); if(/refreshSkills/.test(seg)) throw new Error('self-exciting loop risk'); if(!/if \(!state\.slashPickerOpen\)/.test(seg)) throw new Error('listener must early-return when panel closed'); console.log('listener-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "listener-ok"</fails_when>
    <automated>node -e "const css=require('fs').readFileSync('src/styles/main.css','utf8'); for (const t of ['--skill-source-user','--skill-source-builtin','--skill-source-managed','--skill-limit-text']) { const n=(css.match(new RegExp(t.replace(/[-]/g,'\\\\-'),'g'))||[]).length; if (n<2) throw new Error(t+' must be defined in BOTH theme blocks, found '+n); } for (const c of ['.ai-skill-pill-badge','.ai-skill-content-box-header','.ai-skill-content-box-body']) { if(!css.includes(c)) throw new Error('missing class '+c); } console.log('css-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "css-ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/test-ai-skills.js tests/test-skill-picker-model.js` 输出 `# fail 0` 且 `# pass` ≥ 64。
    - 断言 `src/renderer.js` 的 `SLASH_COMMANDS` 数组字面量项数为 2，且为 `clear` / `compact`。
    - 断言 `src/renderer.js` 内 `fetch('/api/skills` 命中数为 0；`http://localhost` 在技能相关路径命中数为 0。
    - 断言 `src/renderer.js` / `src/preload.js` / `ipc-handlers.js` 三文件内限额字面量（`64 * 1024`、`65536`、`8000`、`MAX_USER_SKILLS` 数值 `50`）命中数为 0。
    - 断言 `onIpcMessage('skills:changed'` 的处理器体内存在 `if (!state.slashPickerOpen)` 早退，且体内**不出现** `refreshSkills`。
    - 断言 `src/styles/main.css` 中四个新令牌各出现 ≥ 2 次（两个主题块），且 `.ai-skill-pill-badge` / `.ai-skill-content-box-header` / `.ai-skill-content-box-body` 三类均存在。
    - 断言技能正文折叠块的 N 口径为 `String.length`（源码含 `msg.skillInvocation.content.length` 且不含 `Buffer.byteLength` / `TextEncoder`）。
    - 断言 renderer 预检分支的拒绝条件只有 `disabled === true` —— **不**因 `disableModelInvocation === true` 拒绝（DISC-07 clause ③ 的手打入口面；与面板侧的可选中性断言成对）。
    - 断言气泡 pill 与折叠块渲染路径不出现 `innerHTML`（源码扫描 `renderAIMessages` 的 `isUser` 技能分支）。
  </acceptance_criteria>
  <done>renderer 侧调用路径闭合：`/skill:name` 命中技能时气泡显示「技能」微标 + args 正文 + 可展开的技能正文折叠块；未找到 / 已禁用走两条不同 system-note 且零气泡；主进程 `skillError` 能正确回滚；`skills:changed` 只重拉快照不自激；新增令牌在两个主题块齐备。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main（IPC） | 用户手打文本、`attachmentIds`、技能调用语法文本从 renderer 进入主进程；两个新通道均为无载荷读取，写入面为零 |
| 磁盘 → 主进程 | `SKILL.md`（技能名 / description / 正文）可由用户手改或经 AI `write`/`bash` 改写，属不可信输入 |
| 主进程 → Agent（SDK） | 组装后的 `<skill>` 块进入 LLM 输入；args 是不可信原文 |
| 主进程 → renderer | 技能名 / description 经投影与事件进入 `innerHTML` 模板与 DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | `ai-skills-manager.readSkillForInvocation` / `parseSkillInvocationText`（`/skill:name` 的 name） | high | mitigate | name 只作 `_cache.skills` 的**查找键**，读盘路径由 `path.dirname(entry.skill.filePath)` 从缓存派生，全函数**零**把用户输入拼进路径；`parseSkillInvocationText` 对 `/skill:` 形态强制 `^[a-z0-9-]+$`；`fresh.name !== name` → `not_found`（防目录被换后冒名注入） |
| T-48-02 | Elevation of Privilege | 全阶段写面 | high | mitigate | 本阶段**零新增技能写路径**：不提供任何创建 / 修改 / 删除技能文件的能力，`refreshSkills()` 与新增的面板刷新入口全部只读；写路径归 Phase 49/50/51（已列入 `must_haves.prohibitions` 首条） |
| T-48-03 | Tampering | `src/renderer.js` 的 `renderSlashPickerList`（`innerHTML` 模板）、气泡 pill、折叠块 | high | mitigate | 面板模板内的 `name` / `description` / `statusText` / `title` 一律经 `escapeHtml()`（`:10834`）；`tier` → class 映射走白名单查表（`{user,builtin,managed}`），不把 tier 值直接拼进 class；气泡与折叠块走 DOM API + `textContent`（`renderToolCard` 的 T-21-03 缓解同款） |
| T-48-04 | Spoofing | `REALM_SYSTEM_PROMPT` / 增强消息组装 | medium | mitigate | `REALM_SYSTEM_PROMPT` 保持**静态前缀**（不含任何运行期数据）；技能正文只经 SDK `formatSkillInvocation` 包裹注入；provenance 行是 Realm 生成的固定模板；args 只追加在 `additionalInstructions` 位置，不拼进 system prompt；`_ensureConversation` 只用于标题派生（前 30 字符，不入 prompt） |
| T-48-05 | Information Disclosure | `ai:get-skills` / `ai:refresh-skills` / 返回契约 | medium | mitigate | 两通道 `assertTrustedSender(event)` + `aiManager` 判空；投影**剔除** `content` / `filePath` / `diagnostics`（≤50 × 64 KiB 正文不上 IPC）；`skillInvocation.content` 只随**当次调用响应**回传给发起方 renderer，不进入广播 |
| T-48-06 | Denial of Service | `refreshSkillsForPanel()` → `syncAgentSystemPrompt()` | low | mitigate | 后台刷新只在面板打开时触发一次；`skills:changed` 的处理器**只重拉快照**（`digest` 相同即 return），不触发重扫 —— 消除「广播 → 刷新 → 再广播」自激回路（P-48-06） |
| T-48-SC | Tampering | npm / pip / cargo 安装 | high | accept | 本阶段**零新增运行时依赖**：`src/skill-picker-model.js` 为手写纯函数模块，不引入任何包；无安装任务，故无包合法性门禁触发条件 |

> **裁定说明**：T-48-01 / T-48-03 为 `high`，均给出可落地缓解且落在本计划的 `<acceptance_criteria>` 内（零路径拼接、`escapeHtml` + 白名单 class 映射）；
> T-48-02 的缓解形态是「不提供能力」而非「过滤输入」，由 `must_haves.prohibitions` 承担。
> 本计划的 `<action>` 内**不书写**任何被 acceptance criteria 负向 grep 的字面量。
</threat_model>

<verification>
1. `node --test tests/test-ai-skills.js tests/test-skill-picker-model.js` → `# fail 0` 且 `# pass` ≥ 64（地板）。
2. 三处源码护栏命令（IPC 通道 `assertTrustedSender`、preload 成对方法、`skills:changed` 无自激）。
3. CSS 令牌双主题块存在性 + 新增类存在性。
4. 端到端行为：临时 workspace 写入技能 → `readSkillForInvocation` 读到正文 → 改盘 → 二次调用读到新正文（实时读盘硬约束）；`content` 置空 → 组装被拒且注入串无 `undefined`。
5. 重载一致性：`getConversationMessages` 还原对象键集合与 live 路径消息对象键集合逐字相等，且 user 行 `content` 逐字符等于 args（provenance 行不留在正文里）。
6. 脚本接线：`src/skill-picker-model.js` 在 `src/index.html` 中先于 `renderer.js` 加载（否则本波形的技能预检在运行时读到 `undefined`）。
</verification>

<success_criteria>
- `/skill:name [args]` 与裸 `/name [args]` 两种语法在主进程被一致解析（七组 args 表全对，本地命令优先）。
- 技能正文**在调用瞬间**从磁盘读取；空正文 / 目录被换 / 文件被删一律按不存在处理，不产出字面量 `undefined`。
- 技能调用进入对话历史并触发 LLM；对话标题不退化；重开对话气泡显示 pill + 折叠块而非原始 XML，且正文为 args（不含 `/skill:name` 前缀与 provenance 行）。
- `ai:get-skills` / `ai:refresh-skills` 两通道经 `assertTrustedSender` 可用，投影零正文。
- `promptOmitted` 与「仅显式」严格区分；三档 `tier` 由主进程唯一计算。
- `disableModelInvocation` 的技能不进 system prompt、仍可经 `/skill:` 显式调用，且与 `disabled` 互不蕴含。
</success_criteria>

<artifacts_this_phase_produces>
> **Artifacts this phase produces** —— 本计划新建的符号（供 plan-review-convergence 的 source-grounding pass 排除）。

**新文件**
- `src/skill-picker-model.js`（双模式导出模块）
- `tests/test-skill-picker-model.js`（纯逻辑断言宿主）

**`src/index.html` 新加载项**
- `<script src="skill-picker-model.js"></script>`（在 `renderer.js` 之前 —— 本计划内闭环，不留到下个计划）

**新函数 / 方法**
- `src/skill-picker-model.js`：`extractArgs` / `parseSkillRef` / `parseSkillInvocationText`
- `src/skill-picker-model.js` 新常量：`SKILL_PREFIX`
- `src/skill-picker-model.js` 新全局：`window.SkillPickerModel`
- `ai-skills-manager.js`：`readSkillForInvocation` / `sourceTierOf` / `toUISkillEntry` / `matchSkillByPath` / `getSkillsForUI`
- `ai-manager.js`：`getBuiltinSkillsSeederLazy` / `getSeededSkillNamesSafe` / `buildSkillInvocationBlock` / `parseSkillInvocationText`（模块级导出）
- `ai-manager.js` 类方法：`getSkillsForUI` / `refreshSkillsForPanel` / `_resolveSkillInvocation`
- `ai-skills-manager.js` 新条目字段：`promptOmitted`（缓存条目级，非 `Skill` 本体）

**新 IPC 通道**
- `ai:get-skills`（invoke，无载荷）
- `ai:refresh-skills`（invoke，无载荷）

**新 realmAPI 方法**
- `realmAPI.ai.getSkills()`
- `realmAPI.ai.refreshSkills()`

**新返回契约字段**
- `ai:prompt` / `ai:prompt-with-context` 响应新增 `skillInvocation` 与 `skillError`
- renderer 消息对象新增 `skillInvocation: {name, tier, content}` 元数据字段

**新 CSS 类 / 自定义属性**
- `.ai-skill-pill`（修饰类，与 `.ai-message-ref-pill` 并用）
- `.ai-skill-pill-badge`
- `.ai-skill-content-box` / `.ai-skill-content-box-header` / `.ai-skill-content-box-chevron` / `.ai-skill-content-box-body`
- `--skill-source-user` / `--skill-source-builtin` / `--skill-source-managed` / `--skill-limit-text`

**新 renderer 状态字段**
- `state.aiSkills` / `state.aiSkillsDigest`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/48-skill-name/48-01-SUMMARY.md` when done
</output>
