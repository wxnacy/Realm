# API Coverage — Phase 48 (`/skill:name`)

No external API integration: Phase 48 is fully local — renderer skill picker plus main-process skill catalog. Detector hits are lexical (internal `api` export object, `/api/skills` negative guards).
