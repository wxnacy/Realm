---
phase: 48-skill-name
plan: 03
type: execute
wave: 3
depends_on: [48-01, 48-02]
files_modified:
  - ai-manager.js
  - src/renderer.js
  - src/index.html
  - src/styles/main.css
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
  - AGENTS.md
autonomous: true
requirements: [DISC-05]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "模型经 `read` 打开技能目录下的 `SKILL.md` 时，`tool_execution_start` 广播的事件带 `skill_invocation = {name, tier}`；非 `read` 工具、参数缺 `path`、`path` 非字符串、basename 非 `SKILL.md`、路径落在技能集之外 → 一律为 `null`（D-15）"
    - "判定**只在工具事件生成侧**完成（`ai-manager.js`），renderer **不按路径字符串自行匹配** `skills/` / `managed-skills/`（那会成为第二份判定实现）"
    - "renderer 的流式映射把事件字段 `skill_invocation` 落到 `toolExecution.skillInvocation`；标记缺失 / 非法 → 渲染**普通 `read` 卡片**（零回归）"
    - "`renderToolCard` 技能变体的标题文案为 `使用技能「{name}」`，附与面板同款的来源徽标（`.slash-picker-source-badge` + `TIER_BADGE` 查表）；状态图标 / 状态文字 / 参数区 / 结果区 / 折叠交互**全部不变**"
    - "技能变体的技能名与徽标一律走 DOM API + `textContent`；tier → class / label / title 一律经 `TIER_BADGE` 白名单查表，不把 tier 值拼进 class，也不为该变体退回 `innerHTML`"
    - "重开对话后 assistant 行的 `toolExecutions` 由**同一个**判定实现重建 `skillInvocation`（`getConversationMessages` 内、同一 `_resolveSkillMarker`）—— 实时链路与重载链路判据一致；技能已被删除 / 改名 → 静默不标（与实时链路「匹配不到就是普通卡片」一致）"
    - "`read` 事件的 `path` 是模型给的原文；相对路径按 `sandboxEnv.cwd` 做**纯词法** `path.resolve` 归一化（与 SDK `absolutePath` 同规则）；**不**额外展开 `~` / `file://`（那类路径解析后落在工作区外 → 不命中 → 普通卡片，与沙箱拒绝行为一致）"
    - "技能化**不额外插 system-note**（D-15）、**不新增卡片形状**、不新增图标、不改 `.tool-card` 系列既有规则"
    - "[UI] 技能变体标题超长由 `.tool-card-name-text` 截断（`overflow:hidden` + `text-overflow:ellipsis` + `white-space:nowrap` + `min-width:0`），徽标 `flex-shrink: 0` 不受影响；卡片 header 仍是既有固定高度，标题截断不改变行高"
    - "[UI] 技能变体的 loading / error 沿用既有状态图标与 `正在执行...` / `完成` / `失败` 文字；技能变体不动状态区"
    - "[UI] `.tool-card-name-skill` 的 `gap: 8px` 与父容器 `.tool-card-header` 存量值同值（不得改用 6px）"
    - "[assumption] 极端 Unicode 文件名（NFD / 窄空格 / 弯引号变体）下标记可能不命中 —— 后果**仅**是「显示为普通 read 卡片」，技能正文仍被正常读取；这是可接受的降级，必须如实写入产品文档"
    - "[assumption] 伪造 `path` 让普通 `read` 被标成「使用技能「x」」属**展示层误标**：不改任何数据、不放宽任何权限（技能不构成额外权限），故接受为低风险；仍以「与缓存 `filePath` 规范化全等 + basename 必须为 `SKILL.md`」双条件约束误标面"
    - "产品文档补全：`docs/product/ai-skills.md` 新增「发现与调用」章节，覆盖面板形态 / 两种语法与本地命令优先 / **实时读盘口径** / 边界技能行为表 / 三档来源徽标与 seeded 判定来源 / `read` 卡片技能化 / 诚实边界；§六 已知限制补「技能文件改动后需重开对话才反映到已注入的历史消息」；§七 测试与验证补本阶段新增测试命令"
  artifacts:
    - ai-manager.js
    - src/renderer.js
    - src/styles/main.css
    - tests/test-ai-skills.js
    - docs/product/ai-skills.md
    - AGENTS.md
  key_links:
    - "`ai-manager._setupEventBroadcasting` 的 `tool_execution_start` → `skill_invocation` → renderer `handleAIStream` 的 `tool_execution_update` 分支 → `toolExecution.skillInvocation` → `renderToolCard`"
    - "`ai-manager._resolveSkillMarker(toolName, args)` → `ai-skills-manager.matchSkillByPath(abs, seededNames)`（48-01）→ `_cache.skills` 的 `filePath` 规范化全等比较"
    - "`sandboxEnv.cwd` + `path.resolve` → 与 SDK `env.absolutePath(path)` 同规则的词法归一化"
    - "`ai-manager.getConversationMessages` → **同一个** `_resolveSkillMarker` → 重载链路的 `toolExecutions[i].skillInvocation`"
    - "`window.SkillPickerModel.TIER_BADGE`（48-02）→ `read` 卡片徽标的 label / className / title（与面板共用同一张表）"
  prohibitions:
    - statement: "`read` 卡片技能化的判定**只在工具事件生成侧**（`ai-manager.js`）完成；renderer 不得按路径字符串自行匹配技能文件名或技能目录名，也不得为此新增第二份判定实现"
      status: unresolved
      verification: judgment
      reason: "renderer 在 `file://` 主窗口、无 `sandboxEnv`，拿不到技能目录的权威路径；按字符串包含猜就是第二份判定实现（T-48-10 的根因）。fallback 召回项，无 wired check，按 flagged-unverified 处理"
    - statement: "技能变体的标题与徽标一律走 DOM API + `textContent`；tier → class / label / title 一律经 `TIER_BADGE` 白名单查表，表外或缺失 tier 时跳过徽标；不得为该变体退回 `innerHTML`，也不得把 tier 值拼进 class 字符串"
      status: unresolved
      verification: judgment
      reason: "T-48-10（high）：技能名来自磁盘并随工具事件到达 renderer；`renderToolCard` 的既有缓解是 `textContent`，为加徽标退回字符串拼接即打开注入面"
    - statement: "不得改动 `.tool-card` 系列既有规则、不新增卡片形状、不新增图标、不为技能化额外插 system-note；也不得改变重载路径 `params` / `status` / `result` / `name` 的存在性与取值"
      status: unresolved
      verification: judgment
      reason: "D-15（不额外插提示条，与卡片信息重复且自动匹配可能多次）；重载链路丢字段会直接破坏历史气泡的普通卡片路径，零回归是本阶段的硬边界"
    - statement: "技能域知识不得下沉到 `ai-conversations-manager.js` —— 存储层保持只懂存储，标记只在 `getConversationMessages` 的装饰层重建"
      status: unresolved
      verification: judgment
      reason: "把标记写进 `tool_calls` 列会污染 `getAgentMessages` 读同一列的 LLM 上下文重建路径"
    - statement: "徽标不得被实现或表述为「技能授予了额外权限」—— 不展示 `allowed-tools`，产品文档必须保留「技能不构成额外权限 / 该字段当前运行时不被强制、仅供参考」的免责口径"
      status: unresolved
      verification: judgment
      reason: "T-48-12：徽标只表达来源分类；`allowed-tools` 当前不被运行时强制（O3 未变），任何「按技能授权」的表述都是虚假安全感"
    - statement: "产品文档不得写入本阶段未实现的能力（技能正文自动注入、mid-prompt 嵌入 `/skill:` token、技能堆叠调用、`$ARGUMENTS` / 参数替换）"
      status: unresolved
      verification: judgment
      reason: "这些均属 `48-CONTEXT.md` 的 Deferred（ECO-03 / ECO-04 / P3 defer）；写入即造成产品说明与实际行为分叉"
---

<objective>
交付 DISC-05 的可见性面与全阶段文档收口：模型**仅凭 description 自动匹配**技能、经 `read` 打开其 `SKILL.md` 时，工具卡片标识为「使用技能「name」」并带来源徽标；重开对话后同一判据重建标记；并把「发现与调用」的完整行为写入 `docs/product/ai-skills.md`（补章节 + 已知限制 + 测试清单）。

Purpose: ROADMAP 判据 4 要求「用户不显式调用也能生效」且**用户看得见**。判定必须在工具事件生成侧 —— renderer 在 `file://` 主窗口、无 `sandboxEnv`，拿不到技能目录的权威路径，只能靠字符串包含猜（就是第二份判定实现）。本计划同时把 D-18 之外的整阶段产品文档补齐（AGENTS.md 的维护约定要求产品说明与实现同步）。

Output: `ai-manager.js` 的新方法 `_resolveSkillMarker` 与两处调用（实时事件 + 重载装饰）、`src/renderer.js` 的事件映射与卡片技能变体、`src/styles/main.css` 的两个新类、`src/index.html` 的 `styles/main.css?v=` 序号推进、`tests/test-ai-skills.js` 的新断言组、`docs/product/ai-skills.md` 与 `AGENTS.md` 的文档同步。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-RESEARCH.md
@.planning/phases/48-skill-name/48-PATTERNS.md
@.planning/phases/48-skill-name/48-UI-SPEC.md
@.planning/phases/48-skill-name/48-01-PLAN.md
@.planning/phases/48-skill-name/48-02-PLAN.md
@ai-manager.js
@ai-skills-manager.js
@src/renderer.js
@src/styles/main.css
@tests/test-ai-skills.js
@docs/product/ai-skills.md
@AGENTS.md
</context>

<precondition>
48-01 与 48-02 已交付且本计划**直接消费、不重建**：`ai-skills-manager.matchSkillByPath(absPath, seededNames)`（48-01 的纯函数）、`ai-manager.getSeededSkillNamesSafe()`（48-01 的 try/catch 降级包装）、`getConversationMessages` 的 user 行 `<skill>` 装饰已**抽成 48-01 的私有方法** `_decorateSkillUserMessage`（配模块级纯函数 `parseStoredSkillInvocation` / `resolveSkillBubbleArgs`；`getConversationMessages` 方法体内因此**不出现**直接 `matchSkillByPath(`，本计划 Task 2 的护栏才可满足）、`window.SkillPickerModel.TIER_BADGE` 与 `.slash-picker-source-badge` 样式（48-02）、`renderer.js` 的 `tool-card-name` 既有结构。若任一不存在（尤其是 user 行装饰仍是内联在 `getConversationMessages` 里的形态），先回到对应计划执行。
</precondition>

<interfaces>
> 本计划消费与产出的精确形状（执行时按此对齐，不自行改名）。

**主进程事件新增字段**（`tool_execution_update`，snake_case 与既有 `tool_execution_id` / `tool_name` 同款）：
```
skill_invocation: { name: string, tier: 'user' | 'builtin' | 'managed' } | null
```

**renderer 侧 `toolExecution` 新增字段**（camelCase，落进 `state.aiMessages[].toolExecutions[]`）：
```
skillInvocation: { name: string, tier: string } | undefined
```

**`ai-manager.js` 新增私方法**：
```
_resolveSkillMarker(toolName: string, args: object | undefined):
  { name: string, tier: string } | null
```
（研究 §3.1 给的骨架是模块级 `resolveSkillMarker`；此处**改为类方法**，因为它的两个依赖 `sandboxEnv` 与 seeded 名集合都是实例状态，且两处调用点都在类内 —— 这使「同一实现两处调用」天然成立。）

**消费的 `ai-skills-manager` 导出**（48-01）：`matchSkillByPath(absPath, seededNames?) → { name, tier } | null`
</interfaces>

<assumptions>
> 以下 4 条来自 `48-CONTEXT.md` 的裁量项、`48-RESEARCH.md` 的 open question 与 §3.2/§3.3，
> 按 spec-less fallback 降级为**显式 planner 裁决**（不静默丢弃）。

1. **DISC-05 / 判定落点** —— CONTEXT 的裁量项「主进程打标记 vs renderer 按路径匹配」。
   裁决：**主进程工具事件生成侧**（`ai-manager.js`）。三条独立成立的理由：① renderer 不掌握技能目录权威路径；
   ② 本处是同步上下文而匹配可同步完成（缓存 `filePath` 是内存数据）；③ `tool_execution_end` 不带 `params`，
   标记必须在 start 事件一次性给出。

2. **DISC-05 / 重载路径是否重建标记** —— 研究 §3.3 的 open question。
   裁决：**重建**（在 `getConversationMessages` 内用同一 `_resolveSkillMarker`）。
   理由：「同一份实现两处调用」比「两条路径行为不同」便宜得多，且零新增 UI 形状。

3. **DISC-05 / realpath 漂移的回退口径** —— 研究 §3.2 的待实测项。
   裁决：先按**纯词法** `path.resolve` 比较（与 SDK `env.absolutePath` 同规则，正常情况逐字符相等）；
   四类路径断言若实测出现漂移，回退方案是**两侧都过一遍** `path.resolve` 与 `fs.realpathSync`（存在时）双比较。
   **不得**退化为「按目录名包含猜测」。

4. **文档口径的两条诚实边界**（必须写进产品文档，不得省略）：
   ① 面板刷新依赖主进程的「重扫 + 回写 + 广播」链路，AI 未初始化时该链路早退 → 面板显示上一次投影；
   ② `read` 标记在极端 Unicode 文件名下可能不命中 → 仅表现为普通 `read` 卡片，技能正文仍被正常读取。
</assumptions>

<!-- planner-discipline-allow: 使用技能 / 未找到技能 / 已被禁用 -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 端到端「模型 read 技能正文 → 卡片显示使用技能」—— 事件标记 → 流式映射 → 卡片变体 → 样式</name>
  <files>ai-manager.js, src/renderer.js, src/index.html, src/styles/main.css, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js`（被改造文件）：`:96-121`（`getAgentWorkspaceLazy` / `getAiSkillsManagerLazy` 惰性 require 模板）、`:1427-1520`（`_setupEventBroadcasting` 全段，特别是 `:1461-1464` 的 `sendNow` 与 `:1474-1484` 的 `tool_execution_start`、`:1497-1510` 的 `tool_execution_end`（**不带 `params`**））、`:2461-2480`（`getConversationMessages` —— Task 2 的落点）、`:845-855`（`sandboxEnv` 赋值点）、`:5780-5795`（`module.exports` 面 —— 既有「挂到导出对象」写法）
    - `src/renderer.js`（被改造文件）：`:9014-9055`（`tool_execution_update` 分支：`existingIdx >= 0` 的 `...spread` 更新分支与 `push` 新对象分支）、`:9224-9310`（`renderToolCard` 全段：`tool-card-name` 名称分支 `:9243-9250`、参数区与结果区的 `textContent` 纪律 `:9274-9284`）、`:9480-9500`（`renderToolCards`）、`:10834-10838`（`escapeHtml`）
    - `src/styles/main.css`：`:5990-6059`（`.tool-card` / `-header` / `-name` / `-icon` / `-status` 现状）、`:6972-7019`（`.slash-picker-*` —— 48-02 新增了 `.slash-picker-source-badge`，本任务**复用**）、`:8404-8421`（`.whitelist-tag`）
    - `ai-skills-manager.js`：`:1053-1096` 区段对应 `48-PATTERNS.md` §⑧（`matchSkillByPath` 的规范化全等匹配形态 —— 48-01 已实现）、文件头 `:18-21`（依赖纪律）
    - `tests/test-ai-skills.js:24-73`（`withTempRoot` / `writeSkill` / `readSource` / `functionBody` 三件套）、`:1237-1296`（源码扫描断言的既有写法）、`:1218-1276`（`wm.broadcast` 覆写 + `t.after` 复位先例）
    - `48-RESEARCH.md` §3.1（落点与三条必要性 + `read` 参数名是 `path`）、§3.2（路径规范化与两个边界）、§3.4（D-18 措辞的三条硬边界 —— 48-01 已落，本任务只做回归断言）
    - `48-PATTERNS.md` §`ai-manager.js` ⑦⑩、§`src/renderer.js` ⑧⑨、§`src/styles/main.css` ⑥
    - `48-UI-SPEC.md` §`read` 工具卡片技能化（触发 / 视觉表 / 负向约束）、§`UI Considerations` 的 `read` 工具卡片四行
  </read_first>
  <behavior>
    - `_resolveSkillMarker('read', {path: '<env.cwd>/managed-skills/alpha/SKILL.md'})` 且 alpha 在缓存中 → `{name:'alpha', tier:'managed'}`（`tier` 按 48-01 的 injected seeded 集合判定）。
    - 相对路径形态 `{path: 'managed-skills/alpha/SKILL.md'}` → 同上（按 `env.cwd` 词法归一化后命中）。
    - `{path: '<env.cwd>/managed-skills/alpha/references/x.md'}` → `null`（basename 非 `SKILL.md`）。
    - `{path: '/tmp/outside/SKILL.md'}`（工作区外）→ `null`。
    - `_resolveSkillMarker('bash', {path: '…/SKILL.md'})` → `null`；`_resolveSkillMarker('read', {})` → `null`；`_resolveSkillMarker('read', undefined)` → `null`；`_resolveSkillMarker('read', {path: 123})` → `null`。
    - `env.cwd` 缺失（`sandboxEnv` 为 null）→ `null`（不得抛错）。
    - `_setupEventBroadcasting` 的 `tool_execution_start` 发出的 `tool_execution_update` 事件对象含 `skill_invocation` 键（普通 `read` 时为 `null`）。
    - renderer 收到 `skill_invocation` 后，新工具执行对象上出现 `skillInvocation`（camelCase）；后续 `completed` 更新经 `...spread` 保留该字段。
    - `renderToolCard({name:'read', params:{path:'…/SKILL.md'}, skillInvocation:{name:'alpha', tier:'builtin'}, status:'completed'})` 的名称文本为 `使用技能「alpha」`，且含一个 `.slash-picker-source-badge` 徽标。
    - `renderToolCard` 收到 `skillInvocation` 缺失 / 非法的对象 → 名称文本仍为 `read`（零回归）。
  </behavior>
  <action>
    **① `ai-manager.js` —— 新增私方法 `_resolveSkillMarker(toolName, args)`（同步、零 IO、零新增依赖）**

    位置放在 `_setupEventBroadcasting` 之前（与 `getAiSkillsManagerLazy` 的调用方同区段）。判定顺序严格如下，任一不满足即 `return null`（接口契约：**缺失/非法一律零回归**，渲染为普通 `read` 卡片）：
    1. `toolName !== 'read'` → null；`args` 为假值 → null；`typeof args.path !== 'string'` → null。
       **注意**：SDK `read` 工具的参数键是 `path`（不是 `filePath`）—— 不要按别的键取值，否则永远是 `undefined`。
    2. `const root = this.sandboxEnv && this.sandboxEnv.cwd;` 取不到 root → null（AI 未初始化时不得抛错）。
    3. 归一化：`path.isAbsolute(args.path) ? path.resolve(args.path) : path.resolve(root, args.path)`（**纯词法**，与 SDK 的 `env.absolutePath` 同规则；**不**额外展开 `~` / `file://` —— 那类路径解析后落在工作区外，标记为 null 是正确行为，不要引入与沙箱不同的第二套解析）。
    4. `path.basename(abs) !== 'SKILL.md'` → null（技能目录下的 `references/*.md` 不算「使用技能」）。
    5. 命中判定走 **48-01 的** `getAiSkillsManagerLazy().matchSkillByPath(abs, this.getSeededSkillNamesSafe())` —— 只与缓存 `filePath` 做规范化全等比较，**不按目录前缀猜、不重新扫盘**。命中返回 `{ name, tier }`，否则 null。
    JSDoc 必须写明三条：判定属工具事件生成侧（renderer 不掌握技能权威路径）；失败返回 `null` 是接口契约；极端 Unicode 文件名下可能不命中的降级口径。

    **② `ai-manager.js` —— `_setupEventBroadcasting` 的 `tool_execution_start`（`:1474-1484`）新增一个字段**

    在 `sendNow({...})` 的对象里、`params: event.args,` 之后加 `skill_invocation: this._resolveSkillMarker(event.toolName, event.args),`。
    三条既有事实支撑「只能在这里打标记」（写进代码注释）：本处是**同步**上下文而匹配可同步完成；`tool_execution_end` **不带 `params`**，标记错过 start 就没有第二次机会；renderer 的更新分支用 `...spread` 保留既有字段，标记在后续状态更新中自然留存。
    **不得**把标记改成异步、**不得**为此改动 `flushPending` / `sendNow` 的时序语义。

    **③ `src/renderer.js` —— 流式映射加一个字段（`:9041-9051` 的 `push` 分支）**

    新工具执行对象加 `skillInvocation: event.skill_invocation`（主进程侧是 snake_case，与既有 `tool_execution_id` / `tool_name` 同款）。`existingIdx >= 0` 的更新分支**零改动**（`...spread` 已保留既有字段）。

    **④ `src/renderer.js` —— `renderToolCard` 名称分支（`:9243-9250`）加技能变体**

    在既有 `execute_action` 分支**之前**加一条 `if (toolExecution.skillInvocation && toolExecution.skillInvocation.name)`：
    - 名称容器：保留 `.tool-card-name`（**零改动既有类**）+ 追加修饰类 `.tool-card-name-skill`。
    - 内层文本：新建 `.tool-card-name-text`，`textContent = \`使用技能「${name}」\``（**等宽字体**，字号字重沿用卡片存量 12px/500，不引入第 3 个字重）。
    - 来源徽标：新建 `.slash-picker-source-badge` + 48-02 的 `window.SkillPickerModel.TIER_BADGE[tier].className`；文案取 `TIER_BADGE[tier].label`；`title` 取 `TIER_BADGE[tier].title`。表外 / 缺失 tier → **跳过徽标**（不产出 `undefined` class），标题仍渲染。
    - **一律 DOM API + `textContent`**（沿用 `renderToolCard` 的 T-21-03 缓解），**不得**为「加徽标」退回 `innerHTML`；技能名与标签**不得**参与任何 class 字符串拼接。
    - 状态图标 / 状态文字 / 参数区 / 结果区 / header 点击折叠交互**全部零改动**；**不额外插 system-note**（D-15）、不新增卡片形状、不新增图标、不改 `.tool-card` 系列既有规则。

    **⑤ `src/styles/main.css` —— 两个新类（消费 48-02 已落的 `.slash-picker-source-badge` 与四个令牌）**

    - `.tool-card-name-skill`：`display: flex; align-items: center; gap: 8px; min-width: 0`（`gap: 8px` 与父容器 `.tool-card-header` 存量同值 —— 使「图标↔名称」与「名称↔徽标」两处间隙一致；**不得**改用 6px，那是折叠框 header 专用的 B 表例外值）。
    - `.tool-card-name-text`：`overflow: hidden; text-overflow: ellipsis; white-space: nowrap; min-width: 0`；等宽字体用既有 `var(--font-mono, monospace)`。
    - **不得**改 `.tool-card-name` / `.tool-card-header` / `.tool-card-icon` / `.tool-card-status` 的既有声明；**不得**新增令牌。
    - **缓存失效序号**：本任务又动 `src/styles/main.css`（两个新类），按既有约定必须把 `src/index.html` 里 `styles/main.css?v=` 的序号**再 +1**（只改序号不改其余）—— 本阶段最后一次 CSS 改动若不带序号，返回用户可能加载到不含这两个类的旧样式表，表现为技能卡片标题未截断、徽标无样式。`src/index.html` 因而在本计划的 `files_modified` 内。

    **⑥ 测试（扩展 `tests/test-ai-skills.js`，新增 `describe` 组，不改既有断言）**

    - 四类路径断言（用 `withTempRoot(t)` + `writeSkill` 建临时 workspace，`createSandboxEnv({cwd: root})`，`aiSkills.refreshSkills(env, {rootDirs:[...]})` 后经 `AIManager.prototype._resolveSkillMarker.call(ctx, 'read', {path})` 调用；`ctx` 只需 `sandboxEnv` 与 `getSeededSkillNamesSafe: () => [...]`）：绝对路径命中 / 相对路径命中 / `references/*.md` 不命中 / 工作区外不命中。
    - 负例组：非 `read` 工具、缺 `args`、`path` 非字符串、`sandboxEnv` 为 null —— 四条均返回 `null` 且不抛错。
    - `tier` 断言**注入 seeded 名数组**（不依赖真实 `skills-builtin/`），规避纯 Node 下 `getSeededSkillNames()` 的环境问题。
    - 源码扫描：`ai-manager.js` 的 `tool_execution_start` 分支体（用 `functionBody`/切片取 `case 'tool_execution_start'` 到 `break;`）内含 `skill_invocation` 且含 `_resolveSkillMarker(`。
    - 源码扫描：`src/renderer.js` 内出现 `skillInvocation: event.skill_invocation`（流式映射接线）。
    - 源码扫描：`renderToolCard` 函数体内含 `使用技能「`、`tool-card-name-skill`、`TIER_BADGE`，且**不含**按路径字符串自行匹配的痕迹（`managed-skills` / `SKILL.md` 字面量在该函数体内命中数为 0 —— 判定必须在主进程）。
    - 回归护栏：`REALM_SYSTEM_PROMPT` 文本含 `技能` 与 `工具`（D-18，由 48-01 落地，本处加跨计划回归断言）；`buildSystemPrompt()` 输出中的技能段与 `require('../ai-skills-manager').buildSkillsPrompt()` 返回值逐字符相等（46 D-02 边界）。
  </action>
  <verify>
    <automated>node --test tests/test-ai-skills.js</automated>
    <fails_when>非零退出，或输出中存在 "# fail" 行且其值 > 0，或 "# pass" 行 < 64（既有基线地板）</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('ai-manager.js','utf8'); const i=s.indexOf(\"case 'tool_execution_start'\"); if(i<0) throw new Error('missing tool_execution_start case'); const seg=s.slice(i, i+700); if(!seg.includes('skill_invocation:')) throw new Error('event must carry skill_invocation'); if(!seg.includes('_resolveSkillMarker(')) throw new Error('missing marker resolver call'); if(!/params:\s*event\.args/.test(seg)) throw new Error('params must stay unchanged'); console.log('event-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "event-ok"</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('src/renderer.js','utf8'); const i=s.indexOf('function renderToolCard(toolExecution)'); if(i<0) throw new Error('missing renderToolCard'); const body=s.slice(i, s.indexOf('\n}', i)); if(!body.includes('使用技能「')) throw new Error('missing skill card title copy'); if(!body.includes('tool-card-name-skill')) throw new Error('missing modifier class'); if(!body.includes('TIER_BADGE')) throw new Error('badge must use the shared whitelist table'); if(/managed-skills|SKILL\.md/.test(body)) throw new Error('renderer must not match skill paths itself'); if(!body.includes('textContent')) throw new Error('skill variant must use textContent'); console.log('card-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "card-ok"</fails_when>
    <automated>node -e "const css=require('fs').readFileSync('src/styles/main.css','utf8'); for (const c of ['.tool-card-name-skill','.tool-card-name-text']) { if(!css.includes(c)) throw new Error('missing class '+c); } const i=css.indexOf('.tool-card-name-skill'); const seg=css.slice(i, i+240); if(!/gap:\s*8px/.test(seg)) throw new Error('skill name gap must be 8px (match .tool-card-header)'); const t=css.indexOf('.tool-card-name-text'); const tseg=css.slice(t, t+300); for (const p of ['overflow: hidden','text-overflow: ellipsis','white-space: nowrap','min-width: 0']) { if(!tseg.includes(p)) throw new Error('name text missing '+p); } console.log('card-css-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "card-css-ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/test-ai-skills.js` 输出 `# fail 0`，且 `# pass` ≥ 64（既有基线地板不回归）。
    - 断言 `_resolveSkillMarker` 四类路径行为（绝对命中 / 相对命中 / 非 `SKILL.md` 不命中 / 工作区外不命中）与四条负例（非 `read` 工具 / 缺 `args` / `path` 非字符串 / `sandboxEnv` 为 null）逐条成立，且负例**不抛错**。
    - 断言 `_resolveSkillMarker` 内不出现任何把 `args.path` 拼进 `_cache` 查找之外用途的路径拼接（name 只作查找键，路径来自缓存条目）。
    - 源码断言 `tool_execution_start` 分支体含 `skill_invocation:` 与 `_resolveSkillMarker(`，且 `params: event.args` 逐字未变。
    - 源码断言 `renderToolCard` 函数体内含 `使用技能「`、`tool-card-name-skill`、`TIER_BADGE`、`textContent`；且 `managed-skills` / `SKILL.md` 字面量命中数为 0。
    - 源码断言 `src/renderer.js` 含 `skillInvocation: event.skill_invocation`。
    - 断言 `src/styles/main.css` 的 `.tool-card-name` / `.tool-card-header` / `.tool-card-icon` / `.tool-card-status` 四条**既有规则体内**不含 `使用技能` / `tool-card-name-skill` / `tool-card-name-text`（即技能变体只经两个新类承载，既有规则零改动）。
    - 断言 `REALM_SYSTEM_PROMPT` 含 `技能` 与 `工具`，且 `buildSystemPrompt()` 的技能段 `=== buildSkillsPrompt()` 返回值。
    - 断言本任务对 `src/styles/main.css` 的改动已在**同一提交内**推进 `src/index.html` 的 `styles/main.css?v=` 序号（否则返回用户拿到不含两个新类的旧样式表 —— 序号只增不改其余）。
  </acceptance_criteria>
  <done>模型自动匹配技能的可见性成立：`read` 打开技能 `SKILL.md` 时工具卡片标题变为「使用技能「name」」并带与面板同款的来源徽标；非技能 `read`、非 `read` 工具、非法参数、工作区外路径一律渲染普通卡片；判定只在主进程，renderer 零路径匹配；重载前实时链路的字段贯通（事件 → 状态 → 渲染）全部有断言。</done>
</task>

<task type="auto">
  <name>Task 2: 重载路径标记重建 —— 同一判据两处调用</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js`（被改造文件）：`:2461-2480`（`getConversationMessages` 现状：直接透传 `conversationStore.getMessages`）、Task 1 新增的 `_resolveSkillMarker`、`:2111-2130`（`saveCurrentConversation` 的落库来源 = `agent.state.messages`）、48-01 已加的 user 行 `<skill>` 装饰（同一函数内，本任务**不得**改动它）
    - `ai-conversations-manager.js:570-612`（`getMessages` 的 assistant 行 `toolExecutions` 形状 `{ id, name, status, params }`，`params: call.arguments`）、`:639-659`（user 行 `<context-summary>` 特判与 user 行显示形状 —— 装饰层形态参照）、`:66-78`（`messages` 表列，含 `tool_calls`）
    - `48-RESEARCH.md` §3.3（重载路径的标记重建：推荐做法与「不做」的替代方案及其理由）
    - `48-PATTERNS.md` §`ai-manager.js` ⑧、§（可选落点）`ai-conversations-manager.js`
    - `tests/test-ai-skills.js:24-73`（夹具三件套）、`:1218-1276`（覆写 + `t.after` 复位先例）
  </read_first>
  <action>
    **① `ai-manager.js` 的 `getConversationMessages(conversationId)`** —— 在 48-01 的 user 行 `<skill>` 装饰**之外**新增 assistant 行的标记重建（`conversationStore` 保持「只懂存储」的单一职责，技能域知识**不得**下沉进去）：
    - 取回显示形状消息数组后遍历；对每条 assistant 行，若 `Array.isArray(m.toolExecutions)`，逐条用工 **Task 1 的同一个** `this._resolveSkillMarker(t.name, t.params)` 判定；命中则挂 `t.skillInvocation = { name, tier }`，不命中则**不挂键**（不写 `undefined`）。
    - **本方法体内不得直接调 `matchSkillByPath(`** —— user 行的 `<skill>` 装饰（连同它那**唯一**的 `matchSkillByPath(location)` 调用）由 **48-01 的私有方法 `_decorateSkillUserMessage`**（及模块级纯函数 `parseStoredSkillInvocation`）承担；`getConversationMessages` 只**调用**该方法，不内联其判定。这样「一处实现、两处调用」的形态是：user 行 → `_decorateSkillUserMessage`（内调 `matchSkillByPath`）、assistant 行 → `_resolveSkillMarker`（内调 `matchSkillByPath`），两个装饰各有一条独立实现，且本方法体内只剩对两个私有落点的调用。若 48-01 交付时 user 行装饰是内联在本方法体里的，先回到 48-01 完成抽取，**不得**在本计划内把 48-01 的实现搬过来。
    - **不得**改写 `t.params` / `t.status` / `t.result` / `t.name`；**不得**改变既有字段的存在性（renderer 的普通卡片路径必须逐字节不变）。
    - 技能已被删除 / 改名 → `matchSkillByPath` 找不到 → 静默不标（与实时链路「匹配不到就是普通卡片」一致）。
    - **不做**的替代方案（写进注释）：把标记写进 `tool_calls` 列会污染 `getAgentMessages` 读同一列的 LLM 上下文重建路径。
    - 整段包 `try/catch`：装饰失败只 `console.warn` 并返回未装饰的结果（**不因元数据缺失丢消息**），与 48-01 的 user 行装饰同款容错。

    **② 测试（扩展 `tests/test-ai-skills.js`）**
    - 重载一致性：构造一条 assistant 显示形状消息（`toolExecutions: [{ id, name: 'read', status: 'completed', params: { path: <真实技能 SKILL.md 绝对路径> } }]`）经 `getConversationMessages` 处理后，`t.skillInvocation` 的形状（键集合与取值）与**实时链路**产出的形状逐字相等（按 `Object.keys().sort()` 比对）。
    - skill 删除后：把缓存复位为空（或删掉技能目录后 `refreshSkills`）再调 `getConversationMessages` → `t.skillInvocation` **不存在**（`!('skillInvocation' in t)`），且消息条数与 `params` 逐字未变。
    - 非技能 `read`（`params.path` 指向工作区内非 `SKILL.md` 文件）→ 不挂键。
    - 容错：把 `_resolveSkillMarker` 替换为抛错实现后调用 `getConversationMessages` → 仍返回完整的消息数组（不抛错、不丢消息）。
    - 源码扫描：`getConversationMessages` 方法体内含 `_resolveSkillMarker(`，且**同处**不出现 `matchSkillByPath(`（必须复用私方法，不得第二次直接调底层 —— 保证「一处实现、两处调用」；user 行的 `<skill>` 装饰连同它那唯一的 `matchSkillByPath(location)` 调用由 48-01 的 `_decorateSkillUserMessage` 承担，**不在本方法体内**，故本条与 48-01 Task 1 ③ / key_links 不冲突）。
    - 源码扫描：`ai-conversations-manager.js` 内**零**出现 `skillInvocation`（技能域知识不得下沉到存储层）。
  </action>
  <verify>
    <automated>node --test tests/test-ai-skills.js</automated>
    <fails_when>非零退出，或输出中存在 "# fail" 行且其值 > 0，或 "# pass" 行 < 64</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('ai-manager.js','utf8'); const i=s.indexOf('getConversationMessages(conversationId)'); if(i<0) throw new Error('missing getConversationMessages'); const body=s.slice(i, s.indexOf('\n  }', i)); if(body.length < 120) throw new Error('body slice too small — 方法体提取口径失效，护栏会假绿'); if(!body.includes('_resolveSkillMarker(')) throw new Error('reload path must reuse the same resolver'); if(body.includes('matchSkillByPath(')) throw new Error('must not inline the user-line decoration (48-01 的 _decorateSkillUserMessage 才是它的落点)'); if(!/toolExecutions/.test(body)) throw new Error('must rebuild assistant tool marks'); console.log('reload-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "reload-ok"</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('ai-conversations-manager.js','utf8'); if(s.includes('skillInvocation')) throw new Error('skill domain knowledge leaked into the store'); console.log('store-clean-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "store-clean-ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/test-ai-skills.js` 输出 `# fail 0` 且 `# pass` ≥ 64。
    - 断言重载路径产出的 `skillInvocation` 对象键集合与实时链路产出**逐字相等**（`Object.keys().sort()` 比对）。
    - 断言技能删除后重载路径**不挂键**（`!('skillInvocation' in t)`），且 `params` / `status` / `result` 逐字未变。
    - 断言 `getConversationMessages` 内的判定复用 `_resolveSkillMarker`，且同方法体内不出现 `matchSkillByPath(`（**约束口径**：user 行的 `<skill>` 装饰及其唯一的 `matchSkillByPath(location)` 调用归 48-01 的私有方法 `_decorateSkillUserMessage` / 模块级纯函数 `parseStoredSkillInvocation`，不在本方法体内 —— 与 48-01 Task 1 ③ / key_links 不冲突）。
    - 断言 `ai-conversations-manager.js` 内 `skillInvocation` 命中数为 0（技能域知识不下沉存储层）。
    - 断言装饰段包 `try/catch`，且抛错时仍返回完整消息数组（不丢消息）。
  </acceptance_criteria>
  <done>实时链路与重载链路的技能标记由同一份判定实现产出、形状逐字一致；技能删除后两条链路都退化为普通卡片而**不丢消息**；存储层保持「只懂存储」。</done>
</task>

<task type="auto">
  <name>Task 3: 文档同步 —— 「发现与调用」章节 + 已知限制 + 测试清单</name>
  <files>docs/product/ai-skills.md, AGENTS.md</files>
  <read_first>
    - `docs/product/ai-skills.md`：`:1-30`（文件头与诊断表）、`:32-38`（§一 能力 —— 「渐进式披露」既有口径）、`:55-73`（§三 优先级 / §四 限额的「要点列表 + 表格」混排形态模板）、`:75-80`（§五 沙箱边界）、`:82-92`（§六 已知限制 —— 本任务**追加**条目）、`:93-100`（§七 测试与验证 —— 本任务**追加**命令）、`:101-160`（§八 内置技能 / §九 bash 安装档 —— 新章节插在其后）
    - `AGENTS.md`：`:255-275`（「AI 工作区与 Bash 权限」小节 —— 测试清单在 `:267`，维护约定在 `:271`）
    - `48-UI-SPEC.md` §Copywriting Contract 的「全部用户可见文案」表（文档必须与实现口径一致）、§面板结构契约、§交互契约、§刷新契约、§`read` 工具卡片技能化
    - `48-RESEARCH.md` §2.8 的八行边界矩阵（文档的边界行为表**直接搬它、去掉实现列**）、§「文档同步」四条要求
    - `48-CONTEXT.md` §decisions 的 D-01..D-19（文档口径的唯一来源）、§`<deferred>`（明确哪些不写进文档）
    - `48-PATTERNS.md` §`docs/product/ai-skills.md`（章节形态模板）
  </read_first>
  <action>
    **① `docs/product/ai-skills.md` 新增章节「十、发现与调用」**（插在 §九 之后，与 §八 / §九 并列；形态照抄 §三 / §四 的「要点列表 + 表格」混排）。内容**必须**覆盖以下七块，且口径与实现/UI-SPEC 逐字一致：
    1. **面板形态**：输入 `/` 触发；两个 sticky 分区「技能」/「命令」（技能在上）；过滤为 name 前缀命中排前 + description 子串兜底排后，命令分区语义与相对顺序不变；`/skill:xxx` 作为过滤 token 时等价于「只显示技能」；行五要素（`/{name}` + 来源徽标 + `仅显式` 标记 + 单行截断描述 + 行尾状态标注）；面板**不显示**体积 / 文件数 / 诊断计数（那些属设置页）。
    2. **两种调用语法**：`/skill:name [args]` 与裸 `/name [args]` 都识别；**本地命令优先**（技能与本地命令同名时本地命令胜出，面板里该技能行灰显不可选中并标注原因）。写明边界语义是「严格前缀 + 空白边界」（`/foo` 命中、`/foobar` 不命中 `foo`）。
    3. **实时读盘口径（用户明确要求，必须单独成段）**：`/skill:name` 的正文在**调用那一刻**从磁盘读取，**不**依赖 system prompt 的冻结快照、**不**沿用对话历史里的旧回答；注入之后它就成为普通历史消息、**跨轮不再重读**（与 §六 的「`/compact` 不保留技能正文」衔接）。不要把它写成「每次请求都重读全部正文」。
    4. **边界技能行为表**：直接搬 `48-RESEARCH.md` §2.8 的八行矩阵并**去掉实现列**（正常 / `disable-model-invocation` / 已禁用 / 被遮蔽 / 与本地命令同名 / 超数量上限 / 未进提示词超预算 / 不存在或被跳过），列为「技能状态 / 面板是否显示 / 能否选中 / 能否显式调用 / 是否进提示词」。必须显式写清两条易混语义：`disable-model-invocation`（**可**显式调用、面板打「仅显式」）与「已禁用」（两个入口都拒、面板不显示）是**两个互不蕴含**的 flag。
    5. **三档来源徽标**：用户（`source === 'user'`）/ 内置（名字属于随包 `skills-builtin/` 扫描结果，指向 §八）/ 托管（`managed` 且非内置，即 AI 自建）；写明判定是**扫随包目录名**、零状态文件零硬编码，并给出 `TIER_BADGE` 查表的实现位置（`src/skill-picker-model.js`）。
    6. **`read` 卡片技能化**：模型自行匹配并 `read` 技能正文时，工具卡片标题变为「使用技能「name」」并带来源徽标；参数区仍显示实际读取的路径（用户可核对）；**不额外插提示条**（与卡片信息重复）。如实写可靠性边界：极端 Unicode 文件名下标记可能不命中，**后果仅是显示为普通 `read` 卡片**，正文仍被正常读取。
    7. **诚实边界**：面板刷新走主进程的「重扫 → 回写 prompt → 广播」链路；AI 未初始化时该链路早退，面板显示上一次投影（此时没有任何可用的读盘环境）。同时写明 **P8 门禁的准确进度**：本阶段让该回写链路**有了生产调用方**（面板打开触发重扫 + 回写），但它是「重扫 + 回写」语义，**不是「写成功后回写」语义** —— 技能集的实际**写入**入口仍归设置页与 AI 自建技能阶段（对应 §六 既有条目）。

    **② `docs/product/ai-skills.md` §六 已知限制 —— 追加三条**
    - 「技能文件改动后需**重开对话**才反映到已注入的历史消息」——已注入的正文是普通历史消息，跨轮不重读（D-09 的生态预期；与实时读盘口径互不矛盾）。
    - 「`allowed-tools` 仍不展示、仍不被运行时强制」——**O3 未变**（既有条目在别处，此处只补一句交叉引用，不要改写既有条目的免责措辞）。
    - 「极端 Unicode 文件名下 `read` 卡片可能不被识别为技能读取」——仅影响卡片标题，不影响正文读取。

    **③ `docs/product/ai-skills.md` §七 测试与验证 —— 追加一行**
    新增：`node --test tests/test-skill-picker-model.js`（面板纯逻辑：解析与 args 取值 / 过滤两档 / 展平与可选中性 / 导航取模 / 三条接线扫描）。既有的 `node tests/test-ai-skills.js` 行**追加说明**其覆盖已扩到「显式调用解析与实时读盘 / 投影收窄 / 三档 tier / `promptOmitted` / `read` 标记 / 重载装饰」。**不得**删改既有命令行。

    **④ `AGENTS.md` —— 两处最小更新**
    - `:267` 的测试清单追加 `node --test tests/test-skill-picker-model.js`（面板纯逻辑断言宿主），与既有条目同款格式。
      - **护栏**：该「`- 测试：`」行同时是 `tests/test-builtin-skills-seeder.js:2200` 的 DOC-02 断言对象（它直跑时校验该行含 `21 例`、且 bash-policy 计数等于实跑值）。**追加新条目不得改动既有条目的数字与措辞**，否则 `node tests/test-builtin-skills-seeder.js`（本阶段的 gate 用直跑形式）会从绿转红。
    - 同小节末尾追加一条维护约定（与「导航入口与分配规则」「AI 工作区与 Bash 权限」同款句式）：**「任何『加载一个技能』或『把技能暴露给用户』的入口（`/` 面板、`/skill:name`、裸 `/name`、模型自动匹配的 `read`）的权威说明是 `docs/product/ai-skills.md` 的『发现与调用』章节；改动面板形态 / 语法优先级 / 实时读盘口径 / 边界技能行为 / 三档徽标 / `read` 卡片技能化时，必须同步更新该章节与本节测试清单。」**
    - **不得**改动 AGENTS.md 的其他小节与既有维护约定。

    **⑤ 文档纪律（负向约束）**
    - **不得**在文档里出现任何 `allowed-tools` 被强制执行 / 技能自带权限的表述（技能不构成额外权限）。
    - **不得**写入「技能可自动注入正文」「mid-prompt 嵌入 `/skill:` token」「技能堆叠调用」「`$ARGUMENTS` 替换」等**本阶段未实现且已 deferred** 的能力。
    - 文案与 UI-SPEC §Copywriting Contract 的「全部用户可见文案」表逐字一致（`未找到技能「foo」，输入 / 查看可用技能`、`技能「foo」已被禁用，可在 设置 → AI → 技能管理 重新启用`、`无匹配技能或命令，输入 / 查看全部`、`使用技能「name」`、`未进提示词 · 超预算`、`超数量上限`、`已遮蔽 · 由用户同名技能胜出`、`与本地命令同名 · 本地命令优先`）。
    - 章节编号顺延为「十」；若执行时发现文件实际编号不同，按实际最大编号顺延并在 SUMMARY 中记录偏差。
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/product/ai-skills.md','utf8'); const need=['发现与调用','实时读盘','/skill:name','TIER_BADGE','使用技能「','disable-model-invocation','已禁用','已遮蔽','超数量上限','未进提示词 · 超预算','与本地命令同名 · 本地命令优先','重开对话','allowed-tools']; for (const k of need) { if(!s.includes(k)) throw new Error('doc missing: '+k); } if(/自动注入正文|mid-prompt|参数替换/.test(s)) throw new Error('doc must not describe deferred capabilities as shipped'); console.log('doc-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "doc-ok"</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('docs/product/ai-skills.md','utf8'); for (const c of ['node tests/test-ai-skills.js','node tests/test-builtin-skills-seeder.js','node tests/test-agent-workspace.js']) { if(!s.includes(c)) throw new Error('existing test command removed: '+c); } if(!s.includes('node --test tests/test-skill-picker-model.js')) throw new Error('new test command missing'); const a=require('fs').readFileSync('AGENTS.md','utf8'); if(!a.includes('tests/test-skill-picker-model.js')) throw new Error('AGENTS.md test list not updated'); if(!a.includes('docs/product/ai-skills.md')) throw new Error('AGENTS.md maintenance pointer missing'); if(!a.includes('node tests/test-unified-navigation.js')) throw new Error('AGENTS.md existing nav test line removed'); console.log('sync-ok');"</automated>
    <fails_when>非零退出，或标准输出不含 "sync-ok"</fails_when>
    <automated>node --test tests/test-ai-skills.js tests/test-skill-picker-model.js tests/test-agent-workspace.js tests/test-ai-bash-policy.js tests/test-ai-conversations.js</automated>
    <fails_when>非零退出，或输出中存在 "# fail" 行且其值 > 0，或 "# pass" 行 < 183（基线地板）</fails_when>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>非零退出，或输出中存在 "# fail" 行且其值 > 0，或 "# pass" 行 < 101（基线地板）</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/product/ai-skills.md` 含新增章节，覆盖任务列出的七块内容；上列 13 个关键词逐条命中。
    - 文档内不出现「技能可自动注入正文」「mid-prompt 嵌入 token」「`$ARGUMENTS` / 参数替换」等本阶段未实现的能力表述（正则命中数 0）。
    - 文档的边界行为表与 `48-RESEARCH.md` §2.8 的八行矩阵**行数一致**（8 行状态），且**不含**实现列（不出现 `overLimit` / `promptOmitted` 字段名 —— 文档讲行为不讲字段）。
    - 文档明确写出「`disable-model-invocation` 与『已禁用』是两个互不蕴含的 flag」这一条。
    - 文档明确写出「实时读盘约束的是**调用那一刻**；注入后即普通历史消息、跨轮不重读」。
    - §七 既有的三条测试命令**逐字保留**，并新增 `node --test tests/test-skill-picker-model.js`。
    - `AGENTS.md` 的测试清单含 `tests/test-skill-picker-model.js`，且含指向 `docs/product/ai-skills.md` 的维护约定；既有行（如 `node tests/test-unified-navigation.js`）逐字未变。
    - **既有红项登记（豁免口径，本任务必须照此执行）**：`node --test tests/test-builtin-skills-seeder.js` 在**基线即红** —— `# tests 101 / # pass 100 / # fail 1`，红项是 `tests/test-builtin-skills-seeder.js:2200` 的用例「AGENTS.md 的「测试：」行计数与实跑输出一致」（它在 `node --test` 下 `spawnSync` 嵌套跑 `node --test tests/test-ai-bash-policy.js`，孙进程输出不再是可解析的 TAP，故 `:2212` 的断言 `应能从 node --test 的 TAP 输出解析出 \`# tests\`` 失败）。**该文件不在任何计划的 `files_modified` 内，本阶段改动不会使其转绿**，故：① gate 用**直跑形式** `node tests/test-builtin-skills-seeder.js`（基线 `101/0` 全绿）、与 5 文件 `node --test` 分成**两条独立命令**；② **不得**为本阶段去修改该测试文件（不在本阶段范围）；③ 该既有红项**不计入**本阶段的引入回归。若执行时实测该文件直跑转红，那才是本阶段引入的回归，必须处置。
    - 全量 gate 两段均须 `# fail 0`：5 文件 `node --test` 段 `# pass` ≥ 183（基线实测 183）、seeder 直跑段 `# pass` ≥ 101（基线实测 101）。
  </acceptance_criteria>
  <done>「发现与调用」的产品说明与实现同步：面板形态 / 两种语法与本地命令优先 / 实时读盘口径 / 八行边界行为表 / 三档徽标与 seeded 判定 / `read` 卡片技能化 / 诚实边界全部成文；§六 补三条已知限制、§七 补测试命令；`AGENTS.md` 的测试清单与维护约定指向该章节；全量 gate 两段（5 文件 `node --test` + seeder 直跑）全绿，`:2200` 的既有红项按登记口径豁免。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK 工具事件 → 主进程 | `event.args`（模型给的 `read` 参数，含 `path` 原文）属不可信输入，进入路径归一化与缓存匹配 |
| 主进程 → renderer | `skill_invocation` 随工具事件推送；技能名与 tier 进入 DOM |
| 磁盘 → 主进程 | 缓存 `filePath` 来自 SDK 扫盘结果，是被匹配的权威侧 |
| 对话库 → 主进程 → renderer | 重载路径从 `tool_calls` 列重建 `params` 后再判定标记 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-10 | Tampering | `renderToolCard` 的技能变体（技能名来自磁盘、随事件到达 renderer） | high | mitigate | 名称与徽标一律 DOM API + `textContent`（沿用既有 T-21-03 缓解）；tier → class / label / title 走 `TIER_BADGE` 白名单查表，值不参与 class 拼接；表外 tier 跳过徽标；整段**不引入** `innerHTML` |
| T-48-11 | Spoofing | `read` 事件里的 `path` 由模型给出，可被伪造成指向技能文件 | low | accept | 误标只影响**展示**（卡片标题），不改任何数据、不放宽任何权限（技能不构成额外权限，见 `docs/product/ai-agent-workspace.md`）；仍以「与缓存 `filePath` 规范化全等 + basename 必须为 `SKILL.md`」双条件约束误标面 |
| T-48-12 | Elevation of Privilege | 徽标被读成「技能授予了额外权限」 | medium | mitigate | 徽标只表达**来源分类**（用户 / 内置 / 托管）；不展示 `allowed-tools`（O3 未变）；产品文档写明技能不构成额外权限并与既有免责条目交叉引用 |
| T-48-13 | Information Disclosure | 重载路径把技能正文/路径带给 renderer | low | accept | `skillInvocation` 只含 `{ name, tier }`（**不含**正文与 `filePath`）；参数区的既有展示语义不变 |
| T-48-SC | Tampering | npm / pip / cargo 安装 | high | accept | 本计划**零新增运行时依赖**（复用既有 SDK 与 48-01/48-02 的产物），无安装任务，故无包合法性门禁触发条件 |

> **裁定说明**：T-48-10 是本计划唯一 `high`，缓解全部落在 `<acceptance_criteria>` 内（`textContent` + 白名单查表 + 表外跳过）；
> T-48-12 的缓解形态是「不展示不该展示的字段 + 文档显式免责」，由 Task 3 的文档断言与既有 §六 条目共同承担。
> 本计划的 `<action>` 内**不书写**任何被 acceptance criteria 负向 grep 的字面量。
</threat_model>

<verification>
1. `node --test tests/test-ai-skills.js` → `# fail 0` 且 `# pass` ≥ 64（每次任务提交后）。
2. 全量 gate（进 `/gsd-verify-work` 之前）：`node --test tests/test-ai-skills.js tests/test-skill-picker-model.js tests/test-agent-workspace.js tests/test-ai-bash-policy.js tests/test-ai-conversations.js` → `# fail 0`（基线 183/0）**并** `node tests/test-builtin-skills-seeder.js` → `# fail 0`（基线 101/0，**直跑形式**）。**不要**用 `node --test` 跑 `tests/test-builtin-skills-seeder.js` —— 那会命中 `:2200` 的既有环境性红项（嵌套 `node --test` 的 TAP 解析），见 Task 3 的既有红项登记与豁免口径。
3. 事件接线护栏：`tool_execution_start` 带 `skill_invocation`、`params` 未变。
4. 判定落点护栏：`renderToolCard` 内零路径字符串匹配；`ai-conversations-manager.js` 内零 `skillInvocation`。
5. 重载一致性：同一 `_resolveSkillMarker` 两处调用，对象形状与实时链路逐字相等。
6. 文档护栏：13 个关键词命中、deferred 能力零出现、既有测试命令逐字保留、`AGENTS.md` 已更新。
7. 缓存失效：本阶段最后一次 CSS 改动随 `src/index.html` 的 `styles/main.css?v=` 序号推进一并提交。
8. 端到端（人工，end-of-phase）：`npm run dev` → 问一个命中技能 description 的任务，观察卡片标题是否为「使用技能「…」」且带徽标 → 切换对话再切回，标记仍在；直接问「你有哪些技能」，模型应区分技能与工具（D-18）。
</verification>

<success_criteria>
- 模型仅凭 description 自动匹配技能并 `read` 其正文时，工具卡片标识为「使用技能「name」」并带来源徽标；非技能读取与非法参数一律普通卡片（零回归）。
- 判定只在工具事件生成侧；renderer 零路径匹配；重载链路用同一实现重建标记、形状逐字一致、技能删除后静默退化且不丢消息。
- 技能化不新增 system-note、不新增卡片形状、不改既有卡片规则；标题截断与徽标保留由新类与既有布局承担；两个新类的样式随缓存失效序号推进一并生效。
- `docs/product/ai-skills.md` 的「发现与调用」章节、§六 已知限制、§七 测试清单，以及 `AGENTS.md` 的测试清单与维护约定同步完成，且不含任何未实现能力的表述。
- 全量 gate 两段全绿（5 文件 `node --test` → `# fail 0`、`# pass` ≥ 183；`node tests/test-builtin-skills-seeder.js` 直跑 → `# fail 0`、`# pass` ≥ 101），既有 64 例基线不回归；`:2200` 的既有环境性红项按登记口径豁免（**不得**改该测试文件）。
</success_criteria>

<artifacts_this_phase_produces>
> **Artifacts this phase produces** —— 本计划新建的符号（供 plan-review-convergence 的 source-grounding pass 排除）。

**`ai-manager.js` 新方法**
- `_resolveSkillMarker(toolName, args)`（工具事件生成侧的技能标记判定；实时链路与重载链路共用）

**`ai-manager.js` 新事件字段**
- `tool_execution_update` 事件的 `skill_invocation` 字段（值形如 `{ name, tier }` 或 `null`）

**`src/renderer.js` 新数据字段**
- `state.aiMessages[].toolExecutions[].skillInvocation`

**`src/styles/main.css` 新类**
- `.tool-card-name-skill`（修饰类，与既有 `.tool-card-name` 并用）
- `.tool-card-name-text`

**`src/index.html` 既有项变更**
- `styles/main.css?v=` 缓存失效序号 `+1`（本阶段最后一次 CSS 改动，随两个新类一并生效）

**`tests/test-ai-skills.js` 新增断言组**
- `read` 标记：四类路径 + 四条负例 + `tier` 注入
- 重载装饰：形状与实时链路一致 / 技能删除后不挂键 / `params` 未变 / 容错不丢消息
- 源码扫描：事件带标记、renderer 零路径匹配、存储层无技能域知识、D-18 与技能段边界回归

**`docs/product/ai-skills.md` 新章节**
- 「十、发现与调用」（面板形态 / 两种语法与本地命令优先 / 实时读盘口径 / 八行边界行为表 / 三档来源徽标 / `read` 卡片技能化 / 诚实边界）

**`docs/product/ai-skills.md` 既有章节新增内容**
- §六 已知限制 3 条；§七 测试与验证新增测试文件说明

**`AGENTS.md` 新增内容**
- 测试清单新增 `tests/test-skill-picker-model.js`
- 「技能发现与调用」维护约定（权威文档指向 `docs/product/ai-skills.md`）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/48-skill-name/48-03-SUMMARY.md` when done
</output>
