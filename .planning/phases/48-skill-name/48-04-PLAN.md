---
phase: 48-skill-name
plan: 4
type: execute
wave: 4
depends_on: [48-01, 48-02, 48-03]
gap_closure: true
gap_ids: [G-48-2]
files_modified:
  - ai-skills-manager.js
  - tests/test-ai-skills.js
autonomous: true
requirements: [DISC-02]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "目录名与 frontmatter `name` 不一致的技能（如 `skills/evil/SKILL.md` 里写 `name: find-skills`）在面板上以**目录名**出现，且手打 `/skill:evil` 或点面板行都能成功调用 —— 修复前恒返回 `not_found`（已用临时探针实测复现：缓存 name = `evil`、`getSkillsForUI` name = `evil`，而 `readSkillForInvocation(env,'evil')` = `{ok:false,reason:'not_found'}`）"
    - "注入块的技能名是**目录名**：`<skill name=\"evil\" location=\"…/skills/evil/SKILL.md\">` —— SDK `formatSkillInvocation`（`node_modules/@earendil-works/pi-agent-core/dist/harness/skills.js:9`）取的是 `skill.name`，故命中后必须把 `name` 重写为入参（目录名），否则冒名 name 会进注入块、重载链路 `parseStoredSkillInvocation` 也会还原出错误技能名（46 D-08 防冒名语义回归）"
    - "失败码域不变：仍只有 `not_found` / `disabled` 两个码（不新增第三个）；正文为空串或仅空白、SKILL.md 被删、目录被移除 → 一律 `not_found`"
    - "「目录被换成别的技能」的判据从「SDK 读回的 name 与入参不等」换成「目录读不到 / 路径不等」—— 该负例仍有能失败的断言（读盘为空 → not_found），不再依赖会与「name≠目录名」混淆的那个字段"
    - "`enforceDirNameAuthority`（缓存层）与 `readSkillForInvocation`（读盘层）两处「目录名权威」口径一致：缓存 `name`、注入 `<skill name>`、provenance 行三者恒为同一目录名"
    - "既有 129 例无一回归（`node tests/test-ai-skills.js`）：名字一致的常规技能、SKILL.md 被删、空正文、同名遮蔽、超限技能可显式调用等用例全部继续通过 —— 新判据只改变「name≠目录名」这一条路径的结果"
  artifacts:
    - "`ai-skills-manager.js`：`readSkillForInvocation` 的命中判据改为目录路径全等（`path.resolve(path.dirname(s.filePath))` 与缓存条目目录比对），返回 `{ok:true, skill:{...fresh, name}, source}`（`name` = 入参目录名）；删除 `|| skills[0]` 兜底与 `fresh.name !== name` 判据；JSDoc 同步改写（失败码域、目录名权威在两条链路上的统一口径、name 重写的理由）"
    - "`tests/test-ai-skills.js`：`writeSkill` 增加 `frontmatterName` 选项（默认 = 目录名，既有调用零改动）；D 组新增「name ≠ 目录名」正例（可调用 + 注入名为目录名 + 改盘后仍实时生效）与改写后的负例；`.planning/phases/48-skill-name/48-04-SUMMARY.md` 记录实测例数与「§10.4 表述与实现一致、无需改文档」的核对结论"
  key_links:
    - "`readSkillForInvocation` → `_resolveSkillInvocation`（`ai-manager.js:1421`）→ `buildSkillInvocationBlock({skill: result.skill, name: parsed.name}, args, formatSkillInvocation)`：`formatSkillInvocation` 只认 `skill.name`，因此命中对象的 `name` 必须已被重写为目录名，`parsed.name`（用户键入的目录名）与它必须相等 —— 这两者一旦不等，注入块的 `name` 属性与 provenance 行就会分叉"
    - "注入块 → 入库 → `parseStoredSkillInvocation` 的 `<skill name=\"([^\"]+)\">` → 重载后气泡 pill：重载链路的技能名取自该属性，故本修复同时是「重开对话后 pill 名称正确」的前提"
    - "文档面（`docs/product/ai-skills.md` §10.4 表下注、§七 例数）**本计划不改** —— 本轮唯一文档写者是 48-06；本计划只做只读核对并把结论写进 SUMMARY"
  prohibitions:
    - statement: "不得把「注入用 name」改成 `fresh.name`（SDK 从 frontmatter 读回的名字），也不得为了让判据通过而跳过 `enforceDirNameAuthority` 之外的第二套名称解析"
      status: unresolved
      verification: judgment
      reason: "D-08 的防冒名语义要求目录名是唯一权威；`fresh.name` 是可以被 SKILL.md 内容伪造的字段，用它做注入名等于把 P3/S1 门禁要堵的『skills/evil/ 里声明 name: find-skills』重新放行"
    - statement: "不得为区分「目录被换成别的技能」而引入对 SKILL.md 内容的前缀/哈希比对（例如记录 mtime 或内容摘要做同一性校验）—— 同一目录内的内容变更正是『实时读盘』要反映的东西（D 组既有用例：改盘后二次调用必须读到新正文）"
      status: unresolved
      verification: judgment
      reason: "「调用那一刻读盘」是用户 2026-09-11 的硬约束（48-CONTEXT.md 开篇）；任何内容级同一性校验都会把「用户改了技能正文」误判为「目录被换掉」，直接违反该约束与既有实时读盘用例"
    - statement: "不得改动 `enforceDirNameAuthority`（缓存层重写）与其诊断、也不得改动 `matchSkillByPath` —— 本计划只改读盘路径的同一性判据"
      status: unresolved
      verification: judgment
      reason: "缓存层重写是 46 D-08 的既有交付且被 46/48 多组断言锁定（含 `realm_name_rewritten` 诊断与投影 tier 判定）；读盘路径的问题仅是『用哪个字段判定同一性』，扩大改动面会把两处口径同时移动"
---

<objective>
关闭 UAT gap **G-48-2**：`frontmatter name ≠ 目录名` 的技能在 `/` 面板列出且可选（文档 §10.4 标「能显式调用 ✅」），但 `readSkillForInvocation` 恒返回 `not_found` —— 面板与实现分叉，用户点了没反应。

Purpose: 该形态技能是 46 D-08 明确要**保留**的合法技能（GitHub 导入常见），而显式调用正是它们在本阶段唯一的可用路径；判据用错了字段（SDK 的 `Skill.name` = `frontmatterName || parentDirName`，而 Realm 的目录名权威只作用于缓存层），必须改用**目录路径**做同一性判据。
Output: `ai-skills-manager.js` 的判据修复 + `tests/test-ai-skills.js` 的正/负例（含 helper 支持写 name≠目录名）。文档面由 48-06 收口，本计划只做只读核对。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-skill-name/48-UAT.md
@.planning/phases/48-skill-name/48-REVIEW.md
@.planning/phases/48-skill-name/48-01-SUMMARY.md
@ai-skills-manager.js
@tests/test-ai-skills.js
@docs/product/ai-skills.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `readSkillForInvocation` 判据改目录路径 + 注入名重写为目录名</name>
  <files>ai-skills-manager.js</files>
  <read_first>
    - `ai-skills-manager.js:756-815`（`readSkillForInvocation` 全文与 JSDoc：失败码域、`|| skills[0]` 兜底、`fresh.name !== name` 判据）
    - `ai-skills-manager.js:310-341`（`enforceDirNameAuthority`：目录名权威只改写缓存层 `skill.name`，并注释说明 SDK 的 name 是 `frontmatterName || parentDirName`）
    - `ai-manager.js:1421-1450`（`_resolveSkillInvocation`：注入块用 `{skill: result.skill, name: parsed.name}` 调 `buildSkillInvocationBlock`）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/skills.js:8-11`（`formatSkillInvocation` 的 `<skill name="${skill.name}" location="${skill.filePath}">` 模板）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `## Gaps` → `G-48-2`（root_cause / artifacts / missing 三条）
    - `.planning/phases/48-skill-name/48-REVIEW.md` → `CR-02` 的 Fix 段（路径判据的参考实现）
  </read_first>
  <action>
    改 `ai-skills-manager.js` 的 `readSkillForInvocation(env, name)`（当前 `:798-815`），**只改命中判据与返回值组装**，其余逐字不动：

    1. 缓存侧查找（按 name 且 `shadowed !== true`，未命中 → `not_found`）与 `entry.disabled === true` → `disabled` 的**早拒顺序保持原样**（读盘之前判，不做无用 IO）。
    2. 读盘调用保持原样：动态 `import('@earendil-works/pi-agent-core')` 取 `loadSkills`，以 `path.dirname(entry.skill.filePath)` 为根读盘；SDK 抛错仍按 `not_found` 处理（不冒名注入）。**实测确认**：`loadSkills(env, <技能目录>)` 直接返回该目录下 `SKILL.md` 的一条技能（不要求 `<root>/<name>/SKILL.md`）。
    3. 命中判据改为**目录路径全等**：先算 `const expectedDir = path.resolve(path.dirname(entry.skill.filePath));`，再在 `skills` 里找 `s && typeof s.filePath === 'string' && path.resolve(path.dirname(s.filePath)) === expectedDir`；**删掉**当前的 `|| skills[0]` 兜底（它会把该目录里的**另一个**技能当成命中）。路径归一化只用纯词法 `path.resolve`（与 48-03 的 `_resolveSkillMarker` / `matchSkillByPath` 同规则），不引入 realpath / `~` 展开。
    4. 删除 `fresh.name !== name` 这条判据，并把紧随其后的注释改写成新语义：同一性由**所在目录**判定；`name` 不一致是 D-08 已用目录名权威化解的合法形态，不是「目录被换成别的技能」的证据。新的「目录被换成别的技能」证据是**目录读不到 / 路径不等**。改写后的注释**不得**再复述被删掉的旧判据表达式本身（本任务的源码探针在该函数体内对其负向检索）—— 写「同一性改由所在目录判定」即可，不要为了留痕把旧表达式抄进函数体。
    5. 空正文检查保持原序与原文（`!fresh`、`typeof fresh.content !== 'string'`、`fresh.content.trim() === ''` → `not_found`）。
    6. 成功返回改为 `{ ok: true, skill: { ...fresh, name }, source: entry.source }` —— **把 `name` 重写为入参（目录名）**，并在 JSDoc 写明理由：SDK 的 `formatSkillInvocation` 用 `skill.name` 生成注入块的 `name` 属性（`skills.js:9`），不重写会让 frontmatter 里的冒名 name 进注入块；同时重载链路 `parseStoredSkillInvocation` 从该属性取技能名，重写才能保证「重开对话后 pill 名称与实时链路一致」。
    7. JSDoc 同步：失败码域仍只有两个码；补一句「命中判据是**目录**（与 `enforceDirNameAuthority` 同口径），不是可能被 frontmatter 伪造的 `Skill.name`」；保留「读失败不回退缓存快照」的既有声明（实时读盘硬约束不变）。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node -e "const s=require('fs').readFileSync('ai-skills-manager.js','utf8');const a=s.indexOf('async function readSkillForInvocation');const b=s.indexOf('\n}', a);const body=s.slice(a,b);if(!/expectedDir/.test(body)){console.error('missing expectedDir');process.exit(1)}if(/fresh\.name !== name/.test(body)){console.error('legacy name judgment still present');process.exit(2)}if(/\|\| skills\[0\]/.test(body)){console.error('skills[0] fallback still present');process.exit(3)}if(!/\.\.\.fresh, name/.test(body)){console.error('name not rewritten to dir name');process.exit(4)}console.log('readSkillForInvocation judgment OK')"</automated>
    <fails_when>非零退出，或标准输出不含 "readSkillForInvocation judgment OK"，或出现 'missing expectedDir' / 'legacy name judgment still present' / 'skills[0] fallback still present' / 'name not rewritten to dir name' 任一行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1</automated>
    <fails_when>非零退出，或输出中 "# fail" 行 > 0，或 "# pass" 行 < 129（既有基线地板），或输出中无 "# pass" 行（进程崩溃 / 用例未跑到）</fails_when>
  </verify>
  <done>
    `readSkillForInvocation` 的同一性判据是目录路径全等；命中返回对象的 `name` 已重写为入参（目录名）；`skills[0]` 兜底与 name 不等判据已删除；失败码域仍只有 `not_found` / `disabled` 两个。改完后 `node tests/test-ai-skills.js` 仍全绿（既有 129 例中受影响的那条负例在新判据下依然 `not_found`：目录读不到）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 测试 helper 支持 name≠目录名 + 正/负例 + 文档口径核对结论</name>
  <files>tests/test-ai-skills.js</files>
  <read_first>
    - `tests/test-ai-skills.js:45-55`（`writeSkill` helper）、`:1424-1434`（`writeRawSkill`）、`:1440-1490`（D 组实时读盘既有用例，含将被改写的「目录被换成别的技能」用例与 `setupSkillsEnv` 夹具）
    - `tests/test-ai-skills.js:1500-1600`（同组其余用例：不存在 / disabled / 同名遮蔽 / overLimit 仍可调用）
    - `tests/test-ai-skills.js:1418-1422`（`sdkFormatSkillInvocation` helper）
    - `ai-manager.js:1421-1450`（`_resolveSkillInvocation` 与 `buildSkillInvocationBlock` 的调用形状 —— 正例第 2 条的组装方式）
    - `docs/product/ai-skills.md:300-322`（§10.4 边界技能行为表与表下注 —— 只读核对对象，本计划**不改**该文件）
    - `.planning/phases/48-skill-name/48-UAT.md` 的 `## Gaps` → `G-48-2` 的 `missing` 第 3、4 条
  </read_first>
  <action>
    1. `writeSkill(scannedDir, name, { description, body })` 增加选项 `frontmatterName = name`，frontmatter 写 `name: ${frontmatterName}` —— 默认值与既有全部调用逐字等价（既有用例零改动）；`writeRawSkill` 如被新用例复用则同理补该选项（或在新用例里改用 `writeSkill`）。
    2. D 组新增正例（`describe('D 组 · 实时读盘（用户 2026-09-11 硬约束，DISC-02）')` 内）：
       - 「目录名与 frontmatter name 不一致的技能可正常显式调用」：`writeSkill(getSkillsDir(), 'evil', { frontmatterName: 'find-skills', description: …, body: '# evil\n\n正文一\n' })` → `setupSkillsEnv(root)` → 断言 `getSkillsSnapshot().skills` 里该条 `skill.name === 'evil'`（缓存层目录名权威）→ `readSkillForInvocation(env, 'evil')` 断言 `ok === true`、`source === 'user'`、`res.skill.content` 含「正文一」、**且 `res.skill.name === 'evil'`**（同时断言它**不等于** `'find-skills'` —— 这条是本修复的防冒名靶心断言）。
       - 「注入块用目录名」：用 `sdkFormatSkillInvocation()` 取 `formatSkillInvocation`，以 `aiManager.buildSkillInvocationBlock({ skill: res.skill, name: 'evil' }, '帮我找 X', fmt)` 组装，断言块内同时出现 `name="evil"` 与 `location="<该技能 SKILL.md 绝对路径>"`（用 `path.join` 与 `workspace.getSkillsDir()` 拼出），且**不出现** `name="find-skills"`。
       - 「name 不一致 + 实时读盘」：在正例基础上把该文件 frontmatter 的 name 改成第三个值（不改目录名、**不重新 refreshSkills**）后再次调用 → 仍 `ok === true`、正文更新、`res.skill.name` 仍是目录名。
    3. 改写既有负例用例「目录被换成别的技能（fresh.name !== name）→ not_found」（`:1475-1487`，标题行在 `:1475`）：标题与注释的判据改为「目录读不到 → not_found」（把 `alpha` 目录整体移除或替换成别的技能目录后按 `alpha` 调用），断言保持 `ok === false` / `reason === 'not_found'`；并核对同组的「SKILL.md 被删除 → not_found」（`:1466-1473`，标题行在 `:1466`）与「正文为空串 / 仅空白 → 组装被拒」（`:1489` 起）两条继续通过（它们现在由**不同的**机制保证：目录读盘为空 / 正文检查）。**本计划是 `tests/test-ai-skills.js` 的唯一写者**（48-05 同 wave、只执行该文件作回归信号，不得编辑它）。
    4. 跑全量并记录实测：`node tests/test-ai-skills.js`（记录通过例数），另跑 `node --test tests/test-skill-picker-model.js` 作渲染端回归对照（本计划不改渲染端）。
    5. 文档口径**只读核对**（本计划不改 `docs/product/ai-skills.md`）：确认 §10.4 的「正常 → 能否显式调用 ✅」在本修复后为真、表下注与 §三「名称以目录名为权威」的表述与实现一致；把「核对结论 + 是否需要改文档」写进 `48-04-SUMMARY.md`。若确有必须改的表述，**不在本计划改**，登记到 SUMMARY 并交由 48-06 的文档收口任务落地。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node tests/test-ai-skills.js 2&gt;&amp;1</automated>
    <fails_when>非零退出，或输出中 "# fail" 行 > 0，或 "# pass" 行 < 132（既有 129 + 本任务新增 3 例后的地板），或输出中无 "# pass" 行</fails_when>
    <automated>cd /Users/wxnacy/Projects/Realm &amp;&amp; node --test tests/test-skill-picker-model.js 2&gt;&amp;1 &amp;&amp; node -e "const s=require('fs').readFileSync('tests/test-ai-skills.js','utf8');if(!/frontmatterName/.test(s)){console.error('helper option missing');process.exit(1)}if(!/res\.skill\.name/.test(s)){console.error('dir-name injection assertion missing');process.exit(2)}console.log('test surface updated')"</automated>
    <fails_when>非零退出（`node --test` 有断言失败，或探针 exit 1/2），或输出中 "# fail" 行 > 0，或 "# pass" 行 < 93（picker 既有基线地板），或标准输出不含 "test surface updated"</fails_when>
  </verify>
  <done>
    `node tests/test-ai-skills.js` 全绿且例数 ≥ 132（129 + 新增 3 例）；新增用例在**修复前**必须失败（`ok:true` 与 `res.skill.name === 目录名` 两条在旧实现下均为假）；`tests/test-skill-picker-model.js` 无回归；SUMMARY 记录实测例数与「§10.4 与实现一致、无需改文档」的核对结论。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 磁盘技能目录 → 注入块 | `SKILL.md` 所在目录名与 frontmatter 内容都是**不可信输入**（用户手放、AI 经 write/bash 自建、GitHub 导入）；只有目录名是 Realm 的权威，frontmatter 是内容方 |
| renderer → main（`/skill:<name>`） | 渲染端只发语法文本，存在性/启停/正文一律由主进程当场读盘裁定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-04-01 | Spoofing | `readSkillForInvocation` 的返回对象 | high | mitigate | 命中后必须把 `name` 重写为入参（目录名）并返回 `{...fresh, name}`；Task 2 的断言同时钉住「注入块 `name=` 是目录名」与「不出现 frontmatter 名」，防「用 `fresh.name` 当注入名」的次生回归（46 D-08 / P3 冒名门禁） |
| T-48-04-02 | Tampering | 同一性判据（`skills.find(...)`） | medium | mitigate | 判据固定为目录路径 `path.resolve` 全等；删除会误纳「该目录里另一个技能」的 `|| skills[0]` 兜底；负例断言覆盖目录读不到的情形 |
| T-48-04-03 | Information Disclosure | 读盘失败路径 | low | accept | 读盘失败/为空一律 `not_found`（不区分原因），与 D-13 推论「被跳过与不存在同形」一致；原因区分留给 Phase 50 的诊断面 |
</threat_model>

<verification>
- `node tests/test-ai-skills.js`：全绿（含新增的 name≠目录名 正例与改写后的负例）
- `node -e` 判据探针：`readSkillForInvocation` 函数体含目录路径判据、不含旧 name 判据与 `skills[0]` 兜底、返回处重写 name
- `node --test tests/test-skill-picker-model.js`：全绿（回归对照，本计划不改渲染端）
- 文档：只读核对 §10.4 / §三 / §七 的口径与实现一致；结论落 SUMMARY（真正的文档编辑归 48-06）
- 指纹提示：本计划改 `ai-skills-manager.js`（在 `48-VERIFICATION.md` 的 `covered_files` 内）→ 48 的 `covered_digest` 会 stale，属**预期**（收尾时重算）；不得改其它阶段的文件
</verification>

<success_criteria>
- `frontmatter name ≠ 目录名` 的技能：面板可见、可选、可手打调用，注入块与 provenance 行用目录名
- 失败码域不变（`not_found` / `disabled`），空正文 / 目录读不到仍按不存在处理
- 既有 129 例无一回归；新增用例在旧实现下**必须失败**（本修复的靶心断言：`ok === true` 与 `res.skill.name === 目录名`）
- 文档 §10.4 的「能显式调用 ✅」与实现一致（核对结论落 SUMMARY）
</success_criteria>

<output>
Create `.planning/phases/48-skill-name/48-04-SUMMARY.md` when done
</output>
