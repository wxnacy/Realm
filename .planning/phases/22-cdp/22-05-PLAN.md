---
phase: 22
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-manager.js
  - .planning/phases/22-cdp/22-UI-SPEC.md
  - .planning/phases/22-cdp/22-UAT.md
autonomous: true
gap_closure: true
requirements:
  - CDP-02
  - CDP-04

must_haves:
  truths:
    - "AI 收到「当前标签打开 <URL>」请求时，唯一可用的打开链接工具是 open_link（newTab=false 走活跃 webview loadURL 真导航），幽灵 Tab 代码路径（主进程直接 createTab 不通知渲染进程）在 ai-manager.js 中不复存在（Gap 1 / CDP-04）"
    - "_buildRealmTools() 返回 7 个工具（get_tabs/search_history/manage_favorites/switch_container/read_page_content/extract_links/open_link），open_link 双模式实现与其他 6 个工具注册零改动"
    - "REALM_SYSTEM_PROMPT 中打开链接职责唯一归属 open_link，不再存在 navigate/open_link 双工具职责重叠的选择歧义（Gap 1 根因③）"
    - "截断标记文案单位语义与实现一致（UTF-16 字符数），22-UI-SPEC 契约同步修订，落地 REVIEW IN-01 的文档/措辞半（Gap 2）"
    - "22-UI-SPEC 的 DevTools 冲突文案契约按 UAT Test 5 用户决策（2026-08-02：DevTools 打开时 AI 工具可正常使用为期望行为）标注废弃，attachForAI 冲突检测代码保持现状不动"
    - "UAT Test 4 补「当前标签打开」措辞回归场景、Test 7 期望修正为条件式（提取后正文超阈值才期望截断标记），可复测"
  artifacts:
    - "ai-manager.js：navigate 工具定义块与系统提示词对应行移除；截断标记模板改字符语义；MAX_CONTENT_SIZE 注释修正单位说明"
    - ".planning/phases/22-cdp/22-UI-SPEC.md：截断文案行与 JSON 示例改字符语义；DevTools 行从错误文案表移除并新增契约变更记录"
    - ".planning/phases/22-cdp/22-UAT.md：Test 4 回归场景、Test 7 期望修正、工具总数 8→7 附注"
  key_links:
    - "「当前标签打开」措辞 → open_link（唯一入口）→ newTab=false → tabManager.getActiveTab + getActiveWebviewContentsIdLazy + wc.loadURL（既有正确实现，本计划不改动）"
    - "REALM_SYSTEM_PROMPT 工具列表 ↔ _buildRealmTools() 注册集合一致（同为 7 个，无导航/打开链接双入口）"
    - "截断标记文案（ai-manager.js）↔ 22-UI-SPEC 契约文案 ↔ UAT Test 7 期望，三处单位语义同为字符"
---

<objective>
关闭 22-UAT.md 判定的 2 个 gap（status: diagnosed），不重开本阶段其他工作，不触碰 VERIFICATION 已延期项（WR-01/WR-03/IN-02~07）。

Gap 1（major，UAT Test 4）：navigate 工具幽灵 Tab。三重根因（详见 .planning/debug/open-link-ghost-tab.md）：① navigate（ai-manager.js:748-790）直接调 tabManager.createTab —— 该函数仅写主进程记录+持久化（tab-manager.js:102-125），webview 只能由渲染进程创建，故 URL 从未加载；② newTab 参数被解构但从未使用（死代码），永远新建 tab 记录、从不当前标签导航；③ REALM_SYSTEM_PROMPT 中 navigate/open_link 职责重叠致 AI 对「当前标签打开」误选 navigate。修复取诊断建议 (b)：移除 navigate 仅保留 open_link（其双模式实现 ai-manager.js:1166-1272 已验证正确：newTab=true 走 open-url-in-tab 渲染进程全链路，newTab=false 走活跃 webview loadURL），并重写系统提示词消除歧义。选 (b) 而非 (a) 复用实现的理由：navigate 全代码库仅 ai-manager.js 两处引用（grep 确认无 renderer 映射/无其他调用方），删除即根除三重根因；保留双工具则选择歧义永存。

Gap 2（minor，UAT Test 7）：截断未触发非代码 bug（详见 .planning/debug/truncation-marker-missing.md）：测试页经真实 readability-bundle 提取后正文 99,630 UTF-16 字符 < MAX_CONTENT_SIZE 102,400，截断分支正确不执行；叠加单位语义错位（契约/标记写 bytes，实现按字符比较，IN-01）与 UAT 用例前提错配（以原始 HTML 大小推断提取后文本量，该页提取率 ~7.3%）。修复取最小改动：接受字符语义，修订标记措辞 + UI-SPEC 契约 + UAT 期望（条件式）；低成本可选项「系统提示引导 AI 转述截断标记」纳入（Task 1 本就重写提示词，仅 +1 行）。

附带文档级修订：UAT Test 5 记录的用户决策（DevTools 打开时 AI 工具可正常使用是期望行为，2026-08-02）使 22-UI-SPEC 的 DevTools 冲突文案契约过期，随 Task 2 一并更新 UI-SPEC（仅文档；attachForAI 冲突检测代码用户明确保持现状，不改）。

Purpose: 让「AI 打开链接时目标网页真实加载」与「截断契约单位语义一致」两个 truth 成立。
Output: ai-manager.js 外科修复（删 1 个工具 + 重写提示词 + 1 行文案 + 1 处注释）+ 2 个 .planning 文档同步；无新增依赖、无新增导出、无 IPC 变更。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/open-link-ghost-tab.md
@.planning/debug/truncation-marker-missing.md
@.planning/phases/22-cdp/22-UAT.md
@.planning/phases/22-cdp/22-UI-SPEC.md
@.planning/phases/22-cdp/22-REVIEW.md
@ai-manager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除 navigate 工具并重写 REALM_SYSTEM_PROMPT（Gap 1 / CDP-04）</name>
  <files>ai-manager.js</files>
  <read_first>
    - .planning/debug/open-link-ghost-tab.md（三重根因与症状日志对照 — 修复目标定义）
    - ai-manager.js:86-104（REALM_SYSTEM_PROMPT 全文 — 重写对象）
    - ai-manager.js:748-790（navigate 工具定义块 — 删除对象：execute 直接 tabManager.createTab 产幽灵 Tab、newTab 参数死代码）
    - ai-manager.js:1166-1272（open_link 工具全文 — 唯一保留的打开链接入口，双模式实现已正确，本任务不得改动）
    - ai-manager.js:124-143 与 :55-57（resolveToolTargetTab / getActiveWebviewContentsIdLazy — open_link newTab=false 路径依赖，确认不受影响即可，不改动）
    - .planning/phases/22-cdp/22-UAT.md Gaps 段 gap#1（missing 两项 — 本任务落地第 1 项）
  </read_first>
  <action>
删除 navigate 工具并重写系统提示词，使 open_link 成为打开链接的唯一入口（消除工具选择歧义）。

1. **删除 navigate 工具定义块**（ai-manager.js:748-790）：从 _buildRealmTools() 的 tools 数组中整体移除 name 属性值为 navigate 的那个对象字面量（含 label/description/parameters/execute 整段，注意连同前导 `{` 与结尾 `},` 一并删除，保持数组语法完整）。此举同时根除：主进程直接调 tabManager.createTab 的幽灵 Tab 路径、被解构从未使用的 newTab 死参数。**不得**改动 tab-manager.js（createTab 设计上由渲染进程 IPC 驱动，ipc-handlers.js:260 正常链路不变）、**不得**改动 open_link 及其余 6 个工具的任何字段。删除后工具总数 8 → 7。

2. **重写 REALM_SYSTEM_PROMPT**（ai-manager.js:86-104），仅改三处，其余原样：
   a. 能力列表：删除其中以 navigate 开头的那条列表行（原 :90）。
   b. 使用指南：将「当用户要求打开某个链接时，使用 open_link」一条强化为无歧义指引，替换为（逐字）：`- 当用户要求打开链接或网址时，一律使用 open_link：默认新标签页打开（newTab 省略或为 true）；用户明确要求"在当前标签页打开"时设 newTab 为 false；containerId 省略时使用当前活跃容器`（D-10/D-11 默认语义写入提示词）。
   c. 使用指南末尾新增一条（Gap 2 可选增强的低成本半，逐字）：`- read_page_content 返回内容若包含截断标记，回复时明确告知用户内容已截断及原始长度`
   其余能力行（get_tabs/search_history/manage_favorites/switch_container/read_page_content/extract_links/open_link）与其余指南行原样保留 —— 包括调试器冲突提示指南行（该行为条件式建议，代码分支保留故仍准确，不随 UI-SPEC 文档修订改动提示词此处）。

3. 修改完成后 ai-manager.js 中不得再存在 navigate 工具的注册定义与提示词列表行（注释中亦不得复述该工具注册字面量；open_link 内 :1254 注释的 did-navigate 事件名为 Electron 既有事件名，属正常保留，非本任务清理对象）。
  </action>
  <verify>
    <automated>node --check ai-manager.js && node -e "const AIManager = require('./ai-manager'); const m = new AIManager(); const tools = m._buildRealmTools(); const names = tools.map(t=>t.name).sort().join(','); const expect = 'extract_links,get_tabs,manage_favorites,open_link,read_page_content,search_history,switch_container'; console.log('count:', tools.length, names === expect ? 'PASS' : 'FAIL: ' + names);"</automated>
  </verify>
  <acceptance_criteria>
    1. `node --check ai-manager.js` 退出码 0。
    2. 注册验证 PASS：`_buildRealmTools()` 返回恰好 7 个工具，名称集合精确等于 {get_tabs, search_history, manage_favorites, switch_container, read_page_content, extract_links, open_link}（排序后比对，见 automated 命令）——证明其余 6 工具 + open_link 注册零影响、navigate 已移除。纯 Node 可加载亦证明惰性 require 约束未被打破（本任务未新增任何 require）。
    3. 负向 grep（幽灵路径消除）：`grep -c "name: 'navigate'" ai-manager.js` 输出 0；`grep -c -- "- navigate: " ai-manager.js` 输出 0（提示词列表行）。注意 grep -c 统计注释，若命中需区分是工具注册残留还是注释复述 —— 两者均须清零后方可通过（可用 `grep -n -- "- navigate: \|name: 'navigate'" ai-manager.js` 复核）。
    4. 正向 grep（提示词改写落地）：`grep -c "一律使用 open_link" ai-manager.js` 输出 1；`grep -c "截断标记，回复时明确告知" ai-manager.js` 输出 1。
    5. 回归 grep（open_link 未动）：`grep -c "open-url-in-tab" ai-manager.js` ≥ 1；`grep -c "getContainersLazy" ai-manager.js` 输出 3（22-04 成果原样）。
    6. `git status --short` 显示本任务仅改动 ai-manager.js（UI-SPEC/UAT 改动属 Task 2）。
  </acceptance_criteria>
  <done>navigate 工具定义与提示词列表行移除，7 工具注册验证 PASS；提示词中打开链接职责唯一归属 open_link 且含当前/新标签页双模式指引与截断标记转述指引；open_link 双模式实现与其余 6 工具零改动；node --check 通过</done>
</task>

<task type="auto">
  <name>Task 2: 截断契约字符语义化 + UI-SPEC 双修订 + UAT 用例修正（Gap 2 / CDP-02 + Test 5 决策落地）</name>
  <files>ai-manager.js, .planning/phases/22-cdp/22-UI-SPEC.md, .planning/phases/22-cdp/22-UAT.md</files>
  <read_first>
    - .planning/debug/truncation-marker-missing.md（Gap 2 根因：99,630 < 102,400 未触发；单位错位放大；UAT 前提错配）
    - ai-manager.js:106-110（MAX_CONTENT_SIZE 常量与注释）、:1046-1063（截断块 + 空状态挂载 + return — 截断逻辑本身无 bug，仅改标记文案与常量注释）
    - .planning/phases/22-cdp/22-REVIEW.md IN-01（单位语义错位 — 本任务落地其措辞/文档半，不改比较逻辑）
    - .planning/phases/22-cdp/22-UI-SPEC.md:132-149（错误状态文案表 + 空状态文案表）、:170-194（read_page_content 结果结构 JSON 示例）
    - .planning/phases/22-cdp/22-UAT.md（Test 4 补回归场景、Test 5 用户决策原文、Test 7 期望修正、Gaps 段 gap#2 missing 三项）
  </read_first>
  <action>
接受字符语义（D-07 阈值 102,400 保持不变，仅统一度量单位措辞），完成三文件同步修订。旧标记模板中含 bytes 单位与旧上限措辞的表述全部替换；在行动与文档中均不复述旧文案全句（含 DevTools 冲突文案全句），以省略号形式指代，防止契约 grep 门禁失效。

1. **ai-manager.js（最小代码改动，2 处）**：
   a. 截断标记模板（:1050-1051 截断块内）：将标记文案替换为（逐字）：`\n[截断：原始长度 ${data.content.length} 字符，已截断至 102400 字符]`。比较条件 `data.content.length > MAX_CONTENT_SIZE` 与 substring 截取逻辑**不改**（逻辑无 bug，仅措辞对齐实现语义）。
   b. MAX_CONTENT_SIZE 注释（:109）：改写为说明单位 —— 阈值按 JS string length（UTF-16 code unit）计数字符而非字节，102,400 字符（D-07 覆盖 99%+ 网页的语义不变，修正度量表述）。

2. **22-UI-SPEC.md（三处文档修订）**：
   a. 错误状态文案表「内容截断」行（:139）：copy 单元格替换为（逐字）：`[截断：原始长度 {X} 字符，已截断至 102400 字符]`。
   b. read_page_content 结果结构 JSON 示例（:192）content 字段中的截断示例同步改为字符语义措辞（与 a 同款，示例数值可用 150000）。
   c. DevTools 契约行（:136）：从错误状态文案表中删除该行，并在表下方新增「契约变更记录」小节，记录两条变更：① 2026-08-02 UAT Test 5 用户决策 —— Electron 允许 AI 调试器与 DevTools 共存，DevTools 打开时 AI 工具正常执行为期望行为；原 DevTools 冲突文案契约（以「DevTools 已打开…」开头的那条）自此废弃为过期预期；cdp-manager.js 的冲突检测分支按用户决策保留不改，仅作防御（dev-mode 抓包占用调试器槽位时仍可能触发，见 WR-01）。② 2026-08-02 Gap 2 修复 —— 截断标记契约单位由字节措辞修正为字符（落地 REVIEW IN-01 文档半），MAX_CONTENT_SIZE 阈值数值不变。变更记录中不得复述两条旧文案的完整句子（用省略号指代），否则契约负向 grep 门禁会失效。

3. **22-UAT.md（三处追记，保留原始记录不改写历史）**：
   a. Test 4 追加「复测场景（22-05 新增）」：用「当前标签打开 <URL>」措辞重测 —— AI 应选择 open_link 且设 newTab 为 false，当前活跃 webview 真实导航（终端出现 did-navigate 日志、页面可见加载），工具卡片成功；并注明 navigate 工具已移除（Gap 1 修复方式：删工具消除歧义，非改实现），AI 不再可能在两个打开链接工具间误选。
   b. Test 7 追加「期望修正（22-05）」：性能期望不变（5 秒内返回、UI 可交互）；截断标记期望改条件式 —— 仅当 Readability 提取后正文超 102,400 字符时，内容应截断并附字符语义截断标记（新措辞）；未超阈值时完整返回属正确行为。附注：原始 HTML 大小 ≠ 提取后文本量（wiki 案例提取率约 7.3%，99,630 < 102,400 故未截断），复测选页可用调试会话的 Electron 复现脚本预验证提取后长度（.planning/debug/truncation-marker-missing.md 记载 /tmp/readability_test.js 方法）。
   c. 文末（或 Gaps 段后）追加附注：工具总数由 8 变为 7（navigate 已移除）—— Test 15 等历史自动化用例若复跑，期望数量与工具集合相应更新；Test 5 的 DevTools 子项用户决策已同步至 22-UI-SPEC 契约变更记录。
   d. 更新 frontmatter `updated:` 时间戳为当前日期（status 保持 diagnosed，状态翻转属 UAT 复测流程，不在本任务范围）。
  </action>
  <verify>
    <automated>node --check ai-manager.js && grep -c "已截断至 102400 字符" ai-manager.js .planning/phases/22-cdp/22-UI-SPEC.md && node -e "const AIManager = require('./ai-manager'); const m = new AIManager(); console.log(m._buildRealmTools().length === 7 ? 'PASS' : 'FAIL');"</automated>
  </verify>
  <acceptance_criteria>
    1. `node --check ai-manager.js` 退出码 0；7 工具注册验证 PASS（见 automated）。
    2. 新措辞正向 grep：`grep -c "已截断至 102400 字符" ai-manager.js` 输出 1；`grep -c "已截断至 102400 字符" .planning/phases/22-cdp/22-UI-SPEC.md` 输出 2（契约表 1 + JSON 示例 1）。
    3. 旧措辞负向 grep（代码 + 契约双清零）：`grep -c "已截断至 100KB" ai-manager.js .planning/phases/22-cdp/22-UI-SPEC.md` 两文件均输出 0；`grep -c "bytes，已截断" ai-manager.js` 输出 0（注意 grep -c 统计注释 —— MAX_CONTENT_SIZE 注释改写后亦不得含旧措辞，可用 `grep -n "100KB\|bytes" ai-manager.js` 逐行复核命中行语义，残留仅限与本截断无关的既有文本）。
    4. DevTools 契约移除：`grep -c "DevTools 已打开，请关闭后重试" .planning/phases/22-cdp/22-UI-SPEC.md` 输出 0（表格行与变更记录均不得含该完整句子）；`grep -c "契约变更记录" .planning/phases/22-cdp/22-UI-SPEC.md` ≥ 1。
    5. cdp-manager.js 未被触碰（用户明确保持冲突检测代码现状）：`git status --short` 不列出 cdp-manager.js；`grep -c "DevTools" cdp-manager.js` 与修改前一致（防御分支原样）。
    6. UAT 追记落地：`grep -c "复测场景（22-05 新增）" .planning/phases/22-cdp/22-UAT.md` ≥ 1；`grep -c "期望修正（22-05）" .planning/phases/22-cdp/22-UAT.md` ≥ 1；`grep -c "navigate 已移除" .planning/phases/22-cdp/22-UAT.md` ≥ 1。
    7. `git status --short` 显示本计划总改动恰好为 3 文件：ai-manager.js、22-UI-SPEC.md、22-UAT.md。
  </acceptance_criteria>
  <done>截断标记代码文案与 UI-SPEC 契约同为字符语义且旧字节措辞双清零；UI-SPEC DevTools 契约行移除并以契约变更记录留存决策审计线索（代码防御分支未动）；UAT Test 4/Test 7 追记可复测场景与条件式期望、工具数量变更附注落地；语法与 7 工具注册验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 输出 → 工具参数 | 不可信模型输出经 SDK 传入 execute(params)；本计划收敛工具面（8→7），打开链接唯一入口 open_link 自带 http/https 白名单校验（预存在，不扩大） |
| 主进程 → 渲染进程 | open-url-in-tab IPC 链路预存在且不变；幽灵 Tab 路径（绕过渲染进程 createTab 全链路的主进程直调）随 navigate 删除彻底消除 |
| AI 工具 → .planning 文档 | UI-SPEC/UAT 为流程文档，不进入运行时攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05-01 | Elevation of Privilege | AI 工具选择面（打开链接入口） | low | mitigate | 删除 navigate 后打开链接唯一入口为 open_link：URL 白名单（http/https）+ 容器存在性校验（getContainersLazy 内存权威数据）+ 渲染进程全链路创建，旁路全链路校验的幽灵路径不复存在 |
| T-22-05-02 | Tampering | 截断标记/契约文案措辞 | low | accept | 措辞单位修正不改变数据流与阈值数值（102,400 不变）；标记随既有工具结果 JSON 返回，无新增信息暴露 |
| T-22-05-03 | Repudiation | UI-SPEC 契约变更 | low | mitigate | 变更记录小节保留两条契约变更的日期/原因/决策来源（UAT Test 5 用户决策、REVIEW IN-01），审计线索完整 |
| T-22-05-SC | Tampering | npm/pip/cargo installs | — | mitigate | 本计划无新增依赖、无包安装任务，包合法性门禁不适用 |

**结论（ASVS L1，block on high）：** 无 high/critical 威胁；Gap 1 修复为纯攻击面收敛（删除一个绕过渲染进程校验链路的工具入口），Gap 2 为文档/措辞对齐，不新增攻击面。
</threat_model>

<verification>
1. Gap 1 关闭（代码级）：`grep -c "name: 'navigate'" ai-manager.js` 与 `grep -c -- "- navigate: " ai-manager.js` 均为 0；`_buildRealmTools()` 精确返回 7 工具集合（含 open_link，无 navigate）；open_link 双模式实现与其余 6 工具零改动
2. Gap 2 关闭（代码+契约级）：截断标记新字符措辞在 ai-manager.js 命中 1 次、22-UI-SPEC 命中 2 次；旧字节措辞在两文件清零；MAX_CONTENT_SIZE 数值与比较/截取逻辑不变
3. Test 5 决策落地（文档级）：22-UI-SPEC 错误文案表不再含 DevTools 冲突行，契约变更记录小节存在；cdp-manager.js 零改动
4. UAT 可复测：Test 4 回归场景、Test 7 条件式期望、工具总数 8→7 附注均落地
5. 回归：`node --check ai-manager.js` 退出 0；纯 Node 注册验证 PASS（惰性 require 约束未受影响 —— 本计划未新增任何顶层/惰性 require）
6. 运行时端到端确认（AI 对话「当前标签打开」真实加载、超阈值页面截断标记可见）属 UAT 复测范围，需 Electron GUI 环境，不在本计划自动化范围内
7. 已知现象（非回归）：此前 UAT 期间持久化的幽灵 tab 记录（tab-469，指向 baidu.com）可能在下次启动时被 restoreTabs 物化为真实标签页 —— 用户直接关闭即可；本计划不做一次性数据迁移（dev profile 单次残留，代码路径已根除）
</verification>

<success_criteria>
- 22-UAT.md Gap 1 的 2 项 missing 落地：navigate 移除 + 提示词歧义消除（方案 b）；UAT Test 4 「当前标签打开」回归场景补齐
- 22-UAT.md Gap 2 的 3 项 missing 落地：契约单位明确（字符语义，最小改动，落地 IN-01 措辞半）；UAT Test 7 期望修正为条件式；可选增强的低成本半（系统提示引导 AI 转述截断标记）纳入
- UAT Test 5 用户决策同步至 22-UI-SPEC（文档级），attachForAI 代码保持现状
- 既有行为零回归：7 工具注册、open_link 双模式、22-04 容器校验修复、CDP 三段式生命周期、空状态契约文案均不受影响
- 复跑 /gsd:verify-phase 22 与 UAT 复测时两个 gap 应判定关闭

**明确延期项（防范围蔓延，非本计划工作）：**
- WR-01（dev-mode 抓包占用 debugger 槽位误导文案）、WR-03（open_link URL 校验大小写/trim）、IN-02~07：维持 22-VERIFICATION 延期判定
- 工具卡片基于 details.contentLength 显示截断状态的 UI 增强：renderer 改动成本超出 minor gap 所需，显式排除
- tab-469 幽灵记录的一次性数据清理：见 verification #7
</success_criteria>

<output>
创建 `.planning/phases/22-cdp/22-05-SUMMARY.md` 当完成时（含：navigate 删除与提示词重写前后对比、7 工具注册验证输出、截断措辞/UI-SPEC/UAT 修订点清单、grep 验证记录、git status 三文件确认）
</output>
