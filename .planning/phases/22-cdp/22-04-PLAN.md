---
phase: 22
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-manager.js
autonomous: true
gap_closure: true
requirements:
  - CDP-02
  - CDP-03
  - CDP-04

must_haves:
  truths:
    - "全新 profile（realm-config.json 无 containers 键）下，AI 调用 open_link 不传 containerId 的默认路径成功打开链接（Gap #1 / CR-01 / CDP-04）"
    - "switch_container 在全新 profile 下同样可用（Phase 20 预存在同款反模式，随 Gap #1 一并消除）"
    - "extract_links 过滤后 0 条链接时，结果携带契约文案「未找到有效链接（仅保留 http/https 协议）」（Gap #2 / CDP-03）"
    - "read_page_content 正文为空时，结果携带契约文案「页面无可读内容，可能是纯应用页面或空白页」（Gap #2 / CDP-02）"
  artifacts:
    - "ai-manager.js open_link 与 switch_container 容器校验改用 containerManager.getContainers() 内存权威数据"
    - "ai-manager.js 中「containers 键 + 空数组默认值」的 configStore 读取零残留"
  key_links:
    - "open_link/switch_container 容器校验 → containerManager.getContainers()（initContainers 内存 Map，含 DEFAULT_CONTAINERS）"
    - "extract_links 返回 → data.total === 0 时挂载 data.message 空状态契约文案"
    - "read_page_content 返回 → data.content 为空时挂载 data.message 空状态契约文案"
---

<objective>
关闭 22-VERIFICATION.md 判定的 2 个 gap（status: gaps_found），不重开本阶段其他工作。

Gap #1（BLOCKER，CR-01，阻碍 CDP-04 验收）：open_link 容器存在性校验读取非权威数据源。container-manager 的 initContainers()（container-manager.js:65）以 DEFAULT_CONTAINERS 为默认值读入内存 Map 但从不写回 store；全库 set('containers') 仅发生在容器 CRUD 三处（139/199/245）。全新 profile 下 store 无 containers 键 → 以空数组兜底的读取返回 [] → find('default') 失败 → 抛出事实性错误的「指定容器不存在或已删除」。AI 最常见调用路径（不传 containerId，默认当前容器）在新用户环境 100% 失败。switch_container（ai-manager.js:925）为 Phase 20 预存在同款反模式，一并修复。

Gap #2（minor，WR-02）：22-UI-SPEC 契约 8 条文案只实现 6 条错误文案，2 条空状态文案未挂载：extract_links 0 链接时、read_page_content 正文为空时。

Purpose: 让「AI 在容器中打开链接」对全新 profile 用户成立，并补齐 UI-SPEC 文案契约。
Output: ai-manager.js 单文件外科修复（2 处校验数据源 + 2 条空状态文案），无新增符号导出。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-cdp/22-VERIFICATION.md
@.planning/phases/22-cdp/22-UI-SPEC.md
@.planning/phases/22-cdp/22-REVIEW.md
@ai-manager.js
@container-manager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 open_link / switch_container 容器校验数据源（Gap #1 / CR-01）</name>
  <files>ai-manager.js</files>
  <read_first>
    - ai-manager.js:42-57（getActiveWebviewContentsIdLazy 惰性 require 模式及其 JSDoc 原因说明 — 本次修复必须沿用同款模式）
    - ai-manager.js:912-949（switch_container execute 全文，校验点在 925-929）
    - ai-manager.js:1156-1177（open_link execute 参数校验段，校验点在 1172-1176）
    - container-manager.js:60-96（initContainers 只读不写回 + getContainers() 返回纯配置字段数组 {id,name,color,icon,phone,email,notes}，module.exports:294 已导出）
    - .planning/phases/22-cdp/22-VERIFICATION.md（Gaps #1 的 missing 三项 — 修复目标定义）
    - .planning/phases/22-cdp/22-REVIEW.md（CR-01 根因描述与修复方向）
  </read_first>
  <action>
将两处容器存在性校验从 electron-store 读取改为 containerManager 内存权威数据（initContainers 已含 DEFAULT_CONTAINERS，窗口创建时已初始化）。

**强制约束 — 禁止顶层 require container-manager**：已实测 `node -e "require('./container-manager')"` 在纯 Node 下崩溃（electron-store 构造时 app.getPath 为 undefined），若在 ai-manager.js 顶层 require 会打破既有的纯 Node 注册验证链路（22-VERIFICATION 自动检查 #3：`node -e "require('./ai-manager')"`）。必须沿用 ai-manager.js:55-57 getActiveWebviewContentsIdLazy 的既有惰性 require 模式（其 JSDoc 42-54 已说明同类原因：纯 Node 下 cookie-manager 依赖链不可加载；Electron 主进程运行时直接命中模块缓存，无循环依赖风险）。

1. **新增惰性辅助函数**：在 getActiveWebviewContentsIdLazy 定义之后（ai-manager.js:57 附近）添加 `getContainersLazy()`，函数体为单行返回（与既有 `return require('./ipc-handlers').getActiveWebviewContentsId();` 同款写法）：`return require('./container-manager').getContainers();`。添加 JSDoc 说明：容器列表的权威来源是 container-manager 内存 Map（initContainers 以 DEFAULT_CONTAINERS 兜底、从不写回 store），纯配置字段、不含 session 对象；惰性 require 原因与上方 ipc-handlers 一致（顶层加载会在纯 Node 注册验证时崩溃）。

2. **修复 open_link（ai-manager.js:1172-1176）**：该校验守护的正是 D-10 默认容器路径（不传 containerId 时取当前活跃容器、'default' 兜底）——全新 profile 下 100% 命中的失败点。删除当前两行 —— 声明 `containers` 变量并以空数组为默认值从 this.configStore 读 containers 键的那行，以及随后的 `containers.find(...)` 行。替换为单行：`const container = getContainersLazy().find(c => c.id === containerId);`。其后的 `if (!container) { throw new Error('指定容器不存在或已删除'); }`（UI-SPEC 契约文案）原样保留。目标效果等价于 REVIEW 建议的 `containerManager.getContainers().find(c => c.id === containerId)`，仅经惰性 helper 间接调用。

3. **修复 switch_container（ai-manager.js:925-926）**：同样删除该处声明 `containers` 变量并以空数组默认值读 store 的行及 `containers.find(...)` 行，替换为 `const container = getContainersLazy().find(c => c.id === containerId);`。其后 `throw new Error(`容器不存在: ${containerId}`)`（Phase 20 预存在文案，非 UI-SPEC 契约项）原样保留；932 行 `windowManager.switchContainer(mainWindow.id, containerId, container)` 与 943 行 `container.name` 的下游消费不受影响（getContainers() 返回 {id,name,color,icon,phone,email,notes} 纯配置对象，与原 store 数据形状一致，window-manager.js:77-92 仅使用配置字段）。

4. 修改完成后 ai-manager.js 中不得再存在「containers 键 + 空数组默认值」的 configStore 读取（全文件仅此两处，22-VERIFICATION 已 grep 确认无其他写入/读取点）。
  </action>
  <verify>
    <automated>node --check ai-manager.js && node -e "const AIManager = require('./ai-manager'); const m = new AIManager(); const tools = m._buildRealmTools(); console.log('tools:', tools.length, tools.map(t=>t.name).includes('open_link') && tools.map(t=>t.name).includes('switch_container') ? 'PASS' : 'FAIL');"</automated>
  </verify>
  <acceptance_criteria>
    1. `node --check ai-manager.js` 退出码 0。
    2. 注册验证 PASS：`_buildRealmTools()` 返回 8 个工具且含 open_link / switch_container（守护惰性 require 约束 —— 若改为顶层 require，此命令会在 require 阶段即崩溃）。
    3. 正向 grep（权威数据源已接入）：`grep -c "getContainersLazy" ai-manager.js` ≥ 3（helper 定义 1 + open_link/switch_container 调用各 1）；`grep -c "require('./container-manager')\.getContainers()" ai-manager.js` 输出 1（helper 体内的权威调用）。
    4. 负向 grep（反模式消除）：`grep -c "get('containers', \[\])" ai-manager.js` 输出 0。注意 grep -c 统计注释，若 JSDoc/注释中复述了该字面量需改写措辞后重新计数（可用 `grep -v '^\s*\*' ai-manager.js | grep -c "get('containers', \[\])"` 排除注释行复核）。
    5. 全新 profile 行为模拟（纯 Node，Module._load stub）：node 单脚本先 stub electron（session.fromPartition 返回空对象、app.getPath 返回临时目录 —— cookie-manager 顶层调用依赖此 stub）与 electron-store（可 new 的 class，get 恒返回传入的默认值、set 为空操作）两个模块，再 require('./container-manager') 并调用 initContainers()，断言 `getContainers().find(c => c.id === 'default')` 为真（新数据源在 store 无 containers 键时仍可找到 default），同时断言 `[].find(c => c.id === 'default') === undefined`（旧数据源失败路径，记录 bug 对比）。脚本输出 "FRESH-PROFILE PASS" 并以退出码 0 结束方可通过。
  </acceptance_criteria>
  <done>全新 profile 模拟下容器校验找到 default（FRESH-PROFILE PASS）；open_link 与 switch_container 两处校验均走 containerManager 内存权威数据；反模式字面量零残留；8 工具注册验证通过；node --check 通过</done>
</task>

<task type="auto">
  <name>Task 2: 补齐 UI-SPEC 两条空状态契约文案（Gap #2 / WR-02）</name>
  <files>ai-manager.js</files>
  <read_first>
    - .planning/phases/22-cdp/22-UI-SPEC.md:143-148（「空状态文案」契约表 — 两条字符串必须逐字一致）
    - ai-manager.js:1026-1037（read_page_content 的 JSON.parse 与 return 位置）
    - ai-manager.js:1123-1128（extract_links 的 JSON.parse 与 return 位置）
    - .planning/phases/22-cdp/22-VERIFICATION.md（Gaps #2 的 missing 两项 — 修复目标定义）
  </read_first>
  <action>
在两个工具的返回数据上按条件挂载 22-UI-SPEC「空状态文案」契约 message（字符串逐字取自契约，不得改动任何字符，含全角括号）：

1. **extract_links（ai-manager.js:1123 之后、1125 return 之前）**：在 `const data = JSON.parse(evalResult.result.value);` 之后插入条件挂载 —— 当 `data.total === 0` 时设置 `data.message = '未找到有效链接（仅保留 http/https 协议）';`。data.total 由页面内脚本返回（uniqueLinks.length），0 链接是合法结果而非错误，不 throw、不改变 return 结构（details.totalLinks 逻辑不变）。

2. **read_page_content（ai-manager.js:1026-1032 截断块之后、1034 return 之前）**：在 100KB 截断判断块之后插入条件挂载 —— 当 `!data.content` 时设置 `data.message = '页面无可读内容，可能是纯应用页面或空白页';`。注意顺序：空内容与截断互斥（空内容不可能超 100KB），挂载在截断块之后不会相互干扰；不 throw、不改变 return 结构。

3. 两条 message 直接附加在 data 对象上，经既有 `JSON.stringify(data, null, 2)` 随结果返回，工具卡片与 AI 上下文均可见；无 UI 侧改动（UI-SPEC 空状态契约的呈现载体就是工具结果 JSON）。
  </action>
  <verify>
    <automated>node --check ai-manager.js && grep -c "未找到有效链接（仅保留 http/https 协议）" ai-manager.js && grep -c "页面无可读内容，可能是纯应用页面或空白页" ai-manager.js</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "未找到有效链接（仅保留 http/https 协议）" ai-manager.js` 输出 1（与 22-UI-SPEC.md:147 逐字一致）。
    2. `grep -c "页面无可读内容，可能是纯应用页面或空白页" ai-manager.js` 输出 1（与 22-UI-SPEC.md:148 逐字一致）。
    3. 条件挂载位置正确：`grep -n "data.total === 0" ai-manager.js` 与 `grep -n "!data.content" ai-manager.js` 各命中且位于对应工具的 JSON.parse 之后、return 之前。
    4. `node --check ai-manager.js` 退出码 0。
    5. 注册验证 PASS：`node -e "const AIManager = require('./ai-manager'); const m = new AIManager(); console.log(m._buildRealmTools().length === 8 ? 'PASS' : 'FAIL');"` 输出 PASS。
  </acceptance_criteria>
  <done>两条契约文案在 ai-manager.js 各命中 1 次且按正确条件挂载；22-UI-SPEC 契约 8 条文案（6 错误 + 2 空状态）在代码库全部可 grep 命中；语法与工具注册验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 输出 → 工具参数 | 不可信模型输出经 SDK 传入 execute(params)，containerId/url 均为攻击面（预存在，本修复不扩大） |
| 主进程 → electron-store 磁盘文件 | realm-config.json 本地可读写；数据源从磁盘 store 切换到内存 Map 后，该校验路径不再受磁盘文件篡改影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04-01 | Tampering | 容器校验数据源（open_link/switch_container） | low | accept | 修复本身即收敛风险：内存 Map 由 initContainers 在启动时建立，运行期不受 realm-config.json 磁盘篡改影响；getContainers() 仅返回纯配置字段（无 session/partition 对象），信息暴露面不大于原 store 读取 |
| T-22-04-02 | Information Disclosure | 容器存在性 oracle（错误文案区分存在/不存在） | low | accept | 单用户本地应用；AI 经 get_tabs 等既有工具本可获知容器列表，oracle 无增量泄露；错误文案保持契约原样不新增细节 |
| T-22-04-03 | DoS | getContainersLazy 每次调用惰性 require | low | accept | Node 模块缓存使重复 require 为 O(1) 哈希查找；initContainers 内存 Map 读取无 I/O |
| T-22-04-SC | Tampering | npm/pip/cargo installs | — | mitigate | 本计划无新增依赖、无包安装任务，包合法性门禁不适用 |

**结论（ASVS L1，block on high）：** 无 high/critical 威胁；修复消除了一条事实性错误的错误路径（用户被告知容器「不存在或已删除」而实际存在），不新增攻击面。WR-01/WR-03 为已知 low 级警告，见「明确延期项」。
</threat_model>

<verification>
1. Gap #1 关闭：全新 profile 模拟脚本输出 FRESH-PROFILE PASS（store 无 containers 键时 getContainers() 仍可 find 'default'）
2. 反模式消除：`grep -c "get('containers', \[\])" ai-manager.js` 为 0（必要时排除注释行复核）
3. Gap #2 关闭：两条空状态契约文案在 ai-manager.js 各命中 1 次，22-UI-SPEC 全部 8 条文案代码库可 grep
4. 回归：`node --check ai-manager.js` 退出 0；`_buildRealmTools()` 返回 8 工具（含 open_link/switch_container/read_page_content/extract_links）
5. 运行时端到端验证（真实窗口中 open_link 双模式 + 全新 profile 回归）属 22-VERIFICATION Human Verification #3，随本修复后的 UAT 一并执行，不在本计划自动化范围内
</verification>

<success_criteria>
- 22-VERIFICATION.md Gaps #1 的全部 3 项 missing 落地（open_link 改内存权威数据、switch_container 同步修复、全新 profile 模拟验证通过）
- 22-VERIFICATION.md Gaps #2 的全部 2 项 missing 落地（两条空状态文案按条件挂载且逐字一致）
- 既有行为零回归：8 工具注册、错误文案契约 6 条、CDP 三段式生命周期均不受影响
- 复跑 /gsd:verify-phase 22 时两个 gap 应判定关闭（Human Verification #1-6 仍需 GUI 环境另行执行）
</success_criteria>

<artifacts_produced>
None — gap closure modifies existing symbols only.

本计划仅修改 ai-manager.js 中既有工具的 execute 内部实现（2 处校验数据源 + 2 处条件挂载 message），并新增一个模块私有辅助函数 getContainersLazy（不导出）。无新增文件、无新增模块导出、无新增 IPC 通道。

**明确延期项（防范围蔓延，非本计划工作）：**
- WR-01（cdp-manager.js:315-317 dev-mode 抓包占用 debugger 槽位时报误导性文案）：涉及 cdp-manager.js，超出本计划单文件范围，留后续迭代。
- WR-03（open_link URL 校验大小写敏感/无 trim）：与 Gap #1 不同关注点（URL 校验 vs 容器校验），VERIFICATION 标记为 recommended 非 gap，留后续迭代。
- IN-01~07：按 22-VERIFICATION.md 维持 info 级延期。
</artifacts_produced>

<output>
创建 `.planning/phases/22-cdp/22-04-SUMMARY.md` 当完成时（含：两处校验修改前后对比、两条文案挂载位置、FRESH-PROFILE 模拟脚本与输出、grep 验证记录）
</output>
