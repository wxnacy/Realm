---
phase: 09-共享收藏数据库
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - favorites-manager.js
  - main.js
autonomous: true
requirements:
  - FAV-07
  - FAV-09
  - FAV-10

must_haves:
  truths:
    - 应用启动时自动执行 migrateToGlobal()，drop 所有旧 favorites_* 表并创建全局 favorites 表
    - favorites-manager.js 所有 CRUD 函数不再接收 containerId 参数，直接操作全局 favorites 表
    - main.js 中 handleFavoritesApi 的所有端点不再从请求中读取 containerId
    - favoritesManager.dropTable() 和 sanitizeContainerId() 已被删除
    - 同一 URL 全局唯一去重（UNIQUE 约束保留）
  artifacts:
    - favorites-manager.js（重构后）
    - main.js（API 端点移除 containerId）
  key_links:
    - favoritesManager.initDatabase() 之后调用 favoritesManager.migrateToGlobal()
    - ensureTable() 不再接收参数，固定操作全局 favorites 表
    - addRecord() 返回 duplicate 错误时 message 保持 '已收藏过该页面'
---

<objective>
重构 favorites-manager.js 和 main.js 中的收藏 API，将收藏数据从按容器隔离（favorites_{containerId} 分表）改为全局共享单一表。

Purpose: 收藏数据全局共享，与容器生命周期解耦。所有容器读写同一份收藏数据。
Output: 重构后的 favorites-manager.js（无 containerId 依赖）+ 更新后的 main.js API 端点 + 启动时自动迁移。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-共享收藏数据库/09-CONTEXT.md
@.planning/phases/09-共享收藏数据库/09-RESEARCH.md
@favorites-manager.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 favorites-manager.js — 移除 per-container 逻辑，新增全局表 + 迁移函数</name>
  <files>favorites-manager.js</files>
  <behavior>
    - Test: ensureTable() 创建全局 favorites 表（无参数），表结构包含 id, url, title, favicon_url, created_at，url 有 UNIQUE 约束
    - Test: addRecord({url, title, faviconUrl}) 成功插入返回 {id: Number}，重复 URL 返回 {error: 'duplicate', message: '已收藏过该页面'}
    - Test: addRecord 后 listRecords({offset:0, limit:50}) 能返回刚插入的记录
    - Test: updateRecord(id, {title}) 更新标题返回 true，不存在的 id 返回 false
    - Test: deleteRecord(id) 删除存在的记录返回 true，不存在的返回 false
    - Test: deleteRecords([id1, id2]) 批量删除返回删除数量
    - Test: searchRecords({keyword}) 按标题和 URL 模糊匹配
    - Test: checkUrl(url) 已收藏的 URL 返回 {id, title, favicon_url}，未收藏返回 null
    - Test: getCount() 返回总记录数
    - Test: migrateToGlobal() 执行后旧 favorites_xxx 表被删除，全局 favorites 表存在且为空
    - Test: 模块导出不包含 dropTable 和 sanitizeContainerId
  </behavior>
  <action>
对 favorites-manager.js 执行以下重构（per D-04, D-06, D-07, D-08, D-09）：

1. **删除函数**：移除 `sanitizeContainerId()`（行 66-74）和 `dropTable()`（行 263-268）。

2. **重写 ensureTable()**：移除 containerId 参数，固定创建全局 `favorites` 表。表结构沿用 Phase 7 字段（id, url, title, favicon_url, created_at），保留 UNIQUE(url) 约束（per D-05, D-06）。创建索引 `idx_favorites_created_at` 和 `idx_favorites_url`。

3. **新增 migrateToGlobal()**（per D-01, D-02）：
   - 查询 `sqlite_master` 中所有 `type='table' AND name LIKE 'favorites_%'` 的表
   - 逐个 DROP TABLE（排除全局 `favorites` 表本身，虽然不应存在名为 favorites_ 的全局表）
   - 调用 `ensureTable()` 创建全局表
   - 使用 `console.log('[Realm] 收藏表已迁移到全局模式')` 记录日志

4. **修改所有 CRUD 函数**（per D-08）：移除 containerId 参数，直接操作全局 `favorites` 表：
   - `addRecord({ url, title, faviconUrl })` — 移除 containerId 参数，不再调用 sanitizeContainerId，直接 INSERT INTO favorites
   - `updateRecord(id, { title })` — 移除 containerId 参数
   - `deleteRecord(id)` — 移除 containerId 参数
   - `deleteRecords(ids)` — 移除 containerId 参数
   - `listRecords({ offset, limit })` — 移除 containerId 参数
   - `searchRecords({ keyword, offset, limit })` — 移除 containerId 参数
   - `checkUrl(url)` — 移除 containerId 参数
   - `getCount()` — 移除 containerId 参数

5. **更新模块导出**：移除 `dropTable`，新增 `migrateToGlobal`。

6. **更新文件头注释**：移除"每个容器使用独立的 SQLite 表（favorites_{containerId}）实现数据隔离"描述，改为"使用单一全局 favorites 表存储所有收藏数据"。

所有函数内部不再调用 `sanitizeContainerId()` 或 `ensureTable(containerId)`。`ensureTable()` 在每次 CRUD 操作前仍然调用（`CREATE TABLE IF NOT EXISTS` 是幂等的），但不再接收参数。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm && node -e "
const fm = require('./favorites-manager');
console.log('migrateToGlobal exists:', typeof fm.migrateToGlobal === 'function');
console.log('dropTable removed:', typeof fm.dropTable === 'undefined');
console.log('sanitizeContainerId not exported:', typeof fm.sanitizeContainerId === 'undefined');
console.log('PASS: module exports verified');
"</automated>
  </verify>
  <done>favorites-manager.js 所有 CRUD 函数签名无 containerId 参数；ensureTable() 无参数；migrateToGlobal() 已添加；dropTable() 和 sanitizeContainerId() 已删除；模块导出正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 main.js — API 端点移除 containerId + 注册迁移调用</name>
  <files>main.js</files>
  <behavior>
    - Test: GET /api/favorites/list 不再读取 containerId 查询参数，直接调用 favoritesManager.listRecords({offset, limit})
    - Test: GET /api/favorites/search 不再读取 containerId 查询参数，直接调用 favoritesManager.searchRecords({keyword, offset, limit})
    - Test: GET /api/favorites/check 不再读取 containerId 查询参数，直接调用 favoritesManager.checkUrl(url)
    - Test: POST /api/favorites/add 从 body 中只读取 url, title, faviconUrl，调用 favoritesManager.addRecord({url, title, faviconUrl})
    - Test: POST /api/favorites/update 从 body 中只读取 id, title，调用 favoritesManager.updateRecord(id, {title})
    - Test: POST /api/favorites/delete 从 body 中只读取 id，调用 favoritesManager.deleteRecord(id)
    - Test: POST /api/favorites/delete-batch 从 body 中只读取 ids，调用 favoritesManager.deleteRecords(ids)
    - Test: favoritesManager.initDatabase() 之后紧跟 favoritesManager.migrateToGlobal() 调用
  </behavior>
  <action>
修改 main.js 中的 handleFavoritesApi 函数（行 299-367）和数据库初始化区域（行 626-633），per D-07：

**handleFavoritesApi 函数修改：**

1. GET /api/favorites/list（行 314-319）：移除 `const containerId = reqUrl.searchParams.get('containerId') || ''`，调用改为 `favoritesManager.listRecords({ offset, limit })`

2. GET /api/favorites/search（行 322-328）：移除 containerId 读取，调用改为 `favoritesManager.searchRecords({ keyword, offset, limit })`

3. GET /api/favorites/check（行 331-335）：移除 containerId 读取，调用改为 `favoritesManager.checkUrl(url)`

4. POST /api/favorites/add（行 338-341）：从 body 中只解构 `{ url, title, faviconUrl }`（移除 containerId），调用改为 `favoritesManager.addRecord({ url, title, faviconUrl })`

5. POST /api/favorites/update（行 344-347）：从 body 中只解构 `{ id, title }`（移除 containerId），调用改为 `favoritesManager.updateRecord(id, { title })`

6. POST /api/favorites/delete（行 350-353）：从 body 中只解构 `{ id }`（移除 containerId），调用改为 `favoritesManager.deleteRecord(id)`

7. POST /api/favorites/delete-batch（行 356-359）：从 body 中只解构 `{ ids }`（移除 containerId），调用改为 `favoritesManager.deleteRecords(ids)`

**数据库初始化区域修改（行 626-633）：**

在 `favoritesManager.initDatabase()` 之后、`frequentSitesManager.initDatabase()` 之前，新增一行：
`favoritesManager.migrateToGlobal();`
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm && grep -c "containerId" main.js | head -5 && echo "---" && grep -n "migrateToGlobal" main.js</automated>
  </verify>
  <done>main.js 中所有 /api/favorites/* 端点不再读取或传递 containerId；migrateToGlobal() 在 initDatabase() 之后被调用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → API | 收藏页面通过 HTTP API 访问数据，token 鉴权 |
| main process → SQLite | favorites-manager 直接操作数据库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | favorites-manager.js SQL 注入 | low | accept | 已移除动态表名拼接（sanitizeContainerId），全局表名固定为 favorites，无用户输入拼接到 SQL |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | 本阶段无新增依赖 |
</threat_model>

<verification>
## 整体验证

1. 启动应用后，检查数据库中全局 favorites 表存在，旧 favorites_xxx 表已被删除
2. 收藏一个页面，验证记录写入全局 favorites 表
3. 切换到另一个容器，收藏页面后在收藏列表中能看到两个容器的收藏
4. 尝试收藏同一 URL，验证返回"已收藏过该页面"提示
5. 删除容器后，收藏数据仍然保留
</verification>

<success_criteria>
- favorites-manager.js 所有函数签名无 containerId 参数
- main.js API 端点不再读取/传递 containerId
- migrateToGlobal() 在应用启动时自动执行
- dropTable() 和 sanitizeContainerId() 已从代码中移除
</success_criteria>

<output>
Create `.planning/phases/09-共享收藏数据库/09-01-SUMMARY.md` when done
</output>
