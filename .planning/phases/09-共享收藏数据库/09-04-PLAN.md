---
phase: 09-共享收藏数据库
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified: ["src/renderer.js", "scripts/test-ipc-favorites.js"]
autonomous: true
gap_closure: true
requirements: ["FAV-09"]
must_haves:
  truths:
    - "realm://newtab 等内置 URL 收藏后，刷新页面星标保持实心（did-navigate → checkBookmarkStatus → favoritesCheck 精确匹配命中），与外部 URL 行为一致"
    - "about:blank 与空 url 仍走早退路径，不触发 favorites 表查询（回归护栏）"
    - "收藏列表双击 realm:// 条目可在新 Tab 打开对应内部页面（相邻问题一并修复）"
    - "外部 http(s) URL 的收藏、星标回显、双击打开行为不回归"
  artifacts:
    - "src/renderer.js checkBookmarkStatus 早退守卫仅排除空 url 与 about:blank"
    - "src/renderer.js handleOpenUrlInTab scheme 白名单含 realm://"
    - "scripts/test-ipc-favorites.js 含 realm:// URL 的 favorites:check 用例"
  key_links:
    - "did-navigate 的 displayUrl（httpUrlToRealm 归一化为 'realm://newtab'，container/token 查询参数已剥离）与 favorites 表存储 url 字符串完全一致 → checkUrl 精确匹配命中"
    - "favorites-page dblclick → window.open → main.js isAllowedWebUrl（main.js:65 已放行 realm://）→ open-url-in-tab → handleOpenUrlInTab（修复后放行）→ createTab → createWebviewForTab realmUrlToHttp 加载内部页面"
---

<objective>
关闭 09-UAT.md 中 severity=major 的唯一 gap：realm://newtab 等内置 URL 收藏后刷新星标不保持实心。根因已经 debug agent 以纯代码证据 100% 确认：checkBookmarkStatus（src/renderer.js:219）的早退守卫对所有 realm:// URL 在 DB 查询之前短路，强制星标为空心，而保存路径无对应守卫（存/查不对称）。该守卫源自 Phase 07-02 commit 93778ab，其「内部页面不显示收藏状态」语义已被 Phase 09 全局收藏 truth 推翻，属过时设计残留。

同时修复 debug 文档标记的相邻问题：收藏列表双击 realm:// 条目经 window.open → 主进程转发 open-url-in-tab → handleOpenUrlInTab（renderer.js:1761 仅放行 http(s)）被拒绝，内置收藏条目无法从列表打开。两者同属 realm:// URL 星标/列表 UX，合并到一个 plan。

Purpose: 让内置页面与外部页面在收藏行为上完全一致（FAV-09 全局共享语义的最后一公里）
Output: 修复后的 src/renderer.js + 扩展 realm:// 用例的 scripts/test-ipc-favorites.js
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-共享收藏数据库/09-UAT.md
@.planning/debug/realm-newtab-star-not-persistent.md
@src/renderer.js
@scripts/test-ipc-favorites.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除 checkBookmarkStatus 守卫中的 realm:// 分支</name>
  <files>src/renderer.js</files>
  <action>
    修改 src/renderer.js 中 checkBookmarkStatus 函数（当前第 218-236 行）的早退守卫（第 219 行）。

    现状：守卫条件为「空 url、realm 协议前缀、about:blank」三者任一即早退，清空 state.isCurrentPageBookmarked / currentBookmarkId / currentBookmarkTitle 并 updateStarButton(false)。第 220 行注释「内部页面不显示收藏状态」已过时（Phase 07-02 残留，与 Phase 09 全局收藏语义冲突）。

    改为：守卫仅排除空 url 与 about:blank 两种情况，移除 realm 协议前缀判断分支。早退体内的 state 清空与 updateStarButton(false) 逻辑保持不变。

    <!-- planner-discipline-allow: startsWith('realm://') -->

    要求：
    - 删除第 220 行「内部页面不显示收藏状态」注释，替换为准确描述新守卫语义的注释（如「about:blank 与空 URL 不查询收藏状态」），注释中不得提及被移除的协议分支或其字面量（区域 grep 门禁会统计注释）。
    - 不得改动函数其余部分（favoritesCheck 调用、state 赋值、catch 分支）。
    - 不得触碰文件内其他合法的 realm:// 使用点（renderer.js:161/179/198/601/669/732 等），尤其是 did-navigate 中的历史记录过滤（renderer.js:669，D-23 决策：realm:// 不记录历史，必须保留）。
    - 修复后，did-navigate（renderer.js:665）以归一化 displayUrl（'realm://newtab'，查询参数已被 httpUrlToRealm 剥离）调用本函数时，将直接走 favoritesCheck 精确匹配路径；该字符串与 favorites 表存储形式完全一致（debug 文档已证实），无需任何额外归一化。
  </action>
  <verify>
    <automated>test "$(sed -n '/^async function checkBookmarkStatus/,/^}/p' src/renderer.js | grep -c "startsWith('realm://')")" = "0" && test "$(sed -n '/^async function checkBookmarkStatus/,/^}/p' src/renderer.js | grep -c 'about:blank')" -ge 1 && node scripts/test-ipc-favorites.js</automated>
  </verify>
  <done>
    checkBookmarkStatus 函数体内不再出现 realm 协议前缀判断（区域 grep 计数为 0，含注释）；about:blank 早退保留；既有 IPC 冒烟测试 8/8 PASS（确认后端链路未被触碰）。
  </done>
</task>

<task type="auto">
  <name>Task 2: handleOpenUrlInTab 放行 realm:// scheme（相邻问题）</name>
  <files>src/renderer.js</files>
  <action>
    修改 src/renderer.js 中 handleOpenUrlInTab 函数（当前第 1760-1786 行）的 scheme 白名单（第 1761 行）。

    背景链路：favorites-page.js:282 双击收藏条目调用 window.open(record.url)；当 record.url 为 'realm://newtab' 时，主进程 setWindowOpenHandler（main.js:121）拦截 → notifyOpenUrlInTab → isAllowedWebUrl（main.js:65-67）已放行 realm:// → 转发 open-url-in-tab 到渲染进程 → 但 handleOpenUrlInTab 第 1761 行的正则仅放行 http/https，拒绝 realm:// 并打 warn 日志。主进程与渲染进程白名单不对称，渲染端是唯一拦截层。

    改为：第 1761 行的 scheme 校验在保留 http/https 的基础上追加放行 realm 协议（与主进程 isAllowedWebUrl 对齐；与 createWebviewForTab 的 WR-9 白名单 renderer.js:596 对齐，后者已放行 realm://）。其余 scheme（file:、data:、javascript: 等）必须继续被拒绝——纵深防御语义不变。

    要求：
    - 同步更新第 1762 行 warn 日志文案（当前「拒绝非 http(s) 的新建 Tab 请求」）使其反映新的 http(s)+realm 白名单。
    - 同步更新函数 JSDoc（1752-1759 行）中「主进程已做 http(s) 白名单校验」的表述为 http(s)+realm://，注明与主进程 isAllowedWebUrl 对齐。
    - 不得改动容器反推逻辑（guestId → partition → containerId）与 createTab 调用。realm:// URL 到达 createTab 后由既有 realmUrlToHttp 转换并自动附带 API token（renderer.js:160-168），无需额外处理。
    - 不得改动 main.js——其白名单已正确，本任务是单侧对齐。
  </action>
  <verify>
    <automated>test "$(sed -n '/^function handleOpenUrlInTab/,/^}/p' src/renderer.js | grep -c 'realm')" -ge 1 && test "$(sed -n '/^function handleOpenUrlInTab/,/^}/p' src/renderer.js | grep -c 'https?')" -ge 1 && test "$(sed -n '/^function isAllowedWebUrl/,/^}/p' main.js | grep -c 'realm')" -ge 1</automated>
  </verify>
  <done>
    handleOpenUrlInTab 函数体内出现 realm scheme 放行（区域 grep 计数 ≥1）且 http(s) 校验保留；main.js isAllowedWebUrl 未被改动（仍含既有 realm 放行）；file:/data: 等其他 scheme 仍被拒（白名单语义为显式枚举，未改为黑名单）。
  </done>
</task>

<task type="auto">
  <name>Task 3: 冒烟测试新增 realm:// 用例，锁定 IPC 链路 scheme 透明性</name>
  <files>scripts/test-ipc-favorites.js</files>
  <action>
    在 scripts/test-ipc-favorites.js 的 cases 数组（当前第 105-154 行）末尾追加一个用例：channel 为 'favorites:check'，payload 为 { url: 'realm://newtab' }，expectCall 为 ['checkUrl', 'realm://newtab']，expectReturn 为 null。

    目的：debug 文档曾排除「IPC payload/validation 拒绝 realm://」这一假设（当前 favorites:check handler 仅校验 data 为 object）。本用例将该排除项固化为持久回归护栏——未来任何人给 favorites:* handler 增加 scheme 校验时此测试立即失败。与既有 8 个用例格式完全对齐，不需要新增桩或改断言逻辑。

    要求：
    - 仅追加用例，不改动既有 8 个用例、STUBS、断言器与汇总输出。
    - 文件头部 JSDoc 中的「8 个 favorites:* handler」表述如与新用例数冲突则顺带更新为实际数量。
  </action>
  <verify>
    <automated>node scripts/test-ipc-favorites.js && grep -c "realm://newtab" scripts/test-ipc-favorites.js</automated>
  </verify>
  <done>
    node scripts/test-ipc-favorites.js 以退出码 0 完成，输出 9/9 PASS；脚本中含 realm://newtab 用例（grep 计数 ≥1）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest webview → 主进程 setWindowOpenHandler | window.open / target=_blank 的 URL 来自不可信页面内容 |
| 主进程 → 渲染进程 open-url-in-tab | 转发 URL 在渲染端二次校验（WR-9 纵深防御） |
| 渲染进程 → favorites:* IPC → SQLite | 收藏查询/写入的 url 字符串来自页面导航事件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Tampering | handleOpenUrlInTab scheme 白名单 widening | medium | mitigate | 仅显式追加 realm 协议枚举，不改为黑名单；realm:// 加载经 realmUrlToHttp 转换并附带本地服务器 API token（guest 无法伪造 token）；主进程 isAllowedWebUrl 先行过滤，file:/data:/javascript: 等 scheme 在两层均被拒 |
| T-09-04-02 | Information Disclosure | realm:// URL 写入 favorites 表 | low | accept | httpUrlToRealm 已剥离 container/token 查询参数，入库 url 为 'realm://newtab' 纯形式；checkUrl 为精确字符串匹配，无注入面（better-sqlite3 参数化查询） |
| T-09-04-03 | Tampering | checkBookmarkStatus 守卫移除后内部页面查询泛化 | low | accept | favorites:check 为只读精确匹配，about:blank/空 url 仍被早退拦截；历史记录过滤（renderer.js:669, D-23）不受本修复影响 |
| T-09-04-SC | Tampering | npm/pip/cargo installs | low | accept | 本计划零新增依赖——纯代码编辑 + Node 内置 assert 冒烟测试，不触发任何包管理器安装 |
</threat_model>

<verification>
自动化（本 plan 内全部可自动验证，无需人工检查点）：

1. 区域静态门禁：
   - checkBookmarkStatus 函数体内 realm 协议前缀判断计数 == 0
   - checkBookmarkStatus 函数体内 about:blank 保留计数 >= 1
   - handleOpenUrlInTab 函数体内 realm 放行计数 >= 1 且 http(s) 保留计数 >= 1
2. 运行时冒烟：node scripts/test-ipc-favorites.js 退出码 0，9/9 PASS（含新增 realm:// 用例）
3. 对称性确认：main.js isAllowedWebUrl 未改动（git diff 中 main.js 无变更）

人工回归（执行完成后由编排层在 UAT 环节处理，不在本 autonomous plan 内）：
- 重跑 09-UAT.md test 9（realm://newtab 收藏 → 刷新 → 星标保持实心）
- 重跑 test 10/11（跨容器共享、重复收藏提示、取消收藏对 realm:// URL 的行为）
- 补充验证：realm://favorites 列表双击 realm://newtab 条目可在新 Tab 打开
</verification>

<success_criteria>
- src/renderer.js 中 checkBookmarkStatus 对 realm://newtab 执行 favoritesCheck 精确匹配（守卫不再拦截），about:blank/空 url 早退保留
- handleOpenUrlInTab 接受 realm:// URL 并 createTab，其余非法 scheme 仍被拒
- scripts/test-ipc-favorites.js 9/9 PASS，退出码 0
- 09-UAT.md gap「realm://newtab 收藏后刷新星标保持实心」的可观察 truth 达成，相邻的列表双击打开问题同步消除
</success_criteria>

<output>
Create `.planning/phases/09-共享收藏数据库/09-04-SUMMARY.md` when done
</output>
