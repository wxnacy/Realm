---
phase: 41-web-fetch-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - ipc-handlers.js
  - src/preload.js
  - main.js
  - src/settings.html
  - src/settings-page.js
  - package.json
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - CONFIG-04

estimate:
  tokens: 80000
  raw_tokens: 55000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "设置页 AI 助手分区内显示「网络搜索」可折叠子区域，点击标题可展开/折叠"
    - "子区域左侧显示 6 个 Provider 列表（auto/tavily/brave/serper/anysearch/anysearch_free），每个显示名称和状态标签"
    - "点击 Provider 列表项，右侧编辑器切换到该 Provider 的 API Key 配置"
    - "用户输入 API Key 后点击「验证 API Key」按钮，发送测试查询验证 Key 有效性"
    - "验证成功显示绿色对勾 + 「Key 有效」，验证失败显示红色叉号 + 具体错误信息"
    - "用户点击「保存配置」后 API Key 保存到 electron-store，Provider 列表状态标签更新为「已配置」"
    - "覆盖已有 API Key 时弹出 inline 确认条（非 window.confirm），5 秒超时自动消失"
    - "[FLAGGED-ASSUMPTION: CONFIG-03 concurrency] 如果用户快速连续点击验证按钮，第二次请求会覆盖第一次的结果——无取消机制，以最后一次结果为准"
    - "[FLAGGED-ASSUMPTION: CONFIG-03 empty] 空 API Key 输入时，验证按钮不应触发验证——前端校验空值并提示"
    - "[FLAGGED-ASSUMPTION: CONFIG-04 ordering] get-search-config 返回的 apiKeys 对象中 Key 的顺序不保证稳定——前端按 Provider ID 索引而非顺序"
    - "[FLAGGED-ASSUMPTION: CONFIG-02 equality] 切换 Provider 时如果新旧 Provider 相同（重复点击），不触发额外操作"
  artifacts:
    - ipc-handlers.js（新增 search-config:get/set/verify-key IPC 通道）
    - src/preload.js（新增 searchConfig API 暴露）
    - main.js（新增 /api/search-config/* HTTP 路由 + handleSearchConfigApi 函数）
    - src/settings.html（AI 助手分区内新增网络搜索子区域 HTML）
    - src/settings-page.js（新增搜索配置 UI 逻辑：Provider 列表、编辑器、验证交互）
    - package.json（新增 jsdom、turndown 依赖）
  key_links:
    - "main.js:handleSearchConfigApi → configStore.get/set('search.*') 配置读写"
    - "src/settings-page.js:searchConfigApi → /api/search-config/* HTTP 调用链"
    - "ipc-handlers.js:search-config:verify-key → searchManager.doSearch('test', 1) 验证调用"
    - "src/settings.html:searchConfigToggle → src/settings-page.js 折叠交互"
  prohibitions:
    - "[FLAGGED-UNVERIFIED: WEBVIEW-IPC] 设置页在 webview guest 中，不得使用 window.realmAPI.* IPC——必须走 HTTP /api/search-config/* 接口"
    - "[FLAGGED-UNVERIFIED: CSP-INLINE-STYLE] realm:// 页面 CSP 拦截内联 style 属性——初始隐藏不得用 style=\"display:none\"，必须用 CSS 类或 JS style.display"
    - "[FLAGGED-UNVERIFIED: CONFIRM-DIALOG] 覆盖 API Key 时不得使用 window.confirm()——必须使用 inline 确认条"
---

<objective>
实现搜索配置 UI 完整功能：IPC 通道 + HTTP API + preload 暴露 + 设置页「网络搜索」子区域（Provider 列表 + API Key 编辑器 + 验证交互）+ npm 依赖安装。

Purpose: 用户可以在设置页中管理搜索 Provider 和 API Key，切换 Provider 即时生效，验证 Key 有效性，覆盖已有 Key 时有确认保护。

Output: ipc-handlers.js、src/preload.js、main.js、src/settings.html、src/settings-page.js 的修改 + package.json 新增 jsdom/turndown 依赖
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-web-fetch-ui/41-CONTEXT.md
@.planning/phases/41-web-fetch-ui/41-RESEARCH.md
@.planning/phases/41-web-fetch-ui/41-PATTERNS.md
@.planning/phases/41-web-fetch-ui/41-UI-SPEC.md
@.planning/phases/41-web-fetch-ui/41-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm 依赖安装 + IPC/HTTP/preload 通道</name>
  <files>package.json, ipc-handlers.js, src/preload.js, main.js</files>
  <read_first>
    - package.json（确认当前依赖列表，检查 jsdom/turndown 是否已存在）
    - ipc-handlers.js（line 2000-2047：settings:get/set IPC 模式 + setAIManager 模式 + module.exports）
    - src/preload.js（line 900-972：ai API 命名空间暴露模式）
    - main.js（line 1186-1245：handleSettingsApi 模式；line 2016-2031：HTTP 路由注册位置；line 129：searchManager require；line 2797：initSearchManager 调用）
    - .planning/phases/41-web-fetch-ui/41-PATTERNS.md（ipc-handlers.js、preload.js、main.js 模式赋值）
  </read_first>
  <action>
实现搜索配置的数据通道层：npm 依赖 + IPC + HTTP API + preload 暴露。

**1. npm 依赖安装：**
- 运行 `npm install jsdom turndown`（安装到 dependencies，非 devDependencies）
- 运行后检查 package.json 确认 jsdom 和 turndown 出现在 dependencies 中
- @mozilla/readability 已在 devDependencies，需移到 dependencies（主进程运行时需要）：`npm install @mozilla/readability`

**2. ipc-handlers.js — 新增 3 个 IPC 通道（per CONFIG-04）：**
- 在 `registerHandlers()` 函数内、`settings:set` 之后（line 2033 之后）追加
- 需要先添加 searchManager 引用：在文件顶部或通过 setter 注入
  - 参考 `setAIManager` 模式（line 2043-2045），新增 `setSearchManager(manager)` 函数
  - 在 module.exports 中导出 `setSearchManager`
  - 在文件顶部声明 `let searchManager = null;`
- `search-config:get`：`assertTrustedSender(event)`，返回 `{provider: configStore.get('search.provider', 'auto'), apiKeys: configStore.get('search.apiKeys', {})}`
- `search-config:set`：`assertTrustedSender(event)`，从 config 参数读取 provider 和 apiKeys，调用 `configStore.set` 保存
- `search-config:verify-key`：`assertTrustedSender(event)`，从 `{provider, apiKey}` 参数中获取信息
  - 临时保存 Key 到 configStore：`configStore.set('search.apiKeys.' + provider, apiKey)`
  - 调用 `searchManager.doSearch('test', 1)` 发送测试查询（per D-13）
  - 成功返回 `{valid: true, provider: result.provider}`
  - 失败返回 `{valid: false, error: err.message}`
  - finally 块中恢复原始 Keys（per D-15：验证不删除）

**3. main.js — 新增 /api/search-config/* HTTP 路由（per CONFIG-01）：**
- 在 HTTP 服务器请求处理函数中、`/api/settings/` 路由之后（line 2019 附近）追加路由判断：`if (reqPath.startsWith('/api/search-config/'))`
- 新增 `handleSearchConfigApi(req, res, reqUrl)` 异步函数：
  - Token 鉴权：`reqUrl.searchParams.get('token') !== REALM_TOKEN` 时返回 403
  - `GET /api/search-config/get`：返回 `{provider: configStore.get('search.provider', 'auto'), apiKeys: maskApiKeys(configStore.get('search.apiKeys', {}))}`
  - `POST /api/search-config/set`：读取 JSON body，更新 configStore 的 search.provider 和 search.apiKeys
  - `POST /api/search-config/verify-key`：读取 JSON body 中的 `{provider, apiKey}`，临时保存 Key，调用 `searchManager.doSearch('test', 1)`，恢复原始 Keys
- 新增 `maskApiKeys(apiKeys)` 辅助函数：对长度 > 8 的 Key 显示前 4 后 4 中间 `****`，短 Key 原样返回
- 在 main.js 中确认 `searchManager` 引用已存在（line 129：`const searchManager = require('./search-manager')`）
- 在 `initSearchManager` 调用之后（line 2797 附近），调用 `ipcHandlers.setSearchManager(searchManager)` 注入 searchManager 到 ipc-handlers

**4. src/preload.js — 暴露 searchConfig API（per CONFIG-04）：**
- 在 `contextBridge.exposeInMainWorld('realmAPI', {...})` 内部、`ai:` 命名空间之后（line 972 之后）追加：
- `searchConfig: { getConfig: () => ipcRenderer.invoke('search-config:get'), setConfig: (config) => ipcRenderer.invoke('search-config:set', config), verifyKey: (provider, apiKey) => ipcRenderer.invoke('search-config:verify-key', { provider, apiKey }) }`

**关键约束：**
- ipc-handlers.js 的 IPC 通道必须调用 `assertTrustedSender(event)`（安全校验）
- main.js 的 HTTP API 必须校验 `REALM_TOKEN`（与 handleSettingsApi 一致）
- 设置页在 webview guest 中，不能走 IPC（assertTrustedSender 会拒绝），必须走 HTTP API
- maskApiKeys 遮蔽逻辑：Key 长度 > 8 时前 4 后 4 中间 ****，否则原样返回
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); console.log('jsdom:', !!pkg.dependencies.jsdom, 'turndown:', !!pkg.dependencies.turndown, 'readability:', !!(pkg.dependencies['@mozilla/readability'] || pkg.devDependencies['@mozilla/readability']))"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies 中包含 jsdom 和 turndown
    - @mozilla/readability 在 dependencies 或 devDependencies 中存在
    - ipc-handlers.js 中存在 `ipcMain.handle('search-config:get'` 注册
    - ipc-handlers.js 中存在 `ipcMain.handle('search-config:set'` 注册
    - ipc-handlers.js 中存在 `ipcMain.handle('search-config:verify-key'` 注册
    - ipc-handlers.js 的 module.exports 包含 `setSearchManager`
    - main.js 中存在 `reqPath.startsWith('/api/search-config/')` 路由判断
    - main.js 中存在 `handleSearchConfigApi` 函数定义
    - main.js 中存在 `maskApiKeys` 函数定义
    - src/preload.js 中存在 `searchConfig:` 命名空间，包含 getConfig、setConfig、verifyKey 三个方法
    - `grep -c "search-config" ipc-handlers.js` 输出 >= 3
    - `grep -c "searchConfig" src/preload.js` 输出 >= 1
  </acceptance_criteria>
  <done>搜索配置数据通道层完成：npm 安装 jsdom/turndown/readability，ipc-handlers.js 新增 3 个 IPC 通道，main.js 新增 /api/search-config/* HTTP 路由，preload.js 暴露 searchConfig API。</done>
</task>

<task type="auto">
  <name>Task 2: settings.html — 网络搜索子区域 HTML 结构</name>
  <files>src/settings.html</files>
  <read_first>
    - src/settings.html（line 265-418：AI 助手 section 完整结构，line 368：AI 供应商管理分栏结束位置，line 417-418：section 结束前的添加供应商弹窗之后）
    - .planning/phases/41-web-fetch-ui/41-UI-SPEC.md（Layout Contract、Component Inventory、Spacing Scale、Typography、Color）
    - .planning/phases/41-web-fetch-ui/41-PATTERNS.md（src/settings.html HTML 结构模式）
  </read_first>
  <action>
在 src/settings.html 的 AI 助手 section 内新增「网络搜索」可折叠子区域。

**插入位置：** 在添加供应商弹窗 `</div>`（line 417）之后、`</section>`（line 418）之前。

**HTML 结构（per D-08, D-09, D-10, UI-SPEC Layout Contract）：**

1. 可折叠子区域容器 `<div class="settings-group search-config-section">`：
   - 标题栏 `<div class="settings-group-header" id="searchConfigToggle">`：
     - `<h2 class="settings-group-title">网络搜索</h2>`（per UI-SPEC Copywriting Contract: Section title）
     - 折叠箭头 SVG（复用现有 chevron 样式）
   - 内容区 `<div class="settings-group-content" id="searchConfigContent" style="display:none;">`（CSP 注意：`style="display:none"` 在 CSP 无 unsafe-inline 时会被拦截。但实际测试中 `display:none` 作为属性值在 Electron 中通常生效。为安全起见，改用 CSS 类初始隐藏：在 main.css 中添加 `.search-config-content { display: none; }`，JS 中用 `el.style.display = 'flex'` 覆盖）

2. 内容区内左右分栏 `<div class="settings-ai-split">`（复用现有样式）：
   - 左侧 `<div class="settings-ai-sidebar">`：
     - Provider 列表 `<div id="searchProviderList" class="ai-provider-list"></div>`
   - 右侧 `<div class="settings-ai-editor">`：
     - 空状态 `<div id="searchEditorEmpty" class="ai-editor-empty">`：
       - 标题文字：`"选择一个搜索 Provider"`（per UI-SPEC Empty state heading）
       - 说明文字：`"从左侧列表选择一个 Provider，配置 API Key 后即可使用网络搜索功能"`（per UI-SPEC Empty state body）
     - 编辑表单 `<div id="searchEditorForm" class="ai-editor-form" style="display:none;">`：
       - 表头 `<div class="ai-editor-header">`：`<h3 id="searchEditorTitle" class="ai-editor-title"></h3>`
       - API Key 表单组 `<div class="ai-form-group">`：
         - label: `API Key`
         - 输入框容器 `<div class="settings-input-wrapper">`：
           - `<input type="password" id="searchEditorApiKey" class="text-input" placeholder="输入 API Key">`（per UI-SPEC API Key 输入 placeholder）
           - `<button class="btn btn-secondary btn-sm" id="searchVerifyBtn">验证 API Key</button>`（per UI-SPEC Primary CTA）
         - 验证结果 `<div id="searchVerifyResult" class="verify-result"></div>`
       - 操作按钮区 `<div class="ai-editor-actions">`：
         - `<button class="btn btn-primary" id="searchSaveBtn">保存配置</button>`（per UI-SPEC Secondary CTA）

**CSS 补充：**
- 在 src/settings.html 或 main.css 中添加 `.search-config-content { display: none; }` 用于初始隐藏
- 验证结果样式 `.verify-result.success` 绿色、`.verify-result.error` 红色

**关键约束：**
- 不要在 HTML 中使用 `style="..."` 内联样式做初始隐藏（CSP 限制），用 CSS 类
- 但 `style="display:none"` 属性在 Electron webview 中实际可用（项目已有大量此模式），所以编辑表单的初始隐藏保持 `style="display:none"` 一致
- 复用现有 CSS 类：`.settings-group`、`.settings-group-header`、`.settings-group-title`、`.settings-group-chevron`、`.settings-group-content`、`.settings-ai-split`、`.settings-ai-sidebar`、`.settings-ai-editor`、`.ai-editor-empty`、`.ai-editor-form`、`.ai-editor-header`、`.ai-form-group`、`.settings-input-wrapper`、`.text-input`、`.btn`、`.ai-provider-list`
  </action>
  <verify>
    <automated>grep -c "searchConfigToggle\|searchProviderList\|searchEditorForm\|searchEditorApiKey\|searchVerifyBtn\|searchSaveBtn" /Users/wxnacy/Projects/Realm/src/settings.html</automated>
  </verify>
  <acceptance_criteria>
    - settings.html 中存在 `id="searchConfigToggle"` 元素（折叠标题栏）
    - settings.html 中存在 `id="searchConfigContent"` 元素（内容区）
    - settings.html 中存在 `id="searchProviderList"` 元素（Provider 列表容器）
    - settings.html 中存在 `id="searchEditorEmpty"` 元素（空状态）
    - settings.html 中存在 `id="searchEditorForm"` 元素（编辑表单）
    - settings.html 中存在 `id="searchEditorApiKey"` input 元素，type="password"
    - settings.html 中存在 `id="searchVerifyBtn"` 按钮，文字为「验证 API Key」
    - settings.html 中存在 `id="searchVerifyResult"` 元素（验证结果区）
    - settings.html 中存在 `id="searchSaveBtn"` 按钮，文字为「保存配置」
    - 新增 HTML 位于 AI 助手 section 内（`settings-ai-assistant` section 的 `</section>` 之前）
    - `grep -c "网络搜索" src/settings.html` 输出 >= 1
  </acceptance_criteria>
  <done>settings.html 在 AI 助手分区内新增「网络搜索」可折叠子区域，包含 Provider 列表容器、API Key 编辑表单、验证按钮和结果区域，复用现有 CSS 类。</done>
</task>

<task type="auto">
  <name>Task 3: settings-page.js — 搜索配置 UI 逻辑</name>
  <files>src/settings-page.js, src/styles/main.css</files>
  <read_first>
    - src/settings-page.js（line 58-71：settingsApi 模式；line 2538-2566：loadAISettings 模式；line 2592-2651：renderProviderList 模式；line 2670-2720：showEditorForm 模式；line 259：loadAISettings 调用位置）
    - src/styles/main.css（line 4449-4479：ai-provider-list/ai-provider-item 样式；line 4526-4532：ai-editor-form 样式；line 878-887：btn-primary/btn-secondary 样式；line 1430：toast 样式）
    - .planning/phases/41-web-fetch-ui/41-PATTERNS.md（settings-page.js 模式赋值：searchConfigApi、renderSearchProviderList、showSearchEditorForm、verifySearchKey）
    - .planning/phases/41-web-fetch-ui/41-UI-SPEC.md（Interaction Contract、Copywriting Contract、Edge Coverage）
    - .planning/phases/41-web-fetch-ui/41-CONTEXT.md（D-08~D-15 决策）
  </read_first>
  <action>
在 src/settings-page.js 中实现搜索配置 UI 的完整交互逻辑，在 main.css 中补充必要样式。

**1. 搜索配置数据和常量（文件顶部附近，在现有常量之后）：**
- 定义 `SEARCH_PROVIDERS` 数组（per D-10, D-11）：
  - `{id: 'auto', name: '自动选择', requiresKey: false, description: '自动选择最佳可用 Provider'}`
  - `{id: 'tavily', name: 'Tavily', requiresKey: true}`
  - `{id: 'brave', name: 'Brave Search', requiresKey: true}`
  - `{id: 'serper', name: 'Serper (Google)', requiresKey: true}`
  - `{id: 'anysearch', name: 'AnySearch', requiresKey: true}`
  - `{id: 'anysearch_free', name: 'AnySearch Free', requiresKey: false}`
- 定义状态变量：`let searchSelectedProviderId = null;` `let searchConfig = { provider: 'auto', apiKeys: {} };`

**2. `searchConfigApi(route, options)` 函数：**
- 参考 `settingsApi` 函数模式（line 58-71）
- 使用 `fetch('/api/search-config/' + route + '?' + params, options)` 调用 HTTP API
- 错误处理与 settingsApi 一致

**3. `loadSearchConfig()` 函数：**
- 调用 `searchConfigApi('get')` 获取当前配置
- 保存到 `searchConfig` 变量
- 调用 `renderSearchProviderList()` 渲染列表
- 若 `searchConfig.provider` 存在，调用 `showSearchEditorForm(searchConfig.provider)` 预选

**4. `renderSearchProviderList()` 函数（参考 renderProviderList line 2592-2651）：**
- 获取 `searchProviderList` 元素
- 遍历 `SEARCH_PROVIDERS` 创建列表项
- 每项使用 `.ai-provider-item` 样式，选中项加 `.active` 类
- 显示 Provider 名称 + 状态标签：
  - `requiresKey: true` 且有 Key → 绿色「已配置」（class: `provider-status configured`）
  - `requiresKey: true` 且无 Key → 灰色「未配置」（class: `provider-status unconfigured`）
  - `requiresKey: false` → 蓝色「免费」（class: `provider-status free`）
- 点击事件调用 `showSearchEditorForm(p.id)`

**5. `showSearchEditorForm(providerId)` 函数（参考 showEditorForm line 2670-2720）：**
- 更新 `searchSelectedProviderId`，重新渲染列表
- 隐藏空状态 `searchEditorEmpty`，显示编辑表单 `searchEditorForm`
- 设置标题 `searchEditorTitle` 为 Provider 名称
- 根据 `requiresKey` 决定是否显示 API Key 输入框和验证按钮
  - `requiresKey: false` → 隐藏 API Key 输入区域和验证按钮，显示说明文字
  - `requiresKey: true` → 显示 API Key 输入框，填入已有 Key（若存在），显示验证按钮
- 清空验证结果 `searchVerifyResult`

**6. 验证交互 `verifySearchKey(providerId, apiKey)` 函数（per D-12, D-13, D-14）：**
- 获取验证按钮和结果元素
- 按钮文字改为「正在验证...」，disabled = true（per UI-SPEC Loading state）
- 调用 `searchConfigApi('verify-key', {method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({provider: providerId, apiKey})})`
- 成功：结果区 class 加 `success`，文字「Key 有效」（per UI-SPEC Success state）
- 失败：结果区 class 加 `error`，文字「验证失败：{具体原因}」（per UI-SPEC Error state）
- finally：恢复按钮状态

**7. 保存交互（per D-15, UI-SPEC Interaction Contract）：**
- `searchSaveBtn` 点击事件处理：
  - 获取当前 Provider ID 和 API Key 输入值
  - **覆盖确认（per UI-SPEC Destructive confirmation）：** 若 Provider 已有 Key 且输入值与已有值不同：
    - 在输入框下方显示 inline 确认条（复用 `.toast` 组件扩展或自定义 div）
    - 文案：「当前 Provider 已有 API Key，确认覆盖？」
    - 两个按钮：[确认覆盖] [取消]
    - 5 秒无操作自动消失
    - 点击 [确认覆盖] 继续保存，点击 [取消] 或超时则中止
  - 调用 `searchConfigApi('set', {method: 'POST', body: JSON.stringify({apiKeys: {...searchConfig.apiKeys, [providerId]: apiKey}})})` 保存
  - 更新 `searchConfig.apiKeys[providerId]` 本地状态
  - 重新渲染 Provider 列表（状态标签更新为「已配置」）

**8. 折叠交互（per D-08）：**
- `searchConfigToggle` 点击事件：切换 `searchConfigContent` 的 `style.display`（'none' ↔ 'flex'）
- 折叠状态保存到 electron-store（通过 settingsApi 'update'）
- 页面加载时恢复折叠状态

**9. 初始化：**
- 在 `loadAISettings()` 调用之后（line 259 附近），调用 `loadSearchConfig()`
- 绑定 `searchConfigToggle` 折叠事件
- 绑定 `searchSaveBtn` 保存事件
- 绑定 `searchVerifyBtn` 验证事件

**10. main.css 样式补充：**
- `.search-config-content { display: none; }` — 子区域内容初始隐藏
- `.provider-status { font-size: 12px; margin-left: auto; }` — 状态标签
- `.provider-status.configured { color: var(--success-color); }` — 已配置绿色
- `.provider-status.unconfigured { color: var(--text-muted); }` — 未配置灰色
- `.provider-status.free { color: var(--accent-color); }` — 免费蓝色
- `.verify-result { font-size: 13px; margin-top: 8px; min-height: 20px; }` — 验证结果区
- `.verify-result.success { color: var(--success-color); }` — 成功绿色
- `.verify-result.error { color: var(--danger-color); }` — 失败红色
- 确认条样式（若不复用 toast，需自定义）

**关键约束：**
- 设置页在 webview guest 中，数据获取必须走 HTTP API（`/api/search-config/*`），不得走 IPC（per Pitfall 4）
- CSP `style-src 'self'` 无 unsafe-inline，元素初始隐藏用 CSS 类，JS 显隐用 `el.style.display = 'flex'/'block'/'none'`（per Pitfall 3）
- API Key 输入框使用 `type="password"` 遮蔽（per UI-SPEC）
- 覆盖确认使用 inline 确认条，不得使用 `window.confirm()`（per UI-SPEC Destructive confirmation）
  </action>
  <verify>
    <automated>grep -c "SEARCH_PROVIDERS\|searchConfigApi\|loadSearchConfig\|renderSearchProviderList\|showSearchEditorForm\|verifySearchKey\|searchSaveBtn\|searchConfigToggle" /Users/wxnacy/Projects/Realm/src/settings-page.js</automated>
  </verify>
  <acceptance_criteria>
    - settings-page.js 中定义了 `SEARCH_PROVIDERS` 常量数组，包含 6 个 Provider（auto/tavily/brave/serper/anysearch/anysearch_free）
    - settings-page.js 中存在 `searchConfigApi` 函数，调用 `/api/search-config/` 路由
    - settings-page.js 中存在 `loadSearchConfig` 函数，调用 searchConfigApi('get')
    - settings-page.js 中存在 `renderSearchProviderList` 函数，渲染 6 个 Provider 列表项
    - settings-page.js 中存在 `showSearchEditorForm` 函数，切换右侧编辑器
    - settings-page.js 中存在 `verifySearchKey` 函数，调用 searchConfigApi('verify-key')
    - settings-page.js 中 `searchSaveBtn` 的点击事件处理包含覆盖确认逻辑
    - settings-page.js 中 `searchConfigToggle` 的点击事件处理包含折叠切换逻辑
    - main.css 中存在 `.provider-status` 相关样式
    - main.css 中存在 `.verify-result` 相关样式
    - `grep -c "searchConfigApi\|loadSearchConfig" src/settings-page.js` 输出 >= 2
  </acceptance_criteria>
  <done>settings-page.js 实现完整的搜索配置 UI 交互：Provider 列表渲染、API Key 编辑器、验证交互（手动触发 + 内联结果）、保存交互（含覆盖确认条）、折叠交互。main.css 补充状态标签和验证结果样式。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview guest → main process | 设置页通过 HTTP API 读写搜索配置，token 鉴权 |
| user input → configStore | 用户输入的 API Key 保存到 electron-store |
| main process → search API | verify-key 调用外部搜索 API 验证 Key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Information Disclosure (API Key 泄露) | main.js:maskApiKeys | high | mitigate | HTTP API 返回时遮蔽 Key（前 4 后 4 中间 ****），不返回完整 Key 给前端。输入框使用 type="password" |
| T-41-06 | Tampering (配置篡改) | ipc-handlers.js + main.js | medium | mitigate | IPC 通道 assertTrustedSender 校验，HTTP API token 鉴权。非受信来源无法修改搜索配置 |
| T-41-07 | Information Disclosure (验证请求泄露 Key) | search-config:verify-key | medium | accept | 验证时临时将 Key 写入 configStore 后调用 doSearch，验证完 finally 恢复。Key 在内存中短暂存在，与正常搜索流程一致 |
| T-41-08 | Elevation of Privilege | main.js:handleSearchConfigApi | medium | mitigate | HTTP API 仅允许 get/set/verify-key 三个路由，其他路由返回 404。configStore 写入限定 search.* 键空间 |
| T-41-SC | Tampering | npm install jsdom turndown readability | high | mitigate | 安装前通过 `npm view` 验证版本号。阻断性人工检查点：npm install 之前暂停确认包名正确 |
</threat_model>

<verification>
1. 打开设置页 AI 助手分区，可见「网络搜索」可折叠子区域
2. 点击「网络搜索」标题，子区域展开显示 Provider 列表
3. 左侧显示 6 个 Provider（auto/tavily/brave/serper/anysearch/anysearch_free）
4. 点击 tavily Provider，右侧显示 API Key 输入框 + 验证按钮
5. 输入 API Key 后点击「验证 API Key」，显示验证结果
6. 点击「保存配置」，Provider 列表状态标签更新为「已配置」
7. `grep -c "search-config" ipc-handlers.js` 输出 >= 3
8. `grep -c "searchConfig" src/preload.js` 输出 >= 1
9. `grep -c "handleSearchConfigApi" main.js` 输出 >= 1
10. `npm run validate` 通过
</verification>

<success_criteria>
- npm 安装 jsdom、turndown、@mozilla/readability 成功
- IPC 通道 search-config:get/set/verify-key 注册完成
- HTTP API /api/search-config/get/set/verify-key 路由完成
- preload 暴露 searchConfig API（getConfig/setConfig/verifyKey）
- 设置页显示「网络搜索」子区域，Provider 列表 + 编辑器 + 验证 + 保存功能完整
- 覆盖已有 Key 时弹出 inline 确认条（非 window.confirm）
- npm run validate 通过
</success_criteria>

<output>
创建 `.planning/phases/41-web-fetch-ui/41-02-SUMMARY.md` 当本计划完成时
</output>
