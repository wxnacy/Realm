---
phase: 10-Cookie管理增强
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/renderer.js
  - cookie-manager.js
  - src/styles/main.css
  - scripts/test-save-domain-cookies.js
autonomous: true
gap_closure: true
requirements:
  - COOKIE-02
  - COOKIE-03
must_haves:
  truths:
    - 在 https://www.baidu.com/ 标签页打开 Cookie 面板点击保存按钮，保存数量与面板"含子域名"过滤所显示的数量一致（用户报告的 6 个场景），不再保存容器全部 65 个 Cookie（UAT test 6, COOKIE-03）
    - handleSaveToFile 通过 state.tabs.get(state.activeTabId).url 提取域名，与 showCookiesModal 同源，不再依赖不存在的 DOM class 选择器
    - saveDomainCookies 的保存集合与渲染层 applyDomainFilter 的 subdomain 过滤集合完全一致（UI 所见即所存）（COOKIE-02, COOKIE-03）
    - 无法获取域名时（realm:// 新标签页等）保持既有回退行为：保存容器全部 Cookie 并显示无域名文案的提示
    - Cookie 列表 name 列文字为 var(--text-primary)（#f0f0f0），value/domain 列为 var(--text-secondary)（#a0a0a0），在 #3a3a3a 行背景上清晰可读（UAT test 7）
    - 所有模态框后代元素不再继承原生 dialog 的 UA 默认黑色文字（.modal 显式 color 防御）
  artifacts:
    - src/renderer.js（handleSaveToFile 改用 state.tabs 模式提取域名）
    - cookie-manager.js（saveDomainCookies 过滤方向与 UI 过滤语义对齐）
    - scripts/test-save-domain-cookies.js（新增：过滤语义 + 合并保留行为的运行时回归测试，纯 Node 可运行）
    - src/styles/main.css（.cookie-col-* 显式颜色 + .cookie-item/.modal 颜色防御 + 死代码清理）
  key_links:
    - handleSaveToFile → realmAPI.saveDomainCookies → cookie:save-domain → cookieManager.saveDomainCookies：域名非空时真正触达域名过滤路径（修复前 100% 落入 save-all fallback）
    - cookie-manager.js saveDomainCookies 过滤语义 ≡ renderer.js applyDomainFilter subdomain 语义（同一集合两处实现，由冒烟测试锁定）
    - .modal 显式 color → 全部 dialog 后代元素的文字颜色兜底
---

<objective>
修复 10-UAT.md 报告的两个 gap：

**Gap 1（major, test 6）**：保存按钮域名过滤失效。handleSaveToFile（src/renderer.js:2275-2314）通过一个应用中从未存在的 DOM class 选择器查找当前 webview，选择器永远返回 null → domain 恒为 '' → 落入 fallback 分支 saveCookie → cookie:save → saveCookies() 无过滤保存容器全部 65 个 session Cookie，saveDomainCookies 从未被触达。次级隐患：saveDomainCookies（cookie-manager.js:183-194）的过滤方向（匹配子域名 Cookie）与 UI applyDomainFilter 的 subdomain 模式（匹配父域名 Cookie，即当前页面可见集合）语义相反，即使主因修复，保存数仍 ≠ 面板显示数。

**Gap 2（cosmetic, test 7）**：Cookie 列表文字纯黑看不清。renderCookiesList 使用的 .cookie-col-* 类（main.css:620-646）只定义布局未定义 color，祖先 .cookie-item/.cookies-list/.modal-content/.modal 也均未设 color，行文本继承原生 dialog 的 UA 默认 color: black，渲染为纯黑 (#000) 在 #3a3a3a 背景上（对比度仅 1.9:1）。Phase 10 重构把旧含色类改名为 .cookie-col-* 时遗漏了颜色声明，旧类成为死代码（main.css:761-783）。

Purpose: 使"保存到文件"按钮真正只保存面板所显示的当前域名 Cookie 集合（UI 所见即所存），并恢复 Cookie 列表在深色主题下的可读性。
Output: renderer.js 域名提取修复 + cookie-manager.js 过滤方向对齐 + 持久回归冒烟测试 + main.css 颜色修复与死代码清理。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Cookie管理增强/10-UAT.md
@.planning/phases/10-Cookie管理增强/10-01-SUMMARY.md
@.planning/debug/save-cookie-wrong-domain-filter.md
@.planning/debug/cookie-list-bg-too-dark.md
@src/renderer.js
@cookie-manager.js
@src/styles/main.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复保存按钮域名过滤 — renderer.js 域名提取 + saveDomainCookies 过滤方向对齐（含运行时冒烟测试）</name>
  <files>src/renderer.js, cookie-manager.js, scripts/test-save-domain-cookies.js</files>
  <read_first>
    - src/renderer.js 行 2271-2314（handleSaveToFile 当前实现：错误的 DOM 查询域名提取 + 两个分支）
    - src/renderer.js 行 1955-1966（showCookiesModal 域名提取对照组：state.tabs.get(state.activeTabId).url → new URL().hostname，本任务要对齐的正确模式）
    - src/renderer.js 行 2030-2046（applyDomainFilter：UI 侧 subdomain 过滤语义，保存侧必须与之同集合）
    - cookie-manager.js 行 164-255（saveDomainCookies 当前实现：过滤方向与 UI 相反）
    - cookie-manager.js 行 1-50（模块依赖：electron、fs、path、electron-store；CONTAINERS_DIR 派生自 app.getPath('userData')，冒烟测试需据此打桩）
    - scripts/test-ipc-favorites.js（09-03 建立的 Module._load 依赖打桩模式，本任务的冒烟测试沿用同款结构）
  </read_first>
  <behavior>
    冒烟测试对 saveDomainCookies 断言以下行为（domain = 'www.baidu.com'）：
    - includeSubdomains=true 时匹配的 session Cookie：domain 为 '.baidu.com'、'baidu.com'、'www.baidu.com'、'.www.baidu.com' 的 4 个（即 UI"含子域名"过滤所显示的同一集合：当前域名 + 其父域）
    - includeSubdomains=true 时不匹配：'api.www.baidu.com'（子域方向，UI 同样不显示）、'.other.com'（无关域）、'.baidu.com.evil.com'（后缀伪装域，安全用例）、'notbaidu.com'（子串伪装域）
    - includeSubdomains=false 时仅匹配 'www.baidu.com' 与 '.www.baidu.com' 的 2 个
    - 合并保留：文件中范围外未过期旧 Cookie 被保留；已过期旧 Cookie 被丢弃；返回 count 等于本次匹配的 session Cookie 数（不含保留数）
    - renderer 侧（静态断言）：handleSaveToFile 函数体内不再出现任何 document 级 CSS 选择器查询调用；src/renderer.js 中 state.tabs.get(state.activeTabId) 至少出现 2 次（showCookiesModal + handleSaveToFile 各一）
  </behavior>
  <action>
严格按 TDD 顺序执行：先写冒烟测试并确认 RED，再改实现确认 GREEN。

**Step 1（RED）— 新建 scripts/test-save-domain-cookies.js 冒烟测试：**

该测试在纯 Node 环境运行（不启动 Electron），通过 Module._load 拦截对 cookie-manager.js 的依赖打桩（沿用 scripts/test-ipc-favorites.js 的既有模式）：

- electron 桩：app.getPath('userData') 返回 fs.mkdtempSync(path.join(os.tmpdir(), 'realm-test-')) 创建的临时目录；session.fromPartition 返回固定对象，其 cookies.get 为 async 函数，返回下列 fixture 数组
- electron-store 桩：导出类，构造函数空实现，get(key, def) 返回 def（模块顶层会实例化 configStore，必须可构造）
- fixture session Cookie（name 任意、domain 如下，其余字段给合理默认值）：{ name:'A', domain:'.baidu.com' }、{ name:'B', domain:'baidu.com' }、{ name:'C', domain:'www.baidu.com' }、{ name:'D', domain:'.www.baidu.com' }、{ name:'E', domain:'api.www.baidu.com' }、{ name:'F', domain:'.other.com' }、{ name:'G', domain:'.baidu.com.evil.com' }、{ name:'H', domain:'notbaidu.com' }
- 预置旧文件：在 {tmpdir}/containers/test-container/cookies.json 写入 2 条旧 Cookie —— OLD_OK（domain '.other.com'，expirationDate 为未来时间）与 OLD_EXPIRED（domain '.legacy.com'，expirationDate 为过去时间）
- 调用 saveDomainCookies('test-container', 'www.baidu.com', true)，断言：result.success 为 true；result.count === 4；写回文件的 Cookie 中包含 A、B、C、D 与 OLD_OK，且不包含 E、F、G、H 与 OLD_EXPIRED（按 name 断言）
- 再次调用 saveDomainCookies('test-container', 'www.baidu.com', false)（精确模式），断言 result.count === 2（仅 C、D）
- 输出每项断言 PASS/FAIL 明细与汇总；任何失败以非零码退出；结尾 fs.rmSync 清理临时目录（recursive + force）

**Step 2 — 运行 node scripts/test-save-domain-cookies.js 确认 RED：** 当前实现 includeSubdomains=true 的过滤方向是匹配子域名 Cookie（cookieDomain.endsWith('.' + domain)），只会命中 C、D、E 共 3 个且漏掉 A、B，应见 count 断言与文件内容断言 FAIL。此即 UAT test 6 根因的可重复回归版本。

**Step 3（GREEN）— 修改两处实现：**

1. **cookie-manager.js saveDomainCookies（行 183-194 的过滤逻辑）**：对齐渲染层 applyDomainFilter 的 subdomain 语义——先剥离 cookie.domain 的前导点（startsWith('.') 时 slice(1)），includeSubdomains=true 时匹配条件为 cookieDomain === domain 或 domain.endsWith('.' + cookieDomain)（即 Cookie 所属域是当前域名本身或其父域，与面板"含子域名"过滤同集合）；includeSubdomains=false 时仅 cookieDomain === domain。保留函数签名与参数名不变（ipc-handlers.js:466-474 与 preload.js:241 的通道签名不动，仅语义修正），但更新 JSDoc：将"只保存当前域名及其子域名的 Cookie"改写为"保存当前页面可见的域名 Cookie（当前域名及其父域，与 Cookie 管理面板'含子域名'过滤集合一致）"。合并保留逻辑（行 210-243）与返回 count 语义（仅计本次匹配的 session Cookie）保持不变。

2. **src/renderer.js handleSaveToFile（行 2277-2290 的域名提取段）**：删除通过 document 的 CSS 选择器查询 API 两次查找元素（先查激活 tab、再按 data-tab-id 查 webview 元素）并从元素属性读 URL 的整个分支——该分支依赖的容器 class 在应用中从未存在（createWebviewForTab 创建的裸 webview 元素直挂 browserView，无该 class、无 data-tab-id），且元素 src 属性不随导航更新。替换为 showCookiesModal（行 1955-1966）同款模式：const activeTab = state.tabs.get(state.activeTabId)，若 activeTab 且 activeTab.url 存在，则 try 内 domain = new URL(activeTab.url).hostname，catch 保持 domain 为 ''。函数其余部分（domain 非空走 saveDomainCookies 分支含域名 toast、为空走 saveCookie 全量保存 fallback 含无域名 toast）原样保留——realm:// 新标签页等无域名场景的既有回退行为是有意设计，不在本 gap 范围。JSDoc 摘要更新为"保存当前标签页可见域名的 Cookie 到文件（与面板过滤集合一致）"。

**明确禁止：**
- 不得修改 saveCookies（全量保存是 fallback 路径，本身设计如此）、editCookie、deleteSingleCookie、getSessionCookies、getFileCookies
- 不得修改 ipc-handlers.js 与 src/preload.js（通道签名与校验不变）
- 不得修改 applyDomainFilter（UI 侧是本次对齐的基准，不动）
- 新增的 JSDoc/注释中不得引用被删除的旧选择器类名（避免死字面值残留）

**Step 4 — 再跑 node scripts/test-save-domain-cookies.js 确认 GREEN：** 全部断言 PASS，退出码 0。

冒烟测试文件纳入 git（它是 UAT test 6 根因的持久运行时回归护栏，不是一次性脚本）。
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm && node scripts/test-save-domain-cookies.js && test "$(sed -n '/async function handleSaveToFile/,/^}/p' src/renderer.js | grep -c 'querySelector')" -eq 0 && test "$(grep -c 'state\.tabs\.get(state\.activeTabId)' src/renderer.js)" -ge 2 && test "$(grep -vE '^\s*(\*|//|/\*\*)' src/renderer.js | grep -c 'webview-container')" -eq 0</automated>
    <human-check>UAT test 6 重跑（end-of-phase 人工验证时执行）：在 https://www.baidu.com/ 页面打开 xiao 容器 Cookie 管理面板，点击保存按钮 → toast 显示的保存数量与面板"含子域名"过滤下显示的数量一致（约 6 个，而非 65 个），且 toast 文案含域名</human-check>
  </verify>
  <done>
    - 冒烟测试全部断言 PASS（过滤集合 4/2、后缀伪装域拒绝、合并保留、过期丢弃、count 语义）
    - handleSaveToFile 函数体内无 querySelector，域名提取与 showCookiesModal 同源（state.tabs）
    - renderer.js 代码区不再引用修复前那个不存在的容器 class 选择器
    - 无域名场景的回退行为（saveCookie 全量保存 + 无域名 toast）保持不变
    - ipc-handlers.js / preload.js / applyDomainFilter 零改动
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 Cookie 列表文字颜色 — .cookie-col-* 显式颜色 + .modal 防御 + 死代码清理</name>
  <files>src/styles/main.css</files>
  <read_first>
    - src/styles/main.css 行 620-646（.cookie-col-name/.cookie-col-value/.cookie-col-domain：只定义布局，缺 color）
    - src/styles/main.css 行 656-662（.cookie-item：缺 color）
    - src/styles/main.css 行 328-334（.modal：只覆盖 background/border/padding，未覆盖 dialog UA 默认 color）
    - src/styles/main.css 行 761-783（三段未被任何渲染端引用的旧版 Cookie 列样式，死代码）
    - src/styles/main.css 行 13-15（--text-primary #f0f0f0 / --text-secondary #a0a0a0 / --text-muted #6b7280 变量定义）
    - .planning/debug/cookie-list-bg-too-dark.md（像素级根因证据：行文本字形 (0,0,0) 纯黑，背景 #3a3a3a 符合设计）
  </read_first>
  <action>
仅修改 src/styles/main.css，不改任何 HTML/JS（renderCookiesList 使用的类名与 CSS 完全匹配，缺的是颜色声明）：

1. **为列表列加显式颜色**（per debug 结论：旧设计 value 用 --text-muted #6b7280 对比度仅 2.4:1 不够，必须用 --text-secondary）：
   - .cookie-col-name 规则内追加 color: var(--text-primary);
   - .cookie-col-value 规则内追加 color: var(--text-secondary);
   - .cookie-col-domain 规则内追加 color: var(--text-secondary);
2. **行级兜底**：.cookie-item 规则内追加 color: var(--text-primary);（行内任何未命名子元素也有可读默认色）
3. **防御性修复**：.modal 规则内追加 color: var(--text-primary);——原生 dialog 的 UA 样式表为其元素级设定黑色文字并阻断 body 继承，本行恢复与 body 一致的文字基色，杜绝所有模态框后代再次踩同一坑（其他模态框若有同类隐患也一并修复，属预期收益）
4. **清理死代码**：删除第 761-783 行三段未被引用的旧版 Cookie 列样式规则（Phase 10 重构改名为 cookie-col-* 系列前的遗留类；已通过全库搜索确认渲染端与 HTML 均不引用，仅 CSS 文件自身存在）

**明确禁止：**
- 不得改动 .cookies-list 的 background-color: var(--bg-tertiary)（#3a3a3a 背景符合 UI-SPEC，像素采样已证实；用户感知的"背景太黑"实为黑字深底，修文字色即可）
- 不得调整任何布局属性（宽度/flex/padding），本任务只增删颜色相关声明与删除死代码
- 不得在注释中原样抄写被删除的旧类选择器名（避免死字面值残留）
  </action>
  <verify>
    <automated>cd /Users/wxnacy/Projects/Realm && test "$(sed -n '/^\.cookie-col-name {/,/}/p' src/styles/main.css | grep -c 'color: var(--text-primary)')" -ge 1 && test "$(sed -n '/^\.cookie-col-value {/,/}/p' src/styles/main.css | grep -c 'color: var(--text-secondary)')" -ge 1 && test "$(sed -n '/^\.cookie-col-domain {/,/}/p' src/styles/main.css | grep -c 'color: var(--text-secondary)')" -ge 1 && test "$(sed -n '/^\.cookie-item {/,/}/p' src/styles/main.css | grep -c 'color: var(--text-primary)')" -ge 1 && test "$(sed -n '/^\.modal {/,/}/p' src/styles/main.css | grep -c 'color: var(--text-primary)')" -ge 1 && test "$(grep -cE '^\.cookie-(name|value|domain) \{' src/styles/main.css)" -eq 0 && test "$(grep -c 'background-color: var(--bg-tertiary)' src/styles/main.css)" -ge 1</automated>
    <human-check>UAT test 7 重跑（end-of-phase 人工验证时执行）：打开 Cookie 管理面板 → Cookie 列表的 name 列呈近白色、value/domain 列呈浅灰色，在深色行背景上清晰可读；面板整体样式与应用深色主题一致</human-check>
  </verify>
  <done>
    - .cookie-col-name 显式 color: var(--text-primary)（#f0f0f0 on #3a3a3a，对比度约 12:1）
    - .cookie-col-value / .cookie-col-domain 显式 color: var(--text-secondary)（#a0a0a0 on #3a3a3a，对比度约 6.4:1，均超 WCAG AA 4.5:1）
    - .cookie-item 与 .modal 均有 color: var(--text-primary) 兜底
    - 行 761-783 三段死代码已删除，文件内不再存在这三条旧版规则
    - .cookies-list 背景色保持 var(--bg-tertiary) 不变
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | 类型 | 说明 |
|----------|------|------|
| src/renderer.js | 修改 | handleSaveToFile 域名提取改用 state.tabs 模式，域名非空时真正触达 saveDomainCookies |
| cookie-manager.js | 修改 | saveDomainCookies 过滤方向与 UI subdomain 过滤语义对齐（父域匹配），JSDoc 同步更新 |
| src/styles/main.css | 修改 | .cookie-col-* 显式颜色 + .cookie-item/.modal 颜色兜底 + 死代码清理 |
| scripts/test-save-domain-cookies.js | 新增 | 保存过滤语义与合并保留行为的运行时回归测试，纯 Node 可重复运行 |
| .planning/phases/10-Cookie管理增强/10-02-SUMMARY.md | 新增 | 本计划执行记录 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → main (IPC) | cookie:save-domain 的 domain/containerId 参数由渲染进程传入，属不可信输入 |
| main process → 文件系统 | saveDomainCookies 写 containers/{id}/cookies.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | saveDomainCookies 域名过滤 | high | mitigate | 过滤方向与 UI 对齐（本计划核心修复）+ scripts/test-save-domain-cookies.js 运行时回归锁定语义，含 '.baidu.com.evil.com' 后缀伪装域拒绝用例 |
| T-10-02-02 | Tampering | cookie:save-domain 参数注入 | low | accept | domain 仅参与字符串相等/后缀比较，不进路径、SQL 或命令拼接；containerId 校验与 assertTrustedSender 为 ipc-handlers.js 既有控制，本计划不改动 |
| T-10-02-03 | Information Disclosure | Cookie 值渲染进 DOM | low | accept | 本计划不改动渲染路径，既有 textContent 防护（10-01 T-10-05）保持有效 |
| T-10-02-SC | Tampering | npm/pip/cargo installs | low | accept | 无新增依赖（冒烟测试仅用 Node 内置模块 fs/path/os 与依赖打桩） |
</threat_model>

<verification>
## 整体验证（对应 gap closure 三层要求）

1. **静态检查**：handleSaveToFile 函数体内 querySelector 出现 0 次；renderer.js 中 state.tabs.get(state.activeTabId) ≥ 2 处；renderer.js 代码区不再引用修复前的不存在容器 class；CSS 五条颜色断言 + 死代码三条规则 0 命中 + 背景色保持断言全部通过
2. **运行时检查**：node scripts/test-save-domain-cookies.js 退出码 0（先 RED 后 GREEN，TDD 流程证据）
3. **UAT 重跑（end-of-phase 人工验证）**：10-UAT.md test 6（baidu 页面保存数量 = 面板"含子域名"过滤显示数量，toast 含域名）与 test 7（列表文字清晰可读）由 issue 转为 pass
</verification>

<success_criteria>
- UAT gap 1 关闭：保存按钮保存的 Cookie 集合 ≡ 面板"含子域名"过滤显示集合（UI 所见即所存），不再出现保存 65 个的情况
- UAT gap 2 关闭：Cookie 列表 name 列 #f0f0f0、value/domain 列 #a0a0a0，在 #3a3a3a 背景上对比度均超 4.5:1
- 冒烟测试纳入 git，作为保存过滤语义的持久回归护栏
- 无回归：来源切换/域名过滤/分页/编辑/删除/全量保存 fallback 均不受影响（改动域严格限于 handleSaveToFile 域名提取段、saveDomainCookies 过滤段、main.css 颜色声明三处）
</success_criteria>

<output>
Create `.planning/phases/10-Cookie管理增强/10-02-SUMMARY.md` when done
</output>
