---
phase: 20
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - ipc-handlers.js
  - src/preload.js
  - src/index.html
  - src/styles/main.css
  - src/renderer.js
autonomous: true
requirements:
  - AI-02

must_haves:
  truths:
    - IPC 通道 ai:prompt, ai:abort, ai:configure, ai:get-models, ai:get-state 注册成功
    - 渲染进程可通过 window.realmAPI.ai 调用所有 AI 接口
    - 设置页面显示 "AI 助手" 分区
    - API Key 输入框和模型选择下拉框可交互
  artifacts:
    - ipc-handlers.js（包含 AI IPC 通道注册）
    - src/preload.js（包含 window.realmAPI.ai）
    - src/index.html（包含 AI 助手设置分区 HTML）
    - src/styles/main.css（包含 AI 设置分区样式）
    - src/renderer.js（包含 AI 设置分区渲染逻辑）
  key_links:
    - ipc-handlers.js → ai-manager.js（IPC 调用 AIManager 方法）
    - src/preload.js → ipc-handlers.js（Preload 桥接 IPC）
    - src/renderer.js → src/preload.js（渲染进程调用 AI API）
---

<objective>
注册 AI IPC 通道，暴露 Preload API，添加设置页面 "AI 助手" 分区。

Purpose: 打通渲染进程与 AI Manager 的通信，提供用户配置界面。
Output: 完整的 IPC 通道、Preload API 和设置页面 UI。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ai-agent/20-CONTEXT.md
@.planning/phases/20-ai-agent/20-RESEARCH.md
@.planning/phases/20-ai-agent/20-UI-SPEC.md
@.planning/phases/20-ai-agent/20-01-SUMMARY.md
@ipc-handlers.js
@src/preload.js
@src/index.html
@src/styles/main.css
@src/renderer.js
@ai-manager.js
@main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注册 AI IPC 通道</name>
  <files>ipc-handlers.js, main.js</files>
  <read_first>
    - ipc-handlers.js（当前 IPC 注册模式）
    - main.js（AIManager 初始化位置）
    - ai-manager.js（AIManager 方法签名）
    - .planning/phases/20-ai-agent/20-CONTEXT.md（D-05~D-08 决策）
    - .planning/phases/20-ai-agent/20-RESEARCH.md（IPC 通道清单）
  </read_first>
  <action>
在 ipc-handlers.js 中注册 AI 相关 IPC 通道，遵循现有模式（kebab-case、动词-名词格式）：

1. **添加 AIManager 引用**：在文件顶部添加 `const AIManager = require('./ai-manager');`

2. **添加 aiManager 实例管理**：
   - 在模块顶部添加 `let aiManager = null;`
   - 添加 `async function initAIManager(configStore)` 函数，创建 AIManager 实例并调用 `init(configStore)`
   - 导出 `initAIManager` 供 main.js 调用

3. **注册 IPC 通道**：

```javascript
// AI 相关通道
ipcMain.handle('ai:prompt', async (event, message) => {
  assertTrustedSender(event);
  if (!message || typeof message !== 'string') {
    throw new Error('无效的消息');
  }
  if (!aiManager) {
    throw new Error('AI Manager 未初始化');
  }
  await aiManager.prompt(message);
  return { success: true };
});

ipcMain.handle('ai:abort', async (event) => {
  assertTrustedSender(event);
  if (aiManager) {
    aiManager.abort();
  }
  return { success: true };
});

ipcMain.handle('ai:configure', async (event, config) => {
  assertTrustedSender(event);
  if (!config || !config.apiKey) {
    throw new Error('无效的配置');
  }
  if (aiManager) {
    await aiManager.configureProviders(config);
  }
  return { success: true };
});

ipcMain.handle('ai:get-models', async (event) => {
  assertTrustedSender(event);
  if (!aiManager) {
    return { models: [] };
  }
  return aiManager.getAvailableModels();
});

// ai:get-state — 新增查询通道（设置页面获取 AI 状态），原 ai:event 已演进为 ai:events-batch（per D-08 批量事件决策）
ipcMain.handle('ai:get-state', async (event) => {
  assertTrustedSender(event);
  if (!aiManager) {
    return { initialized: false, model: null, toolsCount: 0 };
  }
  return aiManager.getState();
});
```

4. **修改 main.js**：
   - 在 app.whenReady() 中调用 `initAIManager(configStore)`
   - 确保 AIManager 在 ipc-handlers 加载后初始化

5. **导出 initAIManager**：在 ipc-handlers.js 底部添加 `module.exports = { ..., initAIManager }`
  </action>
  <verify>
    <automated>grep -c "ai:prompt\|ai:abort\|ai:configure\|ai:get-models\|ai:get-state" ipc-handlers.js | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - ipc-handlers.js 包含 `let aiManager = null;`
    - ipc-handlers.js 包含 `async function initAIManager(configStore)` 函数定义
    - ipc-handlers.js 包含 `ipcMain.handle('ai:prompt', ...)` 注册
    - ipc-handlers.js 包含 `ipcMain.handle('ai:abort', ...)` 注册
    - ipc-handlers.js 包含 `ipcMain.handle('ai:configure', ...)` 注册
    - ipc-handlers.js 包含 `ipcMain.handle('ai:get-models', ...)` 注册
    - ipc-handlers.js 包含 `ipcMain.handle('ai:get-state', ...)` 注册
    - 每个 IPC 通道调用 `assertTrustedSender(event)` 验证来源
    - ipc-handlers.js 导出包含 `initAIManager`
    - main.js 中调用 `initAIManager(configStore)` 初始化 AIManager
  </acceptance_criteria>
  <done>AI IPC 通道全部注册，main.js 调用 initAIManager 初始化</done>
</task>

<task type="auto">
  <name>Task 2: 暴露 Preload.js AI API</name>
  <files>src/preload.js</files>
  <read_first>
    - src/preload.js（当前 contextBridge 暴露模式）
    - .planning/phases/20-ai-agent/20-UI-SPEC.md（Preload API Contract）
    - .planning/phases/20-ai-agent/20-CONTEXT.md（D-05~D-08 决策）
  </read_first>
  <action>
在 src/preload.js 中暴露 AI 相关 API，遵循现有模式（contextBridge.exposeInMainWorld）：

在 `contextBridge.exposeInMainWorld('realmAPI', { ... })` 对象中添加 `ai` 属性：

```javascript
// AI 相关 API
ai: {
  /**
   * 发送用户消息给 AI Agent
   * @param {string} message - 用户输入的消息
   * @returns {Promise<{success: boolean, error?: string}>}
   */
  prompt: (message) => ipcRenderer.invoke('ai:prompt', message),

  /**
   * 取消当前 Agent 执行
   * @returns {Promise<{success: boolean}>}
   */
  abort: () => ipcRenderer.invoke('ai:abort'),

  /**
   * 监听 Agent 事件（批量）
   * @param {function} callback - 接收事件数组的回调
   * @returns {function} 取消监听的清理函数
   */
  onEventsBatch: (callback) => {
    ipcRenderer.on('ai:events-batch', (_, data) => callback(data.events));
    return () => ipcRenderer.removeAllListeners('ai:events-batch');
  },

  /**
   * 配置 AI 提供商 API Key
   * @param {Object} config - { provider: string, apiKey: string }
   * @returns {Promise<{success: boolean, error?: string}>}
   */
  configureProviders: (config) => ipcRenderer.invoke('ai:configure', config),

  /**
   * 获取可用模型列表
   * @returns {Promise<{models: Array<{provider: string, id: string, name: string}>}>}
   */
  getAvailableModels: () => ipcRenderer.invoke('ai:get-models'),

  /**
   * 获取当前 Agent 状态
   * @returns {Promise<{initialized: boolean, model: string|null, toolsCount: number}>}
   */
  getState: () => ipcRenderer.invoke('ai:get-state'),
},
```
  </action>
  <verify>
    <automated>grep -c "window.realmAPI.ai" src/preload.js | grep -v "^0$" && grep -c "ai:prompt\|ai:abort\|ai:events-batch\|ai:configure\|ai:get-models\|ai:get-state" src/preload.js | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/preload.js 中 `contextBridge.exposeInMainWorld('realmAPI', { ... })` 对象包含 `ai` 属性
    - `ai` 属性包含 `prompt` 方法，调用 `ipcRenderer.invoke('ai:prompt', message)`
    - `ai` 属性包含 `abort` 方法，调用 `ipcRenderer.invoke('ai:abort')`
    - `ai` 属性包含 `onEventsBatch` 方法，监听 `ai:events-batch` 通道
    - `onEventsBatch` 返回清理函数 `() => ipcRenderer.removeAllListeners('ai:events-batch')`
    - `ai` 属性包含 `configureProviders` 方法，调用 `ipcRenderer.invoke('ai:configure', config)`
    - `ai` 属性包含 `getAvailableModels` 方法，调用 `ipcRenderer.invoke('ai:get-models')`
    - `ai` 属性包含 `getState` 方法，调用 `ipcRenderer.invoke('ai:get-state')`
    - 每个方法包含 JSDoc 注释说明功能和参数
  </acceptance_criteria>
  <done>window.realmAPI.ai 暴露所有 AI 相关接口，包含完整的 JSDoc 注释</done>
</task>

<task type="auto">
  <name>Task 3: 添加设置页面 "AI 助手" 分区</name>
  <files>src/index.html, src/styles/main.css, src/renderer.js</files>
  <read_first>
    - src/index.html（当前设置页面结构）
    - src/styles/main.css（当前设置页面样式）
    - src/renderer.js（当前设置页面渲染逻辑）
    - .planning/phases/20-ai-agent/20-UI-SPEC.md（Settings AI Section 视觉规范）
    - .planning/phases/20-ai-agent/20-CONTEXT.md（D-01~D-04 决策）
  </read_first>
  <action>
在设置页面添加 "AI 助手" 分区，遵循现有设置页面模式（.settings-page / .settings-section / .settings-group / .settings-item）：

1. **src/index.html - 添加设置侧边栏项**：
   在设置侧边栏中添加 "AI 助手" 项，与现有 "快捷键设置"、"收藏栏" 等同级：
   ```html
   <div class="settings-sidebar-item" data-section="ai-assistant">
     <svg>...</svg>
     <span>AI 助手</span>
   </div>
   ```

2. **src/index.html - 添加设置内容区**：
   ```html
   <div class="settings-section" id="settings-ai-assistant" style="display: none;">
     <h2>AI 助手配置</h2>

     <!-- API 配置组 -->
     <div class="settings-group">
       <h3>API 配置</h3>

       <div class="settings-item">
         <label>API Key</label>
         <div class="settings-input-wrapper">
           <input type="password" id="ai-api-key" placeholder="输入你的 OpenAI API Key" class="settings-input">
           <button id="ai-toggle-key-visibility" class="btn-icon" title="显示/隐藏">
             <svg>...</svg>
           </button>
         </div>
       </div>

       <div class="settings-item">
         <label>默认模型</label>
         <select id="ai-model-select" class="settings-select">
           <option value="">选择模型</option>
         </select>
       </div>

       <div class="settings-item">
         <button id="ai-save-config" class="btn-primary">保存配置</button>
       </div>
     </div>

     <!-- 连接状态组 -->
     <div class="settings-group">
       <h3>连接状态</h3>

       <div class="settings-item">
         <div class="ai-status-indicator">
           <span class="ai-status-dot" id="ai-status-dot"></span>
           <span class="ai-status-text" id="ai-status-text">未配置 API Key</span>
         </div>
       </div>
     </div>
   </div>
   ```

3. **src/styles/main.css - 添加 AI 设置样式**：
   ```css
   /* AI 助手设置分区样式 */
   .ai-status-indicator {
     display: flex;
     align-items: center;
     gap: 6px;
   }

   .ai-status-dot {
     width: 8px;
     height: 8px;
     border-radius: 50%;
     background-color: var(--text-muted);
   }

   .ai-status-dot.connected {
     background-color: var(--success-color);
   }

   .ai-status-dot.error {
     background-color: var(--danger-color);
   }

   .ai-status-text {
     font-size: 12px;
     color: var(--text-secondary);
   }

   .ai-status-text.connected {
     color: var(--success-color);
   }

   .ai-status-text.error {
     color: var(--danger-color);
   }

   .settings-input-wrapper {
     display: flex;
     align-items: center;
     gap: 8px;
   }

   .settings-input-wrapper .settings-input {
     flex: 1;
   }
   ```

4. **src/renderer.js - 添加 AI 设置逻辑**：
   - 添加 `initAISettings()` 函数，初始化 AI 设置页面
   - 加载时调用 `window.realmAPI.ai.getState()` 获取当前状态
   - 加载时调用 `window.realmAPI.ai.getAvailableModels()` 填充模型下拉框
   - 保存按钮点击时调用 `window.realmAPI.ai.configureProviders({ provider: 'openai', apiKey })`
   - 监听 `ai:events-batch` 更新连接状态
   - API Key 显示/隐藏切换按钮逻辑

5. **在设置页面切换逻辑中添加 AI 助手分区**：
   - 在 `switchSettingsSection(section)` 函数中添加 `case 'ai-assistant':` 分支
   - 显示对应的 settings-section，隐藏其他
  </action>
  <verify>
    <automated>grep -c "settings-ai-assistant" src/index.html | grep -v "^0$" && grep -c "ai-status-dot" src/styles/main.css | grep -v "^0$" && grep -c "initAISettings" src/renderer.js | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.html 包含 `class="settings-sidebar-item" data-section="ai-assistant"` 侧边栏项
    - src/index.html 包含 `id="settings-ai-assistant"` 设置内容区
    - src/index.html 包含 `id="ai-api-key"` 密码输入框（type="password"）
    - src/index.html 包含 `id="ai-model-select"` 模型选择下拉框
    - src/index.html 包含 `id="ai-save-config"` 保存按钮
    - src/index.html 包含 `id="ai-status-dot"` 和 `id="ai-status-text"` 状态指示器
    - src/styles/main.css 包含 `.ai-status-indicator`, `.ai-status-dot`, `.ai-status-text` 样式
    - src/styles/main.css 包含 `.settings-input-wrapper` 样式
    - src/renderer.js 包含 `initAISettings()` 函数定义
    - src/renderer.js 调用 `window.realmAPI.ai.getState()` 获取状态
    - src/renderer.js 调用 `window.realmAPI.ai.getAvailableModels()` 填充模型列表
    - src/renderer.js 保存按钮调用 `window.realmAPI.ai.configureProviders()`
    - 设置侧边栏切换时显示 AI 助手分区
  </acceptance_criteria>
  <done>设置页面 "AI 助手" 分区完整实现，包含 API Key 配置、模型选择和连接状态显示</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main | IPC 调用通过 contextBridge 暴露，需要验证来源 |
| main → LLM | API Key 存储在主进程，不暴露到渲染进程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Information Disclosure | API Key 传输 | high | mitigate | API Key 通过 IPC 传输，使用 assertTrustedSender 验证来源 |
| T-20-05 | Tampering | 设置页面输入 | medium | mitigate | 输入验证，防止 XSS 注入 |
| T-20-SC | Tampering | npm installs | low | accept | Phase 19 已验证 pi-ai 和 pi-agent-core 包合法性 |
</threat_model>

<verification>
1. 验证 IPC 通道注册：`grep -c "ai:prompt\|ai:abort\|ai:configure" ipc-handlers.js`
2. 验证 Preload API：`grep -c "window.realmAPI.ai" src/preload.js`
3. 验证设置页面：`grep -c "settings-ai-assistant" src/index.html`
4. 验证样式：`grep -c "ai-status-dot" src/styles/main.css`
5. 验证渲染逻辑：`grep -c "initAISettings" src/renderer.js`
6. 启动 Electron 应用，打开设置页面，检查 AI 助手分区显示正常
</verification>

<success_criteria>
- IPC 通道 ai:prompt, ai:abort, ai:configure, ai:get-models, ai:get-state 注册成功
- window.realmAPI.ai 暴露所有接口
- 设置页面显示 "AI 助手" 分区
- API Key 输入框和模型选择下拉框可交互
- 连接状态指示器正常显示
- 保存配置功能正常
</success_criteria>

<output>
创建 `.planning/phases/20-ai-agent/20-02-SUMMARY.md` 当完成时
</output>
