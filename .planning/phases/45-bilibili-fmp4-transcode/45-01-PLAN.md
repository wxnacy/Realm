---
phase: 45-bilibili-fmp4-transcode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - media-m3u8-parser.js
  - tests/test-m3u8-playlist-parser.js
  - media-remuxer.js
  - tests/test-media-remuxer.js
autonomous: true
requirements: [D-01, D-02, D-04, D-05, D-06, D-07]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 带 #EXT-X-MAP 的 m3u8 清单经 parsePlaylist 解析出 mapUri（多 MAP 覆盖取最新 = RFC 8216 §4.3.2.5 作用域语义）与 mapByterange，无 MAP 清单两字段缺省 null 不回归（D-05）
    - fMP4 分片序列 + initPath 经 convertToMp4 转出 fMP4 产物：init 在前 + 分片按播放序原样字节拼接，不解析 moof/trun 内部（D-01/D-07）
    - fMP4 分片无 initPath → reason=init_missing 拒转且半成品产物被清理（D-06 分流替代旧 unsupported_container 语义）
    - init 非 ftyp 开头 → reason=invalid_init 拒转（fail-fast，畸形 init 不产静默坏产物）
    - MPEG-TS 转换路径（0x47 放行）与加密流拒转（D-04 维持）零回归；「取消 < 嗅探 < push」源码结构断言保持绿（44-08 契约）
    - 产物终检含 mux.js probe 校验（moov 存在 + moof 计数 ≥1，不断言具体 track 数——Pitfall 9）
  artifacts:
    - media-m3u8-parser.js：parsePlaylist 返回值新增 mapUri/mapByterange（缺省 null）
    - media-remuxer.js：concatFmp4ToMp4({ initPath, segmentPaths, outputPath, onProgress, shouldCancel }) 新导出；convertToMp4 新增可选入参 initPath
    - tests/test-m3u8-playlist-parser.js：EXT-X-MAP 用例组
    - tests/test-media-remuxer.js：:176 拒转用例改写为 init_missing 分流语义 + concatFmp4ToMp4 用例组 + mux.js generator 夹具 helper（内联）
  key_links:
    - sniffContainerBuffer fMP4 box 命中 → convertToMp4 首片嗅探块内分流（取消检查之后、TS push 之前）→ concatFmp4ToMp4
    - :176 测试改写与分流语义同 PR 同步（Pitfall 6：不改则测试套件红）
  prohibitions:
    - statement: 不引 ffmpeg、不引 mp4box.js、不做 moof/trun 解析重打包常规 MP4（D-01 locked，零新依赖）
      status: locked
      verification: package.json 依赖无增量；media-remuxer.js 无 ffmpeg/mp4box require；concatFmp4ToMp4 本体为字节拷贝无 box 内部解析
    - statement: 产物不承诺剪辑软件兼容（D-02「能看就行」= mpv/IINA/VLC/QuickTime 可播 fMP4）
      status: locked
      verification: 产物为 fMP4 容器（init 的 mvhd duration=0 是 RFC 8216 强制形态，不修 moov）
---

<objective>
交付 Phase 45 地基：EXT-X-MAP 行级解析（D-05，全链路前置）与 fMP4 纯 JS 拼接执行体 concatFmp4ToMp4（D-01）+ convertToMp4 嗅探分流（D-06）。

Purpose: B 站直播 fMP4 流当前被 sniffContainerFormat 按 unsupported_container 拒转（G-44-7 由来）；本计划把「拒转」改为「分流」——有 init 走字节拼接、无 init 走 init_missing 引导，同时保持 TS 路径与加密拒转（D-04）零回归。
Output: parser mapUri/mapByterange 捕获、concatFmp4ToMp4 拼接执行体、convertToMp4 initPath 分流、冲突测试 :176 改写。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bilibili-fmp4-transcode/45-CONTEXT.md
@.planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md
@.planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md

@media-m3u8-parser.js
@media-remuxer.js
@tests/test-m3u8-playlist-parser.js
@tests/test-media-remuxer.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-m3u8-parser.js`：parsePlaylist 返回字段 `mapUri: string|null`、`mapByterange: string|null`
- `media-remuxer.js`：`concatFmp4ToMp4({ initPath, segmentPaths, outputPath, onProgress, shouldCancel })`（Promise resolve `{ outputPath, segments }`，新导出）；convertToMp4 入参 `initPath`（可选）
- 新 remuxError reason：`init_missing`、`invalid_init`
- tests/test-media-remuxer.js 内联夹具 helper：`makeInit()` / `makeFmp4Segment(seq)`（mux.js mp4.generator 封装）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parsePlaylist EXT-X-MAP 捕获（mapUri/mapByterange）+ parser 单测（D-05）</name>
  <files>media-m3u8-parser.js, tests/test-m3u8-playlist-parser.js</files>
  <read_first>
    - media-m3u8-parser.js（全文件：lines 15-28 JSDoc 返回类型、30-39 result 初始化结构、55-73 EXT-X-KEY 捕获段——同款加法先例、84-89 未知标签忽略逻辑；文件头 line 9「纯 Node 可加载」硬约束注释）
    - tests/test-m3u8-playlist-parser.js（lines 148-175 EXT-X-KEY 用例组——行拼接清单 + 字段断言模式）
    - .planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md（media-m3u8-parser.js 段：行级捕获模式与结果结构缺省字段模式原文摘录）
    - .planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md（Q4 EXT-X-MAP RFC 语义、Q5 Parser 代码草案）
  </read_first>
  <behavior>
    - 含 `#EXT-X-MAP:URI="init.m4s"` 清单 → mapUri='init.m4s'、mapByterange=null
    - 含 BYTERANGE 属性（`URI="init.mp4",BYTERANGE="720@0"`）→ mapByterange 原样字符串捕获备用
    - 无 MAP 标签清单 → mapUri=null、mapByterange=null（向后兼容，旧调用方零影响）
    - 清单含两个 MAP 标签 → 取后一个（覆盖语义 = RFC 8216 §4.3.2.5「applies until the next EXT-X-MAP」）
    - 含 #EXT-BILI-AUX 等未知标签混合清单 → 安全忽略，mapUri 捕获不受影响（B 站实测清单形态）
  </behavior>
  <action>
  1. media-m3u8-parser.js：在 `#` 分支内、EXT-X-KEY 段附近新增 `#EXT-X-MAP:` 行级捕获（per D-05，照 44-18 EXT-X-KEY 同款加法）：`trimmed.startsWith('#EXT-X-MAP:')` 后取 attrs，URI 用正则 `/(?:^|, )URI="([^"]*)"/`（去掉空格，实际为 `/(?:^|,)URI="([^"]*)"/`）匹配，命中则 `result.mapUri = uriMatch[1]`（多 MAP 覆盖取最新）；BYTERANGE 用 `/(?:^|,)BYTERANGE="([^"]*)"/` 捕获，`result.mapByterange = brMatch ? brMatch[1] : null`（每次 MAP 行都刷新，与 mapUri 同作用域）；`continue` 收尾不进 EXTINF 匹配。
  2. result 初始化（lines 30-39）追加 `mapUri: null, mapByterange: null`；JSDoc 返回类型（lines 15-28）同步补两字段说明（含「fMP4 清单必有 MAP（RFC 8216 §4.3.2.5），反之有 MAP 即应按 fMP4 处理」语义注释）。
  3. 顶层不新增任何 electron/原生 require（line 9 纯 Node 硬约束）；未知 `#` 行忽略逻辑（lines 84-89）零改动。
  4. tests/test-m3u8-playlist-parser.js 新增「EXT-X-MAP 捕获（D-05）」用例组，覆盖 <behavior> 五案例（行拼接清单 + 字段断言，照 lines 148-175 模式）。
  </action>
  <verify>
    <automated>node tests/test-m3u8-playlist-parser.js && node -e "require('./media-m3u8-parser'); console.log('pure-node ok')"</automated>
    <fails_when>测试非零退出（fail ≥1 或 pass 0）；或 node -e 非零退出（顶层引入 electron/原生依赖破坏纯 Node 约束）</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "mapUri" media-m3u8-parser.js` 命中 ≥3 处（初始化、捕获赋值、JSDoc）
    - `grep -n "EXT-X-MAP" media-m3u8-parser.js tests/test-m3u8-playlist-parser.js` 两文件均命中
    - 行为断言全绿：<behavior> 五案例对应测试全部 pass
    - 既有 EXT-X-KEY 用例组（lines 148-175）零修改零回归
  </acceptance_criteria>
  <done>parsePlaylist 对 B 站形态清单（EXT-X-MAP + EXT-BILI-AUX 混合）稳定解析出 mapUri/mapByterange，无缺省回归，单测全绿。</done>
  <reversibility rating="reversible">parser 纯增量字段，旧调用方读不到新字段无影响，回退即删分支。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: concatFmp4ToMp4 拼接执行体 + convertToMp4 initPath 嗅探分流 + :176 用例改写（D-01/D-06/D-07）</name>
  <files>media-remuxer.js, tests/test-media-remuxer.js</files>
  <read_first>
    - media-remuxer.js（全文件：lines 25 mux.js require 方式、71-75 remuxError 工厂、125-149 sniffContainerBuffer 判定序、251-405 convertToMp4 执行体六件套、407 module.exports）
    - tests/test-media-remuxer.js（lines 164-174 取消<嗅探<push 结构断言、176-195 ⚠️ 冲突用例、246-260 取消先于嗅探语义、262-270 CONVERT_FAIL_TEXT 文案断言）
    - .planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md（media-remuxer.js 段：执行体契约六件套逐项原文 + 分流位置约束）
    - .planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md（Q3 probe/generator 实测结论、Q5 拼接执行体草案、Q6 夹具方案、Pitfall 3/4/6/9）
  </read_first>
  <behavior>
    - concatFmp4ToMp4(init+两 mk 分片) → 产物字节 = init+seg1+seg2 精确相等（Buffer.compare），probe.findBox 检出 moov 且 moof 计数 ≥ 分片内 moof 总数
    - 单分片文件内含多个 moof+mdat 对（B 站实测形态）→ 原样通过不解析不拆箱
    - init 前 8 字节非 size+ftyp → reject reason=invalid_init 且产物清理
    - shouldCancel 中途为 true → reject reason=cancelled 且半成品清理
    - convertToMp4(fMP4 分片, 无 initPath) → reject reason=init_missing 且产物清理（:176 改写后的新语义）
    - convertToMp4(fMP4 分片, 有 initPath) → 走拼接分支 resolve，产物 probe 终检通过
    - convertToMp4(TS 分片) → 既有 mux.js 路径零回归
  </behavior>
  <action>
  1. 夹具 helper（内联 tests/test-media-remuxer.js 顶部，仓库无 fixtures 目录先例——RESEARCH Q6）：用既有依赖 mux.js 6.3.0 的 `muxjs.mp4.generator`：`makeInit()` = `generator.initSegment([videoTrack])`（实测 ~658B，probe 可解析）；`makeFmp4Segment(seq)` = `Buffer.concat([generator.moof(seq,[track]), generator.mdat(payload), generator.moof(seq+1,[track]), generator.mdat(payload)])`——单文件两个 moof+mdat 对、无 styp/ftyp，复刻 B 站实测多 moof 形态。
  2. 新增 `concatFmp4ToMp4({ initPath, segmentPaths, outputPath, onProgress, shouldCancel })`（per D-01/D-06，~50 行）：逐项复刻 convertToMp4 执行体六件套（PATTERNS 原文清单）——①入参 fail-fast（segmentPaths 空 → no_segments、initPath 缺失/不可读 → init_missing）②`fs.openSync(outputPath,'w')` 同步 fd 产物创建（G-44-7 竞态修复，禁退回 createWriteStream 隐式 open）③fail() = destroy+unlink 统一清理 ④每分片迭代开头 shouldCancel 协作式取消 ⑤onProgress try/catch 不阻断 ⑥stream.end 回调内 empty_output 终检。拼接本体：读 init 前 8 字节校验 size+`ftyp`（不符 → remuxError('invalid_init', ...)）→ 写 init 全量 → 逐分片 `fs.readFileSync` + `stream.write` 原样字节写出（init 在前 + 分片按入参播放序，per D-07；不解析 moof/trun 内部，攻击面=字节拷贝）。产物终检加强档（RESEARCH Q5 建议采纳）：size>0 之外用 `muxjs.mp4.probe.findBox` 校验 moov 存在 + moof 计数 ≥1，不满足按 empty_output 同款清理拒转；**不断言具体 track 数**（Pitfall 9，音频-only 流合法）；probe 调用本身异常时降级为 ftyp 头+大小基线校验（mux.js 为既有依赖，Q3 已实测对真实 B 站产物工作）。
  3. sniffContainerBuffer 改造（per D-06）：fMP4 box 特征分支（②，ftyp/styp/moof/moov/sidx ASCII 命中）不再直接返回 unsupported_container，改为返回可识别的「fMP4 命中」标记（如 `remuxError('fmp4_container', ...)` 内部信号或等价结构——调用方据此分流，该 reason 不进 CONVERT_FAIL_TEXT 不对用户可见）；TS 放行（①0x47）与高熵/空拒转（③）零改动。
  4. convertToMp4 增加可选入参 `initPath`（缺省 null）：首片嗅探块（现 lines 366-372 的 `if (i === 0)` 块内——**必须在取消检查之后、TS push 之前**，44-08 契约 :164 结构断言保持绿，Pitfall 4）命中 fMP4 标记时：initPath 非空 → destroy 当前 stream + unlink 半成品（此时尚未 push 任何分片）+ 委托 `concatFmp4ToMp4({ initPath, segmentPaths, outputPath, onProgress, shouldCancel })` 接续 resolve/reject；initPath 为空 → `fail(remuxError('init_missing', 'fMP4 视频缺少初始化段'))`。TS 路径与 encrypted/discontinuity 早拒（D-04 维持）零改动。
  5. module.exports（line 407）追加 concatFmp4ToMp4。
  6. tests/test-media-remuxer.js：改写 :176 用例为「fMP4 box 分片 + 无 initPath → reason=init_missing 且产物清理」（同一伪造 ftyp 分片夹具、产物清理断言保留——Pitfall 6 必须同 PR）；新增 concatFmp4ToMp4 用例组覆盖 <behavior> 全部案例 + convertToMp4 分流用例（传 initPath 成功 / 不传 init_missing / TS 回归）；:164/:246 断言原样保留不动。
  </action>
  <verify>
    <automated>node tests/test-media-remuxer.js && node tests/test-m3u8-playlist-parser.js && node -e "require('./media-remuxer'); console.log('pure-node ok')"</automated>
    <fails_when>任一测试非零退出（fail ≥1 / pass 0）；或 :164 结构断言红（取消/嗅探/push 优先序被破坏）；或 node -e 非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "concatFmp4ToMp4" media-remuxer.js` 命中（定义 + exports）
    - `grep -n "initPath" media-remuxer.js` 命中（convertToMp4 入参 + 分流点）
    - `grep -n "init_missing\|invalid_init" media-remuxer.js tests/test-media-remuxer.js` 两文件均命中
    - :176 用例断言 reason 为 init_missing（`grep -n "init_missing" tests/test-media-remuxer.js` 命中）且产物清理断言保留
    - 行为断言全绿：<behavior> 七案例对应测试全部 pass；:164/:246 契约断言保持绿
    - package.json 依赖零增量（D-01）：`git diff package.json` 为空
  </acceptance_criteria>
  <done>fMP4 分片有 init 即转出 probe 可解析的 fMP4 产物、无 init 走 init_missing 拒转不产坏文件；TS/加密/取消契约全部零回归；测试套件全绿。</done>
  <reversibility rating="costly">D-01 产物形态契约（fMP4 vs 常规 MP4）为用户可见产物契约，改路线=替换整个拼接模块并重测两链路；但代码边界清晰（独立函数），回退=恢复 unsupported_container 拒转语义，无数据迁移。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端 m3u8 清单 → parser | 清单文本为不可信输入，MAP URI 来自远端 |
| 远端 init/分片字节 → 拼接产物 | init 与 moof/mdat 字节为不可信输入，原样进入用户磁盘产物 |
| 拼接产物 → 本地播放器 | 畸形产物可致播放器异常（崩溃面在播放器，不在本进程） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering | 畸形 init/分片致静默坏产物 | high | mitigate | init ftyp 头校验 fail-fast（invalid_init）+ probe 终检（moov 存在 + moof 计数 ≥1）+ 失败路径 fail() 清理半成品；拼接不解析 moof/trun 内部（攻击面=字节拷贝） |
| T-45-02 | DoS | 超大分片/清单致内存膨胀 | medium | mitigate | 流式写盘（逐分片 readFileSync + createWriteStream，T-44-17 沿用）；分片来源侧 MAX_SEGMENT_BYTES 64MB 上限沿用（T-44-03），拼接层不全量入内存 |
| T-45-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新依赖（D-01 locked）——供应链面零增量；mux.js 6.3.0 为 Phase 44 已审既有依赖（44 RESEARCH legitimacy verdict OK），本计划仅复用 probe/generator |
</threat_model>

<verification>
- 全套件：`node tests/test-media-remuxer.js && node tests/test-m3u8-playlist-parser.js`（< 10s，VALIDATION.md quick run）
- 结构契约：:164「取消 < 嗅探 < push」与 :246「取消先于嗅探」断言保持绿（44-08 契约，Pitfall 4）
- 纯 Node 约束：`node -e "require('./media-remuxer'); require('./media-m3u8-parser')"` 零 electron 依赖
- spec-less probe fallback skipped：本阶段无 REQUIREMENTS.md 需求 ID（team-lead planning_context 明示），不生成 probe predicate——requirements 字段按 Phase 44 先例填 D-XX 决策 ID（可见记录的选择）
</verification>

<success_criteria>
- B 站形态清单（EXT-X-MAP + EXT-BILI-AUX）解析出 mapUri，既有清单解析零回归
- init + fMP4 分片字节拼接产物经 mux.js probe 检出 moov + moof（与 RESEARCH Q2 真实 B 站流实测结论同形态）
- :176 用例改写为 init_missing 分流语义且套件全绿（Pitfall 6 关闭）
- 零新依赖（package.json 无 diff，D-01）
</success_criteria>

<output>
创建 `.planning/phases/45-bilibili-fmp4-transcode/45-01-SUMMARY.md` 当完成
</output>
