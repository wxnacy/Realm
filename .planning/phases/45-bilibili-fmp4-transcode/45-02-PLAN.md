---
phase: 45-bilibili-fmp4-transcode
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - media-record-engine.js
  - main.js
  - tests/test-media-record-duration.js
  - tests/test-media-remuxer.js
autonomous: true
requirements: [D-03, D-05, D-06]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 录制引擎每轮解析后检查 pl.mapUri：首次见到或变化即下载 init 落盘 taskDir/init——**首轮 baseline 也下载**（Pitfall 1 最高危：init 不是媒体分片，D-21 baseline 语义只豁免分片回溯）（D-05）
    - meta.json 附加 mapUri/initFile/mapByterange 字段（44-18 先例不升版本，旧 meta 缺字段 = fMP4 能力缺省关闭）
    - readRecordTaskSegments 读出 meta.initFile 拼 initPath（fs.existsSync 校验），startConvertFromRecordTask input 构造（initPath: bundle.initPath）→ startConvertTask 解构透传 convertToMp4 两跳接线完整（D-06 管线全盘复用：弹框/注册/取消令牌/进度/终态零改动）
    - tracer 闭环（D-03 录制链路先行）：stub 带 MAP 清单录制 → meta.initFile → initPath → convertToMp4 分流 → probe 可解析产物
    - main.js CONVERT_FAIL_TEXT 覆盖 init_missing 与 invalid_init（Pitfall 5：缺条目任务页落「未知原因」，有测试断言）
    - mapByterange 非空 → 不下载 init 并 console.warn（整资源形态以外的 MAP 不支持，转换落 init_missing 兜底不静默产坏产物——RESEARCH Route D）
  artifacts:
    - media-record-engine.js：pollLoop init 下载检查点 + writeMeta 附加字段
    - main.js：readRecordTaskSegments 返回 initPath；startConvertTask 透传 initPath；CONVERT_FAIL_TEXT 新增 init_missing/invalid_init 文案
    - tests/test-media-record-duration.js：init 留存 stub e2e 用例组（tracer 验证）
    - tests/test-media-remuxer.js：CONVERT_FAIL_TEXT 文案断言扩 init_missing/invalid_init
  key_links:
    - pollLoop mapUri 检查点（首轮/追新分支之外，每轮执行）→ init 落盘 taskDir/init → writeMeta.initFile → readRecordTaskSegments.initPath → startConvertFromRecordTask input 构造（initPath: bundle.initPath）→ startConvertTask 解构透传 → convertToMp4(initPath) → concatFmp4ToMp4（45-01 分流）
    - 新 reason（45-01 产生）→ CONVERT_FAIL_TEXT 文案（本计划补齐）→ 任务页「MP4 转换失败：{文案}」
  prohibitions:
    - statement: init 不进 segments 序列/不占用 seq 命名（文件名为固定常量 'init'，远端 URI 不进路径——T-44-11 沿用）
      status: locked
      verification: media-record-engine.js init 写盘路径为 path.join(taskDir, 'init') 字面量；st.seen/st.recorded 不含 init 条目（测试断言）
    - statement: 自动化测试禁止真实 B 站网络（token 时效+直播偶发——Pitfall 7）
      status: locked
      verification: tests 内全部 stub fetchPage/parsePlaylist；grep 无 bilibili.com 字面 URL
---

<objective>
录制链路 tracer 端到端：录制引擎 init 分片留存（D-05，首轮 baseline 也下）+ main.js 编排透传 initPath（D-06）+ CONVERT_FAIL_TEXT 新 reason 文案，打通「B 站直播录制 → 停止 → 转 MP4」全链路（D-03 tracer 先行链路）。

Purpose: 用户痛点原话（44-UAT）：「B 站直播（fMP4 流）停止录制后任务页显示『MP4 转换失败：分片格式暂不支持转换』」——本计划让录制链路的 fMP4 任务转出能看的 mp4。
Output: 引擎 init 留存 + meta 字段、编排透传、文案映射、tracer stub e2e。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bilibili-fmp4-transcode/45-CONTEXT.md
@.planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md
@.planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md
@.planning/phases/45-bilibili-fmp4-transcode/45-01-SUMMARY.md

@media-record-engine.js
@main.js
@tests/test-media-record-duration.js
</context>

<artifacts_this_phase_produces>
本计划新建符号（plan-review-convergence 源接地排除清单）：
- `media-record-engine.js`：pollLoop init 下载检查点（st.mapUri/st.mapByterange 状态）；writeMeta 输出字段 `mapUri`、`initFile`、`mapByterange`
- `main.js`：readRecordTaskSegments 返回字段 `initPath`；startConvertTask convertToMp4 调用入参 `initPath`；CONVERT_FAIL_TEXT 条目 `init_missing`、`invalid_init`
- tests/test-media-record-duration.js：「fMP4 init 留存（D-05）」用例组
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: 录制链路端到端切片 — 引擎 init 留存 + 编排透传 initPath + stub e2e（D-03/D-05/D-06）</name>
  <files>media-record-engine.js, main.js, tests/test-media-record-duration.js</files>
  <read_first>
    - media-record-engine.js（lines 143-160 downloadSegment 下载+落盘模式、174-188 writeMeta 附加字段模式、212-240 pollLoop 首轮/追新分支结构；文件头 lines 18-20 fetchPage 注入契约）
    - main.js（lines 3045-3079 readRecordTaskSegments meta 读取+路径拼接模式、3082-3190 startConvertTask 编排与 convertToMp4 调用点 3137-3152、3193-3212 startConvertFromRecordTask 的 bundle→input 逐字段构造——initPath 中间跳落码点）
    - tests/test-media-record-duration.js（lines 30-45 makeHarness/metaOf、94-120 轮次控制注入模式；sleep(3200) 轮询节奏）
    - .planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md（media-record-engine.js 段与 main.js 段原文摘录）
    - .planning/phases/45-bilibili-fmp4-transcode/45-RESEARCH.md（Q5 录制链路草案、Pitfall 1/7/8）
  </read_first>
  <action>
  1. media-record-engine.js pollLoop：每轮解析后、首轮/追新分支**之外**（`st.consecutiveFailures = 0` 之后，PATTERNS 指定锚点）插入 init 检查点——`pl.mapUri` 非空且（`st.mapUri` 未见过 或 与已存不同）时：`resolveUri(pl.mapUri, st.url)` → `fetchPage(absUrl, { referer: st.referer, containerId: st.containerId })` → `fs.writeFileSync(path.join(taskDir, 'init'), buf)`（文件名固定常量 'init' 不进远端输入，T-44-11；复刻 downloadSegment lines 143-160 结构但**不进** st.seen/st.recorded、不占 seq 命名）；登记 `st.mapUri = pl.mapUri`、`st.mapByterange = pl.mapByterange || null`。**首轮 baseline 轮同样执行**（检查点在分支之外天然覆盖——Pitfall 1 最高危，D-21 baseline 只豁免分片回溯不豁免 init）。`pl.mapByterange` 非空 → 跳过 init 下载 + console.warn（RESEARCH Route D：BYTERANGE 形态不支持，后续转换落 init_missing 兜底）。init 下载当轮失败容错 = 容忍当轮失败下轮重试（不记 consecutiveFailures——init 缺失只影响后续转换不影响录制本体）；MAP 实测 B 站恒定，变化重下分支按 RFC 语义保留（Pitfall 8）。
  2. writeMeta meta.json 附加字段（照 hasDiscontinuity/hasEncryption 先例 lines 174-188，不升版本）：`mapUri: st.mapUri || null`、`initFile: <已下载则 'init' 否则 null>`、`mapByterange: st.mapByterange || null`。
  3. main.js readRecordTaskSegments（lines 3069-3077 同款）：读 `meta.initFile` → 非空则 `path.join(recordDir, meta.initFile)` + `fs.existsSync` 校验，返回对象追加 `initPath`（不存在/缺失 → null）。
  4. main.js 编排透传**拆明两跳**（initPath 中间跳漏接时 stub e2e 直调 convertToMp4 依旧全绿，但录制主链路静默落 init_missing——下方 acceptance 的 grep 断言是该跳唯一自动化覆盖）：
     ① `startConvertFromRecordTask`（main.js:3195-3207）：`readRecordTaskSegments` 返回 bundle 后，在逐字段构造 input 的对象字面量中追加 `initPath: bundle.initPath`（与 segmentPaths/title/containerId/playbackKey/hasDiscontinuity/hasEncryption/sourceTaskId 并列）；
     ② `startConvertTask`（main.js:3082）：解构 `input` 的字段列表（现 `{ segmentPaths, title, containerId, playbackKey, hasDiscontinuity, hasEncryption, decryption }`）追加 `initPath`，convertToMp4 调用点（现 lines 3137-3152 对象字面量）透传该字段（录制 TS 条目为 null 不影响既有 TS 路径）。
     编排其余零改动（D-06：弹框选目录/任务注册/convertCancelTokens 取消令牌/lastPct 进度节流/终态流转/cancelled 拦截全盘复用）。
  5. tests/test-media-record-duration.js 新增「fMP4 init 留存（D-05）」用例组（tracer 端到端验证）：makeHarness 注入 parsePlaylist stub（首轮/次轮清单均带 `mapUri: 'init.m4s'`，照 lines 94-120 轮次控制模式）+ fetchPage stub 按 URL 区分返回清单/分片/init 字节（init 用 mux.js generator 夹具，与 45-01 同法）→ 断言：①首轮 baseline 轮 init 已下载（taskDir/init 存在，Pitfall 1）；②meta.json 含 initFile:'init' 与 mapUri；③meta.segments 不含 init 条目（st.recorded 无 init）；④以录制目录的 init + segments 直接调 convertToMp4({ initPath, segmentPaths, outputPath })（45-01 分流）→ resolve 且产物 probe 终检可解析——录制链路从清单到可播产物的端到端闭环（D-03 tracer）。mapByterange 非空场景单独用例：不下载 init + meta.initFile=null。注入 parsePlaylist stub 不返回 mapUri 时引擎行为保持现状零回归。
  </action>
  <verify>
    <automated>node tests/test-media-record-duration.js && node tests/test-media-remuxer.js && node --check main.js && node --check media-record-engine.js && grep -n "initPath: bundle.initPath" main.js && grep -n "initPath" main.js && grep -n "initFile" main.js media-record-engine.js</automated>
    <fails_when>任一测试非零退出（fail ≥1 / pass 0）；node --check 非零退出（语法错误）；grep 无命中非零退出（main.js 透传或引擎/编排 initFile 接线未落码）</fails_when>
    <human-check>
    真实 B 站直播录制 tracer（D-03，RESEARCH Q2 实测路线的真机复验）：
    1. `npm run dev` 启动开发环境，打开一个 B 站直播间（fMP4 流，http_hls/fmp4/avc）
    2. 播放器开始录制，录 ≥30 秒后停止
    3. 录制停止自动接力弹框（或任务页「已落盘部分续转」）转出 mp4
    4. 用 mpv 或 VLC 打开产物：画面 + 声音正常播放即通过
    已知容忍项不判 fail（D-02「能看就行」）：播放器总时长显示垃圾值（85h 量级，tfdt epoch 基线所致，RESEARCH Q2）；VLC「Fragment sequence discontinuity」警告（mfhd 序号跳变，仅警告）。
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "'init'" media-record-engine.js` 命中（固定常量文件名写盘）
    - `grep -n "mapUri" media-record-engine.js` 命中 ≥3 处（状态登记、writeMeta、检查点）
    - `grep -n "initPath: bundle.initPath" main.js` 命中（中间跳：startConvertFromRecordTask input 构造——stub e2e 绕过此跳，该 grep 是其唯一自动化覆盖）
    - main.js readRecordTaskSegments 返回对象含 initPath、startConvertTask 解构与 convertToMp4 调用点含 initPath（第二跳 grep 命中）
    - stub e2e 断言全绿：首轮 init 落盘 / meta.initFile / segments 无 init / convertToMp4 产物 probe 可解析
    - 既有录制时长用例（lines 94-120 等）零回归
  </acceptance_criteria>
  <done>带 EXT-X-MAP 清单的录制任务首轮即留存 init，meta 自解释，编排层 initPath 贯通到 convertToMp4 分流；stub e2e 从录制到可解析产物闭环；真实流验证留 end-of-phase human-check。</done>
</task>

<task type="auto">
  <name>Task 2: CONVERT_FAIL_TEXT 新 reason 文案 + 文案断言扩展（Pitfall 5）</name>
  <files>main.js, tests/test-media-remuxer.js</files>
  <read_first>
    - main.js（lines 3022-3037 CONVERT_FAIL_TEXT 映射——key_unavailable「引导重播」口吻先例）
    - tests/test-media-remuxer.js（lines 262-270 与 436-444 文案断言循环列表模式）
    - .planning/phases/45-bilibili-fmp4-transcode/45-PATTERNS.md（main.js 段 CONVERT_FAIL_TEXT 摘录、Shared Patterns「reason → 文案同步」硬约束）
  </read_first>
  <action>
  1. main.js CONVERT_FAIL_TEXT（lines 3022-3037）追加两条（per D-06 文案链路复用，参照 key_unavailable 引导口吻）：`init_missing: 'fMP4 视频缺少初始化段，请重新播放或重新录制后再转换'`、`invalid_init: 'fMP4 初始化段损坏，无法转换'`。
  2. tests/test-media-remuxer.js :262 文案断言循环列表扩 init_missing/invalid_init（照 lines 262-270 模式：`src.includes(\`${reason}: '\`)` 断言 main.js 覆盖全部新 reason——45-01 产生的两个新 reason 在本计划闭环文案链路）。
  </action>
  <verify>
    <automated>node tests/test-media-remuxer.js && node --check main.js && grep -n "init_missing: '" main.js && grep -n "invalid_init: '" main.js</automated>
    <fails_when>测试非零退出（文案断言红 = 映射缺条目）；node --check 非零退出；grep 无命中非零退出（文案未落码）</fails_when>
  </verify>
  <acceptance_criteria>
    - main.js CONVERT_FAIL_TEXT 含 init_missing 与 invalid_init 两条目（grep 命中）
    - 文案断言循环列表覆盖两新 reason，测试全绿（Pitfall 5 关闭：任务页不落「未知原因」）
  </acceptance_criteria>
  <done>45-01 新增的全部 reason 在任务页有中文文案，文案断言测试覆盖并转绿。</done>
  <reversibility rating="reversible">纯文案映射增量，回退即删两条目。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远端清单 mapUri → 录制目录 init 文件 | mapUri 为远端不可信输入，下载内容原样落盘 |
| renderer/任务页 → convert 编排 | 转换入口参数不可信（Phase 44 已有 assertPlayerSender/服务端复校，本计划零改动） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-03 | Tampering | 远端 mapUri 路径注入落盘 | high | mitigate | init 落盘文件名为固定常量 'init'（字面量，远端 URI 不进路径——T-44-11 沿用）；下载经 resolveUri 仅决定请求 URL 不决定落盘位置 |
| T-45-04 | DoS | 畸形/超大 init 响应耗尽资源 | medium | mitigate | init 下载走 fetchPage 既有通道（容器 session 约束）；下载失败容忍当轮下轮重试不阻断录制本体；init 大小由拼接层 ftyp 头校验 + probe 终检兜底（T-45-01） |
| T-45-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新依赖（D-01）——本计划无安装动作，供应链面零增量 |
</threat_model>

<verification>
- 全套件：`node tests/test-media-record-duration.js && node tests/test-media-remuxer.js`（< 10s）
- 接线落码：grep initPath/initFile/mapUri 命中（Phase 44 惯例的 grep 级接线验证）
- tracer 人工验证：<verify><human-check> 真实 B 站直播录制 → 转换 → mpv/VLC 可播（end-of-phase UAT 收割）
</verification>

<success_criteria>
- 首轮 baseline 也下载 init（Pitfall 1 关闭，stub e2e 断言）
- meta.json 附加字段不升版本，旧 meta 缺字段 = 能力缺省关闭（44 先例）
- 编排层零改动复用（D-06），唯一变化 = convertToMp4 多传 initPath
- 新 reason 文案闭环（Pitfall 5 关闭）
</success_criteria>

<output>
创建 `.planning/phases/45-bilibili-fmp4-transcode/45-02-SUMMARY.md` 当完成
</output>
