---
phase: 47-bash
plan: 3
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - skills-builtin/skill-creator/SKILL.md
  - skills-builtin/skill-creator/LICENSE.txt
  - skills-builtin/skill-creator/scripts/check_env.mjs
  - skills-builtin/skill-creator/references/schemas.md
  - skills-builtin/skill-creator/assets/eval_review.html
  - skills-builtin/skill-creator/agents/analyzer.md
  - skills-builtin/skill-creator/agents/comparator.md
  - skills-builtin/skill-creator/agents/grader.md
  - skills-builtin/skill-creator/eval-viewer/generate_review.py
  - skills-builtin/skill-creator/eval-viewer/viewer.html
  - skills-builtin/skill-creator/scripts/__init__.py
  - skills-builtin/skill-creator/scripts/aggregate_benchmark.py
  - skills-builtin/skill-creator/scripts/generate_report.py
  - skills-builtin/skill-creator/scripts/improve_description.py
  - skills-builtin/skill-creator/scripts/package_skill.py
  - skills-builtin/skill-creator/scripts/quick_validate.py
  - skills-builtin/skill-creator/scripts/run_eval.py
  - skills-builtin/skill-creator/scripts/run_loop.py
  - skills-builtin/skill-creator/scripts/utils.py
  - THIRD_PARTY_NOTICES.md
  - tests/test-builtin-skills-seeder.js
autonomous: true
requirements: [SEED-01, SEED-03, SEED-04, SEED-05, SKILL-09]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 4
  confidence: medium
  # 注：files_modified 的 21 项中，19 项是 curl 取回、仅用 `wc -c` 核对字节、**从不以 Read 打开**的上游不可变快照，不进入读取预算；真正读写的是 `SKILL.md` / `check_env.mjs` / `THIRD_PARTY_NOTICES.md` / `tests/test-builtin-skills-seeder.js` 四个文件。按实际读写的文件数度量，本计划未越预算阈值。

must_haves:
  truths:
    - "`skills-builtin/skill-creator/SKILL.md` 逐字节等于上游 `anthropics/skills@b0cbd3df1533b396d281a6886d5132f623393a9c` 的 `skills/skill-creator/SKILL.md`（33,168 字节）**除**以下六处受控改动：① 文首新增一行 Apache-2.0 §4(b) 要求的显著修改声明；② 新增 `## Environment Preflight` 章节；③ 删除 `## Package and Present (only if \\`present_files\\` tool is available)` / `## Claude.ai-specific instructions` / `## Cowork-Specific Instructions` 三章；④ `## Running and evaluating test cases` 与 `## Description Optimization` 两章开头各加一句前置（先跑 `check_env.mjs`，脚本路径是可选的）；**⑤ frontmatter 新增 `disable-model-invocation: true`（SEED-04 —— 上游 frontmatter 实测只有 `name` + `description`，该字段缺失）**；**⑥ frontmatter 的 `description` 改写为中文（D-04 —— 内置技能的 description 面向用户，不进 system prompt）**；**⑦（连带结果，非语义偏离）同步 `## Reference files` 章的文件清单**（补 `check_env.mjs`、去掉对已删除三章的引用）—— 它是 ②③ 与新增 `check_env.mjs` 的必然连带，归属说明中单列，但**不视为独立的语义改动**"
    - "受控改动⑤的判据：上游 `skill-creator/SKILL.md` 的 frontmatter 实测为 `name` + `description` 两个字段（全英文），**没有** `disable-model-invocation`。不加该字段则 SEED-04 对 skill-creator 不成立 —— 其约 350 字符的英文 description 会无条件进入每次请求的 system prompt，正是 SEED-04 要避免的成本"
    - "受控改动⑥的判据：D-04 的原文是「**内置技能的 `description` 用中文、面向用户**」（复数，两个技能都算），其理由是 `disable-model-invocation: true` 使 description 只服务 `/` 面板与设置页列表的人眼可读性；**D-06 管的是正文（body）语言，不是 frontmatter 语言** —— 「上游英文正文」与「中文 description」不冲突。因此 skill-creator 的 description 中文化、正文保持英文（抽检断言：正文不含 CJK，description 含 CJK）"
    - "上游 `skill-creator` 的 `agents/`（3 文件 / 26,712 B）**随包** —— 采纳 Q1 的推荐，使「完整保留上游全部内容」成立，且 `## Advanced: Blind comparison` 章的引用不断链"
    - "删除三章的判据是**功能正确性**而非体积：Realm 没有 `present_files` 工具、也不是 Claude.ai / Cowork；保留会让模型尝试调用不存在的工具或读到平台专有指令"
    - "`skills-builtin/skill-creator/LICENSE.txt` 是上游 Apache-2.0 全文（11,357 字节）逐字副本；`managed-skills/skill-creator/` 播种后该文件随之落盘（整目录复制天然携带）"
    - "`skills-builtin/skill-creator/scripts/check_env.mjs` 存在、`node --check` 通过、`node scripts/check_env.mjs`（无参）退出 0 并输出可解析 JSON，含 `ok` 字段与 `capabilities` 分组"
    - "`check_env.mjs` 支持 `REALM_SKILL_CREATOR_PYTHON` 环境变量覆盖解释器路径；缺依赖时输出 `missing_dependency` 并在 `installGuidance` 里明确写「不要从本技能自动安装依赖」（这是**禁止安装**的声明，属零安装语义扫描的**行级豁免项**）"
    - "`check_env.mjs` 对未知 `--capability` 值返回错误而非静默忽略（ASVS V5 输入校验）"
    - "`skills-builtin/**/SKILL.md` 的零安装语义扫描对 skill-creator 也零命中（实测上游 SKILL.md 对安装语义 regex 0 命中；三处改写不引入新命中），且两个 `LICENSE.txt` 同样零命中"
    - "`THIRD_PARTY_NOTICES.md` 位于 repo 根，对**两个**内置技能各记录五要素：来源仓库 URL + 固定 commit SHA + 许可证 + 是否修改 + 修改说明；find-skills 与 skill-creator **都标 `modified`**"
    - "`THIRD_PARTY_NOTICES.md` 的两个固定 SHA 与实测值一致：find-skills = `773fb2c7bbf16781670a3520affc4abd0c6151ae`（vercel-labs/skills，2026-07-10）、skill-creator = `b0cbd3df1533b396d281a6886d5132f623393a9c`（anthropics/skills，2026-03-06）—— **不是仓库 HEAD**（HEAD 每天都在动，用 HEAD 等于没有固定）"
    - "`THIRD_PARTY_NOTICES.md` 如实记录三处易错事实：① find-skills 的上游仓库根**无独立 LICENSE 文件**（仅 `package.json` 声明 `license: MIT`，`author` 字段为空 → 版权行记 `vercel-labs`），其 `ThirdPartyNoticeText.txt` 是 Skills CLI 的第三方组件清单、**不随包**；② `anthropics/skills` 仓库根**无仓库级 LICENSE**（GitHub API `license.spdx_id` 返回 `null`），真实许可证在 `skills/skill-creator/LICENSE.txt`（Apache-2.0）；③ Python 运行时依赖（`pyyaml` / `anthropic`）**不由 Realm 分发**，故不适用本文件的归属义务"
    - "`skills-builtin/skill-creator/**` 的 19 个文件的体积与上游逐一对得上（18 个上游文件合计 225,004 B + `check_env.mjs`），无缺失、无多余"
    - "SKILL-09 的机械证据：技能自带 `scripts/` 经既有 bash 工具执行、**零新增权限机制** —— `ai-bash-policy.js` 的 `DANGEROUS_INTERPRETERS` 仍含 `'node'` 与 `'python3'` 且未被本阶段放宽，因此 `node scripts/check_env.mjs` 与 `python3 scripts/quick_validate.py` 每次调用都弹确认卡片（这是 D-05 的预期代价，不是缺陷）"
  artifacts:
    - "`skills-builtin/skill-creator/**`（新建，19 文件）：上游 18 文件逐字快照（`SKILL.md` 含六处受控改动）+ 新增 `scripts/check_env.mjs`"
    - "`skills-builtin/skill-creator/scripts/check_env.mjs`（新建）：导出 `main()`；`REALM_SKILL_CREATOR_PYTHON` 环境变量；4 个 capability 分组（`baseline` / `quick-validate` / `eval-viewer` / `description-optimize`）；最低 Python 3.10；10s 超时；失败码集合 8 个"
    - "`THIRD_PARTY_NOTICES.md`（新建，repo 根）：两个技能各五要素 + 三处易错事实说明 + Python 依赖免责一节"
    - "`tests/test-builtin-skills-seeder.js`：新增 `describe('内置技能上游快照与归属（SEED-01 / SEED-05 / P10）')` 分组；把 47-01 的第 2 段扫描豁免清单落地为真实条目（`check_env.mjs` 的两处「禁止安装」文案）"
  key_links:
    - "`skills-builtin/skill-creator/SKILL.md` 的 `## Reference files` 章 → 必须列出**实际随包**的文件集（含新增 `check_env.mjs`），否则模型按清单找文件会落空"
    - "`check_env.mjs`（Node ESM）→ `spawnSync` 探测 Python → `importlib.util.find_spec` 检查包 → JSON 输出 + `ok` 决定退出码（0/1）"
    - "`ai-bash-policy.js` 的 `DANGEROUS_INTERPRETERS`（含 `node` / `python3`）→ `node scripts/check_env.mjs` 与 `python3 scripts/*.py` 一律走强制确认卡片（SKILL-09 的执行路径与代价）"
    - "`THIRD_PARTY_NOTICES.md` 的两个固定 SHA ↔ 实际下载源（`raw.githubusercontent.com/<repo>/<sha>/...`）—— 两边必须一致，否则归属记录与实际分发物不符"
  prohibitions:
    - statement: "`skills-builtin/skill-creator/SKILL.md` 除六处受控改动外不得有其他偏离上游正文的内容（不得顺手「中文化」正文、不得重写章节、不得增删正文段落）"
      status: unresolved
      verification: judgment
      reason: "D-06：skill-creator 保持上游英文正文（仅做必要改写）；偏离越多，`THIRD_PARTY_NOTICES` 的修改说明就越难如实覆盖，且日后与上游比对成本上升。**注意语言的边界**：六处受控改动里只有 ⑥ 涉及中文，且它落在 frontmatter 的 `description` 上 —— 正文必须保持英文（抽检断言锁定）"
    - statement: "不得把 `build.asarUnpack` 或 `build.files` 改成会漏掉 `skills-builtin/**` 的形态（allowlist 语义下未列出的目录不入包 → `asarUnpack` 也解不出东西 → SEED-05 与 P10 同时失效）"
      status: unresolved
      verification: judgment
      reason: "47-01 已确立 `asarUnpack: ['skills-builtin/**']`；本计划的 Open Question Q4 若在 47-04 采纳 `build.files` allowlist，必须同时列入 `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md`"
    - statement: "`check_env.mjs` 不得自己执行任何安装命令（不得调用 `pip install` / `npm i` / `brew install`）；缺依赖只能报告并给出用户可操作指引"
      status: unresolved
      verification: judgment
      reason: "P1 门禁的语义延伸 —— 随包脚本自己装依赖等于把安装语义装回随包内容；`installGuidance` 的 `Do not auto-install dependencies from this skill.` 是这条不变式的文字体现"
    - statement: "不得把 `LICENSE.txt` 换成自写的许可证摘要或改写上游许可证文本（必须逐字保留）"
      status: unresolved
      verification: judgment
      reason: "P10 的归属义务要求许可证文本完整；自写摘要等于没有附许可证"
    - statement: "`THIRD_PARTY_NOTICES.md` 不得标 `unmodified`（两个技能都标 `modified`），也不得含糊其辞地说「有改动」而不列明改了什么"
      status: unresolved
      verification: judgment
      reason: "Q2 的 fail-safe 判定：把 modified 写成 unmodified 是潜在许可证违约（Apache-2.0 §4(b)），反之只是过度声明；P10 的判据是五要素完整，含糊等同门禁不闭合"
    - statement: "不得在 `skills-builtin/skill-creator/**` 内引入任何指向工作区外路径的引用，也不得包含 symlink"
      status: unresolved
      verification: judgment
      reason: "47-01 的播种遇 symlink fail-closed；上游快照必须保持纯文本与相对引用"
---

<objective>
把 skill-creator 作为**固定 SHA 的上游快照**随包分发，并按 D-07 把它在 Realm 语境下的三处平台专有内容与两处评测引导改写掉；新增一个 Node 电梯式环境预检脚本 `check_env.mjs`（D-05）；建立一个覆盖两个内置技能的 `THIRD_PARTY_NOTICES.md`（P10 门禁的载体）。

Purpose: skill-creator 是 Realm 里唯一带 `scripts/` 的内置技能，因此它同时是 **SKILL-09 的真实载体**（技能自带脚本经既有 bash 工具执行、零新增权限机制）与 **P10 发布门禁的归属对象**（`anthropics/skills` 无仓库级 LICENSE，目录内为 Apache-2.0，逐字打包有 §4 标注义务）。上游正文的三处平台专有内容（`present_files` 工具 / Claude.ai / Cowork）在 Realm 里会让模型调用不存在的工具或读到错误平台指令，必须处理 —— 而一旦处理，归属标注就必须是 `modified`。

Output: `skills-builtin/skill-creator/`（19 文件：上游 18 文件快照 + `check_env.mjs`）；repo 根 `THIRD_PARTY_NOTICES.md`；`tests/test-builtin-skills-seeder.js` 的上游快照与归属断言分组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-bash/47-CONTEXT.md
@.planning/phases/47-bash/47-RESEARCH.md
@.planning/phases/47-bash/47-PATTERNS.md
@.planning/phases/47-bash/47-VALIDATION.md

@skills-builtin/find-skills/SKILL.md
@builtin-skills-seeder.js
@ai-bash-policy.js
@tests/test-builtin-skills-seeder.js
@AGENTS.md
</context>

<artifacts_this_phase_produces>
本计划（47-03）新建的符号与命名，供 plan-review-convergence 的 source-grounding 排除新增符号误报：

**新文件**
- `skills-builtin/skill-creator/**`（19 文件 —— 上游 18 文件 + 新增 `scripts/check_env.mjs`）
- `THIRD_PARTY_NOTICES.md`（repo 根）

**`check_env.mjs` 的约定符号与字符串**
- 环境变量名 `REALM_SKILL_CREATOR_PYTHON`（本阶段首次出现）
- capability 名 `baseline` / `quick-validate` / `eval-viewer` / `description-optimize`（**4 组，不含依赖 `claude` CLI 的 `run-eval` / `run-loop`**）
- 失败码 `python_not_found` / `python_version_unsupported` / `missing_dependency` / `missing_command` / `invalid_arguments` / `unknown_requirement` / `invalid_environment` / `dependency_check_failed`
- 输出 JSON 顶层字段 `ok` / `python` / `packages` / `commands` / `capabilities` / `installGuidance`
- CLI 参数 `--capability` / `--package` / `--command` / `--json`

**`THIRD_PARTY_NOTICES.md` 的新约定**
- 两个固定 SHA 常量（文本形式）：`773fb2c7bbf16781670a3520affc4abd0c6151ae`、`b0cbd3df1533b396d281a6886d5132f623393a9c`

**测试文件新增**
- `tests/test-builtin-skills-seeder.js` 内新增 describe 分组 `内置技能上游快照与归属（SEED-01 / SEED-05 / P10）`
- 该文件 `EXEMPTIONS` 表新增两条真实条目（`check_env.mjs` 的 `installGuidance` 字段名与 `Do not auto-install dependencies from this skill.` 文案）

**刻意不产出（decided omission，不是遗漏 —— source-grounding 请据此排除）**
- **不产出 `skills-builtin/skill-creator/SKILL.md` 的中文版** —— D-06 明确：自研内容中文、上游内容英文。
- **不随包 `vercel-labs/skills` 的 `ThirdPartyNoticeText.txt`** —— 经核实是 Skills CLI 的第三方组件清单，与本技能文本无关。
- **不随包 Python 运行时依赖（`pyyaml` / `anthropic`）** —— 由用户环境提供；`THIRD_PARTY_NOTICES.md` 单列一节声明其不适用本文件的归属义务。
- **不产出 `run-eval` / `run-loop` 两个 capability** —— 它们依赖 `claude` CLI（用户机器上未必存在）；Realm 版 capability 收缩为 4 组。
- **不改 `ai-bash-policy.js`** —— `check_env.mjs` 的执行确认成本由 47-02 的既有 `DANGEROUS_INTERPRETERS` 承担（零新增权限机制）。
- **不在本计划改文档** —— `docs/product/**` 与 `AGENTS.md` 的同步归 47-04。
</artifacts_this_phase_produces>

<decisions>
本计划实施前拍板的三处（RESEARCH 的 Open Question Q1 / Q2，与 D-05 的文件清单张力）：

- **D-47-03-a（Q1：`agents/` 随包）：包含。** 采纳 47-RESEARCH.md §Q1 的推荐。理由：① D-05 的「完整保留上游 skill-creator 全部内容」是更强的表述，而「照 openhanako 的做法」更像在描述**形状**（「不要只取作者指南部分」这个范围决策），不是逐文件清单；② P10 的归属对象是「固定 SHA 的快照」——若只取 15/18 文件，「是否修改」就不再是纯粹的文本改动而是**删除了文件**，归属表述必须相应加重；③ 26.7 KB 的体积代价可忽略；④ 保留后 `## Advanced: Blind comparison` 章对 `agents/*.md` 的引用不断链（断链会让模型找不到文件）。**若实施中发现 `agents/*.md` 引用了 Claude 平台专有工具**，按「三处平台专有内容」的同一判据处理（删除该引用或整章），并在 `THIRD_PARTY_NOTICES.md` 的修改说明里补记 —— 不要静默保留。
- **D-47-03-b（Q2：`modified` vs `unmodified`）：两个技能一律标 `modified`。** D-06（「仅做必要改写」）与 D-17（「保留上游原样，标注 unmodified」）存在内部张力；按 fail-safe 原则取 `modified`：把 modified 写成 unmodified 是**潜在的许可证违约**（Apache-2.0 §4(b) 要求被修改的文件带显著修改声明），而把 unmodified 写成 modified 只是**过度声明**（无法律风险）。skill-creator 除 `THIRD_PARTY_NOTICES.md` 外，**还必须在 `SKILL.md` 文首加一行显著的修改声明**（Apache-2.0 §4(b) 的直接要求）。
- **D-47-03-c（check_env.mjs 的 capability 面）：收缩为 4 组。** `baseline` / `quick-validate` / `eval-viewer` / `description-optimize`。去掉参照实现里的 `run-eval` 与 `run-loop`（二者依赖 `claude` CLI；Realm 不分发也不假设该 CLI 存在）。最低 Python 版本保留 3.10、超时保留 10s、包检查用 `importlib.util.find_spec`（**不真 import**，避免副作用）。
</decisions>

<tasks>

<task type="tracer">
  <name>Task 1: skill-creator 上游快照落地与六处受控改写（端到端：下载 → 播种 → 零诊断加载）</name>
  <files>skills-builtin/skill-creator/SKILL.md, skills-builtin/skill-creator/LICENSE.txt, skills-builtin/skill-creator/references/schemas.md, skills-builtin/skill-creator/assets/eval_review.html, skills-builtin/skill-creator/agents/analyzer.md, skills-builtin/skill-creator/agents/comparator.md, skills-builtin/skill-creator/agents/grader.md, skills-builtin/skill-creator/eval-viewer/generate_review.py, skills-builtin/skill-creator/eval-viewer/viewer.html, skills-builtin/skill-creator/scripts/__init__.py, skills-builtin/skill-creator/scripts/aggregate_benchmark.py, skills-builtin/skill-creator/scripts/generate_report.py, skills-builtin/skill-creator/scripts/improve_description.py, skills-builtin/skill-creator/scripts/package_skill.py, skills-builtin/skill-creator/scripts/quick_validate.py, skills-builtin/skill-creator/scripts/run_eval.py, skills-builtin/skill-creator/scripts/run_loop.py, skills-builtin/skill-creator/scripts/utils.py, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - 47-RESEARCH.md §B-1（两个固定 SHA 与实测字节数 —— **下载 URL 的 `sha` 必须用这个，不是 HEAD**）、§B-2（上游 `SKILL.md` 的完整标题清单与**逐章处理表** —— 直接照它改）、§B-5（18 个上游文件的逐文件字节清单与合计 225,004 B）
    - 47-RESEARCH.md §Pitfall 7（实测：`LICENSE.txt` 与 `scripts/` 位于 `<name>/` 之下 → 零诊断、不影响 prompt 段；根因是 SDK 找到 `SKILL.md` 即 return）
    - 47-PATTERNS.md §「No Analog Found」的两行（skill-creator 上游快照与 LICENSE.txt 的形态由上游决定，不由 Realm 决定）
    - skills-builtin/find-skills/SKILL.md（47-01 产物 —— 作为「同目录下的完成形态参照」）
    - builtin-skills-seeder.js（47-01 产物 —— 播种按单技能目录粒度遍历，新技能目录会被自动纳入，无需改代码）
    - ai-skills-manager.js:134-198（`createSkillsEnv` 的根层过滤 + `inContractLayout`）与 :440-460（`refreshSkills`）—— 确认新增技能目录被正确识别
    - 47-CONTEXT.md D-05 / D-06；本计划 decisions 的 D-47-03-a / D-47-03-b
  </read_first>
  <action>
    1. 从固定 SHA 下载上游 `skill-creator` 的 **18 个文件**（不是 HEAD）到 `skills-builtin/skill-creator/`，保持上游相对路径：
       `https://raw.githubusercontent.com/anthropics/skills/b0cbd3df1533b396d281a6886d5132f623393a9c/skills/skill-creator/<相对路径>`
       逐文件核对方节数：`LICENSE.txt` 11,357 / `SKILL.md` 33,168 / `agents/analyzer.md` 10,376 / `agents/comparator.md` 7,287 / `agents/grader.md` 9,049 / `assets/eval_review.html` 7,058 / `eval-viewer/generate_review.py` 16,365 / `eval-viewer/viewer.html` 44,998 / `references/schemas.md` 12,061 / `scripts/__init__.py` 0 / `scripts/aggregate_benchmark.py` 14,386 / `scripts/generate_report.py` 12,847 / `scripts/improve_description.py` 11,116 / `scripts/package_skill.py` 4,234 / `scripts/quick_validate.py` 3,972 / `scripts/run_eval.py` 11,464 / `scripts/run_loop.py` 13,605 / `scripts/utils.py` 1,661。**任一文件字节数与上表不符即说明取错了版本，停下核对。** `LICENSE.txt` 必须逐字保留（**不得**改写或摘要化）。
       **字节核验纪律（控制上下文占用）**：一律用 `wc -c <文件>`（或 `curl -s ... | wc -c`）核对字节数，**不要用 Read 工具全文打开** `eval-viewer/viewer.html`（44,998 B）、`eval-viewer/generate_review.py`（16,365 B）、`scripts/aggregate_benchmark.py`（14,386 B）等大文件；`SKILL.md`（33,168 B）是唯一必须通读并编辑的文件。
    2. 改写 `SKILL.md` —— **只做以下六处受控改动，其余正文逐字保留英文原文**（D-06）：
       - **① 文首修改声明**（Apache-2.0 §4(b)，D-47-03-b）：在 `# Skill Creator` 标题下方的第一段位置插入一行显著声明，形如「> Modified for Realm Browser: this file is derived from `anthropics/skills@b0cbd3df…` with the changes listed in `THIRD_PARTY_NOTICES.md` (removed `present_files`/Claude.ai/Cowork sections; added Environment Preflight; Chinese frontmatter description; `disable-model-invocation: true`).」—— 措辞可调，但必须**点名来源 SHA 并列出全部改动类别**。
       - **② 新增 `## Environment Preflight` 章节**（D-05 在正文里的落点；**该章正文用英文撰写** —— 受「正文（frontmatter 之后）不含 CJK」的断言锁定，不要先写中文再被断言打红）：说明「本技能的 Python 脚本是可选的；运行任何脚本前先跑 `node scripts/check_env.mjs --capability <name>` 获取能力清单；缺依赖时按输出的 `installGuidance` 提示用户在**自己的** Python 环境里安装（本技能不会自动安装）；可用 `REALM_SKILL_CREATOR_PYTHON` 环境变量指定解释器路径」。位置照 §B-2 的处理表：置于文首（`## Communicating with the user` 之前）。
       - **③ 删除三章**：`## Package and Present (only if \`present_files\` tool is available)`、`## Claude.ai-specific instructions`、`## Cowork-Specific Instructions`。理由：Realm 没有 `present_files` 工具、也不是 Claude.ai / Cowork；保留会让模型尝试调用不存在的工具或读到平台专有指令。
       - **④ 两章开头各加一句前置（同样用英文撰写，同上原因）**：`## Running and evaluating test cases` 与 `## Description Optimization` 的开头各插入一句「这一章是可选的评测路径：先跑 `node scripts/check_env.mjs --capability eval-viewer`（或 `--capability description-optimize`）确认环境；缺依赖时告知用户，不要自动安装。」（D-07：Realm 工具为主路径、脚本可选路径）
       - **⑤ frontmatter 新增 `disable-model-invocation: true`**（SEED-04）：上游 frontmatter 实测只有 `name` + `description`，**该字段缺失**。不加则 skill-creator 的英文 description 会无条件进入每次请求的 system prompt。字段加在 frontmatter 内的 `description` 之后。
       - **⑥ frontmatter 的 `description` 改写为中文**（D-04）：保留原意（「用于创建/改进/评测 Agent 技能的作者指南」+ 触发场景），长度须 ≤1024 字符（SDK 的 `MAX_DESCRIPTION_LENGTH`）。**只改 frontmatter 的 description，正文（body）保持英文** —— D-06 管正文语言，D-04 管 description 语言，两者不冲突。改写后必须同步记入 ⑦（归属说明）。
       - 同时更新 `## Reference files` 章的文件清单，使其反映**实际随包**的文件集（含新增 `check_env.mjs`，且不再列出已删除章节引用的内容）。**定性说明（避免计数歧义）**：这一项**不计入**上述六处受控改动 —— 它是改动 ②（新增预检章）与 ③（删除三章）以及新增 `check_env.mjs` 的**必然连带结果**，不是独立的语义偏离；但它在 `THIRD_PARTY_NOTICES.md` 的修改说明里**单列一项**（因为归属说明必须覆盖它）。
    3. 核对三个 `agents/*.md` 是否引用 Claude 平台专有工具（`present_files` 等）。若有，按 D-47-03-a 的同一判据处理（删除该引用行或整章），并在 `THIRD_PARTY_NOTICES.md` 的修改说明里补记。
    4. 在 `tests/test-builtin-skills-seeder.js` 新增 `describe('内置技能上游快照与归属（SEED-01 / SEED-05 / P10）')` 分组，用例：
       - **两个技能都被加载**：播种后 `refreshSkills()` 得到 `find-skills` 与 `skill-creator` 两条 `source === 'managed'` 条目，且零诊断、零错误（`skill-creator/LICENSE.txt` 与 `scripts/` **不得**产生诊断 —— Pitfall 7 的实测结论）；
       - **两个都不进 prompt**：`buildSkillsPrompt() === ''`（两个技能都有 `disable-model-invocation: true`）；
       - **字节数护栏**：断言 `SKILL.md` / `LICENSE.txt` 存在且非空；若把上游实测字节数写进断言，用**区间**而非精确值（因为 SKILL.md 有六处受控改动会改变字节数），但 `LICENSE.txt` 必须**精确**等于 11,357；
       - **文件集完整**：断言 19 个相对路径全部存在（用一张清单表驱动）。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或「两个技能都被加载」「两个都不进 prompt」「文件集完整」三组中任一条 assert 抛出</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`node tests/test-builtin-skills-seeder.js` 退出 0；播种后 `refreshSkills()` 同时得到 `find-skills` 与 `skill-creator`（均 `source === 'managed'`），`_cache.diagnostics` 与 `_cache.errors` 均为空数组
    - 行为断言：`buildSkillsPrompt() === ''`（两个内置技能都因 `disable-model-invocation: true` 不进 prompt）
    - 行为断言：`skills-builtin/skill-creator/LICENSE.txt` 字节数**精确**等于 11,357
    - 行为断言：19 个相对路径（18 上游 + `scripts/check_env.mjs` 除外 —— 本任务只要求 18 个上游文件；`check_env.mjs` 由 Task 2 产出）全部存在
    - 源码断言：`skills-builtin/skill-creator/SKILL.md` 含 `b0cbd3df1533b396d281a6886d5132f623393a9c`（文首修改声明点名来源 SHA）
    - 源码断言（**受控改动⑤ / SEED-04**）：`skills-builtin/skill-creator/SKILL.md` 的 frontmatter 含 `disable-model-invocation: true`
    - 源码断言（**受控改动⑥ / D-04**）：`skills-builtin/skill-creator/SKILL.md` 的 frontmatter `description` 含至少一个 CJK 字符且长度 ≤1024
    - 源码断言：`skills-builtin/skill-creator/SKILL.md` **正文（frontmatter 之后的部分）不含 CJK 字符**（D-06 上游英文正文 vs D-04 中文 description 的机械边界证据 —— 用「剥离 frontmatter 后抽检 `## Creating a skill` 等章节」的方式断言）
    - 源码断言：`skills-builtin/skill-creator/SKILL.md` 含 `## Environment Preflight`，且**不含** `present_files` / `Claude.ai-specific` / `Cowork-Specific` 三处标题子串
    - 源码断言（**Reference files 清单同步**）：`skills-builtin/skill-creator/SKILL.md` 的 `## Reference files` 章含 `check_env.mjs`（Task 2 落地后），且不含对已删除三章（`present_files` / Claude.ai / Cowork）的引用
    - 源码断言：`BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-03")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`）；`git diff "$BASE"..HEAD --stat -- builtin-skills-seeder.js` 为空（播种自动纳入新技能目录，无需改代码 —— 这是 D-08 单技能目录粒度设计的直接收益；**不要用裸 `git diff`**；`plan_head_before` 来自 executor 台账，不是 PLAN frontmatter 字段）
  </acceptance_criteria>
  <done>skill-creator 作为固定 SHA 的上游快照（含 `agents/`）随包；SKILL.md 经六处受控改写后不再含平台专有内容且带 §4(b) 修改声明，且 frontmatter 已含 `disable-model-invocation: true` 与中文 description；播种零改动即自动纳入该技能；加载器零诊断识别，且因 `disable-model-invocation` 不进 prompt。</done>
</task>

<task type="auto">
  <name>Task 2: `check_env.mjs` 环境预检脚本（D-05 / D-07 / ASVS V5）</name>
  <files>skills-builtin/skill-creator/scripts/check_env.mjs, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - 47-RESEARCH.md §Pattern 5（`check_env.mjs` 的形状对照表 —— Realm 版的最小可行形状）、§B-3（参照实现的实现要点）
    - 47-PATTERNS.md §6（外部参照的逐字代码：文件头常量与两张表、`IMPORT_SCRIPT` 的 `importlib.util.find_spec` 方式、`printResult` 与退出码契约、`installGuidance` 文案、失败码集合、解释器探测顺序、`spawnSync` 失败双判、Realm 版规模预估 300-350 行）
    - 参照实现（**仓库外，仅供形状参照，不得 require、不得复制路径**）：`/Volumes/ZhiTai/Projects/github/openhanbao/skills2set/skill-creator/scripts/check_env.mjs` —— 若该路径不可访问，改用 47-RESEARCH.md §Pattern 5 与 §B-3 的描述独立实现
    - 47-RESEARCH.md §Security Domain 的 V5 行（`REALM_SKILL_CREATOR_PYTHON` 只做形态校验、不校验路径合法性 —— **这是设计选择**，因为该变量由用户自己设置；文档说明归 47-04）
    - 47-CONTEXT.md D-05 / D-07；本计划 decision D-47-03-c
  </read_first>
  <action>
    1. 新建 `skills-builtin/skill-creator/scripts/check_env.mjs`（Node ESM，`#!/usr/bin/env node` shebang）。结构与约定：
       - **文件头常量**：`ENV_VAR = 'REALM_SKILL_CREATOR_PYTHON'`（**改名，不沿用参照实现的 `HANA_*`**）、`MIN_PYTHON_VERSION = [3, 10, 0]`、`MIN_PYTHON_VERSION_TEXT`、`CHECK_TIMEOUT_MS = 10_000`。
       - **`PACKAGE_SPECS`**：两项 —— `pyyaml`（模块名 `yaml`，用途 `SKILL.md frontmatter validation`）与 `anthropic`（模块名 `anthropic`，用途 `description optimization loop`）。
       - **`CAPABILITIES`**：**收缩为 4 组**（D-47-03-c）—— `baseline: { packages: [], commands: [] }`、`'quick-validate': { packages: ['pyyaml'], commands: [] }`、`'eval-viewer': { packages: [], commands: [] }`、`'description-optimize': { packages: ['anthropic'], commands: [] }`。
       - **解释器探测**：win32 → `py -3` → `python` → `python3`；其他 → `python3` → `python`。`REALM_SKILL_CREATOR_PYTHON` 设置时优先且只做形态校验（非空字符串），**不校验路径合法性**（设计选择，见 read_first）。
       - **版本校验**：`spawnSync` 取 `python --version`，解析 `X.Y.Z` 与 `MIN_PYTHON_VERSION` 比较；低于则 `python_version_unsupported`。
       - **包检查**：把 `PACKAGE_SPECS` 序列化为 JSON 经 `sys.argv[1]` 传给一段 Python 脚本，用 `importlib.util.find_spec(module_name) is not None` 判定 —— **不真 import**（避免副作用）。`spawnSync` 失败按 `result.error || result.status !== 0` 双判（传 `timeout` 时失败会带 `result.error`）。
       - **输出契约**：`process.stdout.write(JSON.stringify(result, null, 2) + '\n')`；`process.exit(result.ok ? 0 : 1)`。顶层字段 `ok` / `python` / `packages` / `commands` / `capabilities` / `installGuidance`。
       - **失败码集合**（8 个）：`python_not_found` / `python_version_unsupported` / `missing_dependency` / `missing_command` / `invalid_arguments` / `unknown_requirement` / `invalid_environment` / `dependency_check_failed`。
       - **CLI 参数**：`--capability <name>` / `--package <name>` / `--command <name>` / `--json`。**未知的 `--capability` / `--package` / `--command` 值必须返回 `unknown_requirement` 错误并非零退出（ASVS V5：不得静默忽略）**；非法参数组合返回 `invalid_arguments`。
       - **`installGuidance` 字段**：缺依赖时填入两行 —— 一行给出「在**你自己的** Python 环境里安装这些包（包名列表）」的指引，另一行必须逐字含 **`Do not auto-install dependencies from this skill.`**。该字段是**禁止安装**的声明（语义与 P1 相反），因此它是 47-01 零安装语义扫描器第 2 段的豁免对象。
    2. `node --check` 与手工实跑：`node skills-builtin/skill-creator/scripts/check_env.mjs`（无参）应输出含 `ok` 的可解析 JSON；`--capability quick-validate` 与 `--capability 不存在的名字` 各跑一次，确认前者按依赖存在与否给 `ok: true/false`、后者返回 `unknown_requirement` 且退出非零。
    3. 在 `tests/test-builtin-skills-seeder.js` 落**第 2 段扫描的豁免清单**（47-01 预留的位置）：`EXEMPTIONS` 加两条真实条目，每条含「文件相对路径 + 匹配该行的正则 + 豁免理由」三元组，**理由必须写在测试注释里**：
       - `scripts/check_env.mjs` 的 `installGuidance: [` 字段名行 → 理由「这是**禁止**安装的声明字段名，命中 `install` 关键词但语义与 P1 相反」
       - `scripts/check_env.mjs` 的 `Do not auto-install dependencies from this skill.` 行 → 同上
    4. 在测试分组内加断言证明豁免机制**精确生效**：第 2 段扫描在豁免生效时对 `check_env.mjs` 零失败；同时临时移除一条豁免应导致该行被报出（**反向验证必须实际执行一次并记录在 SUMMARY** —— 否则「豁免清单」可能是死代码）。
    5. 补 SKILL-09 的**反向证据断言**：`require('../ai-bash-policy').DANGEROUS_INTERPRETERS` 仍含 `'node'` 与 `'python3'`（47-01 Task 3 已加骨架，此处确认其在新增 `scripts/` 之后仍然成立 —— 即本计划**没有**为了让技能脚本跑得顺而放宽解释器危险表）。
  </action>
  <verify>
    <automated>node --check skills-builtin/skill-creator/scripts/check_env.mjs && node skills-builtin/skill-creator/scripts/check_env.mjs > /tmp/check_env_out.json && node -e "const r=require('/tmp/check_env_out.json'); if(typeof r.ok!=='boolean'||!r.capabilities) process.exit(1)" && node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>任一命令非零退出；或 `check_env.mjs` 的输出不是可解析 JSON 或缺少 `ok` / `capabilities` 字段；或 `node tests/test-builtin-skills-seeder.js` 汇总出现 `# fail` 非 0 计数</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`node --check skills-builtin/skill-creator/scripts/check_env.mjs` 退出 0
    - 行为断言：`node scripts/check_env.mjs`（无参）退出 0，stdout 是可解析 JSON 且含 `ok`（boolean）与 `capabilities` 字段
    - 行为断言：`node scripts/check_env.mjs --capability quick-validate` 输出含 `packages.pyyaml` 的检查结果；`--capability 不存在的名字` 返回 `unknown_requirement` 且退出非零
    - 行为断言：`REALM_SKILL_CREATOR_PYTHON` 被设置时优先于自动探测（可用 `/usr/bin/env python3` 之类路径验证探测分支被跳过）
    - 源码断言：`check_env.mjs` 含 `REALM_SKILL_CREATOR_PYTHON`、四个 capability 名（`baseline` / `quick-validate` / `eval-viewer` / `description-optimize`）、八个失败码、`importlib.util.find_spec`、`Do not auto-install dependencies from this skill.`
    - 源码断言：`check_env.mjs` **不含** `pip install` / `npm i` / `brew install` 等实际安装命令的**调用**（提及包名以外的安装动词只在 `installGuidance` 的用户指引文案里出现）
    - 行为断言：第 2 段扫描在豁免生效时对 `check_env.mjs` 零失败；反向验证（移除一条豁免）已执行并记录该行被报出
    - 行为断言：`require('../ai-bash-policy').DANGEROUS_INTERPRETERS` 仍含 `'node'` 与 `'python3'`（SKILL-09 零新增权限机制）
    - 行为断言：`node tests/test-builtin-skills-seeder.js` 退出 0（第 1 段扫描对两个技能的 `SKILL.md` + 两个 `LICENSE.txt` 仍零命中）
  </acceptance_criteria>
  <done>技能自带脚本有了可用的环境预检入口：缺 Python / 版本过低 / 缺依赖 / 缺命令都有明确失败码与用户可操作指引，且脚本本身绝不自动安装任何东西；零安装语义扫描器的豁免机制被证明精确生效而非死代码。</done>
</task>

<task type="auto">
  <name>Task 3: `THIRD_PARTY_NOTICES.md` 归属记录（P10 门禁载体，五要素）</name>
  <files>THIRD_PARTY_NOTICES.md, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - 47-RESEARCH.md §F-4（`THIRD_PARTY_NOTICES` 的落点、形态、五要素与「已实测的固定值」表）、§B-1（两个固定 SHA 与许可证事实）、§Q2（modified / unmodified 的 fail-safe 判据）
    - 47-PATTERNS.md §12（体例参照 `docs/product/*.md` 的 Markdown 表格；五要素；find-skills 必须标 modified；skill-creator 的判定取决于 plan 期拍板 —— 本计划已定为 modified）
    - 本会话已核实的上游事实（写进文中的三处易错点）：`vercel-labs/skills` 仓库根**无独立 LICENSE 文件**（仅 `package.json` 称 `license: MIT`，`author` 空）；其 `ThirdPartyNoticeText.txt`（6,585 B）是 Skills CLI 的第三方组件清单、与本技能文本无关；`anthropics/skills` 仓库根的 GitHub `license.spdx_id` 返回 `null`，真实许可证在 `skills/skill-creator/LICENSE.txt`（Apache-2.0，11,357 B）
    - 47-CONTEXT.md D-17；本计划 decision D-47-03-b
  </read_first>
  <action>
    1. 新建 repo 根 `THIRD_PARTY_NOTICES.md`，Markdown 表格体例。开头一句说明文件用途（随 Realm Browser 分发的第三方内容归属声明）。然后**两个技能各一节**，每节五要素齐备（**P10 的阻断判据，缺一不闭合**）：**来源仓库 URL / 固定 commit SHA / 许可证 / 是否修改 / 修改说明**，另加一行指向随包的许可证副本路径。
       - **find-skills**：仓库 `https://github.com/vercel-labs/skills`；固定 SHA `773fb2c7bbf16781670a3520affc4abd0c6151ae`（2026-07-10）；许可证 `MIT`；**是否修改 = `modified`（Realm 化改写版）**；修改说明具体到五项：【移除全部 CLI 与安装语义（原 `npx skills find/add/update/init` 全删，含上游第 100/103 行的 `-g` / `-y` 绕过说明）】【检索路径改为经 Realm 既有 `web_fetch` 工具请求 GitHub 搜索 API 的结构化端点】【正文与 description 改为中文】【新增「⛔ 本技能的能力边界」显式禁令段】【新增 `disable-model-invocation: true`】；许可证副本 `skills-builtin/find-skills/LICENSE.txt`。
       - **skill-creator**：仓库 `https://github.com/anthropics/skills`；固定 SHA `b0cbd3df1533b396d281a6886d5132f623393a9c`（2026-03-06）；许可证 `Apache-2.0`；**是否修改 = `modified`**；修改说明逐条列出：① `SKILL.md` 文首新增 §4(b) 修改声明；② 新增 `## Environment Preflight` 章节；③ 删除 `## Package and Present (only if \`present_files\` tool is available)` / `## Claude.ai-specific instructions` / `## Cowork-Specific Instructions` 三章；④ `## Running and evaluating test cases` 与 `## Description Optimization` 两章各加前置句；⑤ 更新 `## Reference files` 文件清单；**⑥ frontmatter 新增 `disable-model-invocation: true`（SEED-04）**；**⑦ frontmatter 的 `description` 改写为中文（D-04；正文仍为上游英文）**；⑧ 新增 `scripts/check_env.mjs`（Realm 自研，非上游内容 —— 该文件**不**适用本节的归属义务，单列说明）；⑨ `agents/`（3 文件 / 26,712 B）**完整保留**；⑩ 其余正文逐字保留上游英文原文。许可证副本 `skills-builtin/skill-creator/LICENSE.txt`。
    2. 加**「易错事实说明」**小节，如实记录三处（这些是归属声明最容易被写错的地方）：
       - find-skills 的上游仓库根**没有独立 LICENSE 文件**：许可证仅由 `package.json` 的 `"license": "MIT"` 声明，且 `author` 字段为空 → 随包的 `LICENSE.txt` 为 Realm 补录的标准 MIT 全文，版权行记 `vercel-labs`；
       - 上游仓库根的 `ThirdPartyNoticeText.txt`（6,585 B）是 **Skills CLI 的第三方组件清单**，与本技能文本无关 → **不随包**；
       - `anthropics/skills` **无仓库级 LICENSE**（GitHub API `license.spdx_id` 返回 `null`，这正是不应只信 API license 字段的实例），真实许可证在技能目录内的 `LICENSE.txt`。
    3. 加**「不由 Realm 分发的运行时依赖」**小节：说明 skill-creator 的 Python 脚本需要 `pyyaml` / `anthropic`（以及可选的外部 `claude` CLI），它们**由用户环境提供、不由 Realm 分发**，故不适用本文件的归属义务；并注明 Realm 版的 capability 面已收缩为 4 组（去掉了依赖 `claude` CLI 的 `run-eval` / `run-loop`）。
    4. 在 `tests/test-builtin-skills-seeder.js` 加 P10 断言（照 47-VALIDATION.md 的 P10-1..P10-5 信号）：
       - `THIRD_PARTY_NOTICES.md` 存在且同时含 `find-skills` 与 `skill-creator` 两节；
       - 两个固定 SHA 字符串**逐字**出现（防手误抄错）；
       - 两个技能条目都含 `modified`（**不得**出现 `unmodified`）；
       - 两个 `LICENSE.txt` 的相对路径都被点名；
       - **P10-5 的落盘断言（补 47-VALIDATION.md 的绑定缺口）**：在 `withTempRoot` 注入的临时 root 上跑一次 `seedBuiltinSkills()` + `refreshSkills()`，然后**逐字节比对**两个 `managed-skills/<name>/LICENSE.txt` 与其源文件（`fs.readFileSync` 相等），并对 `skill-creator/LICENSE.txt` 额外断言字节数**精确等于 11,357**。理由：「`LICENSE.txt` 不得产生诊断」这条断言**不能**证明文件存在（文件缺失同样无诊断），必须直接断言落盘内容 —— 否则 P10 发布门禁的自家信号行是不可绑定的；
       - 五要素关键词齐备（≥10 个断言：来源仓库 URL / SHA / 许可证 / modified / 修改说明的具体项）；
       - 三处易错事实说明的关键词（`没有独立 LICENSE` / `ThirdPartyNoticeText` / `不随包` / `spdx_id`）出现。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 P10 任一条断言抛出（输出含 `AssertionError`，失败消息会点名缺哪个要素）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`THIRD_PARTY_NOTICES.md` 存在于 repo 根，且同时含 `find-skills` 与 `skill-creator` 两节
    - 行为断言：两个固定 SHA 字符串逐字出现（`773fb2c7bbf16781670a3520affc4abd0c6151ae` 与 `b0cbd3df1533b396d281a6886d5132f623393a9c`）
    - 行为断言：文档中**不出现** `unmodified`；两个技能条目都标 `modified`
    - 行为断言：五要素关键词齐备（来源仓库 URL / 固定 SHA / 许可证名 / 是否修改 / 逐条修改说明）
    - 行为断言（**P10-5**）：临时 root 内 `managed-skills/find-skills/LICENSE.txt` 与 `managed-skills/skill-creator/LICENSE.txt` 均存在，且与其源文件**逐字节相同**；`skill-creator/LICENSE.txt` 字节数精确等于 11,357
    - 行为断言：三处易错事实（上游无独立 LICENSE、`ThirdPartyNoticeText.txt` 不随包、`license.spdx_id` 为 null）在文档中可检索到
    - 行为断言：两个 `LICENSE.txt` 的相对路径在文档中被点名
    - 行为断言：Python 运行时依赖单列一节且明确「不由 Realm 分发」
  </acceptance_criteria>
  <done>P10 发布门禁的载体就位：两个内置技能的归属五要素、三处最易写错的上游事实、以及不由 Realm 分发的运行时依赖，都被如实记录且可被测试逐条检索；两个技能一律标 `modified`（fail-safe）。</done>
</task>

<task type="auto">
  <name>Task 4: SEED-03 扫描器对 skill-creator 的覆盖确认与门禁信号收敛</name>
  <files>tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - tests/test-builtin-skills-seeder.js 全文（Task 1-3 后的现状）
    - 47-RESEARCH.md §A-4（扫描器与豁免清单的完整表；实测「上游 SKILL.md 对安装语义 regex 0 命中」「两个 LICENSE.txt 也 0 命中」）、§B-2（实测结论）
    - 47-VALIDATION.md §「Gate → Acceptance Signal Map」（P1-a-1..a-3 与 P10-1..P10-6 的观测方式）
    - 47-CONTEXT.md D-02 / D-17
  </read_first>
  <action>
    1. 确认 47-01 的第 1 段扫描器（零容忍、覆盖 `skills-builtin/**` 下所有 `SKILL.md` 与 `LICENSE.txt`）在新增 skill-creator 之后**自动覆盖**新文件（按 glob 遍历，不写死文件名）—— 若 47-01 写死了 `find-skills` 路径，改为递归遍历 `skills-builtin/`；这一步是 SEED-03 从「单技能」扩到「两技能」的关键。
    2. 加 P1-a 三条门禁信号的独立断言（命名带信号号，便于逐条审计）：
       - `P1-a-1`：两个 `SKILL.md` + 两个 `LICENSE.txt` 对 `FORBIDDEN_PATTERNS` 零命中（第 1 段，无豁免）
       - `P1-a-2`：`skills-builtin/**` 内不存在 `npx skills` 任意变体（大小写不敏感、允许中间任意空白）
       - `P1-a-3`：两个技能的 frontmatter 都含 `disable-model-invocation: true`，且 `description` 含 CJK
    3. 加 P10-6 的**人工步骤记录**：文档明确指出「打包后归属仍可读」需要 `make install-nightly` 后确认 `THIRD_PARTY_NOTICES.md` 在 asar 清单内（**不在本计划执行** —— 归 47-04 的打包验证任务；此处只在测试文件注释里标注该信号的人工归属，避免门禁审计误以为已被自动化覆盖）。
    4. 加一条**反向验证并记录**：临时把 skill-creator 的 `SKILL.md` 加一行 `pip install pyyaml`，确认第 1 段扫描**变红**（证明新增技能确实进入了扫描面，不是只扫了 find-skills）；还原后转绿。
    5. 全量回归：`node tests/test-builtin-skills-seeder.js` + `node tests/test-ai-skills.js` + `node tests/test-agent-workspace.js` + `node --test tests/test-ai-bash-policy.js`。注意：47-02 与 47-03 同属 Wave 2 且互不依赖，两者可能并行完成 —— 若 47-02 尚未落地，`test-ai-bash-policy.js` 的 baseline 仍是 32 例（而非 62），**不要**在该文件里加与 install 档相关的断言（那属 47-02）。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js && node tests/test-ai-skills.js && node tests/test-agent-workspace.js</automated>
    <fails_when>任一命令非零退出；或 `tests/test-builtin-skills-seeder.js` 汇总出现 `# fail` 非 0 计数；或 P1-a-1 / P1-a-2 / P1-a-3 三条信号中任一条 assert 抛出</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：P1-a-1 / P1-a-2 / P1-a-3 三条门禁信号各有独立断言且全部通过
    - 行为断言：扫描器按 glob 递归覆盖 `skills-builtin/`（源码中不出现写死的 `find-skills` / `skill-creator` 路径作为扫描范围）
    - 行为断言：`node tests/test-builtin-skills-seeder.js`、`node tests/test-ai-skills.js`（64 例）、`node tests/test-agent-workspace.js`（21 例）全部退出 0
    - 反向验证已执行并记录：给 skill-creator 的 `SKILL.md` 加 `pip install pyyaml` 后第 1 段扫描变红，还原后转绿
    - 源码断言：测试文件注释中标注了 P10-6 的人工步骤归属（`make install-nightly` 后确认 `THIRD_PARTY_NOTICES.md` 在 asar 清单内），明确其未被自动化覆盖
  </acceptance_criteria>
  <done>SEED-03 的门禁面从单技能扩到两个技能且被反向验证证明覆盖有效；P1-a 三条信号可逐条审计；P10-6 的人工归属被显式标注而非留白。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 上游仓库（`anthropics/skills` 固定 SHA）→ repo 内 `skills-builtin/skill-creator/**` | 一次性人工引入的**静态快照**；内容随包分发后即成为模型可见的高可信内容 |
| `skills-builtin/skill-creator/scripts/*` → 用户机器的 Python 环境 | 脚本经 `node` / `python3` 执行时**以用户全权限运行**；`REALM_SKILL_CREATOR_PYTHON` 由用户自己设置 |
| `check_env.mjs` → `spawnSync` 子进程 | 探测解释器与包；命令参数来自用户 CLI 输入与环境变量 |
| `THIRD_PARTY_NOTICES.md` → 发布合规审计 | 归属声明的真实性可被外部审计；错标是许可证违约 |
| Python 运行时依赖（`pyyaml` / `anthropic`）→ Realm 工作区 | **不由 Realm 分发**，但技能正文会引导用户在**自己的工作区之外**安装它们 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-03-01 | **Elevation of Privilege** | `skills-builtin/**` 文本含可执行安装处方（P1 门禁的语义延伸） | **high** | mitigate | 第 1 段零容忍扫描覆盖 `skills-builtin/**` 的**所有** `SKILL.md` 与 `LICENSE.txt`（本计划把扫描面从单技能扩到两技能并被反向验证证明有效）；`check_env.mjs` 的「禁止安装」声明走第 2 段**行级豁免**且豁免理由写死在测试注释里 |
| T-47-03-02 | Elevation of Privilege | `REALM_SKILL_CREATOR_PYTHON` 指向恶意可执行文件 | medium | accept | 该变量由**用户自己**设置（用户控制自己的环境）：设置它 = 授权 Realm 执行该路径的程序。**不是远程输入，不构成漏洞**；`check_env.mjs` 只做形态校验（非空字符串），不校验路径合法性 —— 这是设计选择，文档说明归 47-04 |
| T-47-03-03 | Elevation of Privilege（残余） | 二次 `spawn` 不在 bash 策略视野（`check_env.mjs` 内部 `spawnSync` 调 python） | medium | accept | **无技术缓解**（静态策略的固有边界，见 47-02 的同型残余风险）。用户确认的是「第一条命令」（`node scripts/check_env.mjs`，`node` ∈ `DANGEROUS_INTERPRETERS` → 强制确认卡片），二次调用由该次确认承担。诚实声明归 47-04 |
| T-47-03-04 | Spoofing / Tampering | 上游快照被静默篡改（取了 HEAD 而非固定 SHA / 文件集与声明不符） | medium | mitigate | 下载 URL 一律用固定 SHA（非 HEAD）；逐文件字节数与 §B-5 的实测清单核对；`LICENSE.txt` 断言精确 11,357 B；`THIRD_PARTY_NOTICES.md` 的 SHA 由测试断言逐字匹配 —— 两边一致才通过 |
| T-47-03-05 | Repudiation | 归属声明不实（标 `unmodified` 但实际改过 / 漏记改动项） | medium | mitigate | 两个技能一律标 `modified`（fail-safe：过度声明无法律风险，漏声明违反 Apache-2.0 §4(b)）；`SKILL.md` 文首带 §4(b) 修改声明并点名来源 SHA；测试断言文档中**不出现** `unmodified` 且五要素关键词齐备 |
| T-47-03-06 | Tampering | `check_env.mjs` 的 CLI 参数被静默忽略（未知 `--capability` 返回成功） | low | mitigate | 未知 `--capability` / `--package` / `--command` 一律返回 `unknown_requirement` 且非零退出（ASVS V5）；由实跑断言锁定 |
| T-47-03-07 | Information Disclosure | 平台专有内容（`present_files` / Claude.ai / Cowork）误导模型行为 | low | mitigate | 三章删除（Realm 无 `present_files` 工具、非 Claude.ai / Cowork）；删后模型不会尝试调用不存在的工具；改动记入归属说明 |
| T-47-03-SC | Tampering | npm / 依赖安装 | high | mitigate | **本计划零新增依赖**：`package.json` 的 `dependencies` / `devDependencies` **原样不动**（`pyyaml` / `anthropic` 是**用户环境的 Python 包**，不进 `package.json`）；`skills-builtin/**` 是纯文本静态资源，随包分发不走任何安装器 |
</threat_model>

<edge_coverage_ledger>
`SPECLESS_FALLBACK=true` / `EDGE_ABSENT=1`：本阶段无 SPEC，edge-probe 把全部 8 条需求判为 `unclassified` / `unresolved`（`applicable = 8`，`resolved = 0`）。按 §A「`unclassified` 一律不得自动 resolve」的纪律，本计划负责的 **3 行**结清如下：**3 = 0（写入 must_haves.truths）+ 3（显式 flagged assumption）**。

| # | requirement_id | probe category | 处置 | 落点 |
|---|----------------|----------------|------|------|
| 1 | SEED-01 | unclassified | **flagged assumption** | 「『两个技能』由**文件存在性 + 加载器识别**断言；『skill-creator 完整保留上游』由**固定 SHA 的快照 + 修改说明**保证，但**不**做逐字节 diff 上游（需要网络与上游可访问）。上游若在固定 SHA 之外变更，本阶段的快照按定义不跟随 —— 升级需人工重新走一遍本计划」 |
| 2 | SEED-03 | unclassified | **flagged assumption** | 「零安装语义扫描是**模式匹配**（`FORBIDDEN_PATTERNS` + 豁免清单），不是语义分析：中文/英文的等价改写若不在表内即漏检（表已按 47-RESEARCH §A-4 尽量覆盖，但不可穷举）。**机械防护有上限，真正的判据是逐句人工评审**（Task 1 已按 §A-3 的评审表执行）。另：`skills-builtin/**/scripts/**` 的豁免是**行级**的，新增脚本若引入新的「禁止安装」声明需要同步补豁免条目」 |
| 3 | SKILL-09 | unclassified | **flagged assumption** | 「『零新增权限机制』的机械证据是 `DANGEROUS_INTERPRETERS` 未被放宽（仍含 `node` / `python3`）。**但没有端到端断言证明「脚本真的能跑起来」** —— 环境差异（用户机器有无 Python / 有无 `pyyaml`）无法在本阶段自动化；`check_env.mjs` 的实跑只在**本机**验证，`python3` 缺失时的降级路径（`python_not_found`）未在 CI 化环境中覆盖」 |

> 工具缺口如实记录（来自 orchestrator）：本机安装的 `edge-probe.cjs` 的 `SHAPE_CUES` 映射为空对象，其散文启发式把 8 条需求全部判为 `unclassified`、无法产出类别化探针。因此上表是**文本派生结果，不是本阶段的覆盖上限** —— 真正的验证底座是 `47-VALIDATION.md` 的 P1-a-1..a-3 与 P10-1..P10-6 信号、以及 Task 1-4 的逐条断言。
>
> SEED-05 的 `unclassified` 行落在 **47-04**（打包态验证是它的主交付面），本计划不重复登记。
</edge_coverage_ledger>

<verification>
1. `node tests/test-builtin-skills-seeder.js` 退出 0（本计划四个任务的全部自动化断言）
2. `node tests/test-ai-skills.js` 退出 0（既有 64 例回归不红）
3. `node tests/test-agent-workspace.js` 退出 0（既有 21 例回归不红）
4. `node --check skills-builtin/skill-creator/scripts/check_env.mjs` 退出 0；`node skills-builtin/skill-creator/scripts/check_env.mjs` 输出可解析 JSON
5. `BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-03")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`）；`git diff "$BASE"..HEAD --stat -- builtin-skills-seeder.js ai-bash-policy.js ai-manager.js package.json` 为空（本计划**不碰**播种模块、策略引擎、AI 管理器与打包配置 —— 全部靠 47-01 的已有机制自动纳入；**不要用裸 `git diff`**）
6. `node -e "const p=require('./package.json'); if(p.dependencies['pyyaml']||p.dependencies['anthropic']) process.exit(1)"` 退出 0（零新增 npm 依赖；Python 依赖不进 package.json）
7. 反向验证记录两条：① 给 skill-creator 的 `SKILL.md` 加 `pip install pyyaml` → 第 1 段扫描变红；② 移除一条 `EXEMPTIONS` 条目 → `check_env.mjs` 的对应行被报出。两者还原后转绿
8. 文件集核对：`find skills-builtin -type f | wc -l` = 21（find-skills 2 + skill-creator 19）
</verification>

<success_criteria>
- [ ] `skills-builtin/skill-creator/` 含上游 18 文件（含 `agents/`）+ Realm 自研 `check_env.mjs`，`LICENSE.txt` 精确 11,357 B
- [ ] `SKILL.md` 只做六处受控改动（外加一项连带结果：同步 `## Reference files` 清单），其余正文逐字保留上游英文；文首带 §4(b) 修改声明并点名来源 SHA；三处平台专有内容已删除；frontmatter 含 `disable-model-invocation: true` 与中文 description
- [ ] `check_env.mjs` 可跑、输出 JSON、未知参数报错、`REALM_SKILL_CREATOR_PYTHON` 生效、绝不自动安装
- [ ] 播种零改动即纳入新技能，加载器零诊断识别两个技能，两者都因 `disable-model-invocation` 不进 prompt
- [ ] `THIRD_PARTY_NOTICES.md` 五要素齐备、两技能都标 `modified`、三处易错事实如实记录、Python 依赖单列免责
- [ ] P1-a-1..a-3 三条门禁信号有独立断言且经反向验证证明非空转
- [ ] SKILL-09 的零新增权限机制有反向证据（`DANGEROUS_INTERPRETERS` 未被放宽）
- [ ] 既有 64 + 21 例保持全绿；`package.json` 零新增依赖
</success_criteria>

<output>
Create `.planning/phases/47-bash/47-03-SUMMARY.md` when done
</output>
