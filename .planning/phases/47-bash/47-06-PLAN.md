---
phase: 47-bash
plan: 6
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - builtin-skills-seeder.js
  - tests/test-builtin-skills-seeder.js
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04, SEED-05, SKILL-09]
coupling_justified: "`docs/product/ai-skills.md` §八 由 47-05 Task 3 按本计划的三条既定行为撰写（same 跳过重建 / 崩溃残留清扫 / 播种诊断 console 兜底）；本计划**不得改任何 `docs/**` 或 `AGENTS.md`** —— 显式登记该耦合以便日后核对两侧口径是否漂移"

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ---- GAP 2（WR-03）：same → 跳过重建 ----
    - "连续两次 `seedBuiltinSkills()`（第二次 `detectDiff === 'same'`）后，`managed-skills/<name>/SKILL.md` 的 **inode 不变**、技能目录的 inode 也不变（改前实测：SKILL.md inode 171120956→171120958、技能目录 inode 171120955→171120957 —— 整目录被重建）"
    - "`diff === 'same'` 时**不写盘、不产诊断**：既不做 `safeCopyDir`，也不产 `realm_builtin_seed_overwritten`（那个诊断只表达「磁盘内容与随包版本不一致」，`missing` 与 `same` 都不产 —— D-10 的自愈语义保持不变）"
    - "`detectDiff` 的相对路径集合比较**纳入目录项**（目录记作 `rel + '/'`），因此源侧新增一个**空目录**也会被判 `different` 并自愈到目标；而 size / sha256 两层**只对文件条目生效** —— 不得对目录条目做 `statSync` + `readFileSync`（`hashFile(dir)` 抛 EISDIR 会被 fail-safe catch 吞成 `'different'` → 反而每次启动都重建，正是本 gap 要消除的现象）"
    # ---- GAP 3（WR-02）：启动级残留清扫 ----
    - "`seedBuiltinSkills()` 在进入逐技能循环前，清扫 `managed-skills/` 下名字匹配 `/\\.(tmp|bak)_\\d+$/` 的残留目录（`safeCopyDir` 的 `<dst>.tmp_<ts>` / `<dst>.bak_<ts>` 兄弟目录刻意与 dst 同父目录以保证同卷，因此它们落在技能扫描根内）"
    - "预置残留后播种即消失：手工建 `managed-skills/find-skills.bak_1700000000000/SKILL.md` 与 `managed-skills/skill-creator.tmp_1700000000001/SKILL.md` → 跑 `seedBuiltinSkills()` 后**两个目录都不存在**；随后跑**真实 `refreshSkills()`** → 技能集里没有任何名字带 `.bak_` / `.tmp_` 的条目，且 `snapshot.diagnostics` 与 `snapshot.errors` **均为空数组**（改前实测：残留被加载成 2 个永久幽灵技能 + 4 条诊断）"
    - "清扫不误删：名字不含 `.<tmp|bak>_<digits>` 后缀的目录（如 `not-residue/`、`user.bak_x/`）保持原样 —— 正则必须锚定 `\\d+$`，不能用宽泛的 `includes('.bak_')`"
    - "清扫是 best-effort：`readdirSync` 或删除失败都不得 throw、不得阻断播种（与 `_cleanupDir` 的既有吞错语义一致）"
    # ---- GAP 4（WR-04）：失败路径不得静默 ----
    - "播种结束时把本次累积的 `_diagnostics` **统一输出**：`level === 'error'` → `console.error`，`level === 'warning'` → `console.warn`，每条形如 `[Realm] 内置技能播种 <level> <code>: <message>`"
    - "**早退路径也必须输出**：`names.length === 0`（随包源目录缺失 / `asarUnpack` 未生效 / .app 装歪）时 `realm_builtin_src_missing` 必须出现在 `console.error` —— 改前该路径只 push 诊断、不打日志，导致「一个内置技能都没播种成功」的正式版与一切正常的正式版在用户侧与日志侧完全不可区分（与 nodejieba 事故 9a1ae11 同型）"
    - "`realm_builtin_src_invalid`、`realm_builtin_seed_failed`（含 symlink 拒收）两条内层 error 路径同样出现在 `console.error`；`realm_builtin_seed_overwritten` 出现在 `console.warn`（且不进 `console.error`）"
    - "**顶层 catch 的 `console.error` 必须保留**（`_flushDiagnostics()` 不替代它）：逐技能循环**之外**抛出的异常（`resolveBuiltinSkillsSrc()` / `resolveManagedSkillsDir()` / `mkdirSync` 等，早于任何 `_diagnostics.push`）会走最外层 `catch` —— 此时 `_diagnostics` 为空 → `finally` 的 `_flushDiagnostics()` **无任何输出**，若把原 `console.error('[Realm] 内置技能播种失败（不影响启动）:', err && err.message)` 删空，这条路径就彻底静默。三者的关系是**并列的三条可见面**：① 顶层 catch → `console.error`（零诊断路径）；② `_flushDiagnostics()` → 按级别输出累积诊断；③ `getSeedDiagnostics()` → 结构化诊断（Phase 50 消费）。机械判据：注入「`managedDir` 的中间路径组件是**文件**」使 `mkdirSync` 抛 `ENOTDIR`，断言 `console.error` 至少收到一条含 `内置技能播种失败` 的行，**同时** `getSeedDiagnostics()` 为空数组（证明这条输出只可能来自 catch）"
    - "零诊断且无顶层异常时零噪声：干净首次播种不产生任何 `console.error` / `console.warn` 调用"
    - "**注意反向不成立**（不得写成「零诊断 ⇒ 零 console 输出」的不变式）：顶层 catch 路径在 `_diagnostics` 为空时**仍应**有一条 `console.error` —— 这正是它作为独立可见面的意义；任何把它「优化」成只走 `_flushDiagnostics()` 的改动都是本 gap 的复发"
    - "「不阻断应用启动」契约保持：任何 console 输出路径均不 throw；`seedBuiltinSkills()` 仍在 `main.js` 的 `migrateAiMemory()` 之后、`new AIManager()` 之前同步调用"
  artifacts:
    - "`builtin-skills-seeder.js`：`listRelativeFiles()` 记录目录项；`detectDiff()` 的路径集合层含目录项、size / sha256 层只对文件条目；`seedBuiltinSkills()` 在 `diff === 'same'` 时 `continue`；新增内部函数 `sweepSeedResidue(managedDir)`（启动级残留清扫）与 `_flushDiagnostics()`（诊断 console 兜底），并在 `seedBuiltinSkills()` 的 `finally` 中调用后者；**顶层 `catch` 的 `console.error` 一字不改地保留**（它覆盖逐技能循环之外、零诊断的异常路径）；模块头「失败处理」bullet 与新行为同步"
    - "`tests/test-builtin-skills-seeder.js`：幂等用例改名并补 inode 稳定性断言；新增「空目录也参与差异判定」用例；新增「预置残留 → 清扫 → 真实 `refreshSkills` 零幽灵技能零诊断」用例与「非残留目录不误删」反例；新增 console 捕获脚手架与**五条**失败路径的输出断言（四条诊断路径 + **顶层 catch 路径**，后者用「中间路径组件是文件 → `mkdirSync` 抛 `ENOTDIR`」注入）+ 源码断言"
  key_links:
    - "`seedBuiltinSkills()` 的执行顺序：`_diagnostics = []` → 源目录独立扫描（`realm_builtin_src_invalid`）→ `getSeededSkillNames()`（为空则产 `realm_builtin_src_missing` 并**早退**）→ `mkdirSync(managedDir)` → **`sweepSeedResidue(managedDir)`** → 逐技能 `detectDiff` → **`same` 跳过 / `different` 产诊断后覆盖 / `missing` 直接覆盖** → `safeCopyDir` → `finally { _flushDiagnostics() }`。**新加的清扫必须在逐技能循环之前**，否则本轮 `refreshSkills()` 之前清不完（且下一轮才生效）"
    - "`safeCopyDir` 的 `<dst>.tmp_<ts>` / `<dst>.bak_<ts>` 派生命名 ↔ `sweepSeedResidue` 的正则 `/\\.(tmp|bak)_\\d+$/`（成对不变式：改命名必须同步改正则；正则锚定 `\\d+$` 是因为 `safeCopyDir` 只用 `Date.now()` 作时间戳）"
    - "`ai-skills-manager.inContractLayout(root, filePath)`（`ai-skills-manager.js:196-198`，要求相对扫描根恰好 2 段）**接受** `managed-skills/<name>.bak_<ts>/SKILL.md` —— 这正是残留会被加载成幽灵技能的原因：扫描根过滤与 `inContractLayout` 都不会拒绝它，只有本计划的清扫能清掉"
    - "`getSeedDiagnostics()` 目前**零生产消费方**（全仓 grep 只有 seeder 自身与测试）→ `_flushDiagnostics()` 的 console 输出是打包态唯一可见面；Phase 50 把它接进技能面板/诊断通道之前，这条兜底不得删除"
    - "`main.js:4042-4051`（`ensureWorkspaceDir()` → `migrateAiMemory()` → **`seedBuiltinSkills()`** → `new AIManager()`）**零改动** —— 本计划只改 seeder 内部，不动调用位与调用语义"
  prohibitions:
    - statement: "不得把残留清扫做成「删除所有不含 SKILL.md 的目录」或 `name.includes('.tmp_')` 这类宽泛删除；正则必须锚定 `\\.(tmp|bak)_\\d+$`（`safeCopyDir` 的命名成对不变式）"
      status: unresolved
      verification: judgment
      reason: "`managed-skills/` 是同目录存放 AI 自建技能与用户数据的 app-owned 区域；宽泛删除会把合法目录删掉，破坏面远大于它修的 bug"
    - statement: "不得让 `detectDiff` 的 size / sha256 层遍历到目录条目（`hashFile` 对目录会抛 EISDIR，被 fail-safe catch 吞成 `'different'` → 每次启动都重建，本 gap 会以更隐蔽的形式复发）"
      status: unresolved
      verification: judgment
      reason: "既有用例（`tests/test-builtin-skills-seeder.js:452-471` 的 `sub/` 目录 + `'same'` 断言）正是这条不变式的机械护栏；实现越界会让它变红"
    - statement: "不得改动 `agent-workspace.js`、`ai-skills-manager.js`、`main.js`、`package.json`（`agent-workspace.js` 必须零 diff）；清扫与归一化都必须在 seeder 内部完成"
      status: unresolved
      verification: judgment
      reason: "Phase 46 锁定 `agent-workspace.js` 零改动（其 21 例纯 Node 测试依赖不 require electron）；`ai-skills-manager` 是技能集单一权威，加载语义不在本阶段范围"
    - statement: "不得删除 `getSeedDiagnostics()` 导出或不产诊断（console 兜底是**新增**可见面，不是诊断的替代品；Phase 50 仍需读取结构化诊断）"
      status: unresolved
      verification: judgment
      reason: "GAP 4 的 `missing` 两条：一是统一输出诊断，二是显式记录「Phase 50 接入前 console 兜底不能省」。二者是并列关系"
    - statement: "不得把清扫或 console 输出做成会 throw 的路径（例如不包 try/catch 的 `readdirSync`、在 console 包装器里做断言）"
      status: unresolved
      verification: judgment
      reason: "「任何失败仅 console.error + 产诊断，不 throw、不阻断应用启动」是 47-01 的锁定契约（模块头 :17-18），本计划只增加可见面，不改变该契约"
    - statement: "不得删除或改写 `seedBuiltinSkills()` 顶层 `catch` 里的 `console.error`（`try/catch` 改 `try/catch/finally` 时只允许**追加** `finally`）"
      status: unresolved
      verification: judgment
      reason: "该 catch 覆盖逐技能循环**之外**的异常（`resolveBuiltinSkillsSrc()` / `resolveManagedSkillsDir()` / `mkdirSync`），此时 `_diagnostics` 为空 → `finally` 里的 `_flushDiagnostics()` 零输出；删掉它 → 这条路径彻底静默，与模块头「任何失败仅 console.error + 产诊断」及本计划第 3 项的改写直接矛盾（该断言由新增的「顶层 catch 路径」用例机械守住）"
---

<objective>
关闭播种模块的三个 warning 级 gap：**GAP 2（WR-03）** `detectDiff === 'same'` 仍整目录重建 → 改为「无差异不写盘」；**GAP 3（WR-02）** `<dst>.tmp_<ts>` / `<dst>.bak_<ts>` 残留在技能扫描根内且无清扫 → 加启动级清扫，消除「被加载成永久幽灵技能 + 4 条诊断」；**GAP 4（WR-04）** 三条内层失败路径只 push 诊断、而 `getSeedDiagnostics()` 零生产消费方 → 播种结束时按级别统一 console 输出，让「一个都没播种成功」在正式版可见。

Purpose: 三者是同一棵树上的一串问题。GAP 2 让每次启动都白算最贵的 sha256 层、并**每次启动都制造一次 `rename(dst → bak)` 空窗**——这个空窗正是 GAP 3 的触发源；GAP 3 的残留又被 `ai-skills-manager.inContractLayout`（相对扫描根恰好 2 段即接受）放行，于是崩过一次的安装会永久多出带时间戳、设置页删不掉的幽灵技能；而 GAP 4 让这一切在正式版里一点痕迹都不留 —— 与 seeder 自己在模块头引用的 nodejieba 事故（9a1ae11「漏一侧即在正式版静默失败」）同型。先掐掉重建（源头），再清扫残留（存量），最后补上可见面（下次能发现）。

Output: `builtin-skills-seeder.js` 的 same-跳过、目录项参与差异判定、启动级残留清扫、诊断 console 兜底；`tests/test-builtin-skills-seeder.js` 的 inode 稳定性 / 空目录自愈 / 预置残留 / 非残留不误删 / console 输出四类新断言。

**与既有锁定决策的关系（D-08 / D-09 / D-10 / D-11，不得回退任何一条）**：
- **D-08（每次启动按单技能目录粒度同步 + app-owned，不保留用户修改）**：本计划只把「无条件覆盖」精确化为「**内容一致时不写盘**，缺失或内容不一致时才覆盖」—— `managed-skills/` 仍是 app-owned，用户手改仍不保留（D-08 的绑定语义不变），变的只是「无差异时不做无谓写盘」。ROADMAP §Phase 47 成功判据 1 已写明「内容一致时 `detectDiff` 判 `same` → 零差异、零诊断，重启不重复写」，gaps[1] 记的正是**代码未实现该判据**。
- **D-09（覆盖前先产 warning 诊断，不静默）**：`realm_builtin_seed_overwritten` 的触发面与 level 不变（只在 `different` 时产）；本计划的 console 兜底是给 D-09 补上**可见面**（原先它只在 API 层成立）。
- **D-10（手删后自愈重播）**：`missing` 分支不产覆盖诊断、直接覆盖 —— 语义与断言均保持。
- **D-11（seeded 身份 = 随包目录名集合，零状态文件零硬编码）**：本计划不引入任何状态文件；残留清扫走「命名正则」而不是「已知播种名单」，正是为了不引入硬编码。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-bash/47-CONTEXT.md
@.planning/phases/47-bash/47-REVIEW.md
@.planning/phases/47-bash/47-VERIFICATION.md
@.planning/phases/47-bash/47-01-PLAN.md
@.planning/phases/47-bash/47-01-SUMMARY.md

@builtin-skills-seeder.js
@tests/test-builtin-skills-seeder.js
@agent-workspace.js
@ai-skills-manager.js
@main.js
</context>

<cross_plan_coordination>
本阶段（gap closure）有两个计划，文件集**互不重叠**，同为 `wave: 1`：

- **47-06（本计划）**：`builtin-skills-seeder.js` / `tests/test-builtin-skills-seeder.js` —— GAP 2/3/4。
- **47-05**：`ai-bash-policy.js` / `tests/test-ai-bash-policy.js` / 三份文档 —— GAP 1（CR-01）。

`agent-workspace.js` 在**两个计划中都必须零 diff**（Phase 46 锁定；本阶段硬约束）。

**文档归属（重要）**：`docs/product/ai-skills.md` §八 由 **47-05 Task 3** 编辑，其内容按本计划下面三条**既定行为**撰写 —— ① `diff === 'same'` 跳过重建、不重复写；② 启动播种前清扫 `.tmp_*` / `.bak_*` 残留；③ 播种诊断经 `console.error` / `console.warn` 兜底输出。**本计划不得改任何 `docs/**` 或 `AGENTS.md`**（避免同波文件冲突）。若实施中发现这三条行为必须偏离，**以代码为准**并在 SUMMARY 里显式记一条「须回改 `docs/product/ai-skills.md` §八」，同时在该节 diff 里说明偏离点。
</cross_plan_coordination>

<tasks>

<task type="tracer">
  <name>Task 1: `same` 不写盘 + 目录项参与差异判定（GAP 2 端到端纵切）</name>
  <files>builtin-skills-seeder.js, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - builtin-skills-seeder.js（全文 438 行）：:1-25（模块头；:12-13「覆盖：无条件覆盖」bullet 需补 same-跳过的精度句、:17-18「失败处理」bullet 由 Task 3 改）、:104-125（`listRelativeFiles` —— 本任务改）、:127-134（`hashFile` —— 不改，但**绝不能**被喂目录）、:136-188（`detectDiff` 四层短路 + 两处「不用 mtime」护栏注释 :161-163/:146-148 + IO fail-safe :183-187 —— 本任务改第 ② 层与 ③④ 层的遍历面）、:190-200（`_cleanupDir` 吞错语义 —— Task 2 复用）、:234-285（`safeCopyDir`：`<dst>.tmp_<ts>` / `<dst>.bak_<ts>` 同父目录、`_cleanupDir(bakDst)` 吞错）、:315-390（`seedBuiltinSkills` 主循环 —— 本任务改 `same` 分支）
    - tests/test-builtin-skills-seeder.js（全文 2051 行）：:13-21（require 与 `REPO_ROOT` / `REAL_BUILTIN_SRC`）、:154-167（`withTempRoot`：`t.after` 内恢复 `workspace` / `seeder` / `aiSkills` 三处测试注入与缓存）、:233-254（`copyTree` / `makeSyntheticSrc` / `listResidue`）、:364-401（tracer 用例：`refreshSkills(env, { rootDirs: [workspace.getManagedSkillsDir(), workspace.getSkillsDir()] })` 的调用形态与「零诊断」断言写法 —— 新用例照此复制）、:430-472（`detectDiff` 的 IO fail-safe 与 `missing/same/different` 用例；**:452-471 含 `sub/` 目录 + `'same'` 断言，是本任务最容易踩中的既有护栏**）、:570-589（幂等用例：**当前只断言字节与诊断，未断言未重建** —— 本任务改名并补 inode）、:739-747（剥离注释后不得出现 `mtime` 的机械不变式 —— 本任务不得引入任何时间戳比较）
    - 47-VERIFICATION.md 的 `gaps[1]`（GAP 2 / WR-03，含 inode 实测值与 `listRelativeFiles` 空目录的 `missing` 条目）
    - 47-REVIEW.md 的 WR-03 节（:212-235，含修法代码形状与「若要保留空目录自愈，在 `listRelativeFiles` 里对目录追加 `rel + '/'`」）
    - 47-CONTEXT.md D-08（单技能目录粒度 + 无条件覆盖，本任务精度化为「内容一致时跳过写盘」）/ D-09（差异诊断，只在 `different` 时产）/ D-10（`missing` 自愈重播，不产覆盖诊断）
  </read_first>
  <action>
    1. `listRelativeFiles(dir)`：目录条目也入列表 —— 遍历时对 `entry.isDirectory()` 先 `out.push(childRel + '/')` 再递归；文件条目仍 `out.push(childRel)`；末尾 `out.sort()` 保持确定性定序。同步改 JSDoc：写明「目录以 `rel + '/'` 形式登记，使**空目录**也参与差异判定（否则源侧新增空目录永远检不出，`same` 跳过重建后就不再自愈）」；并写明调用方契约「**size / sha256 两层必须先过滤掉以 `/` 结尾的条目**」。
    2. `detectDiff(srcDir, dstDir)`：
       - 第 ② 层用含目录项的完整列表比较（逐项 `!==` 即 `'different'`，并保留既有「长度不等先短路」的写法）。
       - 第 ③（size）与第 ④（sha256）层**只遍历文件条目**：先 `const srcFiles = srcEntries.filter((rel) => !rel.endsWith('/'))`（`dstFiles` 同法），再做 `statSync` / `hashFile` 逐项比较。**在代码里留一行注释点明这个过滤是必须的**：目录条目若进入 `hashFile` → `fs.readFileSync(dir)` 抛 `EISDIR` → 被本函数的 fail-safe catch 吞成 `'different'` → **每次启动都重建**，正是本 gap 要消除的现象。函数 JSDoc 的「逐层短路」描述与「绝不用 mtime」护栏注释一并同步（护栏一字不改）。
       - 保持 IO 错误的 fail-safe 与「每个文件在本次调用内至多算一次 sha256」两条既有性质不变。
    3. `seedBuiltinSkills()` 的逐技能分支改为**三段式**（顺序不可调换）：
       - `const diff = detectDiff(skillSrc, skillDst);`
       - `if (diff === 'same') continue;` —— 注释写明「**D-08 的精度化**：无差异 → 不写盘、不产诊断（重启不重复写；`detectDiff` 最贵的 sha256 层不再白算，也不再制造 `rename(dst → bak)` 空窗）」
       - `if (diff === 'different') { <既有的 realm_builtin_seed_overwritten warning push，一字不改> }`
       - `safeCopyDir(skillSrc, skillDst);`（覆盖 `missing` 与 `different`；`missing` 不产诊断 —— D-10 自愈语义保持）
       同步模块头 :12-13 的 bullet：`- **覆盖**：无条件覆盖（managed 目录是 app-owned 内容）…` → 补「**内容一致时跳过写盘**（`detectDiff` 判 `same` → 不复制、不产诊断）；差异或缺失时才写」。
    4. `tests/test-builtin-skills-seeder.js`：
       - **改写既有幂等用例（:570-589）**，用例名与实际断言对齐：`幂等：连续两次播种，第二次 detectDiff === same → 零差异、零诊断、不重建（inode 不变）`。保留既有两条断言（无 `realm_builtin_seed_overwritten`、字节不变），**并新增**：
         - `const beforeFileIno = fs.statSync(target).ino; const beforeDirIno = fs.statSync(path.dirname(target)).ino;` 在第二次播种后断言两者**等于**（失败消息写明「改前实测 SKILL.md inode 171120956→171120958、目录 inode 171120955→171120957 —— 重建即会被这条抓住」）；
         - 附带断言 `fs.statSync(target).mtimeMs` 不变（inode 是主判据，mtime 是第二信号）。
       - **新增用例** `空目录也参与差异判定：源侧新增空目录 → 判 different 并自愈到目标`：`makeSyntheticSrc(root)` → 首次播种 → 在**源**目录 `srcDir/find-skills/empty-dir/` 建一个空目录 → 再次播种 → 断言 `managedDir/find-skills/empty-dir` 存在且是目录。**注释写明**：这条用例在「目录项不参与比较」的实现下必然变红（路径集合相同 → `same` → 跳过 → 空目录永不出现）。
       - **新增用例** `目录条目不得进入 size / sha256 层`：直接调 `seeder.detectDiff(src, dst)`，其中两侧都含**嵌套子目录**（至少两层，如 `a/b/c.txt`），断言首次 `copyTree` 后 `detectDiff === 'same'`（若实现把目录喂给 `hashFile`，这里会因 EISDIR 变成 `'different'`）。
    5. 不改 `safeCopyDir` / `_cleanupDir` / `getSeededSkillNames` / `collectInvalidDirs` 的任何行为；不引入任何时间戳比较（`mtime` 剥离注释后的机械不变式 :739-747 必须继续通过）。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总行出现 `# fail` 后跟非 0 计数；或输出中完全不出现 `# pass` 行；或「inode 不变」断言抛出（第二次播种后 `ino` 变化 = 仍在重建）；或「空目录自愈」用例抛出（目录项未参与差异判定）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（**GAP 2 靶心**）：连续两次播种后 `SKILL.md` 与技能目录的 **inode 均不变**、`mtimeMs` 不变，且第二次零诊断；用例名如实体现「不重建」
    - 行为断言：`diff === 'same'` 分支不调用 `safeCopyDir`（源码断言：`seedBuiltinSkills` 函数体内 `continue` 出现在 `safeCopyDir(` 调用之前，且 `detectDiff` 的调用结果被用于早退）
    - 行为断言：`missing` 仍不产 `realm_builtin_seed_overwritten`、`different` 仍先产 warning 再覆盖（既有两条用例继续全绿）
    - 行为断言（**空目录自愈**）：源侧新增空目录后重跑播种，目标出现该空目录
    - 行为断言（**目录不得进哈希层**）：含两层嵌套子目录时 `detectDiff(src, dst) === 'same'`；既有 `:452-471` 用例全绿
    - 源码断言：`listRelativeFiles` 函数体含 `'/'` 结尾的目录标记写法；`detectDiff` 函数体的 size / sha256 层前有 `endsWith('/')` 过滤（或等义的过滤表达式）
    - 源码断言：剥离注释后 `builtin-skills-seeder.js` 仍不出现 `mtime` / `mtimeMs` / `birthtime`（既有机械不变式 :739-747 继续通过）
    - 约束：`agent-workspace.js` / `ai-skills-manager.js` / `main.js` / `package.json` 零 diff
  </acceptance_criteria>
  <done>内容一致时重启不再重建技能目录（inode 不变），`detectDiff` 最贵的 sha256 层不再每次启动白算，`rename(dst → bak)` 的每启动一次空窗消失（GAP 3 的源头被掐掉）；同时目录项参与差异判定，源侧空目录仍能自愈。</done>
</task>

<task type="auto">
  <name>Task 2: 启动级残留清扫 + 幽灵技能消除（GAP 3 / WR-02）</name>
  <files>builtin-skills-seeder.js, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - builtin-skills-seeder.js 当前全文（Task 1 后的 `listRelativeFiles` / `detectDiff` / `seedBuiltinSkills`）
    - builtin-skills-seeder.js:234-285（`safeCopyDir` 的 `<dst>.tmp_<ts>` / `<dst>.bak_<ts>` 命名与 `Date.now()` 时间戳 —— 清扫正则的成对依据）、:190-200（`_cleanupDir` 的「自身吞错」语义 —— 清扫必须复用）、:315-390（`seedBuiltinSkills` 的 `mkdirSync(managedDir)` 位置与逐技能循环起点 —— 清扫的插入点）
    - ai-skills-manager.js:190-198（`inContractLayout`：相对扫描根恰好 2 段即接受 —— 这解释了为什么 `managed-skills/<name>.bak_<ts>/SKILL.md` 会被当成合法技能）、:417-440（`refreshSkills` 的六步管线，第 ① 步是契约布局过滤）、:520-540（`realm_layout_violation` 的触发条件与 `suggested` 文案）
    - tests/test-builtin-skills-seeder.js:250-254（`listResidue` 辅助：`n.includes('.tmp_') || n.includes('.bak_')`）、:364-401（真实 `refreshSkills` 的调用形态与「技能集 + 零诊断 + 零错误」断言写法）、:570-720（既有播种生命周期用例的组织方式）、:863-930（门禁断言分组的位置，用于决定新 describe 的落点）
    - 47-VERIFICATION.md 的 `gaps[2]`（GAP 3 / WR-02，含「残留留存 → 技能集多出 2 个幽灵条目 + 4 条诊断」的实测与 `missing` 三条）
    - 47-REVIEW.md 的 WR-02 节（:178-210，含清扫代码形状与断言要求）
  </read_first>
  <action>
    1. 新增内部函数 `sweepSeedResidue(managedDir)`（**不导出**，与 `_cleanupDir` / `assertNoSymlink` 同层）：
       - `let entries = []; try { entries = fs.readdirSync(managedDir); } catch { return; }`
       - 逐个判断 `if (/\.(tmp|bak)_\d+$/.test(name)) _cleanupDir(path.join(managedDir, name));`
       - JSDoc 写明四点：① **为什么必须清** —— `safeCopyDir` 的 tmp/bak 由 `dst` 派生（**D-08 的原子性契约**：同父目录 → 必然同卷，刻意的设计），因此**落在 `managed-skills/` 扫描根内**；`ai-skills-manager.inContractLayout` 要求「相对扫描根恰好 2 段」，`<name>.bak_<ts>/SKILL.md` 恰好 2 段 → 被当成合法技能加载，成为设置页删不掉的永久幽灵条目 + 诊断噪声；② 残留的产生方式 —— 进程在两次 `rename` 之间被杀、`_cleanupDir(bakDst)` 失败、复制阶段崩溃；③ **正则为何锚定 `\d+$`** —— `safeCopyDir` 只用 `Date.now()` 作时间戳，且宽泛的 `includes('.bak_')` 会误删名字合法的目录；**也正因如此不需要「已知播种名单」**（D-11：零硬编码、零状态文件）；④ best-effort —— 与 `_cleanupDir` 同款吞错，绝不 throw、绝不阻断播种。
       - **不产诊断、不打日志**（注释说明理由：残留是 app-owned 内容在崩溃后的自我修复，不是需要用户知晓的异常；且 GAP 4 的 console 通道专用于失败可见性。若日后需要可见，Phase 50 可加一条 warning 级结构化诊断 —— 但**不得**在本计划里加，以免与 47-05 已写定的文档口径不一致）。
    2. 在 `seedBuiltinSkills()` 中插入调用：`fs.mkdirSync(managedDir, { recursive: true });` 之后、逐技能 `for` 循环**之前**，调用 `sweepSeedResidue(managedDir);`。注释写明「必须在循环之前 —— 否则本轮播种完的 `refreshSkills()` 仍会看到残留」。**不得**放在 `names.length === 0` 的早退之前（那条路径连 `managedDir` 都还没解析，且源缺失时清理任何东西都不合适）；把这条取舍写进注释。
    3. `tests/test-builtin-skills-seeder.js` 新增 `describe('崩溃残留清扫（GAP 3 / WR-02）')`：
       - **主用例 `预置残留 → 清扫 → 真实 refreshSkills 零幽灵技能零诊断`**：`makeSyntheticSrc(root)`（只含 `find-skills`，跑得快）→ 手工建三个目录并各写一个 `SKILL.md`：
         - `managedDir/find-skills.bak_1700000000000/SKILL.md`（内容里的 `name:` 写成 `find-skills`，复现「目录名与 name 不一致」的诊断面）
         - `managedDir/skill-creator.tmp_1700000000001/SKILL.md`（**故意用一个不在随包源里的技能名**，证明正则不依赖「已知播种名单」）
         - `managedDir/not-residue/SKILL.md` 与 `managedDir/user.bak_x/SKILL.md`（**反例**：不含 `.<tmp|bak>_<digits>` 后缀 → 必须**保留**）
       调用 `seeder.seedBuiltinSkills()` 后断言：两个残留目录 `!fs.existsSync`；两个反例目录**仍然存在**且内容未变。
       然后照 tracer 用例的形态跑**真实加载**：`const env = await workspace.createSandboxEnv({ cwd: root }); await aiSkills.refreshSkills(env, { rootDirs: [workspace.getManagedSkillsDir(), workspace.getSkillsDir()] });` → 断言 `aiSkills.getSkillsSnapshot().skills` 里**没有任何** `skill.name.includes('.bak_') || skill.name.includes('.tmp_')` 的条目，且 `snapshot.diagnostics` 与 `snapshot.errors` **均为空数组**。
       注释写明改前实测：残留不被回收 → 技能集为 `find-skills / find-skills.bak_1700000000000 / skill-creator / skill-creator.tmp_1700000000001`，并伴随 `invalid_metadata ×2` + `realm_name_rewritten ×2` 四条诊断。
       - **断言 `listResidue(managedDir)` 在播种后为空**（复用既有辅助，双口径验证）。
       - **源码断言**：`sweepSeedResidue` 源码含 `/\.(tmp|bak)_\d+$/`（或等义且同样锚定 `\d+$` 的写法），且**不含**裸的 `includes('.tmp_')` / `includes('.bak_')` 形式的宽泛匹配；`seedBuiltinSkills` 函数体内 `sweepSeedResidue(` 的调用位置在 `safeCopyDir(` 之前。
       - **反向验证（记录进 SUMMARY）**：临时注释掉 `sweepSeedResidue(managedDir);` 这一行 → 主用例变红（残留留存、技能集出现幽灵条目、诊断非空）；还原后转绿。证明清扫与本断言都不是空转。
    4. 不改 `safeCopyDir`（其 tmp/bak 同父目录是**刻意**的卷约束，注释 :247-249 已声明不得改为 `os.tmpdir()`）；`inContractLayout` 与 `ai-skills-manager.js` 零改动。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或「预置残留」用例抛出 `AssertionError`（残留未清 / 幽灵技能仍在技能集 / `diagnostics` 非空）；或反例目录被误删（`not-residue` / `user.bak_x` 不存在）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（**GAP 3 靶心**）：预置 `find-skills.bak_<ts>/` 与 `skill-creator.tmp_<ts>/`（含 `SKILL.md`）后播种，两者均被清除，随后真实 `refreshSkills()` 的技能集里无任何带 `.bak_` / `.tmp_` 的条目，`diagnostics` 与 `errors` 均为空数组
    - 行为断言：`listResidue(managedDir)` 播种后为空
    - 行为断言（**不误删**）：`not-residue/`（合法名）与 `user.bak_x/`（后缀不含数字）播种后仍存在、内容未变
    - 源码断言：`sweepSeedResidue` 的正则锚定 `\.(tmp|bak)_\d+$`；调用点在逐技能循环（`safeCopyDir(`）之前；函数体内只经 `_cleanupDir` 删除且无 throw 路径
    - 反向验证已执行并记录：注释掉清扫调用 → 主用例变红；还原 → 转绿
    - 约束：`ai-skills-manager.js` / `agent-workspace.js` / `main.js` 零 diff
  </acceptance_criteria>
  <done>崩溃留下的 `.tmp_*` / `.bak_*` 残留不再变成永久幽灵技能：下次启动先清扫扫描根内的残留，再播种；清扫只认 `safeCopyDir` 的成对命名（锚定数字后缀），不误删合法目录。</done>
</task>

<task type="auto">
  <name>Task 3: 播种诊断 console 兜底（GAP 4 / WR-04）</name>
  <files>builtin-skills-seeder.js, tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - builtin-skills-seeder.js 当前全文（Task 1/2 后的 `seedBuiltinSkills` 全文 —— 四条诊断 push 点与唯一的 `console.error` catch:387-389）、:392-399（`getSeedDiagnostics` 的 JSDoc 与「供 Phase 50 消费」的原话）、:1-25（模块头 :17-18 的「失败处理」bullet）
    - ai-skills-manager.js:236-263（`pushEntryDiag` / `toRealmDiag` 的诊断形状 `{ level, code, message, path, skillName? }` —— 输出格式的字段来源）
    - tests/test-builtin-skills-seeder.js:403-414（`realm_builtin_src_missing` 用例 —— 早退路径）、:591-613（`realm_builtin_seed_overwritten` 用例）、:676-698（symlink → `realm_builtin_seed_failed` 用例）、:700-719（`realm_builtin_src_invalid` 用例）、:722-737（4 个诊断 code 与 level 的源码断言）
    - 47-VERIFICATION.md 的 `gaps[3]`（GAP 4 / WR-04，含三条内层失败路径的行号、`getSeedDiagnostics()` 全仓零生产消费方的 grep 结论、以及 `missing` 两条）
    - 47-REVIEW.md 的 WR-04 节（:237-261，含 4 行输出格式的代码形状）
  </read_first>
  <action>
    1. 新增内部函数 `_flushDiagnostics()`（**不导出**，仅供 `seedBuiltinSkills()` 调用；**D-09「不静默」的可见面落点**）：
       - 遍历 `_diagnostics`，构造 `const line = '[Realm] 内置技能播种 ' + d.level + ' ' + d.code + ': ' + d.message;`
       - `d.level === 'error'` → `console.error(line)`；否则 → `console.warn(line)`（覆盖 `warning`，也为未来可能出现的其它级别留出 warn 通道）。
       - JSDoc 写明三点：① **为什么必须有这条** —— 三条内层失败路径（`realm_builtin_src_missing` / `realm_builtin_src_invalid` / `realm_builtin_seed_failed`）与 `realm_builtin_seed_overwritten` 原先只把诊断 push 进 `_diagnostics`，而 `getSeedDiagnostics()` **在生产代码里没有任何调用方**（全仓 grep 除 seeder 自身与测试外零命中）→ 「内置技能一个都没播种成功」的正式版与一切正常的正式版在用户侧与日志侧完全不可区分（与模块头引用的 nodejieba 事故 9a1ae11 同型）；② **Phase 50 之前不得删除** —— Phase 50 把 `getSeedDiagnostics()` 接进技能面板 / 诊断通道后，这条 console 兜底才可以降级或移除，注释里必须出现 `Phase 50` 字样；③ **不阻断启动** —— 只读 `_diagnostics` 并写 console，无 throw 路径。
    2. `seedBuiltinSkills()`：**只在 `catch` 之后加 `finally`，`catch` 体一字不改** —— 结果是
       `try { … } catch (err) { console.error('[Realm] 内置技能播种失败（不影响启动）:', err && err.message); } finally { _flushDiagnostics(); }`。
       **⚠ 不得把 catch 体删空或改成只产诊断**：`finally` 里的 `_flushDiagnostics()` 只输出 `_diagnostics`，而顶层 catch 覆盖的正是**逐技能循环之外**抛出的异常（`resolveBuiltinSkillsSrc()` / `resolveManagedSkillsDir()` / `mkdirSync` …），它们**早于任何诊断 push** —— 此时 `_diagnostics` 为空、`_flushDiagnostics()` 零输出，删掉 catch 里的 `console.error` 就让这条路径**彻底静默**，与模块头「任何失败仅 `console.error` + 产诊断，不 throw」直接矛盾（也与本任务第 3 项自己的改写矛盾）。三者是**并列的三条可见面**（catch / flush / 结构化诊断），不是替代关系。`finally` 让 `names.length === 0` 的早退路径（源目录缺失 / `asarUnpack` 未生效 / .app 装歪）也输出 —— 那正是本 gap 最严重的盲区。`_diagnostics = []` 必须仍在函数最顶部（在 `try` 之前），保证 flush 读到的是本次运行的诊断。
       在 `finally` 旁留一行注释：「早退路径（src 缺失）也走这里 —— 不能只在循环后输出」。在 `catch` 旁留一行注释：「本 catch 覆盖循环之外的异常（此时 `_diagnostics` 为空）—— 它的 console.error 是**独立可见面**，不得被 `_flushDiagnostics()` 取代」。
    3. 模块头 :17-18 的 bullet 补全为：「**失败处理**：任何失败仅 `console.error` + 产诊断，**不 throw、不阻断应用启动**（照 agent-workspace.migrateAiMemory 的先例）；**所有累积诊断在播种结束（含早退路径）按级别统一经 `console.error` / `console.warn` 输出** —— `getSeedDiagnostics()` 目前没有生产消费方，Phase 50 接入技能面板之前这条 console 兜底不得删除；**逐技能循环之外抛出的异常由最外层 `catch` 的 `console.error` 承接（此时诊断为空，该行是本路径的唯一可见面，不得删除）**。」
       同步 `getSeedDiagnostics()` 的 JSDoc：补一句「调用方（Phase 50）接入前，`_flushDiagnostics()` 的 console 输出是打包态唯一可见面」。
    4. `tests/test-builtin-skills-seeder.js`：
       - 新增测试脚手架 `captureConsole(t)`：临时替换 `console.error` / `console.warn` 为收集器（分别 push 到 `errors` / `warns` 数组，**不要**再调用原函数，避免污染测试输出），返回 `{ errors, warns, restore }`，并在 `t.after` 里恢复原函数（放在 `withTempRoot(t)` **之前**或之后皆可，但必须注册 `t.after` 恢复）。注释写明「node:test 的 `t.after` 逆序执行，恢复 console 与清理临时目录互不依赖」。
       - 新增 `describe('播种诊断 console 兜底（GAP 4 / WR-04）')`，**五条**用例：
         - **早退路径（本 gap 最严重的盲区）**：`srcDir` 指向不存在路径 → `assert.doesNotThrow(() => seeder.seedBuiltinSkills())` → 断言 `errors.length >= 1` 且存在一条同时含 `realm_builtin_src_missing` 与 `[Realm] 内置技能播种 error` 的行；断言 `warns.length === 0`。
         - **顶层 catch 路径（零诊断的独立可见面）**：`withTempRoot(t)` → `makeSyntheticSrc(root)` 造出合法源 → 在临时根里写一个**同名文件**占位（`fs.writeFileSync(path.join(root, 'blocker'), 'x')`），把 `managedDir` 设为 `path.join(root, 'blocker', 'managed-skills')`（**中间路径组件是文件** → `fs.mkdirSync(..., { recursive: true })` 抛 `ENOTDIR`，即异常发生在**任何诊断 push 之前**）→ `assert.doesNotThrow(() => seeder.seedBuiltinSkills())` → 断言 `errors.length >= 1` 且有一条含 `内置技能播种失败`，**并且** `seeder.getSeedDiagnostics()` 为 `[]`（零诊断 + 有 console.error ⇒ 这条输出只可能来自最外层 catch；把 catch 里的 `console.error` 删掉本用例立刻变红）。注释写明「`finally` 里的 `_flushDiagnostics()` 在此路径零输出，因此本用例是『catch 的 console.error 不得被删』的机械护栏」。
         - **覆盖 warning 走 warn 通道**：正常播种后手改目标 `SKILL.md` → 重跑 → 断言 `warns` 里有一条含 `realm_builtin_seed_overwritten`，且 `errors` 为空（级别路由正确）。
         - **symlink 拒收走 error 通道**：复用既有 symlink 用例的构造（源里放一个含 symlink 的技能目录）→ 断言 `errors` 里有一条含 `realm_builtin_seed_failed`。
         - **零噪声（仅限干净路径）**：干净首次播种（`makeSyntheticSrc`）→ 断言 `errors.length === 0 && warns.length === 0`。注释写明「该断言的适用范围是『无诊断且无顶层异常』；**不要**把它升格成『零诊断 ⇒ 零 console 输出』的不变式 —— 顶层 catch 路径在零诊断下仍有一条 `console.error`（上一条用例正是钉住这点）」。
       - 源码断言：`builtin-skills-seeder.js` 含 `function _flushDiagnostics(`；该函数体同时含 `console.error` 与 `console.warn`；其上方 JSDoc 含 `Phase 50`；`seedBuiltinSkills` 函数体含 `finally` 且在 `finally` 内调用 `_flushDiagnostics()`；**且 `seedBuiltinSkills` 函数体的 `catch` 块内仍含 `console.error`**（用 `functionBody(source, 'seedBuiltinSkills')` 取体后断言 `catch` 与 `finally` 之间存在 `console.error(` —— 防止「改成 try/catch/finally 时顺手把 catch 清空」）。
       - **反向验证（记录进 SUMMARY，两次）**：① 把 `_flushDiagnostics();` 从 `finally` 里临时删掉 → 四条**诊断**用例变红（顶层 catch 那条仍绿 —— 它不依赖 flush）；② 把 `catch` 里的 `console.error` 临时删掉 → **顶层 catch 用例变红**、其余仍绿。两次都还原并确认全绿 —— 证明五条用例各自钉住不同的可见面，不是空转。
    5. 既有的所有播种用例在新增 console 输出后必须仍然全绿（它们不断言 console 内容）；若某条既有用例因输出变多而失败，说明它误用了 console（不得改断言口径去迁就，须查明原因）。触发失败路径的既有用例（源缺失 / symlink / 覆盖诊断）未注册 `captureConsole` 时会把日志打到测试 stdout —— 属可接受噪声，**不得**为此改动断言；如需安静，可在这些用例上补注册脚手架（复用同一辅助）。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出中 `# fail` 不是 0；或 `# tests` 小于 88（本计划开始前的基线）；或输出中完全不出现 `# pass` 行；或 console 兜底五条用例中任一条抛出（早退路径无 `console.error` = 本 gap 未闭合；**顶层 catch 用例失败 = catch 的 `console.error` 被删空，这条路径会彻底静默**）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言（**GAP 4 靶心**）：`srcDir` 缺失时 `console.error` 至少收到一条含 `realm_builtin_src_missing` 的行 —— 证明**早退路径**也输出（改前该路径只 push 诊断、零日志）
    - 行为断言（**顶层 catch 的独立可见面**）：`managedDir` 的中间路径组件是文件导致 `mkdirSync` 抛 `ENOTDIR` 时，`console.error` 收到含 `内置技能播种失败` 的行，**且** `getSeedDiagnostics()` 为 `[]`（零诊断 + 有输出 ⇒ 只可能来自 catch）
    - 行为断言：`realm_builtin_seed_overwritten` 走 `console.warn`（且不进 `console.error`）；`realm_builtin_seed_failed` 走 `console.error`
    - 行为断言：干净首次播种零 `console.error` / `console.warn` 调用（无噪声）—— 该结论**不得**被升格成「零诊断 ⇒ 零 console 输出」的不变式
    - 行为断言：`assert.doesNotThrow` 包裹上述所有播种调用（不阻断启动契约保持）
    - 源码断言：`_flushDiagnostics` 存在、同时含 `console.error` 与 `console.warn`、JSDoc 含 `Phase 50`；`seedBuiltinSkills` 的 `finally` 内调用它；**`seedBuiltinSkills` 的 `catch` 块内仍含 `console.error`（`catch` 与 `finally` 之间非空）**
    - 反向验证已执行并记录（**两次**）：① 删掉 `finally` 里的 `_flushDiagnostics()` 调用 → 四条诊断用例变红（catch 用例仍绿）；② 删掉 `catch` 里的 `console.error` → 顶层 catch 用例变红；两者还原后均转绿
    - 回归：既有 88 例（含 opt-in 打包组共 92 例）保持全绿；`node tests/test-ai-skills.js` 与 `node tests/test-agent-workspace.js` 退出 0
  </acceptance_criteria>
  <done>播种失败在正式版不再静默：四条诊断路径（含源缺失的早退路径）在播种结束时按级别统一经 `console.error` / `console.warn` 输出；逐技能循环之外的异常仍由顶层 `catch` 的 `console.error` 承接（零诊断路径的独立可见面）；干净路径零噪声，「不阻断启动」契约与结构化诊断（供 Phase 50 消费）双双保持。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 随包 `skills-builtin/` → 播种模块 → `managed-skills/` | 播种是**沙箱外的可信写者**；目标目录同时是技能加载器的扫描根，任何落入其中的目录都可能被当成技能 |
| 进程崩溃 / SIGKILL / 强杀 → `managed-skills/` 内的 `.tmp_*` / `.bak_*` 兄弟目录 | 崩溃点落在两次 `rename` 之间时，临时目录成为**持久状态**并被加载器接受（`inContractLayout` 只要求 2 段） |
| `managed-skills/` 内的残留目录 → `refreshSkills()` → 技能集 / 诊断 | 残留被当成技能 → 永久幽灵条目（设置页删不掉）+ 诊断噪声，污染用户可见的技能清单 |
| 播种诊断 → 启动日志 / Phase 50 设置页 | 诊断 message 会被用户与下游阶段读到；无消费方时它是**不可见**的失败信号（nodejieba 事故同型） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-06-01 | Tampering | 崩溃残留 `.tmp_*` / `.bak_*` 被加载成幽灵技能（设置页不可删） | medium | mitigate | 新增 `sweepSeedResidue(managedDir)`，在逐技能循环前清扫锚定 `\.(tmp|bak)_\d+$` 的目录；主用例走**真实 `refreshSkills()`** 断言技能集无 `.bak_`/`.tmp_` 条目且零诊断；反例目录（合法名 / 非数字后缀）必须保留 |
| T-47-06-02 | Denial of Service / Tampering | 每启动一次的 `rename(dst → bak)` 空窗（`SKILL.md` 短暂不可见）+ 白算 sha256 | medium | mitigate | `diff === 'same'` 时 `continue`（不写盘、不产诊断）；inode 稳定性断言把「未重建」变成机械证据（改前 inode 实测变化） |
| T-47-06-03 | Repudiation / Information Disclosure | 播种整体失败在正式版零痕迹（与 nodejieba 事故 9a1ae11 同型） | medium | mitigate | `_flushDiagnostics()` 在 `finally` 中按级别输出，**覆盖早退路径**；**顶层 `catch` 的 `console.error` 保留**（覆盖「诊断为空」的循环外异常路径）；五条用例各钉一条路径 + 一条「零噪声」反例；两次反向验证（删 flush / 删 catch 的 console.error 各红一条）证明断言非空转 |
| T-47-06-04 | Tampering | 清扫正则过宽导致误删合法目录 | medium | mitigate | 正则锚定 `\.(tmp|bak)_\d+$`（与 `safeCopyDir` 的 `Date.now()` 命名成对）；反例目录 `not-residue/`、`user.bak_x/` 必须存活；源码断言禁止裸 `includes('.tmp_')` 形态 |
| T-47-06-05 | Tampering | `detectDiff` 的 size / sha256 层被目录条目污染 → 恒判 `different` → 每次启动重建（本 gap 以更隐蔽的形式复发） | medium | mitigate | 目录标记 + 文件过滤双层设计；`endsWith('/')` 过滤在源代码中显式存在；既有 `:452-471`（含 `sub/` 目录 + `'same'`）与新增「两层嵌套子目录 → `same`」用例共同护栏 |
| T-47-06-06 | Denial of Service | 清扫 / console 输出引入 throw 路径阻断启动 | low | mitigate | 清扫复用 `_cleanupDir`（自身吞错）+ `readdirSync` 包 try/catch；`_flushDiagnostics` 只读内存数组写 console；四条 console 用例均以 `assert.doesNotThrow` 包裹 |
| T-47-06-SC | Tampering | npm / 依赖安装 | low | mitigate | **本计划零新增依赖**（只改 1 源码文件 + 1 测试文件，不碰 `package.json`）；不执行任何安装命令 → 无包合法性 gate 触发 |
</threat_model>

<verification>
1. `node tests/test-builtin-skills-seeder.js 2>&1 | grep -E "^# (tests|pass|fail)"` → `# fail 0` 且 `# tests` > 88（基线；含本次新增用例）
2. `REALM_PACKAGED_VERIFY=1 node tests/test-builtin-skills-seeder.js` 退出 0（opt-in 打包断言组不受影响；若本机无 `dist/` 产物可跳过，须在 SUMMARY 记录跳过理由）
3. `node tests/test-ai-skills.js` 退出 0（技能加载链路回归）
4. `node tests/test-agent-workspace.js` 退出 0（沙箱回归；`agent-workspace.js` 零 diff）
5. `node --check builtin-skills-seeder.js && node --check tests/test-builtin-skills-seeder.js` 全过
6. 零 diff gate（**不要用裸 `git diff`** —— 对已提交的改动它不产生输出，判据会空绿）：`BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-06")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`）；`git diff "$BASE"..HEAD -- agent-workspace.js ai-skills-manager.js main.js package.json` 输出为空
7. 改动面 gate：`BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-06")`（守卫空基线）→ `git diff "$BASE"..HEAD --stat` 只落在 `builtin-skills-seeder.js` 与 `tests/test-builtin-skills-seeder.js` 两处
8. 反向验证已执行并记录（**三次**）：注释掉 `sweepSeedResidue(managedDir);` → 残留用例变红；删掉 `finally` 里的 `_flushDiagnostics();` → console 四条诊断用例变红（顶层 catch 用例仍绿）；删掉 `catch` 里的 `console.error` → **顶层 catch 用例变红**；三者还原后均转绿
9. 机器不变式复核：剥离注释后的 `builtin-skills-seeder.js` 不含 `mtime` / `mtimeMs` / `birthtime`；`sweepSeedResidue` 正则含 `\d+$`
10. 诊断零硬编码复核：剥离注释后的 `builtin-skills-seeder.js` 仍不含 `'find-skills'` / `'skill-creator'` 字面量（既有断言 :533-539 继续通过）
11. 顶层 catch 可见面复核（命令级）：`node -e` 造「中间路径组件是文件」的 `managedDir` 后直调 `seeder.seedBuiltinSkills()`，记录 stderr 里的 `[Realm] 内置技能播种失败（不影响启动）:` 行与 `getSeedDiagnostics()` 的 `[]` 输出到 SUMMARY
</verification>

<success_criteria>
- [ ] 内容一致时重启**不重建**：连续两次播种后 `SKILL.md` 与技能目录 inode 不变（改前实测 inode 变化）
- [ ] `detectDiff` 的目录项参与路径集合比较（空目录也自愈），而 size / sha256 层只对文件条目生效（不会因 EISDIR 恒判 `different`）
- [ ] 预置 `.bak_<ts>` / `.tmp_<ts>` 残留后播种即清除；真实 `refreshSkills()` 零幽灵技能、零诊断
- [ ] 清扫不误删合法目录（`not-residue/` / `user.bak_x/` 存活）
- [ ] 播种失败不再静默：四条诊断路径（含源缺失的**早退路径**）按级别经 `console.error` / `console.warn` 输出；**逐技能循环之外的异常由顶层 `catch` 的 `console.error` 承接（零诊断路径也有输出）**；干净路径零噪声；不 throw
- [ ] `getSeedDiagnostics()` 导出与结构化诊断保留（Phase 50 仍可消费）；代码注释显式记录 console 兜底在 Phase 50 前不得删除，且顶层 catch 的 `console.error` 是**并列的独立可见面**（不得被 flush 取代）
- [ ] `agent-workspace.js` / `ai-skills-manager.js` / `main.js` / `package.json` 零 diff
- [ ] 既有 88 例（opt-in 后 92 例）全绿；`node tests/test-ai-skills.js`、`node tests/test-agent-workspace.js` 全绿
- [ ] 三处反向验证已执行并记录（清扫 / console flush / 顶层 catch 各证明非空转）
</success_criteria>

<output>
Create `.planning/phases/47-bash/47-06-SUMMARY.md` when done
</output>
