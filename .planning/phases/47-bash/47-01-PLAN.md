---
phase: 47-bash
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - skills-builtin/find-skills/SKILL.md
  - skills-builtin/find-skills/LICENSE.txt
  - builtin-skills-seeder.js
  - main.js
  - package.json
  - tests/test-builtin-skills-seeder.js
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "首次启动后 `agent-workspace/managed-skills/find-skills/SKILL.md` 存在，且 `ai-skills-manager.refreshSkills()` 能零诊断加载出 `name === 'find-skills'` 的条目（`_cache.diagnostics` 与 `_cache.errors` 均为空数组）"
    - "`find-skills/SKILL.md` 的 frontmatter 含 `disable-model-invocation: true`，因此该技能不进 system prompt —— 播种后跑 `refreshSkills()` 得到 `buildSkillsPrompt()` 返回 `''`（SEED-04 的机械证据）"
    - "`find-skills/SKILL.md` 的 `description` 是中文（面向 `/` 面板与设置页列表的用户可读性），正文也是中文"
    - "`skills-builtin/**` 内两个受扫描文件（`find-skills/SKILL.md` + `find-skills/LICENSE.txt`）对 `FORBIDDEN_PATTERNS` 零命中，且不存在 `npx skills` 的任意变体（PITFALLS P1 靶心）"
    - "播种按**单个技能目录**粒度判定：`seedBuiltinSkills()` 逐个遍历随包目录，不为已存在的技能提前返回（D-08 —— 整目录一次性判定会让日后新增的内置技能永不播种）"
    - "播种在 `managed-skills/<name>/` 上做**无条件覆盖**（每次启动都重新同步），且覆盖前若磁盘内容与随包内容不一致，必须先产 `warning` 级诊断再覆盖（D-09「不静默」）"
    - "播种是**原子目录替换**：先写 `<skillDst>.tmp_<ts>`，再 `rename` 覆盖，失败回滚 `<skillDst>.bak_<ts>`，不留半成品目录（`managed-skills/<name>/` 要么不存在、要么是完整技能）"
    - "差异检测**不使用 mtime**（每次启动都覆盖会让 mtime 必变 → 100% 误报 `realm_builtin_seed_overwritten`）；判定按「相对路径集合 → 逐文件 size → 逐文件 sha256」逐层短路"
    - "用户手删 `managed-skills/find-skills/` 后，下次调用 `seedBuiltinSkills()` 重新播种（D-10 自愈）；`getSeededSkillNames()` 只反映随包目录名集合，与本次是否实际播种无关"
    - "播种整体失败不阻断应用启动：任何异常走 `console.error` + 产诊断，不 throw（照 `migrateAiMemory()` 先例）"
    - "随包源目录在 `app.isPackaged` 时解析为 `process.resourcesPath/app.asar.unpacked/skills-builtin`，开发态显式回落 `__dirname/skills-builtin`（两处必须成对：`package.json` 的 `asarUnpack` ↔ 运行时路径）"
    - "`skills-builtin/` 的遍历遇 symlink 直接拒绝并产诊断（fail-closed），不跟随复制到工作区"
  artifacts:
    - "`skills-builtin/find-skills/SKILL.md`（新建）：Realm 化改写版，中文正文，frontmatter 含 name / 中文 description / `disable-model-invocation: true`；零安装语义"
    - "`skills-builtin/find-skills/LICENSE.txt`（新建）：MIT 全文（上游仅经 `package.json` 声明 license: MIT，仓库根无独立 LICENSE 文件）"
    - "`builtin-skills-seeder.js`（新建）：导出 `seedBuiltinSkills()` / `resolveBuiltinSkillsSrc()` / `getSeededSkillNames()` / `setBuiltinDepsForTest()` / `_resetForTest()`"
    - "`main.js`：`agentWorkspace.migrateAiMemory()` 之后、`new AIManager()` 之前新增一行同步播种调用 + 顶部一行 require"
    - "`package.json`：`build.asarUnpack` 追加 `\"skills-builtin/**\"`"
    - "`tests/test-builtin-skills-seeder.js`（新建）：`withTempRoot` 脚手架 + 播种端到端断言 + 零安装语义扫描器 + 差异诊断 / 自愈 / 原子性断言"
  key_links:
    - "`main.js` 启动链路：`ensureWorkspaceDir()` → `migrateAiMemory()` → **`seedBuiltinSkills()`** → `new AIManager()` → `aiManager.init()` → `refreshSkills()` —— 播种必须先于 `init()`，否则首轮加载看不到内置技能"
    - "`resolveBuiltinSkillsSrc()`（随包源，独立链路）→ `getManagedSkillsDir()`（`agent-workspace.js` 既有导出，唯一写入目标）→ 技能加载器的两个扫描根之一"
    - "`package.json` 的 `asarUnpack: ['skills-builtin/**']` ↔ 运行时 `process.resourcesPath/app.asar.unpacked/skills-builtin`（成对不变式，漏一侧即在正式版静默失败）"
  prohibitions:
    - statement: "`find-skills/SKILL.md` 与 `LICENSE.txt` 中不得出现任何「执行外部安装」语义 —— 含 `npx` / `npm i` / `npm install` / `curl | sh` / `-y` / `--yes` / `-g` / `--global` 及其中文等价写法（「安装到全局」「下载并执行」「自动安装」「执行安装命令」「跳过确认」「装到系统」）"
      status: unresolved
      verification: judgment
      reason: "P1（S1 阻断门禁）的字面判据；ROADMAP 成功判据 2。fallback 召回项，无 wired check，按 flagged-unverified 处理"
    - statement: "`find-skills/SKILL.md` 不得给出任何可执行的检索命令，也不得复述上游 `-g 装到全局 / -y 跳过确认` 的安全绕过说明；检索必须经 Realm 已有的 `web_fetch` 工具层完成"
      status: unresolved
      verification: judgment
      reason: "上游第 100 行 + 第 103 行是 P1 靶心的完整绕过说明书；只要正文存在可执行安装路径，门禁即失守"
    - statement: "`find-skills/SKILL.md` 不得包含「若导入入口尚未提供，先手动把目录放入 skills/」这类兼容分支"
      status: unresolved
      verification: judgment
      reason: "D-03 明确否决 —— 手改目录这条指引本身接近安装语义"
    - statement: "`builtin-skills-seeder.js` 与测试以外的代码中不得出现字面量 `'find-skills'` / `'skill-creator'`（seeded 身份必须靠扫描随包目录名得到，不得硬编码）"
      status: unresolved
      verification: judgment
      reason: "D-11：零状态文件 + 零硬编码是「删不掉、不双写、三环境不分叉」的前提；硬编码会让新增内置技能漏判"
    - statement: "不得为 `managed-skills/` 引入第二个沙箱 root、只读挂载，也不得改动 `agent-workspace.js` 的任何既有逻辑（`resolveInside` / `createSandboxEnv` / 既有访问器零 diff）"
      status: unresolved
      verification: judgment
      reason: "Phase 46 已否决只读挂载/第二 root；沙箱零改动是本阶段硬约束（`agent-workspace.js` 当前不 require electron，塞入 `app.isPackaged` 会直接破坏其 21 例纯 Node 测试）"
    - statement: "播种不得异步化到 `ensureWorkspaceDir()` / `migrateAiMemory()` 的同步帧之外（不得返回 Promise 让启动链路 fire-and-forget）"
      status: unresolved
      verification: judgment
      reason: "播种必须早于 `aiManager.init()` → `refreshSkills()`；fire-and-forget 会让首轮请求看不到内置技能"
    - statement: "差异诊断的 `message` 不得拼接 `REALM_SKILL_CREATOR_PYTHON` 等环境变量的值"
      status: unresolved
      verification: judgment
      reason: "ASVS V7：诊断只承载 level / code / message / path / skillName 与限额族数值；环境变量值属实现细节，无诊断价值"
---

<objective>
立起 Phase 47 的端到端纵切（tracer）：把「随包 `skills-builtin/` → 播种模块 → `agent-workspace/managed-skills/` → 技能加载器零诊断识别 → `disable-model-invocation` 使其不进 prompt」这条完整链路一次打通，并把 find-skills 从上游的「安装说明书」改写成 Realm 的「零安装候选清单技能」。

Purpose: 这条链路是 P1（S1 阻断门禁）的载体 —— 随包分发一份「用 `npx` 绕过沙箱装任意代码」的说明书是本阶段要消除的核心风险，而播种链路正是把随包内容送进模型可见范围的那条管道。先把最薄的一条路径（一个技能、一次复制、一次加载）证明掉，Wave 2 的两个计划才是在这条已验证的链路上加语义（install 档 / 上游快照），而不是在未验证的假设上叠代码。

Output: `skills-builtin/find-skills/`（改写版 SKILL.md + MIT LICENSE.txt）；新建 `builtin-skills-seeder.js`（源解析 / 单技能目录粒度同步 / 原子替换 / 差异诊断 / seeded 身份）；`main.js` 启动接线；`package.json` asarUnpack 条目；新建 `tests/test-builtin-skills-seeder.js`。

**tracer 说明**：Task 1 是本阶段唯一的 `type="tracer"` 任务 —— 它横跨五个文件、四个层级（打包层 / 播种层 / 加载层 / prompt 层），并自带一条真实的端到端 `<verify>`（播种 → 加载 → 零诊断 → promptBlock 为空）。Task 2、3 是从这条已验证切面向外扩展的原子性/诊断层与门禁断言层。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-bash/47-CONTEXT.md
@.planning/phases/47-bash/47-RESEARCH.md
@.planning/phases/47-bash/47-PATTERNS.md
@.planning/phases/47-bash/47-VALIDATION.md

@agent-workspace.js
@ai-skills-manager.js
@main.js
@package.json
@tests/test-agent-workspace.js
@tests/test-ai-skills.js
</context>

<artifacts_this_phase_produces>
本计划（47-01）新建的符号与命名，供 plan-review-convergence 的 source-grounding 排除新增符号误报：

**新文件**
- `skills-builtin/find-skills/SKILL.md`
- `skills-builtin/find-skills/LICENSE.txt`
- `builtin-skills-seeder.js`
- `tests/test-builtin-skills-seeder.js`

**`builtin-skills-seeder.js` 导出（本计划建立全部）**
- `seedBuiltinSkills()`（同步，启动期一次性调用）
- `resolveBuiltinSkillsSrc()`（随包源目录解析；`app.isPackaged` 分支）
- `getSeededSkillNames()`（seeded 身份 = 随包目录名集合，D-11）
- `setBuiltinDepsForTest({ srcDir, managedDir })`（测试注入器）
- `_resetForTest()`

**模块内部函数（不导出）**
- `safeCopyDir(src, dst)`（原子目录替换）
- `detectDiff(srcDir, dstDir)`（返回 `'same' | 'different' | 'missing'`）
- `hashFile(file)`（`crypto.createHash('sha256')`）
- `copyDirRecursive(src, dst)`（遇 symlink 拒绝）

**新诊断 code（`realm_` 前缀，对齐 `ai-skills-manager.js` 既有形状）**
- `realm_builtin_src_missing`（error）
- `realm_builtin_seed_overwritten`（**warning**）
- `realm_builtin_seed_failed`（error）
- `realm_builtin_src_invalid`（error）

**`package.json` 变更**
- `build.asarUnpack` 新增条目 `"skills-builtin/**"`（既有 `"node_modules/nodejieba/**"` 保留）

**新测试文件内的常量**
- `FORBIDDEN_PATTERNS`（零安装语义禁用模式表）、`INSTALL_IMPERATIVES`、`EXEMPTIONS`（行级豁免清单）

**刻意不产出（decided omission，不是遗漏 —— source-grounding 请据此排除）**
- `skill-creator/**` —— 归 47-03（上游快照 + check_env.mjs）。
- `THIRD_PARTY_NOTICES.md` —— 归 47-03（需要两个固定 SHA 的五要素，find-skills 与 skill-creator 一起写）。
- `PACKAGE_MANAGER_INSTALL_PATTERNS` / `install` 档 —— 归 47-02；本计划**不碰** `ai-bash-policy.js`。
- `build.files` 排除项（`.planning/**` 等）—— 归 47-04（Open Question Q4 的决策落点）。
- 文档同步 —— 归 47-04。
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端最小播种纵切（随包 → 播种 → 加载 → 零诊断 → 不进 prompt）</name>
  <files>skills-builtin/find-skills/SKILL.md, skills-builtin/find-skills/LICENSE.txt, builtin-skills-seeder.js, main.js, package.json, tests/test-builtin-skills-seeder.js</files>
  <reversibility rating="costly">D-12：随包分发走 `asarUnpack` + `app.isPackaged` 路径分支。改动需同步 `package.json` 的 `asarUnpack` 与播种模块的路径解析两侧，且必须重跑 `make install` 后的 .app 验证（开发态 `npm run dev` 走的是 `__dirname` 回落分支，测不到打包态）。**被否决的竞争路线：** 47-RESEARCH.md §「内置技能目录的打包」实测后推荐「显式递归 `readdirSync` + `readFileSync` 读 asar 内文件、零构建配置改动」。本计划**不采用**它，理由三条：① `asarUnpack` 得到真实文件路径，播种可直接用 `fs.cpSync` / 递归复制；② 与既有的 nodejieba 先例（`favorites-manager.js` 的 `app.isPackaged` + `process.resourcesPath`）一致，不引入第二套 asar 读取写法；③ 该路线的前提顾虑是「`cpSync` 在 asar 源上不可靠」——而在 `asarUnpack` 下源是真实路径，这个顾虑不成立。代价是引入构建配置与路径分支，由 47-04 的打包实跑收口。</reversibility>
  <read_first>
    - agent-workspace.js（全文，391 行）：:24-58（测试注入覆写字段 + `setWorkspaceDir` 的唯一入口形态）、:96-126（`getSkillsDir` / `getManagedSkillsDir` / `ensureWorkspaceDir` —— 播种的唯一写入目标，**只调用不改动**）、:128-145（`migrateAiMemory` —— 幂等 + 非阻断错误处理 + `fs.cpSync` 的逐字先例）、:17-19 与 :38-42（惰性 electron require 纪律）、:378-391（扁平具名导出块）
    - favorites-manager.js:275-314（`app.isPackaged` + `process.resourcesPath/app.asar.unpacked` 路径分支的逐字先例；注意 nodejieba 在开发态**不设路径**，而 `skills-builtin/` 开发态必须**显式回落 `__dirname`**）
    - main.js:4030-4059（`ensureWorkspaceDir()` / `migrateAiMemory()` / `new AIManager()` 三行的精确位置与上下文；require 区照 `const agentWorkspace = require('./agent-workspace')` 的并列写法）
    - ai-skills-manager.js:1-46（模块头 + `LIMITS` + `EMPTY_CACHE` 形状）、:236-263（`pushEntryDiag` / `toRealmDiag` —— 诊断形状）、:440-460（`refreshSkills` 的 `rootDirs` 顺序语义：`rootDirs[0]` → managed、`rootDirs[1]` → user）、:637-668（`buildSkillsPrompt` / `getSkillsSnapshot` / `_resetCacheForTest` / 导出块）
    - tests/test-agent-workspace.js:1-42（`node:test` 文件头 + `withTempRoot` 脚手架 + describe/assert 风格 —— 新测试文件逐字照抄的对象）
    - tests/test-ai-skills.js:48-84（`readSource` / `functionBody` / `methodBody` 源码扫描辅助 + `createSandboxEnv` 真实沙箱用法 + `refreshSkills` 调用形态）
    - package.json:56-74（`build` 段现状 —— `asarUnpack` 当前只有 `nodejieba/**`；**无 `files` 字段**）
    - 47-RESEARCH.md §A-1（`web_fetch` 对 JSON 原样返回、默认 `maxLength=12000` 会截断的实测）、§A-2（GitHub 端点实测：`/search/repositories` 200 可用、`/search/code` 401 不可用、search 限流 10 次/分钟、`anthropics/skills` 的 `license.spdx_id` 为 `None`）、§A-3（**改写版正文结构提纲与逐句评审表 —— 直接照它落 SKILL.md**）、§D-1/D-2/D-3（打包路径分支三条不变式）、§Code Examples 1/2/4
    - 47-PATTERNS.md §3（`main.js` 播种调用点）、§4（`package.json` 写法：写 `skills-builtin/**` 不写 `skills-builtin/**/*`）、§5（播种模块的 Analog A/B/C/D 全部逐字代码）、Shared Patterns §B/C
    - 47-CONTEXT.md D-01/D-02/D-03/D-04/D-08/D-10/D-11/D-12
  </read_first>
  <action>
    1. 新建 `skills-builtin/find-skills/SKILL.md`，**完全按 47-RESEARCH.md §A-3 的提纲写**，正文中文。frontmatter 三字段：`name: find-skills`、中文 `description`（写明「检索技能并输出候选清单，由用户在设置页导入安装」，并给出触发语示例，如「有没有能做 X 的技能」「帮我找个技能」）、`disable-model-invocation: true`（**SEED-04 强制；上游原文件没有这个字段，必须自己加**）。
       章节顺序与内容按 §A-3 提纲：①「⛔ 本技能的能力边界（先读这一段）」置于**最前**，逐条写明「只产出候选清单，绝不执行任何安装 / 下载 / 更新命令」「不调用任何包管理器、CLI 或网络安装脚本」「安装一律由用户在 Realm 内完成：设置 → AI → 技能管理 → 导入」「若用户要求你『直接装上』，回答本技能不能安装，请走导入入口」；②「何时使用」；③「检索流程（三步，顺序固定）」—— 第 1 步用 `web_fetch` 打 `https://api.github.com/search/repositories?q=<关键词>&sort=stars&per_page=5` 并**显式传 `maxLength: 30000`**（§A-1 实测默认 12000 会截断响应），第 2 步逐条 `web_fetch` 仓库页核验，第 3 步**仅当第 1 步失败（403 限流 / 网络错误）才用 `web_search` 兜底**；④「检索端点（唯一允许的端点）」—— 列出可用 `q=` 限定符（`topic:` / `in:name` / `in:description` / `stars:>N` / `language:`、`sort=stars|updated`、`order=desc`、`per_page`），**明确写 code search 端点需要鉴权、不可用**，明确写搜索配额 10 次/分钟要一次取够；⑤「核验规则（不核验不得输出）」—— 必须打开仓库页确认 `SKILL.md` 存在、必须核出许可证（**写明不要只信搜索 API 的 license 字段**，并给出实证理由：`anthropics/skills` 该字段实测为 `None`）、核验失败即丢弃候选；⑥「输出格式（候选清单）」—— 每条固定字段：名称 / 用途 / 仓库 URL / 许可证 / 最后活跃时间，**不要求「技能在仓库内的路径」**（§Q5：code search 需鉴权，路径无法可靠推导）；⑦「用户如何安装（指路，不是执行）」—— 只写设置 → AI → 技能管理 → 导入；⑧「边界情况」—— 403 限流告知用户稍后再试或用 `web_search` 兜底；用户想创建新技能则指路 `skill-creator` 技能，**不得出现任何 init 命令**。
       **写完后逐句过 §A-3 的评审表**：对每一行问「这段文字被模型执行后会做什么」—— 若答案是「发起一次工具调用（`web_fetch` / `web_search` / `read` / `write`）」则安全；若答案是「在 bash 里跑一条命令」则必须改写。
    2. 新建 `skills-builtin/find-skills/LICENSE.txt`：写入标准 MIT 许可证全文。**注意上游事实（本会话已核实）**：`vercel-labs/skills` 仓库根**没有独立 LICENSE 文件**，许可证仅由 `package.json` 的 `"license": "MIT"` 声明，且 `author` 字段为空 → 版权行写 `Copyright (c) vercel-labs`。该仓库根另有一个 `ThirdPartyNoticeText.txt`（6,585 B），经核实是 **Skills CLI 的第三方组件清单**，与本技能文本无关，**不随包、不复制**。
    3. 新建 `builtin-skills-seeder.js`（仓库根，`'use strict'` 风格与既有主进程模块一致）。模块头 JSDoc 写明：本模块是**随包内置技能的唯一播种者**；写入目标是 `agent-workspace.getManagedSkillsDir()`；seeded 身份 = 随包目录名集合（**零状态文件、零硬编码**）；任何失败仅告警不阻断启动；**electron 依赖惰性获取**（纯 Node 测试环境不可加载）。
       - `resolveBuiltinSkillsSrc()`：函数体内惰性 `const { app } = require('electron');` → `if (app.isPackaged) return path.join(process.resourcesPath, 'app.asar.unpacked', 'skills-builtin');` → `return path.join(__dirname, 'skills-builtin');`（**开发态必须显式回落 `__dirname`**，与 nodejieba 先例的差异点）。
       - `getSeededSkillNames()`：`fs.readdirSync(resolveBuiltinSkillsSrc(), { withFileTypes: true })` → 过滤 `entry.isDirectory()` → 过滤 `fs.existsSync(path.join(dir, 'SKILL.md'))` → 返回 name 数组。源目录不存在时返回 `[]`。**不加任何硬编码技能名。**
       - `safeCopyDir(src, dst)`：照 47-PATTERNS.md §5 Analog C 的算法，三条必须保留（timestamped 的 tmp/bak 命名、`renameSync` 失败把 bak 改回的回滚路径、失败后清理 tmp），三条必须改（用 `fs.cpSync(src, tmpDst, { recursive: true })` 代替手写递归；**遇 symlink 拒绝**而非跟随；`AppError`/`errorBus` 改成本模块的诊断形状）。tmp 与 bak **必须是 `<dst>.tmp_<ts>` / `<dst>.bak_<ts>`（同父目录 → 必然同卷）**，**不要放 `os.tmpdir()`**（macOS `/var/folders` 可能异卷 → `renameSync` 抛 `EXDEV`）。`_cleanupDir(bakDst)` 必须包在自己的 `try` 里吞错 —— 否则「清理失败但替换其实成功」会被误报为失败。
       - `detectDiff(srcDir, dstDir)` 返回 `'same' | 'different' | 'missing'`，逐层短路：① `dstDir` 不存在 → `'missing'`；② 递归收集两边相对路径集合不等 → `'different'`；③ 逐文件 `size` 不等 → `'different'`；④ 逐文件 `sha256` 不等 → `'different'`；⑤ 全同 → `'same'`。**绝不用 mtime 判差异。** `hashFile` 用 `crypto.createHash('sha256')`（仓库既有先例 `media-cache-manager.js`）。
       - `seedBuiltinSkills()`：`getSeededSkillNames()` 为空则产 `realm_builtin_src_missing`（error）并 return；否则 `fs.mkdirSync(getManagedSkillsDir(), { recursive: true })`，然后**逐个技能目录**（不得整目录一次性判定/提前返回）：`detectDiff` → 若 `'different'` 先产 `realm_builtin_seed_overwritten`（**warning**，`message` 含技能名 + 「已被随包版本覆盖」+ 正确做法「如需定制请复制到 `skills/` 同名目录以遮蔽；如需停用请在设置页禁用」）→ 再 `safeCopyDir`；单技能失败 `continue` 不 abort，产 `realm_builtin_seed_failed`（error，`message` 含原始错误）。整个过程包在 `try/catch` 里，`catch` 只 `console.error('[Realm] 内置技能播种失败（不影响启动）:', err.message)`。
       - `setBuiltinDepsForTest({ srcDir, managedDir })` / `_resetForTest()`：照 `agent-workspace.js:44-50` 的覆写字段模子，`null` 恢复默认。
       - `module.exports = { seedBuiltinSkills, resolveBuiltinSkillsSrc, getSeededSkillNames, setBuiltinDepsForTest, _resetForTest };`
    4. `main.js`：require 区（照 `const agentWorkspace = require('./agent-workspace')` 并列）新增 `const builtinSkillsSeeder = require('./builtin-skills-seeder');`；在 `agentWorkspace.migrateAiMemory();` 与 `// 初始化 AI Manager（per Phase 19）` 之间插入两行注释 + 一行调用：注释说明「播种随包内置技能到 managed-skills；必须在 aiManager 创建之前 —— 否则首轮 refreshSkills 看不到内置技能」，调用 `builtinSkillsSeeder.seedBuiltinSkills();`。**同步调用，不 await、不 fire-and-forget。**
    5. `package.json`：`build.asarUnpack` 数组追加 `"skills-builtin/**"`（**写 `skills-builtin/**`，不要写 `skills-builtin/**/*`** —— 后者的 glob 语义在部分实现里不含目录本身；既有 `node_modules/nodejieba/**` 已实测覆盖三层嵌套，是 `**` 语义的现场证据）。既有条目与其余 `build` 字段一律不动。
    6. 新建 `tests/test-builtin-skills-seeder.js`（`node:test` + `node:assert`，纯 Node，零框架依赖）：
       - 文件头照抄 `tests/test-agent-workspace.js:1-19`（含 `用法: node tests/test-builtin-skills-seeder.js`）；
       - `withTempRoot(t)` 照抄同款，临时目录前缀用 `realm-seeder-test-`，`t.after` 内清理 + `workspace.setWorkspaceDir(null)` + `seeder.setBuiltinDepsForTest(null)` + `seeder._resetForTest()` + `aiSkills._resetCacheForTest()`（模块级缓存跨用例污染会让后续断言假失败）；
       - **一条端到端纵切断言**（本任务的 tracer 验收）：把 `setBuiltinDepsForTest` 的 `srcDir` 指向**真实仓库内**的 `skills-builtin`（`path.join(__dirname, '..', 'skills-builtin')`）、`managedDir` 指向临时 root 下的 `managed-skills` → 调 `seedBuiltinSkills()` → 断言 `managed-skills/find-skills/SKILL.md` 存在且与源文件逐字节相同（`fs.readFileSync` 相等）→ `await aiSkills.refreshSkills(await workspace.createSandboxEnv({ cwd: root }), { rootDirs: [workspace.getManagedSkillsDir(), workspace.getSkillsDir()] })`（**注意 `rootDirs` 顺序：managed 在前**）→ 断言 `getSkillsSnapshot().skills` 含 `source === 'managed'` 且 `skill.name === 'find-skills'` 的条目 → 断言 `_cache.diagnostics` 与 `_cache.errors` 均为空数组（**零诊断**）→ 断言 `aiSkills.buildSkillsPrompt() === ''`（SEED-04 的机械证据：`disable-model-invocation: true` → 不进 prompt）。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总行出现 `# fail` 后跟非 0 计数；或输出中完全不出现 `# pass` 行（说明测试文件未被真正执行）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`node tests/test-builtin-skills-seeder.js` 退出 0，且端到端纵切断言通过 —— 链路为「真实 `skills-builtin/` → `seedBuiltinSkills()` → 临时 `managed-skills/find-skills/SKILL.md` 与源逐字节相同 → `refreshSkills()` 零诊断加载出 `name: find-skills` 且 `source: managed` → `buildSkillsPrompt() === ''`」
    - 源码断言：`builtin-skills-seeder.js` 含 `function seedBuiltinSkills(` / `function resolveBuiltinSkillsSrc(` / `function getSeededSkillNames(` / `function safeCopyDir(` / `function detectDiff(`；`module.exports` 含全部 5 个导出
    - 源码断言：`resolveBuiltinSkillsSrc()` 函数体内同时含 `app.isPackaged` 与 `process.resourcesPath` 与 `__dirname`（三条路径不变式齐备），且 `require('electron')` **不在模块顶层**（`grep -c "require('electron')" builtin-skills-seeder.js` 为 1 且其行号 > 文件头 20 行）
    - 源码断言：`package.json` 的 `build.asarUnpack` 同时含 `"node_modules/nodejieba/**"` 与 `"skills-builtin/**"`
    - 源码断言：`main.js` 中 `builtinSkillsSeeder.seedBuiltinSkills()` 的行号位于 `migrateAiMemory()` 之后、`new AIManager()` 之前（可用 `readSource` + `indexOf` 断言三者行序）
    - 源码断言：`skills-builtin/find-skills/SKILL.md` 含 `disable-model-invocation: true`，且首段含「绝不执行任何安装」
    - 源码断言：`builtin-skills-seeder.js` 中不出现字面量 `'find-skills'` 或 `'skill-creator'`（零硬编码，D-11）
    - 行为断言：`node --check builtin-skills-seeder.js && node --check main.js` 均退出 0
  </acceptance_criteria>
  <done>一条最薄的播种链路端到端跑通并通过自动化断言：随包 `skills-builtin/find-skills/` 经播种进入 `managed-skills/`，技能加载器零诊断识别它，且因 `disable-model-invocation: true` 而不进 system prompt；`package.json` 与运行时路径解析两侧成对就位（打包态路径由 47-04 的实跑验证收口）。</done>
</task>

<task type="auto">
  <name>Task 2: 播种的原子性、差异诊断与自愈（safeCopyDir / detectDiff / 4 诊断 code / symlink fail-closed）</name>
  <files>builtin-skills-seeder.js, tests/test-builtin-skills-seeder.js</files>
  <reversibility rating="costly">D-08/D-09：每次启动无条件覆盖 `managed-skills/<name>/` + 覆盖前产差异诊断。该模型显式偏离 ROADMAP 的 O4「版本戳 + 未修改才覆盖」；回退需引入内容 hash 状态与三分支判定，并同步调整 Phase 48/50 的诊断与展示语义。</reversibility>
  <read_first>
    - builtin-skills-seeder.js 当前全文（Task 1 刚落下的 `safeCopyDir` / `detectDiff` / `seedBuiltinSkills`）
    - 47-RESEARCH.md §C-1（原子性的失败路径清单，含「`_cleanupDir(bakDst)` 必须自己吞错」与「tmp 必须同父目录/同卷」两条修正）、§C-3（差异检测口径与「绝不用 mtime」的理由）、§Pattern 2（`safeCopyDir` 的完整形状与三条简化建议）、§Pattern 4（诊断形状对齐）
    - 47-PATTERNS.md §5（Analog C 的逐字算法 + 必须保留/必须改的六条）与 Shared Patterns §A（4 个新诊断 code 的表：level / 触发 / 必需字段）
    - ai-skills-manager.js:236-263（`pushEntryDiag` / `toRealmDiag` 的字段全集）与 :499-536（限额族带 `limit` / `currentValue`；warning 级诊断必须给出「正确做法」的逐字写法）
    - 47-CONTEXT.md D-09（warning 而非 error：覆盖照常完成，功能未降级）、D-10（自愈重播）
  </read_first>
  <action>
    1. `detectDiff` 补全为可测的导出内部函数并补齐断言覆盖面：确保逐层短路顺序在源码中可读（相对路径集合 → size → sha256），且**源码中不出现 `mtime` / `mtimeMs` / `birthtime`**（这是 D-09 的机械不变式：每次启动都覆盖会让 mtime 必变 → 100% 误报）。
    2. `seedBuiltinSkills()` 的诊断产出补齐为 4 个 code 的完整集合，全部经**模块内统一的诊断收集器**（建议 `_diagnostics` 模块级数组 + `pushDiag()` 助手），形状严格对齐 `ai-skills-manager.js` 的 `{ level, code, message, path, skillName? }`：
       - `realm_builtin_src_missing`（**error**，`path` = 随包源目录；触发：源目录不存在或无任何含 `SKILL.md` 的子目录）
       - `realm_builtin_seed_overwritten`（**warning**，`path` = 目标技能目录，`skillName`；触发：`detectDiff` 返回 `'different'`；`message` 必须同时含「已被随包版本覆盖」与正确做法（`skills/` 同名遮蔽 / 设置页禁用）—— 照 `realm_layout_violation` 的「warning 必须给正确做法」先例）
       - `realm_builtin_seed_failed`（**error**，`path` + `skillName`；`message` 含原始错误文本）
       - `realm_builtin_src_invalid`（**error**，`path`；触发：源目录下某子目录缺 `SKILL.md` —— 它与 `getSeededSkillNames()` 的过滤互为表里：过滤掉的和被判定为不合法的都要有诊断，不得静默）
       - 导出 `getSeedDiagnostics()`（或在 `seedBuiltinSkills()` 返回值里返回诊断数组 —— **二选一，但必须可被测试读取**），供 Phase 50 消费与测试断言。
    3. `copyDirRecursive` 的 symlink 处理改为 **fail-closed**：遇 `fs.lstatSync(s).isSymbolicLink()` 时抛错（携带该条目的相对路径），由 `safeCopyDir` 的 `catch` 转成 `realm_builtin_seed_failed` 并跳过该技能。理由：随包源是 git 追踪的静态文本，**绝不应含 symlink**；跟随复制等于把工作区外的目标内容注入技能目录。
    4. 自愈语义（D-10）补齐：`detectDiff` 返回 `'missing'` 时**不产** `realm_builtin_seed_overwritten`（那是「覆盖了用户的修改」，`'missing'` 是「本来就没有」）直接 `safeCopyDir`。手删后的重播由此自然成立，无需额外状态。
    5. `tests/test-builtin-skills-seeder.js` 增补一个 `describe('播种原子性与差异诊断（SEED-02 / D-08 / D-09 / D-10）')` 分组，用例：
       - **幂等**：连续调两次 `seedBuiltinSkills()`，第二次不产生新的 `realm_builtin_seed_overwritten`（内容一致 → `detectDiff === 'same'` → 无诊断），且目标文件字节不变；
       - **不静默覆盖**：手工把目标 `SKILL.md` 改写为不同内容后重跑，断言 `getSeedDiagnostics()` 含 `code === 'realm_builtin_seed_overwritten'` 且 `level === 'warning'`，**且**目标文件已被覆盖为随包版本（诊断与覆盖同时发生 —— 这是 D-09 与 O4 的分野）；
       - **自愈重播**：`fs.rmSync(<managed>/find-skills, { recursive: true, force: true })` 后重跑，断言目录重新出现、`SKILL.md` 与源逐字节相同、且**未**产 `realm_builtin_seed_overwritten`（`'missing'` 语义）；
       - **原子性**：构造一次 `safeCopyDir` 失败（例如把 `dst` 的父目录设为只读，或注入一个会使 `renameSync` 失败的桩），断言目标目录**要么不存在、要么是完整技能**（不出现「有目录但 `SKILL.md` 缺失」的半成品），且残留的 `.tmp_*` / `.bak_*` 已被清理；
       - **symlink fail-closed**：在临时 `srcDir` 下造一个含 symlink 的假技能目录，断言该技能被跳过、其余技能照常播种、且产 `realm_builtin_seed_failed`；
       - **src 缺失**：把 `srcDir` 指向不存在路径，断言产 `realm_builtin_src_missing` 且**不 throw**（不阻断启动）。
    6. 全部新用例的临时目录经 `withTempRoot` 建立、`t.after` 清理，不触碰真实 userData。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或「不静默覆盖」「自愈重播」「原子性」「symlink fail-closed」四组中任一条 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：连续两次播种后第二次**不产** `realm_builtin_seed_overwritten`（`detectDiff === 'same'` 生效）
    - 行为断言：手工改写目标文件后重跑，同时观察到 `warning` 级 `realm_builtin_seed_overwritten` 诊断**与**目标文件被覆盖为随包版本
    - 行为断言：删掉目标技能目录后重跑，目录重新完整出现，且**未**产 `realm_builtin_seed_overwritten`
    - 行为断言：`safeCopyDir` 失败后目标目录不含半成品（不存在「目录在但 `SKILL.md` 缺失」的状态），且无残留 `.tmp_*` / `.bak_*` 目录
    - 行为断言：临时源目录内的 symlink 技能被跳过且产 `realm_builtin_seed_failed`，同批其余技能仍正常播种
    - 行为断言：`srcDir` 不存在时产 `realm_builtin_src_missing` 且调用方未收到 throw（`assert.doesNotThrow` 语义）
    - 源码断言：`builtin-skills-seeder.js` 中不出现 `mtime` / `mtimeMs` / `birthtime`
    - 源码断言：4 个新 code（`realm_builtin_src_missing` / `realm_builtin_seed_overwritten` / `realm_builtin_seed_failed` / `realm_builtin_src_invalid`）全部出现在源码中；`realm_builtin_seed_overwritten` 的 `level` 为 `'warning'`（不是 `'error'`）
    - 源码断言：`safeCopyDir` 内的临时目录命名含 `.tmp_` 与 `.bak_` 且基于 `dst` 派生（不含 `os.tmpdir()` / `tmpdir(`）
  </acceptance_criteria>
  <done>播种具备生产级失败语义：整目录原子替换不留半成品、覆盖用户修改一定可见、用户删除后自愈、源目录被投毒（symlink）时失败关闭而非跟随、源缺失不阻断启动；seeded 身份与差异诊断均可被下游阶段（48/50）读取。</done>
</task>

<task type="auto">
  <name>Task 3: 零安装语义扫描器与 SEED-03/SEED-04 门禁断言（二段式，含豁免清单）</name>
  <files>tests/test-builtin-skills-seeder.js</files>
  <read_first>
    - 47-RESEARCH.md §A-4（`FORBIDDEN_PATTERNS` / `INSTALL_IMPERATIVES` 的完整表与「已知需要豁免的例外」清单 —— **直接照抄，不要重新设计正则**）
    - 47-RESEARCH.md §A-3（逐句评审表：❌ 必须避免的句子形态 vs ✅ 允许的等价表达）
    - 47-RESEARCH.md §B-2（实测：上游 skill-creator `SKILL.md` 对安装语义 regex **0 命中**；两个 `LICENSE.txt` 也 0 命中）
    - 47-CONTEXT.md D-02（显式禁令段）、D-05（check_env.mjs 的 `installGuidance` 与 `Do not auto-install dependencies from this skill.` 是**反向语义**，必须豁免）
    - tests/test-ai-skills.js:48-73（`readSource` / `functionBody` / `methodBody` 源码扫描辅助）
  </read_first>
  <action>
    1. 在 `tests/test-builtin-skills-seeder.js` 顶部定义三张表，**逐字照抄 47-RESEARCH.md §A-4**：`FORBIDDEN_PATTERNS`（含英文 CLI/包管理器形态、危险旗标 `-y` / `--yes` / `-g` / `--global`、中文语义等价写法「安装到全局」「下载并执行」「自动安装」「执行安装命令」「跳过确认」「装到系统」）、`INSTALL_IMPERATIVES`（英文 + 中文祈使句）、`EXEMPTIONS`（行级豁免清单）。
    2. 落**二段式扫描器**（这是 §A-4 的落地建议，不得做成无豁免全文扫描 —— 那会被 `check_env.mjs` 的「请勿自动安装」声明直接打爆）：
       - **第 1 段（零容忍、无豁免）**：扫描 `skills-builtin/**` 下**所有 `SKILL.md` 文件 + 所有 `LICENSE.txt`**（本计划只有 find-skills 两个文件；47-03 会把 skill-creator 的加进来，扫描器按 glob 自动覆盖，不要写死文件名）。命中即 `assert.fail`，失败消息里给出**文件相对路径 + 行号 + 命中模式 + `why`**。
       - **第 2 段（带行级豁免）**：扫描 `skills-builtin/**` 下**所有尚未被第 1 段覆盖的文件** —— 即第 1 段之外的**全部**内容（本计划期为零个；47-03 落地后会覆盖 `agents/*.md`、`assets/eval_review.html`、`eval-viewer/*`、`references/schemas.md`、`scripts/*`）。**用「排除法」而不是「列举 `scripts/` + `resources/`」表达扫描范围** —— 列举法会漏掉 `agents/` / `assets/` / `eval-viewer/` / `references/` 四类目录（47-RESEARCH §A-4 的扫描器表头明确写的是 `skills-builtin/**/*`，SEED-03 也说的是「内置技能文本」而未限定文件类型）。豁免清单 `EXEMPTIONS` 以「文件相对路径 + 匹配该行的正则 + 豁免理由」三元组表达，**必须在测试注释里写死豁免理由**。本阶段 47-01 该目录集为空 → 断言「扫描器已就位且豁免清单非空时不影响第 1 段」；47-03 落地后由它实测豁免生效与排除法覆盖面。
    3. 加一条**专项断言**（PITFALLS P1 靶心）：`skills-builtin/**` 内不存在 `npx skills` 的任意变体（大小写不敏感、允许中间任意空白），源码级断言而非 shell grep —— 与上面第 1 段重叠也保留，因为它是门禁判据的**点名项**。
    4. 加 **SEED-04 断言**：遍历 `skills-builtin/**/SKILL.md`，用与 `ai-skills-manager` 相同的判定面断言每个内置技能的 frontmatter 含 `disable-model-invocation: true`，且 `description` 含至少一个 CJK 字符（中文面向用户 —— D-04）。
    5. 加 **D-11 零硬编码断言**：`builtin-skills-seeder.js` 的源码（去掉注释后）不含 `'find-skills'` / `'skill-creator'` 字面量。用 `readSource` + 简单注释剥离（`/\*[\s\S]*?\*/` 与 `//.*`）后断言。**测试文件自身不受此约束**（测试必须能点名技能）。
    6. 加 **P1 反向证据断言**（为 47-03 的 SKILL-09 预埋，本计划先立骨架）：`ai-bash-policy.js` 的 `DANGEROUS_INTERPRETERS` 集合仍含 `'node'` 与 `'python3'`（证明「技能脚本经既有 bash 工具执行、零新增权限机制」这个不变式没有被本阶段破坏）。用 `require('../ai-bash-policy').DANGEROUS_INTERPRETERS` 直接断言，不用源码扫描。
  </action>
  <verify>
    <automated>node tests/test-builtin-skills-seeder.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或扫描器在 `skills-builtin/**/SKILL.md|LICENSE.txt` 上命中任一 `FORBIDDEN_PATTERNS`（失败消息会打印文件+行号+模式）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`node tests/test-builtin-skills-seeder.js` 退出 0，第 1 段扫描在 `skills-builtin/find-skills/SKILL.md` 与 `LICENSE.txt` 上零命中
    - 行为断言：专项断言「`skills-builtin/**` 内不存在 `npx skills` 任意变体」通过
    - 行为断言：SEED-04 断言通过 —— `find-skills/SKILL.md` frontmatter 含 `disable-model-invocation: true`，`description` 含 CJK 字符
    - 行为断言：`require('../ai-bash-policy').DANGEROUS_INTERPRETERS` 同时含 `'node'` 与 `'python3'`（SKILL-09 反向证据）
    - 源码断言：测试文件含 `FORBIDDEN_PATTERNS` / `INSTALL_IMPERATIVES` / `EXEMPTIONS` 三张表；`EXEMPTIONS` 的每条含豁免理由字段
    - 源码断言：`builtin-skills-seeder.js` 剥离注释后不含 `'find-skills'` / `'skill-creator'`
    - 反向验证（必须实际执行一次并记录）：临时把 `skills-builtin/find-skills/SKILL.md` 加一行 `npx skills add foo -g -y`，确认测试**变红**（证明扫描器不是空转），然后还原
  </acceptance_criteria>
  <done>P1 门禁的文本半（SEED-03 + SEED-04）具备机器可检的断言，且断言被证明不是空转（反向验证已执行）；扫描器二段式结构为 47-03 的 `check_env.mjs` 豁免预留了正确位置。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 随包 `skills-builtin/`（repo 内 git 追踪的静态文本）→ 播种模块 | 播种是**沙箱外的可信写者**：源内容随包分发、受 git 历史约束，但一旦内容含安装处方就等于把 RCE 说明书送达模型可见范围 |
| `resolveBuiltinSkillsSrc()` → `process.resourcesPath` / `__dirname` | 打包态与开发态两条路径分支；错一侧即在正式版静默失败（nodejieba 事故同型） |
| `managed-skills/`（播种写入目标）→ 技能加载器 + 模型 `read` | 模型可见的目录；写入者（播种）与消费者（加载器/模型）之间只隔着硬沙箱路径判据 |
| 播种诊断 → 启动日志 / Phase 50 设置页 | 诊断 message 会被用户与下游阶段读到 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01-01 | Elevation of Privilege | `skills-builtin/find-skills/SKILL.md` 文本 | **high** | mitigate | 零安装架构（D-01：正文里根本没有可执行的安装路径，检索走 `web_fetch` 工具层）+ 显式禁令段（D-02）+ 二段式静态扫描（Task 3 第 1 段零容忍，含 `npx skills` 专项断言）+ 反向验证证明扫描器非空转。**这是 P1 门禁本身** |
| T-47-01-02 | Tampering | `resolveBuiltinSkillsSrc()` 路径解析 | high | mitigate | 打包态一律 `path.join(process.resourcesPath, 'app.asar.unpacked', 'skills-builtin')`，开发态显式回落 `__dirname`，**不用 `__dirname` 表达打包态**（AGENTS.md 明文约定）；`package.json` 的 `asarUnpack` 与运行时路径成对，由 47-04 的 `make install-nightly` 实跑收口 |
| T-47-01-03 | Denial of Service / Tampering | 播种中断留下半成品技能 | medium | mitigate | `safeCopyDir` 的 tmp → bak → rename → rollback，tmp 与目标同父目录（同卷，避免 `EXDEV`）；`_cleanupDir(bakDst)` 自身吞错避免误报；测试断言「目标目录要么不存在、要么完整」 |
| T-47-01-04 | Tampering | 随包源目录被投毒（symlink 指向工作区外） | medium | mitigate | 遍历遇 symlink **fail-closed**：抛错 → `realm_builtin_seed_failed` → 跳过该技能。随包源是 git 追踪的纯文本，本不应含 symlink，跟随复制等于把工作区外内容注入技能目录 |
| T-47-01-05 | Information Disclosure | 播种诊断泄露环境细节 | low | mitigate | 诊断字段限定为 `level` / `code` / `message` / `path` / `skillName`；`path` 是 userData 下工作区路径（非敏感）；**不拼接任何环境变量值**；`message` 只含技能名与原始错误 |
| T-47-01-06 | Elevation of Privilege（间接） | 用户手改内置技能被静默抹掉的心理模型 | low | mitigate | D-09：覆盖前一律产 `warning` 诊断并指明正确定制通道（`skills/` 同名遮蔽）；覆盖不静默。**已知残余：**用户若不看日志，手改仍会在下次启动被覆盖 —— 这是 D-08 自愈模型的既定语义，由 47-04 写入 `docs/product/ai-skills.md` |
| T-47-01-SC | Tampering | npm / 依赖安装 | medium | mitigate | **本计划零新增依赖**（`package.json` 的 `dependencies` / `devDependencies` 原样，只改 `build.asarUnpack`）；不执行任何安装命令 → 无包合法性 gate 触发；`node_modules/` 下的既有依赖为只读契约，不写入 `files_modified` |
</threat_model>

<edge_coverage_ledger>
`SPECLESS_FALLBACK=true` / `EDGE_ABSENT=1`：本阶段无 SPEC，edge-probe 报告为**纯文本派生**（`coverage.applicable = 8`，`resolved = 0`，`unresolved = 8` —— 安装版 `edge-probe.cjs` 把全部 8 条需求判为 `category: unclassified`）。按 §A/§C 的无静默丢弃等式结清本计划负责的 **2 行：2 = 0（写入 must_haves.truths）+ 2（显式 flagged assumption）**。

| # | requirement_id | probe category | 处置 | 落点 |
|---|----------------|----------------|------|------|
| 1 | SEED-02 | unclassified | **flagged assumption** | 「幂等性断言只在**临时 root 注入**路径下验证，未覆盖打包态 `app.isPackaged` 下的真实 userData 路径 —— 打包态由 47-04 的 `make install-nightly` 人工实跑收口」 |
| 2 | SEED-04 | unclassified | **flagged assumption** | 「`disable-model-invocation: true` 的机械证据是 `buildSkillsPrompt() === ''`；这只证明『该技能不占 prompt 段』，**不**证明模型在显式调用（Phase 48 的 `/skill:name`）下能读到正文 —— 显式调用链路根本阶段无实现」 |

> 工具缺口如实记录（来自 orchestrator）：本机安装的 `edge-probe.cjs` 的 `SHAPE_CUES` 映射为空对象，其散文启发式把 8 条需求全部判为 `unclassified`、无法产出类别化探针。因此上表是**文本派生结果，不是本阶段的覆盖上限** —— 真正的验证底座是 `47-VALIDATION.md` 的逐需求断言与下面 `must_haves.truths` 的 12 条谓词。
>
> SEED-01 / SEED-03 虽也是 `unclassified`，但它们的本计划侧谓词已由 Task 1（write 与写什么）与 Task 3（零安装语义扫描）的具体断言覆盖 —— 按 §A「unclassified 一律不得自动 resolve」的纪律，其 ledger 行落在 **47-03**（那里是它们的主交付面），本计划不重复登记。
</edge_coverage_ledger>

<verification>
1. `node tests/test-builtin-skills-seeder.js` 退出 0（本计划三任务的全部自动化断言）
2. `node tests/test-agent-workspace.js` 退出 0（既有 21 例回归不红 —— 验证 `agent-workspace.js` 零 diff）
3. `node tests/test-ai-skills.js` 退出 0（既有 64 例回归不红 —— 验证技能加载链路未被本阶段的播种产物破坏）
4. 源码 gate（**分两种情况，避免空转**）：若改动尚未提交，用 `git diff -- agent-workspace.js` 断言为空；若已提交，则从 **executor 的 plan-head 台账**取基线 —— `BASE=$(cat "$(git rev-parse --git-dir)/gsd-plan-head-before-47-01")`，**先守卫空基线**（`test -n "$BASE" || { echo "plan-head 台账未解析（基线为空）—— 判据不得以空基线求值"; exit 1; }`），后 `git diff "$BASE"..HEAD -- agent-workspace.js` 断言为空。**锚点说明：** `plan_head_before` 不是 PLAN frontmatter 的字段，它由 executor 落在台账文件 `$(git rev-parse --git-dir)/gsd-plan-head-before-47-NN`（NN = 两位数补零的计划号，如本计划的 `-47-01`；现场证据：`.git/` 下 20 个既有台账全部是补零形态），并在 SUMMARY frontmatter 里回写；零 diff gate 求值时 SUMMARY 尚未写出，**台账是当时唯一可解析的来源**。裸 `git diff` 对已提交的改动不产生输出，会让这条判据假绿（本阶段硬约束：沙箱与工作区模块零改动）
5. `node --check builtin-skills-seeder.js && node --check main.js && node --check tests/test-builtin-skills-seeder.js` 全过
6. `node -e "const p=require('./package.json'); const u=p.build.asarUnpack; if(!u.includes('node_modules/nodejieba/**')||!u.includes('skills-builtin/**')) process.exit(1)"` 退出 0
7. 反向验证已执行并记录：临时插入 `npx skills add foo -g -y` 后测试变红，还原后转绿（证明扫描器非空转）
</verification>

<success_criteria>
- [ ] 首次启动后 `managed-skills/find-skills/` 出现且技能加载器零诊断识别其 name（中文 description）
- [ ] 播种幂等：重启不重复写；手改后覆盖并产 `warning` 诊断；手删后自愈重播
- [ ] 播种原子：任何失败都不留半成品目录；symlink 源 fail-closed
- [ ] 播种不阻断启动；源缺失时产 error 诊断且不 throw
- [ ] `find-skills` 全文零「执行外部安装」语义（扫描器零命中，且反向验证证明非空转）
- [ ] `find-skills` 有 `disable-model-invocation: true` 且不进 system prompt（`buildSkillsPrompt() === ''`）
- [ ] `package.json` 的 `asarUnpack` 与运行时路径解析两侧成对就位
- [ ] seeded 身份 = 随包目录名集合，代码零硬编码、零状态文件
- [ ] 既有 `tests/test-agent-workspace.js`（21 例）与 `tests/test-ai-skills.js`（64 例）保持全绿
</success_criteria>

<output>
Create `.planning/phases/47-bash/47-01-SUMMARY.md` when done
</output>
