---
phase: 50-api-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-skills-manager.js
  - ai-manager.js
  - main.js
  - src/skill-picker-model.js
  - src/settings.html
  - src/settings-page.js
  - src/styles/main.css
  - tests/test-skills-management.js
  - tests/test-skill-picker-model.js
autonomous: true
requirements: [USER-01, USER-07]
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

key-decisions:
  - '本计划是 **Phase 50 的 tracer**：先打通「设置页技能管理区读到真实技能列表」这一条端到端纵切（沙箱遍历 → manager 管理投影 → ai-manager 读路径初始化 → main.js 的 GET /api/skills/list → 设置页 guest 渲染 → CSS），再在 Wave 2/3 扩散到写路径（启停 / 卸载）、SEC-09 体积闸与完整交互面。选这条纵切为 tracer 的理由：本阶段最大的架构不确定性是「**设置页是纯 HTTP guest，而它今天连 `skill-picker-model.js` 都没加载过**」—— 这条路径一次跑通能同时证伪「HTTP 端点接错」「双模式导出在 `realm://` CSP 下不可用」「主进程投影形状不足」三类假绿'
  - '**`ensureSkillsFresh()` 的判据必须是「缓存是否已加载过」，不得按「有没有 provider」判**（D-19 / research Pitfall 15）：`ai-manager.js` 有**两条**早退（`:789-793` 无 API Key、`:893-896` 未找到模型），两条都在 `createSandboxEnv()`（`:899`）与首次 `refreshSkills()`（`:903`）**之前** ⇒ 只修第一条会漏掉第二条（配了 Key 但模型名写错 ⇒ 管理区又空白）。它不是写路径，**不并入** `syncAgentSystemPrompt()` 的写侧 2/3 份账（OQ-5）'
  - '**`getSkillsForManagement()` 与 `getSkillsForUI()` 并列共存，前者不得扩展后者**（D-13）：`getSkillsForUI()` 是 `/` 面板的**有意收窄投影**（48 明文剔除 `content` / `filePath` / `diagnostics`，防一次 IPC 送最坏 ~3 MB 正文）。本计划的投影**新增** `bytes` / `fileCount` / `statsUnavailable` / `diagnostics` 四项，仍**不带正文**。这条判断只在注释里活着就会在下个阶段被当成「消除重复」合并回去 ⇒ 必须在 `ai-skills-manager.js` 的投影函数 JSDoc 里写明理由'
  - '**尺寸统计只进重扫管线，绝不进 `computeDigest`**（D-13 / D-18 双禁令）：`refreshSkills()` 内算一次并随 `refreshedAt` 失效；`computeDigest` 的字段集**一字不改** —— 它今天逐条含 `disabled` / `overLimit` / `shadowed`，正是跨窗口失效链能工作的原因（renderer 侧 `pullAiSkillsSnapshot` 按 `snapshot.digest === state.aiSkillsDigest` 早退，`src/renderer.js:9134`）'
  - '**状态文案走 `STATUS_TEXT` 单源，设置页不新写一份**（D-12）：本计划只加第 5 条 `disabled: ''''已禁用''''`，既有 4 键的值**一个字都不许动**（`tests/test-skill-picker-model.js:1029-1041` 有逐字冻结断言，且该表**没有键数断言** ⇒ 新增第 5 键不会打翻它）。设置页的状态链是 `disabled > shadowed > overLimit > promptOmitted`，**不含 `nameClash`** —— 后者依赖本地命令表（`src/renderer.js:344-347` 的 `SLASH_COMMANDS`，浏览器脚本、guest 拿不到），是 `/` 面板独有维度（research 矛盾 3 的处置 (i)），必须写进产品文档'
  - '**设置页新增代码必须包在一对 region 标记之间**：`/* Phase 50 skill-manage region: start */` 与 `/* Phase 50 skill-manage region: end */`。理由：`src/settings-page.js` 今天**已有 46 处 `innerHTML`/`insertAdjacentHTML`**（实测），整文件级的「零 innerHTML」门禁**字面不可满足**；UI-SPEC 的注入纪律针对的是**本阶段新增的插值面**，因此该门禁必须 region-scoped（#968 的 region-scoped negative gate），并配一条**正命题**（region 内必须出现 `document.createElement(` 与 `.textContent`）以免退化成「空集真」'
  - '**设置页必须走 HTTP、主窗口必须走 IPC，两条不可互换**（D-17）：设置页是 `<webview>` guest，实测零 `realmAPI` / 零 `ipcRenderer` / 零 `require(`；主窗口 `file://` fetch 本地 HTTP 被 CORS 拦（Phase 38 事故）。本计划只落 HTTP 半边，IPC 半边在 50-03'
  - '**音量/文件数的口径以 UI-SPEC 区说明 ③ 为准**：递归合计、**含 `SKILL.md`**、**不含隐藏文件**（research A2 —— 计入 `SKILL.md` 会让用户看到 `64.1 KB` 而上限是 `64 KiB`，故文档必须写明口径以免困惑）。目录条目的 `size` 是 inode 数据**不得累加**；`symlink` **不得穿入**（内部链接环会无限递归 —— 遍历发生在每次 Agent 创建/重建，等于卡死主进程）'

must_haves:
  truths:
    - '设置页 `realm://settings` 的 AI 分区出现「技能管理」区，逐条列出技能的**名称 / 描述 / 来源 / 体积 / 文件数**五项，另有**行尾状态标注**；数据来自主进程的管理投影，前端不做任何分组 / 排序 / 档位判定。（**诊断的第 6 项在 50-01 只到「投影携带 `diagnostics`」这一层**：行内诊断徽标与详情区由 **50-04** 交付 —— 本计划的 `success_criteria` 与 `<done>` 只承诺五项 + 状态标注，验收时不得按「六项齐备」判本计划未达成）'
    - '管理投影 `getSkillsForManagement(seededNames)` **同步零 IO**，返回 `{ groups, errors, refreshedAt, digest, limits }`：`groups` 按 `user → builtin → managed` 三档分好组且**空组已剔除**，组内顺序 = `bySkillPriority` 全序的投影；`limits` 回传 `LIMITS` 的五个数值（设置页**不得写死任何限额数值**）；每组条目**不携带 `content` 与 `filePath`**'
    - '[D-19] 未配置任何 AI provider 时，设置页仍能列出随包内置技能：`ensureSkillsFresh()` 懒建 `sandboxEnv`，有 Agent 时走 `syncAgentSystemPrompt()`（内含恰一次 `refreshSkills`）、无 Agent 时直接 `refreshSkills`，**不得两条都执行**；`refreshedAt > 0` 且列表非空'
    - '[D-19] `refreshedAt === 0`（从未加载）**不得**被渲染成「无技能」—— 空态必须区分「技能列表尚未加载」与「尚无任何技能」（UI-SPEC 空态 A / B 两态）'
    - '[E1] 区说明三段静态文案允许换行（`word-break: break-word`）、不截断；窗口收窄时随 `.settings-group` 卡片折行，不横向溢出。`<code>` 内的 `manage_skill` / `/` / `SKILL.md` 是静态字面量、不含任何来自技能数据的插值'
    - '[E3] 状态区 `#skillManageState` 四态互斥：加载中 = 单行纯文本「正在加载技能…」（无标题 / 无按钮 / 无 spinner / 无骨架屏）；空态 A/B/C 各有标题 + 正文 + `.btn.btn-secondary.btn-sm` 的「重新加载」按钮；空态 C 的正文取后端 `error` 原文'
    - '[E4] 三组容器 `#skillManageGroups` 只承载组间间距；空组由主进程剔除、渲染层零判定；列表**不新建滚动容器**（滚动归既有 `.settings-content`）；`.skill-manage-row-main` **不得**声明 `flex-wrap`'
    - '[E6] 描述单行 CSS 截断 + `title` 放全文（D-02 锁定：截断用 CSS 而非 JS，避免「截断逻辑」成为第二份实现）；元信息是 `{大小} · {N} 个文件` 定长格式、`flex-shrink: 0` + `nowrap` **永不截断、永不让位**'
    - '[D-12] 行尾状态标注查 `src/skill-picker-model.js` 的 `STATUS_TEXT` 单源；本阶段新增第 5 条 `disabled: ''''已禁用''''`，既有 4 键值逐字未变；**链序与「多命中取首条」由 `SETTINGS_STATUS_CHAIN` + `pickStatusKey()` 单源决定**（`disabled` > `shadowed` > `overLimit` > `promptOmitted`，不含 `nameClash`），渲染端只消费、**不重写 if 链**；「同时命中 `disabled` + `overLimit` ⇒ 只显示『已禁用』」有独立用例'
    - '[D-13] 体积 / 文件数在 `refreshSkills()` 重扫管线内算一次并缓存（挂在缓存条目上、随 `refreshedAt` 失效），**不在**读取投影时做 IO；`computeDigest` 的字段集**一个都没改**（尤其 `disabled` / `overLimit` / `shadowed` 三条必须仍在）'
    - '[D-03] 分组与排序都在主进程完成；渲染层只 `groups.forEach`，不重排、不重判档位'
    - '[assumption:USER-01 边界] 尺寸统计的失败与截断**不静默**：单技能目录读不到 ⇒ 该技能 `bytes=0` / `fileCount=0` + 一条 `realm_` 诊断（不影响其余技能）；条目数超 `SKILL_SIZE_WALK_MAX_ENTRIES`(5000) 或深度超 `SKILL_SIZE_WALK_MAX_DEPTH`(16) ⇒ 截断 + 一条 warning 诊断，**不拒绝加载**（技能仍可用，体积显示为下限值）'
    - '[assumption:USER-01 边界] `bytes === 0 && fileCount === 0` 是「统计不可用」而非「0 字节」：投影用显式布尔 `statsUnavailable` 表达（**不得**依赖「此时必有诊断」这类隐式契约 —— 它会被后续「顺手去掉冗余诊断」打翻），前端据此渲染「统计不可用」而非失实的 `0 B · 0 个文件`'
    - '[assumption:A3 待实测] 设置页加载 `src/skill-picker-model.js` 后在 `realm://` CSP（`script-src ''''self''''`）下可用：该文件零依赖、双模式导出（`module.exports` + `window.SkillPickerModel`）、纯函数；`<script>` 必须排在 `settings-page.js` **之前**（`init()` 期即读 `window.SkillPickerModel`）'
  artifacts:
    - 'ai-skills-manager.js：`getSkillsForManagement()`（新导出）、`measureSkillDir()`（内部）、`SKILL_SIZE_WALK_MAX_ENTRIES` / `SKILL_SIZE_WALK_MAX_DEPTH`（新导出供单测）、`refreshSkills()` 内的尺寸统计挂载点（④ 定序之后、⑤ 禁用标记之前）'
    - 'ai-manager.js：`ensureSkillsFresh()`（新，读路径初始化）、`getSkillsForManagement()`（新，转发层）'
    - 'main.js：`handleSkillsApi(req, res, reqUrl)`（新，本计划只含 `list` 子路由）、`realmServer` 的 `/api/skills/` 分发分支（插在 `/api/ai-memory` 分支之前）'
    - 'src/skill-picker-model.js：`STATUS_TEXT.disabled = ''''已禁用''''`（第 5 条）；`SETTINGS_STATUS_CHAIN`（冻结数组，顺序即 D-12 链）；`pickStatusKey(skill)`（纯函数，多命中取首条）'
    - 'src/settings.html：`.settings-group.skill-manage-section` 区外壳 + `#skillManageState` / `#skillManageGroups` / `#skillManageHint` + `<script src="skill-picker-model.js">`（排在 `settings-page.js` 之前）'
    - 'src/settings-page.js：`skillsApi()`（新 HTTP 客户端）+ 技能管理区的 region 标记与只读渲染函数'
    - 'src/styles/main.css：末尾新增 `/* ===== 设置页「技能管理」区（Phase 50） ===== */` 专属段（区间 4/8/16 间距、`.skill-manage-*` 命名空间、新增令牌 `--skill-success-text` 两套主题各一份）'
    - 'tests/test-skills-management.js：**新建**独立 `node:test` 套件（本计划的用例：读路径初始化端到端 / 投影形状与分组 / 空组剔除 / 无 content / `limits` 齐备 / 源码扫描）'
  key_links:
    - '单源链：`LIMITS`（`ai-skills-manager.js:48-54`）→ 管理投影的 `limits` 字段 → 设置页渲染。设置页**不得**出现 `64 KiB` / `50` / `8000` / `1 MiB` 之类字面量（45/46 建立的「端点与前端零字面量」纪律）'
    - '单源链：`sourceTierOf()`（`:776-780`）→ 管理投影的 `tier` → 设置页查 `TIER_BADGE` 取 label/title/className。渲染端**不得**重写档位判定（48 D-14 已锁）'
    - '单源链：`src/skill-picker-model.js` 的双模式导出（`module.exports` + `window.SkillPickerModel`）→ 面板（浏览器）与设置页（guest 脚本）与纯 Node 测试三方拿到**同一份** `STATUS_TEXT` / `TIER_BADGE`'
    - '失效链（读侧）：设置页进页面 → `GET /api/skills/list` → `ensureSkillsFresh()`（无 Agent 时直接 `refreshSkills`）→ 管理投影。设置页**收不到任何主进程广播**（`windowManager.broadcast` 只发各 BrowserWindow 的 webContents，`window-manager.js:310`）⇒ 每次进入该页与每次操作后都要自行重拉'
    - '回归护栏：`node tests/test-ai-skills.js` 的 `computeDigest` 相关断言与 `tests/test-ai-skills.js:1239/:3289/:3455` 的广播次数断言 —— 本计划**不改** `computeDigest` 与 `syncAgentSystemPrompt()` 的函数体，它们必须保持绿'
  prohibitions:
    - statement: '不得把 `disabled` / `shadowed` / `overLimit` 从 `computeDigest` 里摘掉（任何「digest 只该反映 prompt 内容」的优化）'
      status: unresolved
      verification: judgment
      reason: 'digest 是**面板可见字段的超集**，这正是跨窗口失效链今天能工作的原因：`syncAgentSystemPrompt()` 的改写判据是 digest + prompt 的**合取**，`src/renderer.js:9134` 的 `pullAiSkillsSnapshot` 也按 `snapshot.digest === state.aiSkillsDigest` 早退。摘字段会让「禁用后 / 面板隐藏该技能」静默失效 —— 补播一百次也不会重渲染'
    - statement: '不得把 `bytes` / `fileCount` / `statsUnavailable` 加进 `computeDigest`'
      status: unresolved
      verification: judgment
      reason: '尺寸不影响 prompt 段。加进去会让「加一个 `references/notes.md`」触发 systemPrompt 改写 + 广播 ⇒ provider 前缀缓存 miss，是一条纯性能回归'
    - statement: '不得在设置页新写一份状态文案 / 来源徽标文案 / 限额数值'
      status: unresolved
      verification: judgment
      reason: '48 / 49 已反复付过「同值不同物」的代价（`49-UI-REVIEW` 的 `W1-04` 是同形态）。同一状态两处文案必然漂移；限额数值写死会在 `LIMITS` 变更后静默失实'
    - statement: '不得为让「管理投影」有第二个消费者而扩展 `getSkillsForUI()`，也不得为消除「重复」把两个投影合并'
      status: unresolved
      verification: judgment
      reason: '`getSkillsForUI()` 是 `/` 面板的**有意收窄投影**；合并会让 `/` 面板的 IPC 白传 100 条技能的诊断与目录遍历结果（最坏 ~3 MB 正文的防护会被拆掉）'
    - statement: '不得在尺寸遍历里直接使用 `fs` 或跟随 symlink'
      status: unresolved
      verification: judgment
      reason: '直接 `fs` 绕过沙箱的 `resolveInside` 双基准 + realpath 复核（`agent-workspace.js:162-192`），等于给「运维/统计」开一个新的越界读入口；跟随 symlink 会让内部链接环把主进程拖死（遍历发生在每次 Agent 创建/重建）'
    - statement: '不得在渲染端用 `innerHTML` / `insertAdjacentHTML` / 字符串模板拼 HTML 渲染技能数据'
      status: unresolved
      verification: judgment
      reason: '`TD-48-01` **仍开且未被修**（`escapeHtml` 只转义 `& < >`、不转义引号，用户已裁决延后）。本阶段的责任是**不扩大缺口**：新增插值一律走 DOM API（`textContent` / `el.title = …` / `setAttribute`）'
---

<objective>
打通 **Phase 50 的端到端纵切**：设置页 `realm://settings` 的 AI 分区出现「技能管理」区，逐条列出全部技能的**名称 / 描述 / 来源 / 体积 / 文件数 / 状态标注**，数据由主进程的**管理面投影**供给、经 `/api/skills/list` + token 送达。（**判据 1 的第六项「诊断」在本计划内的承载面到「管理投影携带 `diagnostics` 字段」为止**：行内诊断徽标与可展开详情区由 **50-04** 交付 —— 本计划的验收只按五项 + 状态标注判，不得按「六项齐备」判未达成。）

Purpose: 后续三个计划（写路径、SEC-09、完整交互面）都建立在这条已证伪过假绿的路径上。本计划同时交付三条**无法靠后续阶段补**的地基：① **管理读路径的初始化**（D-19）—— 没有它，判据 1 在「未配 provider / 配了 Key 但模型名写错」的用户处**整体空白**且用户无法自查；② **尺寸统计的口径**（D-13）—— 它必须住在重扫管线内且**不进 digest**，两条判断都只在注释里活着；③ **`STATUS_TEXT` 第 5 条与「仅显式」之外的设置页单源消费面**（D-12）。

Output: `ai-skills-manager.js` 的管理投影与尺寸遍历、`ai-manager.js` 的读路径初始化与转发、`main.js` 的 `handleSkillsApi` + 分发分支、设置页的区骨架与只读列表渲染、`src/skill-picker-model.js` 的第 5 条状态文案、新建独立测试套件 `tests/test-skills-management.js`。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-api-skills/50-CONTEXT.md
@.planning/phases/50-api-skills/50-RESEARCH.md
@.planning/phases/50-api-skills/50-PATTERNS.md
@.planning/phases/50-api-skills/50-UI-SPEC.md
@.planning/phases/50-api-skills/50-VALIDATION.md
@ai-skills-manager.js
@ai-manager.js
@main.js
@src/skill-picker-model.js
@src/settings.html
@src/settings-page.js
@src/styles/main.css
@tests/test-manage-skill.js
@tests/test-ai-skills.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 管理读路径端到端纵切 —— 投影 → 读路径初始化 → GET /api/skills/list → 新测试套件</name>
  <files>ai-skills-manager.js, ai-manager.js, main.js, tests/test-skills-management.js</files>
  <read_first>
    - `ai-skills-manager.js`：`:46-68`（`LIMITS` 与 `MAX_SKILL_NAME_CHARS` —— **不得改既有五项数值**，只允许加项）、`:126-138`（`getAgentWorkspaceLazy` / `EMPTY_CACHE`：`refreshedAt: 0` 的判据来源）、`:159`（模块级 `_cache` 单例）、`:334-394`（`pushEntryDiag` / `toRealmDiag` / `pushError` 三件套 —— 尺寸遍历的诊断按归属分流）、`:451-504`（`applyShadowing` / `bySkillPriority` —— D-03 的全序来源）、`:538-730`（`refreshSkills()` 全段：④ 定序在 `:648`、⑤ 禁用标记在 `:655`、`digest` 在 `:715`、`refreshedAt` 在 `:716`、整体 `catch` 回滚在 `:717-728`、`return _cache` 在 `:729`、`:752-759`（`getSkillsSnapshot` 是**浅拷贝，条目仍是活引用**）、`:776-813`（`sourceTierOf` / `toUISkillEntry` —— 管理投影的骨架，**不得重写这 8 个字段的取值逻辑**）、`:853-859`（`getSkillsForUI` —— **不得扩展**）、`:1663-1687`（`module.exports` 的分段注释风格）
    - `agent-workspace.js`：`:102-104` / `:113-115`（`getSkillsDir` / `getManagedSkillsDir` —— **路径恒由它们派生，不得手拼字符串**）、`:162-192`（`resolveInside` 双基准 + realpath 复核）、`:205-207`（`createSandboxEnv` **不读任何 provider 状态** —— D-19 可行性的依据）、`:286-290` / `:292-296` / `:309-313` / `:321-325`（`fileInfo` / `listDir` / `exists` / `remove` 四个原语；`:215` 逐字「FileSystem 契约：永不 throw，失败编码进 Result」）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/env/nodejs.js`：`:43-51`（`fileKindFromStats` 只返回 `file` / `directory` / `symlink`）、`:52-63`（`fileInfoFromStats` 的 `{name, path, kind, size, mtimeMs}`）、`:547-574`（`listDir` = `readdir withFileTypes` + 逐项 `lstat`；**内层失败是 `return err`（整目录失败，不是跳过该项）**）、`:584-591`（`exists` 对**任何** `fileInfo.ok` 都返回 `ok(true)` ⇒ 判不出 `kind === 'directory'`）
    - `ai-manager.js`：`:789-793`（**第一条早退：无 API Key**）、`:893-896`（**第二条早退：未找到模型**）、`:899`（建 `sandboxEnv`）、`:903-909`（首次 `refreshSkills`）、`:1565-1573`（`getSeededSkillNamesSafe` 的**降级为空集合**语义与 `console.warn` 留痕）、`:1583-1585`（`getSkillsForUI` 的转发形状 —— 新管理方法照此形状）、`:3026-3054`（`syncAgentSystemPrompt()` —— **本计划只读它，不碰它**；注意首行 `if (!this.agent || !this.sandboxEnv) return;`）
    - `main.js`：`:104`（`REALM_TOKEN = crypto.randomUUID()`）、`:112`（`configStore` 单例）、`:877-880`（`sendJson`）、`:887-900`（`readJsonBody` —— **本计划不改**，SEC-09 在 50-03）、`:1336-1344`（`handleSettingsApi` 的首四行范式：token → `route = pathname.replace('/api/settings/', '')` → 分支）、`:2649-2654`（`handleAiMemoryApi` 的同款首四行）、`:2698-2701`（`catch` 与 `{ error }` 形状）、`:2756-2766`（分发区，新分支插在 `/api/ai-memory` 之前）、`:4045-4051`（启动顺序：`seedBuiltinSkills()` 在 `new AIManager()` **之前**且无条件 ⇒ 内置技能早已在盘上）
    - `tests/test-manage-skill.js`：`:1-86`（**逐字照抄文件骨架**：头注释含「用法:」行、require 头、`readSource` 读取器、`withTempRoot` 夹具（**必须调 `aiSkills._resetCacheForTest()`**，否则模块级 `_cache` 跨用例污染会让「空技能集」断言假失败）、`makeEnv`）
    - `.planning/phases/50-api-skills/50-CONTEXT.md`：D-15（REST 子路由形态）、D-19（读路径初始化）、D-13（管理投影不扩展 `getSkillsForUI`）、D-03（分组与排序在主进程）
    - `.planning/phases/50-api-skills/50-RESEARCH.md`：§Pattern 2（管理面投影的形状建议与 `limits` 回传理由）、§Pattern 5（`ensureSkillsFresh` 的四条约束）
  </read_first>
  <action>
    1. **`ai-skills-manager.js` 新增管理投影**（**零 electron 依赖**保持不变；只 require Node 内建与 SDK）：
       - 新增两个常量 `SKILL_SIZE_WALK_MAX_ENTRIES = 5000` 与 `SKILL_SIZE_WALK_MAX_DEPTH = 16`（OQ-4 已裁决），写成模块常量并**一并导出**（单测要用）。
       - 新增 `async function measureSkillDir(env, dir)`：本任务**只落骨架**（返回 `{ bytes: 0, fileCount: 0, errors: [] }`），遍历规则由 Task 3 落 —— 但签名、`env` 参数、返回值三键形状必须现在定死（Task 3 只填充函数体，不改签名）。
       - 新增 `function getSkillsForManagement(seededNames)`（**同步、零 IO**），返回 `{ groups, errors, refreshedAt, digest, limits }`：
         - `groups` = 三档稳定分组：先对整个 `_cache.skills` 求 `tier = sourceTierOf(entry, seededNames)`，按 `user → builtin → managed` 的顺序产出数组，**空组直接剔除**（D-03 要求分组与空组剔除都在主进程；渲染层零判定）。
         - 每组条目 = `toUISkillEntry(entry, seededNames)` 的**全部**字段，**加上** `bytes`（`entry.bytes ?? 0`）、`fileCount`（`entry.fileCount ?? 0`）、`statsUnavailable`（`entry.bytes === 0 && entry.fileCount === 0`）、`diagnostics`（**必须 `entry.diagnostics.slice()` 深拷** —— 条目级 `diagnostics` 是 `_cache` 内的活数组，浅拷会让设置页的展开操作写回权威快照）。**仍不携带 `content` / `filePath`**。
         - `errors` = `_cache.errors` 的浅拷贝；`refreshedAt` / `digest` 直接取 `_cache` 同名键。
         - `limits` = `LIMITS` 的五个字段投影，键名用 `maxSkillMdBytes` / `maxUserSkills` / `maxManagedSkills` / `skillsPromptCharBudget` / `maxDescriptionChars`（前端零字面量）。
         - JSDoc 里必须逐字写明**为什么不扩展 `getSkillsForUI()`**（两个消费者的字段需求不同；`/` 面板 IPC 不应白传 100 条诊断与目录遍历结果）。
       - `module.exports` **另起一段注释**新增：`getSkillsForManagement`、`SKILL_SIZE_WALK_MAX_ENTRIES`、`SKILL_SIZE_WALK_MAX_DEPTH`。**不导出** `measureSkillDir`（测试经公开函数的行为面覆盖）。
    2. **`ai-manager.js` 新增读路径初始化与转发**（**不得**进入 `syncAgentSystemPrompt()` 的函数体）：
       - `async ensureSkillsFresh()`：① `if (!this.sandboxEnv) this.sandboxEnv = await getAgentWorkspaceLazy().createSandboxEnv();`；② `if (this.agent) { await this.syncAgentSystemPrompt(); return; }`；③ `await getAiSkillsManagerLazy().refreshSkills(this.sandboxEnv, { disabled: this.configStore ? this.configStore.get('settings.aiSkills.disabled', []) : [], rootDirs: [getAgentWorkspaceLazy().getManagedSkillsDir(), getAgentWorkspaceLazy().getSkillsDir()] });`。**判据是「Agent 存在与否」，不得按「有没有 provider」判**（`ai-manager.js` 有两条 provider 相关早退，按 provider 判会漏掉第二条）。**不得两条都执行**（会变成两次全量重扫）。
       - `getSkillsForManagement()`：`return getAiSkillsManagerLazy().getSkillsForManagement(this.getSeededSkillNamesSafe());`（seeded 集合**由调用方注入** —— `ai-skills-manager` 不自行解析随包目录）。
       - 两个新方法的 JSDoc 照 `getSkillsForUI()`（`:1583-1585`）与 `refreshSkillsForPanel()`（`:3072-3075`）的既有口径，并写明 `ensureSkillsFresh` **不是**写路径、**不并入** `syncAgentSystemPrompt()` 的写侧份账。
    3. **`main.js` 新增 `handleSkillsApi` 与分发分支**：
       - 新增 `async function handleSkillsApi(req, res, reqUrl)`，**首四行逐字照抄** `handleSettingsApi`（`:1338-1344`）：token 校验（`if (reqUrl.searchParams.get('token') !== REALM_TOKEN) { sendJson(res, 403, { error: 'Forbidden' }); return; }`）→ `const route = reqUrl.pathname.replace('/api/skills/', '');`；本任务只实现 `if (route === 'list' && req.method === 'GET')` 分支：先 `await aiManager.ensureSkillsFresh();` 再 `sendJson(res, 200, aiManager.getSkillsForManagement());`（**`getSkillsForManagement()` 是同步的，不要 `await` 它**），末尾 `return;`。未知路由 `sendJson(res, 404, { error: 'Not Found' })`。`catch (err)` → `console.error('[Realm] 技能 API 处理失败:', err.message); sendJson(res, 400, { error: err.message, code: err.code || undefined });`（`code` 必须回传 —— UI 按 `code` 查表、不解析 `message`）。
       - 在 `realmServer` 的分发区**插入在 `/api/ai-memory` 分支之前**：`if (reqPath.startsWith('/api/skills/')) { handleSkillsApi(req, res, reqUrl); return; }`（用 `startsWith` 而非 `===`，因为要承载三个子路由）。
    4. **新建 `tests/test-skills-management.js`**：逐字照抄 `tests/test-manage-skill.js` 的文件骨架（头注释含「用法: node tests/test-skills-management.js」、`require('node:test')` 自跑形态、`readSource`、`withTempRoot` 含 `aiSkills._resetCacheForTest()`、`makeEnv`）。本任务落的用例：
       - **端到端（本计划的 tracer 判据）**：`setWorkspaceDir` 注入临时根 → 造一个 managed 技能目录（含 `SKILL.md`）→ **不创建 Agent**、不预先 `refreshSkills` → 调 `aiManager.ensureSkillsFresh()`（用 `Object.create(aiManager.prototype)` 造一个只带 `sandboxEnv` / `configStore`（仅 `get`）/ `agent: undefined` 的 ctx）→ 断言 `aiManager.getSkillsForManagement()` 的 `refreshedAt > 0` 且 `groups` 里能查到该技能名（**这就是「无 provider 也能列出技能」的机器判据**）。
       - **投影形状**：三档分组顺序 / 空组被剔除（只造 managed 技能时 `groups.length === 1` 且 `groups[0].tier === 'managed'`）/ 组内顺序 = `bySkillPriority` 投影（造 `z-user` 与 `a-user`，断言 user 组内为 `a-user` 在前）/ 字段齐备（六字段 + 状态字段）/ **不含 `content` 与 `filePath`**（对每个条目断言 `!('content' in item) && !('filePath' in item)`）/ `limits` 五键齐备且数值等于 `LIMITS` / `refreshedAt === 0` 时 `groups` 为空数组（干净缓存场景，`_resetCacheForTest()` 后立即调投影）。
       - **源码扫描（本计划三条接线的机器判据）**：`main.js` 含 `async function handleSkillsApi(`；其函数体前 700 字符内含 `searchParams.get('token') !== REALM_TOKEN`；`main.js` 含 `reqPath.startsWith('/api/skills/')`；`ai-manager.js` 含 `async ensureSkillsFresh()` 且其函数体同时含 `await this.syncAgentSystemPrompt()` 与 `refreshSkills(`（分流两分支）。
       - **必须用真实数据形状断言**，不得手工构造被测实现从不构造的对象：技能目录用 `fs.mkdirSync` + `fs.writeFileSync` 真实落盘，再经沙箱 `env` 读。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js  &&  node --check ai-manager.js  &&  node --check main.js</automated>
    <fails_when>non-zero exit，或 stderr 出现 `SyntaxError`</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const mgr=fs.readFileSync("ai-skills-manager.js","utf8");for(const t of ["async function measureSkillDir(","function getSkillsForManagement(","SKILL_SIZE_WALK_MAX_ENTRIES","SKILL_SIZE_WALK_MAX_DEPTH"])if(!mgr.includes(t))bad.push("ai-skills-manager.js 缺 "+t);if(!/module\.exports\s*=\s*\{[\s\S]*?\bgetSkillsForManagement\b/.test(mgr))bad.push("getSkillsForManagement 未从 module.exports 导出");const am=fs.readFileSync("ai-manager.js","utf8");for(const t of ["async ensureSkillsFresh()","getSkillsForManagement("])if(!am.includes(t))bad.push("ai-manager.js 缺 "+t);const main=fs.readFileSync("main.js","utf8");if(!main.includes("async function handleSkillsApi("))bad.push("main.js 缺 handleSkillsApi");if(!main.includes("reqPath.startsWith(\x27/api/skills/\x27)"))bad.push("main.js 缺 /api/skills/ 分发分支");const i=main.indexOf("async function handleSkillsApi(");const head=main.slice(i,i+700);if(!/searchParams\.get\(\x27token\x27\)\s*!==\s*REALM_TOKEN/.test(head))bad.push("handleSkillsApi 首段缺 token 鉴权");if(bad.length){console.error("管理读路径主干不合格:\n"+bad.join("\n"));process.exit(1)}console.log("管理读路径主干 ok（投影/ensureSkillsFresh/GET list 路由/token）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `管理读路径主干 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --check` 三个文件全部通过，`exit 0`。
    - 源码扫描脚本输出 `管理读路径主干 ok（投影/ensureSkillsFresh/GET list 路由/token）`。
    - `node tests/test-skills-management.js` 输出含 `# fail 0` 且 `# pass` 非 0。
    - 端到端用例证明「**不创建 Agent** 时 `ensureSkillsFresh()` 之后投影仍能列出盘上的技能」—— 若该用例把 `ensureSkillsFresh()` 换成 `syncAgentSystemPrompt()` 则必然失败（这是它的可失败性）。
    - `ai-skills-manager.js` 内 `require(` 的清单未新增 `electron`；`ai-manager.js` 的 `syncAgentSystemPrompt()` 函数体**逐字未改**。
    - `getSkillsForManagement()` 返回的条目对 `content` / `filePath` 两个键的 `in` 断言为 `false`。
  </acceptance_criteria>
  <done>设置页所需的全部服务端件就位：管理投影可从缓存同步产出三档分组数据，读路径初始化在无 Agent 时也能重扫，`GET /api/skills/ list` 带 token 返回投影，新建套件的 tracer 用例绿。</done>
</task>

<task type="auto">
  <name>Task 2: 设置页「技能管理」区骨架 + 只读分组列表渲染 + `STATUS_TEXT` 第 5 条单源</name>
  <files>src/skill-picker-model.js, src/settings.html, src/settings-page.js, src/styles/main.css, tests/test-skill-picker-model.js</files>
  <read_first>
    - `src/settings.html`：`:484-505`（AI 记忆子区）、`:506-520`（**AI Bash 白名单区 —— `.settings-group` 范式与 `setting-description` 先例**）、`:522-531`（视觉模型区）、`:532`（AI 分区 `</section>` —— 新区插在它之前）、`:783-785`（脚本区：`ai-brand-map.js` / `model-family.js` / `settings-page.js`；**当前无 `skill-picker-model.js`**）
    - `src/settings-page.js`：`:41`（`state.aiBashWhitelist` 式的模块级 state）、`:49`（`const apiToken = pageParams.get('token') || '';`）、`:85-95`（页面切换）、`:172-186`（`aiMemoryApi` —— **`skillsApi()` 逐字照抄的 HTTP 客户端范式**：token 进 query、非 2xx 时优先取后端 `error` 文案）、`:3878-3900`（`setAiMemoryHint` / `resetAiMemoryHint` 的色调类切换形状）、`:4787-4815`（`init()` —— 新区的初始化并列加一行）、`:4846`（`document.addEventListener('DOMContentLoaded', init);`）
    - `src/skill-picker-model.js`：`:236-244`（`STATUS_TEXT` —— **本阶段加第 5 条的位置**，注意今天已有 **4 键且含 `nameClash`**）、`:345-365`（`TIER_BADGE` —— 徽标查表的唯一权威，**只查表、不扩表**）、`:452-478`（`api` 对象与双模式导出 —— **不得破坏** `module.exports` + `window.SkillPickerModel` 两条）
    - `src/styles/main.css`：`:7-34`（暗色主题令牌块）、`:36-64`（浅色主题块）、`:3195-3215`（`.settings-group` / `.settings-group-title` / `.settings-item` —— 区外壳与行距的既有值）、`:5827-5891`（`.ai-skill-content-box` 家族 —— 诊断区与汇总条的**唯一**折叠视觉；⚠️ 其 JS 侧 `renderSkillContentBox` 在 `src/renderer.js`、**无 `module.exports`**、跨文件不可调用）、`:7236-7279`（`.slash-picker-source-badge*` 与 `.slash-picker-tag-explicit`）、`:5350-5358`（`.ai-modal-overlay` 的 `display: none` 初始隐藏范式）、`:906-936` / `:1349-1353` / `:5638-5641`（`.btn` / `.btn-secondary` / `.btn-sm` 的**生效**取值：`font-size: 12px` / `padding: 4px 8px` / `height: 28px`）
    - `.planning/phases/50-api-skills/50-UI-SPEC.md`：`## 区结构（骨架）`、`## 行结构（li.skill-manage-row）`、`## 长文本与溢出纪律`、`## 状态机`、`## 新增 CSS 声明清单`（**整段 CSS 已定稿于 `:826-1045`，直接照抄，不要自行设计**）、`## Typography`、`## Color` 的硬禁令、`### 硬前置条件`（「仅显式」单源化 — **本计划不做，归 50-04**）
    - `.planning/phases/50-api-skills/50-PATTERNS.md`：`### src/settings.html` 与 `### src/settings-page.js` 两节（含**不可用的既有工具**对照表：`showToast` 不用、`escapeHtml` 不用、`renderSkillContentBox` 不可调）
  </read_first>
  <action>
    1. **`src/skill-picker-model.js`：`STATUS_TEXT` 第 5 条 + 设置页状态链的单源函数**：
       - 在 `Object.freeze({...})` 内新增 `disabled: '已禁用',`。**既有 4 键（`shadowed` / `nameClash` / `promptOmitted` / `overLimit`）的值一个字都不许动**。不得新增键数断言、不得动 `MANAGE_SKILL_SHORT_REASON`（那是**另一张表**，须保持恰 9 键）。JSDoc 的「四条定长文案」改为「五条」，并补一句：新增的 `disabled` 其**消费方只有设置页**（`/` 面板根本不列出被禁用技能 —— 48 D-10）—— 这是同一张表的**不同消费者**，不是第二份文案。
       - **新增设置页状态链的单源**（D-12 的优先级链必须能被判据决定，**不能只活在渲染代码的 if 顺序里** —— 否则「同时命中多条的取首条」这条锁定行为无任何门禁/用例能证伪）：`const SETTINGS_STATUS_CHAIN = Object.freeze(['disabled', 'shadowed', 'overLimit', 'promptOmitted']);`（**顺序即 D-12 锁定的链，不得重排**；**不含 `nameClash`**）+ 纯函数 `function pickStatusKey(skill)`：按链顺序返回**首个**命中的键（判据依次为 `skill.disabled === true` / `skill.shadowed === true` / `skill.overLimit === true` / `skill.promptOmitted === true`），一个都不命中返回 `null`。两者**一并挂进 `api` 对象**（双模式导出面不变）。JSDoc 写明三点：① 它是设置页状态标注的**唯一**判据，渲染端**不得**重写 if 链（46 D-06 的「渲染端不重实现判定」）；② 面板链（`buildPickerItems` 内的 `shadowed > nameClash > promptOmitted > overLimit`，`:296-310`）**保持原样不动**，两条链服务不同维度、**不得**合并（面板链依赖本地命令表 `SLASH_COMMANDS`，`realm://` guest 拿不到）；③ `disabled` 是设置页链新增的唯一维度。
    2. **`src/settings.html` 新增区外壳**：插在 AI 分区最后一个子区（`ai-vision-section` 的 `</div>` 之后、AI 分区 `</section>` 之前），类名与 id **严格按 UI-SPEC 的区结构骨架**：`div.settings-group.skill-manage-section` > `h2.settings-group-title`（文案 `技能管理`）+ 三段 `p.setting-description`（区说明 ①②③ 逐字取 UI-SPEC Copywriting 表 `#3` / `#4` / `#5`，其中 `manage_skill` / `/` / `SKILL.md` 用 `<code>` 静态字面量包裹）+ `div#skillManageSummary`（本计划**不渲染**，占位可省 —— 汇总条归 50-04）+ `div#skillManageState.skill-manage-state` + `div#skillManageGroups.skill-manage-groups` + `p#skillManageHint.skill-manage-hint`。⚠️ **区外壳不带 `settings-group-header` / `settings-group-content` 两层包装**（UI-SPEC 明文以它为准）。⚠️ **绝不在 markup 里写内联 `style="display:none"`**（`realm://` CSP `style-src 'self'` 会拦）—— 初始隐藏一律走 CSS 类。
       同时在脚本区 `<script src="settings-page.js"></script>` **之前**插入 `<script src="skill-picker-model.js"></script>`（顺序是硬约束：`init()` 期即读 `window.SkillPickerModel`）。
    3. **`src/styles/main.css` 末尾追加专属段**：段注释 `/* ===== 设置页「技能管理」区（Phase 50） ===== */`（**必须追加在文件末尾** —— `.skill-manage-danger-btn` 一类与 `.btn-secondary` 同特异性、靠后出现才能覆盖）。本计划只落**只读渲染所需**的规则：区间距与作用域锚注释、`.skill-manage-group-title`、`.skill-manage-group + .skill-manage-group`、`.ai-skill-content-box.skill-manage-summary + .skill-manage-groups`、`.skill-manage-list`、`.skill-manage-row` 与行分隔线、`.skill-manage-row-main`（含**不得声明 `flex-wrap`** 的注释）、`.skill-manage-name`（等宽 13px/600 + 单行截断）、`.skill-manage-tail`（`margin-left: auto` + `flex-shrink: 0`）、`.skill-manage-status`、`.skill-manage-row-sub`、`.skill-manage-desc`、`.skill-manage-meta`、`.skill-manage-state` 家族、`.skill-manage-hint` 家族。新增令牌 `--skill-success-text`（暗 `#6EE7B7` / 亮 `#065F46`）**必须写进两套主题块**（缺一即在对应主题下失效）。⚠️ **硬禁令**：`.skill-manage-row` **不得**声明 `:hover` / `.active` 的 `background: var(--bg-hover)`（会把描述 / 元信息 / 中性状态标注同时拖到 4.5:1 以下）。凡是 UI-SPEC 已定稿的取值一律照抄，不自行设计。
    4. **`src/settings-page.js` 新增 region 与只读渲染**（全部代码包在 `/* Phase 50 skill-manage region: start */` 与 `/* Phase 50 skill-manage region: end */` **之间**）：
       - `skillsApi(route, options, query)`：逐字照抄 `aiMemoryApi` 的形状（`token` 进 `URLSearchParams`、非 2xx 时先 `res.json()` 取 `data.error` 作为详情、抛 `new Error(detail || ...)`），路径前缀为 `/api/skills`。失败时**同时保留 `data.code`**（UI 按 `code` 查表，不解析 `message`）。
       - `formatSkillSize(bytes)`：沿既有「逐页局部实现」惯例（`toFixed(1)` + `B/KB/MB/GB`，`0 → '0 B'`）。**不得**在设置页写死任何限额数值。
       - `renderSkillManagement(projection)`：**零 HTML 字符串模板** —— 全树用 `document.createElement` + `textContent` + `el.title = …` / `setAttribute` 构建。渲染内容：空态三态（按 `refreshedAt` 与总条数分流，文案逐字取 UI-SPEC Copywriting `#17`/`#18`/`#19`/`#20`）→ 三组（`groups.forEach`，**不重排、不重判**）→ 每组 `h3.skill-manage-group-title`（组名查固定映射 `user→我的技能` / `builtin→内置技能` / `managed→AI 创建`）+ `ul.skill-manage-list` → 每行 `li.skill-manage-row[data-skill-name][data-skill-tier]`：第一行 = `span.skill-manage-name`（`title` 放全文）+ 来源徽标（**查 `window.SkillPickerModel.TIER_BADGE`** 取 label/className/title，表外 tier **跳过徽标**、不得产出 `undefined` 字面量）+ 条件「仅显式」标记 + `div.skill-manage-tail`（本计划**只放**行尾状态标注 `span.skill-manage-status`，诊断徽标 / 开关 / 卸载按钮归 50-04）；第二行 = `span.skill-manage-desc`（`title` 放全文）+ `span.skill-manage-meta`（`{大小} · {N} 个文件`，`statsUnavailable === true` 时渲染「统计不可用」而非 `0 B · 0 个文件`）。状态标注取 `window.SkillPickerModel.pickStatusKey(item)` 的返回值（**单源；不得在本页重写状态链的 if 顺序**），非 `null` 时文案查 `window.SkillPickerModel.STATUS_TEXT[key]`；命中 `overLimit` / `promptOmitted`（**按 `pickStatusKey` 返回的键字符串判定** —— 写 `key === 'overLimit' || key === 'promptOmitted'` 这类字符串比较；**不得**直接读 `item.overLimit` / `item.promptOmitted` 字段，那等于在本页重写这两个维度的判定）时加 `.skill-manage-status-limit` 类。
       - `setupSkillManageListeners()`：绑定空态 / 失败态的「重新加载」按钮 → `loadSkillManagement()`；在 `init()` 里并列加一行调用；**进入页面即拉一次**（设置页收不到任何主进程广播）。`loadSkillManagement()` 内部对 `skillsApi('list')` 的失败走**空态 C**（标题 + 后端 `error` 原文）。
       - **不得**调用 `showToast`（D-06 把本区的反馈面锁定为 inline hint；本计划的 inline hint 只有结构、交互归 50-04）。
    5. **`tests/test-skill-picker-model.js` 补 `STATUS_TEXT.disabled` 值断言 + 状态链的三条断言**：
       - 新增一条用例断言 `model.STATUS_TEXT.disabled === '已禁用'`，并断言既有 4 键值仍逐字未变（复用 `:1029-1041` 的既有断言，不新写计数断言）。
       - **链序断言**：`model.SETTINGS_STATUS_CHAIN` 与 `['disabled', 'shadowed', 'overLimit', 'promptOmitted']` **逐字相等**（**链序即契约**；把 `disabled` 挪到链尾或把 `overLimit`/`promptOmitted` 调序必须转红）。
       - **同时命中用例（RESEARCH `:1387` 的硬要求 —— 「测试要覆盖『同时命中 `disabled` + `overLimit`』的行尾只显示『已禁用』」）**：构造 `{ disabled: true, overLimit: true }` ⇒ `pickStatusKey` 返回 `'disabled'`，且据此查表得到的文案恰为 `已禁用`；再加一条 `{ shadowed: true, disabled: true }` ⇒ `'disabled'`（用户主动意图优先）。
       - **值域断言**：链上每个键都存在于 `STATUS_TEXT`（防链与表漂移）；且 `nameClash` **不在** `SETTINGS_STATUS_CHAIN` 上（它是 `/` 面板独有维度）。
       - **不得**改 `MANAGE_SKILL_SHORT_REASON` 的 9 键断言（那是另一张表）。**不得**改 `:1262-1279` 的「仅显式」三条断言（单源提升归 50-04）。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const bad=[];const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const html=fs.readFileSync("src/settings.html","utf8");if(!/class="settings-group skill-manage-section"/.test(html))bad.push("settings.html 缺区外壳 .settings-group.skill-manage-section");if(!/id="skillManageGroups"/.test(html))bad.push("settings.html 缺 #skillManageGroups");if(!/id="skillManageState"/.test(html))bad.push("settings.html 缺 #skillManageState");if(!/id="skillManageHint"/.test(html))bad.push("settings.html 缺 #skillManageHint");if(!html.includes("体积与文件数为该技能目录的递归合计（含"))bad.push("区说明 ③ 与 UI-SPEC Copywriting #5 不逐字一致");if(!html.includes("<code>SKILL.md</code>"))bad.push("区说明 ③ 的 SKILL.md 未用 <code> 包裹");if(!html.includes("内置技能与 AI 创建的技能不可在此卸载（AI 创建的技能请让 AI 用 "))bad.push("区说明 ② 与 UI-SPEC Copywriting #4 不逐字一致");const si=html.indexOf("src=\"skill-picker-model.js\""),sj=html.indexOf("src=\"settings-page.js\"");if(si&lt;0)bad.push("settings.html 未引用 skill-picker-model.js");else if(sj&gt;=0 && si&gt;sj)bad.push("skill-picker-model.js 必须排在 settings-page.js 之前");const secIdx=html.indexOf("skill-manage-section");const secTail=(secIdx&lt;0?"":html.slice(secIdx,html.indexOf("&lt;/section&gt;",secIdx))).replace(/&lt;!--[\s\S]*?--&gt;/g,"");if(/style="[^"]*display\s*:\s*none/.test(secTail))bad.push("技能管理区出现 markup 内联 display:none（realm:// CSP 会拦）；判据已剥除 HTML 注释 —— 注释里如实写明「不用内联 display:none」不构成违规");const sp=fs.readFileSync("src/settings-page.js","utf8");if(!/function skillsApi\(/.test(sp))bad.push("settings-page.js 缺 skillsApi");if(!/window\.SkillPickerModel\./.test(sp))bad.push("settings-page.js 未消费 window.SkillPickerModel 单源");const START="/* Phase 50 skill-manage region: start */";const END="/* Phase 50 skill-manage region: end */";const a=sp.indexOf(START),b=sp.indexOf(END);if(a&lt;0||b&lt;0||b&lt;=a){bad.push("settings-page.js 缺技能管理区 region 标记（起点/终点必须成对且有序）")}else{const region=stripC(sp.slice(a,b+END.length));if(/\binnerHTML\b|insertAdjacentHTML/.test(region))bad.push("技能管理区 region 内出现 innerHTML/insertAdjacentHTML（注入纪律，TD-48-01 不扩大缺口）；判据已剥注释 —— 注释里如实写明「本区零 innerHTML」不构成违规");if(!/document\.createElement\(/.test(region))bad.push("技能管理区 region 未用 DOM 构建（注入纪律无正命题 ⇒ 空集真）");if(!/textContent/.test(region))bad.push("技能管理区 region 内未见 textContent");if(!/pickStatusKey\(/.test(region))bad.push("设置页未消费 pickStatusKey（D-12 状态链单源，不得在本页重写）");if(/\.\s*(overLimit|promptOmitted)\b/.test(region))bad.push("设置页直接读了三链维度之外的 overLimit / promptOmitted 字段（D-12：这两个维度的判定只能取自 pickStatusKey 的返回值，本页不得直接读该字段）；判据已剥注释");for(const c of ["技能列表尚未加载","尚无任何技能","统计不可用"])if(!region.includes(c))bad.push("设置页缺 UI-SPEC 文案 "+c)}const modelC=stripC(fs.readFileSync("src/skill-picker-model.js","utf8"));if(!/disabled:\s*\x27已禁用\x27/.test(modelC))bad.push("STATUS_TEXT 缺 disabled: \x27已禁用\x27");if(!/function pickStatusKey\(/.test(modelC))bad.push("skill-picker-model.js 缺 pickStatusKey（D-12 状态链单源）");if(!/SETTINGS_STATUS_CHAIN\s*=\s*Object\.freeze\(\s*\[[\s\S]{0,300}?[\x27"]disabled[\x27"][\s\S]{0,120}?[\x27"]shadowed[\x27"][\s\S]{0,120}?[\x27"]overLimit[\x27"][\s\S]{0,120}?[\x27"]promptOmitted[\x27"]/.test(modelC))bad.push("SETTINGS_STATUS_CHAIN 缺失或链序不是 D-12 锁定的 disabled>shadowed>overLimit>promptOmitted（判据只钉链序与冻结形态，容忍换行 / 注释 / 引号风格）");if(!/module\.exports\s*=\s*api/.test(modelC))bad.push("skill-picker-model.js 双模式导出被破坏");if(/MANAGE_SKILL_SHORT_REASON\s*=\s*Object\.freeze\(\{[\s\S]*?not_user_owned/.test(modelC))bad.push("MANAGE_SKILL_SHORT_REASON 混入 not_user_owned（该表须保持恰 9 键）");if(bad.length){console.error("设置页技能管理区接线不合格:\n"+bad.join("\n"));process.exit(1)}console.log("设置页技能管理区接线 ok（区外壳/脚本序/单源消费/region 无 innerHTML 且有 DOM 正命题）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `设置页技能管理区接线 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node --test tests/test-skill-picker-model.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node -e 'const css=require("fs").readFileSync("src/styles/main.css","utf8");const bad=[];const n=(css.match(/--skill-success-text\s*:/g)||[]).length;if(n!==2)bad.push("--skill-success-text 定义 "+n+" 处（须两处主题块各一）");if(!/--skill-success-text:\s*#6EE7B7/.test(css))bad.push("暗色 --skill-success-text 不是 #6EE7B7");if(!/--skill-success-text:\s*#065F46/.test(css))bad.push("浅色 --skill-success-text 不是 #065F46");const i=css.indexOf("/* ===== 设置页「技能管理」区（Phase 50） ===== */");const cssC=css.replace(/\/\*[\s\S]*?\*\//g,"");if(i&lt;0)bad.push("缺 Phase 50 专属段注释");else if(css.indexOf(".skill-manage-row {")&lt;i)bad.push("专属段不在文件末尾（源序依赖会静默失效）");const rowI=cssC.indexOf("\n.skill-manage-row {");if(rowI&lt;0)bad.push("专属段缺 .skill-manage-row {（Color 硬禁令无判据对象 ⇒ 空集真）");else{const row=cssC.slice(rowI,cssC.indexOf("}",rowI));if(/background\s*:\s*var\(--bg-hover\)/.test(row))bad.push(".skill-manage-row 声明了 --bg-hover 底（Color 硬禁令；判据已剥除 CSS 注释 —— 规则块里如实写明「不得声明 background: var(--bg-hover)」不构成违规）")}const mainI=cssC.indexOf("\n.skill-manage-row-main {");if(mainI&lt;0)bad.push("专属段缺 .skill-manage-row-main {（单行不变式无判据对象 ⇒ 空集真）");else{const main=cssC.slice(mainI,cssC.indexOf("}",mainI));if(/flex-wrap/.test(main))bad.push(".skill-manage-row-main 声明了 flex-wrap（破坏单行不变式；判据已剥除 CSS 注释 —— 本任务要求写进该规则块的「不得声明 flex-wrap」注释不构成违规）")}if(bad.length){console.error("样式段不合格:\n"+bad.join("\n"));process.exit(1)}console.log("样式段 ok（双主题令牌 + 末尾专属段 + 两条硬禁令）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `样式段 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 接线扫描输出 `设置页技能管理区接线 ok（区外壳/脚本序/单源消费/region 无 innerHTML 且有 DOM 正命题）`。
    - **可失败性（本门禁只做过负方向验证，未做 stub 正方向与单点变异）**：本门禁在落地前对**当前树**实跑过，失败项全部是「新代码尚未落地」类（缺区外壳 / 缺 `pickStatusKey` / 缺 DOM 正命题等），**无**「合规实现也修不掉」的断言；`document.createElement(` / `textContent` 的正命题与 region 负向扫描都指向**本阶段新增的 region**，不会命中既有代码。执行者落地后**必须自行**做下面两次单点变异并确认转红（这是它可失败性的来源）：把 region 内任一 `textContent` 改成 `innerHTML` ⇒ 报「region 内出现 innerHTML」；去掉 `document.createElement(` ⇒ 报「未用 DOM 构建」。
    - **本轮修订新增的证据（针对「注释被误判为违规」这一误报面，已用合成三件套实跑）**：① region 内保留一条如实写明「本区零 `innerHTML` / `insertAdjacentHTML`，全部用 `createElement` + `textContent`」的注释 ⇒ **不**转红；把某个 `textContent` 真改成 `innerHTML` ⇒ 转红（已实测）。② `.skill-manage-section` 段内保留一条 HTML 注释提及 `style="display:none"` ⇒ **不**转红（判据先剥 `&lt;!-- --&gt;`）；markup 里真写内联 `style="display:none"` ⇒ 转红（已实测）。③ 状态维度：region 里 `item.overLimit` / `item.promptOmitted` 的**字段直读** ⇒ 转红（已实测）；改用 `pickStatusKey(item)` 的返回值判定（含 `key === 'overLimit'` 这类**字符串比较**）⇒ 不转红（已实测）。④ `SETTINGS_STATUS_CHAIN` 判据**只钉链序与 `Object.freeze([` 形态，容忍换行 / 注释 / 单双引号**：写成多行 + 双引号 ⇒ 通过；把 `overLimit` / `promptOmitted` 调序、把 `disabled` 挪到链尾、或去掉 `Object.freeze(` ⇒ 三者均转红（已实测）。
    - 样式扫描输出 `样式段 ok（双主题令牌 + 末尾专属段 + 两条硬禁令）`；**验证状态**：两条硬禁令（`.skill-manage-row` 无 `--bg-hover` 底 / `.skill-manage-row-main` 无 `flex-wrap`）的判据**已做过等价的合成正/反验证** —— 合规段（`display: flex`）无输出；加 `background: var(--bg-hover)` ⇒ 报「声明了 --bg-hover 底」；**且选择器缺失时不再静默通过**（改为报「专属段缺 …（无判据对象 ⇒ 空集真）」；修复前的 `css.slice(indexOf(...), indexOf(...)+N)` 在找不到选择器时是对**空串**做断言 ⇒ **恒绿**，是一条假门禁）。**本轮修订**把规则块的判据改为**先剥 CSS 注释再判**，并新增实跑证据：在该规则块内如实写明「不得在这里加 `background: var(--bg-hover)`」与「不得声明 `flex-wrap`」的**注释**（本任务 action 明确要求写后者）⇒ **不**转红；两处真写声明 ⇒ 各自转红。令牌与专属段两条**未做**单点变异 —— 执行者落地后自行变异：把 `--skill-success-text` 从任一主题块删掉 ⇒ 报「定义 0 处」。
    - `node --test tests/test-skill-picker-model.js` 的 `# fail` 为 0，且新增的 `STATUS_TEXT.disabled` 断言在其中。
    - `src/settings.html` 中 `skill-picker-model.js` 的 `<script>` 行号**小于** `settings-page.js` 的。
    - 区外壳与区说明逐字匹配 UI-SPEC 的区结构骨架与 Copywriting `#1`/`#3`/`#4`/`#5`；区外壳**没有** `settings-group-header` / `settings-group-content` 包装。
  </acceptance_criteria>
  <done>设置页 AI 分区出现「技能管理」区，进入页面即按三档分组渲染出全部技能的名称 / 描述 / 来源 / 体积 / 文件数 / 状态标注，空态与失败态可区分，`STATUS_TEXT` 第 5 条成为跨进程单源。</done>
</task>

<task type="auto">
  <name>Task 3: 体积 / 文件数递归统计进重扫管线（口径 + 双上限 + 不静默）</name>
  <files>ai-skills-manager.js, tests/test-skills-management.js</files>
  <read_first>
    - `ai-skills-manager.js`：`:538-730`（`refreshSkills()` —— 挂载点在 ④ 定序 `:648` **之后**、⑤ 禁用标记 `:655` **之前**；注意 ③ 遮蔽与 ④ 定序的注释占位）、`:334-351`（`pushEntryDiag` —— 条目级诊断的唯一落点）、`:189-202`（`computeDigest` —— **本任务只读它、绝不改它**）、`:717-728`（整体 `catch` 回滚 —— 尺寸统计的失败**不得**走这条路径）
    - `agent-workspace.js`：`:292-296`（`listDir` 的沙箱包装）、`:215`（「FileSystem 契约：永不 throw，失败编码进 Result」）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/env/nodejs.js`：`:547-574`（`listDir` 的内层失败是 `return err` ⇒ **整目录失败**，实现必须把「目录不可读」当正常分支处理并产诊断，不得让它冒泡）、`:52-63`（`size` 字段语义；目录条目的 `size` 来自 `lstat().size`、是 inode 数据）
    - `.planning/phases/50-api-skills/50-RESEARCH.md`：§Pattern 1 的「遍历规则表」与「本会话新发现的 SDK 细节」三条、§Pitfall 4（symlink 环）、§Pitfall 5（目录 `size` 与隐藏文件）、§Pitfall 6（`refreshSkills` 的返回值是活引用 —— 测试必须取**值副本**）
    - `.planning/phases/50-api-skills/50-CONTEXT.md`：D-13 的「体积 / 文件数口径」与「计算时机」两条、OQ-4（双上限与截断语义）
  </read_first>
  <action>
    1. 填充 `measureSkillDir(env, dir)` 的遍历规则（签名与返回三键在 Task 1 已定死，**不得改形**）：
       - 起点 = 传入的 `dir`（调用方按 `path.dirname(entry.skill.filePath)` 派生 —— 目录名才是权威，SDK `Skill` 无位置字段）。内部递归用局部 `walk(p, depth)`，深度从 1 起；**`walk(p, depth)` 内 `await env.listDir(p)`，对子目录条目调 `walk(path.join(p, e.name), depth + 1)`**（子路径**必须**由 `path.join` 拼 —— 门禁的判据之一；不得字符串相加）。
       - **每条规则都必须显式落地**：`e.kind === 'symlink'` ⇒ **`continue`（不递归、不计字节、不计文件数）**；`e.name.startsWith('.')` ⇒ **`continue`**（`.DS_Store` 等不是技能内容，且在 macOS 下会因 Finder 浏览随机增减）；`e.kind === 'directory'` ⇒ 递归、**不累加 `size`**（目录的 `size` 是 inode 数据不是内容）；`e.kind === 'file'` ⇒ `bytes += Number.isFinite(e.size) ? e.size : 0` 且 `fileCount += 1`。
       - 一切 `res.ok !== true` 按「读不到」处理：产一条 **entry 级** warning 诊断（`code: 'realm_skill_dir_unreadable'`，`message` 含沙箱码与路径），返回、**不 throw**（沙箱 FileSystem 契约：永不 throw）。
       - 深度超 `SKILL_SIZE_WALK_MAX_DEPTH` ⇒ 置截断标记并返回（不递归更深）；条目累计超 `SKILL_SIZE_WALK_MAX_ENTRIES` ⇒ 置截断标记并停止遍历。两种截断**各产一条 warning 诊断**（`realm_skill_dir_too_deep` / `realm_skill_dir_too_many_entries`，`message` 须写明「统计已截断」（显示为下限值）」），**都不拒绝加载** —— 技能仍可用。
       - 所有诊断的 `path` 字段用沙箱内路径原文（设置页**不上屏** `path`，见 UI-SPEC）。
       - **不得**直接 `require('fs')` 或用 `fs.readdir/stat/lstat`（绕过 `resolveInside` 的 realpath 复核 = 新增越界读入口）。
    2. 在 `refreshSkills()` 的 ④ 定序之后、⑤ 禁用标记之前插入逐技能统计：`for (const entry of _cache.skills) { const dir = path.dirname(entry.skill.filePath); const m = await measureSkillDir(env, dir); entry.bytes = m.bytes; entry.fileCount = m.fileCount; for (const d of m.errors) pushEntryDiag(entry, d); }`。逐技能隔离失败 —— **不得**让任何异常冒泡到 `:717-728` 的整体 catch 回滚（那会把一次 stat 失败放大成技能集消失）。注释里写明「**尺寸不进 `computeDigest`**：它不影响 prompt 段；加了会让加一个 `references/notes.md` 就触发 systemPrompt 改写 + 广播 ⇒ provider 前缀缓存 miss」。
    3. `computeDigest()` **一个字符都不改**（尤其是 `e.disabled === true` / `e.overLimit === true` / `e.shadowed === true` 三行必须仍在 —— 这是跨窗口失效链的承重字段）。
    4. 在 `tests/test-skills-management.js` 补尺寸口径用例（用真实 `fs` 在临时技能目录内造场景，再经沙箱 `env` 走一遍 `refreshSkills`）：
       - **递归含子目录**：`SKILL.md` + `scripts/a.js` + `references/deep/b.md` ⇒ `fileCount === 4`、`bytes` = 四个文件真实字节和（用 `fs.statSync(...).size` 求和作为期望值，**不是**手写常量）。
       - **`SKILL.md` 自身计入**：单文件技能 ⇒ `fileCount === 1` 且 `bytes === fs.statSync(SKILL.md).size`。
       - **隐藏文件不计**：加一个 `.DS_Store`（任意字节）⇒ `bytes` 与 `fileCount` **不变**。
       - **目录 `size` 不计**：断言 `bytes` 严格小于「把所有条目的 `size` 无条件求和」的值（或直接断言与只累加 `kind === 'file'` 的期望值相等）。
       - **不穿 symlink**：造 `references/self → references`（内部环）+ 一个指向目录外文件的 symlink ⇒ 遍历**必须在有限步内返回**（用例本身有超时保护），且 `fileCount` 不含 symlink、`bytes` 不含链接长度。
       - **不进 digest**：先 `refreshSkills` 一次取**值副本**（`JSON.parse(JSON.stringify(getSkillsSnapshot()))` 或直接读 `digest` 字符串），再往技能目录加一个普通文件、**不改 `SKILL.md`**，二次 `refreshSkills` ⇒ 断言 `digest` 与首轮**逐字相等**而 `bytes` 变大。⚠️ **必须取值副本**：`refreshSkills` 返回模块级 `_cache`（活引用），`getSkillsSnapshot()` 只做浅拷贝（数组新、条目仍是同一批引用）—— 直接比较会让该用例**恒真**（假绿）。
       - **统计不可用不静默**：造一个不可读的技能子目录（`fs.chmodSync(dir, 0o000)`，用例结束前 `chmod` 复原）⇒ 该技能条目 `statsUnavailable === true` 且 `diagnostics` 非空，**其余技能仍照常统计**。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const bad=[];const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const src=fs.readFileSync("ai-skills-manager.js","utf8");const i=src.indexOf("async function measureSkillDir(");if(i&lt;0){bad.push("缺 async function measureSkillDir(")}else{const j=src.indexOf("\n}\n",i);const b=stripC(src.slice(i,j&lt;0?i+4000:j));if(!b.includes("env.listDir("))bad.push("尺寸遍历未走沙箱 env.listDir（不得直接 fs）");if(/\brequire\(\x27fs\x27\)|fs\.(readdir|stat|lstat|readFile)/.test(b))bad.push("尺寸遍历直接用了 fs（绕过沙箱 resolveInside）；判据已剥注释 —— 注释里如实写明「不得直接 fs」不构成违规");if(!/e\.kind === \x27symlink\x27[\s\S]{0,30}continue/.test(b))bad.push("未显式跳过 symlink（内部链接环会无限递归）");if(!/startsWith\(\x27\.\x27\)/.test(b))bad.push("未过滤隐藏文件（.DS_Store 会随机改变体积）");if(!/e\.kind === \x27file\x27/.test(b))bad.push("未按 kind === file 累加字节（目录 size 会被误计）");if(!/SKILL_SIZE_WALK_MAX_ENTRIES/.test(b))bad.push("缺 entries 上限（深目录会拖死主进程）");if(!/SKILL_SIZE_WALK_MAX_DEPTH/.test(b))bad.push("缺 depth 上限");if(!/pushEntryDiag|errors\.push/.test(b))bad.push("失败路径未产诊断（禁止静默失败）");const dirIdx=b.indexOf("e.kind === \x27directory\x27");const dirLine=dirIdx&lt;0?"":b.slice(dirIdx).split("\n")[0];if(dirIdx&lt;0)bad.push("未按 kind === directory 分流递归");else if(/bytes \+=/.test(dirLine))bad.push("目录分支累加了 size（目录 size 是 inode 数据）");if(!/path\.join\(/.test(b))bad.push("子目录递归未用 path.join 拼子路径（口径要求含 scripts/ 与 references/）")}if(!/SKILL_SIZE_WALK_MAX_ENTRIES = 5000/.test(src))bad.push("SKILL_SIZE_WALK_MAX_ENTRIES 不等于 5000（OQ-4）");if(!/SKILL_SIZE_WALK_MAX_DEPTH = 16/.test(src))bad.push("SKILL_SIZE_WALK_MAX_DEPTH 不等于 16（OQ-4）");const ci=src.indexOf("function computeDigest(");const cj=ci&lt;0?-1:src.indexOf("\n}\n",ci);const cds=ci&lt;0?"":stripC(src.slice(ci,cj&lt;0?ci+900:cj));if(!cds)bad.push("无法定位 computeDigest（口径失效）");else{for(const f of ["e.overLimit === true","e.shadowed === true","e.disabled === true"])if(!cds.includes(f))bad.push("computeDigest 缺 "+f+"（D-18 禁令）");if(/bytes|fileCount/.test(cds))bad.push("尺寸统计进了 computeDigest（应只反映 prompt 段因素）；判据已剥注释 —— 在函数体里写一句「勿把 bytes / fileCount 加进来」的注释不构成违规")}if(bad.length){console.error("尺寸口径不合格:\n"+bad.join("\n"));process.exit(1)}console.log("尺寸口径 ok（递归/跳 symlink/跳隐藏/目录 size 不计/双上限/不进 digest）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `尺寸口径 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node tests/test-ai-skills.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出（改 digest 字段集或 refreshSkills 结构会让该套件转红）</fails_when>
  </verify>
  <acceptance_criteria>
    - 尺寸口径扫描输出 `尺寸口径 ok（递归/跳 symlink/跳隐藏/目录 size 不计/双上限/不进 digest）`。
    - **可失败性（分条如实披露，不得整段声称已预跑）**：`computeDigest` 那一条已在本仓**当前树**上做过**正方向实跑**（通过 ⇒ 无该条输出）与**单点变异**（在内存中删掉 `e.disabled === true` ⇒ 该条转红）—— 它是本计划**唯一**在落地前就同时具备正/反证据的判据（因为 `computeDigest` 与 `measureSkillDir` 的前半部分判据不同，它已存在于 base）。**本轮修订**把 `computeDigest` 的窗口与 `measureSkillDir` 的窗口都改为**先剥注释再判**，并对 `computeDigest` 重新取过三条证据：正方向（当前树 ⇒ 无该条输出）、变异（删 `e.disabled === true` ⇒ 报「computeDigest 缺 …」；改成 `|| e.bytes > 0` ⇒ 报「尺寸统计进了 computeDigest」）、注释反向对照（在函数体内写一句「勿把 `bytes` / `fileCount` 加进来」⇒ **不**转红）。`measureSkillDir` 相关的六条只做过**负方向**（当前树上报「缺 `async function measureSkillDir(`」，属新代码未落地），**未做** stub 等价树正方向、**未做**单点变异。执行者落地后**必须自行**做三次单点变异确认转红：往目录分支加 `bytes += …`；删掉 symlink 的 `continue`；从 `computeDigest` 摘掉任一承重字段。
    - 「只加一个文件、不改 `SKILL.md` ⇒ `digest` 不变而 `bytes` 变大」的用例通过，且实现里比较的是**值副本**（把比较对象改回活引用会让该用例恒真 ⇒ 执行者须在 SUMMARY 记录它用的是值副本）。
    - 「`.DS_Store` 不计入」与「内部 symlink 环在有限步内返回」两条用例通过。
    - `node tests/test-ai-skills.js` 全绿（证明尺寸统计没有扰动 `computeDigest` 与 `refreshSkills` 的既有契约）。
  </acceptance_criteria>
  <done>体积与文件数按「递归、含 SKILL.md、不含隐藏文件、不穿 symlink、目录 size 不计」的口径在重扫管线内算一次并缓存；`computeDigest` 逐字未改；尺寸统计的失败与截断各自产诊断、不静默、不拒绝加载。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `realm://` guest（设置页）→ 本地 HTTP `realmServer` | 唯一授权是 URL 里的 `REALM_TOKEN`（能力令牌）。无 token / 错 token 必须在任何副作用之前 403 |
| 磁盘上的技能目录 → 主进程（沙箱 `ExecutionEnv`） | 目录内容与结构**不可信**（bash 可随时改写；用户可从 Finder 塞入任意文件 / 链接） |
| `LIMITS` 常量 → 设置页渲染 | 限额数值只能经投影回传，前端写死即失实 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Information Disclosure / Tampering | 新增 `GET /api/skills/list` | high | mitigate | 首行 `reqUrl.searchParams.get('token') !== REALM_TOKEN` ⇒ 403；与既有 `/api/*` 端点同一控制面（`handleSettingsApi` 范式）。门禁 A 断言该行存在且落在函数体前 700 字符内 |
| T-50-02 | Denial of Service | `measureSkillDir` 的递归遍历（内部 symlink 环 / 深目录 / 海量条目） | high | mitigate | 显式 `kind === 'symlink'` ⇒ `continue`（不递归）+ `SKILL_SIZE_WALK_MAX_DEPTH = 16` + `SKILL_SIZE_WALK_MAX_ENTRIES = 5000` 双上限，超限截断 + warning 诊断、不拒绝加载。门禁 M 断言三条同时存在 |
| T-50-03 | Elevation of Privilege / Information Disclosure | 尺寸遍历的路径来源 | high | mitigate | 路径恒由 `path.join` + `getSkillsDir()` / `getManagedSkillsDir()` 派生，遍历只经沙箱 `env.listDir`（自带 `resolveInside` 双基准 + realpath 复核）；**禁止**直接 `fs`。门禁 M 断言 `measureSkillDir` 内无 `fs.*` 且必有 `env.listDir(` |
| T-50-04 | Denial of Service | `refreshSkills()` 的整体 `catch` 回滚 | medium | mitigate | 尺寸统计逐技能隔离失败：单技能读不到只影响它自己（`bytes=0` / `fileCount=0` + 一条 entry 级诊断），不得让异常冒泡到 `:717-728` 的整体回滚（否则一次 stat 失败会放大成技能集消失）。用例「不可读目录 ⇒ 该技能 `statsUnavailable` 且其余技能照常统计」 |
| T-50-05 | Repudiation | 统计失败的静默呈现 | medium | mitigate | `bytes === 0 && fileCount === 0` 是「统计不可用」而非「0 字节」：投影用显式布尔 `statsUnavailable` 表达，前端渲染「统计不可用」（UI-SPEC 文案 11 锁定：不得渲染 `0 B · 0 个文件`，那是失实文案） |
| T-50-06 | Tampering | 设置页渲染技能数据（技能名 / 描述 / 诊断 message 均来自磁盘，可由用户或 AI 写入任意内容） | high | mitigate | 本页新增插值**一律** DOM API（`createElement` + `textContent` + `el.title = …` / `setAttribute`）；`TD-48-01`（`escapeHtml` 只转义 `& < >`、不转义引号）**仍开未修** ⇒ 本阶段责任是**不扩大缺口**。门禁 B 对 region 内做 `innerHTML` / `insertAdjacentHTML` 负向扫描 + `document.createElement(` / `textContent` 正命题 |
| T-50-07 | Tampering | 跨窗口失效链（`computeDigest` 的字段集） | high | mitigate | `computeDigest` 逐字不改；门禁 M 断言 `e.disabled` / `e.overLimit` / `e.shadowed` 三条仍在且无 `bytes` / `fileCount`。摘任一字段会让「禁用后 `/` 面板隐藏」静默失效 |
| T-50-08 | Denial of Service | 设置页进入时的一次全量重扫 | low | accept | 100 个技能 × 整目录 stat 是首屏一次成本，且只在**进入该页**时发生（不在每次投影读取时）；UI-SPEC A6 的实测项记录在 `50-VALIDATION.md` 的 Manual-Only 表。规模已由 `MAX_USER_SKILLS` + `MAX_MANAGED_SKILLS`（各 50）封顶 |
| T-50-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | **本阶段零新增依赖**（research §Standard Stack：全部能力由既有内置模块承担），无安装动作 ⇒ 无供应链面。Phase 51 引入 zip 库时必须走完整 Package Legitimacy Gate |
</threat_model>

<verification>
- 三个任务的 automated 门禁 全部绿（含两条既有套件 `tests/test-ai-skills.js` 与 `node --test tests/test-skill-picker-model.js` 的回归）。
- 本计划的 7 条门禁**全部**只在**当前树**上做过负方向实跑（结论：失败项一律是「新代码尚未落地」类，无「合规实现也修不掉」的断言；且已顺手修掉两条**恒红/恒绿**的假判据：`computeDigest` 的窗口从 900 字符改为**函数体括号界定**、`.skill-manage-row` 两条从「对空串断言」改为**括号界定 + 判据对象存在性**）。**在落地前取得正/反证据的只有两条子判据**：① 50-01-T3 的 `computeDigest` 字段集与「尺寸不进 digest」（正方向通过；内存变异删 `e.disabled === true` ⇒ 转红）；② 50-01-T2 的两条样式硬禁令（合规段无输出；加 `--bg-hover` 底 ⇒ 转红；选择器缺失 ⇒ 报「无判据对象」而**不再**静默通过）。其余门禁与 `measureSkillDir` 相关判据**未做** stub 等价树正方向、**未做**单点变异 —— 可失败性由各任务 `<acceptance_criteria>` 列出的变异步骤承担，执行者**必须**在落地后自行跑一遍。**「已实测可失败」这类表述在本计划里只对上面点名的两条子判据成立。** 执行者若发现门禁与环境不符，按 `<acceptance_criteria>` 里记录的可失败性自行复现，**不得**为了让门禁变绿而放宽判据。
- **本轮修订（iteration 2）的门禁改动与新增证据**：凡**负向 token 禁令**（region 内的 `innerHTML` / `insertAdjacentHTML`、规则块内的 `flex-wrap` / `--bg-hover`、`measureSkillDir` 内的 `fs.*` / `require('fs')`、`computeDigest` 内的 `bytes` / `fileCount`、markup 内的内联 `display:none`）一律改为**词法级剥注释再判**（`stripC`：按**单引号 / 双引号 / 模板反引号字符串**、**正则字面量**、**行注释**、**块注释**逐字符状态扫描 + **等长空白化** —— 注释不计数；字符串与正则字面量内的 `//` **不再被误判为注释**，真实代码不被吞），并在失败文案里写明「注释中的同一字面量不构成违规」；`SETTINGS_STATUS_CHAIN` 的判据改为**只钉链序与冻结形态**（容忍换行 / 注释 / 单双引号）；另新增一条可失败判据「region 不得**直读** `item.overLimit` / `item.promptOmitted`」（D-12：这两个维度的判定只能取自 `pickStatusKey`）。**已实跑的三组正/反证据**：① 三份合成 `settings.html` / `settings-page.js` / `skill-picker-model.js`（合规含解释性注释、`innerHTML` 变异、字段直读变异、链序调序/去 freeze 变异、真实内联 `display:none` 变异）；② 合成 `main.css`（合规含本任务要求的注释、`flex-wrap` 变异、`--bg-hover` 变异）；③ `computeDigest` 的三条（正方向 / 删字段与加 `bytes` 两个变异 / 注释反向对照）。**仍未做** stub 等价树正方向的：区外壳与区说明逐字断言、`ensureSkillsFresh` 分流、`limits` 齐备等（失败项全为「新代码未落地」类）。
- **本轮修订（iteration 3）—— 剥注释机制更换与实测证据**：全部「剥注释再判」的判据从「按前一字符判 `//`」换成**词法级四态扫描**（`stripC` 逐字符走 代码 / 行注释 / 块注释 / 字符串 / 正则字面量 各态，**等长**空白化 —— 旧写法会把字符串与正则字面量里的 `//` 误判为注释起点从而吞掉同行**真实代码**）。已实测两条证据：① 本计划含 `stripC` 的两条门禁在当前树上逐条重跑，**判定与换机制前逐字一致**；② 机制本体的四项控制（剥后副本 `node --check` 三文件全过、两个基线计数不变、注释里的被禁字面量不计数、字符串 / 正则 / 模板串内的 `//` 同行送发点仍被计入）—— 完整命令与输出记在 `50-03-PLAN.md` 的 T2 acceptance「本轮修订（iteration 3）」。iteration 2 记录的「注释不转红 / 真代码转红」合成夹具证据**仍成立**（新机制在该方向上只会更严格）。
- counts-parity 在本波**预期为红**（`tests/test-skill-picker-model.js` 的例数会变、且新增了第四个套件而账本尚未扩）—— 这是 `50-RESEARCH.md` Pitfall 11 明说的「中途 red」，由 Wave 4 的 50-05 一次性收口。**不得**在本计划里改账本（改了两处就会与 Wave 2/3 的后续改动再次漂移）。
- 人工面（不进本计划）：`realm://` CSP 下 `skill-picker-model.js` 的实际加载表现（research A3）、100 条技能时的渲染耗时与投影字节数（research A6）—— 记在 `50-VALIDATION.md` 的 Manual-Only 表。
</verification>

<success_criteria>
- 打开设置页 → AI 分区 → 「技能管理」区列出全部技能的名称 / 描述 / 来源 / 体积 / 文件数 / 状态标注，按「我的技能 / 内置技能 / AI 创建」三组呈现、空组不渲染。
- **未配置任何 AI provider 时该区不为空**（至少列出两个随包内置技能）—— 这是判据 1 在无 provider 用户处的整体成立条件。
- 从未加载（`refreshedAt === 0`）时显示「技能列表尚未加载」而非「尚无任何技能」。
- 尺寸统计按 D-13 口径输出，且 `computeDigest` 的字段集逐字未变（`tests/test-ai-skills.js` 全绿为证）。
- 设置页零 `innerHTML` 的注入纪律在**本阶段新增的 region 内**成立，且有正命题（`document.createElement` + `textContent`）。
</success_criteria>

<output>
Create `.planning/phases/50-api-skills/50-01-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（50-01）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 导出函数 | `getSkillsForManagement(seededNames)` | `ai-skills-manager.js` |
| 模块常量（导出） | `SKILL_SIZE_WALK_MAX_ENTRIES`（5000）、`SKILL_SIZE_WALK_MAX_DEPTH`（16） | `ai-skills-manager.js` |
| 内部函数 | `measureSkillDir(env, dir)` | `ai-skills-manager.js` |
| 类方法 | `AIManager.prototype.ensureSkillsFresh()`、`AIManager.prototype.getSkillsForManagement()` | `ai-manager.js` |
| HTTP 路由 | `GET /api/skills/list`（`?token=` 鉴权） | `main.js` |
| 内部函数 | `handleSkillsApi(req, res, reqUrl)` | `main.js` |
| 分发分支 | `reqPath.startsWith('/api/skills/')` | `main.js` |
| 数据表键 | `STATUS_TEXT.disabled`（值 `'已禁用'`） | `src/skill-picker-model.js` |
| 数据表 + 纯函数 | `SETTINGS_STATUS_CHAIN`（冻结数组，顺序即 D-12 链）、`pickStatusKey(skill)`（多命中取首条，`null` = 无标注） | `src/skill-picker-model.js` |
| DOM id / class | `#skillManageState` / `#skillManageGroups` / `#skillManageHint` / `.skill-manage-section` / `.skill-manage-state` / `.skill-manage-groups` / `.skill-manage-list` / `.skill-manage-row` / `.skill-manage-row-main` / `.skill-manage-row-sub` / `.skill-manage-name` / `.skill-manage-tail` / `.skill-manage-status` / `.skill-manage-status-limit` / `.skill-manage-desc` / `.skill-manage-meta` / `.skill-manage-group-title` / `.skill-manage-hint` / `.skill-manage-hint-success` / `.skill-manage-hint-danger` | `src/settings.html` / `src/styles/main.css` |
| CSS 令牌 | `--skill-success-text`（暗 `#6EE7B7` / 亮 `#065F46`） | `src/styles/main.css` 两套主题块 |
| 前端函数 | `skillsApi(route, options, query)`、`formatSkillSize(bytes)`、`renderSkillManagement(projection)`、`loadSkillManagement()`、`setupSkillManageListeners()` | `src/settings-page.js` |
| region 标记 | `/* Phase 50 skill-manage region: start */` … `/* Phase 50 skill-manage region: end */` | `src/settings-page.js` |
| 测试套件 | `tests/test-skills-management.js`（**新建文件**，本阶段账本新增的第 4 个套件） | `tests/` |

### 本阶段其余计划将新建（此处仅登记，避免在评审时被当作「应已存在」）

| 计划 | 符号 |
|------|------|
| 50-02 | `MANAGE_SKILL_ERROR.NOT_USER_OWNED`（`'not_user_owned'`）、`deleteUserSkill(env, {name})`、`validateSkillNameForManagement(name)`、`validateDisabledListForSettings(list)`、`AIManager.prototype.setSkillDisabled(name, disabled)`、`AIManager.prototype.uninstallUserSkill(name)`、`POST /api/skills/set-disabled`、`POST /api/skills/uninstall`、`tests/test-skills-http-api.js`（新建第 5 个套件） |
| 50-03 | `MAX_JSON_BODY_BYTES`（1 MiB）、`MAX_JSON_BODY_BYTES_LARGE`（32 MiB）、`readJsonBody(req, res, {maxBytes})` 的新签名与 `res` 缺失降级分支、`sendJson` 的幂等护栏；IPC 通道 `ai:get-skills-management` / `ai:set-skill-disabled` / `ai:uninstall-skill`；`realmAPI.ai.getSkillsManagement` / `setSkillDisabled` / `uninstallSkill` |
| 50-04 | `.skill-manage-diag-badge` / `.skill-manage-diag` / `.skill-manage-diag-list` / `.skill-manage-diag-item` / `.skill-manage-diag-level` / `.skill-manage-diag-message` / `.skill-manage-diag-code` / `.skill-manage-summary` / `.skill-manage-actions` / `.skill-manage-danger-btn` / `.skill-manage-confirm` / `.skill-manage-confirm-body` / `.skill-manage-confirm-note` / `.skill-manage-confirm-actions` / `.skill-manage-state-title` / `.skill-manage-state-title-danger` / `.skill-manage-state-body`；`window.SkillPickerModel.EXPLICIT_TAG`（「仅显式」单源表，**具体命名交执行期**）；`setSkillManageHint` / `resetSkillManageHint` / `setCollapsed` 单点赋值函数 |
| 50-05 | `docs/product/ai-skills.md` §十二【管理面】；`AGENTS.md` 的技能管理面维护约定条目与测试清单刷新；`50-VALIDATION.md` 的回填 |
