---
phase: 50-api-skills
plan: 03
type: execute
wave: 3
depends_on: [50-01, 50-02]
files_modified:
  - main.js
  - ipc-handlers.js
  - src/preload.js
  - tests/test-skills-http-api.js
autonomous: true
requirements: [SEC-09, USER-07]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

key-decisions:
  - '**`readJsonBody` 的签名按用户裁决取「方案 A」**（`50-RESEARCH.md` 矛盾 1 的 ✅ 已裁决块，2026-09-14 —— **BINDING，不得再开**）：`readJsonBody(req, res, { maxBytes = MAX_JSON_BODY_BYTES } = {})`，`res` 为**第二位置参**；D-16 原文的「零调用点改动」条款由用户主动放松。被否决的 B（两参 + 只 reject ⇒ 400，零调用点改动且前端零改动）与 C（入口 `req.__res = res` 隐式绑定）**不得**再被重提或"改良"'
  - '**`res` 缺失的降级分支是强制项，不是防御性装饰**：`if (!res || typeof res.writeHead !== ''''function'''')` ⇒ **不答响应、只 reject**（带 `code: ''''BODY_TOO_LARGE''''`）。理由：全仓 `grep -n "uncaughtException\|unhandledRejection" main.js ipc-handlers.js agent-workspace.js ai-manager.js` **零命中**（无全局兜底）⇒ 若在 `req.on(''''data'''')` 监听器内对 `undefined` 调 `sendJson` 会 `TypeError` ⇒ **Electron 主进程直接退出**。这条分支把「漏改一处调用点」的后果从**崩溃**降为**一个 400** —— 而 D-16 要的「漏了上限当场可见」仍然成立（该端点超限返回 400 而非 413，一样可见但不致命）'
  - '**413 的实现形态固定，不得自由发挥**：`sendJson(res, 413, { error, limit })` + **`req.resume()`**（停止累积、把剩余流排空）。**禁用** `req.destroy()`（实测客户端拿 `EPIPE` ⇒ 413 永远看不到）与 **`Connection: close`**（本会话两次实测结论相反、无任何收益；「设了就 EPIPE」**不得**写进代码注释或产品文档当成事实）。可选的快路径增强是读第一个字节**之前**用 `req.headers[''''content-length'''']` 预检，但它**不得取代**累积中判（`Content-Length` 可伪造 / 可缺失）'
  - '**`sendJson` 的幂等护栏是一次修好全部发送点**：13 处宿主（12 个具名 handler + `realmServer` 内的 `/api/bookmarks-bar/toggle` 内联分支）的 `catch → sendJson` 形状一致；若 `readJsonBody` 已 `res.end(413)` 之后仍 reject，每个 catch 都会再写一次头 ⇒ `ERR_HTTP_HEADERS_SENT`，而 `realmServer` 的回调是 `async` ⇒ HTTP 层不接管返回的 Promise ⇒ **unhandled rejection** ⇒（无全局兜底）主进程退出。加护栏后 catch 路径 no-op，`unhandledRejection` 计数为 0'
  - '**两个书签导入端点必须显式放大上限**（research 修正 3）：`main.js` 的 `POST /api/favorites/import-chrome` 与 `POST /api/favorites/import-html` 的 body 是**用户书签文件全文**（`src/favorites-page.js` 提交 `file.text()` 的完整内容；Chrome / Safari 常规 1–10 MB，且 import-html 的预览与执行各发一次）。不显式覆盖就是**静默破坏既有功能**。命名必须区分量：`MAX_JSON_BODY_BYTES`（1 MiB，传输层闸）与 PITFALLS P7 的「单 entry 解压 ≤ 1 MB」是**同数不同量**'
  - '**机械判据**：`await readJsonBody(req` 的出现次数与 `await readJsonBody(req, res` 的出现次数**必须相等**（差为 0），且总数 ≥ 57（既有调用点一个都不许丢）。⚠️ 注意 `grep -c "await readJsonBody(req)"` 这一写法在改签名后**恒为 0**（`(req)` 的右括号不再紧邻 `req`）—— 判据必须用「带 `res` 的调用点数 == 总数」，不能用「不带 `res` 的调用点数 == 0」以外的字面形式'
  - '**双入口（USER-07 的 IPC 半边）**：三个新通道 `ai:get-skills-management` / `ai:set-skill-disabled` / `ai:uninstall-skill`，**首行 `assertTrustedSender(event)`**（拒 webview guest / DevTools / 非受管窗口），其余只做转发 —— **零判定逻辑**（判据 / 校验 / 写函数全在 `ai-skills-manager.js`），与 `ai:get-skills`（`ipc-handlers.js:1747`）/ `ai:refresh-skills`（`:1763`）并列。主窗口 **必须**走 IPC（`file://` fetch 本地 HTTP 被 CORS 拦 —— Phase 38 事故）；设置页 **必须**走 HTTP。二者**不可互换**'
  - '**IPC 写侧当前无 UI 消费者**（D-17 明文）：通道的存在价值是「两入口同一权威」的对称性 + 未来主窗口管理面 + 开发 / 调试便利。**不得**为了让 IPC 有消费者而在主窗口新造一套管理 UI（那是新能力，不属本阶段）'
  - '**诚实边界（必须如实成文）**：`/api/skills/*` 三个端点的载荷远小于 1 MiB ⇒ `BODY_TOO_LARGE` 在技能域**正常不可达**，它是 SEC-09 的兜底。该事实须写进产品文档的管理面章节，以免读者以为这三个端点会触发 413'

must_haves:
  truths:
    - '[SEC-09] `readJsonBody` 在**累积过程中**判体积：`size += chunk.length` 后立即比较，超限即**停止拼接 body**（`if (rejected) return;`）、答 `413` 并 `req.resume()` 排水 —— **不是**「读完再判 `body.length`」（实测：40 MB body 全量累积 ⇒ 堆 +45.7 MB 且返回 200，那不是防护）'
    - '[SEC-09] 超限请求的客户端**能拿到 413 与 JSON body**（不是网络错误）：实现里**没有** `req.destroy()`、**没有** `Connection: close`。这是判据 5「返回明确错误」的可达性条件'
    - '[SEC-09] `sendJson` 有幂等护栏（`headersSent || writableEnded` ⇒ no-op），因此「`readJsonBody` 已答 413 后又 reject」不会让 13 处 `catch → sendJson` 二次写头；`process.on(''''unhandledRejection'''')` 的计数为 0'
    - '[SEC-09] 全部既有调用点（57 处）改为传 `res`，且两个书签导入端点显式传 `{ maxBytes: MAX_JSON_BODY_BYTES_LARGE }`（32 MiB）；默认值路径行为不变（既有全量套件绿）'
    - '[裁决 A · 强制项] `res` 缺失（漏传 / `undefined`）时**不答响应、只 reject**（`code: ''''BODY_TOO_LARGE''''`）⇒ 漏改调用点的后果是一个 400，**不是主进程崩溃**（全仓无 `uncaughtException` / `unhandledRejection` 全局兜底）'
    - '[USER-07] 三个新 IPC 通道与两个新 HTTP 写端点**转发到同一组 manager 函数**（`getSkillsForManagement` / `setSkillDisabled` / `uninstallUserSkill`）；两个 handler 都**不含判定逻辑**（无 `kind ===` / `source ===` / 目录名判断）'
    - '[D-17] 设置页走 HTTP（guest 无 `realmAPI`）、主窗口走 IPC（`file://` 不能 fetch 本地 HTTP）；`realmAPI.ai` 新增 `getSkillsManagement` / `setSkillDisabled` / `uninstallSkill` 三个方法，与既有 `getSkills` / `refreshSkills` 并列'
    - '[assumption:SEC-09 边界] `Content-Length` 可伪造 / 可缺失 ⇒ 预检只作快路径（可选的零字节拒收），**累积中判是唯一的判据**；预检存在时也**不得**跳过累积中判'
    - '[assumption:USER-07 并发边界] 两个入口（HTTP 与 IPC）并发写同一技能时，最终状态由「主进程内同步读-改-写」保证一致（不是「后写覆盖先写」）；广播是幂等的（renderer 侧监听**无条件重拉快照**），重复广播零副作用'
    - '[assumption:USER-07 并发边界] `windowManager.broadcast` **只到各 BrowserWindow 的 webContents、不到 webview guest** ⇒ 「任一入口的变更跨窗口即时同步」这条判据**只在 BrowserWindow 宿主上成立**；设置页（guest）收不到任何广播，靠「每次操作后用响应体回传的投影就地重渲染 + 进入页面时重拉」保证一致。这条**不得**被写成「跨全部窗口即时同步」的更强声明'
  artifacts:
    - 'main.js：常量 `MAX_JSON_BODY_BYTES`（1024 * 1024）与 `MAX_JSON_BODY_BYTES_LARGE`（32 * 1024 * 1024）；`readJsonBody(req, res, { maxBytes })` 新签名 + `res` 缺失降级分支 + 413 形态；`sendJson` 的幂等护栏；全部调用点改签名；两个书签端点的显式覆盖'
    - 'ipc-handlers.js：`ai:get-skills-management` / `ai:set-skill-disabled` / `ai:uninstall-skill` 三个通道'
    - 'src/preload.js：`realmAPI.ai.getSkillsManagement` / `setSkillDisabled` / `uninstallSkill`'
    - 'tests/test-skills-http-api.js：SEC-09 的真 `http.createServer` harness（413 + JSON + 堆不线性增长 + `unhandledRejection` 计数为 0 + 降级分支 reject 而非崩溃）+ token 403 组 + 57/59 调用点覆盖度的机械判据 + 双入口源码扫描'
  key_links:
    - '机械判据链：`main.js` 的调用点总数（`await readJsonBody(req` 出现次数）== 传 `res` 的次数 == 既有 57 + 本阶段新增的两处写路由（共 59）。漏改一处 ⇒ 降级分支接手 ⇒ 该端点超限答 400（可见但不致命）'
    - '双入口链：`ipc-handlers.js` 的三通道 → `aiManager.<同一方法>` ← `main.js` 的 `handleSkillsApi` 三子路由。两端**共用** manager 的实现，这是「两入口同一权威」的可核形式'
    - '回归护栏：`tests/test-manage-skill.js` / `tests/test-ai-skills.js` / `tests/test-skill-picker-model.js` / `tests/test-skills-management.js` 在改签名后必须全绿（默认值路径行为不变）'
  prohibitions:
    - statement: '不得用 `req.destroy()` 拒收超限请求体'
      status: unresolved
      verification: judgment
      reason: '实测：`req.destroy()` 在响应被客户端读取前撕掉 socket，而客户端此刻**仍在写** body ⇒ 客户端拿到 `EPIPE` 而非 413 ⇒ ROADMAP 判据 5 的「返回明确错误」不成立，前端只能显示通用失败文案'
    - statement: '不得把「设置 `Connection: close` 会导致客户端 EPIPE」写进代码注释或产品文档当成事实'
      status: unresolved
      verification: judgment
      reason: '该结论在重研会话中**不可复现**（`http.request` 与 `fetch` 两种客户端都拿到了 413）。可复现的规则只是「不 `destroy()`、不设 `Connection: close`、用 `req.resume()`」。把不可复现的因果写成事实会污染后续阶段的判断依据'
    - statement: '不得新增 `process.on(''''uncaughtException'''')` / `process.on(''''unhandledRejection'''')` 之类的全局兜底来「吸收」漏改调用点的后果'
      status: unresolved
      verification: judgment
      reason: '本仓的既有事实是**没有**全局兜底，这正是 `res` 缺失降级分支「承重」的前提。加一个全局兜底会同时掩盖真实的编程错误（把崩溃变成一个静默的日志），并让降级分支的存在理由不可验证'
    - statement: '不得为「需要大 body 的端点」另写第二个请求体读取函数'
      status: unresolved
      verification: judgment
      reason: '`readJsonBody` 是全部 `/api/*` POST 端点的**唯一**公共入口；第二份实现必然与第一份在体积闸、降级分支、错误形状上漂移。Phase 51 的 zip base64 导入端点届时同样走它 + `{ maxBytes }`'
    - statement: '不得**新增**任何 `res.writeHead` 响应发送点（基线 14 处已逐行登记：既有 13 处非 JSON 发送点 + `sendJson` 1 处），也不得在 `sendJson` 之外写 JSON 响应'
      status: unresolved
      verification: judgment
      reason: '13 处宿主的 `catch → sendJson` 全部依赖这一处幂等护栏；新增第二个发送点会绕过护栏，让 `ERR_HTTP_HEADERS_SENT` / unhandled rejection 的旧缺陷重新出现。⚠️ 该禁令是「不得**新增**」而非「全文件唯一」—— 媒体代理（`:317/:332/:375/:413/:429/:453/:470`）、静态资源（`:1315/:1464`）与 `realm://` 静态兜底（`:2898/:2907/:2917/:2922`）这 13 处是**既有**的非 JSON 发送路径，与本阶段改动无关；**不得**为了让判据变成「全文件恰 1 处」而删除它们（那是破坏既有功能）'
    - statement: '不得在 IPC handler 里做判定（技能来源 / 目录类型 / seeded 判定），也不得让主窗口绕过 `assertTrustedSender`'
      status: unresolved
      verification: judgment
      reason: '判据单源在 `ai-skills-manager.js`；handler 一旦有判定就会有第二份，且两入口的判定必然漂移（一个入口放行、另一个拒绝同一操作）。`assertTrustedSender` 是拒 webview guest / DevTools / 非受管窗口的既有控制面，缺一行就是把写能力暴露给不受信来源'
---

<objective>
收口 **SEC-09（请求体体积上限，安全项而非优化项）** 与 **USER-07 的 IPC 半边**：`readJsonBody` 加「累积中拒收」的体积闸（默认 1 MiB，需大 body 的端点显式放大），`sendJson` 加幂等护栏一次修好全部发送点；同时补上「两个前端入口读写对等」的主窗口侧接线。

Purpose: SEC-09 是 Phase 51 那条 **zip base64 放大**路径的前置闸 —— 形状必须是「默认 fail-closed + 需大者显式放大」，让「忘了声明上限」在一个 413 处**当场可见**，而不是静默地退化成假边界。同时本计划一次性关闭两个**会崩主进程**的隐患（`res` 缺失时在 data 监听器里 `TypeError`；二次 `sendJson` 的 `ERR_HTTP_HEADERS_SENT` → unhandled rejection）—— 本仓**没有**任何全局异常兜底，这两条不是理论风险。

Output: `main.js` 的 `readJsonBody` 新签名与 413 形态、`sendJson` 幂等护栏、全部调用点改签名与两个书签端点的显式覆盖；`ipc-handlers.js` 的三个管理通道与 `src/preload.js` 的三个 `realmAPI` 方法；`tests/test-skills-http-api.js` 的 SEC-09 真 harness 组与双入口源码扫描。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-api-skills/50-CONTEXT.md
@.planning/phases/50-api-skills/50-RESEARCH.md
@.planning/phases/50-api-skills/50-PATTERNS.md
@.planning/phases/50-api-skills/50-01-PLAN.md
@.planning/phases/50-api-skills/50-02-PLAN.md
@main.js
@ipc-handlers.js
@src/preload.js
@src/favorites-page.js
@tests/test-skills-http-api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-09 主体 —— `readJsonBody(req, res, { maxBytes })` + 降级分支 + 413 形态 + 单源常量</name>
  <files>main.js, tests/test-skills-http-api.js</files>
  <read_first>
    - `main.js`：`:877-880`（`sendJson` 现状）、`:887-900`（`readJsonBody` 现状 —— 两参、`body += chunk` **无上限**）、`:1156-1177`（**两个书签导入端点**：`import-chrome` / `import-html` 从 body 取 `content`）、`:2704`（`realmServer` = `http.createServer(async (req, res) => {…})` —— **回调是 `async` ⇒ HTTP 层不接管返回的 Promise**）、`:2818-2835`（`/api/bookmarks-bar/toggle` 的内联分支，其 `catch` 答 500 —— 13 个发送点之一）、`:104`（`REALM_TOKEN`）
    - `src/favorites-page.js`：`:2235-2259`（`pickBookmarkFile` —— `file.text()` 提交**用户书签文件全文**；`import-html` 的预览与执行各发一次）
    - `.planning/phases/50-api-skills/50-RESEARCH.md`：**§「矛盾 1」的 ✅ 已裁决块（BINDING —— 六条实现契约）**、§Pitfall 1（413 形态的四组实测对照与堆数字）、§Pitfall 2（幂等护栏的必要性实测）、§Pitfall 3（两个书签端点的完整调用点分表：**12 个具名 handler + 1 个内联分支 = 57 处；13 个宿主全都有 `catch → sendJson`**）、§Code Examples `:1221-1277`（`readJsonBody` 的完整骨架）、§Phase 51 体积上限交接（`MAX_JSON_BODY_BYTES` 与 P7「单 entry 1 MB」是**同数不同量**）
    - `.planning/phases/50-api-skills/50-02-PLAN.md` Task 3（本阶段新增的两处 `await readJsonBody(req, res)` —— 它们也在改签名的范围内）
  </read_first>
  <action>
    1. **新增两个单源常量**（写在 `realmServer` 的闭包层、`sendJson` 附近；**数值只此一份**）：`const MAX_JSON_BODY_BYTES = 1024 * 1024;` 与 `const MAX_JSON_BODY_BYTES_LARGE = 32 * 1024 * 1024;`，各带一行注释说明量纲（前者是 `/api/*` POST 的全局默认、后者是「body 是用户文件全文」的端点专用；**并注明它与 PITFALLS P7 的「单 entry 1 MB」是同数不同量**）。
    2. **改造 `readJsonBody`**（**逐条实现，不得自由发挥**）：
       - 签名：`function readJsonBody(req, res, { maxBytes = MAX_JSON_BODY_BYTES } = {})`（`res` 是**第二位置参**）。
       - 能力探测：`const canRespond = !!(res && typeof res.writeHead === 'function');`（等价于裁决块要求的 `if (!res || typeof res.writeHead !== 'function')` 降级分支 —— 写成能力布尔时，**两个原始条件必须都出现在函数体内**：`res` 的存在性与 `typeof res.writeHead === 'function'`）。
       - `let body = ''; let size = 0; let rejected = false;` 与一个 `tooLarge()` 闭包：置 `rejected = true`；`canRespond` 为真时 `sendJson(res, 413, { error: '请求体超过上限（' + maxBytes + ' 字节）', limit: maxBytes }); req.resume();`（**排水；不得 destroy、不得设 `Connection: close`**）；`canRespond` 为假时**什么都不答**（只走 reject）。
       - 可选的快路径：`const declared = Number(req.headers['content-length']); if (Number.isFinite(declared) && declared > maxBytes) { tooLarge(); reject(Object.assign(new Error('请求体超过上限'), { code: 'BODY_TOO_LARGE', limit: maxBytes })); return; }`（注释写明「`Content-Length` 可伪造 / 可缺失 ⇒ 不得取代累积中判」）。
       - `req.on('data', chunk => { if (rejected) return; size += chunk.length; if (size > maxBytes) { tooLarge(); reject(Object.assign(new Error('请求体超过上限'), { code: 'BODY_TOO_LARGE', limit: maxBytes })); return; } body += chunk; });`
       - `req.on('end', () => { if (rejected) return; try { resolve(body ? JSON.parse(body) : {}); } catch (err) { reject(err); } });` 与 `req.on('error', reject);` 保持不变。
       - 函数 JSDoc 写清三件事：超限即停收并答 413；**不断连**（`req.destroy()` 会让客户端拿 EPIPE 而非 413）；`res` 缺失时只 reject（漏改调用点的后果是外层 catch 答 400 而非在 data 监听器里 `TypeError` ⇒ 主进程退出）。
       - ⚠️ **不得**依赖任何全局异常兜底；**不得**新增 `process.on('uncaughtException' | 'unhandledRejection')`。
    3. **两个书签导入端点显式放大**：`main.js:1156-1157`（`import-chrome`）与 `:1176-1177`（`import-html`）改为 `await readJsonBody(req, res, { maxBytes: MAX_JSON_BODY_BYTES_LARGE })`（本任务只改这两处 + 函数本体；**其余调用点的批量改签名在 Task 2**）。
    4. **`tests/test-skills-http-api.js` 补 SEC-09 行为组**（真起 `http.createServer` 的小 harness，**形同 research 的探针但独立书写**，用来测一个**从 `main.js` 复制不出来的闭包函数** ⇒ 采用「等价实现 + 契约断言」双层）：
       - ⚠️ **可测性说明（必须在用例注释里写明）**：`readJsonBody` 住在 `main.js` 的 `realmServer` 闭包内、**不可 require**。因此本套件对 SEC-09 采用**源码契约断言**（门禁 F：签名 / 降级分支 / 413 形态 / 幂等护栏 / 调用点覆盖度）**加** `http` 层的行为回归（用 `http.createServer` 复刻同一形态，断言「超限 ⇒ 413 + JSON + 堆不线性增长 + `unhandledRejection` 计数为 0」，并断言「`req.destroy()` 形态的客户端可见性不是 413」作为**反向对照**）。**不得**把该行为组写成「测试了 `main.js` 里的那个函数」—— 那是失实声明。
       - 行为组至少四条：① 40 MiB body + 1 MiB 上限 ⇒ 客户端拿到 **413 且 body 可 JSON.parse**；② 同一请求下 `process.on('unhandledRejection')` 计数 **=== 0**；③ `process.memoryUsage().heapUsed` 的增量**远小于 body 体积**（断言 `< body 体积 / 4` —— 用比值而非绝对值以避开机器差异；「远小于」的判据必须是可失败的数字比较）；④ 反向对照：把拒收形态换成 `req.destroy()` ⇒ 客户端**拿不到** 413（`fetch` 抛错，断言捕获到异常）。
       - **不得**手工构造被测实现从不构造的对象：harness 用真实的 `http.request` / `fetch` 发真实字节流。
  </action>
  <verify>
    <automated>node --check main.js</automated>
    <fails_when>non-zero exit，或 stderr 出现 `SyntaxError`</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const s=fs.readFileSync("main.js","utf8");const i=s.indexOf("function readJsonBody(");if(i&lt;0)bad.push("缺 readJsonBody");const j=s.indexOf("\n  }\n",i);const body=i&lt;0?"":s.slice(i,j&lt;0?i+2500:j);const blankC=(m)=&gt;m.replace(/[^\n]/g," ");const stripC=(x)=&gt;x.replace(/(^|[^:])(\/\/[^\n]*)/g,(m,p1,p2)=&gt;p1+p2.replace(/[^\n]/g," ")).replace(/\/\*[\s\S]*?\*\//g,blankC);const bodyCode=stripC(body);if(!/function readJsonBody\(req, res, \{ maxBytes = MAX_JSON_BODY_BYTES \} = \{\}\)/.test(s))bad.push("readJsonBody 签名不是 (req, res, { maxBytes = MAX_JSON_BODY_BYTES } = {})");const capOk=/typeof res\.writeHead/.test(body) && /(!res\b|res\s* && )/.test(body);if(!capOk)bad.push("缺 res 缺失的降级分支（漏改调用点会崩主进程）");if(!body.includes("sendJson(res, 413"))bad.push("超限未答 413");if(!body.includes("req.resume()"))bad.push("超限未 req.resume()（排水）");if(/req\.destroy\(/.test(bodyCode))bad.push("readJsonBody 使用了 req.destroy()（客户端拿 EPIPE，413 不可达）");if(/setHeader\(\s*[\x27"]Connection[\x27"]/.test(bodyCode)||/[\x27"]Connection[\x27"]\s*:/.test(bodyCode))bad.push("readJsonBody 写入了 Connection 头（实测无收益，禁用）");if(!/if \(rejected\) return;/.test(body))bad.push("拒收后仍在累积 body（内存 O(n)）");if(/process\s*\.\s*(on|once)\s*\(\s*[\x27"](uncaughtException|unhandledRejection)/.test(stripC(s)))bad.push("main.js 出现全局异常兜底的**注册形态**（process.on(\x27uncaughtException\x27|\x27unhandledRejection\x27) ⇒ 降级分支的前提被破坏）；判据已 stripC 剥除注释，注释里如实写明「本仓无全局兜底」不构成违规");if(bad.length){console.error("SEC-09 主体不合格:\n"+bad.join("\n"));process.exit(1)}console.log("SEC-09 主体 ok（三参签名 + 降级分支 + 413+resume + 无 destroy/无 Connection）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `SEC-09 主体 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - `readJsonBody` 的签名逐字为 `(req, res, { maxBytes = MAX_JSON_BODY_BYTES } = {})`。
    - 函数体内**同时**出现 `res` 的存在性判断与 `typeof res.writeHead` 的能力判断（降级分支）；**代码里没有** `req.destroy()`，**也没有**写 `Connection` 头的形态（`setHeader('Connection', …)` / 头对象字面量里的 `'Connection': …`）。⚠️ 门禁**先剥注释再扫**：**允许**代码注释如实写明这条可复现的规则（「不 `destroy()`、不设 `Connection: close`、用 `req.resume()`」）—— 只禁**代码形态**，不禁这句正确描述（否则一条正确的注释会把门禁判成违规）。
    - 413 走 `sendJson(res, 413, …)` + `req.resume()`。
    - 行为组四条用例通过，其中 ③ 的堆增量判据是**可失败的数字比较**（把累积改成「读完再判长度」会让它转红），④ 的反向对照证明 `req.destroy()` 形态确实拿不到 413。
    - 「`main.js` 没有全局异常兜底」这条判据 = **注册形态**（`process.on/once('uncaughtException'|'unhandledRejection')`）+ **剥注释**；两条已实测：在 `readJsonBody` 前插一条**合规注释**（含 `process.on('uncaughtException')` 字面量）⇒ 不转红；真写一行 `process.on('uncaughtException', …)` ⇒ 报「出现全局异常兜底的**注册形态**」⇒ 转红。
    - 用例注释里**明确写出**「本组测的是等价 harness 的 `http` 语义 + `main.js` 的源码契约，不是直接测那个不可 require 的闭包函数」——不得写成失实的覆盖声明。
  </acceptance_criteria>
  <done>`readJsonBody` 具备 fail-closed 的体积闸与承重的降级分支；413 形态与「不 destroy / 不设 Connection: close / 用 resume」三条被源码契约与 `http` 行为双层钉住。</done>
</task>

<task type="auto">
  <name>Task 2: `sendJson` 幂等护栏 + 全部调用点改签名 + 两个书签端点覆盖的回归</name>
  <files>main.js, tests/test-skills-http-api.js</files>
  <read_first>
    - `main.js`：`:877-880`（`sendJson` —— 本任务加首行护栏）、分批的调用点宿主（`:908-975` `handleHistoryApi` 3 处 / `:976-1286` `handleFavoritesApi` **17 处** / `:1336-1709` `handleSettingsApi` 10 处 / `:1710-1866` `handleSearchConfigApi` 2 / `:1867-1995` `handleDevRequestsApi` 2 / `:1996-2165` `handleDownloadsApi` 6 / `:2166-2333` `handleTasksApi` 3 / `:2334-2424` `handleCredentialsApi` 3 / `:2425-2483` `handleAddressApi` 2 / `:2512-2580` `handleRulesApi` 5 / `:2581-2648` `handleShortcutsApi` 2 / `:2649-2703` `handleAiMemoryApi` 1 / 内联 `/api/bookmarks-bar/toggle` 1）
    - `50-03-PLAN.md` 本任务的上一步（Task 1 已定死签名与 413 形态）
    - `.planning/phases/50-api-skills/50-RESEARCH.md` §Pitfall 3 的**完整调用点分表**（用它而不是重新数 —— 12 个具名 handler + 1 个内联分支 = 57）
  </read_first>
  <action>
    1. **`sendJson` 加幂等护栏**：函数体**首行**新增 `if (res.headersSent || res.writableEnded) return;`，其余三行保持不变。JSDoc 补一句：本护栏是「一次修好全部发送点」—— 13 处宿主的 `catch → sendJson` 全部依赖它；`readJsonBody` 自己答了 413 又 reject 时，外层 catch 的二次发送会在此 no-op（否则 `ERR_HTTP_HEADERS_SENT` ⇒ `async` 回调的 unhandled rejection ⇒ 无全局兜底 ⇒ 主进程退出）。
       **不得新增**任何 `res.writeHead` 发送点 —— `main.js` 的累计基线是 **14 处**（既有 13 处非 JSON 发送点：媒体代理 7 / 静态资源 2 / `realm://` 静态兜底 4，加 `sendJson` 自身 1 处），不得在 `sendJson` 之外写 JSON 响应。
    2. **批量改签名**：把 `main.js` 内**全部**两参形态的请求体读取调用点（只传 `req` 的那种）改为传入 `res` 的三参形态（含 50-02 新增的两个写路由 —— 它们已经是两参形态，本步让它们带上 `res`）。**两个书签端点**（`import-chrome` / `import-html`）的调用改为 `await readJsonBody(req, res, { maxBytes: MAX_JSON_BODY_BYTES_LARGE })`（若 Task 1 已改，本步只做校验）。改完做一次**机械复核**：`await readJsonBody(req` 的出现次数 == `await readJsonBody(req, res` 的出现次数 == 57 + 本阶段新增调用点数（预期 59）。两个数不相等时**必须逐条定位差异**，不得直接放宽判据。
    3. **逐个端点评审**（research 明文要求「不能按『都是小 JSON』的假设批量放行」）：按 `50-RESEARCH.md` §Pitfall 3 的分表逐宿主过一遍，确认除了两个书签端点外，没有其它端点会拿到「用户文件全文」级别的 body。**评审结论写进 SUMMARY**（哪个宿主 / 调用点数 / 是否有大 body 风险 / 结论）。若发现第三个大 body 端点，在本 Plan 内一并显式覆盖并在 SUMMARY 记录。
    4. **`tests/test-skills-http-api.js` 补两组**：
       - **调用点覆盖度的机械判据**（源码扫描）：断言 `await readJsonBody(req` 的次数 == `await readJsonBody(req, res` 的次数，且前者 ≥ 57；断言 `maxBytes: MAX_JSON_BODY_BYTES_LARGE` 恰出现 2 次；断言 `const MAX_JSON_BODY_BYTES = 1024 * 1024;` 与 `const MAX_JSON_BODY_BYTES_LARGE = 32 * 1024 * 1024;` 各恰出现 1 次（单源）。
       - **降级分支的行为断言**（本任务的可失败性来源）：以**旧两参形态**调用被测的等价实现（`res` 为 `undefined`）⇒ 断言得到的是一个 **reject**（`code === 'BODY_TOO_LARGE'`）而**不是** `TypeError` 崩溃，且进程未退出。这条对应裁决 A 的强制项。
       - **既有端点回归 smoke**：断言 `/api/settings/update` 的调用点仍在（它是最重的那个：任意键值写入），且默认值路径的参数形态正确（两参 + 无第三参）。
  </action>
  <verify>
    <automated>node --check main.js  &&  node -e 'const fs=require("fs");const bad=[];const s=fs.readFileSync("main.js","utf8");const blankC=(m)=&gt;m.replace(/[^\n]/g," ");const stripC=(x)=&gt;x.replace(/(^|[^:])(\/\/[^\n]*)/g,(m,p1,p2)=&gt;p1+p2.replace(/[^\n]/g," ")).replace(/\/\*[\s\S]*?\*\//g,blankC);const sc=stripC(s);if(!/function sendJson\(res, status, data\)\s*\{/.test(sc))bad.push("sendJson 签名被改动（须为 function sendJson(res, status, data)）");const sj0=s.indexOf("function sendJson(");const sj1=sj0&lt;0?-1:s.indexOf("\n  }\n",sj0);const sj=sj0&lt;0?"":stripC(s.slice(sj0,sj1&lt;0?sj0+400:sj1));if(!sj)bad.push("无法定位 sendJson（口径失效）");else{const inSend=(sj.match(/res\.writeHead\(/g)||[]).length;if(inSend!==1)bad.push("sendJson 内 res.writeHead( 出现 "+inSend+" 处（须恰 1；判据已剥除注释）");if(!/res\.headersSent/.test(sj)||!/res\.writableEnded/.test(sj))bad.push("sendJson 缺幂等护栏（须**同时**具备 res.headersSent 与 res.writableEnded 两个条件；判据只看代码形态，注释不算）");const gIdx=sj.indexOf("res.headersSent"),hIdx=sj.indexOf("res.writeHead(");if(gIdx&lt;0)bad.push("sendJson 缺幂等护栏（函数体内未出现 res.headersSent）");else if(hIdx&gt;=0 && hIdx&lt;gIdx)bad.push("sendJson 的护栏不在 res.writeHead 之前（顺序反 ⇒ 二次写头不被吸收）")}const c1=(sc.match(/await readJsonBody\(req/g)||[]).length;const c2=(sc.match(/await readJsonBody\(req, res/g)||[]).length;if(c1!==c2)bad.push("调用点未全部传 res：总计 "+c1+" / 传 res "+c2);if(c1&lt;57)bad.push("readJsonBody 调用点数 "+c1+" &lt; 57（既有调用点被误删）");const c3=(sc.match(/maxBytes: MAX_JSON_BODY_BYTES_LARGE/g)||[]).length;if(c3!==2)bad.push("两个书签导入端点的显式覆盖 "+c3+" 处（须恰 2）");if((sc.match(/const MAX_JSON_BODY_BYTES = 1024 \* 1024;/g)||[]).length!==1)bad.push("MAX_JSON_BODY_BYTES 单源常量缺失或不唯一");if((sc.match(/const MAX_JSON_BODY_BYTES_LARGE = 32 \* 1024 \* 1024;/g)||[]).length!==1)bad.push("MAX_JSON_BODY_BYTES_LARGE 单源常量缺失或不唯一");const BASE_HEADS=14;const heads=(sc.match(/res\.writeHead\(/g)||[]).length;if(heads!==BASE_HEADS)bad.push("main.js 的 res.writeHead( 计数 "+heads+" ≠ 基线 "+BASE_HEADS+"（基线 = 既有 13 处非 JSON 发送点 :317/:332/:375/:413/:429/:453/:470、:1315/:1464、:2898/:2907/:2917/:2922 + sendJson 1 处 :878）。计数**已 stripC 剥除注释** ⇒ 只反映真实代码形态：新增第二个响应发送点会绕过 sendJson 的幂等护栏，删既有发送点同样要显式说明。⚠️ 注释里如实写明「此处统一走 sendJson，不直接 res.writeHead(」**不会**改变该计数，也不构成违规 —— 计数变化只可能来自真实发送点的增删");if(bad.length){console.error("SEC-09 体积闸不合格:\n"+bad.join("\n"));process.exit(1)}console.log("SEC-09 体积闸 ok（签名/降级分支/413+resume/sendJson 幂等/"+c1+" 调用点全覆盖/书签端点覆盖/发送点计数未越基线)")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `SEC-09 体积闸 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node tests/test-manage-skill.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node tests/test-ai-skills.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node --test tests/test-skill-picker-model.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>任一输出里出现计数非 0 的 `# fail` 行，或任一套件缺少 `# pass` 行（改签名破坏了既有端点的默认值路径会在此现形）</fails_when>
  </verify>
  <acceptance_criteria>
    - 「SEC-09 体积闸」扫描输出 ok 行；**可失败性（分条如实披露）**：其中**判既有代码的两条已做过正方向实跑 + 单点变异** —— ① `sendJson` 内 `res.writeHead(` 计数（实测 1）；② 全文件 `res.writeHead(` 的基线计数（实测 14）。变异 A：在内存中于 `sendJson` 之后插一个 `res.writeHead(` ⇒ 报「main.js 的 res.writeHead( 计数 15 ≠ 基线 14」；变异 B（合成形态，因该函数尚未落地）：`sendJson` 内「护栏在 `res.writeHead` 之后」的形态 ⇒ 报「护栏不在 res.writeHead 之前（顺序反）」，合规首行形态 ⇒ 无输出。**本轮修订新增两条已实跑的取证（针对「注释被误计为违规」这一误报面）**：③ 在 `sendJson` 后插入一条**合规注释**（内容含 `res.writeHead(` 字面量，形如「此处统一走 `sendJson`，不直接 `res.writeHead(`」）⇒ 计数仍为 **14**、无该条输出（已实测）；④ 把 `sendJson` 的护栏删成只剩 `res.headersSent` 一个条件 ⇒ 报「须**同时**具备 … 两个条件」（已实测）。门禁的计数与 token 扫描**一律经 `stripC` 剥除注释后再判**（`stripC` = 行注释优先、块注释其次、**长度保留**的空白化 —— 长度保留是必须的，否则索引会与原始源码错位）。**注意**：判据对象是「**不得新增**发送点」而非「全文件唯一」—— 既有 13 处非 JSON 发送点（媒体代理 / 静态资源 / `realm://` 静态兜底）是**基线的一部分**，不得为凑判据而删除。其余四条（签名 / 降级分支 / 413 形态 / 调用点覆盖度 / 两个常量的单源）只做过**负方向**（失败项为「签名不符 / 调用点未全部传 res / 常量缺失」等新代码未落地类），**未做** stub 正方向、**未做**单点变异。执行者落地后**必须自行**做三次变异：加回一处两参调用 ⇒ 报「总计 59 / 传 res 58」；删掉 `sendJson` 的护栏；把 `MAX_JSON_BODY_BYTES_LARGE` 的其中一处覆盖去掉 ⇒ 报「1 处」。
    - `await readJsonBody(req` 的次数与传 `res` 的次数**相等**且 ≥ 57；SUMMARY 里记录**实测的两个数**（预期 59）与逐端点评审结论。
    - 「以旧两参形态调用 ⇒ reject `BODY_TOO_LARGE` 而非 TypeError 崩溃」用例通过。
    - 四套既有套件（`test-manage-skill` / `test-ai-skills` / `test-skill-picker-model` / `test-skills-management`）全绿 —— 这是「57 个既有调用点在改签名后行为不变」的回归证据。
  </acceptance_criteria>
  <done>全部 `/api/*` POST 端点在体积闸下行为不变，两个书签端点显式放大到 32 MiB，`sendJson` 成为唯一的 **JSON** 发送点且幂等（既有 13 处非 JSON 发送点保持原样、计入基线 14），漏改调用点的后果被降级分支限制为「一个 400」。</done>
</task>

<task type="auto">
  <name>Task 3: 双入口 IPC 接线 —— 三个管理通道 + `realmAPI` 三方法 + 转发层零判定</name>
  <files>ipc-handlers.js, src/preload.js, tests/test-skills-http-api.js</files>
  <read_first>
    - `ipc-handlers.js`：`:113-122`（`assertTrustedSender` —— 拒 webview guest / DevTools / 非受管窗口）、`:1743`（**逐字注释「**不得**走 `/api/skills/*`（主窗口 `file://` 不能 fetch 本地 HTTP —— Phase 38 事故）」**）、`:1747-1753`（`ai:get-skills` 的形状 —— 三行：`assertTrustedSender(event)` → `if (!aiManager) throw new Error('AI Manager 未初始化')` → `return aiManager.<method>()`）、`:1763-1769`（`ai:refresh-skills` 同款）、`:40`（`let aiManager`）、`:2874`（`setAIManager`）、`:2927`（`module.exports`）
    - `src/preload.js`：`:1028-1043`（`getSkills` / `refreshSkills` —— 单行箭头 + `@returns` JSDoc 的形状）、`:724-726`（`onIpcMessage` 泛型透传）、`:1030-1031`（CORS 事故的既有注释）
    - `.planning/phases/50-api-skills/50-PATTERNS.md`：`### ipc-handlers.js` 与 `### src/preload.js` 两节
    - `.planning/phases/50-api-skills/50-02-PLAN.md` Task 3（两个 HTTP 写路由 —— 本任务的三通道必须转发到**同一组** manager 方法）
  </read_first>
  <action>
    1. **`ipc-handlers.js` 三个新通道**（紧挨 `ai:refresh-skills` 之后并列）：`ai:get-skills-management` / `ai:set-skill-disabled` / `ai:uninstall-skill`。每个通道**逐字照抄** `ai:get-skills` 的三行形状：首行 `assertTrustedSender(event);`（来源校验 —— **不得**省略、不得改顺序）→ `if (!aiManager) { throw new Error('AI Manager 未初始化'); }` → `return aiManager.<method>(…)`（读投影传无载荷；set-disabled 从 `(event, { name, disabled } = {})` 解构；uninstall 从 `(event, { name } = {})` 解构）。JSDoc 逐条写明「与 HTTP `/api/skills/<route>` 是**同一 manager 函数**」并接一句「主窗口 `file://` 不能 fetch 本地 HTTP ⇒ 必须走本通道」。
       **硬约束**：handler 内**零判定逻辑** —— 不出现 `kind ===` / `source ===` / `isSeededName` / `managed-skills` 之类的判据字符串。
    2. **`src/preload.js` 三个 `realmAPI.ai` 方法**（紧挨 `refreshSkills` 之后并列，同款单行箭头 + `@returns` JSDoc）：`getSkillsManagement: () => ipcRenderer.invoke('ai:get-skills-management')`、`setSkillDisabled: (name, disabled) => ipcRenderer.invoke('ai:set-skill-disabled', { name, disabled })`、`uninstallSkill: (name) => ipcRenderer.invoke('ai:uninstall-skill', { name })`。通道名必须与 `ipc-handlers.js` 逐字一致。
       ⚠️ **写侧当前无 UI 消费者**（D-17 明文）：**不得**为了让 IPC 有消费者而在主窗口新造一套管理 UI —— 本任务只接线。
    3. **`tests/test-skills-http-api.js` 补双入口源码扫描组**（这是「两入口同一权威」的可核形式）：断言三个通道名各在 `ipc-handlers.js` 出现且**每个通道段内**含 `assertTrustedSender(event)`；断言三个通道各转发到对应 manager 方法名（`getSkillsForManagement` / `setSkillDisabled` / `uninstallUserSkill`）；断言通道段内**不含**判定素材（`kind ===` / `source ===` / `isSeededName` / `managed-skills`）；断言 `src/preload.js` 的三个 API 名各绑定到正确通道；断言 `main.js` 的两个 REST 子路由转发到**同一组**方法名（跨文件一致性 —— 两入口同一权威的判据）。**再加一条 token 403 的源码契约断言**：`handleSkillsApi` 函数体首段含 `searchParams.get('token') !== REALM_TOKEN` 且其后的第一条语句是 `sendJson(res, 403, …)`（确保鉴权在任何副作用之前）。
  </action>
  <verify>
    <automated>node --check ipc-handlers.js  &&  node --check src/preload.js</automated>
    <fails_when>non-zero exit，或 stderr 出现 `SyntaxError`</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const ih=fs.readFileSync("ipc-handlers.js","utf8");const main=fs.readFileSync("main.js","utf8");const pl=fs.readFileSync("src/preload.js","utf8");const blankC=(m)=&gt;m.replace(/[^\n]/g," ");const stripC=(x)=&gt;x.replace(/(^|[^:])(\/\/[^\n]*)/g,(m,p1,p2)=&gt;p1+p2.replace(/[^\n]/g," ")).replace(/\/\*[\s\S]*?\*\//g,blankC);const channels={"ai:get-skills-management":"getSkillsForManagement","ai:set-skill-disabled":"setSkillDisabled","ai:uninstall-skill":"uninstallUserSkill"};for(const [ch,m] of Object.entries(channels)){const i=ih.indexOf("ipcMain.handle(\x27"+ch+"\x27");if(i&lt;0){bad.push("缺 IPC 通道 "+ch);continue}const seg=stripC(ih.slice(i,ih.indexOf("\n  });",i)));if(!/assertTrustedSender\(event\)/.test(seg))bad.push(ch+" 未做 assertTrustedSender（来源校验）");if(!seg.includes("aiManager."+m+"("))bad.push(ch+" 未转发到同一 manager 函数 "+m+"（两入口同一权威）");if(/kind ===|source ===|isSeededName|managed-skills/.test(seg))bad.push(ch+" 出现判定逻辑（只做转发，判定须在 manager 层）；判据已剥除注释 —— 注释里如实写明「不得出现 kind === / source === / isSeededName / managed-skills 判定」不构成违规")}for(const [api,ch] of [["getSkillsManagement","ai:get-skills-management"],["setSkillDisabled","ai:set-skill-disabled"],["uninstallSkill","ai:uninstall-skill"]]){if(!new RegExp("\\b"+api+":").test(pl))bad.push("preload 缺 realmAPI.ai."+api);else{const k=pl.indexOf(api+":");if(!stripC(pl.slice(k,k+160)).includes(ch))bad.push("preload."+api+" 未绑定通道 "+ch)}}for(const r of ["set-disabled","uninstall"])if(!new RegExp("route === \x27"+r+"\x27").test(main))bad.push("handleSkillsApi 缺子路由 "+r);if(!main.includes("await aiManager.setSkillDisabled(name, disabled)"))bad.push("HTTP set-disabled 未转发到 setSkillDisabled");if(!main.includes("await aiManager.uninstallUserSkill(name)"))bad.push("HTTP uninstall 未转发到 uninstallUserSkill");if(!/sendJson\(res, 400, \{ error: err\.message, code: err\.code \|\| undefined \}\)/.test(main))bad.push("错误形状缺 { error, code }（UI 按 code 查表）");const mi=main.indexOf("async function handleSkillsApi(");const mh=stripC(main.slice(mi,mi+700));if(!/searchParams\.get\(\x27token\x27\)\s*!==\s*REALM_TOKEN/.test(mh))bad.push("handleSkillsApi 首段缺 token 鉴权");if(!/sendJson\(res, 403/.test(mh))bad.push("token 不通过时未答 403（鉴权必须早于任何副作用）");const mgr=fs.readFileSync("ai-skills-manager.js","utf8");if(!/function validateDisabledListForSettings\(/.test(mgr))bad.push("缺 validateDisabledListForSettings（D-10 的列表校验单源）");if(!/module\.exports\s*=\s*\{[\s\S]*?\bvalidateDisabledListForSettings\b/.test(mgr))bad.push("validateDisabledListForSettings 未导出");if(bad.length){console.error("双入口与写路由不合格:\n"+bad.join("\n"));process.exit(1)}console.log("双入口与写路由 ok（三 IPC 通道 + preload + 两 REST 子路由 + 同一 manager 函数 + token 403 前置 + code 回传）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `双入口与写路由 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 「双入口与写路由」扫描输出 ok 行；**可失败性（只做过负方向，未做 stub 正方向与单点变异）**：落地前对当前树实跑，失败项为「缺 IPC 通道 / handleSkillsApi 缺子路由」等新代码未落地类（该门禁无任何判既有代码的断言）。执行者落地后**必须自行**做三次变异确认转红：删掉任一通道的 `assertTrustedSender(event)`；把某通道的转调改成别的 manager 方法；把 token 校验挪到函数体后半段。
    - 三个通道与两个 REST 子路由**转发到同一组方法名**（跨文件断言在同一门禁内通过）。
    - `src/preload.js` 的三个方法名与通道名逐字一致；`node --check` 通过。
    - 通道段内零判定素材（`kind ===` / `source ===` / `isSeededName` / `managed-skills` 全部不出现）。**判据已剥注释**，两条已实测（合成 `ipc-handlers.js`）：段内保留一条如实写明「不得出现 `kind ===` / `source ===` / `isSeededName` / `managed-skills` 判定」的注释 ⇒ **不**转红；段内真写 `if (skill.kind === 'user')` ⇒ 报「出现判定逻辑」⇒ 转红。
    - SUMMARY 记录「IPC 写侧当前无 UI 消费者」这一事实（D-17），且**没有**新增任何主窗口管理 UI。
  </acceptance_criteria>
  <done>两个前端入口读写对等：设置页经 `/api/skills/*` + token，主窗口经 `realmAPI` 的三个新方法与三个 IPC 通道，两者转发到同一 manager 函数且 handler 零判定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 客户端 → `realmServer` 的请求体 | 体积**不受信**（Phase 51 的 zip base64 会把它放大到数十 MiB）；`Content-Length` 可伪造 / 可缺失 |
| 主窗口 renderer / 设置页 guest → IPC 通道 | IPC **不校验调用者身份即等同主机能力**；`assertTrustedSender` 是唯一控制面 |
| 响应写入路径（13 处宿主） | `async` 服务器回调中的二次写头 = unhandled rejection；本仓无全局兜底 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-16 | Denial of Service | 无上限读入请求体（zip base64 放大） | high | mitigate | `readJsonBody` 累积中判 + 立即停收（`if (rejected) return`）+ 413 + `req.resume()`；默认 1 MiB fail-closed，需大者**显式**放大。行为组③断言堆增量远小于 body 体积（可失败的数字比较） |
| T-50-17 | Denial of Service | 拒收路径本身崩掉主进程 | high | mitigate | 两条：① `sendJson` 幂等护栏（吸收 13 处 `catch` 的二次发送）；② `res` 缺失降级分支（把 data 监听器内的 `TypeError` 降为外层 catch 的 400）。行为组②断言 `unhandledRejection` 计数为 0。**不得**新增全局兜底（那会掩盖真实错误并让降级分支失去可验证性） |
| T-50-18 | Denial of Service | 413 不可达（客户端只拿到网络错误） | high | mitigate | 禁用 `req.destroy()`；不设 `Connection: close`；用 `req.resume()` 排水。行为组①断言客户端拿到 413 + 可解析 JSON；行为组④以 `destroy` 形态作**反向对照**证明该断言能失败 |
| T-50-19 | Tampering | 新增第二个响应发送点（绕过 `sendJson` 幂等护栏） | medium | mitigate | 门禁把 `main.js` 的 `res.writeHead(` 总数冻结在**实测基线 14 处**（既有 13 处非 JSON 发送点 + `sendJson` 1 处，行号登记在判据注释里），并断言 `sendJson` 函数体内恰 1 处且幂等护栏在其**之前**。新增或删减发送点都会转红 —— 注意判据对象是「**不得新增**」而非「全文件唯一」 |
| T-50-20 | Elevation of Privilege | 未受信的 IPC 来源（webview guest / DevTools / 非受管窗口）调用管理写通道 | high | mitigate | 三个通道首行 `assertTrustedSender(event)`；门禁断言每个通道段内含该调用，且断言其是**首行**（通道段内位置） |
| T-50-21 | Tampering | 两入口判定漂移（一个入口放行、另一个拒绝同一操作） | high | mitigate | handler 零判定（门禁断言通道段内无判定素材）+ 跨文件断言「两入口转发到同一组方法名」 |
| T-50-22 | Repudiation | 文档 / 注释把不可复现的因果写成事实（「设 `Connection: close` 就 EPIPE」） | low | mitigate | 代码注释与产品文档只写**可复现**的规则（「不 destroy、不设 `Connection: close`、用 `resume`」）。该诚实约束写进本计划的 prohibition 与 50-05 的文档验收 |
| T-50-23 | Denial of Service | 两个书签导入端点在 1 MiB 默认下 413（静默破坏既有功能） | high | mitigate | 两处显式传 `{ maxBytes: MAX_JSON_BODY_BYTES_LARGE }`（32 MiB）；门禁断言恰 2 处覆盖。SUMMARY 记录逐端点评审结论（research 要求的「不能按『都是小 JSON』批量放行」） |
| T-50-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本阶段**零新增依赖**，无安装动作 ⇒ 无供应链面 |
</threat_model>

<verification>
- 三个任务的 automated 门禁 全绿；**四套既有套件 + 新建 HTTP 套件**一并跑通（改签名后的默认值路径回归）。
- 本计划 3 条门禁**逐条如实披露**（不得整段声称已预跑）：3 条都只在**当前树**上做过负方向实跑（失败项一律为「新代码尚未落地」类）；**已被正方向实跑 + 内存单点变异取证**的只有 50-03-T2 那一条门禁里**判既有代码**的两个子判据 —— `sendJson` 内 `res.writeHead(` 计数（实测 1）+ 全文件基线计数（实测 14）。其余全部（签名 / 降级分支 / 413 形态 / 调用点覆盖度 / 常量单源 / 三 IPC 通道 / 两 REST 子路由 / token 403 前置 / `{ error, code }` 形状）**未做** stub 等价树正方向、**未做**单点变异 —— 变异义务已写进各任务 `<acceptance_criteria>`，执行者必须自行跑。
- **本轮修订（iteration 2）的门禁改动与新增证据**：① 凡计数 / token 禁令（全文件 `res.writeHead(` 基线、`sendJson` 内的发送点与护栏、`await readJsonBody(req` 调用点、两个常量单源、三 IPC 通道段内的判定素材、`main.js` 的全局异常兜底）一律改为**先剥注释再判**（`stripC`：**行注释优先**、块注释其次、**长度保留**的空白化 —— 「行注释优先」与「长度保留」两条都是必须的，`main.js:315` 的 `// … 与 /api/* 一致 …` 里的 `/*` 曾让朴素实现吃掉 1235 字符的**真实代码**，使基线计数从 14 掉到 12）。② `sendJson` 的护栏判据从「逐字正则 + 单行形态」改为「同时具备 `res.headersSent` 与 `res.writableEnded` 两个条件 **且** 位于 `res.writeHead(` 之前」—— 写法形态（一行 / 多行 / 括号）不再被钉。③ `main.js` 的全局兜底判据从「全文件出现 `uncaughtException` 子串」改为**注册形态** `process.on/once('uncaughtException'|'unhandledRejection')`。**已实跑的正/反证据（均锁定旧形态与新形态的差别）**：`sendJson` 后插一条**含 `res.writeHead(` 的合规注释** ⇒ 计数仍 14、不转红；插一个真发送点 ⇒ 报「计数 15 ≠ 基线 14」；护栏删到只剩 `res.headersSent` ⇒ 报「须同时具备两个条件」；护栏置于 `res.writeHead(` 之后 ⇒ 报「顺序反」；`readJsonBody` 前写一条含 `process.on('uncaughtException')` 字面量的**注释** ⇒ 不转红、真写注册 ⇒ 转红；合成 `ipc-handlers.js` 的通道段内保留一条写明「不得出现 `kind ===` …」的注释 ⇒ 不转红、段内真写判定 ⇒ 转红。
- **诚实边界**：SEC-09 的探针只在系统 Node v22.22.0 上跑过，运行时是 Electron 43.6.0（内 Node 24.20.0）。本计划把探针落成可重跑的 `node:test` 用例；**在 Electron 内复跑一次**属人工验证（`50-VALIDATION.md` 的 Manual-Only 表）。SUMMARY 必须如实披露「自动用例跑在系统 Node 上」。
- **不得**写「`Connection: close` 会导致客户端 EPIPE」；该结论在重研会话中不可复现。
- counts-parity 在本波仍**预期为红**（两个新套件尚未入账本、既有套件例数已变）—— 由 50-05 一次性收口。
</verification>

<success_criteria>
- 超过上限的 POST body 在 `/api/*` 层被拒：客户端拿到 **413 + JSON**，堆不随 body 线性增长，且主进程不退出（`unhandledRejection` 计数为 0）。
- 「导入书签」在 32 MiB 以内的文件上仍正常工作（不被新的默认上限打断）。
- 37 个既有端点与 60 余处调用点在改签名后行为不变（既有全量套件绿）。
- 主窗口可经 `realmAPI` 读到与管理面同一份投影、执行同一套启停 / 卸载（与 HTTP 入口同一权威）。
</success_criteria>

<output>
Create `.planning/phases/50-api-skills/50-03-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号由本计划新建**（或改签名），不是既有符号的漂移。评审时不得要求它们在 base 上存在。（50-01 已登记的符号不在此重复；本阶段的完整清单以 50-01 的 §「Artifacts this phase produces」为准。）

| 类别 | 符号 | 落点 |
|------|------|------|
| 常量 | `MAX_JSON_BODY_BYTES`（`1024 * 1024`，`/api/*` POST 的全局默认，**单源**） | `main.js` |
| 常量 | `MAX_JSON_BODY_BYTES_LARGE`（`32 * 1024 * 1024`，书签导入端点专用，**单源**） | `main.js` |
| 函数签名（**改签名**） | `readJsonBody(req, res, { maxBytes = MAX_JSON_BODY_BYTES } = {})` —— `res` 为第二位置参（方案 A） | `main.js` |
| 降级分支 | `res` 缺失 ⇒ 不答响应、只 reject `{ code: 'BODY_TOO_LARGE' }`（强制项） | `main.js` |
| 错误码（**新引入的字符串值**） | `BODY_TOO_LARGE`（`readJsonBody` reject 的 `err.code`；同时也是设置页失败文案白名单的一键） | `main.js`（消费方在 `src/settings-page.js`） |
| 幂等护栏 | `sendJson` 首行 `if (res.headersSent || res.writableEnded) return;` | `main.js` |
| 请求头快路径 | `req.headers['content-length']` 预检（可选增强，**不得取代**累积中判） | `main.js` |
| 常量/端点覆盖 | `POST /api/favorites/import-chrome` 与 `POST /api/favorites/import-html` 显式传 `{ maxBytes: MAX_JSON_BODY_BYTES_LARGE }` | `main.js` |
| IPC 通道 | `ai:get-skills-management` | `ipc-handlers.js` |
| IPC 通道 | `ai:set-skill-disabled` | `ipc-handlers.js` |
| IPC 通道 | `ai:uninstall-skill` | `ipc-handlers.js` |
| preload API | `realmAPI.ai.getSkillsManagement()` | `src/preload.js` |
| preload API | `realmAPI.ai.setSkillDisabled(name, disabled)` | `src/preload.js` |
| preload API | `realmAPI.ai.uninstallSkill(name)` | `src/preload.js` |
| 测试内容 | `tests/test-skills-http-api.js` 的 SEC-09 组（真 `http.createServer` harness：413 + JSON / `unhandledRejection` 计数 0 / 堆增量比值 / `destroy` 反向对照）+ 降级分支组 + 调用点覆盖度组 + 双入口源码扫描组 | `tests/test-skills-http-api.js` |

