---
phase: 50-api-skills
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - ai-skills-manager.js
  - ai-manager.js
  - main.js
  - tests/test-skills-management.js
  - tests/test-ai-skills.js
  - tests/test-manage-skill.js
  - tests/test-skills-http-api.js
autonomous: true
requirements: [USER-02, USER-06]
user_setup: []

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

key-decisions:
  - '**「仅 user 可卸载」的判据口径 = 「`skills/<name>` 存在且 `kind === ''directory''`」（OQ-1 已由用户裁决 2026-09-14）**：删除对象**恒为** `agent-workspace/skills/<name>/`；**不得**把「`managed-skills/<name>` 是否存在」用作拒绝条件 —— 同名双存在（user + managed 同名）时 user 条目**确实存在**（user 胜出、managed 被 `applyShadowing` 标 `shadowed` 保留在数据层），列表里显示的 tier 就是 `user`；按「后者存在即拒」会让用户点自己列表里那条「我的技能」时被告知「这不是你的技能」，与 ROADMAP 判据 3 直接冲突。`managed-skills/<name>` 的存在**只用来提示**「同名内置 / 托管技能将在删除后重新可见」'
  - '**判据必须用 `env.fileInfo` 而不是 `env.exists`**：SDK 的 `exists` 实现是「`fileInfo` 成功即 `ok(true)`」（`nodejs.js:584-591`）⇒ 一个**普通文件** `skills/foo`（不是目录）也会让 `exists` 返回 `true`，而此时 `env.remove(dir, {recursive:true})` 会删掉那个文件。D-07 的字面判据是「存在**且 `kind` 是目录**」⇒ 只有 `fileInfo` 能表达'
  - '**判据一律读盘，不用 `_cache`**：bash 可随时改写磁盘（P8 第 6 条），缓存只反映上次重扫的时刻。用例必须覆盖「暖缓存 → 从盘删目录 → 调卸载 ⇒ `not_found`」这条**读盘而非快照**的判据'
  - '**不得复用 `resolveManagedTarget()`**：它的顺序是「seeded 保护 → 同名 user 存在即拒（`user_owned_conflict`）→ managed 不存在即 `not_found`」，方向与本阶段**相反**（那是 managed 视角的「用户撞名保护」）。本阶段需要一处显式的「仅 user 可删」判据，形状模板只借 `deleteManagedSkill()` 的四段结构（校验 → 读盘判定 → `env.remove({recursive:true})` → 失败折叠 `UNKNOWN`）'
  - '**第十码 `not_user_owned` 只加进 `MANAGE_SKILL_ERROR`，`MANAGE_SKILL_SHORT_REASON` 保持恰 9 键不动**（OQ-3）：新码在本阶段**只经 HTTP 400 的 `{code}` 返回**，不进 `manage_skill` 的工具卡片渲染路径。`MANAGE_SKILL_ERROR` 实测 **10 键**（九码 + `UNKNOWN`），加码后 **11 键**；`tests/test-manage-skill.js:1588-1621` 的两条 `deepStrictEqual` 冻结断言必须**同批**改为「恰十一键 / 十码」，且 `:1588` 的 describe 标题（「本计划不增不减不改名」）与新设计失实、须一并改写'
  - '**管理面名称谓词取「安全超集」，不得复用 `validateManagedSkillName`**（OQ-2）：加载管线对磁盘上的技能名**故意宽松**（46 D-08），用严格形态 `^[a-z0-9-]+---
 会让「手动放进 `skills/My_Skill/` 的技能无法被禁用」（设置页开关点了必然 400）。新谓词 `validateSkillNameForManagement(name)` = 非空字符串 + ≤ `MAX_SKILL_NAME_CHARS`(64) + 无 `/` `\` 与控制字符；返回形状与 `validateManagedSkillName` **同形**（`{ok:true,value} | {ok:false,code:INVALID_NAME,reason}`）以便同样经 `makeManageSkillError` 构造。三处消费（`/api/settings/update` 校验、`set-disabled`、`uninstall`）**必须同宽**'
  - '**禁用名单的列表级校验也住 manager 模块**：`validateDisabledListForSettings(list)` 返回 `{valid, reason}`，规则 = 数组 + 每项经 `validateSkillNameForManagement` + 条数 ≤ `LIMITS.MAX_USER_SKILLS + LIMITS.MAX_MANAGED_SKILLS`（= 100，`[VERIFIED: ai-skills-manager.js:48-54]` 的两项之和，**不得改既有五项数值**）。`main.js` 直接 `require` 该模块（零 electron 依赖 ⇒ 可被主进程直接 require，`AGENTS.md:273` 硬约束 ① 已为该用法背书）'
  - '**写路径收口 = 「重扫恰一次」+「调用侧补播恰一次」**（D-18）：写成功后调 `await this.ensureSkillsFresh()` —— 有 Agent 时它内部走 `syncAgentSystemPrompt()`（该函数体内**已含**恰一次 `refreshSkills`），无 Agent 时直接 `refreshSkills`；**不得两条都执行**（会变成两次全量重扫，49-01 的 L 组断言 `rescanCalls === 2` 而非 3 已付过这个代价）。随后在**调用侧**无条件补一次 `windowManager.broadcast(''''skills:changed'''')`'
  - '⚠️ **补播的理由不是「prompt 不变 ⇒ 不广播」**（那是推不出来的：`computeDigest` 逐条含 `disabled` / `overLimit` / `shadowed` ⇒ 禁用任何在缓存里的技能都会改 digest ⇒ **会**广播）。**真实理由是覆盖 `syncAgentSystemPrompt()` 的忙时早退**：`isProcessing || streaming` 时它只置 `_skillsPromptDirty = true` 并 **return**（该分支**不广播**），要等下一次成功出口的 `_flushDeferredSkillsPrompt()` 才落地；而设置页点开关**不经过 Agent 轮次** ⇒ 用户若不再发消息，`/` 面板会一直显示已被禁用的技能。**验收面因此是「忙时（`isProcessing === true`）补播仍然发出」**'
  - '**不得改 `syncAgentSystemPrompt()` 的函数体**：46-04 的方法体源码扫描断言与 48 的广播次数断言（`tests/test-ai-skills.js:1239` / `:3289` / `:3455`）同时钉着它。补播**幂等**（48-06 已把 renderer 的监听改成无条件重拉快照，`src/renderer.js:4405`）⇒ 多播一次零副作用；**不得**为省这一次广播去改被钉住的函数体'
  - '**D-09 的派生不变式必须实现**：卸载一个 user 技能成功后**必须同步从 `settings.aiSkills.disabled` 移除该名字**。否则残留名单会随技能名复用而误伤 —— 同名新技能一装上就被静默禁用，属「静默失效」，比功能本身更需要测试。启用 / 禁用单条时无需清理（技能仍在盘上）'
  - '**载荷形状取增量 `{name, disabled}`，不取全量 `{disabled: [...]}`**（Claude''''s Discretion 建议 + 实现层理由）：读-改-写全程在**主进程内**同步完成（`configStore.get` → 算出新数组 → `configStore.set`，中间无 `await`）⇒ 两个并发的设置页操作不会互相覆盖；全量载荷会在并发下丢更新'
  - '**`configStore` 的 `set` 是同步的，且 `ai-manager` 与 `main.js` 共享同一个 `Store` 实例**（`main.js:112` 建实例 → `aiManager.init(configStore)` 注入，`ai-manager.js:778`）⇒ 写名单后立刻重扫必然读到新值（三处读取点 `:904` / `:3031` / `:3147`）'

must_haves:
  truths:
    - '[USER-02] 用户可对单个技能启用 / 禁用：禁用写 `settings.aiSkills.disabled`（名字数组）、**文件仍在盘上**（禁用只过滤不删文件）、重新启用即从名单移除并恢复；禁用后该技能不再进 system prompt（`refreshSkills` 的 ⑤ 标记）与 `/` 面板（`filterPickerItems` 跳过 `s.disabled === true`，该过滤**已存在、不重判**）'
    - '[USER-02] 禁用的技能**仍可卸载**（卸载不因 `disabled` 而置灰）—— 禁用是「先别进 prompt 试试」的轻量试探，卸载是「确实不要了」的终局；要求用户先启用再卸载是反直觉的两步操作'
    - '[USER-06] 卸载**仅允许** `source === ''''user''''` 的技能；对内置 / 托管技能的卸载请求被拒绝 —— 且**手改 URL 直接调端点也不例外**（判据在 **manager 层**、不经 HTTP handler 的友善包装）。前端不渲染按钮只是 UX，两层各司其职'
    - '[D-07] 卸载的**三态拒绝面**：① `skills/<name>` 不存在（含「存在但读不到」/「存在但不是目录」）⇒ `not_found`；② 同名双存在（user + managed 同名）⇒ **允许**卸载且删除成功（`skills/<name>` 是唯一删除对象）；③ `skills/<name>` 不存在而 `managed-skills/<name>` 存在 ⇒ `not_user_owned`（第十码）。**卸载响应不携带 `shadowNotice` 一类的提示字段** —— 「同名内置 / 托管技能将在删除后重新可见」这句提示由 **50-04 在删除前的确认弹框**（条件追加行，纯数据查找投影的 `shadowed` / `shadowedBy`）承担；响应只回 `{ name, filePath, action }`（**不新增没有消费者的字段** —— 无消费者的字段必然腐化成第二次实现）'
    - '[D-08] 新增第十码 `not_user_owned` 到 `MANAGE_SKILL_ERROR`；`MANAGE_SKILL_SHORT_REASON` **保持恰 9 键不动**（那是**工具面**的表，新码不经 `manage_skill` 的卡片路径）'
    - '[D-09] 卸载成功后 `settings.aiSkills.disabled` 中的同名条目被移除 —— 派生不变式；用例须覆盖「卸载后装上同名新技能不会被静默禁用」'
    - '[D-10] `/api/settings/update` 对 `aiSkills.disabled` 与 `aiSkills` **两种键形态**都做服务端校验（非数组 / 非字符串项 / 超长 / 含路径分隔符或控制字符 / 超条数 ⇒ 400 且**不落盘**）。只覆盖点号键会让手改 URL 提交 `{ "aiSkills": { "disabled": [...] } }` 整体覆写 `settings.aiSkills` 对象'
    - '[D-18] 写路径收口：写成功后重扫**恰一次**（有 Agent 时经 `ensureSkillsFresh()` → `syncAgentSystemPrompt()`；无 Agent 时直接 `refreshSkills`）+ **调用侧无条件补播恰一次** `windowManager.broadcast(''''skills:changed'''')`；`syncAgentSystemPrompt()` 的函数体**逐字未改**'
    - '[D-18] **忙时（`isProcessing === true`）补播仍然发出**：置忙后调管理写方法 ⇒ 广播频道含 `skills:changed` 恰一次，且 `agent.state.systemPrompt` 未变（忙时只置脏标记）。这条是补播存在的**唯一理由**，必须有独立用例'
    - '[OQ-2] 管理面名称谓词与列表的来源谓词**同宽**：`skills/My_Skill/` 这类名单里的技能，其开关**必须可操作**（不得 render 出一个「点了必然 400」的开关）；「禁用名单是消费侧信号、不需要合法到能写盘」这条**故意的不对称**必须成文'
    - '[assumption:USER-02 边界] 同一技能被两个设置页实例并发操作时不互相覆盖 —— 增量载荷 + 主进程内同步读-改-写保证最后一次写不丢前一次的改动（不是「后写覆盖先写」的全量语义）'
    - '[assumption:USER-06 边界] 卸载的失败路径不留半成品：`env.remove(dir, {recursive:true})` 失败时**不清理**任何 `disabled` 名单条目（名单只在删盘成功后才清），且错误折叠为 `UNKNOWN`（沙箱原始码不泄漏给调用方）'
  artifacts:
    - 'ai-skills-manager.js：`MANAGE_SKILL_ERROR.NOT_USER_OWNED`、`deleteUserSkill(env, {name})`（新导出）、`validateSkillNameForManagement(name)`（新导出）、`validateDisabledListForSettings(list)`（新导出）'
    - 'ai-manager.js：`setSkillDisabled(name, disabled)`、`uninstallUserSkill(name)`（两个新方法，均含「重扫恰一次 + 调用侧补播恰一次」）'
    - 'main.js：`handleSkillsApi` 的 `set-disabled` / `uninstall` 两个 POST 子路由；`handleSettingsApi` 的 `update` 校验循环新增 `aiSkills.disabled` / `aiSkills` 双键分支'
    - 'tests/test-skills-management.js：卸载三态 / 名单清理 / 读盘判据 / 谓词值域 / 投影 `disabled` 字段等用例'
    - 'tests/test-ai-skills.js：写路径次数账与**忙时补播**用例组（复用既有 `promptCtx` / `captureBroadcasts` 夹具）'
    - 'tests/test-manage-skill.js：`MANAGE_SKILL_ERROR` 的「恰十一键 / 十码」冻结断言刷新'
    - 'tests/test-skills-http-api.js：**新建**套件（本计划落基础骨架与 `/api/settings/update` 双键校验用例；SEC-09 的 413 组由 50-03 续写）'
  key_links:
    - '两入口同一权威：`main.js` 的 `set-disabled` / `uninstall` 子路由与 50-03 的三个 IPC 通道**转发到同一组 manager 方法**（`aiManager.setSkillDisabled` / `aiManager.uninstallUserSkill` / `aiManager.getSkillsForManagement`）；handler **只是转发层**，判据 / 校验 / 写函数全在 `ai-skills-manager.js`'
    - '单源链：`ai-skills-manager.js` 的三个新导出（谓词 / 列表校验 / 卸载判据）→ `main.js` 的 `handleSkillsApi` 与 `handleSettingsApi` 的 `update` 分支 → 同一份判据。**不得**在 `main.js` 或设置页另写一份正则'
    - '错误形状链：`makeManageSkillError(code, message, extra)` → `err.code` → `handleSkillsApi` 的 `catch` → `sendJson(res, 400, { error, code })` → 设置页按 `code` 查表（**不解析 `message`**）'
    - '失效链（写侧）：configStore.set（同步）→ `ensureSkillsFresh()`（重扫恰一次）→ 缓存 `disabled` 标记 + digest 变化 → `syncAgentSystemPrompt()` 改写 prompt 并广播 → 调用侧补播 → 主窗口 renderer 的 `skills:changed` 监听无条件重拉快照 → `/` 面板隐藏该技能'
    - '⚠️ **诚实边界**：`windowManager.broadcast` 只发到各 BrowserWindow 的 webContents，**不到 webview guest**。设置页**收不到**任何广播 ⇒ 每次操作后用响应体回传的最新投影就地重渲染（零二次请求），进入页面时重拉一次；多个设置页实例之间**不做**即时同步（不新增 guest push 通道）。这条须写进产品文档'
  prohibitions:
    - statement: '不得把「`managed-skills/<name>` 是否存在」用作卸载的**拒绝**条件'
      status: unresolved
      verification: judgment
      reason: '同名双存在时 user 条目确实存在、列表里显示的 tier 就是 `user`；按「后者存在即拒」会让用户点自己列表里那条「我的技能」被告知「这不是你的技能」，与 ROADMAP 判据 3「卸载仅允许 `source === ''user''` 的技能」直接冲突。`managed-skills/<name>` 只作提示（由 50-04 的确认弹框条件追加行承担），不作拒绝'
    - statement: '不得把 `not_user_owned` 加进 `MANAGE_SKILL_SHORT_REASON`，也不得改写 `validateManagedSkillName`'
      status: unresolved
      verification: judgment
      reason: '前者被 `tests/test-skill-picker-model.js:946` 硬断言为**恰 9 键**（那是工具面短原因表，与管理面错误码是两张表）；后者是 `manage_skill` 的**唯一**写入侧闸（49-01 明文「写权威与读权威同源」），改写它会让合法技能被拒或被非法技巧放行'
    - statement: '不得在 `syncAgentSystemPrompt()` 的函数体内新增广播、新增 `refreshSkills()` 调用，或把补播挪进去'
      status: unresolved
      verification: judgment
      reason: '46-04 的方法体源码扫描断言与 48 的广播次数断言（`tests/test-ai-skills.js:1239` / `:3289` / `:3455`）同时钉着它；改函数体会同时打翻两套断言，且让「忙时只置脏」的语义不可推理'
    - statement: '不得用缓存快照（`_cache.skills` / `getSkillsSnapshot()`）作为卸载判据'
      status: unresolved
      verification: judgment
      reason: 'bash 可随时改写磁盘（P8 第 6 条），缓存只反映上次重扫的时刻。用缓存会让「盘上已删、缓存还在」时删一个不存在的目录（或反之放行一个已消失的目标），且这条错误只在真实使用中出现、单测不易暴露'
    - statement: '不得为管理面新建第二个名称校验器（正则 / 谓词 / 列表校验任一层）'
      status: unresolved
      verification: judgment
      reason: 'ROADMAP 的 `Depends on: Phase 49` 原文就是「复用同一份 name / description / 大小 / 注入校验器，避免第二份实现漂移」；`AGENTS.md:273` 硬约束 ① 明写「Phase 50/51 直接 require 同一份，不得另写第二份」。第二份必然与第一份漂移，而漂移的后果是「列表里有的技能开关点了 400」这类用户可见的失实'
    - statement: '不得给 `manage_skill` 的三个动作或管理面的启停 / 卸载加「AI 侧确认」以外的第二套确认机制；也不得在 `manage_skill` 的工具路径上引入管理面错误码'
      status: unresolved
      verification: judgment
      reason: '49 D-01 已锁定：给 **AI 工具**加确认挡不住 AI（它本可经 `write`/`bash` 直改同一路径，不完整的保护是负面价值）；而给人加确认（设置页卸载的二次确认）有效，因为人类点击没有绕道路径 —— 两者论据正交，不得互相"统一口径"'
---

<objective>
把 **Phase 50 的写路径**从数据权威一路做到 HTTP 端点：用户可启用 / 禁用单个技能、卸载**仅属于自己的**技能；拒绝判据落在 `ai-skills-manager.js`（零 electron、可单测），**手改 URL 直接调端点也不例外**；写成功后技能集与 system prompt 在本轮收口，并通过一次**调用侧补播**覆盖「忙时只置脏不广播」的分支。

Purpose: 这是 ROADMAP 判据 2 与判据 3 的全部实现面，也是 `STATE.md:292` 的 ⚠️（`syncAgentSystemPrompt()` 的生产调用方）从 **1/3 走到 2/3** 的那一份。三条判断只在注释里活着、被后续阶段当成冗余删掉就会静默退回：① `computeDigest` 的字段集（跨窗口失效链的承重）；② 补播必须在**调用侧**（函数体被两套断言钉住）；③ 卸载判据必须**读盘**且**只认 user 目录**。

Output: manager 侧的第十码与三个新导出（卸载 / 名称谓词 / 名单校验）、`ai-manager.js` 的两个写方法与失效链收口、`main.js` 的两个写路由与 `/api/settings/update` 双键校验、四份测试（三个既有套件的增补 + 新建 HTTP 套件的骨架）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-api-skills/50-CONTEXT.md
@.planning/phases/50-api-skills/50-RESEARCH.md
@.planning/phases/50-api-skills/50-PATTERNS.md
@.planning/phases/50-api-skills/50-01-PLAN.md
@ai-skills-manager.js
@ai-manager.js
@main.js
@src/skill-picker-model.js
@tests/test-manage-skill.js
@tests/test-ai-skills.js
@tests/test-skills-management.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: manager 写层 —— 仅 user 可卸载的三态判据 + 第十码 + 两个管理面谓词</name>
  <files>ai-skills-manager.js, tests/test-skills-management.js, tests/test-manage-skill.js</files>
  <read_first>
    - `ai-skills-manager.js`：`:960-996`（`MANAGE_SKILL_ERROR` 的十键与 `makeManageSkillError` —— **九码是闭合白名单，`UNKNOWN` 是沙箱层兜底**）、`:1008-1011`（`isSeededName`）、`:1027-1063`（`validateManagedSkillName` —— **只作对照理解「写入门严」的形态，不得复用、不得改写**；其 JSDoc 逐字写明「**故意的不对称：写入门严、读入门宽**（D-06）…本校验器**只被写入侧调用**」）、`:1248-1272`（`sandboxErrorCode`）、`:1317-1321`（`managedSkillPaths` 的 `path.join` 派生形状）、`:1367-1399`（`resolveManagedTarget` —— **明示不得复用**，读它以确认方向相反）、`:1603-1623`（`deleteManagedSkill` —— **四段结构的形状模板**：校验 → 读盘判定 → `env.remove(destDir, {recursive:true})` → 失败折叠 `UNKNOWN`）、`:1659-1687`（导出面与分段注释风格）、`:62`（`MAX_SKILL_NAME_CHARS = 64` —— 长度上限的单源）
    - `agent-workspace.js`：`:102-104` / `:113-115`（两个目录的**唯一**派生点）、`:286-290`（`fileInfo`）、`:309-313`（`exists`）、`:321-325`（`remove`）
    - `node_modules/@earendil-works/pi-agent-core/dist/harness/env/nodejs.js`：`:43-51`（`kind` 三值域）、`:538-546`（`fileInfo` 用 `lstat`）、`:584-591`（`exists` 对普通文件也返回 `true`）、`:602-611`（`remove` 的 `recursive` 默认 **false**，必须显式传）
    - `tests/test-manage-skill.js`：`:1588-1621`（两条 `deepStrictEqual` 冻结断言：键集合恰十条、值集合恰十个 —— **本任务要改的就是它们**）、`:1624-1639`（「不新立第十码」用例 —— 新设计下该用例的语义要重新表述，见 action 第 4 点）、`:1-86`（测试骨架，新增用例沿用同一夹具）
    - `.planning/phases/50-api-skills/50-RESEARCH.md`：§Pattern 6（`deleteUserSkill` 的完整骨架与「三态拒绝面」表；注意骨架里用的是 `env.fileInfo` —— 旧 RESEARCH 只用 `exists`，**必须改**）、§Pitfall 9（`settings.aiSkills.disabled` 的两种键形态与超集谓词）
  </read_first>
  <action>
    1. **加第十码**：在 `MANAGE_SKILL_ERROR` 的 `UNKNOWN: 'unknown',` **之后**新增 `NOT_USER_OWNED: 'not_user_owned',`。**既有十键的键名与取值一个字都不许动**。表上方的 JSDoc 补一句：`NOT_USER_OWNED` 是**管理面**（设置页卸载）专用的第十码，只经 HTTP 400 的 `{code}` 返回，**不进** `MANAGE_SKILL_SHORT_REASON`（那张表只覆盖工具面、保持恰 9 键）。
    2. **新增 `validateSkillNameForManagement(name)`**（安全超集谓词，OQ-2）：返回形状与 `validateManagedSkillName` **同形**（`{ok:true, value}` / `{ok:false, code: MANAGE_SKILL_ERROR.INVALID_NAME, reason}`）。判据：① 必须是字符串；② `trim()` 后非空；③ 长度 ≤ `MAX_SKILL_NAME_CHARS`(64)（复用既有常量，**不得**新写一个 64）；④ **不含** `/`、`\` 与控制字符（`\u0000`-`\u001f`）。**不加** `^[a-z0-9-]+$`、**不加**首尾连字符与连续连字符判据。JSDoc 必须写明这条**故意的不对称**：加载管线对磁盘上的技能名故意宽松（46 D-08），严格谓词会让「手动放进 `skills/My_Skill/` 的技能无法被禁用」（设置页开关点了必然 400）；禁用名单是「按名字过滤」的消费侧信号，**不需要名字合法到能写盘**（OQ-2 要求成文）。
    3. **新增 `validateDisabledListForSettings(list)`**：返回 `{valid: true}` / `{valid: false, reason}`。判据：① `Array.isArray(list)`；② 条数 ≤ `LIMITS.MAX_USER_SKILLS + LIMITS.MAX_MANAGED_SKILLS`（= 100 —— 两个既有数值之和，**不得改既有五项数值**；若需独立常量则**另立**）；③ 每项经 `validateSkillNameForManagement` 且 `ok === true`。**不得**用 `validateManagedSkillName`。
    4. **新增 `deleteUserSkill(env, { name } = {})`**（照 `deleteManagedSkill` 的四段结构，但第 2 段换成 D-07 的读盘判据）：
       - 段 1：`const skillName = typeof name === 'string' ? name.trim() : name;` → `validateSkillNameForManagement` 失败即 `throw makeManageSkillError(nameCheck.code, nameCheck.reason)`。
       - 段 2（**判据核心**）：`const userDir = path.join(getAgentWorkspaceLazy().getSkillsDir(), skillName);` → `const info = await env.fileInfo(userDir);` → `const isDir = info && info.ok === true && info.value && info.value.kind === 'directory';`
         - `isDir === true` ⇒ 允许（**不因 `managed-skills/<name>` 存在而拒绝**）。**也不必**为「提示」去 `exists` 查 `managed-skills/` —— 那句提示由 50-04 从**投影数据**（`shadowed` / `shadowedBy`）在**删除前的确认弹框**里渲染，卸载响应不携带任何提示字段。
         - `isDir === false` ⇒ 再查 `managed-skills/<name>`：存在则 `throw makeManageSkillError(MANAGE_SKILL_ERROR.NOT_USER_OWNED, …)`；否则 `throw makeManageSkillError(MANAGE_SKILL_ERROR.NOT_FOUND, …)`。两条 `message` 都必须是**可读且可操作**的中文（内置技能只可禁用 / AI 创建的技能请让 AI 用 `manage_skill` 删除 / 只能卸载 `skills/` 目录下的用户技能），且**不得回显被拒内容原文**（49 既有口径）。
       - 段 3：`const res = await env.remove(userDir, { recursive: true });`（**必须显式传 `recursive: true`** —— SDK 默认 false）。
       - 段 4：`if (!res || res.ok !== true) throw makeManageSkillError(MANAGE_SKILL_ERROR.UNKNOWN, '卸载技能 "…" 失败（沙箱码 …）');`（经 `sandboxErrorCode(res)`，**不泄漏沙箱原始码**）。成功返回 `{ name: skillName, filePath: path.join(userDir, 'SKILL.md'), action: 'uninstall' }`（**不带** `shadowNotice` 之类的提示字段）。
       - **禁止**：直接 `fs`、手拼路径字符串、复用 `resolveManagedTarget`、以 `_cache` 为判据。
    5. **导出**：在 `module.exports` 另起一段注释新增 `deleteUserSkill` / `validateSkillNameForManagement` / `validateDisabledListForSettings`。**不导出** `makeManageSkillError`（判定全在模块内）。
    6. **测试增补**：
       - `tests/test-skills-management.js` 新增：三态拒绝面（不存在 ⇒ `not_found`；同名双存在 ⇒ **允许且删除成功**（`skills/<name>` 消失、`managed-skills/<name>` 原封不动，且**不**断言任何提示字段）；仅 managed 存在 ⇒ `not_user_owned`）—— **三态都要经 `aiSkills.deleteUserSkill(env, {name})` 直接调用**（这不经 HTTP handler，正是 ROADMAP 判据 3「手改 URL 直接调端点也不例外」的承重点）；**读盘而非缓存**（先 `refreshSkills` 暖缓存 → `fs.rmSync` 从盘删目录 → 调卸载 ⇒ `not_found`）；`recursive` 生效（删掉的目录连同其 `scripts/` 子目录一并消失）；`validateSkillNameForManagement` 值域矩阵（`My_Skill` 通过 / 空串与纯空白拒绝 / 65 字符拒绝 / 含 `/`、`\`、`\u0000` 拒绝 / 非字符串拒绝）；`validateDisabledListForSettings` 值域（非数组 / 含空串 / 含非字符串 / 含超长项 / 含路径样串 / 101 项 ⇒ 全部 `valid: false`；100 项合法项 ⇒ `valid: true`）。
       - `tests/test-manage-skill.js`：把 `:1589-1605` 的键集合断言改为**恰十一键**（加 `NOT_USER_OWNED`）、值集合断言改为**恰十一个值**（加 `'not_user_owned'`）；`:1588` 的 describe 标题「本计划不增不减不改名」在新设计下**失实**，改为「闭合白名单的不变式（改名须同批改契约与账本）」；两条 `assert` 的第三条消息文本一并改（**不得**留下自相矛盾的注释）；`:1624-1639` 的「不新立第十码」用例改述为「新失败面必须取自闭合白名单，不得**再**新立第十二码」并保留其 `Object.values(...).includes(err.code)` 的成员校验语义。
  </action>
  <verify>
    <automated>node --check ai-skills-manager.js  &&  node tests/test-manage-skill.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或 `node --check` 非零退出</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const src=fs.readFileSync("ai-skills-manager.js","utf8");const srcC=stripC(src);const bodies=(name)=&gt;{const i=src.indexOf("async function "+name+"(");if(i&lt;0)return "";const j=src.indexOf("\n}\n",i);return src.slice(i,j&lt;0?i+6000:j)};const del=stripC(bodies("deleteUserSkill"));if(!del)bad.push("缺 async function deleteUserSkill(（口径失效或函数未实现）");else{if(del.includes("resolveManagedTarget"))bad.push("deleteUserSkill 复用了 resolveManagedTarget（D-07 明示不得复用；判据已剥注释）");if(!del.includes("env.fileInfo("))bad.push("deleteUserSkill 未用 env.fileInfo 判 kind（exists 判不出目录）");if(!/kind === \x27directory\x27/.test(del))bad.push("deleteUserSkill 未断言 kind === directory");if(!/env\.remove\([^)]*\{\s*recursive:\s*true\s*\}/.test(del))bad.push("deleteUserSkill 未显式传 { recursive: true }");if(!del.includes("NOT_USER_OWNED"))bad.push("deleteUserSkill 缺 not_user_owned 分支");if(!del.includes("NOT_FOUND"))bad.push("deleteUserSkill 缺 not_found 分支");if(!del.includes("getSkillsDir()"))bad.push("deleteUserSkill 未从 getSkillsDir() 派生路径");if(/\bfs\./.test(del))bad.push("deleteUserSkill 直接用了 fs（绕过沙箱；判据已剥注释 —— 注释里如实写明「不得直接 fs」不构成违规）")}if(!/NOT_USER_OWNED:\s*\x27not_user_owned\x27/.test(srcC))bad.push("MANAGE_SKILL_ERROR 缺 NOT_USER_OWNED");if(!/function validateSkillNameForManagement\(/.test(srcC))bad.push("缺 validateSkillNameForManagement 谓词");if(!/function validateDisabledListForSettings\(/.test(srcC))bad.push("缺 validateDisabledListForSettings（D-10 的列表校验单源）");if(!/module\.exports\s*=\s*\{[\s\S]*?\bdeleteUserSkill\b/.test(srcC))bad.push("deleteUserSkill 未导出");if(!/module\.exports\s*=\s*\{[\s\S]*?\bvalidateSkillNameForManagement\b/.test(srcC))bad.push("validateSkillNameForManagement 未导出");if(!/module\.exports\s*=\s*\{[\s\S]*?\bvalidateDisabledListForSettings\b/.test(srcC))bad.push("validateDisabledListForSettings 未导出");if(/require\(\x27electron\x27\)/.test(srcC))bad.push("ai-skills-manager.js 出现 electron 依赖（判据已剥注释 —— 注释里如实写明「本模块零 electron 依赖」不构成违规）");const pick=fs.readFileSync("src/skill-picker-model.js","utf8");const t=pick.match(/MANAGE_SKILL_SHORT_REASON\s*=\s*Object\.freeze\(\{([\s\S]*?)\n  \}\);/);if(!t)bad.push("无法定位 MANAGE_SKILL_SHORT_REASON（口径失效）");else if(/not_user_owned/.test(t[1]))bad.push("MANAGE_SKILL_SHORT_REASON 混入 not_user_owned（须保持恰 9 键）");if(bad.length){console.error("管理写路径数据层不合格:\n"+bad.join("\n"));process.exit(1)}console.log("管理写路径数据层 ok（仅 user 可删三态 / 第十码 / 两个谓词 / 零 electron）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `管理写路径数据层 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node --test tests/test-skill-picker-model.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行（`MANAGE_SKILL_SHORT_REASON` 的 9 键断言若被打翻会在此现形）</fails_when>
  </verify>
  <acceptance_criteria>
    - 「管理写路径数据层」扫描输出 ok 行；**可失败性（只做过负方向，未做 stub 正方向与单点变异）**：本门禁在落地前对当前树实跑过，失败项全部是「新代码尚未落地」类（缺 `deleteUserSkill` / 缺第十码 / 缺两个谓词），无「合规实现也修不掉」的断言。执行者落地后**必须自行**做五次单点变异确认转红：删掉 `kind === 'directory'` 断言；把 `env.fileInfo(` 换成 `env.exists(`；删掉 `NOT_USER_OWNED` 分支；把 `deleteUserSkill` 内的 `env.remove` 去掉 `recursive: true`；往 `MANAGE_SKILL_SHORT_REASON` 加 `not_user_owned`。**Token 型禁令一律经 `stripC` 词法级剥注释后再判（按单引号 / 双引号 / 模板反引号字符串、正则字面量、行注释、块注释逐字符状态扫描 + 等长空白化：注释不计数，字符串与正则字面量内的 `//` 不被误判为注释）** —— 已实测：在 `deleteUserSkill` 内写一条含 `fs.` 与 `resolveManagedTarget` 字面量的**解释性注释**（如「不复用 resolveManagedTarget；不直接 fs」）**不转红**；注释里写明「本模块零 electron 依赖」也不触发 electron 依赖项。
    - 三态拒绝面的三条用例全部经 `aiSkills.deleteUserSkill()` **直接调用**（不经 HTTP），且同名双存在那条断言的是「**允许且删除成功**（结果对象为真、`skills/<name>` 目录消失、`managed-skills/<name>` 原封不动）」，**不**断言任何提示字段（提示由 50-04 的确认弹框承担）。
    - 「暖缓存 → 盘上删目录 → 调卸载 ⇒ `not_found`」用例通过（把判据改成读 `_cache` 会让该用例失败）。
    - `tests/test-manage-skill.js` 的键 / 值集合断言为**恰十一键 / 十一个值**，且其 describe 标题与消息文本与新设计自洽（不再出现「本计划不动它」这类失实表述）。
    - `MANAGE_SKILL_SHORT_REASON` 的 9 键断言仍绿（`node --test tests/test-skill-picker-model.js` 的 `# fail` 为 0）。
    - `ai-skills-manager.js` 仍零 `require('electron')`。
  </acceptance_criteria>
  <done>管理面的写判据全部落在 `ai-skills-manager.js` 且可被纯 Node 单测：仅 user 可删的三态拒绝面、第十码、安全超集的名称谓词与名单校验器；`manage_skill` 的闭合白名单与短原因表按 OQ-3 分别刷新与冻结。</done>
</task>

<task type="auto">
  <name>Task 2: ai-manager 写路径与失效链收口 —— 重扫恰一次 + 调用侧补播恰一次（含忙时）</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js`：`:3026-3054`（`syncAgentSystemPrompt()` —— **只读，绝不改**。注意三处：`:3027` 的 `if (!this.agent || !this.sandboxEnv) return;`、`:3038-3041` 的忙时「只置脏 + return」、`:3053` 的广播点）、`:3072-3075`（`refreshSkillsForPanel` —— 读侧调用方的既有形状）、`:3100-3114`（`_flushDeferredSkillsPrompt` —— 延迟补刷的**唯一**实现，**不碰**）、`:1565-1585`（`getSeededSkillNamesSafe` / `getSkillsForUI` 的转发形状）、`:778`（`this.configStore = configStore` 注入点）、`:904` / `:3031` / `:3147`（三处 `this.configStore.get('settings.aiSkills.disabled', [])` 的既有读取形状 —— 新写路径照此形状并加空值保护）、`:25`（`windowManager` 在本文件**顶层已 require** ⇒ 广播不需要新增依赖）、`:6252-6258`（`manage_skill` 的 `syncAgentSystemPrompt()` 恰一次先例 —— 注释口径照抄）
    - `tests/test-ai-skills.js`：`:3210-3247`（`promptCtx` 夹具 —— 含 `syncAgentSystemPrompt` 的 own-property **包装计数** `rescanCalls`；⚠️ `:3233` 的 fake `configStore` **目前只有 `get`**）、`:3260-3266`（`channels` 数组的收集方式）、`:3394-3408`（`captureBroadcasts`）、`:3289-3296`（`rescanCalls === 2` 的次数账先例）、`:1299-1306`（`syncAgentSystemPrompt` 的方法体源码形态断言 —— **本任务改它就会红**）
    - `.planning/phases/50-api-skills/50-CONTEXT.md`：D-18（写路径收口与补播的真实理由）、D-19 的约束 ①（「保持恰一次重扫的次数账」）
    - `.planning/phases/50-api-skills/50-01-PLAN.md`：Task 1 对 `ensureSkillsFresh()` 的形状规定（本任务的写路径**复用它**，不另写第二条重扫路径）
  </read_first>
  <action>
    1. **`setSkillDisabled(name, disabled)`**（新方法，位置紧挨 `getSkillsForManagement()`）：
       - ① 校验：`const check = getAiSkillsManagerLazy().validateSkillNameForManagement(name); if (!check.ok) throw makeManageSkillError(check.code, check.reason);`（谓词单源，错误经既有构造点）。若本文件不便直接构造带 `code` 的错误，则抛出与 `handleSkillsApi` 的 `catch` 能读出 `.code` 的等价形态 —— **不得**在 manager 模块外另造一份校验正则。
       - ② 读-改-写名单（**全程同步、中间无 `await`**）：`const list = this.configStore ? this.configStore.get('settings.aiSkills.disabled', []) : [];` → `const current = Array.isArray(list) ? list.slice() : [];` → `const next = disabled ? [...new Set([...current, name])] : current.filter((n) => n !== name);` → `this.configStore.set('settings.aiSkills.disabled', next);`
       - ③ 收口：`await this.ensureSkillsFresh();`（**恰一次** —— 有 Agent 时它内部走 `syncAgentSystemPrompt()`，其中已含恰一次 `refreshSkills`；无 Agent 时直接 `refreshSkills`。**不得**在这一句之外再调 `syncAgentSystemPrompt()` 或 `refreshSkills()`，那会变成两次全量重扫）。
       - ④ **调用侧补播**：`windowManager.broadcast('skills:changed');`（注释必须写明理由 = 覆盖 `syncAgentSystemPrompt()` 的 `isProcessing || streaming` 分支「只置脏、return、不广播」，而管理写路径**不经过 Agent 轮次**；**禁止**把它挪进 `syncAgentSystemPrompt()` 的函数体）。
       - ⑤ 返回最新投影：`return this.getSkillsForManagement();`（响应体即最新投影 ⇒ 设置页零二次请求）。
    2. **`uninstallUserSkill(name)`**（新方法）：
       - ① 取得 env：`const env = this.sandboxEnv || (this.sandboxEnv = await getAgentWorkspaceLazy().createSandboxEnv());`
       - ② 删盘：`const result = await getAiSkillsManagerLazy().deleteUserSkill(env, { name });`（判据全在 manager；失败时 `throw` 带 `code` 的错误，**本方法不得吞掉或改写 code**）。
       - ③ **D-09 派生不变式**：删盘成功后从 `settings.aiSkills.disabled` 移除该名字（同 Task 2 的同步读-改-写形状；**只在删盘成功之后**执行 —— 失败路径不得清名单）。
       - ④ 收口与补播：`await this.ensureSkillsFresh(); windowManager.broadcast('skills:changed');`
       - ⑤ 返回：`return { ...result, management: this.getSkillsForManagement() };`
    3. **`tests/test-ai-skills.js` 增补**：
       - **先补夹具**：`:3233` 的 fake `configStore` 只有 `get` ⇒ 补一个内存实现：`get(key, fallback)` 从内部对象取值（缺省回落 `fallback`）、`set(key, value)` 写内部对象。**必须补 `set`**，否则管理写路径根本没法测。补的时候**不得**改动 `promptCtx` 的既有行为（`rescanCalls` 的 own-property 包装必须原样保留）。
       - **写路径次数账**：用 `promptCtx(env)`（可带 `agent`）调 `setSkillDisabled(name, true)` ⇒ 断言 `rescanCalls === 1`（**不是 2** —— 双写会变成两次全量重扫）、`channels` 里 `skills:changed` **恰一次**、`configStore` 的 `settings.aiSkills.disabled` 含该 name、且返回的投影里该技能 `disabled === true`。
       - **忙时补播（本任务的核心验收面）**：置 `ctx.isProcessing = true` 后调 `setSkillDisabled(name, true)` ⇒ 断言 `channels` 含 `skills:changed` **恰一次**（补播**仍然发出**），且 `ctx.agent.state.systemPrompt` **未变**（忙时只置脏标记）。⚠️ 该用例的标题**必须**含「忙时补播」四字 —— 门禁按 `test(` 的**标题**判它的存在；**不得**用 `isProcessing` / `skipped` 之类的**文件级子串**当判据（它们在 50-02 落地之前就已存在于 `tests/test-ai-skills.js`：实测 `isProcessing` 13 处 / `skipped` 3 处，用它判会让门禁**恒真**）。反向对照：把补播那一行删掉 ⇒ 该用例必然失败（这就是它的可失败性）。
       - **读路径不双扫**：无 Agent（`ctx.agent = undefined`）时调 `setSkillDisabled` ⇒ `rescanCalls === 0`（`syncAgentSystemPrompt` 未被调用）但投影的 `refreshedAt` 变大（说明确实走的是直接 `refreshSkills` 分支）。
       - **卸载的名单清理**：`configStore` 预置 `[name]` ⇒ 调 `uninstallUserSkill(name)` ⇒ 断言名单不含该 name、盘上目录消失、返回体带 `management`；**再加一条**「同名新技能装上后不被静默禁用」（重新 refresh 后该技能的 `disabled === false`）—— 这是 D-09 派生不变式存在的全部理由。
       - **失败路径不清名单**：让 `deleteUserSkill` 抛错（目标不存在）⇒ 断言 `throw` 且 `configStore` 的名单**未变**。
  </action>
  <verify>
    <automated>node --check ai-manager.js  &&  node -e 'const fs=require("fs");const bad=[];const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const src=fs.readFileSync("ai-manager.js","utf8");const method=(name)=&gt;{const i=src.indexOf("  async "+name+"(");if(i&lt;0)return "";const j=src.indexOf("\n  }\n",i);return stripC(src.slice(i,j&lt;0?i+4000:j))};const count=(s,re)=&gt;(s.match(re)||[]).length;for(const n of ["setSkillDisabled","uninstallUserSkill"]){const b=method(n);if(!b){bad.push("缺方法 "+n);continue}const sync=count(b,/await this\.(?:syncAgentSystemPrompt|ensureSkillsFresh)\(\)/g);if(sync!==1)bad.push(n+" 的刷新链调用 "+sync+" 次（必须恰 1 —— 双写会变成两次全量重扫；判据已剥注释，注释里的同一写法不计入）");if(/refreshSkills\(/.test(b))bad.push(n+" 直接调了 refreshSkills（重扫只允许经 syncAgentSystemPrompt / ensureSkillsFresh；判据已剥注释）");const bc=count(b,/windowManager\.broadcast\(\x27skills:changed\x27\)/g);if(bc!==1)bad.push(n+" 的调用侧补播 "+bc+" 次（必须恰 1；判据已剥注释）")}const syncBody=stripC(src.slice(src.indexOf("  async syncAgentSystemPrompt()"),src.indexOf("  async refreshSkillsForPanel()")));if(!syncBody)bad.push("无法定位 syncAgentSystemPrompt（口径失效）");else if(count(syncBody,/windowManager\.broadcast\(\x27skills:changed\x27\)/g)!==1)bad.push("syncAgentSystemPrompt 函数体被改动（广播次数 ≠ 1；判据已剥注释）");if(!/  async ensureSkillsFresh\(\)/.test(src))bad.push("缺 ensureSkillsFresh（D-19 的读路径初始化）");else{const i0=src.indexOf("  async ensureSkillsFresh()");const ef=stripC(src.slice(i0,src.indexOf("\n  }\n",i0)));if(!/await this\.syncAgentSystemPrompt\(\)/.test(ef))bad.push("ensureSkillsFresh 缺「有 Agent → 走 syncAgentSystemPrompt」分支");if(!/refreshSkills\(/.test(ef))bad.push("ensureSkillsFresh 缺「无 Agent → 直接 refreshSkills」分支");if(!/if \(this\.agent\)/.test(ef))bad.push("ensureSkillsFresh 未按 Agent 存在与否分流（不得两条都执行）")}if(/process\s*\.\s*(on|once)\s*\(\s*[\x27"](uncaughtException|unhandledRejection)/.test(stripC(fs.readFileSync("main.js","utf8"))))bad.push("main.js 出现全局异常兜底的**注册形态**（与「本仓无兜底 ⇒ 降级分支承重」的事实不符，需重新评估）；判据已剥注释，注释里的同一字面量不构成违规");if(bad.length){console.error("写路径失效链不合格:\n"+bad.join("\n"));process.exit(1)}console.log("写路径失效链 ok（重扫恰一次 + 调用侧补播恰一次 + 函数体未改 + 读路径初始化分流）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `写路径失效链 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-ai-skills.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node -e 'const out=require("child_process").execSync("node tests/test-ai-skills.js 2>&1",{encoding:"utf8"});const src=require("fs").readFileSync("tests/test-ai-skills.js","utf8");const bad=[];for(const t of ["setSkillDisabled","uninstallUserSkill","isProcessing = true","rescanCalls","configStore"])if(!src.includes(t))bad.push("缺用例素材 "+t);if(!/test\(\s*["\x27][^"\x27]*忙时补播/.test(src))bad.push("缺「忙时补播」用例（须是一条 test/it/describe 的标题；注释、或文件里既有的 isProcessing/skipped 出现都不算 —— 后者在本阶段之前就已存在，用它们判会恒真）");const m=out.match(/# fail (\d+)/);if(!m||Number(m[1])!==0)bad.push("测试有失败项");if(bad.length){console.error("写路径用例面不合格:\n"+bad.join("\n"));process.exit(1)}console.log("写路径用例面 ok（写路径次数账 + 忙时补播 + 名单清理素材齐备）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `写路径用例面 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 「写路径失效链」扫描输出 ok 行；**可失败性（分条如实披露）**：其中**「`syncAgentSystemPrompt()` 函数体未被改动（广播次数 ≠ 1）」那一条已在本仓当前树上做过正方向实跑**（通过 ⇒ 无该条输出）**与单点变异**（在内存中往该函数体插一行 `windowManager.broadcast('skills:changed')` ⇒ 该条转红）—— 它判的是**既有代码**，故落地前就能取证。**本轮修订把该条改为「先剥注释再计数」并**重新实跑了两条取证**：正方向（当前树 ⇒ 无该条输出）与单点变异（往 `syncAgentSystemPrompt()` 函数体插一行真实的 `windowManager.broadcast('skills:changed')` ⇒ 报「函数体被改动（广播次数 ≠ 1；判据已剥注释）」）；另加一条反向对照（在函数体里写一句**注释**提及同一广播 ⇒ **不**转红）。其余五条（两方法的「刷新链恰一次 / 补播恰一次」、`ensureSkillsFresh` 的两分支、`main.js` 无全局兜底）只做过**负方向**（失败项为「缺方法 `setSkillDisabled` / 缺 `ensureSkillsFresh`」等新代码未落地类），**未做** stub 正方向、**未做**单点变异。执行者落地后**必须自行**做三次变异：删掉任一方法的 `windowManager.broadcast('skills:changed')` ⇒ 报「补播 0 次」；在方法里加第二次 `await this.syncAgentSystemPrompt()` ⇒ 报「刷新链调用 2 次」；把补播挪进 `syncAgentSystemPrompt()` ⇒ 报「函数体被改动」。
    - **忙时补播**用例存在并通过：`isProcessing === true` 时 `channels` 含 `skills:changed` 恰一次，且 `agent.state.systemPrompt` 未变。
    - 「无 Agent 时 `rescanCalls === 0` 而 `refreshedAt` 变大」用例通过 —— 它证明 `ensureSkillsFresh()` 的两分支是**互斥**的，不是串行执行。
    - 「卸载后同名新技能不被静默禁用」用例通过（D-09 派生不变式的端到端形式）。
    - `tests/test-ai-skills.js` 既有断言（含 `:1239` / `:1299-1306` / `:3289-3296` / `:3455`）**全部保持绿** —— 证明 `syncAgentSystemPrompt()` 的函数体逐字未改。
  </acceptance_criteria>
  <done>管理写路径在 ai-manager 层收口：启停与卸载都经「重扫恰一次 + 调用侧补播恰一次」，忙时补播有独立用例钉住，卸载的名单清理与失败路径均有用例。</done>
</task>

<task type="auto">
  <name>Task 3: HTTP 写路由 + `/api/settings/update` 双键校验 + 新建 HTTP 套件骨架</name>
  <files>main.js, tests/test-skills-http-api.js, tests/test-skills-management.js</files>
  <read_first>
    - `main.js`：`:1336-1344`（`handleSettingsApi` 的 token + route 范式）、`:1372-1392`（**update 校验循环 —— D-10 在此加分支**；注意既有两条是 `if (key === 'aiBashWhitelist')` 与 `if (key === 'cacheMaxGB')`，其余键一律 `configStore.set(\`settings.${key}\`, value)` 直写）、`:1391`（`configStore.set` 的键拼接形态 —— **这是两种键形态问题的根源**）、`:1395`（`settings:updated` 广播先例）、`:2649-2701`（`handleAiMemoryApi` 的单端点范式与 `catch` 形状）、`:877-900`（`sendJson` / `readJsonBody` —— **本任务只调用、不改造**；SEC-09 在 50-03）
    - `50-01-PLAN.md` Task 1 的 `handleSkillsApi`（本任务在它内部续写两个 POST 子路由；**不得**另起第二个 handler 或第二个分发分支）
    - `tests/test-skills-management.js`（50-01 建好的骨架与夹具 —— 本任务在里面补 D-10 的用例；新套件的夹具写法照抄它）
    - `AGENTS.md` 的「数据访问分层约定」（`file://` 主窗口不能 fetch 本地 HTTP —— Phase 38 事故）与「新增 IPC 接口」小节
  </read_first>
  <action>
    1. **`handleSkillsApi` 续写两个 POST 子路由**（插在 `list` 分支之后、`sendJson(res, 404, …)` 之前）：
       - `if (route === 'set-disabled' && req.method === 'POST')` ⇒ `const { name, disabled } = await readJsonBody(req, res);` → `sendJson(res, 200, await aiManager.setSkillDisabled(name, disabled));` → `return;`
       - `if (route === 'uninstall' && req.method === 'POST')` ⇒ `const { name } = await readJsonBody(req, res);` → `sendJson(res, 200, await aiManager.uninstallUserSkill(name));` → `return;`
       - `catch (err)` 的形状在 50-01 已落（`sendJson(res, 400, { error: err.message, code: err.code || undefined })`）—— **`code` 必须回传**，本任务不得把它删掉或换成不含 `code` 的形状。
       - ⚠️ `readJsonBody` 的**第二位置参 `res`** 本期尚未引入（50-03 才加），因此本任务按当前两参形态书写；50-03 会全量改签名（含本任务新增的两处，属其「57 → 59 调用点」机械判据的一部分）。**本任务不得自行改 `readJsonBody` 的签名**（那是 50-03 的单一写者范围）。
    2. **`/api/settings/update` 的 D-10 双键校验**：在既有校验循环里新增一条分支（与 `aiBashWhitelist` / `cacheMaxGB` 两条**并列**）：
       - `key === 'aiSkills.disabled'` ⇒ 校验 `value` 本体。
       - `key === 'aiSkills'` ⇒ 校验 `value && value.disabled`（**必须同时覆盖这一形态** —— 否则手改 URL 提交 `{ "aiSkills": { "disabled": [ … ] } }` 时 `key === 'aiSkills'`，会把整个 `settings.aiSkills` 对象覆写掉）。
       - 两者共用**同一份**判据：`aiSkillsManager.validateDisabledListForSettings(list)`（`main.js` 直接 require `./ai-skills-manager` —— 该模块零 electron 依赖，`AGENTS.md:273` 硬约束 ① 已为该用法背书）。`valid === false` ⇒ `sendJson(res, 400, { error: check.reason }); return;`（**必须在 `configStore.set` 之前 return**，否则「校验失败但仍落盘」）。
       - `list` 取不到（`aiSkills` 键但无 `disabled` 子键）⇒ 视为合法（该形态只是不带禁用名单的 `aiSkills` 写入）还是拒绝，必须**在代码里明确写死一种**并加注释说明理由；无论哪种，**不得**让它落到默认的 `configStore.set(\`settings.aiSkills\`)` 整体覆写路径上（整体覆写会顺手冲掉 `settings.aiSkills` 上的其它字段）。
    3. **新建 `tests/test-skills-http-api.js`**（照抄 `tests/test-skills-management.js` 的文件骨架与「用法:」行；**独立成文件**的理由：SEC-09 的 413 组与 token 403 组是 HTTP/传输层关注点，与管理数据面职责不同，且独立文件才有独立 `# tests` 计数可入账本）。本任务落的用例：
       - **真起一个 `http.createServer` 小 harness**（形同 `50-RESEARCH.md` 的探针形状，但**不复制** `main.js` 的 `realmServer`）——用一个最小 server 直接调用被测的校验逻辑？**不可行**（`handleSkillsApi` 住在 `main.js` 的闭包内、不可 require）。因此本套件对 HTTP 面的验证分两层：① **源码扫描**（`handleSkillsApi` 的子路由名 / token 首行 / `{ error, code }` 形状 / `readJsonBody(req, res …)` 覆盖度 —— 后两项由 50-03 续写）；② **对可 require 的纯逻辑做行为断言**（`validateDisabledListForSettings` 的全部拒绝面 + `deleteUserSkill` 的三态，用真实 `http` 只用于 50-03 的 413 探针）。
       - 本任务落的：`validateDisabledListForSettings` 的两种键形态取值面（模拟 `key === 'aiSkills'` 时取 `value.disabled` 的那条路径：非数组 / 含非字符串 / 超长 / 含 `../` ⇒ `valid: false`；合法数组 ⇒ `valid: true`）+ 源码扫描断言（`main.js` 同时含 `key === 'aiSkills.disabled'` 与 `key === 'aiSkills'`；`main.js` 含 `route === 'set-disabled'` 与 `route === 'uninstall'`；`main.js` 含 `await aiManager.setSkillDisabled(name, disabled)` 与 `await aiManager.uninstallUserSkill(name)`）。
       - **必须用真实数据形状**：拒绝面用例逐条喂真实数组（含 `['ok-name', '']`、`['ok-name', 42]`、`['../escape']`、`Array(101).fill('a')`），不得用「手工构造一个实现从不构造的对象」式断言。
    4. `tests/test-skills-management.js` 补两条**服务端拒绝不经 handler** 的护栏：断言 `deleteUserSkill` 的三种拒绝码都是 `MANAGE_SKILL_ERROR` 的成员（`Object.values(...).includes(err.code)`）；断言 `validateSkillNameForManagement` 的拒绝码是 `invalid_name`（不是 `unknown`）。
  </action>
  <verify>
    <automated>node --check main.js  &&  node -e 'const fs=require("fs");const bad=[];const stripC=(x)=&gt;{let o="",i=0,s=0,p="";const N=x.length;while(i!==N){const c=x[i],d=x[i+1];if(s===1){o+=c==="\n"?"\n":" ";if(c==="\n")s=0;i++;continue}if(s===2){if(c==="*"&&d==="/"){o+="  ";i+=2;s=0;continue}o+=c==="\n"?"\n":" ";i++;continue}if(s===3||s===4||s===5){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if((s===3&&c==="\x27")||(s===4&&c==="\x22")||(s===5&&c==="\x60"))s=0;i++;continue}if(s===6){if(c==="\\"){o+=c+(d===undefined?"":d);i+=d===undefined?1:2;continue}o+=c;if(c==="/")s=0;i++;continue}if(c==="/"&&d==="/"){o+="  ";i+=2;s=1;continue}if(c==="/"&&d==="*"){o+="  ";i+=2;s=2;continue}if(c==="\x27"){o+=c;s=3;i++;p=c;continue}if(c==="\x22"){o+=c;s=4;i++;p=c;continue}if(c==="\x60"){o+=c;s=5;i++;p=c;continue}if(c==="/"&&(p===""||"[=(,;:![{&|?+-*%~^".indexOf(p)!==-1)){o+=c;s=6;i++;p=c;continue}o+=c;if(c.trim())p=c;i++}return o};const ih=fs.readFileSync("ipc-handlers.js","utf8");const main=fs.readFileSync("main.js","utf8");for(const r of ["set-disabled","uninstall"])if(!new RegExp("route === \x27"+r+"\x27").test(main))bad.push("handleSkillsApi 缺子路由 "+r);if(!main.includes("await aiManager.setSkillDisabled(name, disabled)"))bad.push("HTTP set-disabled 未转发到 setSkillDisabled");if(!main.includes("await aiManager.uninstallUserSkill(name)"))bad.push("HTTP uninstall 未转发到 uninstallUserSkill");if(!/sendJson\(res, 400, \{ error: err\.message, code: err\.code \|\| undefined \}\)/.test(main))bad.push("错误形状缺 { error, code }（UI 按 code 查表）");const loopI=main.indexOf("for (const [key, value] of Object.entries(updates)) {");const loopEnd=main.indexOf("broadcast(\x27settings:updated\x27",loopI);const win=stripC(loopI&lt;0?"":main.slice(loopI,loopEnd&lt;0?loopI+2000:loopEnd));if(!win)bad.push("无法定位 /api/settings/update 的校验循环（口径失效）");else{if(!/key\s*===\s*[\x27"]aiSkills\.disabled[\x27"]/.test(win))bad.push("/api/settings/update 的校验循环未覆盖 aiSkills.disabled 键形态");if(!/key\s*===\s*[\x27"]aiSkills[\x27"]/.test(win))bad.push("/api/settings/update 的校验循环未覆盖 aiSkills 键形态（只覆盖点号键会让 { aiSkills: {…} } 整体覆写绕过校验）")}if(!/validateDisabledListForSettings/.test(main))bad.push("main.js 未复用 manager 的名单校验器（第二份实现）");if(/process\s*\.\s*(on|once)\s*\(\s*[\x27"](uncaughtException|unhandledRejection)/.test(stripC(main)))bad.push("main.js 出现全局异常兜底的**注册形态**（与本仓事实不符）；判据已剥注释");const mgr=fs.readFileSync("ai-skills-manager.js","utf8");if(!/function validateDisabledListForSettings\(/.test(mgr))bad.push("缺 validateDisabledListForSettings（D-10 的列表校验单源）");if(!/module\.exports\s*=\s*\{[\s\S]*?\bvalidateDisabledListForSettings\b/.test(mgr))bad.push("validateDisabledListForSettings 未导出");if(bad.length){console.error("写路由与双键校验不合格:\n"+bad.join("\n"));process.exit(1)}console.log("写路由与双键校验 ok（两 REST 子路由 + 同一 manager 函数 + code 回传 + 双键共用单源校验）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `写路由与双键校验 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
    <automated>node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出</fails_when>
  </verify>
  <acceptance_criteria>
    - 「写路由与双键校验」扫描输出 ok 行；**可失败性（只做过负方向，未做 stub 正方向与单点变异）**：落地前对当前树实跑，失败项为「handleSkillsApi 缺子路由 / 未转发 / 校验循环未覆盖两种键形态」等新代码未落地类。执行者落地后**必须自行**做三次变异确认转红：只保留 `key === 'aiSkills.disabled'` 那一半；把 `code: err.code || undefined` 去掉；把 set-disabled 分支的转调删掉。
    - **两种键形态的判据已改为「书写形式无关」**（本轮修订）：判据在「校验循环 → `settings:updated` 广播」这段窗口内，用 `key\s*===\s*['"]aiSkills(\.disabled)?['"]` **各判一次**，不再要求写成同一个 `||` 表达式、也不要求单行。已用四份合成 `main.js` 实测：① **两段独立 `if (key === '…')` 分支**（即 该任务 action 引用的先例 `main.js:1376` / `:1384` 的形态）⇒ 通过；② 单条 `||` 表达式 ⇒ 通过；③ 只保留点号键 ⇒ 报「未覆盖 aiSkills 键形态」；④ 两种键只出现在**注释**里 ⇒ 同样报红（注释不满足判据）。
    - `/api/settings/update` 的 `aiSkills` 形态：非数组 / 含非字符串项 / 超长项 / 含路径分隔符 ⇒ `valid: false`，且实现里**在 `configStore.set` 之前 return**（用例断言拒绝时名单未落盘）。
    - 新建的 `tests/test-skills-http-api.js` 可独立跑通（`node tests/test-skills-http-api.js`），输出含 `# fail 0`；它**不复制** `main.js` 的 `realmServer`（真起 server 的 413 探针由 50-03 续写）。
    - `main.js` 未出现 `.includes` 之外的第二个名称校验实现（源码扫描断言 `validateDisabledListForSettings` 被引用）。
  </acceptance_criteria>
  <done>两个写端点可被设置页调用、错误以 `{ error, code }` 回传；`/api/settings/update` 对 `aiSkills` 与 `aiSkills.disabled` 两种键形态都做服务端校验且拒绝时不落盘；HTTP 层的独立测试套件就位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 设置页 guest → `POST /api/skills/set-disabled` / `uninstall` | 客户端可任意构造 body 与 URL（含直接手改 URL 调端点）。授权只有 token；**语义判据必须在 manager 层**，handler 只做转发 |
| 客户端提供的技能名 → 文件系统删除路径 | 卸载会**递归删目录** ⇒ 名称必须经谓词过滤（拒 `/` `\` 与控制字符）+ `path.join` + 沙箱 `resolveInside` 双基准 |
| `settings.aiSkills.disabled` → 加载管线过滤 | 该键目前零服务端校验；手改 URL 可写入任意值污染名单 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-09 | Elevation of Privilege | `POST /api/skills/uninstall` 被用于删内置 / 托管技能 | high | mitigate | 判据落在 `ai-skills-manager.deleteUserSkill()`（不经 handler 的友善包装）：只有 `skills/<name>` 存在**且 `kind === 'directory'`** 才允许；否则 `not_user_owned` / `not_found`。用例经 **manager 函数直接调用**验证三态，正是「手改 URL 直调端点也不例外」的承重点（门禁 C + 三态用例） |
| T-50-10 | Tampering | 路径穿越（技能名含 `../` / 分隔符 / 控制字符） | high | mitigate | `validateSkillNameForManagement` 拒 `/`、`\`、`\u0000`-`\u001f`；路径恒由 `path.join(getSkillsDir(), name)` 派生（**不手拼**）；删除经沙箱 `env.remove`（自带 `resolveInside` 双基准 + realpath 复核）。门禁 C 断言函数体内无 `fs.`、有 `getSkillsDir()` |
| T-50-11 | Tampering | `/api/settings/update` 的禁用名单污染（非数组 / 非字符串项 / 超大数组 / 路径样串） | high | mitigate | 新增 `validateDisabledListForSettings`（数组 + 每项非空字符串 ≤64 + 无分隔符/控制字符 + 条数 ≤100），`aiSkills.disabled` 与 `aiSkills` **两种键形态都覆盖**（只覆盖点号键会让 `{ "aiSkills": {…} }` 整体覆写并绕过校验）。门禁 G 断言两种键形态同时出现 |
| T-50-12 | Tampering | 跨窗口失效链断裂（写后 `/` 面板仍显示已禁用技能） | high | mitigate | 写路径「重扫恰一次 + 调用侧补播恰一次」；门禁 D 断言两方法各恰一次 + `syncAgentSystemPrompt()` 函数体广播次数仍为 1；独立用例断言**忙时（`isProcessing === true`）补播仍发出** |
| T-50-13 | Repudiation | 卸载失败被告知为「技能不存在」（失实文案） | medium | mitigate | 第十码 `not_user_owned` 与 `not_found` 分离；`message` 可读且可操作、不回显被拒内容原文。门禁 C 断言两个分支同时存在 |
| T-50-14 | Tampering | 第二份名称校验实现导致的漂移（列表里有、开关点了 400） | medium | mitigate | 谓词单源在 `ai-skills-manager.js` 并导出；`main.js` 直接 require。门禁 C 断言导出面、门禁 G 断言 `main.js` 引用该单源 |
| T-50-15 | Denial of Service | 卸载的递归删除在超大目录上耗时 | low | accept | 删除目标是用户在 `skills/` 下自建的技能目录（规模由 `MAX_USER_SKILLS` = 50 个技能封顶）；删除是**一次性用户动作**（有二次确认），不是可循环触发的路径。尺寸统计的双上限（50-01）不约束删除（删除必须删干净） |
| T-50-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本阶段**零新增依赖**，无安装动作 ⇒ 无供应链面 |
</threat_model>

<verification>
- 三个任务的 automated 门禁 全绿，且**四套回归**（`test-manage-skill.js` / `test-ai-skills.js` / `test-skill-picker-model.js` / `test-skills-management.js`）与新建的 `test-skills-http-api.js` 一并跑通。
- 本计划 4 条门禁**逐条如实披露**（不得整段声称已预跑）：4 条都只在**当前树**上做过负方向实跑（失败项一律为「新代码尚未落地」类）；**已被正方向实跑 + 单点变异取证**的只有两条**判既有代码**的子判据 —— `deleteUserSkill` 无关的 `MANAGE_SKILL_SHORT_REASON` 9 键冻结、以及「`syncAgentSystemPrompt()` 函数体广播次数仍为 1」（后者做过内存变异：插一行广播 ⇒ 转红）。**本轮修订**把 token 型禁令与计数一律改为「先剥注释（`stripC`：词法级四态扫描 + 等长空白化）再判」，并对**判既有代码**的那条重新跑过正方向 + 变异 + 注释反向对照；「`aiSkills` / `aiSkills.disabled` 双键」那一条另用**四份合成 `main.js`** 实测了「两段独立 `if`（先例形态） / 单条 `||` / 只留点号键 / 键只写在注释里」四种形态。其余全部（`deleteUserSkill` 的形状与三态、两个谓词、两方法的刷新链次数、`ensureSkillsFresh` 分流、两处 REST 子路由）**未做** stub 等价树正方向、**未做**单点变异 —— 变异义务已逐条写进对应任务的 `<acceptance_criteria>`，执行者必须自行跑。
- **本轮修订（iteration 3）—— 剥注释机制更换与实测证据**：全部「剥注释再判」的判据从「按前一字符判 `//`」换成**词法级四态扫描**（`stripC` 逐字符走 代码 / 行注释 / 块注释 / 字符串 / 正则字面量 各态，**等长**空白化 —— 旧写法会把字符串与正则字面量里的 `//` 误判为注释起点从而吞掉同行**真实代码**）。已实测两条证据：① 本计划含 `stripC` 的三条门禁在当前树上逐条重跑，**判定与换机制前逐字一致**（含 `deleteUserSkill` 内 `fs.` / `resolveManagedTarget` 字面量的解释性注释仍不转红）；② 机制本体的四项控制（剥后副本 `node --check` 三文件全过、两个基线计数不变、注释里的被禁字面量不计数、字符串 / 正则 / 模板串内的 `//` 同行送发点仍被计入）—— 完整命令与输出记在 `50-03-PLAN.md` 的 T2 acceptance「本轮修订（iteration 3）」。iteration 2 记录的四组合成夹具证据**仍成立**。
- counts-parity 在本波仍**预期为红**（`test-ai-skills.js` 与 `test-manage-skill.js` 的例数会变、第四个套件尚未入账本、第五个套件本波新建）—— 由 50-05 一次性收口。
- **`STATE.md:292` 的份额记账**：本计划完成后 `syncAgentSystemPrompt()` 的**写路径**收口到 **2/3**（49 的 `manage_skill` + 本阶段的设置页启停 / 卸载），只剩 51 的导入；50-01 新增的 `ensureSkillsFresh` 是**读侧 / 兜底**，**不得并入**「6 个触发点」的分子（OQ-5）。SUMMARY 必须分别记两个数。
</verification>

<success_criteria>
- 启用 / 禁用一个技能后，名单落盘、文件仍在、投影的 `disabled` 随之变化，且**下一条消息起**该技能不进 system prompt、不出现在 `/` 面板。
- 卸载只对 `source === 'user'` 生效；直接调 manager 函数同样拒绝内置 / 托管技能，且三种拒绝态给出不同且可读的错误码。
- 卸载后 `settings.aiSkills.disabled` 的同名条目被清理；同名新技能装上后不会被静默禁用。
- 忙时（AI 正在回复）点开关，`/` 面板在下一次广播消费时仍能拿到变更（补播发出）。
- 手改 URL 提交 `{ "aiSkills": { "disabled": … } }` 时被服务端拒绝且不落盘。
</success_criteria>

<output>
Create `.planning/phases/50-api-skills/50-02-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号由本计划新建**，不是既有符号的漂移。评审时不得要求它们在 base 上存在。（50-01 已登记的符号不在此重复；本阶段的完整清单以 50-01 的 §「Artifacts this phase produces」为准。）

| 类别 | 符号 | 落点 |
|------|------|------|
| 错误码表新增键 | `MANAGE_SKILL_ERROR.NOT_USER_OWNED`（值 `'not_user_owned'`，**第十一码 / 第十键**） | `ai-skills-manager.js` |
| 导出函数 | `deleteUserSkill(env, { name })` —— 仅 user 可删的三态拒绝面 | `ai-skills-manager.js` |
| 导出谓词 | `validateSkillNameForManagement(name)` —— 管理面「安全超集」名称谓词（`{ok, value}` / `{ok:false, code, reason}`） | `ai-skills-manager.js` |
| 导出校验器 | `validateDisabledListForSettings(list)` —— 禁用名单的列表级校验（`{valid, reason}`） | `ai-skills-manager.js` |
| 类方法 | `AIManager.prototype.setSkillDisabled(name, disabled)` | `ai-manager.js` |
| 类方法 | `AIManager.prototype.uninstallUserSkill(name)` | `ai-manager.js` |
| HTTP 路由 | `POST /api/skills/set-disabled`（`?token=` 鉴权，增量载荷 `{name, disabled}`） | `main.js` |
| HTTP 路由 | `POST /api/skills/uninstall`（`?token=` 鉴权，载荷 `{name}`） | `main.js` |
| 服务端校验分支 | `/api/settings/update` 新增的 `aiSkills.disabled` **与** `aiSkills` 双键校验 | `main.js` |
| 错误响应字段 | `{ error, code }` 形状（技能域 HTTP 400 统一回传 `code`） | `main.js` |
| 测试套件 | `tests/test-skills-http-api.js`（**新建文件**，本阶段账本第 5 个套件；本计划落基础骨架 + 双键校验与双入口源码扫描，SEC-09 组由 50-03 续写） | `tests/` |
| 测试断言 | `tests/test-manage-skill.js` 的「恰十一键 / 十码」冻结断言（**改写既有断言**，非新增符号） | `tests/test-manage-skill.js` |
| 测试夹具 | `tests/test-ai-skills.js` 的 `promptCtx` fake `configStore` 补齐 `set`（**扩展既有夹具**） | `tests/test-ai-skills.js` |

