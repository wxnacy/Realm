---
phase: 50-api-skills
plan: 05
type: execute
wave: 4
depends_on: [50-01, 50-02, 50-03, 50-04]
files_modified:
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/phases/50-api-skills/50-VALIDATION.md
autonomous: true
requirements: [USER-01, USER-02]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

key-decisions:
  - '**本计划不改任何源码与业务测试**：只做文档收口与账本同步。理由：`AGENTS.md` 的维护约定把【本功能的产品说明权威文档】与【测试清单例数】绑成了同一份契约；50-01..50-04 的改动面（投影口径 / 尺寸遍历 / 拒绝面 / 两入口 / 体积闸 / UI 状态链 / 单源表）必须在一处成文，否则 Phase 51 只能靠读代码反推口径 —— 而 51 正是「攻击面最大、耦合最小」的那个阶段'
  - '**章节编号取 §十二**（现有十一节：一能力 / 二双目录 / 三优先级 / 四限额 / 五沙箱边界 / 六已知限制 / 七测试与验证 / 八内置技能 / 九 bash 安装档 / 十发现与调用 / 十一 AI 自建技能）：管理面（用户对技能集的直接操作）与自建面（AI 对技能集的直接操作）在概念上并列，**新增独立一级小节**而不是塞进 §十一'
  - '**五条诚实边界必须成文**（AGENTS.md 的既有口径：bash 白名单、`allowed-tools`、`manage_skill` 的 bash 绕过面都已成文）：① **设置页收不到任何主进程广播**（`windowManager.broadcast` 只到各 BrowserWindow 的 webContents，`window-manager.js:310`）⇒ 多开设置页之间**不做**即时同步，靠「操作后用响应体投影重渲染 + 进页面重拉」；② **`allowed-tools` 在管理面缺席**（字段尚未被解析，其解析半边归 Phase 51）⇒ ROADMAP 的「展示时必须带免责标注」按**「不展示」**满足（条件句前置条件不成立），并留下「51 解析后必须补免责标注」的交接；③ **`digest` 的字段集禁令**（禁止把 `disabled` / `shadowed` / `overLimit` 从 `computeDigest` 摘掉，也禁止把 `bytes` / `fileCount` 加进去）；④ **管理面名称谓词故意比写入门宽**（`skills/My_Skill/` 这类技能必须能被禁用/卸载，否则 render 出一个「点了必然 400」的开关）；⑤ **「仅 user 可删」是判据而非能力边界**（用户仍可经 Finder / 终端删 `managed-skills/`）'
  - '**进度账本必须分开记两个数**（OQ-5）：本阶段完成后 `syncAgentSystemPrompt()` 的**写路径**收口到 **2/3**（49 的 `manage_skill` + 50 的设置页启停 / 卸载），只剩 51 的导入；50-01 新增的 `ensureSkillsFresh()` 是**读侧 / 兜底**，**不得并入**「6 个触发点」的分子。P8 失效链**仍不得**声称 6/6 全覆盖；`STATE.md:292` 的 ⚠️ 保持挂着（由 SUMMARY 披露、由后续 review / verify 落到 `STATE.md` —— 本计划**不手工改 `STATE.md`**，那会与工具写入竞争）'
  - '**§11.3 的自相矛盾必须消除**（OQ-3）：该节今天写「九条拒绝原因 / **不新增第十码**」，而本阶段加了 `not_user_owned`。改为「**工具侧**九条不变；管理面另有 `not_user_owned`（第十码，见第十二节）」，并写明「`MANAGE_SKILL_SHORT_REASON` 只覆盖**工具面**，管理面错误码由设置页自己的文案表承载」'
  - '**counts-parity 的四处必须同批扩**（research Pitfall 11）：① 命令的 `suites` 数组（3 → 5）；② `AGENTS.md` 的测试清单行；③ `docs/product/ai-skills.md` §七 / §11.8 的账本行；④ `50-VALIDATION.md` 里的那条命令副本。阈值从 `< 8` 提到 `< 12`（三套件既有 8 单元 + 两个新套件各 2 单元）。⚠️ 该判据的**逐套件覆盖检查**（`for (const base of Object.keys(meas)) if (!hit[base]) bad.push(...)`）是权威 —— 漏加新套件的账本单元**会**报错；阈值只是下限。**本计划是最后一个改动测试相关文件的计划 ⇒ counts-parity 必须在本计划结束时转绿**（前三个波次的红色是 Pitfall 11 明说的「中途 red」，不是缺陷）'
  - '**本计划的验证命令「先跑后写」—— 已实证到哪一步，如实披露**：counts-parity 的机制（跑套件读实测值 + 扫账本比对 + 逐套件覆盖检查）**已在本仓当前树上正方向实跑通过** —— 3 套件版实测 `cells=8`、`measured={"test-manage-skill.js":"55","test-ai-skills.js":"178","test-skill-picker-model.js":"111"}`；**并做过内存单点变异**（把 `docs/product/ai-skills.md:98` 的 `178 例` 改成 `179 例` ⇒ 机制报「账本单元取到 [179] ≠ 实测 178」而转红）。⚠️ 但 **5 套件版本身未跑过**（两个新套件尚不存在），执行者必须把 `meas` 的来源换成最终树上的真实套件后**首跑**；`50-VALIDATION.md` 一侧的命令副本目前**尚不存在** —— 门禁已把「副本缺失」并入失败条件（不是「两边都在才比对」），故漏写副本会当场报错而不是静默跳过'

must_haves:
  truths:
    - '`docs/product/ai-skills.md` 新增 §十二【管理面】，覆盖：列表字段与口径（名称 / 描述 / 来源 / 体积 / 文件数 / 诊断六项 + 三档分组 + 组内 `bySkillPriority` 全序投影 + 空组剔除）、**体积与文件数的计量口径**（递归合计、含 `SKILL.md`、不含隐藏文件、不穿 symlink、目录 `size` 不计、超上限截断并给诊断、`bytes === 0 && fileCount === 0` 时显示「统计不可用」而非 `0 B`）、启停语义（写 `settings.aiSkills.disabled`、只过滤不删文件、禁用后不进 prompt 与 `/` 面板、重新启用即恢复、**禁用的技能仍可卸载**）、卸载语义（**仅 `source === ''''user''''`**、三态拒绝面、同名托管 / 内置技能删除后重新可见的提示、无备份）、诊断的两层承载、失败文案按 `code` 查表、两入口（设置页 HTTP + 主窗口 IPC 与「不可互换」的理由）、五条诚实边界'
    - '§十二 **必须写明 `allowed-tools` 在管理面缺席的原因**（字段尚未被解析，解析半边归 Phase 51；一旦 51 在管理面展示它，**必须**带「当前运行时不被强制，仅供参考」免责标注）—— 这是 ROADMAP 的 Doc sync 条款按「不展示」满足的成文依据'
    - '§十二 **必须写明「与本地命令同名」（`STATUS_TEXT.nameClash`）是 `/` 面板独有维度**：设置页的状态链是 `disabled > shadowed > overLimit > promptOmitted`，**不含 `nameClash`**（它依赖本地命令注册表，guest 拿不到）'
    - '§12 的每条边界都能对应到代码事实（写清 module:line 或符号名），且**不含**任何「已修 / 已闭合」的越界声明：`TD-48-01` / `TD-48-02` / `WR-02` / `WR-06` 与 `STATE.md:292` 的 ⚠️ 一律保持挂账，本阶段只声明写路径收口到 **2/3**'
    - '§11.3 的「不新增第十码」改为「**工具侧**九条不变 + 管理面 `not_user_owned`（第十码，见 §十二）」，消除文档自相矛盾；并写明 `MANAGE_SKILL_SHORT_REASON` **保持恰 9 键**、只覆盖工具面'
    - '`AGENTS.md` 新增「技能管理面（设置页 + `/api/skills/*`）的维护约定」条目：产品说明权威 = `docs/product/ai-skills.md` §十二；改动**列表字段与口径 / 体积计量口径 / 启停与卸载语义 / 拒绝面与错误码 / 两入口的转发层 / 状态链与单源文案表 / `/api/*` 的请求体体积闸**时**必须同步该章节与该节测试清单**；并明记四条硬约束（① 判据 / 校验 / 投影全住 `ai-skills-manager.js` 且该模块零 electron 依赖 ⇒ 两入口**直接复用同一份**，handler 只做转发；② 管理面谓词与写入门**故意不同宽**；③ 写路径「重扫恰一次 + 调用侧补播恰一次」，**不得改 `syncAgentSystemPrompt()` 的函数体**；④ `LIMITS` 只允许加项，前端零字面量）'
    - '`AGENTS.md` 的测试清单行与 `docs/product/ai-skills.md` §七 / §11.8 的账本行**同时**刷为四套件（含 `test-skills-management.js` / `test-skills-http-api.js`）的**实测**例数；三处（含 `50-VALIDATION.md` 的命令副本）必须在**同一个任务**里刷，避免漂移'
    - '`50-VALIDATION.md` 的 Per-Task Verification Map 按已交付的计划 / 任务重键（`50-01-T1..T3` … `50-04-T1..T3` + 本计划的 T3），`wave_0_complete` 置 `true`，`Known-Open Items` 把 research 的 A2 / A3 / A4 / A6 四条待实测项逐条写处置（已实测 / 转入 Manual-Only / 未做及原因）；`nyquist_compliant` **保持 false**（该字段由 `/gsd-validate-phase` 判定，本计划不自我认证）'
    - 'counts-parity（5 套件版）**通过**：`cells` ≥ 12，且每个套件名在两个文件里都有账本单元、每个单元的例数与实测逐字一致'
    - '[assumption] 文档中的示例命令必须可直接复制执行（`node tests/<file>.js` / `node --test tests/<file>.js` 的显式形式），**不得**出现 `npm test`（本项目 `package.json` 无该脚本，两处 gate 恒把它解析成不存在的脚本）'
  artifacts:
    - 'docs/product/ai-skills.md：新增 §十二【管理面】（含五条诚实边界 + 体积口径 + `allowed-tools` 缺席原因 + `nameClash` 的归属 + 两入口 + 状态链）+ §11.3 的第十码修订 + §七 / §11.8 的账本刷新'
    - 'AGENTS.md：新增技能管理面维护约定条目（四条硬约束 + 测试口径）+ 测试清单行的两处新条目与例数刷新'
    - '.planning/phases/50-api-skills/50-VALIDATION.md：Per-Task Verification Map 重键 + `wave_0_complete: true` + A2/A3/A4/A6 四条的处置 + counts-parity 命令副本更新为 5 套件版'
  key_links:
    - '维护约定链：`AGENTS.md` 的技能管理面条目 → `docs/product/ai-skills.md` §十二 → `ai-skills-manager.js`（判据 / 谓词 / 投影 / 尺寸遍历）/ `ai-manager.js`（失效链与两入口转发）/ `main.js`（HTTP 层与体积闸）/ `src/settings-page.js`（渲染与交互）/ `src/skill-picker-model.js`（文案单源）'
    - '测试账本链：四套件的 `node tests/<file>.js` 实测 `# tests` 值 → `AGENTS.md` 测试清单行 + `docs/product/ai-skills.md` §七/§11.8 → 与 `50-VALIDATION.md` 的命令副本同源。四处必须同值'
    - '交接链（给 Phase 51）：§十二 的 `allowed-tools` 缺席说明 + 体积闸的「可显式覆盖」形状（`readJsonBody` 的 `{ maxBytes }`）+ 「与内置同名拒导入 / 与用户技能同名三选一」（SEC-07）的口径承接点'
  prohibitions:
    - statement: '不得在文档里声称本阶段已闭合 `TD-48-01` / `TD-48-02` / `WR-02` / `WR-06`，也不得声称 `syncAgentSystemPrompt()` 的生产调用方已完整收口或 P8 失效链已 6/6 全覆盖'
      status: unresolved
      verification: judgment
      reason: '用户已裁决 TD-48-01 / TD-48-02 延后；WR-02 / WR-06 逐字保持挂账；本阶段只完成写路径的 2/3（仅剩 51 的导入），且新增的是**读侧**调用方（不计入分子）。文档误声明会让后续阶段据此跳过工作，是**不可逆的账本污染**'
    - statement: '不得把「设置 `Connection: close` 会导致客户端 EPIPE」写成事实'
      status: unresolved
      verification: judgment
      reason: '该结论在重研会话中**不可复现**（`http.request` 与 `fetch` 两种客户端都拿到了 413）。文档只写可复现的规则：「不 `destroy()`、不设 `Connection: close`、用 `req.resume()`」'
    - statement: '不得在 §十二 里写入实现细节级别的代码片段（函数签名清单、行号表、正则字面量）'
      status: unresolved
      verification: judgment
      reason: '产品说明与实现台账是两类文档。§十二 若复制实现细节，会与代码形成第二份需要同步的真相（`AGENTS.md` 的既有纪律：产品说明改动必须与**行为**一致，而不是与某几行代码一致）'
    - statement: '不得在文档里把「跨窗口即时同步」写成比实际更强的声明'
      status: unresolved
      verification: judgment
      reason: '`windowManager.broadcast` 只到各 BrowserWindow 的 webContents、**不到 webview guest** ⇒ 「任一入口的变更跨窗口即时同步」**只在 BrowserWindow 宿主上成立**；设置页（guest）收不到任何广播。写成全量即时同步会让后续阶段据此省掉「操作后自行重拉」这条必要机制'
    - statement: '不得把管理面名称谓词的「故意更宽」写成缺陷或待修项'
      status: unresolved
      verification: judgment
      reason: '它是**设计**（禁用名单是「按名字过滤」的消费侧信号，不需要名字合法到能写盘）。写成缺陷会让后续阶段「修」成严格形态，从而让 `skills/My_Skill/` 这类技能的开关点了必然 400'
    - statement: '不得在本计划里手工修改 `.planning/STATE.md`'
      status: unresolved
      verification: judgment
      reason: 'GSD 自身的 state 台账由 plan-phase 与 verify-work 维护，手工改会与工具写入竞争。本计划只在 §十二 与 `AGENTS.md` 成文，并在 SUMMARY 里给出建议措辞'
---

<objective>
收口 **Phase 50 的文档与账本**：把「设置页技能管理面」从代码事实提升为一份可复用的产品说明（`docs/product/ai-skills.md` §十二），把维护约定与测试清单同步进 `AGENTS.md`，并把 `50-VALIDATION.md` 的验证矩阵按已交付的计划 / 任务重键、把 research 的四条待实测项逐条写处置，最后让 **counts-parity（5 套件版）转绿**。

Purpose: Phase 51（导入管线 + 技能域威胁模式组）**必须复用**本阶段产出的判据、谓词、体积闸形状与两入口转发层 —— 那三份工作只能通过 §十二 与 `AGENTS.md` 的维护约定读到【口径是什么、边界在哪里、哪些面故意不做】。本计划同时是**本阶段最后一个改动测试相关面**的计划 ⇒ counts-parity 的最终收口点。

Output: `docs/product/ai-skills.md` 的 §十二 与 §11.3 / §七 / §11.8 的增量；`AGENTS.md` 的技能管理面维护约定与测试清单刷新；`50-VALIDATION.md` 的重键矩阵、四条假设处置与命令副本更新。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-api-skills/50-CONTEXT.md
@.planning/phases/50-api-skills/50-RESEARCH.md
@.planning/phases/50-api-skills/50-PATTERNS.md
@.planning/phases/50-api-skills/50-UI-SPEC.md
@.planning/phases/50-api-skills/50-VALIDATION.md
@.planning/phases/50-api-skills/50-01-PLAN.md
@.planning/phases/50-api-skills/50-02-PLAN.md
@.planning/phases/50-api-skills/50-03-PLAN.md
@.planning/phases/50-api-skills/50-04-PLAN.md
@docs/product/ai-skills.md
@docs/product/ai-agent-workspace.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/product/ai-skills.md` 新增 §十二【管理面】+ §11.3 第十码修订 + §七 账本与覆盖面</name>
  <files>docs/product/ai-skills.md</files>
  <read_first>
    - `docs/product/ai-skills.md`：`:32-105`（§一..§七 全段 —— 新章节的口径**必须与它们不矛盾**，尤其 §二 双目录、§四 限额表、§六 已知限制、§七 的账本行形态）、`:253-431`（§十 发现与调用 —— 「行为契约 + 指向」的写法与 10.7 的诚实边界口径）、`:433-543`（§十一 AI 自建技能 —— **新章节的体例模板**；特别注意 `:457-483` 的 §11.3 九码表与「不新增第十码」的原文、`:534-543` 的 §11.8 账本三条）、`:545-…`（§十一 尾部的 counts-parity 可重跑命令 —— **本计划要把它扩成 5 套件版**）
    - 50-01..50-04 的 PLAN/SUMMARY：管理投影的字段与分组、尺寸口径、拒绝面三态与第十码、两个入口、体积闸、UI 状态链与单源表（**逐条对照行为写文档，不抄实现**）
    - `.planning/phases/50-api-skills/50-UI-SPEC.md` 的 Copywriting Contract（用户可见文案的权威清单 —— 文档引用文案时以它为准）
    - `docs/product/ai-agent-workspace.md`（同族产品文档的章节组织与措辞风格）
  </read_first>
  <action>
    1. 在文件末尾（§十一 之后、counts-parity 命令之前或之后的合适位置）新增 `## 十二、管理面（设置页 + /api/skills/*）` 一级章节，用与既有章节同款的【行为契约 + 指向】写法（**不写函数签名、不写行号、不写正则字面量**）。至少包含：
       - **12.1 列表字段与口径**：六项字段（名称 / 描述 / 来源 / 体积 / 文件数 / 诊断）；按来源三组（我的技能 / 内置技能 / AI 创建）；**分组与排序都在主进程**（组内是全序的投影，前端只渲染不重排）；**空组不渲染**；不展示 `allowed-tools`（见 12.6）。
       - **12.2 体积与文件数的计量口径**：递归合计、**含 `SKILL.md`**、**不含隐藏文件**（`.` 前缀，如 `.DS_Store`）、**不穿符号链接**（含防内部链接环）、目录条目的 `size` 不计、超条目 / 深度上限时**截断并给出诊断**（显示为下限值、技能仍可用）、统计不可用时显示「统计不可用」而**不得**渲染 `0 B · 0 个文件`（那是失实文案）。写明「含 `SKILL.md`」是为了让「64.1 KB vs 64 KiB 上限」可解释。
       - **12.3 启停**：写 `settings.aiSkills.disabled`（名字数组）；**只过滤不删文件**；禁用后不进 system prompt、不出现在 `/` 面板；重新启用即恢复；**禁用的技能仍可卸载**；该键在 `/api/settings/update` 上有服务端校验（**两种键形态**都覆盖）。
       - **12.4 卸载**：**仅 `source === ''user''`** 的技能可卸载；三态拒绝面（不存在 ⇒ `not_found`；同名双存在 ⇒ 允许并提示；仅托管存在 ⇒ `not_user_owned`）；判据在 **manager 层**（手改 URL 直接调端点同样被拒）；删除是**递归且无备份**（确认弹框是唯一防线）；卸载成功后同步清理禁用名单（否则同名新技能一装上就被静默禁用）。
       - **12.5 诊断与状态**：两层承载（模块级 `errors[]` → 顶部汇总条，默认展开；每条技能的 `diagnostics[]` → 行内徽标 + 内联可展开详情区，默认折叠）；行尾状态标注按 `disabled > shadowed > overLimit > promptOmitted` 取首条；**`nameClash` 是 `/` 面板独有维度、不在设置页链上**（理由：它依赖本地命令注册表）；诊断的 `path` 不上屏；失败文案按服务端 `code` 查表、前端**不解析 `message`**。
       - **12.6 五条诚实边界**（逐条写清「为什么」）：① 设置页收不到主进程广播（`windowManager.broadcast` 只到各 BrowserWindow 的 webContents）⇒ 多开设置页之间不做即时同步，靠「操作后用响应体投影重渲染 + 进页面重拉」；② **`allowed-tools` 在管理面缺席** —— 字段尚未被解析（解析半边归 Phase 51）；**ROADMAP 的「展示时必须带免责标注」按「不展示」满足**（条件句前置条件不成立）；**51 若在管理面展示它，必须带「当前运行时不被强制，仅供参考」**；③ **`digest` 的字段集是跨窗口失效链的承重面** —— 禁止把 `disabled` / `shadowed` / `overLimit` 摘掉，也禁止把 `bytes` / `fileCount` 加进去（尺寸不影响 prompt 段，加了会让「加一个引用文件」触发 prompt 改写 + 广播）；④ 管理面名称谓词**故意比写入门宽**（禁用名单是消费侧信号，不需要名字合法到能写盘；严格形态会让 `skills/My_Skill/` 的开关点了必然 400）；⑤ 「仅 user 可删」是**判据**而非能力边界（用户仍可经 Finder / 终端删 `managed-skills/`）。
       - **12.7 两个前端入口与不可互换的理由**：设置页是 `realm://` guest（无 `realmAPI`）⇒ `/api/skills/*` + token；主窗口 `file://` **不能** fetch 本地 HTTP（CORS）⇒ IPC。两入口转发到**同一组 manager 函数**，handler 只做转发、零判定。
       - **12.8 请求体体积闸**：`/api/*` 的 POST body 有全局默认上限（1 MiB），需要大 body 的端点**显式放大**（书签导入 32 MiB）；超限在**累积过程中**即停收并答 413 + JSON（不 `destroy()`、不设 `Connection: close`、用 `resume()` 排水）；**技能域三个端点的载荷远小于 1 MiB ⇒ `BODY_TOO_LARGE` 正常不可达**（它是兜底）；Phase 51 的 zip 导入需自行评估是否用 base64 并显式声明上限。
       - **12.9 维护约定与测试**：指向 `AGENTS.md` 的技能管理面条目；列出本阶段新增的两个测试套件与命令。
    2. **修订 §11.3**（`:457-483`）：「九条拒绝原因 / **不新增第十码**」改为「**工具侧**九条不变；**管理面另有 `not_user_owned`（第十码，见 §十二）**」；并在同一处写明「`MANAGE_SKILL_SHORT_REASON` **保持恰 9 键**、只覆盖**工具面**，管理面错误码由设置页自己的文案表承载」。
    3. **刷新 §七（`:96-105`）**：新增两个套件的账本行（`node tests/test-skills-management.js` / `node tests/test-skills-http-api.js`，例数一律取**实测**），并把覆盖面对应补上（管理投影 / 尺寸口径 / 启停 / 卸载三态 / 名单清理 / 双入口 / SEC-09 体积闸与降级）。**不得**出现 `npm test`。
    4. **刷新 §11.8（`:534-543`）**：在既有三条账本行中补入两个新套件的行；同时把「syncAgentSystemPrompt 的生产调用方」措辞按 OQ-5 写成**两个数分开记**（写路径 2/3；读侧触发点另计）。
    5. **更新文末的 counts-parity 可重跑命令**：`suites` 数组扩到 5 个（新增 `tests/test-skills-management.js` 与 `tests/test-skills-http-api.js`），阈值从 `cells<8` 提到 `cells<12`，并在注释里写明「两个新套件各 2 个账本单元（本文件 + `AGENTS.md`）」。该命令的**权威副本**在 `50-VALIDATION.md`（Task 3 同步），两处必须逐字一致。
  </action>
  <verify>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const bad=[];const i=d.indexOf("## 十二、");if(i&lt;0)bad.push("缺 §十二 章节");else{const sec=d.slice(i);for(const t of ["我的技能","内置技能","AI 创建","SKILL.md","隐藏文件","符号链接","统计不可用","settings.aiSkills.disabled","只过滤","not_found","not_user_owned","nameClash","allowed-tools","免责标注","windowManager.broadcast","webview","computeDigest","bytes","token","1 MiB","32 MiB","413","req.resume","MAX_JSON_BODY_BYTES"]){if(!sec.includes(t))bad.push("§十二 缺要点 "+t)}for(const h of ["12.1","12.2","12.3","12.4","12.5","12.6","12.7","12.8","12.9"])if(!sec.includes(h))bad.push("§十二 缺小节标题 "+h)}if(!/不展示/.test(d.slice(i)))bad.push("§十二 未声明 allowed-tools 不展示的理由");const s113=d.slice(d.indexOf("### 11.3"),d.indexOf("### 11.4"));if(/不新增第十码/.test(s113))bad.push("§11.3 的「不新增第十码」未修订（文档自相矛盾）");if(!/not_user_owned/.test(s113))bad.push("§11.3 未提到管理面的第十码");if(d.split("\n").some((x)=&gt;x.includes("npm test")&&!/不存在|没有|无该|无此|禁止|不得|严禁|不提供|不使用|不出现|勿|改用|替换|零/.test(x)))bad.push("文档把 npm test 当命令使用（本项目无该脚本；「不得出现 npm test」这类说明性文字不算违规）");if(!/test-skills-management\.js/.test(d))bad.push("§七/§11.8 未新增 test-skills-management.js 条目");if(!/test-skills-http-api\.js/.test(d))bad.push("§七/§11.8 未新增 test-skills-http-api.js 条目");if(!/cells&lt;12/.test(d))bad.push("文末 counts-parity 命令未扩到 5 套件（cells&lt;12）");if(bad.length){console.error("§十二 不合格:\n"+bad.join("\n"));process.exit(1)}console.log("§十二 要点齐备 + §11.3 第十码修订 + 两处账本 + 命令扩到 5 套件 ok")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `§十二 要点齐备`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const sec=d.slice(d.indexOf("## 十二、"));const bad=[];const lines=d.split("\n");const NEG=/(不得|不声称|不要|不可复现|未|禁止|无|非|挂账|仍开|延后|没)/;const claim=(id,verbs)=&gt;{const re=new RegExp(id+"[^\\n]{0,80}("+verbs+")");const hit=(l)=&gt;{const m=re.exec(l);if(!m)return false;return !NEG.test(l.slice(Math.max(0,m.index-40),m.index))&&!NEG.test(l.slice(m.index,m.index+m[0].length-m[1].length))};return lines.filter(hit)[0]||null};for(const p of [["TD-48-01","已修|已闭合|已修复"],["TD-48-02","已修|已闭合|已修复"],["WR-02","已修|已闭合|已修复"],["WR-06","已修|已闭合|已修复"],["P8","全覆盖|已完整收口|6\\/6"]]){const h=claim(p[0],p[1]);if(h)bad.push("文档出现越界声明（同一行是肯定式表述，且行内没有「不得 / 未 / 挂账 / 仍开 / 不可复现」这类否定限定）："+h.trim())}const cLine=lines.filter((l)=&gt;/Connection[^\n]{0,40}(会导致|将导致|造成)[^\n]{0,20}EPIPE/.test(l)&&!NEG.test(l))[0];if(cLine)bad.push("文档把不可复现的 Connection: close ⇒ EPIPE 写成事实（同行的否定限定不构成违规；只允许写「不设 Connection: close」这条**可复现的规则**，以及「该因果不可复现」这句**披露**）："+cLine.trim());if(!/2\/3|三分之二/.test(sec))bad.push("§十二 未声明写路径收口只到 2/3");if(!/读侧|读路径/.test(sec))bad.push("§十二 未把读侧触发点与写路径份额分开记");if(bad.length){console.error("诚实边界不合格:\n"+bad.join("\n"));process.exit(1)}console.log("诚实边界 ok（挂账项未越界 + 两个份额分开记 + 无不可复现因果）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `诚实边界 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - §十二 存在且覆盖 12.1–12.9 的要点（门禁逐字检查 26 个关键要点）；**每条边界都能对应到代码事实**（写清符号名或章节号）。
    - §11.3 **不再出现**「不新增第十码」的绝对表述，且提到管理面的 `not_user_owned`。
    - §七 与 §11.8 各含两个新套件的账本行，例数取**实测值**；文档全文**零** `npm test`。
    - 文末 counts-parity 命令的 `suites` 为 5 个、阈值为 `cells<12`，与 `50-VALIDATION.md` 的副本逐字一致（Task 3 校验）。
    - **两条门禁的验证状态（如实披露：未做正方向实跑、未做单点变异 —— 文档内容尚未落地，落地前无法取证）**：执行者写完后**必须自行**做四次单点变异确认转红：删掉 §十二 里任一关键要点或任一 `12.x` 小节标题；把「不新增第十码」改回绝对表述；把 `2/3` 改成 `3/3`；写入一句「`Connection: close` 会导致 EPIPE」。
    - ⚠️ **第一轮修订（iteration 2：越界声明的判据改为「同行否定限定豁免」）**：原判据是**全文件窗口**扫描（`/TD-48-01[\s\S]{0,80}已修/`），会把**如实写明挂账的句子**判成违规 —— 例：「`TD-48-01` / `TD-48-02` 一律保持挂账，本阶段**不声称**它们已修」「P8 失效链**仍不得**声称 `6/6` 全覆盖」「「`Connection: close` 会导致 EPIPE」这一因果**不可复现**」这三句都是本任务要求写下的**诚实披露**，却会各自命中。现改为**逐行判定 + 同行否定限定豁免**（行内含 `不得 / 不声称 / 不要 / 不可复现 / 未 / 禁止 / 无 / 非 / 挂账 / 仍开 / 延后 / 没` 之一即豁免）。**已用合成文档实测四态**：① 上述三句如实披露 ⇒ **不**转红；② 真写「`TD-48-01` 已修复」⇒ 转红；③ 真写「P8 失效链已完整收口，6/6 全覆盖」⇒ 转红；④ 真写「设置 `Connection: close` 会导致客户端 EPIPE」⇒ 转红。**本轮修订（iteration 3 —— 越界声明的判据再加**位置限定**）**：iteration 2 的形态是**逐行 + 同行否定限定豁免**（行内含 `不得 / 不声称 / 不要 / 不可复现 / 未 / 禁止 / 无 / 非 / 挂账 / 仍开 / 延后 / 没` 之一即豁免）。它修掉了跨行误报，却带入一个**同形的假绿**：`无遗漏` / `无残留` 这类**肯定式越界**天然带一个 `无`，整行因此被豁免（本轮 INFO-a 报出的正是这两例）。现改为**位置限定豁免**：对每个命中取两段文本 —— ① 标识符前 40 字；② 标识符到**被断言动词**之间 —— **只有否定限定出现在断言之前**（即它确实在否定该断言）才豁免；否定词出现在动词**之后**不再豁免。**已用合成行实测两方向共 11 条**：① 六条如实披露**全部保持绿**（「`TD-48-01` / `TD-48-02` 一律保持挂账，本阶段**不声称**它们已修」「P8 失效链**仍不得**声称 `6/6` 全覆盖」「「`Connection: close` 会导致 EPIPE」这一因果**不可复现**」「`TD-48-02` 一律保持挂账」「P8 失效链**尚未** 6/6 全覆盖」「本阶段**不声称** `TD-48-01` 已修复」）；② 五条越界声明**全部转红**（「`TD-48-01` 已修复」「`TD-48-01` 已修复，**无残留**」「P8 失效链已完整收口，6/6 全覆盖，**无遗漏**」「P8 已完整收口，6/6 全覆盖，未遗漏」「Connection: close 会导致客户端 EPIPE」）—— 前三条是本轮报出的逃逸形与同形变体，`未遗漏` 是**换一个否定词的第三例**。**两条真实文件对照**：`docs/product/ai-skills.md` 与 `AGENTS.md` 在当前树上**各有 0 条违规**，改判据前后**同值** ⇒ 未给既有内容制造新红。**残余代价（两条方向相反，如实记）**：① **漏判** —— 否定词恰好落在标识符之前、但它否定的是**别的东西**（要读懂语义，正则做不到）；② **误报** —— 用「不再声明 / 不再提」这类**不在豁免词表**里的否定式诚实表述（如「本文不再声明 `TD-48-01` 已修复」）会被判红，执行者需改写成词表内形式（`不得` / `不声称` / `未` / `挂账` 等）。按本仓一贯取舍：**可见的误报优于静默的漏判**。
  </acceptance_criteria>
  <done>`docs/product/ai-skills.md` 的 §十二【管理面】成文（含五条诚实边界与体积口径），§11.3 的第十码矛盾消除，§七 / §11.8 的账本行就位，文末命令扩到 5 套件。</done>
</task>

<task type="auto">
  <name>Task 2: `AGENTS.md` 技能管理面维护约定 + 测试清单刷新</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md`：`:267` 附近的**测试清单行**（单行多套件、文件名与例数间距达数百字符的形态 —— 改它时**不得**破坏该行的结构，账本解析按「同一行内下一个测试文件名」定右边界）、`:272-273`（「技能发现与调用」与「AI 自建技能（`manage_skill`）」两条维护约定的**写法模板**：权威文档指针 + 「改动 X 时必须同步该章节与该节测试清单」+ 若干硬约束）
    - `AGENTS.md` 的其余相关小节：**数据访问分层约定**（主窗口 → `realmAPI` IPC；`realm://` guest → `/api/*` + token；两条不可互换）、**内部页面 CSP**（`style-src ''self''` ⇒ 禁 markup 内联 style）、**弹框居中约定**（`realm://` 页面用 div 遮罩 + CSSOM，不用原生 `<dialog>`）、**开发/正式环境差异 → 发布前必查**
    - `docs/product/ai-skills.md` §十二（Task 1 的产物 —— 维护约定必须指向它）
    - `.planning/phases/50-api-skills/50-PATTERNS.md` 的 `### docs/product/ai-skills.md / AGENTS.md` 一节（三处必须同批刷新的清单）
  </read_first>
  <action>
    1. 在 `AGENTS.md` 的「AI 工作区与 Bash 权限」小节内、**紧接既有「AI 自建技能（`manage_skill`）的维护约定」条目之后**，新增一条 **「技能管理面（设置页 + `/api/skills/*`）的维护约定」**，照既有两条的写法：
       - 权威文档指针：产品说明权威 = `docs/product/ai-skills.md` 第十二节。
       - 同步义务：改动**列表字段与口径 / 体积计量口径 / 启停与卸载语义 / 拒绝面与错误码 / 两入口的转发层 / 状态链与单源文案表 / `/api/*` 的请求体体积闸**时，**必须同步该章节与该节测试清单**。
       - **四条硬约束**：① 判据 / 校验 / 投影全住 `ai-skills-manager.js`，该模块**零 electron 依赖** ⇒ 设置页的 HTTP 端点与主窗口的 IPC 通道**直接复用同一份**，handler 只做转发、零判定（这是「两入口同一权威」的全部机制）；② 管理面名称谓词与写入门（`validateManagedSkillName`）**故意不同宽**（禁用名单是消费侧信号）—— 不得「统一」成严格形态；③ 写路径「重扫**恰一次** + 调用侧补播**恰一次**」，**不得改 `syncAgentSystemPrompt()` 的函数体**（46-04 的方法体断言与 48 的广播次数断言同时钉着它），补播**不得**挪进函数体；④ `LIMITS` **只允许加项**、前端**零字面量**（限额数值一律取管理投影回传的 `limits`）。
       - **测试口径**：`node tests/test-skills-management.js`（管理数据面）+ `node tests/test-skills-http-api.js`（HTTP / 传输面）；两者用例的 `seededNames` 必须**显式注入**（纯 Node 下 `getSeededSkillNamesSafe()` 会降级为 `[]`）；**禁止 `npm test`**（本项目无该脚本）。
       - **诚实边界成文**：设置页收不到主进程广播 ⇒ 多开设置页之间不做即时同步（详见 §十二的诚实边界小节）。
    2. **刷新测试清单行**（`:267` 附近）：新增两个套件的条目（含各自覆盖面描述与**实测**例数），并**保持该行的既有结构**（同一行内「文件名 + 例数」的单元形态不变，不得拆成多行或改变分隔符，否则 counts-parity 的右边界解析会漏检）。**必须**同时保留既有四个套件的条目与例数（若 50-01..50-04 改变了某一套件的例数，此处一并刷新 —— 一律取实测）。
    3. 若 `AGENTS.md` 别处有与本阶段直接冲突的表述（例如把 `syncAgentSystemPrompt()` 的收口写成「已 6/6」或把 `allowed-tools` 说成「已在管理面展示」），**同批修正**并写明理由；**不得**留下自相矛盾。
  </action>
  <verify>
    <automated>node -e 'const a=require("fs").readFileSync("AGENTS.md","utf8");const bad=[];const i=a.indexOf("技能管理面（设置页");if(i&lt;0)bad.push("缺技能管理面维护约定条目");else{const seg=a.slice(i,i+2200);for(const t of ["docs/product/ai-skills.md","第十二节","ai-skills-manager.js","零 electron","转发","谓词","syncAgentSystemPrompt","函数体","LIMITS","零字面量","test-skills-management.js","test-skills-http-api.js","seededNames","npm test"]){if(!seg.includes(t))bad.push("维护约定缺要点 "+t)}}if(!/test-skills-management\.js/.test(a))bad.push("测试清单行未新增 test-skills-management.js");if(!/test-skills-http-api\.js/.test(a))bad.push("测试清单行未新增 test-skills-http-api.js");if(bad.length){console.error("AGENTS 维护约定不合格:\n"+bad.join("\n"));process.exit(1)}console.log("AGENTS 维护约定 ok（权威指针 + 同步义务 + 四条硬约束 + 测试口径 + 两个新条目）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `AGENTS 维护约定 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const a=require("fs").readFileSync("AGENTS.md","utf8");const bad=[];const lines=a.split("\n");const NEG=/(不得|不声称|不要|未|禁止|无|非|挂账|仍开|延后|没)/;const claim=(id,verbs)=&gt;{const re=new RegExp(id+"[^\\n]{0,80}("+verbs+")");const hit=(l)=&gt;{const m=re.exec(l);if(!m)return false;return !NEG.test(l.slice(Math.max(0,m.index-40),m.index))&&!NEG.test(l.slice(m.index,m.index+m[0].length-m[1].length))};return lines.filter(hit)[0]||null};const h1=claim("P8","全覆盖|6\\/6");if(h1)bad.push("AGENTS 出现越界声明（同一行是肯定式表述且无否定限定）："+h1.trim());const h2=claim("TD-48-01","已修");if(h2)bad.push("AGENTS 出现越界声明（同一行是肯定式表述且无否定限定）："+h2.trim());const line=lines.find((l)=&gt;/test-ai-skills\.js/.test(l) && /test-manage-skill\.js/.test(l))||"";if(!line)bad.push("测试清单行未被识别（结构可能被破坏）");else{if(!/test-skills-management\.js/.test(line)||!/test-skills-http-api\.js/.test(line))bad.push("测试清单行未同时包含两个新套件（test-skills-management.js 与 test-skills-http-api.js 都必须在这一行里）")}if(bad.length){console.error("AGENTS 诚实与结构不合格:\n"+bad.join("\n"));process.exit(1)}console.log("AGENTS 诚实与结构 ok（无越界声明 + 测试清单行结构保持）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `AGENTS 诚实与结构 ok`，或 stderr 列出任意一条不合格项</fails_when>
  </verify>
  <acceptance_criteria>
    - 新增的维护约定条目含权威指针、同步义务、四条硬约束与测试口径（门禁逐字检查 15 个要点）；**验证状态（如实披露：未做正方向实跑、未做单点变异）** —— 执行者落地后必须自行变异：删掉「零 electron」⇒ 红；把 P8 写成 6/6 ⇒ 红。**本轮修订**：① 越界声明的判据改为「逐行 + 同行否定限定豁免」（否则「`AGENTS.md` 不得声称 P8 已 6/6」这类**如实写明禁止事项**的句子会被误判）；已用合成 `AGENTS.md` 实测：如实挂账句 ⇒ 不转红，「`TD-48-01` 已修复」⇒ 转红。**本轮修订（iteration 3）**：该判据与 50-05-T1 同批再加**位置限定**（否定限定必须出现在被断言动词**之前**才豁免 —— 否则 `无遗漏` / `无残留` 这类肯定式越界会因行内带一个 `无` 而整行豁免）；已在 50-05-T1 侧实测两方向共 11 条，并对 `AGENTS.md` 跑**真实文件对照**（当前树 0 条违规，改判据前后同值）。② 测试清单行那条判据原为「**任一**新套件在列即通过」（`!a && !b`），只补一个套件时会静默通过 —— 与本条 acceptance「测试清单行含两个新套件的实测例数」不符 ⇒ 改为「两个都必须在同一行里」；已实测：两个都列 ⇒ 通过，只列 `test-skills-management.js` ⇒ 报「测试清单行未同时包含两个新套件」。
    - 测试清单行保持既有结构（同一行内文件名 + 例数的单元形态）；两个新套件的条目在列。
    - `AGENTS.md` 全文**无**「P8 已全覆盖 / 6/6」「TD-48-01 已修」之类越界声明。
    - 维护约定与实际行为一致：删掉 `ai-skills-manager.js` 的零 electron 依赖或把 handler 写成带判定的形态，都会与该条目矛盾（点出「改动时必须同步」的对象）。
  </acceptance_criteria>
  <done>`AGENTS.md` 有技能管理面的权威指针与四条硬约束，测试清单行含两个新套件的实测例数且结构未被破坏，无任何越界声明。</done>
</task>

<task type="auto">
  <name>Task 3: `50-VALIDATION.md` 回填（重键矩阵 + 四条假设处置 + 命令副本）+ counts-parity 收口</name>
  <files>.planning/phases/50-api-skills/50-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/50-api-skills/50-VALIDATION.md`：`:44-76`（Per-Task Verification Map —— 现在按**需求维度**键控，要改为按**已交付的计划 / 任务**键控 `50-01-T1..T3` … `50-04-T1..T3` + `50-05-T3`）、`:80-87`（Wave 0 Requirements —— 逐条标记已交付）、`:91-99`（Manual-Only Verifications —— 四条）、`:1-10`（frontmatter：`status: draft` / `nyquist_compliant: false` / `wave_0_complete: false`）
    - 50-01..50-04 的 PLAN 与 SUMMARY（任务编号与实际交付的映射 —— 重键必须与实际一致，不得凭 plan 的意图填）
    - `docs/product/ai-skills.md` 文末的 counts-parity 命令（Task 1 已扩到 5 套件版 —— 本文件要放**逐字一致**的副本）
    - `.planning/phases/50-api-skills/50-RESEARCH.md` 的 §Assumptions Log（A2 / A3 / A4 / A6 四条待实测项）与 §Validation Architecture（测试跑法与两种跑法的区别）
  </read_first>
  <action>
    1. **重键 Per-Task Verification Map**：把表头与行改为按**已交付的计划 / 任务**键控（`Task ID` 列取 `50-01-T1` … `50-04-T3` 与本计划的 `50-05-T3`），并回填 `Plan` / `Wave` / `Requirement` / `Threat Ref` / `Test Type` / `Automated Command` 六列。**每一行必须对应一个真实存在的任务**（回填前对照各 PLAN 的 `## tasks` 与各 SUMMARY 的实际交付，**不得**凭 plan 的意图填）。`Status` 保持 `⬜ pending`（本文件不在本计划内自证通过 —— 状态由 verify-work 回填）。
       - `Automated Command` 列一律用具名命令：`node tests/test-skills-management.js` / `node tests/test-skills-http-api.js` / `node tests/test-manage-skill.js` / `node tests/test-ai-skills.js` / `node --test tests/test-skill-picker-model.js` / `node tests/test-agent-workspace.js` / `node tests/test-builtin-skills-seeder.js` + 本阶段新增的源码扫描命令。**严禁** `npm test`。
       - **两种跑法不可混**：`test-ai-skills.js` / `test-manage-skill.js` / `test-skills-management.js` / `test-skills-http-api.js` 用 `node tests/<file>.js`（文件内自带 `require(''node:test'')`）；`test-skill-picker-model.js` 用 `node --test`。
    2. **frontmatter**：`wave_0_complete` 置 `true`（Wave 0 的交付已在 50-01 / 50-02 / 50-03 落地）；`nyquist_compliant` **保持 `false`**（由 `/gsd-validate-phase` 判定，本计划不自证）；`status` 保持 `draft`（该字段的生命周期是 draft → validated，由 validate-phase 写）。
    3. **Wave 0 Requirements 清单**：逐条标为已完成并指向落地计划（`tests/test-skills-management.js` → 50-01 建、50-02/50-04 扩；`tests/test-skills-http-api.js` → 50-02 建、50-03 扩；`tests/test-ai-skills.js` 的夹具补 `set` 与写路径用例 → 50-02；`tests/test-manage-skill.js` 的冻结断言刷新 → 50-02；`tests/test-skill-picker-model.js` 的 `STATUS_TEXT.disabled` + 三条断言改写 → 50-01 / 50-04；counts-parity 四处同步 → 本计划）。**Framework install: none**（`node:test` 内建）保持。
    4. **Known-Open Items / Assumptions 处置（A2 / A3 / A4 / A6 逐条写）**：
       - **A2**（`SKILL.md` 是否计入 `bytes`）⇒ 已定为**计入**，并在 §十二 12.2 与 UI-SPEC 区说明 ③ 逐字一致（写清是「已实测 / 已定稿」还是「仅定稿未实测」—— 如实写）。
       - **A3**（设置页在 `realm://` CSP 下加载 `skill-picker-model.js`）⇒ 转入 **Manual-Only** 表（`npm run dev` → 打开设置页 → 断言 `window.SkillPickerModel` 存在），并补一条**源码扫描**断言（`settings.html` 含该 `<script>` 且排在 `settings-page.js` 之前 —— 50-01 已落）。
       - **A4**（413 探针在 Electron 内复跑）⇒ 如实写「自动用例跑在**系统 Node**（v22.22.0）上；Electron 43.6.0 内的 Node 24.20.0 复跑属人工验证」，并保留在 Manual-Only 表。
       - **A6**（100 条技能时管理投影的字节数与渲染耗时）⇒ 转入 **Manual-Only** 表，并写明「`limits` 与投影**不含正文**」这一结构性依据（最坏约 200 KB 的估值来自「100 条 × 描述 ≤1024 字符 + 诊断」，**未经实测**）。
    5. **Manual-Only 表**：已有四条（多窗口 / 多设置页实例间同步、Electron 内的 413 复跑、CSP 下加载 `skill-picker-model.js`、禁用后 `/` 面板可见性）保留；补入 A6 的渲染观感与窄窗口行首行实测（E5 的 backstop 条目 —— **必须带正命题**，不得是「否命题空集真」形态）。**另必须补两行**（这两条是**锁定决策**与既有纪律明文的验收面，漏登即「验收面悄悄消失」）：
       - **「未配置任何 provider 时仍能列出两个内置技能」**（`50-CONTEXT.md` 的 D-19 验收面原文 + `50-RESEARCH.md:83` / `:894` 两处重复要求）：`Requirement` 列写 `USER-01（D-19 的验收面）`；`Why Manual` = 「无 provider 的判据依赖真实 `AIManager` 实例与 `configStore` 的组合状态；纯 Node 单测只能证 `ensureSkillsFresh()` 的**分流**（50-01-T1 的 tracer 用例），端到端呈现须真机；`npm run dev` 打开设置页 → 断言**两个随包内置技能在列**且 `refreshedAt > 0`（**不得**出现「技能列表尚未加载」；可用从未配置过 provider 的 `realm-dev` 用户数据，或临时清空 `settings.ai` 的 Key）。
       - **「打包态（`make install` 产物）列出内置技能」**（`50-RESEARCH.md:1547` 的 Manual-Only 行）：`Requirement` 列写 `USER-01`；`Why Manual` = 「`asarUnpack` + `app.isPackaged` 的路径分支只在正式 / `make install` 产物上生效（`builtin-skills-seeder.js` 的 `resolveBuiltinSkillsSrc()`），`npm run dev` 覆盖不到 —— 这正是 `AGENTS.md`『开发/正式环境差异 → 发布前必查』的纪律；判据 1 依赖随包内置技能在**打包产物**里也在盘上」。`Test Instructions` = `make install` → 启动 `/Applications/Realm.app` → 打开设置页 → 断言内置技能组列出两个技能；播种失败时顶部汇总条须给出 `realm_*` 诊断，**不得**静默为空。
    6. **更新文末的 counts-parity 命令副本**为 Task 1 落地后的 **5 套件版**（`suites` 含两个新套件、阈值 `cells<12`），与 `docs/product/ai-skills.md` 的版本**逐字一致**。
    7. **跑一次 counts-parity 并让它转绿**（本计划是最后一个改动测试相关面的计划）：命令取 `docs/product/ai-skills.md` 文末（或本文件的副本）的 5 套件版。若报出「账本单元取到 X ≠ 实测 Y」，**改账本而不是改判据**；若报出「账本未覆盖某套件」，**补账本单元而不是把该套件从 `suites` 里删掉**。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const v=fs.readFileSync(".planning/phases/50-api-skills/50-VALIDATION.md","utf8");const d=fs.readFileSync("docs/product/ai-skills.md","utf8");const bad=[];if(!/wave_0_complete:\s*true/.test(v))bad.push("wave_0_complete 未置 true");if(!/nyquist_compliant:\s*false/.test(v))bad.push("nyquist_compliant 必须保持 false（不得自证）");if(v.split("\n").some((x)=&gt;x.includes("npm test")&&!/不存在|没有|无该|无此|禁止|不得|严禁|不提供|不使用|不出现|勿|改用|替换|零/.test(x)))bad.push("VALIDATION 把 npm test 当命令使用（本项目无该脚本；说明性文字不算违规）");for(const t of ["50-01-T","50-02-T","50-03-T","50-04-T"])if(!v.includes(t))bad.push("Per-Task 矩阵未按计划任务重键（缺 "+t+"）");for(const s of ["test-skills-management.js","test-skills-http-api.js","test-manage-skill.js","test-ai-skills.js","test-skill-picker-model.js"])if(!v.includes(s))bad.push("矩阵缺套件 "+s);for(const a of ["A2","A3","A4","A6"])if(!v.includes(a))bad.push("缺假设处置 "+a);if(!v.includes("两个内置技能"))bad.push("Manual-Only 表缺 D-19 那条（表内未出现「两个内置技能」字样）");if(!v.includes("make install"))bad.push("Manual-Only 表缺打包态那条（表内未出现「make install」字样）");if(!/USER-01（D-19 的验收面）/.test(v))bad.push("D-19 那条的 Requirement 列未写明 USER-01（D-19 的验收面）");if(!/cells&lt;12/.test(v))bad.push("命令副本未扩到 5 套件");const pick=(t)=&gt;{const i=t.indexOf("const suites=");return i&lt;0?"":t.slice(i,i+220)};const a1=pick(v).replace(/\s+/g," "),a2=pick(d).replace(/\s+/g," ");if(!a1)bad.push("50-VALIDATION.md 缺 counts-parity 命令副本（未找到 const suites= —— 不存在与不一致必须都算失败）");if(!a2)bad.push("docs/product/ai-skills.md 缺 counts-parity 命令副本（未找到 const suites=）");if(a1 && a2 && a1!==a2)bad.push("两处 counts-parity 命令的 suites 副本不一致");if(bad.length){console.error("VALIDATION 回填不合格:\n"+bad.join("\n"));process.exit(1)}console.log("VALIDATION 回填 ok（重键 + wave_0 置真 + 四条假设处置 + 命令副本 5 套件）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `VALIDATION 回填 ok`，或 stderr 列出任意一条不合格项</fails_when>
    <automated>node -e 'const cp=require("child_process"),fs=require("fs");const suites=["tests/test-manage-skill.js","tests/test-ai-skills.js","tests/test-skill-picker-model.js","tests/test-skills-management.js","tests/test-skills-http-api.js"];const meas={};for(const f of suites){const o=cp.execSync("node "+(f.indexOf("picker")&gt;=0?"--test ":"")+f,{encoding:"utf8",stdio:["ignore","pipe","ignore"]});meas[f.split("/").pop()]=String(o.match(/# tests (\d+)/)[1])}const FN=/test-[\w-]+\.js/g;let bad=[],cells=0;for(const file of ["AGENTS.md","docs/product/ai-skills.md"]){const hit={};fs.readFileSync(file,"utf8").split("\n").forEach((line,ln)=&gt;{const idx=[];FN.lastIndex=0;let m;while((m=FN.exec(line)))idx.push([m.index,m[0]]);for(let i=0;i&lt;idx.length;i++){const base=idx[i][1];if(!(base in meas))continue;const end=i+1&lt;idx.length?idx[i+1][0]:line.length;const uniq=[...new Set([...line.slice(idx[i][0],end).matchAll(/(\d+)\s*例/g)].map(x=&gt;x[1]))];if(!uniq.length)continue;cells++;hit[base]=(hit[base]||0)+1;if(uniq.length!==1||uniq[0]!==meas[base])bad.push(file+":"+(ln+1)+" "+base+" 账本单元取到 ["+uniq.join("/")+"] ≠ 实测 "+meas[base])}});for(const base of Object.keys(meas))if(!hit[base])bad.push(file+" 的账本未覆盖 "+base)}if(cells&lt;12)bad.push("账本单元数 "+cells+" &lt; 12（两个新套件各需 2 个单元）");if(bad.length){console.error("例数不一致: "+bad.join("; "));process.exit(1)}console.log("counts-parity ok  cells="+cells+" measured="+JSON.stringify(meas))'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `counts-parity ok`，或 stderr 含 `账本单元取到` / `账本未覆盖` / `账本单元数`</fails_when>
    <automated>node tests/test-manage-skill.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node tests/test-ai-skills.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node --test tests/test-skill-picker-model.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node tests/test-skills-management.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node tests/test-skills-http-api.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node --test tests/test-agent-workspace.js 2>&1 | grep -E "^# (tests|pass|fail)"  &&  node tests/test-builtin-skills-seeder.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>任一套件输出里出现计数非 0 的 `# fail` 行，或任一套件缺少 `# pass` 行，或任一命令非零退出（这是 Phase 50 的阶段门禁：全套件必须全绿）</fails_when>
  </verify>
  <acceptance_criteria>
    - `50-VALIDATION.md` 的 Per-Task 矩阵按**真实存在的任务**重键（`50-01-T1..T3` … `50-04-T3` + `50-05-T3`），每行有对应的具名命令，零 `npm test`。
    - `wave_0_complete: true`；`nyquist_compliant` 保持 `false`；`status` 保持 `draft`。
    - A2 / A3 / A4 / A6 四条逐条有处置（已定稿 / 转 Manual-Only / 未实测如实披露 —— 不得把未实测写成已实测）。
    - 两处 counts-parity 命令副本**都存在**且 `suites` 逐字一致（门禁比对 `const suites=` 后的片段；**任一副本缺失也算失败** —— 不得用「两边都在才比对」的形式静默跳过）。**该「缺失即失败」分支已实测**：当前树上 `50-VALIDATION.md` 尚无该副本 ⇒ 门禁实测报出「50-VALIDATION.md 缺 counts-parity 命令副本（未找到 `const suites=` —— 不存在与不一致必须都算失败）」（修复前该情形会被静默跳过）。
    - **counts-parity 转绿**：`cells ≥ 12` 且每套件在两个文件里都有账本单元、例数与实测逐字一致。
    - **Manual-Only 表含原有四条 + 本节新补的四条**（A6 渲染观感 / E5 窄窗行首行 backstop / **D-19「未配置任何 provider 时仍能列出两个内置技能」** / **打包态（`make install` 产物）列出内置技能**）：后两条**可机械核验**，且该核验**已落进门禁**（本轮修订补齐 —— 此前只有 acceptance 声明、四条门禁里没有任何一条查它）：`50-VALIDATION.md` 的 VALIDATION 门禁现断言表内出现「两个内置技能」与「make install」两处字样，且 D-19 那条的 `Requirement` 列写明 `USER-01（D-19 的验收面）`（缺任一即报「Manual-Only 表缺 … 那条」/「D-19 那条的 Requirement 列未写明 …」）。**该三条断言在当前树上实测为红**（该表尚未补写这两行），属「新代码未落地」类。
    - **全套件全绿**：七个套件（含 `test-agent-workspace.js` / `test-builtin-skills-seeder.js` 两个回归套件）的 `# fail` 均为 0。
  </acceptance_criteria>
  <done>`50-VALIDATION.md` 的矩阵按实际交付重键、四条假设逐条处置、命令副本与产品文档一致；counts-parity 转绿；Phase 50 的全套件门禁全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 / 账本 → 后续阶段（51）与用户 | 文档是**下一阶段的唯一口径来源**；误写「已修 / 已闭合」会被当成前提 |
| 测试清单的例数账本 → 机械判据 | 本文件与 `AGENTS.md` / `docs` 的单元被 counts-parity 逐字比对 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-32 | Repudiation | 文档误声明「已修 / 已闭合 / 6/6 全覆盖」⇒ 后续阶段据此跳过工作（不可逆的账本污染） | high | mitigate | 门禁对 `TD-48-01` / `TD-48-02` / `WR-02` / `WR-06` / P8 的越界声明做正向扫描（两条诚实边界门禁）；另由「写路径 2/3 + 读侧另计」的显式两个数防止被读成 6/6。**这两条门禁的验证状态如实披露**：只做过负方向实跑，未做正方向与单点变异（文档内容尚未落地），变异义务见 T1/T2 的 acceptance。禁止在 `STATE.md` 里手工改台账 |
| T-50-33 | Tampering | 账本单元与实测例数漂移（机械判据的承重面） | medium | mitigate | counts-parity（5 套件版）逐单元比对 + 逐套件覆盖检查；**本计划是最后一个改动测试相关面的计划 ⇒ 必须在本计划内转绿**。两处命令副本逐字一致由门禁比对 |
| T-50-34 | Information Disclosure | 文档把不可复现的因果（`Connection: close` ⇒ EPIPE）写成事实 ⇒ 误导后续实现 | low | mitigate | 门禁扫描该因果表述；文档只写可复现的规则（不 `destroy()`、不设 `Connection: close`、用 `resume()`） |
| T-50-35 | Repudiation | 把「读侧触发点」并入写路径份额 ⇒ 被读成 P8 已 6/6 | medium | mitigate | §十二 与 `AGENTS.md` 均要求**两个数分开记**（写路径 2/3；读侧触发点另计）；门禁断言 §十二 同时出现 `2/3` 与「读侧」 |
| T-50-36 | Tampering | 验证矩阵按「意图」而非「实际交付」重键 ⇒ 覆盖声明失实 | medium | mitigate | 重键前必须对照各 PLAN 的 `## tasks` 与各 SUMMARY 的实际交付；矩阵的 `Automated Command` 列必须是**可跑**的具名命令（含两个新套件） |
| T-50-SC | Tampering | npm / pip / cargo 安装 | high | mitigate | 本阶段**零新增依赖**，无安装动作 ⇒ 无供应链面 |
</threat_model>

<verification>
- 三条任务门禁全绿；**counts-parity（5 套件版）转绿**（`cells ≥ 12`）；七个套件的 `# fail` 全为 0。
- 本计划的门禁**逐条如实披露（不得整段声称已预跑）**：**唯一具备落地前正/反证据的是 counts-parity 的机制** —— 3 套件版正方向实跑通过（`cells=8`、`measured=55/178/111`），且在内存单点变异（`178 例 → 179 例` 于 `docs/product/ai-skills.md:98`）下转红；**5 套件版本身未跑过**（两个新套件尚未存在），执行者必须首跑。其余五条门禁（§十二 要点与结构、§十二 诚实边界、`AGENTS` 维护约定、`AGENTS` 诚实与结构、`VALIDATION` 回填）**全部未做**正方向实跑与单点变异 —— 它们判的都是**尚未写下的文档内容**，落地前无法取证；变异义务已逐条写进各任务 `<acceptance_criteria>`。**本轮修订（iteration 3）另用合成行取证的子判据**：越界声明的判据从「同行否定限定豁免」改为**位置限定豁免**（否定限定必须出现在被断言动词**之前**才算豁免）。已实测两方向共 11 条（6 条如实披露保持绿 / 5 条越界声明全部转红，含 `无遗漏`、`无残留`、`未遗漏` 三种同形），并对 `docs/product/ai-skills.md` 与 `AGENTS.md` 各跑一次**真实文件对照**（当前树各 0 条违规，改判据前后同值）—— 细节与残余代价见 50-05-T1 的 acceptance；同为合成输入的取证，不是对文档内容的取证。**本轮修订（iteration 2）另用合成文档取证的子判据**：① 越界声明的判据改为「逐行 + 同行否定限定豁免」后，如实挂账/披露句不转红、肯定式越界声明转红（各实测一次，覆盖 TD-48-01 / P8 / `Connection: close` 三组）；② `AGENTS.md` 测试清单行改为「两个新套件都必须在同一行里」，只补一个套件时转红（实测）；③ `50-VALIDATION.md` 的门禁补齐了 Manual-Only 表两条新行的**机械核验**（「两个内置技能」/「make install」/`USER-01（D-19 的验收面）` 三条断言，当前树上实测为红）。**这三组都是对「门禁自身可失败性」的取证（合成输入），不是对文档内容的取证。**
- 本计划**不改源码与业务测试**：`docs/product/ai-skills.md` / `AGENTS.md` / `50-VALIDATION.md` 三个文件之外无误改。
- **人工面**：§十二 是产品说明，其正确性由「与代码行为一致」承担 —— 执行者须逐条对照 50-01..50-04 的 SUMMARY 与源码，**不得**照着 PLAN 的意图写（plan 与实现若有偏差，以实现为准并如实写进 SUMMARY）。
</verification>

<success_criteria>
- `docs/product/ai-skills.md` §十二【管理面】成文，含五条诚实边界、体积口径、`allowed-tools` 缺席原因、`nameClash` 的归属、两入口与不可互换理由、体积闸形状。
- §11.3 的「不新增第十码」被修订为「工具侧九条不变 + 管理面 `not_user_owned`」。
- `AGENTS.md` 有技能管理面的权威指针与四条硬约束；测试清单行含两个新套件的实测例数。
- `50-VALIDATION.md` 的矩阵按实际交付重键、四条假设逐条处置、命令副本与产品文档一致。
- counts-parity（5 套件）通过；Phase 50 的全套件门禁全绿。
</success_criteria>

<output>
Create `.planning/phases/50-api-skills/50-05-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下文档结构由本计划新建**，不是既有结构的漂移。评审时不得要求它们在 base 上存在。（50-01..50-04 已登记的代码符号不在此重复；本阶段的完整清单以 50-01 的 §「Artifacts this phase produces」为准。）

| 类别 | 符号 / 结构 | 落点 |
|------|-------------|------|
| 文档章节 | `## 十二、管理面（设置页 + /api/skills/*）`（含 12.1–12.9 九个小节：列表字段与口径 / 体积与文件数口径 / 启停 / 卸载 / 诊断与状态 / 五条诚实边界 / 两个前端入口 / 请求体体积闸 / 维护约定与测试） | `docs/product/ai-skills.md` |
| 文档修订 | §11.3 的「不新增第十码」⇒「工具侧九条不变 + 管理面 `not_user_owned`（第十码，见 §十二）」 | `docs/product/ai-skills.md` |
| 文档账本行 | §七 与 §11.8 新增两个套件的行（`node tests/test-skills-management.js` / `node tests/test-skills-http-api.js`，实测例数） | `docs/product/ai-skills.md` |
| 可重跑判据（**改版**） | 文末的 counts-parity 命令：`suites` 从 3 扩到 5、阈值 `cells<8` ⇒ `cells<12` | `docs/product/ai-skills.md`（权威副本同步在 `50-VALIDATION.md`） |
| 维护约定条目 | 「技能管理面（设置页 + `/api/skills/*`）的维护约定」（权威指针 + 同步义务 + 四条硬约束 + 测试口径 + 诚实边界） | `AGENTS.md` |
| 测试清单行（**刷新**） | `- 测试：` 行新增两个套件的条目与实测例数（保持单行多套件结构） | `AGENTS.md` |
| 验证矩阵（**重键**） | Per-Task Verification Map 改为按 `50-01-T1..T3` … `50-05-T3` 键控 | `.planning/phases/50-api-skills/50-VALIDATION.md` |
| frontmatter 字段值 | `wave_0_complete: true`（`nyquist_compliant` 保持 `false`、`status` 保持 `draft`） | `.planning/phases/50-api-skills/50-VALIDATION.md` |
| 假设处置 | A2 / A3 / A4 / A6 四条逐条处置（已定稿 / 转 Manual-Only / 未实测如实披露） | `.planning/phases/50-api-skills/50-VALIDATION.md` |

