---
phase: 49-manage-skill-ai
plan: 08
type: execute
wave: 5
depends_on: [49-02]
files_modified:
  - src/renderer.js
  - src/styles/main.css
  - tests/uat-49-g49-4-card-a11y-tab-order.js
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/phases/49-manage-skill-ai/49-UI-SPEC.md
  - .planning/phases/49-manage-skill-ai/49-UI-REVIEW.md
autonomous: true
gap_closure: true
gap_ids: [UI-49-W6-01]
gap_source: .planning/phases/49-manage-skill-ai/49-UI-REVIEW.md
requirements: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

key-decisions:
  - '**修法选 C（卡片语境不施加焦点语义），不选 A′（卡片头部接管可达性）、不选 A（全仓卡片范式升级）。** 裁决依据三条：① `49-UI-SPEC.md:508` 逐字锁定了「展开 / 折叠（卡片）…… **本阶段零改动**（不新增键盘语义 —— 全仓所有工具卡片同一范式）」—— A′ 与 A 都要推翻这条锁定决策，而本计划是 **gap-closure**（职责是闭合、不是重设计）；② A 的爆炸半径覆盖 `renderToolCard` 的**全部**工具卡片（execute_action / read / …），会牵动其他阶段的既有测试基线与契约，对一次 gap-closure 属超范围；③ C 的代价（卡片仍无键盘展开入口）与 **49 之前完全持平**，是**非回归**的既存面，而 A/A′ 带来的「仅本变体可键盘展开」范式分裂是新面。**A/A′ 不是被否决，而是被登记为需单独立项的全仓范式升级**（Phase 50 的设置页技能管理区是同一族折叠控件的第二个宿主，届时一并决策更合适）。'
  - '**C 的精确含义**：`renderSkillContentBox` 增加 `{ interactive = true }` 选项；`interactive === false` 时**不设** `role` / `tabindex` / `aria-expanded`、**不绑** `keydown`（`click` 保留 —— 卡片范式本就是鼠标语义，与契约 :508 的零改动一致）。气泡调用点（`renderer.js:9067`）**逐字不动**，故 48-UI-REVIEW Pillar 6 的那条修复在原位**完整保留**；卡片调用点（`:9739`）传 `{ interactive: false }`。'
  - '**承重判据必须是真实渲染的键盘遍历，不是源码扫描。** 本阶段已反复记录「子串 / 声明扫描会假绿」（G-49-3 的旧驱动 `noteNotClipped` 就是同型假绿：比的是元素自身的 `scrollWidth` / `clientWidth`，而裁切 / 焦点发生在其**祖先**上）。因此 Task 1 的核心判据是真实 `playwright _electron` 下的**真实 Tab 遍历**：`document.activeElement` 逐个站检查，凡落在 `.tool-card` 内的停靠点都必须「外接矩形高 > 0 **且** 其中心点命中测试落回自身（或其祖先 / 后代）」。源码面护栏（Task 2）只作辅助，**不得**单独作为闭合依据。'
  - '**非空真（红→绿）的实现方式：红轮本身就是单点变异态。** 判据 R3 在绿轮可能**空真**（卡片内零停靠点 ⇒ 每个停靠点可见 = 空集真）。故：① 用 R2 断言遍历本身有效（停靠点总数 ≥ 20，且至少命中过一个非 `body` 元素）—— 排除「遍历没跑起来」；② 用 **R6 单点变异自检**：把卡片调用点的 `{ interactive: false }` 去掉即回到红轮形态，R3 必须转红（实测：`.ai-skill-content-box-header` 会被 Tab 命中，且其中心点命中测试返回**另一条消息里的 `<CODE>`**）。红轮证据落盘即该变异态的实测记录。**若红轮不红（R3 在未修复树上为绿），说明驱动取错了判据对象，必须先修驱动，不得改源码。**'
  - '**判据必须方向无关（不把「卡片内零键盘停靠点」写成 must-have）。** 若日后真做了 A/A′ 的范式升级（卡片头部成为可聚焦入口），那是 a11y 的**改善**，不应让本门禁转红。因此 R3 判的是「**凡在卡片内的停靠点都必须可见**」，而不是「卡片内不得有停靠点」。C 的实现让该集合为空（通过），A 的实现让该集合含一个可见停靠点（同样通过），只有「不可见的停靠点」才会失败 —— 这才是本 gap 的真实不变式。'
  - '**不动契约 :508 的锁定决策，但要修正契约对 a11y 增量的适用范围描述。** `49-UI-SPEC.md:427`（「该补齐作用于**唯一构建实现** ⇒ 气泡实例同时获得，属**纯增量**」）与 `:509`（「展开 / 折叠（正文块）| 点击折叠块 header 切换 `.collapsed`；**新增** `role="button"` + `tabindex="0"` + `aria-expanded` + Enter/Space」）都把增量写成**无条件的** —— 这与 C 之后的事实不符，须补上「气泡实例；卡片实例因宿主折叠态（`max-height: 0` 而非 `display: none`，后代不自动退出顺序焦点导航）不施加焦点语义」的限定与理由。同款口径同步到 `docs/product/ai-skills.md` §11.7。'
  - '**不得把 `.tool-card-content` 的折叠机制从 `max-height: 0` 改成 `display: none`。** 这是另一条看似更省事的路线（`display: none` 会自动把后代移出 Tab 序），但它会**杀掉 200ms 的 `max-height` 过渡**，即全仓所有工具卡片的展开 / 折叠动画 —— 属「用一个视觉回归换一个 a11y 修复」，且爆炸半径覆盖全部卡片。`visibility: hidden` 的等价方案同样要动过渡与全部卡片，一并排除。'
  - '**例数必须按 49-06 的既有纪律取实测值回填，不得硬编码预测值。** Task 2 会新增用例，`tests/test-ai-skills.js` 的实测 `# tests` 会变化，`AGENTS.md` 测试行三条 + `docs/product/ai-skills.md` §七两条 + §11.8 三条 = **八个账本单元**必须在同一任务刷新，并用 §11.8 既有的可重跑一致性命令验收（不是人工核对）。'
  - '**安全纪律（沿用 49-07 的既有教训）**：驱动只经 playwright `_electron.launch` 启动**自己拉起的 dev 子进程**（`NODE_ENV=development`，`realm-dev` userData），收尾 `electronApp.close()` + `process.exit(code)`。**严禁** `pkill` / `pgrep` 模式匹配杀进程 —— 撰写的当时实测用户正在运行正式版 `/Applications/Realm.app`（PID 22083）；若驱动被中断，只按**自己记录的 PID** 清理（`ps -p <pid> -o command=` 核对命令确属 `Projects/Realm/node_modules/electron` 后再 kill）。'
  - '**所有验证命令写成 `node …` 显式形式，不引用 `npm test`** —— 本项目 `package.json` 无该脚本，GSD 的两处 gate 恒解析成不存在的 `npm test` 是已记录的环境问题。'
  - '**本计划由编排器（主会话）撰写，未经 `gsd-planner` 子代理与 `gsd-plan-checker` 独立环节。** 原因：`/gsd-verify-work 49` 的 verify:post → `ui-review` 步骤在派发子代理时命中 **429 配额限制**（重置 2026-09-14 21:30 UTC+8），`gsd-ui-auditor` / `gsd-planner` / `gsd-plan-checker` 同属该配额。按既有降级约定由主会话接管。**执行者必须把这一点当作降低信任度的信号**：`<verify>` 里每条门禁都要亲自跑一遍红 / 绿两态，不得因为「计划写了」就跳过实测。'

must_haves:
  truths:
    - '**真性 · 主轴（真实渲染 + 真实键盘）**：在一张**折叠态**的 `manage_skill` 卡片上，对全页面做真实 Tab 遍历时，**不存在**落在该卡片内的「不可见停靠点」—— 即凡 `document.activeElement` 位于某个 `.tool-card` 内，它的外接矩形高必须 > 0，且其中心点的 `document.elementFromPoint` 必须落回它自身（或其祖先 / 后代）。修复前实测违反 1 条（`.ai-skill-content-box-header`，矩形高 29 但其中心点命中到**另一条消息的 `<CODE>`**）。'
    - '**非空真 · 遍历有效性**：同一轮遍历的停靠点总数 ≥ 20，且至少命中过一个非 `body` 元素 —— 排除「Tab 遍历没跑起来」导致的空真绿。修复前实测 46 站、卡片域内 1 站。'
    - '**前提成立**：被判定的那张卡片在遍历时确实处于折叠态，且其 `.tool-card-content` 的 computed `max-height` 解析为 `0px` —— 排除「拿一张已展开的卡片判绿」。'
    - '**对照 · 防过度修复（气泡面）**：**气泡**实例（不在任何 `.tool-card` 内的 `.ai-skill-content-box-header`）仍带 `role="button"` + `tabindex="0"` + `aria-expanded`，且真实 Enter 键能切换其 `.collapsed` 并同步 `aria-expanded` —— 48-UI-REVIEW Pillar 6 的修复在原位保留，C 不是「一刀切删掉增量」。'
    - '**语境一致性**：卡片实例的折叠块 header 在**任何**卡片状态下（折叠与展开）都不带 `tabindex` / `role` / `aria-expanded`（C 的语义是「卡片范式为鼠标语义」，不随状态切换 —— 避免出现「展开后突然多一个焦点语义」的第三种形态）。'
    - '**单源未破**：`renderSkillContentBox` 仍**恰 1 处定义**、**恰 2 处调用**（气泡 / 卡片），未新增第二份折叠实现、未新增第三个使用面而漏传选项。'
    - '**非空真 · 源码面红→绿**：源码形态判据在**未修复树**上为红（无 `interactive` 选项、属性未受守卫、卡片调用点未传 `false`），修复后为绿；判据对象是**剥注释后的代码**（本阶段已记录：关于代码的断言不得被同一文件里的散文满足）。'
    - '**契约与文档口径一致**：`49-UI-SPEC.md:427` 与 `:509` 两处对 a11y 增量适用范围的描述已补上「气泡实例；卡片实例不施加」的限定与理由；`docs/product/ai-skills.md` §11.7 同款口径。'
    - '**账本一致**：`AGENTS.md` 测试行三条 + `docs/product/ai-skills.md` §七两条 + §11.8 三条共八个例数单元与三套件**实测** `# tests` 逐字一致（§11.8 的可重跑命令判定，非人工核对）。'
    - '**无回归**：`tests/test-manage-skill.js` / `tests/test-ai-skills.js` / `tests/test-skill-picker-model.js` 三套件 `# fail 0`，且 `tests/test-*.js` 全量**全部退出码 0**。'
    - '**安全边界**：驱动只操作自己拉起的 `realm-dev` 子进程；用户的正式版 `/Applications/Realm.app` 全程未被操作；全程无任何 `pkill` / `pgrep` 模式匹配杀进程。'
  artifacts:
    - 'src/renderer.js：`renderSkillContentBox` 增加 `{ interactive = true }` 选项并在该守卫内施加焦点语义；卡片调用点传 `{ interactive: false }`；气泡调用点逐字不动'
    - 'src/styles/main.css：只改 `.ai-skill-content-box-header:focus-visible` 上方的注释（说明焦点环仅服务气泡实例），**零声明改动**'
    - 'tests/uat-49-g49-4-card-a11y-tab-order.js：新增可重跑的真实渲染 + 真实键盘门禁（Tab 遍历 / 命中测试 / 气泡对照 / Enter 切换 / 单点变异自证），脚本名以 `uat-` 开头故永不被 `test-*.js` 类套件拾取'
    - 'tests/test-ai-skills.js：M9 的 a11y 增量断言改为「条件施加」形态 + 新增语境与调用点断言'
    - 'docs/product/ai-skills.md + AGENTS.md：§11.7 口径补限 + 八个账本单元按实测刷新'
    - '.planning/phases/49-manage-skill-ai/49-UI-SPEC.md：`:427` / `:509` 两处补限（**不改 `:508`**）'
    - '.planning/phases/49-manage-skill-ai/49-UI-REVIEW.md：把 `UI-49-W6-01` 标注为 closed-by 本计划（**不改其余评分与发现**）'
  key_links:
    - '`renderSkillContentBox` 的 `interactive` 选项 → 两个调用点：气泡不传（缺省 true ⇒ 增量保留）、卡片传 false（⇒ 无焦点语义）。可核对面是源码形态判据（剥注释后）与真实渲染的 Tab 遍历结果，二者不可互相替代'
    - '`.tool-card-content` 的 `max-height: 0` → 后代仍在顺序焦点导航内：这是**浏览器语义**（`display: none` 才会移出），不是本项目可调的行为 ⇒ 判据只能落在「有没有停靠点 / 停靠点可不可见」，不能落在「声明写没写」'
    - '折叠块 header 的可见性 → 宿主语境：气泡里恒可见（焦点语义成立）、卡片里默认被裁切（焦点语义失效）⇒ 同一份构建代码在两种宿主下需要**语境相关**的行为，这是本 gap 的全部成因'
---

<objective>
闭合 `49-UI-REVIEW.md` 的 BLOCKER `UI-49-W6-01`：49-02 给共享构建函数 `renderSkillContentBox` 的折叠块 header 补了 `role="button"` + `tabindex="0"`（本意是兑现 48-UI-REVIEW Pillar 6 的建议），但当该构建产物被放进**卡片语境**（`renderer.js:9739`）时，它落在 `.tool-card-content` 这个**默认折叠**的容器内 —— 该容器用 `max-height: 0; overflow: hidden` 实现折叠（为保住 200ms 过渡），而 `overflow: hidden` **不会**把后代移出顺序焦点导航。结果是：折叠的 `manage_skill` 卡片上出现一个**零可见高度**的 Tab 停靠点，焦点环被祖先完全裁掉，且它是卡片内**唯一**的键盘停靠点（卡片头部本身不可聚焦）。

Purpose: 让「键盘焦点永远落在看得见的控件上」这条不变式在卡片语境下成立，同时**不牺牲**气泡语境（那里控件恒可见，增量有效）与**不推翻**契约 `:508` 的卡片范式锁定决策。

Output: `src/renderer.js` 的语境相关焦点语义（`{ interactive = true }` 选项 + 卡片调用点传 `false`）+ `tests/uat-49-g49-4-card-a11y-tab-order.js` 真实渲染门禁（真实 Tab 遍历 + 命中测试 + 气泡对照 + 红→绿两轮证据）+ `tests/test-ai-skills.js` 的源码形态护栏 + `49-UI-SPEC.md:427/:509` 与 `docs/product/ai-skills.md` §11.7 的口径补限 + 八个账本单元按实测刷新 + `49-UI-REVIEW.md` 的 gap 闭合标注。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-manage-skill-ai/49-UI-REVIEW.md
@.planning/phases/49-manage-skill-ai/49-UI-SPEC.md
@.planning/phases/49-manage-skill-ai/49-VERIFICATION.md
@.planning/phases/49-manage-skill-ai/49-UAT.md
@.planning/phases/49-manage-skill-ai/49-07-PLAN.md
@.planning/phases/48-skill-name/48-UI-SPEC.md
@src/renderer.js
@src/styles/main.css
@tests/test-ai-skills.js
@docs/product/ai-skills.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端「折叠卡片内不存在不可见键盘停靠点」—— 写驱动 → 在当前树上跑红 → 改源码 → 跑绿</name>
  <files>tests/uat-49-g49-4-card-a11y-tab-order.js, src/renderer.js, src/styles/main.css</files>
  <precondition>全局 playwright 可用（`NODE_PATH="$(npm root -g)" node -e "process.exit(require('playwright')._electron?0:1)"` 退出码 0）；`~/Library/Application Support/realm-dev/` 下存在至少一个含 `manage_skill`（且带正文折叠块，即 create / update 成功态）卡片的 AI 会话（`2026-09-13` 实测 50 个历史会话中有之，命中 `3dec0492-01e9-49c5-bbf0-5d8f04e84035`）。任一不成立即停手上报，**不得**改用合成 DOM（手工构造被测实现从不构造的对象正是本阶段记录过的假绿来源）、不得跳过断言后报绿。两项由驱动第 0 步自检并输出 `E-PW` / `E-DATA` 失败码。</precondition>
  <action>
本任务是本 gap 的垂直切片：从一句源码改动一路走到真实键盘的行为，中间不停在任何一层。**顺序不可调换** —— 先写驱动并在**当前（未修复）树**上跑出一轮红证据，再改源码，再重跑得绿。红轮证据是本判据「不是恒真断言」的唯一证明（绿轮在卡片内零停靠点时可能空真），缺失即视为本任务未完成。

**第 1 步 · 写驱动 `tests/uat-49-g49-4-card-a11y-tab-order.js`（不改任何源码）**

文件头 JSDoc 必须写明：用途（`UI-49-W6-01` 的真实渲染 + 真实键盘门禁）、运行方式（须带全局 playwright，即 `NODE_PATH="$(npm root -g)" node tests/uat-49-g49-4-card-a11y-tab-order.js`）、为什么不能用源码扫描替代（焦点与裁切都是**运行时**事实；本阶段已记录「子串 / 声明存在 ≠ 行为成立」）、以及脚本名以 `uat-` 开头故永不被 `test-*.js` 套件拾取。骨架与启动方式照抄 `tests/uat-49-g49-3-panel-layout.js`（同一个 `_electron.launch({ args: ['.'], cwd: REPO_ROOT, executablePath: ELECTRON_EXECUTABLE, env: { ...process.env, NODE_ENV: 'development' } })`、同一个「按 `w.url().includes('index.html')` 选主窗口」、同一个 `#aiPanel` / `#aiInput` 就绪等待与面板打开路径）。

驱动行为（逐条实现，不得省略）：

0. **前置自检（在任何启动动作之前）**。`E-PW` = 全局 playwright / `_electron` 不可得（`NODE_PATH="$(npm root -g)"` 下 `require('playwright')._electron` 为假）；`E-DATA` = 遍历 `#aiConvList`（**先展开历史下拉**：`if (!dropOpen) click('#aiHistoryBtn')`，否则 `$$eval` 读到 0 项 —— 这是实际踩过的坑）后**找不到**任何含 `.tool-card.tool-card-manage` 且带 `.ai-skill-content-box-header` 的会话。两条各给独立码、非零退出并写入证据 JSON 的 `preflight`。

   **定位目标卡片后必须收敛到「唯一一张」并记录其标识**（卡片数量、`tool-card-status` 文本、折叠块 header 文本），后续所有几何量与遍历判定都锚在这一张上。

1. **前提断言（P 组）**：
   - `P1`：目标卡片处于折叠态（`!card.classList.contains('expanded')`）。
   - `P2`：`getComputedStyle(card.querySelector('.tool-card-content')).maxHeight === '0px'`，且该容器的外接矩形高为 0。
   - **P1 / P2 任一为假即硬退出**（`E-STATE`）—— 否则会拿一张已展开的卡片判绿。

2. **真实 Tab 遍历（R 组 · 承重）**。`document.activeElement` 先 `blur()` 再 `document.body.focus()`，随后 `page.keyboard.press('Tab')` 连续 N 次（N = 80），**每次按后立即** `page.evaluate` 读回当前 `document.activeElement` 的：`className` / `tagName` / `textContent.slice(0, 30)` / `getBoundingClientRect()` 的 `h` / `closest('.tool-card') !== null` / 是否 `.ai-skill-content-box-header`；全部站写入证据 JSON 的 `tabStops`。判定：
   - `R1`（非空真 · 遍历有效性）：`tabStops.length >= 20` 且至少一站不是 `BODY`。
   - `R2`（前提）：`P1 && P2` 在遍历**之前**已成立。
   - `R3`（**承重判据 · 方向无关**）：**凡** `inToolCard === true` 的停靠点，都必须满足「`rect.h > 0`」**且**「其中心点的 `document.elementFromPoint(cx, cy)` 落回它自身、其祖先或其后代」。**这一条不写成「卡片内不得有停靠点」** —— 否则日后若真做了「卡片头部成为可聚焦入口」的范式升级，改善反而会让门禁转红（见 frontmatter 的 key-decisions 第 5 条）。**红轮实测**：卡片域内恰 1 站（`.ai-skill-content-box-header`，索引 42，`rect.h = 29`），其中心点命中测试返回**另一条消息里的 `<CODE>`**（文本 `当需要撰写符合 conventional commits 规…`）⇒ `R3` 红。
   - `R4`（对照 · 防过度修复）：**气泡**实例（`document.querySelector('.ai-message-ai .ai-skill-content-box-header')`，即 `!el.closest('.tool-card')` 的那个）带 `role="button"` + `tabindex="0"` + `aria-expanded="false"`；对它真实 `focus()` 后按 `Enter`，断言其宿主的 `.ai-skill-content-box` 的 `collapsed` 类被切换**且** `aria-expanded` 同步为 `"true"`。**这一条在红轮就应为绿**（气泡本来就有增量）—— 它防的是「为了修卡片而把增量一刀切删掉」。
   - `R5`（语境一致性）：对目标**卡片**实例的折叠块 header 断言 `getAttribute('tabindex') === null && getAttribute('role') === null && getAttribute('aria-expanded') === null`；随后用真实点击展开卡片（`click()` 卡片头部，断言 `card.classList.contains('expanded')` 为真、`.tool-card-content` 的 `maxHeight !== '0px'`），**再次断言同样三个属性仍为 `null`**（C 的语义不随卡片状态切换）。红轮：第一次断言即红（`tabindex === '0'`）。
   - `R6`（**单点变异自检**）：`R3` 的非空真由红轮承担 —— 红轮等价于「把卡片调用点的 `{ interactive: false }` 去掉」这一变异态。驱动在证据 JSON 里写 `mutation: { name: 'card-call-without-interactive-false', expectR3Red: true }`，并在输出末尾打印 `R3` 的逐站明细（命中项 + 其中心点命中到的元素），使「红是因为这个」可被人工核读。

3. 全部断言通过 → 退出码 0；任一条失败 → 打印逐条布尔表 + 失败项的实测值，退出码非零。证据 JSON 写 `/tmp/uat49/evidence-g49-4.json`（**不得覆盖** `evidence.json` / `evidence3.json` / `evidence-g49-3.json`），红 / 绿两轮各自的完整输出落 `/tmp/uat49/g49-4-red.log` 与 `/tmp/uat49/g49-4-green.log`。

4. 收尾：`await Promise.race([electronApp.close(), sleep(6000)])` 后 `process.exit(code)`；`finally` 里若进程仍在，用 `electronApp.process().kill('SIGKILL')` 兜底。**严禁**任何 `pkill` / `pgrep` 模式匹配 —— 见 frontmatter 的安全纪律条。

**第 2 步 · 在当前树上跑红（改源码之前）**

运行驱动，把输出原样保存到 `/tmp/uat49/g49-4-red.log`。这一轮**必须**是：`R1` / `R2` / `R4` 绿（遍历有效、前提成立、气泡对照成立），`R3` 红（卡片内 1 个不可见停靠点），`R5` 红（`tabindex === '0'`）。**若 `R3` 已经是绿**，说明判据取错了对象（例如误判成「卡片内不得有停靠点」而当前卡片恰好没有，或遍历没跑到卡片域）—— **必须先修驱动，不得继续改源码**。这是防「断言恒真」的自校验，与 49-07 的红轮同一纪律。

**第 3 步 · 改源码（`src/renderer.js`）**

`renderSkillContentBox` 增加第二参数 `options`，只用一个开关：

```js
function renderSkillContentBox(skillInvocation, { interactive = true } = {}) {
```

- `interactive === true`（缺省，气泡语境）：**逐字保留**现有三条 `setAttribute`（`role` / `tabindex` / `aria-expanded`）与 `keydown`（Enter / Space + `preventDefault`）绑定，以及 `toggleCollapsed` 内的 `aria-expanded` 同步。
- `interactive === false`（卡片语境）：**不设**那三条属性、**不绑** `keydown`；`click` 绑定**保留**（卡片范式本就是鼠标语义，与契约 `:508` 一致）；`toggleCollapsed` 内的 `aria-expanded` 同步加同一开关守卫（无属性可同步时不要凭空造出它）。
- **落笔形态由 Task 2 的源码判据钉住**：三条 `setAttribute` 必须落在 `if (interactive) { … }` 守卫**之内**（判据正则锚在这个形态上），不要写成 `interactive && header.setAttribute(...)` 的散句，也不要靠 `deleteAttribute` 事后清（会让卡片实例在创建瞬间带上属性，语义上仍是「偶发存在」）。
- JSDoc 必须写明**四件事**：① `interactive` 的含义与缺省值；② 卡片语境**必须**传 `false` 及其**原因**（`.tool-card-content` 默认折叠用 `max-height: 0; overflow: hidden`，而 `overflow: hidden` 不会把后代移出顺序焦点导航 ⇒ 会得到「可 Tab 到达但零可见高度」的控件，焦点环被祖先裁掉）；③ **为什么不用 `display: none` 替代**（会杀掉 200ms 过渡，爆炸半径覆盖全部工具卡片）；④ 气泡语境**不受影响**的理由（气泡恒可见，控件可见即焦点语义成立）。**不得**在 JSDoc 里写「卡片不可键盘操作是缺陷」这类与契约 `:508` 冲突的措辞 —— 正确表述是「卡片的展开 / 折叠沿用全仓既有的鼠标语义（契约 `:508` 锁定），本计划不改变该范式」。

卡片调用点（`renderer.js:9739`）改为 `renderSkillContentBox({ content: params.content }, { interactive: false })`。**气泡调用点（`:9067`）逐字不动**（判据要求它保持 `renderSkillContentBox(msg.skillInvocation)` 的原样）。

**第 4 步 · 改 CSS 注释（`src/styles/main.css`）**

只更新 `.ai-skill-content-box-header:focus-visible` **上方**的注释块（`:5852-5856`），补一句焦点环的服务对象：**气泡实例**；并说明卡片实例因宿主折叠态不施加焦点语义（`UI-49-W6-01`），故该规则对卡片无效也不该有效。**零声明改动** —— `<verify>` 里的 `css-decl-freeze` 会比对包含该规则在内的声明集，改声明即转红。

**第 5 步 · 重跑驱动得绿**

再次运行驱动，输出保存到 `/tmp/uat49/g49-4-green.log`。`R1`–`R6` 全绿 + 退出码 0 才算完成。若 `R3` 仍红，逐条核对：卡片调用点是否真的传了 `{ interactive: false }`；`if (interactive)` 是否真的包住了三条 `setAttribute`；卡片内是否还有**别的**可聚焦元素（`<a href>` / `<button>` / 其它 `[tabindex]`）—— 有则说明本计划的范围估计不足，**停手上报**，不要就地扩大改动面。
  </action>
  <verify>
    <automated>NODE_PATH="$(npm root -g)" node -e "process.exit(require('playwright')._electron?0:1)"</automated>
    <automated>NODE_PATH="$(npm root -g)" node tests/uat-49-g49-4-card-a11y-tab-order.js</automated>
    <automated>node -e '
const fs=require("fs");
const css=fs.readFileSync("src/styles/main.css","utf8").replace(/\/\*[\s\S]*?\*\//g,"");
const props=(sel)=>{const i=css.indexOf(sel+" {");if(i<0)throw new Error("缺规则 "+sel);const b=css.slice(i+sel.length+2,css.indexOf("}",i));const m=b.match(/(^|;)\s*(outline|outline-offset|background|border|color|opacity|pointer-events|display|visibility|position|z-index)\s*:/g)||[];return m.map(x=>x.replace(/^;?\s*/,"").replace(/\s*:$/,"")).sort().join(",");};
const fv=props(".ai-skill-content-box-header:focus-visible");
if(fv!=="outline,outline-offset") throw new Error("焦点环规则的声明集被改动（只允许 outline + outline-offset）: "+fv);
if(!/\.ai-skill-content-box-header:focus-visible\s*\{\s*outline:\s*2px solid var\(--accent-color\)/.test(css)) throw new Error("焦点环取值被改动");
const hdr=props(".ai-skill-content-box-header");
if(hdr!=="color,display") throw new Error("折叠块 header 基类声明集被改动（白名单口径下实测冻结值为 color,display；新增 pointer-events / visibility / position / opacity 等任一条都会改变焦点可达性）: "+hdr);
console.log("css-decl-freeze ok · focus-visible=outline,outline-offset · header=color,display");
'</automated>
    <automated>node -e '
const fs = require("fs");
const Q = String.fromCharCode(39), D = String.fromCharCode(34);
const strip = (t) => t.replace(/\/\*[\s\S]*?\*\//g, "").replace(/(^|[^:])\/\/[^\n]*/g, "$1");
const src = fs.readFileSync("src/renderer.js", "utf8");
// 把单/双引号统一成 Q，使判据正则无需在 shell 里出现引号字符
const code = strip(src).split(Q).join("Q").split(D).join("Q");
const i = code.indexOf("function renderSkillContentBox");
if (i < 0) throw new Error("缺 renderSkillContentBox 定义");
const rest = code.slice(i + 10);
const j = rest.indexOf("\nfunction ");
const body = rest.slice(0, j < 0 ? rest.length : j);
const guard = (a) => new RegExp("if\\s*\\(interactive\\)\\s*\\{[\\s\\S]{0,200}?setAttribute\\(\\s*Q" + a + "Q");
const checks = [
  ["签名 interactive = true", /function\s+renderSkillContentBox\s*\(\s*skillInvocation\s*,\s*\{[^}]*interactive\s*=\s*true[^}]*\}\s*=\s*\{\}\s*\)/],
  ["role 受守卫", guard("role")],
  ["tabindex 受守卫", guard("tabindex")],
  ["aria-expanded 受守卫", guard("aria-expanded")],
  ["keydown 受守卫", new RegExp("if\\s*\\(interactive\\)\\s*\\{[\\s\\S]{0,400}?addEventListener\\(\\s*QkeydownQ")],
  ["卡片调用点传 false", /renderSkillContentBox\(\s*\{\s*content:\s*params\.content\s*\}\s*,\s*\{\s*interactive:\s*false\s*\}\s*\)/],
  ["气泡调用点原样", /renderSkillContentBox\(\s*msg\.skillInvocation\s*\)/],
];
let red = 0;
for (const c of checks) {
  const ok = c[1].test(body) || c[1].test(code);
  if (!ok) red++;
  console.log((ok ? "GREEN" : "RED  ") + "  " + c[0]);
}
const calls = (code.match(/renderSkillContentBox\s*\(/g) || []).length - 1;
if (calls !== 2) red++;
console.log((calls === 2 ? "GREEN" : "RED  ") + "  调用点计数 = " + calls + "（应为 2）");
let cleared = 0;
for (const p of ["deleteAttribute", "removeAttribute"]) {
  if (new RegExp(p + "\\(\\s*Q(role|tabindex|aria-expanded)Q").test(code)) cleared++;
}
if (cleared) red++;
console.log((cleared ? "RED  " : "GREEN") + "  无事后清除形态");
if (red) {
  console.error("a11y-guard-form FAILED · 红 " + red + " 条");
  process.exit(1);
}
console.log("a11y-guard-form ok · calls=2 · guarded=role,tabindex,aria-expanded,keydown");
'</automated>
    <automated>node tests/test-ai-skills.js</automated>
  </verify>
  <done>
驱动脚本存在且可重跑；`/tmp/uat49/g49-4-red.log` 记录**未修复树**上的形态 —— `R1` / `R2` / `R4` 为绿、`R3` 为红（卡片域内 1 个停靠点、其中心点命中到另一条消息的元素）、`R5` 为红（卡片实例 `tabindex === '0'`）；`/tmp/uat49/g49-4-green.log` 记录修复后 `R1`–`R6` 全绿且退出码 0；`/tmp/uat49/evidence-g49-4.json` 含 `preflight` / `tabStops` 全量明细 / `mutation` / 红绿两轮的 `R3` 明细，且**未覆盖** `evidence.json` / `evidence3.json` / `evidence-g49-3.json`；`renderSkillContentBox` 的第二参数 `interactive`（缺省 true）存在，三条 `setAttribute` 落在 `if (interactive)` 守卫内、`keydown` 绑定同样受守卫，卡片调用点传 `{ interactive: false }`、气泡调用点逐字未动；`src/styles/main.css` 的焦点环与折叠块 header 基类**声明零改动**（由 `css-decl-freeze` 门禁承担）；驱动收尾 `electronApp.close()` + `process.exit()`，全程未对任何进程做 `pkill` 类操作，用户正式版实例未被触碰。
  </done>
</task>

<task type="auto">
  <name>Task 2: 源码面护栏 —— 条件施加的形态、两个调用点的取值、单源未破</name>
  <files>tests/test-ai-skills.js</files>
  <action>
在既有 **M9**（「源码 · 折叠块复用」）旁新增 **M9b**（或就地扩写 M9，二者择一但须与既有用例计数纪律一致），断言对象一律是**剥注释后的代码**（`stripComments` —— 本阶段已记录：关于代码的断言不得被同一文件里的散文满足，且**行尾注释同样要剥**：`tests/test-ai-skills.js` 既有 `stripComments` 只剥整行注释，本任务顺手把行尾注释也剥掉，否则「`noteText = X; // 注释里写 role`」这类形态可让判据假绿）。

断言（逐条，全部在剥注释后的文本上）：

1. `renderSkillContentBox` 仍**恰 1 处定义**（既有断言，保留）。
2. **签名形态**：`/function\s+renderSkillContentBox\s*\(\s*skillInvocation\s*,\s*\{[^}]*interactive\s*=\s*true[^}]*\}\s*=\s*\{\}\s*\)/` 命中 —— 即选项参数带**缺省 `true`** 的解构形态（缺省值不可省：它决定气泡调用点不传参时的行为）。
3. **属性受守卫**：`/if\s*\(interactive\)\s*\{[\s\S]{0,200}?setAttribute\(\s*['"]role['"]/`、同款对 `['"]tabindex['"]` 与 `['"]aria-expanded['"]` 各一条 —— 三条都必须落在同一个 `if (interactive) {` 之后 200 字符窗口内（窗口是**为了绑定距离**，不是为了放宽）。
4. **`keydown` 受守卫**：`/if\s*\(interactive\)\s*\{[\s\S]{0,400}?addEventListener\(\s*['"]keydown['"]/` 命中。
5. **卡片调用点传 false**：`/renderSkillContentBox\(\s*\{\s*content:\s*params\.content\s*\}\s*,\s*\{\s*interactive:\s*false\s*\}\s*\)/` 命中。
6. **气泡调用点逐字未动**：`/renderSkillContentBox\(\s*msg\.skillInvocation\s*\)/` 命中。
7. **调用点总数恰 2**：全文件 `renderSkillContentBox(` 出现次数（剥注释后）减去 1 处定义，恰为 2 —— 防新增第三个使用面而漏传选项。
8. **反向断言**：文件中**不存在** `deleteAttribute('role')` / `removeAttribute('tabindex')` / `removeAttribute('aria-expanded')` 这类「事后清除」形态（key-decisions 已裁定必须用守卫内施加，不得先加后删）。

**必须同步刷新例数账本**（本阶段纪律，不得硬编码）：跑三套件取**实测** `# tests`，把 `AGENTS.md` 测试行三条 + `docs/product/ai-skills.md` §七两条 + §11.8 三条共**八个单元**刷成实测值，并用 §11.8 携带的可重跑一致性命令验收。

**不得**为了让断言通过而改动被测源码 —— 若某条断言与 Task 1 的落笔形态冲突，**改判据或停在 Task 1 修形态**，不得把判据写成恒真的弱形态（如裸 `includes('interactive')`）。
  </action>
  <verify>
    <automated>node -e '
const fs=require("fs");
const Q=String.fromCharCode(39), D=String.fromCharCode(34);
const src=fs.readFileSync("src/renderer.js","utf8");
const strip=(t)=>t.replace(/\/\*[\s\S]*?\*\//g,"").replace(/(^|[^:])\/\/[^\n]*/g,"$1");
// 引号统一成 Q ⇒ 判据正则无需在 shell 里出现引号字符（zsh 会吃掉 \\x27 / \\x22）
const code=strip(src).split(Q).join("Q").split(D).join("Q");
const i=code.indexOf("function renderSkillContentBox");
if(i<0)throw new Error("缺 renderSkillContentBox 定义");
const rest=code.slice(i+10);
const j=rest.indexOf("\nfunction ");
const body=rest.slice(0, j<0?rest.length:j);
const guard=(a)=>new RegExp("if\\s*\\(interactive\\)\\s*\\{[\\s\\S]{0,200}?setAttribute\\(\\s*Q"+a+"Q");
const checks=[
  ["签名 interactive = true（缺省解构形态）", /function\s+renderSkillContentBox\s*\(\s*skillInvocation\s*,\s*\{[^}]*interactive\s*=\s*true[^}]*\}\s*=\s*\{\}\s*\)/],
  ["role 落在守卫内", guard("role")],
  ["tabindex 落在守卫内", guard("tabindex")],
  ["aria-expanded 落在守卫内", guard("aria-expanded")],
  ["keydown 受守卫", new RegExp("if\\s*\\(interactive\\)\\s*\\{[\\s\\S]{0,400}?addEventListener\\(\\s*QkeydownQ")],
  ["卡片调用点传 false", /renderSkillContentBox\(\s*\{\s*content:\s*params\.content\s*\}\s*,\s*\{\s*interactive:\s*false\s*\}\s*\)/],
  ["气泡调用点原样", /renderSkillContentBox\(\s*msg\.skillInvocation\s*\)/],
];
let red=0;
for(const c of checks){const ok=c[1].test(body)||c[1].test(code); if(!ok)red++; console.log((ok?"GREEN":"RED  ")+"  "+c[0]);}
const calls=(code.match(/renderSkillContentBox\s*\(/g)||[]).length-1;
if(calls!==2)red++;
console.log((calls===2?"GREEN":"RED  ")+"  调用点计数 = "+calls+"（应为 2）");
let cleared=0;
for(const p of ["deleteAttribute","removeAttribute"]){if(new RegExp(p+"\\(\\s*Q(role|tabindex|aria-expanded)Q").test(code))cleared++;}
if(cleared)red++;
console.log((cleared?"RED  ":"GREEN")+"  无事后清除形态");
if(red){console.error("a11y-guard-form FAILED · 红 "+red+" 条");process.exit(1);}
console.log("a11y-guard-form ok · calls=2 · guarded=role,tabindex,aria-expanded,keydown");
'</automated>
    <automated>node tests/test-ai-skills.js > /tmp/g49-4-ai-skills.log 2>&1; RC=$?; grep -E "^# (tests|pass|fail)" /tmp/g49-4-ai-skills.log; grep -q "^# fail 0" /tmp/g49-4-ai-skills.log && [ "$RC" -eq 0 ] && echo "ai-skills ok"</automated>
    <automated>node tests/test-manage-skill.js >/dev/null 2>&1 && node --test tests/test-skill-picker-model.js >/dev/null 2>&1 && echo "two-suites ok"</automated>
  </verify>
  <done>
`tests/test-ai-skills.js` 新增的源码面断言全部落在**剥注释后**的文本上（含行尾注释），逐条钉住：签名的 `interactive = true` 缺省解构形态、三条 `setAttribute` 与 `keydown` 绑定落在 `if (interactive)` 守卫窗口内、卡片调用点传 `{ interactive: false }`、气泡调用点保持 `renderSkillContentBox(msg.skillInvocation)` 原样、调用点总数恰 2、无 `deleteAttribute` / `removeAttribute` 的事后清除形态；三套件实测 `# fail 0`；八个例数账本单元已按**实测**值刷新且 §11.8 的一致性命令通过；**未**为让判据通过而弱化判据或改被测源码。
  </done>
</task>

<task type="auto">
  <name>Task 3: 契约与文档口径收口 —— a11y 增量的适用范围补限 + UI-REVIEW 的 gap 闭合标注</name>
  <files>.planning/phases/49-manage-skill-ai/49-UI-SPEC.md, docs/product/ai-skills.md, AGENTS.md, .planning/phases/49-manage-skill-ai/49-UI-REVIEW.md</files>
  <action>
1. **`49-UI-SPEC.md:427`**（折叠块「键盘可达性（新增实例一并补齐）」段）：保留原意，补一句限定 —— 该增量**施加于气泡实例**；卡片实例因宿主 `.tool-card-content` 默认折叠用 `max-height: 0`（而非 `display: none`），后代**不会**自动退出顺序焦点导航，施加焦点语义会产出「可 Tab 到达但零可见高度」的控件（焦点环被祖先裁掉），故卡片调用点传 `{ interactive: false }`。同时写明**为什么不用 `display: none` 替代**（会杀掉 200ms 过渡，爆炸半径覆盖全部工具卡片）。原句里的「该补齐作用于唯一构建实现 ⇒ 气泡实例同时获得，属纯增量」**改为**「该补齐作用于唯一构建实现并由**语境开关**控制 —— 气泡实例施加、卡片实例不施加」。

2. **`49-UI-SPEC.md:509`**（交互契约「展开 / 折叠（正文块）」行）：在「**新增** `role="button"` + `tabindex="0"` + `aria-expanded` + Enter/Space」后补「（**气泡实例**；卡片实例沿用与卡片头部同为鼠标语义的既有范式 —— 见 `## 卡片结构契约` 的键盘可达性段）」。

3. **`49-UI-SPEC.md:508` 逐字不动** —— 「展开 / 折叠（卡片）| 既有行为：点击 `.tool-card-header` 切换 `.tool-card.expanded`；**本阶段零改动**（不新增键盘语义 —— 全仓所有工具卡片同一范式）」这条锁定决策本计划**不推翻**，C 的选择正是为了不改它。**不得**顺手把卡片头部也加上键盘语义。

4. **`docs/product/ai-skills.md` §11.7**（可见性与时序节）：在折叠块 / 卡片相关段落补一句口径 —— 技能正文折叠块的键盘语义（`role` / `tabindex` / `aria-expanded` / Enter / Space）**只在气泡实例施加**；卡片实例不施加，理由是卡片的正文折叠块位于默认折叠的容器内（`max-height: 0`，`overflow: hidden` 不移出 Tab 序），施加即产出不可见焦点。卡片的展开 / 折叠保持全仓工具卡片的鼠标语义（`49-UI-SPEC.md:508`）。这句是**卡片形态**的说明，故落在 §11.7 而非 §11.6。

5. **`AGENTS.md`**：若 Task 2 的例数已在该文件刷新，此处只复核口径一致（八个单元与 §11.8 同源）。**不得**在此新增与 `49-UI-SPEC.md` 冲突的表述。

6. **`49-UI-REVIEW.md`**：在 `## Detailed Findings` 的 `W6-01` 段落末尾追加一行闭合标注：`> **Closed by:** 49-08-PLAN.md（2026-09-XX）—— 修法 C（卡片语境不施加焦点语义）+ tests/uat-49-g49-4-card-a11y-tab-order.js 的真实键盘门禁（红→绿两轮证据）`；**并在 `## Pillar Scores` 的表格下方追加一行** `> **修订（2026-09-XX）**：W6-01 已由 49-08 闭合 —— Experience Design 的 2/4 是**审计当时**的评分，不回溯改写。`。**不得**改动其余任何评分、发现或 `## 实测证据索引` 的内容（审计报告是历史记录，只加不删 —— 与本阶段「卡片是不可变历史记录」同款口径）。

7. **`I6-01`（嵌套滚动，INFO）与 `I2-01`（装饰 SVG 无 `aria-hidden`，INFO）不在本计划范围** —— 它们是 INFO 级、且 `I2-01` 属全仓既存约定（非本阶段引入）。**不得**顺手扩大改动面；如实留在 `49-UI-REVIEW.md` 的未闭合清单里。

8. **用词要求（`<verify>` 第一条判据锚在这些词上，必须逐字出现）。** 撰写本计划时实测这些词在目标文件中**零命中**，故它们是非恒真判据；**不要**改用同义词，否则门禁会因「锚点不在场」转红而你要去改判据 —— 那是判据被弱化的开始。必须逐字出现：`49-UI-SPEC.md` 里写进 **`卡片实例`**、**`零可见高度`**、**`顺序焦点导航`**、**`语境开关`**；`docs/product/ai-skills.md` §11.7 里写进 **`卡片实例`**、**`焦点语义`**。反过来，**不得**使用「气泡实例 / `max-height: 0` / `display: none`」这三者作为**判据锚点** —— 它们在两处目标文件里**当前就已存在**（`:428` 的「气泡实例同时获得」、`:77` 的 `max-height:0`、`:417` 的 `display:none`），拿它们当锚点就是恒真断言（本计划的第一版判据正是这么写的，实跑时在未改动树上即判绿，属自毁形态，已修正）。
  </action>
  <verify>
    <automated>node -e '
const fs=require("fs");
const R=(p)=>fs.readFileSync(p,"utf8");
const bad=[];
const spec=R(".planning/phases/49-manage-skill-ai/49-UI-SPEC.md");
// ① :508 的卡片范式锁定句逐字未破
if(!/既有行为：点击 `\.tool-card-header` 切换 `\.tool-card\.expanded`；\*\*本阶段零改动\*\*/.test(spec))bad.push("UI-SPEC :508 的卡片范式锁定句被改动");
// ② 新词锚点（撰写本计划时实测**零命中** ⇒ 本判据非恒真）：Task 3 必须写进这四个词
for(const a of ["卡片实例","零可见高度","顺序焦点导航","语境开关"]){if(!spec.includes(a))bad.push("UI-SPEC 缺新词锚点 "+a);}
// ③ 产品文档同一口径（同样零命中的两个新词）
const doc=R("docs/product/ai-skills.md");
for(const a of ["卡片实例","焦点语义"]){if(!doc.includes(a))bad.push("产品文档 §11.7 缺新词锚点 "+a);}
// ④ 审计报告只加不删：原评分保留 + 追加闭合标注与「不回溯改写」声明
const rev=R(".planning/phases/49-manage-skill-ai/49-UI-REVIEW.md");
if(!/Closed by:\s*49-08/.test(rev))bad.push("UI-REVIEW 未标注 W6-01 的闭合来源");
if(!/Experience Design[\s\S]{0,400}?2\/4/.test(rev))bad.push("UI-REVIEW 的审计当时评分被改（应保留 2/4 并只追加修订行）");
if(!/审计当时|不回溯改写/.test(rev))bad.push("UI-REVIEW 未声明评分不回溯改写");
if(bad.length){console.error("copy-parity 不合格:\n"+bad.join("\n"));process.exit(1);}
console.log("copy-parity ok · :508 未破 · 四个新词锚点在场 · UI-REVIEW 只加不删");
'</automated>
    <automated>node -e '
const cp=require("child_process"),fs=require("fs");
const suites=["tests/test-manage-skill.js","tests/test-ai-skills.js","tests/test-skill-picker-model.js"];
const meas={};
for(const f of suites){const o=cp.execSync("node "+(f.indexOf("picker")>=0?"--test ":"")+f,{encoding:"utf8",stdio:["ignore","pipe","ignore"]});meas[f.split("/").pop()]=String(o.match(/# tests (\d+)/)[1]);}
const FN=/test-[\w-]+\.js/g;let bad=[],cells=0;
for(const file of ["AGENTS.md","docs/product/ai-skills.md"]){const hit={};fs.readFileSync(file,"utf8").split("\n").forEach((line,ln)=>{const idx=[];FN.lastIndex=0;let m;while((m=FN.exec(line)))idx.push([m.index,m[0]]);for(let i=0;i<idx.length;i++){const base=idx[i][1];if(!(base in meas))continue;const end=i+1<idx.length?idx[i+1][0]:line.length;const uniq=[...new Set([...line.slice(idx[i][0],end).matchAll(/(\d+)\s*例/g)].map(x=>x[1]))];if(!uniq.length)continue;cells++;hit[base]=(hit[base]||0)+1;if(uniq.length!==1||uniq[0]!==meas[base])bad.push(file+":"+(ln+1)+" "+base+" 账本取到 ["+uniq.join("/")+"] ≠ 实测 "+meas[base]);}});for(const base of Object.keys(meas))if(!hit[base])bad.push(file+" 未覆盖 "+base);}
if(cells<8)bad.push("账本单元数 "+cells+" < 8");
if(bad.length)throw new Error("例数不一致: "+bad.join("; "));
console.log("counts-parity ok · cells="+cells+" · measured="+JSON.stringify(meas));
'</automated>
    <automated>FAIL=0; for f in tests/test-*.js; do node "$f" >/dev/null 2>&1 || { echo "FAILED: $f"; FAIL=1; }; done; if [ "$FAIL" -eq 0 ]; then echo "all-suites ok"; else exit 1; fi</automated>
  </verify>
  <done>
`49-UI-SPEC.md` 的 `:427` 与 `:509` 两处已补上「气泡实例施加 / 卡片实例不施加」的限定与理由（含为什么不用 `display: none`），**`:508` 的卡片范式锁定句逐字未破**；`docs/product/ai-skills.md` §11.7 已同步同款口径；`AGENTS.md` 的例数账本与 §11.8 同源；`49-UI-REVIEW.md` 的 `W6-01` 已标注 `Closed by: 49-08-PLAN.md` 并在评分表下追加「不回溯改写」的修订行，其余评分 / 发现 / 实测证据索引**逐字未动**；口径收口判据、八个账本单元的一致性命令、以及 `tests/test-*.js` 全量回归（全部退出码 0）三条门禁全绿；`I2-01` / `I6-01` 两条 INFO 如实留在未闭合清单，未被顺手扩大改动面。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 共享构建函数 → 两种宿主语境 | 同一份 `renderSkillContentBox` 在气泡（恒可见）与卡片（默认折叠）下的行为必须**语境相关**；这条边界正是本 gap 的成因 —— 上一轮把「语境」当成了不言自明 |
| 折叠机制 → 顺序焦点导航 | `max-height: 0; overflow: hidden` 与 `display: none` 在**焦点语义**上不等价；这条边界是浏览器语义，不是本项目可调的行为 |
| 断言形态 → 真实行为 | 源码扫描只能证明「代码写成什么样」，不能证明「键盘实际停在哪」；把前者当后者是本阶段反复记录的假绿来源 |
| 驱动脚本 → 用户运行中的生产实例 | 驱动必须只操作自己 `_electron.launch` 拉起的 dev 子进程；用户当时正在运行正式版 `/Applications/Realm.app`（PID 22083） |
| 审计报告 → 后续读者 | `49-UI-REVIEW.md` 是审计当时的**历史记录**；用事后修订覆盖原评分会让「当时看到什么」不可复原 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-08-01 | Tampering | 修复过度：把 a11y 增量从**气泡实例**一并删掉（回退 48-UI-REVIEW Pillar 6 的修复） | high | mitigate | Task 1 的 `R4` 在**红轮就应为绿**：断言气泡实例仍带三属性，且真实 Enter 能切换 `.collapsed` 并同步 `aria-expanded`；Task 2 的断言 6 额外钉住气泡调用点**逐字未动**（`renderSkillContentBox(msg.skillInvocation)`）。两条互相独立：一条判行为、一条判形态 |
| T-49-08-02 | Repudiation | 用源码扫描 / 声明扫描判绿，而真实键盘仍停在不可见控件上（本阶段已记录的假绿形态） | high | mitigate | 承重判据是真实 `_electron` 下的**真实 Tab 遍历** + 逐站命中测试（`R3`），源码判据（Task 2）只作辅助且**不得**单独作为闭合依据；`R6` 把红轮定义为单点变异态并在证据 JSON 里显式记录 `mutation`，保证非空真 |
| T-49-08-03 | Denial of Service | 判据在绿轮**空真**（卡片内零停靠点 ⇒ 「凡卡片内停靠点都可见」为空集真） | high | mitigate | `R1` 断言遍历有效（停靠点总数 ≥ 20 且至少一站非 `body`）+ `R6` 由红轮承担单点变异的非空真 + `P1`/`P2` 断言前提（卡片确实折叠、容器确实 0 高）。三者缺一即判据不可信 |
| T-49-08-04 | Tampering | 判据写成「卡片内不得有键盘停靠点」，导致日后 a11y 改善（卡片头部成为可聚焦入口）反而让门禁转红 | medium | mitigate | `R3` 的措辞固定为**方向无关**的条件式（「凡在卡片内的停靠点都必须可见」），并在 frontmatter 的 key-decisions 第 5 条写明这条取舍的理由；Task 2 的源码判据同样只判「属性是否受守卫」而不判「卡片内有无焦点语义」 |
| T-49-08-05 | Tampering | 为让门禁通过而改被测源码或弱化判据（如把断言退化成裸 `includes('interactive')`） | medium | mitigate | Task 2 的 `<action>` 明文禁止，并把「改判据 / 停在 Task 1 修形态」写成唯一两条出路；`R3` 的判据体（外接矩形高 + 命中测试）本身无法用源码扫描替代 |
| T-49-08-06 | Tampering | 写侧误把 `.tool-card-content` 的折叠机制改成 `display: none`（杀掉 200ms 过渡、波及其他全部工具卡片） | medium | mitigate | frontmatter 的 key-decisions 第 6 条明文禁止，并在 Task 1 第 3 步的 JSDoc 要求里把「为什么不用 display: none」写成必写的一条；Task 3 要求该理由同时落进 `49-UI-SPEC.md` |
| T-49-08-07 | Tampering | 误改 `49-UI-SPEC.md:508` 的卡片范式锁定决策（把卡片头部也加上键盘语义） | high | mitigate | Task 3 明文「`:508` 逐字不动」；`<verify>` 第一条判据用正则逐字断言该句仍在；C 的选法本身就是为不改它 |
| T-49-08-08 | Tampering | 审计报告被事后覆盖，原始评分不可复原 | low | mitigate | Task 3 第 6 条要求**只加不删**（追加 `Closed by` 与「不回溯改写」修订行），并由 `<verify>` 断言 `2/4` 与「不回溯改写」同时仍在 |
| T-49-08-09 | Repudiation | 例数账本漂移（新增用例后八个单元填估算值或只刷一半） | medium | mitigate | Task 2 要求按**实测**回填八单元，并由 §11.8 的可重跑一致性命令（Task 3 的 `<verify>` 第二条）机械判定，任一单元不等于实测即以非零退出并指名到「文件:行号 + 套件名 + 取到的值」 |
| T-49-08-10 | Tampering | 进程管理：驱动收尾 / 中断 | high | mitigate | 只用 `electronApp.close()` + `process.exit(code)`（含 `Promise.race` 超时 + `SIGKILL` 兜底）；**严禁** `pkill` / `pgrep` 模式匹配；若中断，只按自己记录的 PID 经 `ps -p <pid> -o command=` 核对确属 `Projects/Realm/node_modules/electron` 后再 kill（本计划撰写时实测踩过：`kill -TERM` 对 Electron 主进程无效，须 `-9`） |
| T-49-08-11 | Tampering | realm-dev 的可复现前提被清掉（目标会话 / `manage_skill` 卡片被删） | medium | mitigate | 驱动只做读取与真实点击（展开 / 折叠），**不**清理任何技能目录、**不**删除任何会话；`E-DATA` 判负即硬退出并提示前提缺失，不得改用合成 DOM |
| T-49-08-12 | Tampering | 文档口径与实现再次分叉（只改一处的适用范围，另一处仍写「无条件增量」） | medium | mitigate | Task 3 要求 `49-UI-SPEC.md:427`、`:509`、`docs/product/ai-skills.md` §11.7 三处**同一任务内**同款补限，并由 `<verify>` 第一条判据逐条断言（含「气泡实例」与 `max-height: 0` 成因句必须在场） |
| T-49-08-SC | Tampering | npm / pip / cargo 安装 | n/a | accept | 本计划不新增任何依赖（playwright 走全局已装版本），无包管理器安装面，无需 legitimacy gate |
</threat_model>
