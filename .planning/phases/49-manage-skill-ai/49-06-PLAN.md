---
phase: 49-manage-skill-ai
plan: 06
type: execute
wave: 3
depends_on: [49-04, 49-05]
files_modified:
  - docs/product/ai-skills.md
  - AGENTS.md
autonomous: true
gap_closure: true
requirements: [MGMT-01, MGMT-03]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

key-decisions:
  - '本计划**不改任何源码与测试**：只做文档纠偏与账本同步。它承担用户在本 run 明确列入范围的第 4 项（Doc correction），并收口 49-04 / 49-05 落地后变成**不成立**的三处既有声明'
  - '三处「落地后变成假」的声明必须逐条纠正，缺一即为文档与现实背离：① `docs/product/ai-skills.md` §11.3 的 `oversize` 行（WR-01 证伪后 49-04 已改，本计划只做核对与交叉指引）；② §11.7 明文的「重开对话后的失败历史卡片**不显示**短原因」—— 49-05 让原因码落库后这句即为假；③ §11.8 的挂账句**本身一个字都不移出**（它列的是 **48 号**编号），要补的是**另起一句、命名空间显式的闭合声明**'
  - '**编号命名空间是本计划最容易踩的坑（BLOCKER 级纪律）**：§11.8 挂账句里的裸 `TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06` **全部是 `48-REVIEW.md` 的编号**（`.planning/STATE.md` 明文「⚠️ [Phase 48 · 继续挂账，**不得**读成已修] WR-02 / WR-06」；48 的 `WR-01` = 三档徽标文案两处重复、`WR-02` = 技能解析链三处裸 await、`WR-06` = **已缓存**技能的实时读盘命中路径仍绕过 64 KiB 字节闸），与本轮 `49-REVIEW.md` 的 `WR-01`（写侧权威字节闸）/ `WR-02`（失败态原因码不落库）**同名不同物**。因此：**这五个一个都不许从那句里移出（含 `WR-06`）**，本轮闭合声明的五个编号必须**另起一句**并显式写出 `49-REVIEW.md` 作为命名空间 —— 「把 WR-01 / WR-02 从未闭合清单移进闭合句」这种改法会把 **Phase 48 仍开的技术债误标为已闭合**（且 49-03 已用 `T-49-03-01` 把该风险列为 high），是本计划最严重的可能失败'
  - '**必须如实保留的挂账（两个命名空间都要守）**：① **48 号台账** —— §11.8 挂账句里的 `TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06` 五项原样保留、一个都不许写成已闭合（含 `WR-06`），外加 `STATE.md` 的 `syncAgentSystemPrompt()` 生产调用方 ⚠️（本阶段只完成 3 分之 1）；② **49 号台账** —— 本轮只闭合 `49-REVIEW.md` 的 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02` 五项，其 `WR-03` / `WR-04` / `IN-01`–`IN-06` 不在本 run 范围内，同样照旧挂账'
  - '**命名空间的可机械判据**：挂账句必须携带 `48-REVIEW` 标记（并写明两处同名编号「不是同一物 / 不得混读」），闭合句必须携带 `49-REVIEW` 标记且**不含** `TD-48-01`。一致性命令按**行**取句（挂账句 = 含「未闭合项如实挂账」的那一行；闭合句 = 同时含 `49-REVIEW.md` 与 `CR-01` 的那一行），因此两句必须是**两个独立行**，不得合并成一句 —— 合并后「同名不同物」的判定就无法机械表达'
  - '**新引入的可见面变化必须成文**（49-05 的失败消息 `[code]` 词缀）：它改变了用户与模型看到的失败文案形态，属「改行为先改本节」的强制同步面。成文时必须写清三点 —— 词缀在消息**起始**、内容是**白名单原因码**（不含被拒内容原文）、被权威文案原样跟在词缀之后'
  - '测试例数一律取**实测值**（跑套件读 `# tests`），不按估算或计划里的任务数填。三处账本（`docs/product/ai-skills.md` §七 与 §11.8、`AGENTS.md` 的测试行）必须在**同一个任务**里一起刷，避免两处漂移 —— 并用一条可重跑的**例数一致性命令**把「文档数字 == 实测数字」变成机械判据'
  - '`AGENTS.md` 的维护约定扩充只加**不变式**（写侧权威闸口按组装全文 / 净化后必须复验非空 / 失败态原因码经消息词缀持久化 / 卡片标记的并入单源），不复制 §十一 的正文 —— 维护约定的职责是「改什么必须同步哪里」，不是第二份产品说明'
  - '`AGENTS.md` 的那一行测试清单当前被多个技能域条目共用（同一段里还有 bash 策略 / 播种器 / 取消状态等套件），编辑必须**只替换本 run 涉及的三个套件计数与其覆盖面描述**，不得重排或重写整行'
  - '所有验证命令必须写成 `node -e "…"` / `node tests/<file>.js` 的显式形式，不得引用 `npm test` —— 本项目 package.json 无该脚本'

must_haves:
  truths:
    - '`docs/product/ai-skills.md` §11.7 不再声称失败历史卡片丢失短原因，而是说明失败态的短原因**可由持久化的原因码还原**（原因是它随消息落库）'
    - '`docs/product/ai-skills.md` §11.8 **按命名空间分别记账**：挂账句里那五个 **48 号**编号（`TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06`，**含 `WR-06`**）原样留在未闭合语境，并带 `48-REVIEW` 标记与「同名不同物 / 不得混读」的显式提示；本轮的闭合声明**另起一句**、显式挂在 **`49-REVIEW.md`** 名下（`CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`），且该句**不含** `TD-48-01`；`syncAgentSystemPrompt()` 的 ⚠️（本阶段只完成 3 分之 1）仍如实挂账'
    - '失败消息的原因码词缀形态成文（含一个具体示例 `[seeded_protected]`），并写明词缀在消息起始、只含白名单原因码、权威中文文案原样跟在词缀之后'
    - '三处测试例数账本（`docs/product/ai-skills.md` §七、`docs/product/ai-skills.md` §11.8、`AGENTS.md` 的测试行）与三个套件的**实测** `# tests` 逐字一致，且由一条可重跑的一致性命令机械判据'
    - '`AGENTS.md` 的 `manage_skill` 维护约定含四条新增不变式：写侧权威字节闸口按**组装后的 SKILL.md 全文**、净化后必须对净化值**复验非空**、失败态原因码经**消息词缀**持久化并由重载链路还原、卡片标记的并入逻辑**单源**在 `src/skill-picker-model.js`（渲染端与测试共用同一实现）'
    - '[assumption] 文档中出现的示例命令必须可直接复制执行（`node tests/test-manage-skill.js` / `node tests/test-ai-skills.js` / `node --test tests/test-skill-picker-model.js`），不得出现 `npm test` 或需要先手工构造环境才能跑的命令；可跑性由这三个套件在本 run 的前置计划里已实测通过承担'
    - '[assumption] 文档措辞不逐字规定（除词缀示例 `[seeded_protected]` 与四条不变式的关键名词外），但**判据面**不得弱化：每一条纠正都必须能指出它对应哪个 Review / Verification 编号或哪条不变式'
  artifacts:
    - 'docs/product/ai-skills.md：§11.3 邻接处新增失败消息词缀的成文（形态 + 示例 + 三条边界）'
    - 'docs/product/ai-skills.md：§11.7 的失败态可见性口径改写（短原因可还原）'
    - 'docs/product/ai-skills.md：§11.8 挂账句补 `48-REVIEW` 命名空间标记 + **另起一行新增**挂在 `49-REVIEW.md` 名下的闭合声明（`CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`；并注明 49 号其余条目仍挂账）+ 三条套件例数刷新'
    - 'docs/product/ai-skills.md：§七 的两条套件例数刷新'
    - 'AGENTS.md：测试行的三个套件计数与覆盖面描述刷新'
    - 'AGENTS.md：`manage_skill` 维护约定的四条新增不变式'
  key_links:
    - '纠正 → 依据链：§11.3 `oversize` 行 ← `49-REVIEW.md` WR-01 的实测（content 65200 B + description 1024 字符 → 落盘 66256 B > 65536 B）；§11.7 短原因 ← 49-05 的失败态原因码落库与还原；§11.8 的分账 ← 两个命名空间的权威定义（`48-REVIEW.md` 的 `WR-01` / `WR-02` / `WR-06` 与 `STATE.md` 的「继续挂账，不得读成已修」）对本 run 实际闭合的五项（`49-REVIEW.md` 的 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`）'
    - '例数账本链：`node tests/test-manage-skill.js` / `node tests/test-ai-skills.js` / `node --test tests/test-skill-picker-model.js` 的 `# tests` → 三处账本 → 可重跑的一致性命令。三者必须同源，任一处漏刷都会让一致性命令转红'
    - '维护约定链：`AGENTS.md` 的 `manage_skill` 条目 → 指向 `docs/product/ai-skills.md` 第十一节为产品说明权威 → 四条不变式分别对应本 run 的四条实现约束（组装全文闸口 / 净化后复验 / 失败态词缀 / 并入单源）'
  prohibitions:
    - statement: '不得把未闭合项写成已闭合；也不得把**别轮**的编号按同名读成本轮的编号（两个方向的错误都属同一类失败：账本污染）'
      status: unresolved
      verification: judgment
      reason: '本 phase 的 gap 里已有一条「文档过度声称」的先例（§11.3 声称两侧判据同值，被 WR-01 实测证伪）。本 run 的方向相反：把**本轮已闭合**的五项以 `49-REVIEW.md` 为命名空间写进闭合句，同时把 §11.8 挂账句里的五个 **48 号**编号（`TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06`）**一字不动地留在挂账句里**（`WR-06` 尤其不许漏 —— 它是 48 号仍开的实时读盘闸，与本轮无关），再加 `syncAgentSystemPrompt()` 的 ⚠️。审阅证据（两条同时成立才算合格）：① 挂账句含全部五个 48 号编号 + `48-REVIEW` 标记 + 「不是同一物 / 不得混读」提示；② 闭合句含 `49-REVIEW.md` + 五个 49 号编号，且**不含** `TD-48-01`。**反例**：把 `WR-01` / `WR-02` 从挂账句移出并写成已闭合 —— 那是把 48 号仍开的债（三档徽标文案两处重复 / 技能解析链三处裸 await）误标为已修'
---

<objective>
收口本轮（Gap 1 / Gap 2 / Gap 3 闭合）之后**变成不成立**的文档声明与账本数字。

四处必须纠正：

1. §11.7 明文写「重开对话后的失败历史卡片**不显示**短原因」（括号里给出理由是原因码没有落库）—— 49-05 让原因码经消息词缀落库并还原后，这句即为假（且它正是 WR-02 当初被接受为「已知偏差」的成文依据，不删就等于继续声称一个不存在的限制）；
2. §11.8 的挂账句里列的是 **48 号**编号（`TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06`）—— 那句话**本身不改、一项不移出**；真正缺的是**本轮闭合声明的命名空间**：若只是把 `WR-01` / `WR-02` 从挂账句移进闭合句，就会把 48 号仍开的技术债误标为已闭合（48 的 `WR-01` = 三档徽标文案两处重复、`WR-02` = 技能解析链三处裸 await），也会漏保 `WR-06`；正确做法是**另起一句**并显式标出 `49-REVIEW.md` 命名空间；
3. 失败消息的 `[code]` 词缀是**新引入的可见面变化**（LLM 与卡片展开区都看得到），按 AGENTS.md 的维护约定必须成文；
4. 三处测试例数账本（`docs/product/ai-skills.md` §七 与 §11.8、`AGENTS.md` 的测试行）在 49-04 / 49-05 加了行之后必然过期。

Purpose: 让产品说明文档与实现**逐条一致**，并把「文档数字 == 实测数字」变成一条可重跑的机械判据；同时保证仍挂账的技术债一个都没被顺手写成已闭合。

Output: `docs/product/ai-skills.md` 的 §11.3 邻接成文 / §11.7 口径 / §11.8 挂账与计数 / §七 计数；`AGENTS.md` 的测试行计数与 `manage_skill` 维护约定的四条新增不变式。

**禁用项召回记录的归属（本计划不重跑召回）**：本 phase 的两段式禁用项召回记录（含 canon-referral 的丢弃理由与逐条结论）以 **`49-04-PLAN.md` 的那一份为准**，是本 phase 的权威副本；本计划与 49-05 一样**不重复召回**，只保留自己 `must_haves.prohibitions` 里的三条（原样保留、不删不改）。本计划的禁用项与 49-04 的三条同源：都是「账本口径必须可被机械判据表达、且判据必须能失败」这条纪律在不同对象上的投影。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-manage-skill-ai/49-VERIFICATION.md
@.planning/phases/49-manage-skill-ai/49-REVIEW.md
@.planning/phases/49-manage-skill-ai/49-04-PLAN.md
@.planning/phases/49-manage-skill-ai/49-05-PLAN.md
@.planning/phases/49-manage-skill-ai/49-03-PLAN.md
@.planning/phases/49-manage-skill-ai/49-04-SUMMARY.md
@.planning/phases/49-manage-skill-ai/49-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §11.7 失败态可见性口径 + §11.8 命名空间化记账（48 号挂账句不动 / 49 号闭合另起一句）+ 失败消息词缀成文</name>
  <files>docs/product/ai-skills.md</files>
  <read_first>
    - `docs/product/ai-skills.md`（被改造文件）：`:457-476`（§11.3 九条拒绝原因表 —— 在它之后或 `oversize` 行邻接处新增词缀成文）、`:501-508`（§11.6 三条诚实边界）、`:509-516`（§11.7 可见性与时序 —— 含「卡片头部」那一条里「重开对话后的失败历史卡片不显示短原因」的旧口径，以及「`delete` 成功后不显示来源徽标」这一条**仍成立**的口径）、`:517-527`（§11.8 维护约定与测试 —— 含三处例数声明与**末尾那一行**未闭合项挂账句）
    - `.planning/STATE.md` 的 Phase 48 挂账段（**必读**：`⚠️ [Phase 48 · 继续挂账，不得读成已修] WR-02 / WR-06` 一行逐字定义了裸编号的归属 —— 这是本任务判定「挂账句里的 WR-01 / WR-02 / WR-06 是 48 号编号」的权威依据）
    - `.planning/phases/48-skill-name/48-REVIEW.md` 的 `WR-01` / `WR-02` / `WR-06` 三条（确认 48 号的定义：徽标文案两处重复 / 技能解析链三处裸 await / 已缓存技能的实时读盘绕过 64 KiB 闸 —— 与 49 号的同名编号**不是同一物**）
    - `49-05-PLAN.md` 的 Task 2（失败态原因码经消息词缀落库与还原的具体机制：词缀在消息起始、只含白名单原因码、旧消息不设键）
    - `49-04-PLAN.md` 的 Task 2（写侧权威字节闸口已按组装全文口径 —— §11.3 的那一行已在本 run 的前置计划里改过，本任务只做核对与交叉指引）
    - `49-REVIEW.md` 的 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`（本轮闭合项）与 `WR-03` / `WR-04` / `IN-01`–`IN-06`（本 run 不动的 49 号挂账项）
    - `49-03-PLAN.md` 的 Task 与威胁 `T-49-03-01`（既有的全文件扫描门禁：`TD-48-01` / `TD-48-02` / `WR-02` / `WR-06` 附近 80 字符内不得出现【已修 / 已闭合 / 已修复】—— **本任务的措辞必须让该门禁继续通过**：闭合动词只能出现在编号**之前**，不得跟在 48 号编号之后）
    - `AGENTS.md` 的 `- 测试：` 行与 `AI 自建技能（manage_skill）的维护约定` 条目的既有措辞（本计划 Task 2 / Task 3 要改它，先读清它的现状）
  </read_first>
  <behavior>
    - §11.7 的「卡片头部」那一条：说明失败态的短原因**来自持久化的原因码**（因此重开对话后**同样显示**）；`delete` 成功后不显示来源徽标这一条逐字保留。
    - §11.3 邻接处新增一段：失败消息以原因码词缀起头（给出示例 `[seeded_protected]`），词缀只含白名单原因码、不含被拒内容原文，权威中文完整文案原样跟在词缀之后；并说明它为什么不改变安全边界（原因码本身不是指令、不是路径、不是凭据）。
    - §11.8 末尾是**两句、两个独立行**：第一句是**原挂账句的忠实保留 + 命名空间标注** —— 裸 `TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06` **五项一个不少（含 `WR-06`）**、仍处于未闭合语境，并带 `48-REVIEW` 标记与「与下段 49 号同名编号**不是同一物**、不得混读」的提示；第二句是**本轮闭合声明**，显式写 `49-REVIEW.md` 并列出 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`，且该句**不含** `TD-48-01`；49 号的其余条目（`WR-03` / `WR-04` / `IN-01`–`IN-06`）与 `syncAgentSystemPrompt()` 的 ⚠️（本阶段只完成 3 分之 1）在挂账语境里如实保留。
    - 全文中不出现任何把仍挂账项说成已闭合的措辞；也不出现「本轮已闭合 `WR-01` / `WR-02`」这类**不带命名空间**的裸声明。
  </behavior>
  <action>
    1. **§11.7 改写**「卡片头部」那一条：该条现在把失败态短原因的被丢弃写成一个永久限制（并给出「原因码事后无法还原」式的理由，具体措辞见 `read_first` 里的 §11.7 段 —— 以读到的实际文本为准）。改为如实口径 —— 失败态的短原因**由随消息持久化的原因码还原**，因此重开对话后的失败历史卡片**同样显示**短原因；并补一句边界：**改动前落库的旧失败消息没有原因码**，那些历史卡片仍只有「失败」而无短原因（宁缺勿猜），这是版本演进而非缺陷。`delete` 成功后不显示来源徽标这条**逐字保留**（它仍然成立）。
    2. **§11.3 邻接处新增词缀成文**（放在 §11.3 表后那段说明里，或紧接其后的小段）：失败结果的**消息文本**以方括号包裹的原因码起头（示例写成 `[seeded_protected] …`），随后是中文完整文案原文；并说明三条边界 —— ① 词缀在**消息起始**（因此可被机械识别，且成功文案不可能误命中）；② 只含**白名单原因码**（小写字母与下划线），**不含**被拒内容原文（延续既有的「错误消息不回显被拒文本」口径）；③ 它**不改变任何安全边界**（原因码是枚举标识，不是指令 / 路径 / 凭据），也不改变 `isError` 语义与 LLM 的修正路径。
    3. **§11.8 末尾改成两句、两个独立行（本任务的核心，最容易做错的一步）**：
       - **第一句 = 原挂账句的忠实保留 + 命名空间标注**。原句的五个裸编号 `TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06` **按原顺序、一个不少地留着**（**`WR-06` 不许漏**），在编号后加一个括号注明：以上五个均为 **`48-REVIEW.md`** 的编号，其中 `WR-01` / `WR-02` 与下段 49 号的同名编号**不是同一物**、不得混读。原句其余内容（「也不声称失效链已完整闭合 …… 合计约 1/3」）逐字保留。
       - **第二句 = 本轮闭合声明（另起一行）**，形如：「**本轮（gap closure）已闭合的是 `49-REVIEW.md` 的 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`**」，随后各用一句说明闭合机制（`CR-01` = 渲染端终态标记改为并入且并入逻辑单源；`CR-02` = frontmatter 的 description 改经 YAML 单引号标量编码；`CR-03` = 净化后对净化值复验非空；`WR-01` = 写侧权威字节闸口改按组装后的 SKILL.md 全文；`WR-02` = 失败态原因码经消息词缀持久化并在重载链路还原）；末尾注明：`49-REVIEW.md` 的其余条目（`WR-03` / `WR-04` / `IN-01`–`IN-06`）不在本轮范围内、仍逐条挂账。
       - **两条硬约束**：① 闭合动词（已闭合 / 已修 / 已修复）不得出现在任何 48 号编号**之后**的 80 字符内 —— 否则 49-03 的既有全文件门禁（`T-49-03-01`）会转红；把闭合动词放在编号**之前**即可（第二句开头「已闭合的是 `49-REVIEW.md` 的 …」天然满足）。② 两句必须是**两个独立行**（不得合成一行）：一致性命令按行取句，合并后「同名不同物」的判定无法机械表达。
       - **明令禁止的改法**：把 `WR-01` / `WR-02` / `WR-06` 中任何一个从第一句移进第二句、或把它们从第一句删掉、或把第一句改写成已闭合语境 —— 三者都会把 48 号仍开的技术债误标为已闭合（其中 `WR-06` 与本轮毫无关系，只是因为同名才出现在同一句里）。
    4. **核对**（不改）§11.3 的 `oversize` 行与 §11.5 的净化步骤是否已是 49-04 落地后的口径；若发现仍有被 WR-01 证伪的表述，一并在本任务里纠正（这是核对而非重做）。核对后**必须**重跑 49-03 的全文件门禁命令（`read_first` 里给了它的位置），确认本次文档改动没有把任何 48 号编号变成「已闭合」。
    5. 全文措辞纪律：不出现 `npm test`；不在文档里声称本 run 未验证的事（例如不要写「已人工验证卡片视觉」—— 人工观察仍归 `49-VALIDATION.md` 的 Manual-Only 表，且其中步骤①要在 49-05 的 CR-01 修复之后才有意义）。
  </action>
  <verify>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const b=d.slice(d.indexOf("### 11.7"),d.indexOf("### 11.8"));if(/原因码不落库/.test(b))throw new Error("§11.7 仍声称原因码不落库");if(!/短原因/.test(b))throw new Error("§11.7 未说明失败态的短原因");console.log("11.7 ok");'</automated>
    <fails_when>非零退出（脚本抛出 `§11.7 仍声称原因码不落库` / `§11.7 未说明失败态的短原因`），或没有打印 `11.7 ok`</fails_when>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const i=d.indexOf("[seeded_protected]");if(i<0)throw new Error("未成文失败消息的原因码词缀形态");const w=d.slice(Math.max(0,i-600),i+600);if(!/起始/.test(w))throw new Error("词缀成文未说明它在消息起始");if(!/白名单/.test(w))throw new Error("词缀成文未说明只含白名单原因码");if(!/不回显被拒|不含.{0,8}被拒内容/.test(w))throw new Error("词缀成文未说明不含被拒内容原文");console.log("tag-doc ok");'</automated>
    <fails_when>非零退出（脚本抛出 `未成文失败消息的原因码词缀形态` / `词缀成文未说明它在消息起始` / `词缀成文未说明只含白名单原因码` / `词缀成文未说明不含被拒内容原文`），或没有打印 `tag-doc ok`</fails_when>
    <automated>node -e 'const d=require("fs").readFileSync("docs/product/ai-skills.md","utf8");const lines=d.split("\n");const ledger=lines.find(l=>l.includes("未闭合项如实挂账"));if(!ledger)throw new Error("未找到 §11.8 的挂账句");for(const id of ["TD-48-01","TD-48-02","WR-01","WR-02","WR-06"]){if(!ledger.includes(id))throw new Error("挂账句丢失仍开项 "+id);}if(!ledger.includes("48-REVIEW"))throw new Error("挂账句的编号未标注 48-REVIEW 命名空间");if(!/不得混读|不是同一物/.test(ledger))throw new Error("挂账句未声明同名编号的命名空间隔离");const closure=lines.find(l=>l.includes("49-REVIEW.md")&&l.includes("CR-01"));if(!closure)throw new Error("未找到挂在 49-REVIEW 名下的闭合声明句");for(const id of ["CR-01","CR-02","CR-03","WR-01","WR-02"]){if(!closure.includes(id))throw new Error("49-REVIEW 闭合清单缺 "+id);}if(closure.includes("TD-48-01"))throw new Error("闭合声明句混入 48 命名空间的编号");if(/TD-48-01[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))throw new Error("48 号编号被写成已闭合（49-03 门禁 T-49-03-01 会转红）");if(/WR-02[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))throw new Error("48 号 WR-02 被写成已闭合（49-03 门禁 T-49-03-01 会转红）");if(/WR-06[\s\S]{0,80}(已修|已闭合|已修复)/.test(d))throw new Error("48 号 WR-06 被写成已闭合（49-03 门禁 T-49-03-01 会转红）");console.log("11.8 ok");'</automated>
    <fails_when>非零退出（脚本抛出 `未找到 §11.8 的挂账句` / `挂账句丢失仍开项 …` / `挂账句的编号未标注 48-REVIEW 命名空间` / `挂账句未声明同名编号的命名空间隔离` / `未找到挂在 49-REVIEW 名下的闭合声明句` / `49-REVIEW 闭合清单缺 …` / `闭合声明句混入 48 命名空间的编号` / `48 号编号被写成已闭合…`），或没有打印 `11.8 ok`。**这条命令把「命名空间」做成机械判据**：挂账句必须自证是 48 号的账（含 `48-REVIEW`），闭合句必须自证是 49 号的账（含 `49-REVIEW.md`）且不得把 48 号编号拖进来；末尾三条复跑 49-03 的 `T-49-03-01` 判据，保证本任务的措辞不会把 48 号编号写成已闭合</fails_when>
  </verify>
  <done>§11.7 的失败态口径与实现一致；词缀形态成文（含示例与三条边界）；§11.8 变成「48 号挂账句原样 + 49 号闭合句另起」两句记账，五个 48 号编号（含 `WR-06`）全部留在挂账语境，49-03 的既有门禁仍通过。</done>
  <acceptance_criteria>
    - §11.7 段不再含「原因码不落库」这一表述，且仍说明失败态的短原因。
    - §11.8 的挂账句仍含**全部五个** 48 号编号（`TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06`），带 `48-REVIEW` 标记与「不得混读 / 不是同一物」提示，并仍表述为未闭合语境。
    - §11.8 另有一句挂在 `49-REVIEW.md` 名下的闭合声明，列出 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`，且该句不含 `TD-48-01`。
    - 全文含 `[seeded_protected]` 这一词缀示例，且示例所在段落说明词缀在消息起始、只含白名单原因码、不含被拒内容原文。
    - 49 号的 `WR-03` / `WR-04` / `IN-01`–`IN-06` 仍出现在挂账语境；全文中不存在**不带命名空间**的「WR-01 / WR-02 已闭合」式裸声明。
    - 重跑 49-03 的全文件门禁命令仍输出其通过标记（本任务的措辞没有把任何 48 号编号写成已闭合）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 三处测试例数账本按实测刷新 + 例数一致性判据</name>
  <files>docs/product/ai-skills.md, AGENTS.md</files>
  <read_first>
    - `docs/product/ai-skills.md`：`:96-104`（§七 测试与验证的套件清单 —— 两条含例数：`node tests/test-ai-skills.js` 与 `node tests/test-manage-skill.js`）、`:517-527`（§11.8 的三条例数声明：`test-manage-skill.js` / `test-ai-skills.js` / `test-skill-picker-model.js`）
    - `AGENTS.md`：`- 测试：` 那一行（同一行内含多个技能域套件的计数 —— **只替换本 run 涉及的三个**：`tests/test-manage-skill.js`、`tests/test-ai-skills.js`、`tests/test-skill-picker-model.js`）
    - `49-04-SUMMARY.md` / `49-05-SUMMARY.md` 的 verification 段（三个套件的实测 `# tests` 值；若 SUMMARY 未记录，直接跑套件读输出）
    - `49-03-PLAN.md` 的相关决策（例数一律取实测值、两处账本在同一任务里刷的既有纪律）
  </read_first>
  <behavior>
    - 跑三个套件读 `# tests`：`node tests/test-manage-skill.js`、`node tests/test-ai-skills.js`、`node --test tests/test-skill-picker-model.js`，把三处账本（§七 两条 + §11.8 三条 + `AGENTS.md` 测试行三条）刷新为同一组数字。
    - 每一个被刷新的例数旁保留「实测」标注（沿用既有写法），覆盖面描述补上本 run 新增的面（description 值域与写↔读闸口 / 词缀与重载还原 / 合并函数值域）。
    - 一致性命令：跑三个套件并与文档里的例数比对，任一不一致即以非零退出报错。比对粒度是**账本单元**而不是整份文件：每个 `test-*.js` 出现处从其文件名切到「同一行内的下一个 `test-*.js` 文件名（无后继则行尾）」，该单元内必须**恰好一个**例数且等于实测 —— 这样 `AGENTS.md` 单行里三个套件各自可判，且不受间距宽度影响。
    - 三个套件的实测 `# tests` 是基准（41 / 172 / 99），但命令**不硬编码**这三个数字，而是现场跑套件读取 —— 避免「文档与命令写死了同一组过期数字」的第二种假绿。
  </behavior>
  <action>
    1. 依次跑三个套件，记录各自的 `# tests` 实测值（不要用估算或计划里的任务数）。
    2. 把 `docs/product/ai-skills.md` 的 §七 两条例数、§11.8 三条例数、`AGENTS.md` 测试行的三条例数**全部**刷为同一组实测值，并沿用既有的「（**N 例，实测**）」/「，N 例）」写法（各处的既有措辞不同，**不要**统一改写格式，只改数字与覆盖面描述）。
    3. 覆盖面描述各补本 run 的新增面：自建技能域补「description 值域（`: ` / `#` / 裸标量 / 纯零宽）与写↔读闸口边界」；接线与刷新链补「失败态原因码的词缀与重载还原 / 三态 `promptIncluded` 的消费侧」；卡片纯逻辑补「标记并入函数的值域」。
    4. **不要**动 `AGENTS.md` 那一行里其它套件的计数与描述（bash 策略 / 播种器 / 沙箱 / 取消状态等都不属于本 run）。
    5. `AGENTS.md` 是**整个文件一行一段**的形态，`- 测试：` 那一行是**单条约 799 字符的长行**（含多个套件）。编辑必须用**精确的字符串替换**只改三个计数与相邻的覆盖面片段，绝不整行重写；定位该行时用**它自己的锚点**（以 `- 测试：` 开头的整行），不要用「某个标题之前/之后」的相对取行。改完后跑本任务的两条一致性命令：`counts-parity ok`（**逐账本单元**比对三处账本 == 实测 —— 单元右边界由同一行内下一个 `test-*.js` 文件名或行尾决定，因此 step 3 加宽描述文字不会让任何单元脱管）与 `test-line intact`（本 run 之外的套件未被误删）。
    6. **自查（本任务的验收方式，必做）**：改完后做一次**单点变异验证** —— 分别把 `AGENTS.md` 测试行里三个计数各改成一个明显错误的值（逐次只改一个、改完即跑、跑完复原），确认 `counts-parity ok` 每次都**转红并指名到「文件:行号 + 套件名 + 取到的值」**。三个都测（`test-manage-skill.js` / `test-ai-skills.js` / `test-skill-picker-model.js`），结论写进 SUMMARY。**只测一个不算**：三者的间距不同，正是间距最大的那个（`test-ai-skills.js`）曾被固定宽度窗口漏检。
  </action>
  <verify>
    <automated>node -e 'const cp=require("child_process"),fs=require("fs");const suites=["tests/test-manage-skill.js","tests/test-ai-skills.js","tests/test-skill-picker-model.js"];const meas={};for(const f of suites){const o=cp.execSync("node "+(f.indexOf("picker")>=0?"--test ":"")+f,{encoding:"utf8",stdio:["ignore","pipe","ignore"]});meas[f.split("/").pop()]=String(o.match(/# tests (\d+)/)[1]);}const FN=/test-[\w-]+\.js/g;let bad=[],cells=0;for(const file of ["AGENTS.md","docs/product/ai-skills.md"]){const hit={};fs.readFileSync(file,"utf8").split("\n").forEach((line,ln)=>{const idx=[];FN.lastIndex=0;let m;while((m=FN.exec(line)))idx.push([m.index,m[0]]);for(let i=0;i<idx.length;i++){const base=idx[i][1];if(!(base in meas))continue;const end=i+1<idx.length?idx[i+1][0]:line.length;const uniq=[...new Set([...line.slice(idx[i][0],end).matchAll(/(\d+)\s*例/g)].map(x=>x[1]))];if(!uniq.length)continue;cells++;hit[base]=(hit[base]||0)+1;if(uniq.length!==1||uniq[0]!==meas[base])bad.push(file+":"+(ln+1)+" "+base+" 账本单元取到 ["+uniq.join("/")+"] ≠ 实测 "+meas[base]);}});for(const base of Object.keys(meas))if(!hit[base])bad.push(file+" 的账本未覆盖 "+base);}if(cells<8)bad.push("账本单元数 "+cells+" < 8（§七 两条 + §11.8 三条 + AGENTS.md 测试行三条）");if(bad.length)throw new Error("例数不一致: "+bad.join("; "));console.log("counts-parity ok");console.log("cells="+cells+" measured="+JSON.stringify(meas));'</automated>
    <fails_when>非零退出（脚本抛出 `例数不一致: …`，逐条报出「文件:行号 套件名 账本单元取到 [值] ≠ 实测 值」；或某文件里某套件的账本单元全无例数 ⇒ `… 的账本未覆盖 …`；或账本单元总数 < 8 ⇒ `账本单元数 … < 8（§七 两条 + §11.8 三条 + AGENTS.md 测试行三条）`），或输出里没有 `counts-parity ok`（后者说明从未走到结论行）。**单元边界是本命令的关键，宽度不是**：每个账本单元的右边界由**同一行内下一个 `test-*.js` 文件名（无后继则行尾）**决定，并只在单元内部取 `(\d+)\s*例` —— 因此文件名与例数之间夹多少描述文字都不影响命中。`AGENTS.md` 的 `- 测试：` 是**单行 799 字符**、三个目标套件同处一行（实测文件名→例数间距：`test-skill-picker-model.js` 96、`test-manage-skill.js` 125、`test-ai-skills.js` **258**），且本任务 step 3 还要往同一行补覆盖面描述、把间距**继续加宽**；任何固定宽度的窗口在这种形态下都会漏检至少一个单元 —— 实测反例：`AGENTS.md` 的 `172 例` 改成 `999 例` 后，`[\s\S]{0,200}?` 版本仍打印 `counts-parity ok`（`test-ai-skills.js` 的 258 字符间距超出窗口，该单元零命中，唯一命中值由 `docs/product/ai-skills.md` 供给），即**变异未被检出** —— 这正是必须消除的假绿形态。八个账本单元（§七 两条 + §11.8 三条 + `AGENTS.md` 测试行三条）必须全部落在命中集合里，且每个单元必须**恰好取到一个唯一值**并等于该套件实测的 `# tests`；少一个单元即由末条 `账本单元数 … < 8` 兜住</fails_when>
    <automated>node -e 'const a=require("fs").readFileSync("AGENTS.md","utf8");const line=a.split("\n").find(l=>l.startsWith("- 测试："));if(!line)throw new Error("未找到 `- 测试：` 账本行");for(const k of ["test-manage-skill.js","test-ai-skills.js","test-skill-picker-model.js","test-ai-bash-policy.js"]){if(!line.includes(k))throw new Error("账本行缺少 "+k);}console.log("test-line intact");'</automated>
    <fails_when>非零退出（脚本抛出「未找到账本行」或「账本行缺少 …」），或没有打印 `test-line intact`。**锚点纪律（本 run 已实测的缺陷）**：账本行必须**由它自己的锚点定位**（以 `- 测试：` 开头的整行），**不得**用「维护约定标题出现位置之前的最后一行」之类的相对取行 —— 该标题在 `AGENTS.md` 里以 `- **…` 形态起头，取「之前最后一行」得到的是 `- **`（标题自身的词缀片段）而不是账本行，三条套件断言会全部抛错、命令恒失败（属性：每条自动化命令必须在被强制的实现正确时**可能通过**）。另：不得整行重写该行，只允许精确替换本 run 涉及的三个计数与相邻的覆盖面片段</fails_when>
  </verify>
  <done>三处账本（共八个账本单元）与三个套件的实测例数逐字一致；一致性命令逐单元通过、且单点变异时对三个计数**各自**都能转红并指名到单元；AGENTS.md 测试行的其它套件未被扰动。</done>
  <acceptance_criteria>
    - 例数一致性命令输出 `counts-parity ok`，且其第二行报出 `cells=8`（八个账本单元全部命中：§七 两条 + §11.8 三条 + `AGENTS.md` 测试行三条）。
    - 单点变异验证：把 `AGENTS.md` 测试行的三个计数**逐一**改成错误值（每次只改一个、改完即跑、跑完复原），三次都转红并指名到「文件:行号 + 套件名 + 取到的值」；三个都测过（间距最大的 `test-ai-skills.js` 必须包含在内）。
    - `AGENTS.md` 的测试行仍含 `test-ai-bash-policy.js` 等非本 run 套件（未被整行重写）。
    - 三处账本的例数写法保持各自既有格式（只改数字与覆盖面描述）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: `AGENTS.md` 的 `manage_skill` 维护约定补四条不变式</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md`：`AI 自建技能（\`manage_skill\`）的维护约定` 那一条（现状含四条硬约束：校验器住 `ai-skills-manager.js` 且零 electron；seeded 判定按播种登记表；`LIMITS` 只允许加项；三动作不加确认卡片、`SKILL_THREAT_PATTERNS` 归 Phase 51；以及时序口径与测试口径两句）
    - `docs/product/ai-skills.md` §11.3 / §11.5 / §11.7 / §11.8（本计划 Task 1 / Task 2 的产出 —— 维护约定必须指向它们，正文不重复）
    - `49-04-PLAN.md` / `49-05-PLAN.md` 的关键决策（四条不变式的来源：组装全文闸口 / 净化后复验 / 失败态词缀 / 并入单源）
  </read_first>
  <behavior>
    - 维护约定条目在既有四条硬约束之后追加四条不变式，每条一句话 + 指向应同步的文档位置，不复制正文。
    - 追加后该条目仍完整保留既有四条（校验器位置与依赖纪律 / seeded 判定依据 / `LIMITS` 只加项 / 三动作不加确认卡片与 `SKILL_THREAT_PATTERNS` 归 Phase 51）。
    - 四条不变式的关键名词在条目内逐字出现：写侧权威字节闸口按「组装」后的 SKILL.md 全文、净化后必须「复验」非空、失败态原因码经消息词缀「持久化」、卡片标记的并入逻辑「单源」在 `src/skill-picker-model.js`（并给出函数名 `mergeManageSkillMarker`）。
  </behavior>
  <action>
    1. 在该条目末尾（时序口径 / 测试口径两句之后）追加四条不变式，逐条写成「**约束** —— 理由 + 应同步的文档位置」的形态：
       - 写侧字节闸口的判据对象是**组装后的 SKILL.md 全文**（不是单独的正文字节）—— 两侧判的必须是同一个量，否则会产出「落盘成功但整条被跳过」的幽灵技能；同步 `docs/product/ai-skills.md` §11.3；
       - 净化之后必须对**净化值**复验非空（净化只能缩减，纯零宽字符描述会被净化为空）—— 同步同节 §11.5；
       - 失败态的机器可读原因码经**消息词缀**持久化（词缀在消息起始、只含白名单原因码），重载链路用同一常量还原；改动词缀格式时必须同时改 encode / decode 两侧与 §11.3 的成文；
       - 卡片标记的并入逻辑**单源**在 `src/skill-picker-model.js` 的 `mergeManageSkillMarker`（渲染端与纯 Node 测试共用同一实现）—— 渲染端不得另写一份 `{...prev, ...incoming}`，否则守卫会退回「断言意图」的假绿形态；同步 §11.7 与测试清单。
    2. 把本 run 新增/改写的测试面写进该条目的测试口径句（description 值域与写↔读闸口 / 失败态词缀与重载还原 / 三态 `promptIncluded` / 合并函数值域），与 Task 2 刷新的例数保持一致。
    3. **不得**删除或弱化既有四条硬约束；**不得**把 `SKILL_THREAT_PATTERNS` 的技术债写成已闭合（它仍归 Phase 51，本 run 未触碰）。
    4. 变更条目后复核 `AGENTS.md` 的其它条目未被牵动（同段还有 `allow-tools` 免责、播种维护约定、技能发现与调用维护约定等）。
  </action>
  <verify>
    <automated>node -e 'const a=require("fs").readFileSync("AGENTS.md","utf8");const i=a.indexOf("AI 自建技能（`manage_skill`）的维护约定");if(i<0)throw new Error("未找到 manage_skill 维护约定条目");const b=a.slice(i,i+4000);for(const k of ["组装","复验","词缀","mergeManageSkillMarker","单源"]){if(!b.includes(k))throw new Error("维护约定缺少 "+k);}if(!/LIMITS/.test(b))throw new Error("维护约定丢失既有 LIMITS 约束");if(!/播种登记表/.test(b))throw new Error("维护约定丢失既有 seeded 判定约束");if(!/SKILL_THREAT_PATTERNS[\s\S]{0,40}Phase 51/.test(b))throw new Error("维护约定丢失 SKILL_THREAT_PATTERNS 归 Phase 51 的口径");console.log("agents-md ok");'</automated>
    <fails_when>非零退出（脚本抛出「维护约定缺少 …」/「维护约定丢失既有 LIMITS 约束」/「维护约定丢失既有 seeded 判定约束」/「维护约定丢失 SKILL_THREAT_PATTERNS 归 Phase 51 的口径」），或没有打印 `agents-md ok`。**关键名词的选取纪律**：四条不变式的判据词取「组装 / 复验 / 词缀 / 单源」这类**机制词**，**不取产品文档里的示例串**（例如词缀示例 `[seeded_protected]`）—— 维护约定的职责是「改什么必须同步哪里」，把产品说明的示例复制进来等于长了第二份说明，与 key-decisions 的口径冲突</fails_when>
  </verify>
  <done>维护约定含四条新增不变式与既有四条硬约束；关键名词（组装 / 复验 / 词缀 / `mergeManageSkillMarker` 单源）逐字出现；测试口径句与本 run 新增面一致。</done>
  <acceptance_criteria>
    - `AGENTS.md` 的 `manage_skill` 维护约定条目内含 `组装`、`复验`、`词缀`、`单源`、`mergeManageSkillMarker` 五个关键名词。
    - 既有四条硬约束的关键名词仍在（`LIMITS`、`播种登记表`、零 electron、不加确认卡片），且 `SKILL_THREAT_PATTERNS` 归 Phase 51 的口径逐字保留。
    - 条目内不出现把 `SKILL_THREAT_PATTERNS` 写成已实现的表述。
    - 条目内不复制 `docs/product/ai-skills.md` 的示例串（如词缀示例 `[seeded_protected]`）—— 只写机制与「应同步哪里」。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 实现 → 产品说明文档 | 文档是下一阶段（50 / 51）复用口径的**唯一依据**；文档与实现的偏差会以「照抄错误口径」的形式放大 |
| 文档 → 用户 / 审阅者 | 挂账清单是对外承诺的一部分；写反了就是虚假的安全感或虚假的完成度 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-06-01 | Repudiation | §11.7 / §11.8 的陈旧声明 | high | mitigate | 逐条纠正三处「落地后变成假」的声明（oversize 口径 / 失败态短原因 / 未闭合清单），并用可重跑的源码扫描断言把「不再含旧表述」变成机械判据 |
| T-49-06-02 | Tampering | §11.3 的词缀成文 | medium | mitigate | 明确成文词缀的三个边界（起始位置 / 只含白名单原因码 / 不含被拒内容原文），使后来者不会把它当成可携带任意文本的通道；同时说明它不改变安全边界 |
| T-49-06-03 | Denial of Service | 测试例数账本漂移 | low | mitigate | 三处账本在同一任务里刷，并由一条可重跑的一致性命令（跑套件比对文档数字）把漂移变成非零退出 |
| T-49-06-04 | Information Disclosure | 文档中的示例 | low | accept | 词缀示例只用公开的枚举名（不涉及凭据 / 路径 / 被拒内容），本任务不新增任何敏感示例 |
| T-49-06-SC | Tampering | npm / pip / cargo 安装 | n/a | accept | 纯文档改动，无依赖与安装面，无需 legitimacy gate |
</threat_model>

<verification>
- 六条 `node -e` 源码 / 文档断言（§11.7 口径、§11.3 词缀成文、§11.8 命名空间记账、例数一致性、测试行完整性、`AGENTS.md` 维护约定）各自非零退出即失败。
- 例数一致性命令：跑三个套件并与三处账本（§七 两条 + §11.8 三条 + `AGENTS.md` 测试行三条 = **八个账本单元**）比对，**逐单元**任一不一致即非零退出；单元右边界由同一行内下一个 `test-*.js` 文件名（无后继则行尾）决定，因此不受文件名→例数间距宽度影响（`AGENTS.md` 三处间距实测 96 / 125 / 258 字符，固定宽度窗口会漏检其中至少一个）。变异方向必须实测：三个计数各自改成错误值时命令都要转红。
- 测试行完整性命令：`AGENTS.md` 的测试行仍含本 run 之外的套件（证明编辑是精确替换而非整行重写）。
- 跨计划门禁回归：本任务改完 `docs/product/ai-skills.md` 后**必须重跑 49-03 的全文件门禁命令**（`T-49-03-01` 的源码扫描），确认没有任何 48 号编号被写成已闭合。
- 人工复核面（不自动化）：§11.8 的两个命名空间与 `48-REVIEW.md` / `49-REVIEW.md` 逐条对齐 —— **48 号五个编号（含 `WR-06`）一个都不许移出挂账句**；本轮闭合声明只覆盖 `49-REVIEW.md` 的 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`。
</verification>

<success_criteria>
- 文档与实现逐条一致：写侧闸口口径、失败态可见性、§11.8 的 48 号/49 号分账三处都不再与代码事实相反。
- 新引入的可见面变化（失败消息词缀）成文且带边界，不留未记录的偏差。
- 两个命名空间的账本都没有被污染：§11.8 挂账句里的五个 48 号编号（`TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06`，含 `WR-06`）与 `syncAgentSystemPrompt()` 的 ⚠️（1/3）一个都没被写成已闭合；本轮的五个 49 号闭合项挂在 `49-REVIEW.md` 名下、缺命名空间的裸声明不存在；49-03 的 `T-49-03-01` 门禁重跑仍通过。
- 三处测试例数账本 == 实测值，且可被一条命令重跑校验；`AGENTS.md` 测试行只做了精确替换。
- `AGENTS.md` 的维护约定同时含既有四条硬约束与四条新增不变式，其余条目零改动。
</success_criteria>

## Artifacts this phase produces

本计划创建或改动的每一个符号 / 契约面（plan-review 的 source-grounding 段按此表逐项核对）：

| 类型 | 符号 / 契约面 | 落点 | 变化 |
|------|---------------|------|------|
| doc section | §11.3 邻接的失败消息词缀成文（形态 + 示例 `[seeded_protected]` + 三条边界） | `docs/product/ai-skills.md` | 新增 |
| doc section | §11.7「卡片头部」条的失败态可见性口径 | `docs/product/ai-skills.md` | 改写（短原因可还原；`delete` 无徽标条保留） |
| doc section | §11.8 的记账段（**两句**：48 号挂账句加命名空间标注 + **新增一行**挂在 `49-REVIEW.md` 名下的闭合声明） | `docs/product/ai-skills.md` | 改写（五个 48 号编号一项不移出，含 `WR-06`；闭合声明按 49 号命名空间另起） |
| doc section | §七 两条套件例数 + 覆盖面描述 | `docs/product/ai-skills.md` | 刷新 |
| doc section | §11.8 三条套件例数 + 覆盖面描述 | `docs/product/ai-skills.md` | 刷新 |
| doc section | `- 测试：` 行的三个套件计数与覆盖面片段 | `AGENTS.md` | 刷新（其余套件与其它条目零改动） |
| maintenance clause | `manage_skill` 维护约定的四条新增不变式（组装全文闸口 / 净化后复验 / 失败态词缀 / 并入单源） | `AGENTS.md` | 新增 |

<output>
Create `.planning/phases/49-manage-skill-ai/49-06-SUMMARY.md` when done
</output>
