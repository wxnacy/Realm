---
phase: 49-manage-skill-ai
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - ai-manager.js
  - src/skill-picker-model.js
  - src/renderer.js
  - src/styles/main.css
  - tests/test-ai-skills.js
  - tests/test-skill-picker-model.js
autonomous: true
requirements: [MGMT-01, MGMT-05]
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

key-decisions:
  - '本计划是 Phase 49 的 UI 计划，Task 1 是 tracer 任务：先把【标题在运行中即可见 → 终态徽标与标注出现】这条最短的卡片纵切跑通（主进程标记 + 事件字段 + 渲染端两个分支），再在 Task 2 补参数摘要 / 正文折叠块 / 结果文本 / 重载同形，Task 3 补样式与令牌。理由：本阶段唯一的 UI 面就是这一张卡片，纵切先钉住【两个事件时点的标记契约】，其余都是同一条链上的增量'
  - '判定只在主进程（UI-SPEC 硬约束 4 / 48-03 的既有纪律）：渲染端**零路径字符串匹配、零来源判定、零限额判定、零撞名判定**，只做白名单查表（TIER_BADGE / MANAGE_SKILL_ACTION_LABEL / MANAGE_SKILL_SHORT_REASON）与呈现。理由：渲染端在 file:// 下无 sandboxEnv，拿不到权威路径，任何自行判定都是第二套判定'
  - '失败态的原因码必须经【工具注册层的按 toolCallId 短期元数据】传递，**不能经 result.details**：SDK 在工具 throw 时走 createErrorToolResult（agent-loop.js:519-524），产出的 details 恒为 {}（本会话实读）。业务校验失败仍然 throw（49-01 已落地，LLM 语义不变），工具注册层捕获业务错误对象上的 code / tier 转存为该次工具执行的元数据（读后即删），使成功与失败两条路径的**标记来源与形状逐字一致**（禁止成功读 details、失败读别处两套读法）'
  - '两个事件时点的分工固定：tool_execution_start 只给 {action, name}（params 是**唯一**携带参数的时点，且零 IO），tier / code / promptIncluded 三项**只在 end 给出**。理由：create 的目标在调用前并不存在，磁盘判定只能在执行之后做；而标题必须在运行中即可见（与 48-03 的 read 卡片一致）'
  - 'start 事件的标记必须与新条目分支的 ...spread 兼容：新条目分支（renderer.js:9355-9365）逐字段列举，故新增 manage_skill 字段要显式写进去；而**已存在条目分支（:9347-9353）当前只并入 status / result / error** —— 必须追加标记终态字段的并入，否则徽标与短原因**永不出现**（这是 RESEARCH Pitfall 4，也是本计划最容易漏的一处）'
  - '三张白名单表住 src/skill-picker-model.js（跨进程单源）：ai-manager.js:138 已 require 同一 api 对象，renderer 经 window.SkillPickerModel 取同一份。**不得**在任一侧另写一份映射。表内容严格照 49-UI-SPEC 的 Copywriting Contract（三动作标题 / 九条短原因），且短原因一律**定长 ≤ 6 字、不含技能名、不含变量** —— 这是头部单行不变式在最小面板宽下成立的前提'
  - '全部用户可见字符串经 DOM API + textContent 注入，**零 innerHTML 模板拼接、零不可信字符串进属性**（UI-SPEC 的注入纪律 / TD-48-01 的教训面）。徽标的 label / title / className 一律查 TIER_BADGE 白名单表，表外跳过徽标（不产出 undefined 字面量进 class）；短原因表同理是闭合白名单，表外不渲染标注'
  - '正文折叠块复用 48 D-09 的同一实现（renderSkillContentBox），**不得新建第二份折叠实现**；卡片语境只允许两处 scoped CSS 覆盖（水平内边距对齐 .tool-card-params、内层 max-height: none 以便唯一滚动容器）。同时按 48-UI-REVIEW Pillar 6 的建议为折叠块 header 补 role=button + tabindex=0 + aria-expanded + Enter/Space —— 该补齐作用于**唯一构建实现**，故气泡实例同时获得，属纯增量（无布局 / 配色 / 文案变化）'
  - '两条前置修复是本阶段复用面成立的前提（不是新工作，是 48-UI-REVIEW 挂账的共用面缺陷）：① 三档徽标底色基准由 transparent 改为 var(--bg-secondary)（main.css:7172-7188）；② 浅色 --skill-limit-text 由 #B45309 改为 #92400E（main.css:55）。理由：本阶段把这两个共用元素搬到**同一个带 hover 的卡片头部**上，不修则卡片 hover 态复现 48 的同一条对比度 finding。修完需回归核对面板行（48 的既有面，同一类同时生效）'
  - '新增唯一令牌 --skill-error-text（暗 #FCA5A5 / 亮 #B91C1C），写入 :root,[data-theme=dark] 与 [data-theme=light] **两处**（缺一即对应主题失效）。理由：--danger-color (#EF4444) 作 11px 文字色在卡片底上不达标（实测暗 4.07:1 / 亮 3.30:1），故按 48 对 --skill-limit-text 的同款处置定义专用令牌。四档对比度（卡片底 / hover 底 × 两主题）全部 ≥ 4.5:1'
  - '结果区渲染【结果文本】而非 JSON.stringify(result)：对象形状时取 result.content[] 中的 text 块文本、字符串形状时原样 —— 使实时链路与重载链路渲染**逐字一致**（重载链路的 toolExecutions[].result 本就是文本字符串），并让【下一条消息起可用】与完整失败原因可读。details **不进**结果区（对用户无意义；code / tier / promptIncluded 已由头部与标注承载）'
  - '卡片不响应 skills:changed 广播：它是**不可变历史记录**（与 48-03 的 read 卡片同款），其标题 / 徽标 / 标注 / 正文全部由该次工具调用的参数与终态决定，不由快照重算。渲染端既有的【广播到达即无条件重拉快照】（48-06 / 48-07）保持不变，但不得据此改写已渲染的历史卡片'
  - '本计划**不改** src/skill-picker-model.js 的既有导出语义（只**加**两张白名单表），不动 renderSlashPickerList 的渲染逻辑，不动 renderAISkillPill / 气泡字段（WR-01 的 SKILL_TIER_TITLES 第二份 title 副本**不得**被扩成第三处）'
  - '本计划**不闭合** TD-48-01（面板行 escapeHtml 不转义引号）与 TD-48-02、WR-02、WR-06：用户已裁决 TD-48-01 / TD-48-02 与 49 同批处置但**不在 49 需求范围内**，若执行期顺手修则一并核对并在 SUMMARY 记明，否则逐字保持挂账。本阶段的新增插值面**全部**是 DOM 属性赋值或 textContent，**不扩大**该缺口'

must_haves:
  truths:
    - '工具卡片技能化：模型调用 manage_skill 时，卡片标题在**运行中即时**变为【创建 / 更新 / 删除技能「name」】（来源 = start 事件的 action + name，零 IO），终态再并入来源徽标与至多一条内联标注（D-02）'
    - 'action 不在三值白名单内 / name 非 trim 后非空字符串时，**整个技能变体不成立** → 回落为既有普通卡片（标题 = 工具名 manage_skill），零回归（照抄 48-03 的非法标记回落口径）'
    - '标记字段形状固定为 manageSkill = { action, name, tier?, code?, promptIncluded? }：action 与 name 在 start 给出；tier / code / promptIncluded 在 end 并入；不可判定时**省略键**（跳过徽标 / 标注），不得猜（UI-SPEC 数据契约）'
    - '失败态的原因码与档位**不经 result.details**（SDK 的 createErrorToolResult 产出 details 恒为 {}），而经工具注册层的按 toolCallId 短期元数据转存；成功与失败的标记来源与形状**逐字一致**（单一读法）'
    - '重开对话后卡片形状与实时链路**逐字一致**：getConversationMessages 的既有标记重建循环内**同址**重建 manage_skill 标记（参数已在 t.params 中持久化），终态元数据随显示形状回传；装饰失败原样透传、不丢消息（UI-SPEC 硬约束 3）'
    - '九条原因码 → 头部短原因标注（闭合白名单，表外不渲染）：seeded_protected→内置不可改删 / user_owned_conflict→用户技能占用 / already_exists→已存在 / not_found→不存在 / limit_exceeded→超数量上限 / invalid_name→名称不合法 / invalid_description→描述不合法 / oversize→正文超限 / unscannable→内容含风险；短原因一律定长且不含技能名（Copywriting Contract）'
    - 'limit_exceeded 的短原因与面板行尾标注（STATUS_TEXT.overLimit）**同值**（查同一张表 / 复用同一字符串，不新写一份）'
    - 'promptIncluded === false 时，成功结果文本末尾追加一句【该技能暂未进入模型提示词（技能段预算已满），仍可用 /skill:{name} 手动调用。】，头部同时出现【未进提示词 · 超预算】标注（逐字复用 48 的 STATUS_TEXT.promptOmitted），两者**互斥于失败态**（失败态不判提示词归属，故标注至多一个）'
    - '成功结果文本含时间语义（【下一条消息起可用 / 生效 / 不再可用】）—— D-13 的忙时语义对用户不可见，不说明会被读成【AI 建的技能没用】（Copywriting Contract 明文：这不是可选修饰语而是契约）'
    - '参数区不出现 content：create / update 渲染三行可读摘要（动作 / 技能名 / 描述），delete 只有前两行且不渲染正文折叠块；**不得** JSON.stringify 整个 params（64 KiB 正文会变成一整面 JSON 墙且正文被二次转义不可读）'
    - '实时链路与重载链路渲染**逐字一致**：结果区取文本而非 JSON.stringify(result)；正文折叠块 header 文案逐字复用 48 D-09 的【技能正文（N 字符）】，N = content.length（JS String.length 口径，与 prompt 段预算同口径，不是字节数）；展开态显示 content 原文（不含 frontmatter、不含包裹标签）'
    - '[UI] E1 loading（explicit）：运行中头部 = 标题（start 的 action + name）+ 既有旋转图标 + 既有【正在执行...】；来源徽标与内联标注**按设计尚未出现**（终态才判定）。无骨架屏、无新增 spinner'
    - '[UI] E1 error（explicit）：失败态头部 = 既有红 ✗ 图标 + 既有【失败】+ 新增 11px 短原因（.tool-card-manage-note-error，取九码白名单）；可判定时同时显示来源徽标。**不弹确认卡片、不插 system-note**（D-01 / D-02）'
    - '[UI] E1 populated（explicit）：终态头部恒为 36px 单行：图标 + 标题（三动作白名单文案）+ 来源徽标（三档 TIER_BADGE）+ 至多一个内联标注 + 状态文字（完成 / 失败）'
    - '[UI] E1 zero-one-many（explicit）：内联标注**至多一个** —— 失败短原因与【未进提示词 · 超预算】互斥（失败态不判提示词归属）；0 个时**不渲染元素**（不占位、不留空元素）'
    - '[UI] E1 long-text（explicit）：超长技能名由 .tool-card-name-text 承担**全部**压缩（min-width: 0 + ellipsis）；徽标与内联标注 flex-shrink: 0 + white-space: nowrap ⇒ 永不截断、永不换行（.tool-card-header 不得声明 flex-wrap）'
    - '[UI] E2 overflow（explicit）：复用存量 .tool-card-value（pre-wrap + word-break: break-word + overflow-x: auto）⇒ 长描述折行，不横向溢出、不撑破卡片宽度'
    - '[UI] E2 long-text（explicit）：description 不截断（受写入门长度约束），pre-wrap 强制折行；参数摘要行数固定为 2–3 行'
    - '[UI] E3 error（explicit）：失败态下 create / update **不产生**正文折叠块（写入未落盘，无正文可示）；展开区只有既有【错误】区全文'
    - '[UI] E3 overflow（explicit）：正文上限 64 KiB —— 默认折叠；展开后由 .tool-card.expanded .tool-card-content 的 max-height: 500px; overflow-y: auto 作**唯一滚动容器**（卡片语境覆盖内层 max-height: none，不产生嵌套滚动条）'
    - '[UI] E3 long-text（explicit）：正文 white-space: pre-wrap + word-break: break-word，长行强制折行、无横向滚动（照抄 48 D-09）'
    - '[UI] E4 overflow（explicit）：结果 / 错误全文经 textContent 注入存量 .tool-card-value（pre-wrap + break-word + overflow-x: auto）⇒ 折行不溢出'
    - '[UI] E4 long-text（explicit）：失败文案长度由九条白名单固定（完整文案单点产出于主进程）；成功文案三条定长'
    - '[UI] E5 zero-one-many（explicit）：0 / 1 / N 张卡片共用存量 .tool-cards-container 的 gap: 8px 纵向排列；一次回合多次 manage_skill 各产一张卡互不影响 —— D-02 拒绝额外 system-note 正是为多时刷屏'
    - statement: '在 AI 面板最小宽度（--ai-panel-min-width 280px，扣气泡内边距）下，带来源徽标 + 内联标注的 manage_skill 卡片头部保持单行不换行，徽标与短原因完整可读（仅技能名缩略，不出现标注被裁切或头部高度变化）'
      verification: backstop
    - '[assumption] 判定只在主进程：渲染端不含任何来源 / 限额 / 撞名的判定逻辑，只查三张白名单表；因此渲染端不可能与主进程判定漂移（源码扫描断言 renderer 侧零 TIER 判定、零 filePath 字符串匹配）'
    - '[assumption] 无障碍增量：折叠块 header 的 role=button + tabindex=0 + aria-expanded + Enter/Space 补齐作用于唯一构建实现（renderSkillContentBox），气泡实例同时获得；本阶段不新增其它键盘语义（卡片头部的展开 / 折叠沿用全仓所有工具卡片的同一范式）'
    - '[assumption] 密集文本不新增截断规则：头部文案长度天然可控（短原因定长 ≤ 6 字，技能名受 ^[a-z0-9-]+$ 与长度 64 约束），故不需要额外的 truncate 规则'
  artifacts:
    - 'src/skill-picker-model.js：MANAGE_SKILL_ACTION_LABEL（Object.freeze 三动作标题白名单）、MANAGE_SKILL_SHORT_REASON（Object.freeze 九码短原因闭合白名单）—— 加入既有 api 对象与 module.exports / window.SkillPickerModel 双挂载'
    - 'ai-manager.js：_resolveManageSkillMarker(action, name) 与 _resolveManageSkillTerminal({ action, name, tier, code, promptIncluded })（后者按 toolCallId 元数据 Map 读取）'
    - 'ai-manager.js：_manageSkillMeta = new Map()（按 toolCallId 的短期元数据；工具 execute 的成功 / 失败两个出口写、tool_execution_end 读后即删）'
    - 'ai-manager.js：tool_execution_start / tool_execution_end 事件新增 manage_skill 字段（snake_case，与 skill_invocation 同款命名法）'
    - 'ai-manager.js：getConversationMessages 的标记重建循环内新增 manage_skill 分支（与既有 read 分支同址、键集合逐字相等）'
    - 'src/renderer.js：renderToolCard 的 manageSkill 分支（标题 + 徽标 + 内联标注）、新条目分支显式映射 manageSkill、**已存在条目合并分支追加终态字段**、结果区文本化分支、参数摘要分支、正文折叠块复用'
    - 'src/renderer.js：renderSkillContentBox 的 a11y 增量（role=button / tabindex=0 / aria-expanded / Enter / Space）'
    - 'src/styles/main.css：.tool-card-manage-note / -error / -limit 三条新规则；--skill-error-text 令牌（两处主题块）；前置修复 ①（徽标底色基准）与 ②（浅色 --skill-limit-text 值）；卡片语境的 .ai-skill-content-box 两处 scoped 覆盖；折叠块 header 的 :focus-visible'
    - 'tests/test-skill-picker-model.js：两张白名单表的值域断言（三动作 + 九码，闭合性：表外键 undefined）'
    - 'tests/test-ai-skills.js：M 组（标记两时点 + 失败态元数据通道 + 重载同形 + 合并分支源码护栏 + 渲染端零判定护栏）'
  key_links:
    - '标记链（实时）：LLM → 工具 execute → tool_execution_start 事件（manage_skill: {action, name}）→ renderer 新条目分支显式映射 → toolExecution.manageSkill → renderToolCard 渲染标题 → 工具返回 → 包 toolCallId 元数据 → tool_execution_end 事件（manage_skill: {tier, code, promptIncluded}）→ renderer **已存在条目合并分支**并入 → 重渲染出徽标与标注'
    - '标记链（重载）：对话库 t.params（已持久化工具参数）+ tool_results 的 details（已持久化）→ getConversationMessages 的重建循环 → 与实时链路**同一个** _resolveManageSkillMarker → toolExecutions[].manageSkill 键集合逐字相等 → renderToolCard 渲染同一形状'
    - '刷新链的可见性收尾：工具成功后 await syncAgentSystemPrompt() → 忙分支置 _skillsPromptDirty → 本轮成功出口 _flushDeferredSkillsPrompt() → 非忙 syncAgentSystemPrompt() → agent.state.systemPrompt 回写 + broadcast(skills:changed) → renderer 既有监听无条件重拉 state.aiSkills（/ 面板同步，48-06 已建）。**卡片不响应该广播**（不可变历史记录）'
    - '白名单单源链：src/skill-picker-model.js 的 TIER_BADGE（:324-340）/ MANAGE_SKILL_ACTION_LABEL（新增）/ MANAGE_SKILL_SHORT_REASON（新增）→ ai-manager.js:138 的 require 同一 api 对象 + renderer 的 window.SkillPickerModel。面板行徽标 / read 卡片 / manage_skill 卡片三处零分叉'
    - '回归护栏：48-03 的实时 vs 重载键集合逐字相等纪律、48-06 的广播消费（无条件重拉快照、无自激回路）、48-02 的面板行渲染、48-09 的折叠块形态断言必须继续成立；renderer 侧 .refreshSkills( 计数仍恒 1'
  prohibitions:
    - statement: '渲染端不得自行判定来源档位 / 名称冲突 / 限额，也不得按路径字符串匹配技能'
      status: unresolved
      verification: judgment
      reason: '硬约束 4 / 48-03 的既有纪律：渲染端在 file:// 下无 sandboxEnv，拿不到权威路径，任何自行判定都是第二套判定来源。源码扫描断言 renderer.js 的 manage_skill 相关区域内不含 filePath / managed-skills / skills-builtin 之类的路径字面量参与判定，也不含 seededNames'
    - statement: '不得把任何来自 LLM 参数或工具结果的字符串拼进 innerHTML / 属性值'
      status: unresolved
      verification: judgment
      reason: 'UI-SPEC 注入纪律（TD-48-01 的教训面）。全部插值面必须是 DOM 属性赋值或 textContent；徽标 label / title / className 一律查白名单表（表外跳过），短原因同理是闭合白名单。本阶段**不扩大** TD-48-01 的缺口（面板行渲染的既有 escapeHtml 归挂账），源码扫描断言新增区域内零 innerHTML'
    - statement: '不得新建第二份折叠实现、第二套徽标实现或第五种折叠外观'
      status: unresolved
      verification: judgment
      reason: 'UI-SPEC 明文【不得另起一套设计系统、第二份徽标实现或第五种折叠外观】；折叠块必须复用 renderSkillContentBox，徽标必须复用 .slash-picker-source-badge* 与 TIER_BADGE。源码扫描断言 renderer.js 中构建技能正文折叠 DOM 的函数恰 1 处'
    - statement: '不得为 manage_skill 插入 system-note / toast / 通知，也不得新增确认卡片'
      status: unresolved
      verification: judgment
      reason: 'D-02 明确否决（与 tool card 信息重复，且一次对话建多个技能会刷屏）；D-01 锁定三动作一律自动。源码扫描断言 manage_skill 相关区域内零 pushSystemNote、零 requestActionConfirmation'
---

<objective>
交付 Phase 49 的**用户可见面**：把 `manage_skill` 的工具卡片技能化（`renderToolCard()` 的 `manage_skill` 变体），使 AI 自建技能的创建 / 更新 / 删除成为用户可见的持久动作记录，并让失败原因、来源档位、是否进入提示词三件事在卡片上可读。

本计划是三个计划中的第二个，**Task 1 是 tracer 任务**：先打通【运行中标题即可见 → 终态徽标与标注出现】这条最短纵切（主进程两时点标记 + 失败态元数据通道 + 渲染端两分支），Task 2 补参数摘要 / 正文折叠块 / 结果文本化 / 重载同形，Task 3 补样式与令牌（含 48-UI-REVIEW 的两条前置修复）。

Purpose: 让「AI 自建技能」这件事对用户**可观察、可追责**，并且把「为什么被拒」从一句模糊的失败变成九条闭合白名单里的可读短原因 —— 同时不新增任何判定源（判定全在 49-01 落地的 manager 层，渲染端只查表）。

Output: `src/skill-picker-model.js` 的两张白名单表；`ai-manager.js` 的两时点标记与失败态元数据通道 + 重载链路；`src/renderer.js` 的卡片变体 / 合并分支 / 结果文本化 / 折叠块 a11y；`src/styles/main.css` 的三条新规则 + 一个新令牌 + 两条前置修复 + 两处 scoped 覆盖；`tests/test-skill-picker-model.js` 与 `tests/test-ai-skills.js` 的对应断言组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-manage-skill-ai/49-CONTEXT.md
@.planning/phases/49-manage-skill-ai/49-RESEARCH.md
@.planning/phases/49-manage-skill-ai/49-UI-SPEC.md
@.planning/phases/49-manage-skill-ai/49-VALIDATION.md
@.planning/phases/49-manage-skill-ai/49-01-PLAN.md
@ai-manager.js
@src/renderer.js
@src/skill-picker-model.js
@src/styles/main.css
@ai-conversations-manager.js
@tests/test-ai-skills.js
@tests/test-skill-picker-model.js
@AGENTS.md
</context>

<assumptions>
> 本阶段的 UI Considerations 报告为 `applicable 23 / resolved 23 / unresolved 0`（`byVerification { explicit: 13, backstop: 1 }`，`dismissed 9`）。
> **13 条 explicit 已逐条 lift 到上方 `must_haves.truths` 的 `[UI]` 行**（无静默丢弃）；
> **1 条 backstop 已作为结构化扁平标量标记**（`{ statement, verification: backstop }`）lift 到 `must_haves.truths` ——
> 该条在 verify 期若无显式证据（真实数据集的视觉确认）则路由 `human_needed`，绝不静默通过。
> **9 条 dismissed 逐条记录在此（不静默丢弃，但按设计不成立为可验证面）**：
> 1. **E1 / empty** —— 卡片由工具调用产生，本回合没有调用 `manage_skill` 就没有卡片，容器不渲染空壳；不存在【无数据】呈现面。
> 2. **E1 / partial** —— 无渐进就绪态；整张卡片由一次工具执行的两个事件驱动，不存在字段部分到位仍可交互的中间态。
> 3. **E3 / loading** —— 折叠块不加载；`content` 来自本次工具调用的参数，展开即时可用，无任何异步取数与中间态。
> 4. **E5 / empty** —— 容器内 0 张卡片时不渲染任何东西（无空态文案、无占位）。
> 5. **E5 / loading** —— 容器自身不加载数据；卡片由流式事件逐个插入，运行中态由单卡 E1 承载。
> 6. **E5 / error** —— 容器无独立失败态；单次调用失败由该卡片自身的 error 态（E1）承载，容器继续正常排列其余卡片。
> 7. **E5 / populated** —— 容器不定义自身视觉；1 张卡片的 happy path 即存量 `.tool-card` 正常渲染，容器只提供 `gap: 8px` 纵向排列。
> 8. **E5 / partial** —— 卡片在 `tool_execution_start` 时**原子插入**，不存在【容器已有但内容未齐】的中间态。
> 9. **E5 / overflow** —— 容器纵向排列且不设 `overflow`；卡片数量增长由外层聊天消息滚动区承担（存量链路），容器自身无滚动 / 裁切行为。
>
> 另一条显式记录（不是 dismiss、也不是要求）：**未采用 `media` 元素种类** —— 头部三态 SVG 是**状态字形**而非内容图片
> （缺它会改变 E1 的 `empty` 计数，故显式记录此判断）。
</assumptions>

<!-- planner-discipline-allow: 技能名 / 描述 / 正文 / 内置技能 / 托管技能 / 来源徽标 -->

<tasks>

<task type="tracer">
  <name>Task 1: 卡片纵切最小可运行 —— 两时点标记 + 失败态元数据通道 + 渲染端标题与徽标</name>
  <files>src/skill-picker-model.js, ai-manager.js, src/renderer.js, tests/test-skill-picker-model.js, tests/test-ai-skills.js</files>
  <read_first>
    - `src/skill-picker-model.js`（被改造文件）：`:1-30`（双模式导出的文件头纪律与 `SKILL_NAME_RE`）、`:239-247`（`STATUS_TEXT` 的 `Object.freeze` 白名单形态与 `promptOmitted` / `overLimit` 两条既有字符串 —— 新增两张表要**照它的形态**，且 `limit_exceeded` 的短原因必须与 `overLimit` **同值**）、`:324-340`（`TIER_BADGE` 三档的 `label` / `className` / `title` 形状）、`:342-357`（`api` 对象与双挂载结尾 —— 新增表加进 `api`）
    - `ai-manager.js`（被改造文件）：`:138`（`require` 同一 `skill-picker-model` api 对象 —— 主进程与 renderer 共用一份表）、`:129` / `:1512` / `:1532-1540`（`getSeededSkillNamesSafe()`）、`:1661-1691`（`_resolveSkillMarker` 的 JSDoc 与实现 —— **新增的 `_resolveManageSkillMarker` 要与它同款**：同步、零 IO、只读已有内存数据）、`:1751-1766`（`tool_execution_start` 的现有分支与 `skill_invocation` 字段写法）、`:1780-1830`（`tool_execution_end` 分支 —— 终态标记在此并入，需要看它当前发了哪些字段）、`:2754-2789`（`getConversationMessages` 的显示层装饰段与 read 标记重建循环 —— **新增的 manage_skill 重建与它同址**）、`:5813-5931`（`memory` 工具项与 49-01 新增的 `_buildManageSkillTool()`）、`:2831-2859`（`syncAgentSystemPrompt()` —— 本计划不改）
    - `src/renderer.js`（被改造文件）：`:9332-9367`（`tool_execution_update` 的三分支：**已存在条目合并分支**（`:9347-9353`，只并入 status / result / error —— 必须追加标记终态字段）、新条目分支（`:9355-9365`，逐字段列举 —— 必须显式加 `manageSkill`）、以及其后的 `renderToolCards` 调用）、`:9543-9600`（`renderToolCard` 的 JSDoc 与函数体：`:9560-9573` 三态图标 / 状态、`:9574-9593` 的 `skillInvocation` 分支（**可复用片段**：`name.classList.add('tool-card-name-skill')` + `.tool-card-name-text` + `TIER_BADGE` 查表三行）、`:9594-9598` 的 `execute_action` 与默认分支）、`:8890-8925`（`renderAISkillPill` —— 徽标构造的**另一处**既有实现，注意它与卡片分支都查同一张 `TIER_BADGE` 但**不共用函数**，本计划**不**强行合并（避免动气泡面））
    - `tests/test-skill-picker-model.js`：文件头（纯 Node 可直接 require 双模式导出）与既有 `TIER_BADGE` 断言组的写法
    - `tests/test-ai-skills.js`：`:1127-1205`（源码断言基建 `readSource` / `methodBody`）、`:3160-3190`（K 组 `promptCtx` 与 `rescanCalls` 断言范式）、`:2758-2781`（`skillResolveCtx` 的 own-property 包装纪律）
    - `node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js:519-524`（`createErrorToolResult` 产出 `details: {}` —— **硬约束 2 的根因**，本任务必须据此把失败态 code / tier 改走元数据通道）
    - `.planning/phases/49-manage-skill-ai/49-UI-SPEC.md`：§数据契约（两时点分工 + 四条硬约束）、§Copywriting Contract 的三动作标题与小节纪律、§卡片结构契约的头部三元素与单行不变式
  </read_first>
  <behavior>
    - `MANAGE_SKILL_ACTION_LABEL`：`create` → `创建技能「{name}」`；`update` → `更新技能「{name}」`；`delete` → `删除技能「{name}」`；表外键取值为 `undefined`（**闭合白名单**，不回落任何默认文案）。
    - `MANAGE_SKILL_SHORT_REASON`：九个键（`seeded_protected` / `user_owned_conflict` / `already_exists` / `not_found` / `limit_exceeded` / `invalid_name` / `invalid_description` / `oversize` / `unscannable`）→ 逐字为 `内置不可改删` / `用户技能占用` / `已存在` / `不存在` / `超数量上限` / `名称不合法` / `描述不合法` / `正文超限` / `内容含风险`；表外键 `undefined`；且 `MANAGE_SKILL_SHORT_REASON.limit_exceeded === STATUS_TEXT.overLimit`（同值，不是同文案的第二份拷贝）。
    - `_resolveManageSkillMarker(action, name)`：`action ∈ {create,update,delete}` 且 `name` 为 trim 后非空字符串 → `{ action, name }`；否则 → `null`（整个技能变体不成立，回落普通卡片）。该函数**零 IO、同步**。
    - 工具执行的成功出口写入 `_manageSkillMeta`（键 = toolCallId），失败出口写入 `{ action, name, code, tier }`；`tool_execution_end` 读后即删（Map 不累积）。
    - `tool_execution_start` 事件的 `manage_skill` 字段 = `{ action, name }`（不可判定时 `null`）；`tool_execution_end` 事件的 `manage_skill` 字段 = `{ tier?, code?, promptIncluded? }`（省略不可判定的键）。
    - `_resolveManageSkillTerminal(toolCallId)` 返回合并后的三键对象或 `null`；合并语义：成功取 `tier` + `promptIncluded`，失败取 `tier`（可判定时）+ `code`。
    - renderer 的**已存在条目合并分支**在 `status` / `result` / `error` 之外**额外并入** `manageSkill`（仅当事件带该字段时；不带时保留既有值，避免旧事件把标记抹掉）。
    - 卡片标题在 status === 'running' 时即为【创建技能「foo」】；`manageSkill` 缺失或 action 表外时标题回落到既有 `tool-card-name`（工具名 `manage_skill`）。
  </behavior>
  <action>
    **A. `src/skill-picker-model.js`：两张跨进程单源白名单表**

    1. 在 `TIER_BADGE` 之后新增两个 `Object.freeze` 常量：
       - `MANAGE_SKILL_ACTION_LABEL`：三个动作 → 标题模板（模板里有 `{name}` 占位；**渲染端负责替换**，因为主进程不该拼 DOM 文案）。表内容**逐字**照 UI-SPEC 的 Copywriting Contract。
       - `MANAGE_SKILL_SHORT_REASON`：九个原因码 → 短原因；`limit_exceeded` 的值**必须写成 `STATUS_TEXT.overLimit`**（引用同一常量）而不是重复字面量 —— 这是【同值】的机械保证。
       两表都要加 JSDoc：① 这是**跨进程单源**（`ai-manager.js:138` 与 renderer 的 `window.SkillPickerModel` 共用同一对象）；② 是**闭合白名单**，表外键不渲染（不得回落到 `undefined` 字面量）；③ 短原因一律定长 ≤ 6 字、不含技能名、不含变量（头部单行不变式的前提）。
    2. 把两个常量加进 `api` 对象（`:342-357`），保持双挂载不变。
    3. **不要**改 `STATUS_TEXT`、`TIER_BADGE`、`buildPickerItems`、`filterPickerItems`、`buildSelectableIndexes` 或任何既有导出的语义。

    **B. `ai-manager.js`：两时点标记 + 失败态元数据通道 + 重载链路**

    4. 新增私有字段 `this._manageSkillMeta = new Map();`（在构造函数里与其它私有字段同址初始化；加注释说明它按 `toolCallId` 存**短期**元数据、读后即删、不跨轮累积）。
    5. 新增私有方法 `_resolveManageSkillMarker(action, name)`（放在 `_resolveSkillMarker` 之后）：
       - `if (!name || typeof name !== 'string') return null;` → `const trimmed = name.trim();` → 空则 `null`；
       - `if (action !== 'create' && action !== 'update' && action !== 'delete') return null;`
       - `return { action, name: trimmed };`
       JSDoc 写明：**零 IO、同步**（与 `_resolveSkillMarker` 同款约束 —— start 事件是唯一携带 `params` 的时点，错过就没有第二次机会）；`tier` 不在此判定（`create` 的目标在调用前并不存在）；renderer 只消费，不自行判定。
    6. 新增私有方法 `_resolveManageSkillTerminal(toolCallId)`：从 `this._manageSkillMeta.get(toolCallId)` 取（取到即 `delete`），返回 `{ tier?, code?, promptIncluded? }` 或 `null`。
    7. 在 `_buildManageSkillTool()` 的 `execute` 内（49-01 已建）把两个出口都接上元数据：
       - 成功出口（三个动作分支内、`syncAgentSystemPrompt()` 之前）：`this._manageSkillMeta.set(toolCallId, { action, name: trimmedName, tier: <按 name 判定>, promptIncluded });`
         - `tier` 的判定：**复用 49-01 的既有数据** —— `const list = getAiSkillsManagerLazy().getSkillsForUI(this.getSeededSkillNamesSafe()); const hit = list.find((e) => e.name === trimmedName);` → `hit ? hit.tier : undefined`（`create` 成功后缓存已被忙分支重扫过，故此刻必定能查到；查不到时省略该键，不猜）。**不得**在工具层重新实现来源判定。
       - 失败出口：在 `execute` 外层包 `try { ... } catch (err) { this._manageSkillMeta.set(toolCallId, { action, name: trimmedName, code: err && err.code ? err.code : undefined, tier: <同上判定；目标可能不存在故多为 undefined> }); throw err; }` —— **仍然 throw**（49-01 的语义不变，LLM 照常收到 `isError: true` toolResult），只是顺带把 `code` 留给 end 事件。
       - 注释必须写明**为什么不能走 `result.details`**：SDK 的 `createErrorToolResult(message)`（`agent-loop.js:519-524`）产出的 `details` 恒为 `{}`，失败态的原因码经它传递等于永远丢给渲染端。
    8. `tool_execution_start` 分支（`:1751-1766`）在 `skill_invocation` 旁边新增一行字段：`manage_skill: event.toolName === 'manage_skill' ? this._resolveManageSkillMarker(event.args && event.args.action, event.args && event.args.name) : null,`（snake_case，与 `skill_invocation` 同款命名法；非 `manage_skill` 工具 → `null`，零回归）。
    9. `tool_execution_end` 分支（`:1780-1830`）新增字段：`manage_skill: event.toolName === 'manage_skill' ? this._resolveManageSkillTerminal(event.toolCallId) : null,`。注意 `_resolveManageSkillTerminal` 读后即删 —— 同一 toolCallId 若被重复发送 end 事件，第二次得 `null` 是**正确**行为（标记已在第一次给出），注释写明。
    10. `getConversationMessages` 的 `toolExecutions` 重建循环（`:2774-2782`）在既有 `const marker = this._resolveSkillMarker(t.name, t.params);` 之后，**同址**追加 manage_skill 标记重建：
        - 用 `t.params` 走 `_resolveManageSkillMarker(t.params && t.params.action, t.params && t.params.name)`；
        - 终态三键从**已持久化**的 `t` 上取（`tool_results` 的 `details` 已入库；执行期写入的 `details` 里含 `action` / `name` / `filePath` / `description` / `promptIncluded`，而 `code` 在失败态由 `isError` + 主进程错误消息承载）—— 实际可回填的是 `tier`（按 name 查当前 `getSkillsForUI`）与 `promptIncluded`（`details.promptIncluded`）；`code` 在重载链路上**无法还原**时**省略键**（宁缺勿猜，UI-SPEC 的【无法判定时省略该键】）。
        - 重建对象的**键集合必须与实时链路逐字相等**：把两条链路的产出统一经一个私有方法 `_buildManageSkillDecoration(params, terminal)` 构造（实时链路把 terminal 传内存元数据、重载链路把 terminal 传从 `t` 还原的对象），**不得**写两份对象字面量。
        - 装饰失败仍走既有 `try/catch` 原样透传 + 告警，不丢消息。
    11. **不要**改 `_resolveSkillMarker`、`_decorateSkillUserMessage`、`syncAgentSystemPrompt()`、`refreshSkillsForPanel()`、`_flushDeferredSkillsPrompt()` 的任何一行。

    **C. `src/renderer.js`：事件字段映射与卡片标题 / 徽标**

    12. `tool_execution_update` 事件处理：
        - **新条目分支**（`:9355-9365`）在 `skillInvocation: event.skill_invocation` 旁追加 `manageSkill: event.manage_skill`；
        - **已存在条目合并分支**（`:9347-9353`）改为：保留 `...spread` 的四个既有字段，并**条件并入**标记终态字段 —— 仅当 `event.manage_skill` 非空时写 `manageSkill`（写成 `...(event.manage_skill ? { manageSkill: event.manage_skill } : {})` 或先算一个局部变量再展开；**不要**无条件写 `manageSkill: event.manage_skill`，否则后续不带该字段的 update 事件会把已写入的标记抹成 `undefined`）。
        - 注释写明这是 UI-SPEC 硬约束 1：合并分支此前只并入 status / result / error，不追加这一行则徽标与短原因**永不出现**（新条目分支的 `...spread` 只覆盖首个事件）。
    13. `renderToolCard(toolExecution)`（`:9552`）在既有 `skillInvocation` 分支**之前**插入 `manageSkill` 分支（顺序理由：两种标记互斥，`manageSkill` 更具体）：
        - `const manageSkill = toolExecution.manageSkill;`
        - 判定：`manageSkill && typeof manageSkill.name === 'string' && manageSkill.name.trim() !== '' && MANAGE_SKILL_ACTION_LABEL[manageSkill.action]` 为真才成立；否则落回既有 `skillInvocation` 分支继续判。
        - 成立时：`name.classList.add('tool-card-name-skill')`；文本元素用 `textContent = MANAGE_SKILL_ACTION_LABEL[manageSkill.action].replace('{name}', manageSkill.name)`（**不得**用 `innerHTML`；`replace` 只替换第一个占位符，模板只含一个）；随后按既有三行片段查 `TIER_BADGE[manageSkill.tier]` 构造徽标（表外 / 缺失 → **跳过**，不产出 `undefined` 字面量进 class）。
        - `MANAGE_SKILL_ACTION_LABEL` / `TIER_BADGE` 从 `window.SkillPickerModel` 取（renderer 的既有取值方式，与本文件取 `TIER_BADGE` 的写法一致）。
    14. 本任务**不**加内联标注（`.tool-card-manage-note*`，归 Task 2）、**不**改参数区 / 结果区（归 Task 2）、**不**加任何 CSS（归 Task 3）。因此本任务的验收是【运行中标题正确 + 终态徽标出现且标注尚未出现】。

    **D. 断言**

    15. `tests/test-skill-picker-model.js` 追加一组：两张表的值域逐字断言（三动作 + 九码）、闭合性（表外键 `undefined`）、`limit_exceeded === STATUS_TEXT.overLimit`、表的 `Object.isFrozen`。
    16. `tests/test-ai-skills.js` 追加 `M 组 · Phase 49 manage_skill 卡片标记`：
        - M1（源码）：`_resolveManageSkillMarker` 方法体含三值白名单判定与 `trim`、不含任何 `await`（零 IO 证据）、不含 `getSkillsForUI`；
        - M2（源码）：`tool_execution_start` 分支含 `manage_skill:` 字段、`tool_execution_end` 分支含 `_resolveManageSkillTerminal(`、`_manageSkillMeta` 在 `execute` 的成功与失败两个出口各写一次、失败出口后紧跟 `throw`；
        - M3（源码，**合并分支护栏**）：`src/renderer.js` 的已存在条目合并分支区域内含 `manageSkill`（即 RESEARCH Pitfall 4 的回归护栏），且新条目分支也含 `manageSkill`；
        - M4（源码，渲染端零判定）：`src/renderer.js` 的 manage_skill 相关区域内不含 `seededNames` / `managed-skills` / `skills-builtin` 字符串参与判定、不含 `innerHTML`；
        - M5（源码，重载同形）：`getConversationMessages` 的装饰段含 `_buildManageSkillDecoration(` 且全文件该标识符**恰 1 处定义**（两条链路共用一份对象字面量）；
        - M6（行为）：`_resolveManageSkillMarker` 的四个分支打表（三动作成立 / action 表外 → null / name 空 → null / name 非字符串 → null）。
    17. 跑 `node tests/test-skill-picker-model.js` 与 `node tests/test-ai-skills.js`，记录实测例数。
  </action>
  <verify>
    <automated>node --check ai-manager.js  && node --check src/skill-picker-model.js  && node --check src/renderer.js  && node -e 'const m=require("./src/skill-picker-model.js");const bad=[];for(const t of ["MANAGE_SKILL_ACTION_LABEL","MANAGE_SKILL_SHORT_REASON"]){if(!m[t])bad.push("缺表 "+t);else if(!Object.isFrozen(m[t]))bad.push(t+" 未冻结")}const A=m.MANAGE_SKILL_ACTION_LABEL||{};for(const [k,v] of [["create","创建技能「{name}」"],["update","更新技能「{name}」"],["delete","删除技能「{name}」"]]){if(A[k]!==v)bad.push("动作文案不符 "+k+" = "+A[k])}const R=m.MANAGE_SKILL_SHORT_REASON||{};const want={seeded_protected:"内置不可改删",user_owned_conflict:"用户技能占用",already_exists:"已存在",not_found:"不存在",limit_exceeded:"超数量上限",invalid_name:"名称不合法",invalid_description:"描述不合法",oversize:"正文超限",unscannable:"内容含风险"};for(const k of Object.keys(want)){if(R[k]!==want[k])bad.push("短原因不符 "+k+" = "+R[k])}if(Object.keys(R).length!==9)bad.push("短原因表键数不是 9（必须是闭合白名单）")if(R.limit_exceeded!==m.STATUS_TEXT.overLimit)bad.push("limit_exceeded 与 STATUS_TEXT.overLimit 不同值")if(bad.length){console.error("白名单表不合格:\n"+bad.join("\n"));process.exit(1)}console.log("两张白名单表值域 ok（三动作 + 九码 + 冻结 + 同值约束）")'</automated>
    <fails_when>非零退出，或出现 '白名单表不合格' 或任一条 `缺表` / `未冻结` / `动作文案不符` / `短原因不符` / `键数不是 9` / `不同值` 行，或标准输出不含 "两张白名单表值域 ok（三动作 + 九码 + 冻结 + 同值约束）"</fails_when>
    <automated>node -e 'const fs=require("fs");const src=fs.readFileSync("ai-manager.js","utf8");const bad=[];if(!src.includes("_manageSkillMeta"))bad.push("缺 _manageSkillMeta 元数据通道");if(!src.includes("_resolveManageSkillMarker("))bad.push("缺 _resolveManageSkillMarker");if(!src.includes("_resolveManageSkillTerminal("))bad.push("缺 _resolveManageSkillTerminal");if(!src.includes("_buildManageSkillDecoration("))bad.push("缺两链路共用的装饰构造");const n=(src.match(/_buildManageSkillDecoration\s*\(/g)||[]).length;if(n&lt;3)bad.push("_buildManageSkillDecoration 出现 "+n+" 次（须 1 定义 + 2 调用）");const s=src.indexOf("case \x27tool_execution_start\x27");const e=src.indexOf("case \x27tool_execution_end\x27");const seg1=src.slice(s,src.indexOf("break;",s));const seg2=src.slice(e,src.indexOf("break;",e));if(!/manage_skill:/.test(seg1))bad.push("start 事件缺 manage_skill 字段");if(!/_resolveManageSkillTerminal/.test(seg2))bad.push("end 事件未取终态元数据");if(!/createErrorToolResult/.test(src)||!/details/.test(src))bad.push("注释未记录 details 恒为空的根因（硬约束 2）");if(bad.length){console.error("主进程标记通道不合格:\n"+bad.join("\n"));process.exit(1)}console.log("两时点标记 + 元数据通道 + 重载共用构造 ok")'</automated>
    <fails_when>非零退出，或出现 '主进程标记通道不合格' 或任一条 `缺 ` / `出现 ` / `start 事件缺` / `end 事件未取` / `注释未记录` 行，或标准输出不含 "两时点标记 + 元数据通道 + 重载共用构造 ok"</fails_when>
    <automated>node -e 'const fs=require("fs");const r=fs.readFileSync("src/renderer.js","utf8");const bad=[];const i=r.indexOf("toolExecutions[existingIdx]");const seg=r.slice(i,i+900);if(!/manageSkill/.test(seg))bad.push("已存在条目合并分支未并入 manageSkill（徽标与短原因将永不出现）");const j=r.indexOf("toolExecutions.push(");const seg2=r.slice(j,j+900);if(!/manageSkill/.test(seg2))bad.push("新条目分支未映射 manageSkill");const k=r.indexOf("function renderToolCard(");const seg3=r.slice(k,k+4000);if(!/MANAGE_SKILL_ACTION_LABEL/.test(seg3))bad.push("renderToolCard 未查动作白名单");if(!/TIER_BADGE/.test(seg3))bad.push("renderToolCard 未查徽标白名单");if(/innerHTML/.test(seg3))bad.push("renderToolCard 出现 innerHTML（注入纪律）");if(/seededNames|managed-skills|skills-builtin/.test(seg3))bad.push("渲染端出现了来源判定素材（硬约束 4）");if(bad.length){console.error("渲染端接线不合格:\n"+bad.join("\n"));process.exit(1)}console.log("渲染端三处接线 ok（新条目 / 合并分支 / 标题与徽标查表）")'</automated>
    <fails_when>非零退出，或出现 '渲染端接线不合格' 或任一条 `合并分支未并入` / `新条目分支未映射` / `未查` / `innerHTML` / `来源判定素材` 行，或标准输出不含 "渲染端三处接线 ok（新条目 / 合并分支 / 标题与徽标查表）"</fails_when>
    <automated>node --test tests/test-skill-picker-model.js 22>&1 | grep -E "^# (tests|pass|fail)"  && node tests/test-ai-skills.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（任一测试断言失败），或任一套件的 "# fail" 行 &gt; 0，或 picker 的 "# pass" 行 &lt; 95、（ai-skills）"# pass" 行 &lt; 156（49-01 地板 150 + M 组 ≥6），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    两张白名单表住 `src/skill-picker-model.js` 并跨进程单源（`limit_exceeded` 与 `STATUS_TEXT.overLimit` 同值、九键闭合、两表冻结）；主进程有了零 IO 的两时点标记与按 `toolCallId` 的失败态元数据通道（失败仍 throw，`code` / `tier` 不再依赖恒为 `{}` 的 `result.details`）；重载链路与实时链路共用**一个** `_buildManageSkillDecoration` 构造，键集合逐字相等；渲染端三个接线点齐备（新条目分支映射、**已存在条目合并分支并入**、`renderToolCard` 的 manage_skill 分支查两张白名单表且零 `innerHTML` / 零来源判定）；运行中标题即为【创建 / 更新 / 删除技能「name」】，终态出现来源徽标。
  </done>
</task>

<task type="auto">
  <name>Task 2: 卡片信息的其余三区 —— 参数摘要（剔除 content）、正文折叠块复用（含 a11y 增量）、结果区文本化、短原因与【未进提示词】标注</name>
  <files>src/renderer.js, tests/test-ai-skills.js</files>
  <read_first>
    - `src/renderer.js`（被改造文件）：`:8927-8965`（`renderSkillContentBox(skillInvocation)` 的完整实现 —— 本任务复用它并加 a11y 增量；注意它当前读的是 `skillInvocation.content.length` 与 `skillInvocation.content`）、`:9002-9050`（`renderAIMessageContent` 里 pill 与折叠块的挂载位置 —— 气泡实例，**不改**）、`:9543-9650`（`renderToolCard` 全段：本任务要改的是参数区（`JSON.stringify(toolExecution.params ...)` 的既有写法）与结果区（`JSON.stringify(result)` 的既有写法），以及头部标注的插入点）、`:9332-9367`（事件处理 —— Task 1 已改，本任务不动）
    - `src/styles/main.css`：`:5819-5870`（`.ai-skill-content-box*` 全族：header / chevron / body 的既有属性 —— Task 3 会加两处 scoped 覆盖，本任务只保证 DOM 与类名复用）、`:6082-6160`（`.tool-card-header` / `.tool-card-name` / `.tool-card-name-skill` 的既有规则 —— 头部单行不变式的宿主）、`:6193-6210`（`.tool-card-value` 的 `pre-wrap` + `word-break` + `overflow-x` —— 参数区与结果区的载体）
    - `.planning/phases/49-manage-skill-ai/49-UI-SPEC.md`：§卡片结构契约的【参数区】【技能正文折叠块】【结果区】三小节与【头部三个新增/复用元素】的第 3 项（内联标注）、§Copywriting Contract 的成功结果文本三条与九条完整文案、§UI Considerations 的 E1 error / E1 zero-one-many / E2 overflow / E3 error / E3 overflow / E4 overflow 六行
    - `src/skill-picker-model.js`：Task 1 新增的 `MANAGE_SKILL_SHORT_REASON` 与 `STATUS_TEXT.promptOmitted`
    - `tests/test-ai-skills.js`：`:684-770`（`promptOmitted` 的既有断言）、M 组（Task 1 新增）
  </read_first>
  <behavior>
    - 参数区（create / update）：三行纯文本 `动作：创建` / `技能名：foo` / `描述：<description 原文>`，`content` **不出现**；`delete` 只有前两行。
    - 参数区两行之间的分隔与既有 `.tool-card-value` 的 `<pre>` 语义一致（`\n` 连接，无 JSON 转义可见）。
    - 正文折叠块：create / update 且 `status !== 'failed'` 时出现，`header` 文案为 `技能正文（N 字符）`，N == `content.length`；默认折叠（带 `collapsed` 类）；展开后 `body.textContent` == `content` 原文。
    - `delete` 不渲染正文折叠块；失败态下 create / update **也不**渲染折叠块（写入未落盘，无正文可示）。
    - 结果区：`status === 'completed'` 时标签 `结果`，内容取 `result.content[]` 中第一个 `type === 'text'` 的 `text`（对象形状）或 `result` 本身（字符串形状）；`status === 'failed'` 时标签 `错误`，内容为 `error` 文本。两条链路（实时 / 重载）渲染**逐字一致**（重载链路的 `result` 本就是文本字符串）。
    - 头部内联标注**至多一个**：失败态 → `MANAGE_SKILL_SHORT_REASON[manageSkill.code]` + 类 `tool-card-manage-note tool-card-manage-note-error`（表外 code → **不渲染元素**）；成功且 `manageSkill.promptIncluded === false` 且动作为 create / update → `STATUS_TEXT.promptOmitted` + 类 `tool-card-manage-note tool-card-manage-note-limit`；其余情况**不渲染元素**（不占位、不留空元素）。
    - 折叠块 header 具备 `role="button"` + `tabindex="0"` + `aria-expanded`（随展开态更新），Enter / Space 可切换。
    - 结果区 / 参数区 / 标注内的**全部**动态字符串经 `textContent`，零 `innerHTML`。
  </behavior>
  <action>
    **A. `src/renderer.js`：参数区摘要**

    1. 在 `renderToolCard(toolExecution)` 内，`manageSkill` 分支成立时把参数区内容改为**可读摘要**而非 `JSON.stringify(params)`：
       - 用 `window.SkillPickerModel.MANAGE_SKILL_ACTION_LABEL` 反查动作中文（或另建一个 `MANAGE_SKILL_ACTION_NAME` 三值短表 —— **优先复用**：从 `MANAGE_SKILL_ACTION_LABEL[action]` 里剥掉 `技能「…」` 后缀不可靠，故若需要动作中文名，在 `src/skill-picker-model.js` 的同一张表里补一个 `MANAGE_SKILL_ACTION_NAME = Object.freeze({ create: '创建', update: '更新', delete: '删除' })`，**不新立第三张表**）；
       - 三行（delete 两行）用 `\n` 连接后赋给既有 `.tool-card-value` 元素的 `textContent`；
       - `description` **不截断**（受写入门长度约束，`pre-wrap` 会折行）；
       - 其余工具的参数区渲染路径**零改动**（只在 `manageSkill` 分支内改）。
    2. 参数区的既有 `JSON.stringify` 写法若被多处共用（例如一个 `renderToolParams(params)` 辅助函数），**不要**改那个函数的默认行为 —— 在 `manageSkill` 分支内单独构造摘要文本，避免影响其它工具卡片。

    **B. `src/renderer.js`：正文折叠块复用 + a11y 增量**

    3. `renderSkillContentBox(skillInvocation)` 改为接受一个**最小形状** `{ content }`（当前读的是 `skillInvocation.content`，本任务要允许 `manage_skill` 的卡片传 `{ content: manageSkill.content }` —— 若卡片参数里没有 `content`，从 `toolExecution.params.content` 取）：
       - 保持既有 DOM 结构与类名（`.ai-skill-content-box` / `-header` / `-title` / `-chevron` / `-body`）**逐字复用**；
       - 保持 `title.textContent = '技能正文（' + content.length + ' 字符）'` 与 `body.textContent = content` 的口径不变；
       - **新增 a11y**：`header.setAttribute('role', 'button')`、`header.setAttribute('tabindex', '0')`、`header.setAttribute('aria-expanded', String(!collapsed))` 并**在每次切换时同步更新** `aria-expanded`；`header.addEventListener('keydown', ...)` 处理 `Enter` 与 `Space`（Space 需 `preventDefault` 防页面滚动）。
       - **不要**新建第二个折叠块构建函数 —— 源码门禁断言该构建函数的定义恰 1 处。
    4. 在 `renderToolCard` 的 `manageSkill` 分支内挂载折叠块：条件 = `action !== 'delete'` 且 `status !== 'failed'` 且 `params.content` 是字符串；挂到 `.tool-card-content` 内、参数区之后、结果区之前（照 UI-SPEC 的结构图）。

    **C. `src/renderer.js`：结果区文本化与头部内联标注**

    5. 结果区：在 `manageSkill` 分支成立时，把结果 / 错误区的内容改为**文本**而非 `JSON.stringify`：
       - `const textOf = (v) => { if (typeof v === 'string') return v; if (v && Array.isArray(v.content)) { const t = v.content.find((c) => c && c.type === 'text'); if (t && typeof t.text === 'string') return t.text; } return ''; };`
       - `completed` → `textOf(toolExecution.result)`；`failed` → `typeof toolExecution.error === 'string' ? toolExecution.error : ''`；
       - 标签仍用既有的 `结果` / `错误`；`details` **不进**结果区。
    6. 头部内联标注：在标题文本与来源徽标之后追加（**至多一个**）：
       - 先算 `let noteText = null; let noteClass = null;`；
       - 若 `status === 'failed'` 且 `manageSkill.code` 在 `MANAGE_SKILL_SHORT_REASON` 内 → `noteText = MANAGE_SKILL_SHORT_REASON[manageSkill.code]`、`noteClass = 'tool-card-manage-note tool-card-manage-note-error'`；
       - 否则若 `status === 'completed'` 且 `manageSkill.promptIncluded === false` 且 `action !== 'delete'` → `noteText = window.SkillPickerModel.STATUS_TEXT.promptOmitted`、`noteClass = 'tool-card-manage-note tool-card-manage-note-limit'`；
       - `noteText` 非 null 时才 `createElement` + `textContent` + `className`，并 `appendChild` 到 `.tool-card-name-skill` 容器（**0 个时不渲染元素**）。
    7. 卡片根元素加钩子类：`card.classList.add('tool-card-manage')`（UI-SPEC 明文：**刻意的无 CSS 规则钩子**，供测试与后续阶段定位，不承担样式职责 —— 注释里显式声明它【不是可用样式钩子】）。
    8. **不要**改 `renderAISkillPill`、`renderAIMessageContent`、`renderToolCards` 的既有行为，也不要给卡片头部新增键盘语义（沿全仓所有工具卡片的同一范式）。

    **D. 断言（全部落在 `tests/test-ai-skills.js` 的 M 组）**

    9. 追加：
       - M7（源码 · 参数区）：manage_skill 分支区域内含【动作：】/【技能名：】/【描述：】三行构造，且**不含** `JSON.stringify(toolExecution.params`（说明 content 不再整体入参区）；
       - M8（源码 · 结果区）：manage_skill 分支区域内含取 `type === 'text'` 的文本提取实现，且**不含** `JSON.stringify(result)`；
       - M9（源码 · 折叠块复用）：`renderer.js` 中构建 `.ai-skill-content-box` 的 DOM 函数**恰 1 处**定义；该函数含 `role` / `tabindex` / `aria-expanded` 三个属性设置与 `keydown` 监听（a11y 增量）；且 `manage_skill` 分支挂载折叠块的条件含 `status !== 'failed'` 与 `action !== 'delete'`；
       - M10（源码 · 标注至多一个）：manage_skill 分支区域内 `tool-card-manage-note` 的 **createElement 挂载点恰 1 处**（不是两处 if 各自 append），且短原因取值来自 `MANAGE_SKILL_SHORT_REASON[` 而【未进提示词】取值来自 `STATUS_TEXT.promptOmitted`；
       - M11（源码 · 钩子类）：`card.classList.add('tool-card-manage')` 恰 1 处。
    10. 跑 `node tests/test-ai-skills.js`，记录实测例数。
  </action>
  <verify>
    <automated>node --check src/renderer.js  && node -e 'const fs=require("fs");const r=fs.readFileSync("src/renderer.js","utf8");const k=r.indexOf("const manageSkill = toolExecution.manageSkill");if(k&lt;0){console.error("未找到 manage_skill 分支起点");process.exit(1)}const seg=r.slice(k,k+7000);const bad=[];for(const t of ["动作：","技能名：","描述："]){if(!seg.includes(t))bad.push("参数摘要缺 "+t)}if(/JSON\.stringify\(toolExecution\.params/.test(seg))bad.push("参数区仍整体 JSON.stringify（content 会变成 JSON 墙）");if(/JSON\.stringify\(.*result/.test(seg))bad.push("结果区仍是 JSON.stringify");if(!/type === \x27text\x27|type===\x27text\x27/.test(seg))bad.push("结果区未取文本块");if(!seg.includes("tool-card-manage-note"))bad.push("缺内联标注类");if(!seg.includes("MANAGE_SKILL_SHORT_REASON"))bad.push("短原因未查白名单表");if(!seg.includes("promptOmitted"))bad.push("未进提示词标注未复用 STATUS_TEXT.promptOmitted");if(!seg.includes("tool-card-manage"))bad.push("缺 tool-card-manage 钩子类");if(/innerHTML/.test(seg))bad.push("分支内出现 innerHTML");if(bad.length){console.error("卡片三区不合格:\n"+bad.join("\n"));process.exit(1)}console.log("参数摘要 + 结果文本化 + 内联标注（至多一个）ok")'</automated>
    <fails_when>非零退出，或出现 '卡片三区不合格' 或任一条 `参数摘要缺` / `仍整体 JSON.stringify` / `仍才是 JSON.stringify` / `结果区未取文本块` / `缺内联标注类` / `未查白名单表` / `未复用` / `缺 tool-card-manage` / `innerHTML` 行，或标准输出不含 "参数摘要 + 结果文本化 + 内联标注（至多一个）ok"</fails_when>
    <automated>node -e 'const fs=require("fs");const r=fs.readFileSync("src/renderer.js","utf8");const bad=[];const defs=(r.match(/function renderSkillContentBox\s*\(/g)||[]).length;if(defs!==1)bad.push("技能正文折叠块构建函数定义 "+defs+" 处（须恰 1 处，不得新建第二份）");const i=r.indexOf("function renderSkillContentBox(");const body=r.slice(i,r.indexOf("\n}\n",i));for(const t of ["role","tabindex","aria-expanded","keydown","Enter","preventDefault"]){if(!body.includes(t))bad.push("折叠块 a11y 缺 "+t)}if(!/ai-skill-content-box/.test(body))bad.push("折叠块未复用既有类名");const k=r.indexOf("const manageSkill = toolExecution.manageSkill");const seg=r.slice(k,k+7000);if(!/status !== \x27failed\x27/.test(seg))bad.push("折叠块挂载缺失败态条件（失败时无正文可示）");if(!/action !== \x27delete\x27/.test(seg))bad.push("折叠块挂载缺 delete 条件");const notes=(seg.match(/tool-card-manage-note/g)||[]).length;if(notes&lt;3)bad.push("标注素材出现 "+notes+" 次（类名需含 -error / -limit 两条规则名）");if(bad.length){console.error("折叠块与 a11y 不合格:\n"+bad.join("\n"));process.exit(1)}console.log("折叠块单实现 + a11y 增量 + 两条件挂载 ok")'</automated>
    <fails_when>非零退出，或出现 '折叠块与 a11y 不合格' 或任一条 `须恰 1 处` / `a11y 缺` / `未复用既有类名` / `缺失败态条件` / `缺 delete 条件` / `标注素材出现` 行，或标准输出不含 "折叠块单实现 + a11y 增量 + 两条件挂载 ok"</fails_when>
    <automated>node tests/test-ai-skills.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（M 组或既有用例回归失败），或 "# fail" 行 &gt; 0，或 "# pass" 行 &lt; 161（Task 1 地板 156 + 本任务 ≥5），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    manage_skill 卡片的信息面齐备且全部走 DOM API / textContent：参数区是可读三行摘要（`content` 只在折叠块出现）、create / update 且非失败态才挂正文折叠块（复用 `renderSkillContentBox` 唯一实现，header 文案与 N 的口径逐字复用 48 D-09）、结果区渲染文本而非 JSON（两条链路同形）、头部内联标注至多一个（失败短原因查九码白名单 / 成功且未进提示词复用 `STATUS_TEXT.promptOmitted`，0 个时不渲染元素）、折叠块获得 role / tabindex / aria-expanded 与 Enter·Space 支持、卡片根元素带 `tool-card-manage` 钩子类。
  </done>
</task>

<task type="auto">
  <name>Task 3: 样式与令牌 —— 新增标注规则、--skill-error-text 双主题、48 前置修复两条、卡片语境两处 scoped 覆盖与焦点环</name>
  <files>src/styles/main.css, tests/test-ai-skills.js</files>
  <read_first>
    - `src/styles/main.css`：`:20-60`（两处主题令牌块 `:root,[data-theme="dark"]` 与 `[data-theme="light"]` —— 新增令牌**两处都要写**；注意 `--skill-limit-text` 暗 `#F59E0B` / 亮 `#B45309` 的当前位置）、`:5819-5870`（`.ai-skill-content-box*` 全族 —— 卡片语境的两处覆盖要**新增**而**不改**既有规则）、`:6082-6160`（`.tool-card-header`（36px 单行、不得声明 `flex-wrap`）/ `.tool-card-header:hover` / `.tool-card-name` / `.tool-card-name-skill`（含 `gap: 8px` —— 新增标注的间隙**由它提供**，不新增间距声明））、`:6193-6210`（`.tool-card-value`）、`:7162-7200`（`.slash-picker-source-badge` 与三档 `-user` / `-builtin` / `-managed` 的 `color-mix(... 15%, transparent)` / `30%, transparent` —— **前置修复 ① 的落点**）、`:7212`（`.slash-picker-status-limit { color: var(--skill-limit-text) }`）
    - `.planning/phases/49-manage-skill-ai/49-UI-SPEC.md`：§Color 全节（含新增令牌表、四档对比度核算表、前置修复两条的原因与位置）、§Typography 的表末【继承登记】说明（`.tool-card-name` 的 500 字重是存量值、**不得**因此新增任何 500 字重的新类）、§Spacing Scale 的 Exceptions 表、§卡片结构契约的【新增标注类的完整 CSS 规格】代码块与头部单行不变式五条
    - `docs/product/ai-skills.md`：§十 的既有诚实边界段落（样式改动**不**产生文档变更，此处只用于确认本任务**不**需要动文档）
  </read_first>
  <behavior>
    - `.tool-card-manage-note` 的关键属性逐字为：`flex-shrink: 0` / `white-space: nowrap` / `font-size: 11px` / `font-weight: 400` / `line-height: 1.4`；**零** `padding` / 零 `margin`（间隙由宿主 `.tool-card-name-skill` 的既有 `gap: 8px` 提供）。
    - `.tool-card-manage-note-error { color: var(--skill-error-text) }`；`.tool-card-manage-note-limit { color: var(--skill-limit-text) }`。
    - `--skill-error-text` 在 `:root,[data-theme="dark"]` 下为 `#FCA5A5`、在 `[data-theme="light"]` 下为 `#B91C1C`（两处都定义）。
    - `--skill-limit-text` 的**浅色值**为 `#92400E`（暗色值仍为 `#F59E0B`，不动）。
    - 三档徽标的 `background` / `border` 的 `color-mix` 第二参数由 `transparent` 改为 `var(--bg-secondary)`（六处：三档 × background / border）。
    - 卡片语境两处 scoped 覆盖：`.tool-card-content .ai-skill-content-box { margin: 4px 12px 8px; }` 与 `.tool-card-content .ai-skill-content-box-body { max-height: none; }`。
    - 折叠块 header 有 `:focus-visible` 视觉（沿用既有视觉语言，不新增 `outline: none`）。
    - `.tool-card-header` 区域内**不得**出现 `flex-wrap`；本阶段**不新增任何 accent 用途**、不新增 500 字重类。
    - 本阶段新增的 CSS 声明里**零** `padding` / `margin` / `gap` 数值（除上面两处 scoped 覆盖的 `margin` 与 UI-SPEC 明确给出的值以外）。
  </behavior>
  <action>
    **A. 两处主题令牌块（`:20-60`）**

    1. 在 `:root, [data-theme="dark"]` 块内新增 `--skill-error-text: #FCA5A5;`（位置紧邻既有 `--skill-limit-text`），并在该行上方加注释：这是**失败短原因专用文字色**（11px 小字对卡片底与 hover 底四档 ≥ 4.5:1；`--danger-color` 作文字色不达标 —— 暗 4.07:1 / 亮 3.30:1，故按 48 对 `--skill-limit-text` 的同款处置定义专用令牌）。
    2. 在 `[data-theme="light"]` 块内新增 `--skill-error-text: #B91C1C;`，**并把 `--skill-limit-text` 的值由 `#B45309` 改为 `#92400E`**（前置修复 ②），注释写明改值原因（原值对浅色 hover 底实测 3.80:1 不达标；卡片头部与面板行同款 hover，可达性要求一致）。
    3. 两处令牌块的其它行一律不动。

    **B. 前置修复 ①：徽标底色基准钉死（`:7162-7200`）**

    4. 把 `.slash-picker-source-badge-user` / `-builtin` / `-managed` 六个 `color-mix(...)` 调用的**第二个颜色参数**由 `transparent` 改为 `var(--bg-secondary)`（`background` 的 15% 与 `border` 的 30% 都改）。注释写明：底色钉死后不再随宿主背景漂移，比值在面板行与卡片头部的任意 hover 态下同时成立；不修则卡片 hover 态复现 48-UI-REVIEW 的同一条对比度 finding。
    5. `.slash-picker-source-badge` 基类的既有属性（`font-size` / `font-weight` / `line-height` / `padding: 1px 4px` / `border-radius: 3px` / `white-space` / `flex-shrink`）**一律不动**。

    **C. 新增标注规则（紧随 `.tool-card-name-skill` 之后）**

    6. 追加三条规则（逐字照 UI-SPEC 的【新增标注类的完整 CSS 规格】代码块）：
       - `.tool-card-manage-note { flex-shrink: 0; white-space: nowrap; font-size: 11px; font-weight: 400; line-height: 1.4; }`
       - `.tool-card-manage-note-error { color: var(--skill-error-text); }`
       - `.tool-card-manage-note-limit { color: var(--skill-limit-text); }`
       上方注释说明：纯文本、无底色无边框（与 `.slash-picker-status` 同族）；**零 padding / 零 margin** —— 与相邻元素的间隙由宿主 `.tool-card-name-skill` 的存量 `gap: 8px` 提供。
    7. **不得**给 `.tool-card-header` 加 `flex-wrap`（头部恒 36px 单行、永不换行）；**不得**给 `.tool-card-manage-note*` 加 `overflow: hidden` / `text-overflow`（短原因定长，永不截断、永不让位）。

    **D. 卡片语境的两处 scoped 覆盖与焦点环**

    8. 追加两处 scoped 规则（放在 `.tool-card-*` 段内，**不改** `.ai-skill-content-box*` 的既有规则）：`.tool-card-content .ai-skill-content-box { margin: 4px 12px 8px; }`（水平内边距与 `.tool-card-params` 对齐）与 `.tool-card-content .ai-skill-content-box-body { max-height: none; }`（去掉内层滚动 —— 由 `.tool-card.expanded .tool-card-content` 的 500px + `overflow-y: auto` 承担唯一滚动容器，避免嵌套滚动条）。
    9. 为折叠块 header 补 `:focus-visible`（沿用既有视觉语言，例如与既有折叠 / 按钮的焦点视觉一致的一圈描边；**不要**引入 `outline: none`）。注释写明：项目无全局 `outline: none`，故不会出现不可见焦点；本条是 48-UI-REVIEW Pillar 6 建议的落地。
    10. **不要**新增 accent 用途、不要新增 500 字重类、不要改 `.tool-card-name`（存量 500 字重属【继承登记】，不计入本阶段声明的字重数）。

    **E. 断言（追加到 `tests/test-ai-skills.js` 的 M 组）**

    11. 追加：
       - M12（源码 · 令牌两处）：`--skill-error-text` 在 `main.css` 出现**恰 2 次定义**（两处主题块各一次）且两个值分别为 `#FCA5A5` / `#B91C1C`；`--skill-limit-text` 的浅色值为 `#92400E`（旧值 `#B45309` 零命中）；
       - M13（源码 · 徽标底色）：三档徽标规则里 `color-mix(` 的第二颜色参数**全部**为 `var(--bg-secondary)`（`transparent` 在这些规则内零命中）；
       - M14（源码 · 标注规格）：三条 `.tool-card-manage-note*` 规则存在，基类含 `flex-shrink: 0` / `white-space: nowrap` / `font-size: 11px` / `font-weight: 400` / `line-height: 1.4`，且基类**不含** `padding` / `margin`；
       - M15（源码 · 单行不变式）：`.tool-card-header` 规则块内不含 `flex-wrap`；`.tool-card-manage-note` 相关规则内不含 `overflow` / `text-overflow`；
       - M16（源码 · scoped 覆盖与焦点）：两条 `.tool-card-content .ai-skill-content-box` 覆盖存在（一条 `margin`、一条 `max-height: none`），且折叠块 header 的 `:focus-visible` 规则存在。
    12. 跑 `node tests/test-ai-skills.js` 与 `node --test tests/test-skill-picker-model.js`，记录实测例数并写入 SUMMARY 的 `actuals`。
  </action>
  <verify>
    <automated>node -e 'const css=require("fs").readFileSync("src/styles/main.css","utf8");const bad=[];const n=(css.match(/--skill-error-text\s*:/g)||[]).length;if(n!==2)bad.push("--skill-error-text 定义 "+n+" 处（须两处主题块各一）");if(!/--skill-error-text:\s*#FCA5A5/.test(css))bad.push("暗色 --skill-error-text 不是 #FCA5A5");if(!/--skill-error-text:\s*#B91C1C/.test(css))bad.push("浅色 --skill-error-text 不是 #B91C1C");if(!/--skill-limit-text:\s*#92400E/.test(css))bad.push("浅色 --skill-limit-text 未改为 #92400E（前置修复 2）");if(/--skill-limit-text:\s*#B45309/.test(css))bad.push("浅色 --skill-limit-text 旧值 #B45309 仍在");const seg=css.slice(css.indexOf(".slash-picker-source-badge-user"),css.indexOf(".slash-picker-status"));if(/transparent/.test(seg))bad.push("三档徽标仍用 transparent 作底色基准（前置修复 1 未落）");if((seg.match(/#\{0,1\}var\(--bg-secondary\)/g)||[]).length&lt;3)bad.push("徽标底色未全部改用 var(--bg-secondary)（须 3 档 background + 3 处 border）");if(bad.length){console.error("令牌与前置修复不合格:\n"+bad.join("\n"));process.exit(1)}console.log("令牌两处 + 前置修复两条 ok")'</automated>
    <fails_when>非零退出，或出现 '令牌与前置修复不合格' 或任一条 `定义 ` / `不是 #` / `未改为` / `旧值` / `仍用 transparent` / `未全部改用` 行，或标准输出不含 "令牌两处 + 前置修复两条 ok"</fails_when>
    <automated>node -e 'const css=require("fs").readFileSync("src/styles/main.css","utf8");const bad=[];const i=css.indexOf(".tool-card-manage-note {");if(i&lt;0)bad.push("缺 .tool-card-manage-note 规则");else{const seg=css.slice(i,i+400);for(const t of ["flex-shrink: 0","white-space: nowrap","font-size: 11px","font-weight: 400","line-height: 1.4"]){if(!seg.includes(t))bad.push("标注基类缺 "+t)}if(/(^|\n)\s*(padding|margin)\s*:/.test(seg))bad.push("标注基类出现 padding/margin（间隙应由宿主 gap 提供）")}if(!/\.tool-card-manage-note-error\s*\{[^}]*var\(--skill-error-text\)/.test(css))bad.push("缺 -error 规则或未用 --skill-error-text");if(!/\.tool-card-manage-note-limit\s*\{[^}]*var\(--skill-limit-text\)/.test(css))bad.push("缺 -limit 规则或未用 --skill-limit-text");const h=css.slice(css.indexOf(".tool-card-header {"),css.indexOf(".tool-card-header {")+600);if(/flex-wrap/.test(h))bad.push(".tool-card-header 声明了 flex-wrap（破坏单行不变式）");if(!/\.tool-card-content \.ai-skill-content-box \{/.test(css))bad.push("缺卡片语境作用域覆盖 1");if(!/\.tool-card-content \.ai-skill-content-box-body \{[^}]*max-height: none/.test(css))bad.push("缺卡片语境作用域覆盖 2（内层 max-height: none）");if(!/ai-skill-content-box-header:focus-visible/.test(css))bad.push("缺折叠块 header 的 :focus-visible");if(bad.length){console.error("标注规则与作用域覆盖不合格:\n"+bad.join("\n"));process.exit(1)}console.log("标注规则 + 单行不变式 + 两处作用域覆盖 + 焦点环 ok")'</automated>
    <fails_when>非零退出，或出现 '标注规则与作用域覆盖不合格' 或任一条 `缺 .tool-card-manage-note` / `标注基类缺` / `出现 padding/margin` / `缺 -error` / `缺 -limit` / `flex-wrap` / `缺卡片语境作用域覆盖` / `缺折叠块 header` 行，或标准输出不含 "标注规则 + 单行不变式 + 两处作用域覆盖 + 焦点环 ok"</fails_when>
    <automated>node --test tests/test-skill-picker-model.js 22>&1 | grep -E "^# (tests|pass|fail)"  && node tests/test-ai-skills.js 22>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>非零退出（任一测试断言失败），或任一套件的 "# fail" 行 &gt; 0，或 picker 的 "# pass" 行 &lt; 95、（ai-skills）"# pass" 行 &lt; 166（Task 2 地板 161 + 本任务 ≥5），或输出中无 "# pass" 行</fails_when>
  </verify>
  <done>
    `--skill-error-text` 在两个主题块各定义一次（#FCA5A5 / #B91C1C），浅色 `--skill-limit-text` 改为 #92400E（旧值零命中），三档徽标底色基准钉死为 `var(--bg-secondary)`（transparent 零命中），三条 `.tool-card-manage-note*` 规则逐字落地且基类零 padding / 零 margin，`.tool-card-header` 未声明 `flex-wrap`、标注规则不含 overflow / text-overflow，卡片语境的两处 scoped 覆盖与折叠块 header 的 `:focus-visible` 就位；M 组 16 条断言全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主进程 → 渲染端（工具事件 / `skills:changed` 广播） | 事件字段是渲染端唯一的信息来源；渲染端**不做任何判定**，只查白名单表 —— 这条边界保证【判定只在主进程】有机械证据 |
| LLM 参数 / 工具结果 → DOM | 技能名、description、正文、错误全文都是**不可信字符串**，必须经 `textContent` / DOM 属性赋值，绝不进 `innerHTML` 或属性值拼接 |
| 对话库 → 历史卡片（重载链路） | 重载链路的标记必须与实时链路**逐字同形**，否则历史卡片会退化成普通卡片（形状不一致本身是可用性缺陷，也会掩盖判定漂移） |
| 白名单表 → 渲染输出 | `TIER_BADGE` / `MANAGE_SKILL_ACTION_LABEL` / `MANAGE_SKILL_SHORT_REASON` 是闭合白名单：表外键**跳过**渲染，不产出 `undefined` 字面量进 class / 文本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-02-01 | Tampering | XSS / 属性逃逸（技能名与错误全文进 DOM） | high | mitigate | 卡片变体的**全部**插值面为 `textContent` 或白名单表取值（`badgeEl.textContent` / `badgeEl.title = <白名单表值>` / `nameText.textContent` / 参数区 `.textContent`），**零** `innerHTML` 模板拼接（源码门禁 M4 断言）；本阶段**不扩大** TD-48-01 的既有缺口（面板行 `escapeHtml` 仍挂账，SUMMARY 必须披露） |
| T-49-02-02 | Spoofing | 渲染端自行判定来源档位 / 撞名 / 限额（第二套判定源） | medium | mitigate | 硬约束 4：渲染端只查表；源码门禁 M4 断言 manage_skill 相关区域内不含 `seededNames` / `managed-skills` / `skills-builtin` 等判定素材，也不含 `filePath` 匹配 |
| T-49-02-03 | Repudiation | 失败态原因码经 `result.details` 传递（SDK 产出恒为 `{}`）⇒ 短原因永不出现 | high | mitigate | 失败态的 `code` / `tier` 改走**按 toolCallId 的短期元数据**（工具注册层写、`tool_execution_end` 读后即删）；业务失败**仍然 throw**（LLM 语义不变）；成功与失败两条路径的标记来源与形状逐字一致（单一读法，源码门禁 M2 + M5） |
| T-49-02-04 | Tampering | 终态字段被合并分支丢弃（徽标与短原因永不出现） | high | mitigate | 已存在条目的合并分支追加条件并入 `manageSkill`（仅当事件带该字段时，避免后续事件把标记抹成 `undefined`）；源码门禁 M3 专门断言这一行 |
| T-49-02-05 | Repudiation | 重载链路形状与实时链路不一致（历史卡片退化） | medium | mitigate | 两条链路共用**一个** `_buildManageSkillDecoration` 构造（恰 1 处定义 + 2 处调用，源码门禁 M5）；重载链路对无法还原的 `code` **省略键**（宁缺勿猜）；装饰失败原样透传不丢消息（既有 try/catch） |
| T-49-02-06 | Information Disclosure | 结果区把 `details` 或原始 JSON 全量展示（可能含工作区绝对路径） | low | mitigate | 结果区只渲染**文本**（对象取 `type === 'text'` 块、字符串原样），`details` 不进结果区（UI-SPEC 明文：对用户无意义；`code` / `tier` / `promptIncluded` 已由头部与标注承载） |
| T-49-02-07 | Denial of Service | 结果区 / 参数区渲染 64 KiB 正文导致一次性构建巨量 DOM | medium | mitigate | 正文**只**出现在默认折叠的折叠块内（`display:none`），展开后由 `.tool-card.expanded .tool-card-content` 的 500px + `overflow-y: auto` 作唯一滚动容器；参数区**剔除** `content`（否则展开即一整面 JSON 墙）；折叠块无嵌套滚动条（卡片语境覆盖内层 `max-height: none`） |
| T-49-02-08 | Denial of Service | 头部在最小面板宽下换行 / 标注被裁切 | medium | mitigate | 头部单行不变式：`.tool-card-header` 不得声明 `flex-wrap`；徽标与标注 `flex-shrink: 0` + `white-space: nowrap`；`.tool-card-name-text` 是唯一压缩承担者；短原因定长 ≤ 6 字且不含技能名（源码门禁 M15 断言前两条 + M14 断言规格）。**已知且接受的退化**：280px 最小宽下同时带徽标 + 标注时技能名可能仅剩省略号 —— 该退化已登记为 1 条 backstop（真实数据集视觉确认；不成立时的处理方式是缩短短原因至 ≤ 4 字，**不得**改成换行头部或加 system-note） |
| T-49-02-09 | Tampering | 第二份折叠实现 / 第二份徽标实现 / 第五种折叠外观 | medium | mitigate | 折叠块复用唯一构建函数（源码门禁 M9 断言定义恰 1 处 + a11y 增量在同一函数内）；徽标复用 `.slash-picker-source-badge*` 与 `TIER_BADGE`；本阶段不新建任何图标 / 折叠家族 |
| T-49-02-10 | Repudiation | 复用面（徽标 / `--skill-limit-text`）在卡片 hover 态下对比度不达标 | medium | mitigate | 前置修复 ①②（徽标底色基准钉死为 `var(--bg-secondary)`、浅色 `--skill-limit-text` 改 #92400E），对照 UI-SPEC 的四档对比度核算表；修完回归核对面板行（同一类同时生效） |
</threat_model>

<verification>
- `node --check` 三个被改的 JS 文件通过；`node --test tests/test-skill-picker-model.js` → `# fail 0`、`# pass ≥ 95`；`node tests/test-ai-skills.js` → `# fail 0`、`# pass ≥ 166`
- 白名单单源：两张表冻结、九键闭合、`limit_exceeded === STATUS_TEXT.overLimit`；表内容逐字等于 UI-SPEC 的 Copywriting Contract
- 两时点标记：start 给 `{action, name}`（零 IO、同步、表外 → null）；end 给 `{tier?, code?, promptIncluded?}`（省略不可判定键）；失败态经 `_manageSkillMeta` 而非 `result.details`（`details` 恒为 `{}` 的根因写在注释里）
- 重载同形：`_buildManageSkillDecoration` 恰 1 处定义 + 2 处调用；两条链路键集合逐字相等
- 渲染端三处接线：新条目分支映射 `manageSkill`、**已存在条目合并分支条件并入**、`renderToolCard` 的 manage_skill 分支查两张白名单表 + 零 `innerHTML` + 零来源判定素材
- 卡片信息面：参数摘要剔除 `content`（三行 / delete 两行）、折叠块单实现 + a11y 增量 + 两条件挂载、结果区文本化（不 JSON.stringify）、内联标注至多一个（0 个不渲染元素）
- 样式：`--skill-error-text` 两处定义、浅色 `--skill-limit-text` = #92400E、三档徽标底色基准 = `var(--bg-secondary)`、三条标注规则规格正确、`.tool-card-header` 无 `flex-wrap`、两处 scoped 覆盖 + `:focus-visible` 齐备
- 1 条 backstop（280px 最小宽下的头部单行与可读性）在 verify 期若无可判定证据 → 路由 `human_needed`，**不得**静默通过；9 条 dismissed 已在 `<assumptions>` 逐条记录（无静默丢弃）
- 不动项：`syncAgentSystemPrompt()` / `_flushDeferredSkillsPrompt()` 方法体零 diff；`renderAISkillPill` / `renderAIMessageContent` / `renderToolCards` / `renderSlashPickerList` 行为零变化；`agent-workspace.js` / `ai-skills-manager.js` 零 diff（归 49-01）；TD-48-01 / TD-48-02 / WR-01 / WR-02 / WR-06 逐字保持挂账
- 指纹提示：本计划改 4 个源文件 + 2 个测试文件 ⇒ 49 的阶段指纹在本计划提交后会 stale，属**预期**（收尾时重算）
</verification>

<success_criteria>
- 模型调用 `manage_skill` 时卡片标题在运行中即为【创建 / 更新 / 删除技能「name」】，终态出现来源徽标与至多一条内联标注（D-02）
- 失败态短原因可读且来自九码闭合白名单；`promptIncluded === false` 时成功文案与头部同时给出【未进提示词】提示；短原因与面板行的 `STATUS_TEXT.overLimit` 同值
- 失败态的原因码**不依赖** `result.details`（SDK 产出恒为 `{}`），而经工具注册层的短期元数据通道；成功与失败两条路径的标记形状一致
- 重开对话后卡片形状与实时链路逐字一致（`_buildManageSkillDecoration` 单源）
- 卡片操作面板的十三个 explicit UI consideration 与 1 条 backstop 全部有可验证的落点，九条 dismissed 逐条记录，无静默丢弃
- 渲染端零判定、零 `innerHTML`；本阶段不扩大 TD-48-01 的缺口
- 48-UI-REVIEW 的两条共用面前置修复落地（徽标底色基准 + 浅色 `--skill-limit-text`），四档对比度全部 ≥ 4.5:1
</success_criteria>

<artifacts_this_phase_produces>
> **Artifacts this plan produces** —— 本计划新建的符号与文件（供 plan-review-convergence 的 source-grounding pass 排除）。

**`src/skill-picker-model.js`（新符号）**
- `MANAGE_SKILL_ACTION_LABEL`（Object.freeze，三动作标题模板）
- `MANAGE_SKILL_SHORT_REASON`（Object.freeze，九码短原因闭合白名单）
- `MANAGE_SKILL_ACTION_NAME`（Object.freeze，三动作中文短名；仅在需要时新增，与上一张表同址、不新立第三张表）
- `api` 对象新增上述键（双挂载不变）

**`ai-manager.js`（新符号）**
- 私有字段 `_manageSkillMeta`（Map<toolCallId, 短期元数据>）
- 私有方法 `_resolveManageSkillMarker(action, name)`
- 私有方法 `_resolveManageSkillTerminal(toolCallId)`
- 私有方法 `_buildManageSkillDecoration(params, terminal)`（两链路唯一构造）
- `tool_execution_start` / `tool_execution_end` 事件的新字段 `manage_skill`
- `getConversationMessages` 的 manage_skill 标记重建分支

**`src/renderer.js`（新符号）**
- `renderToolCard` 的 `manageSkill` 分支（标题 + 徽标 + 参数摘要 + 折叠块挂载 + 结果文本化 + 内联标注）
- 卡片根元素钩子类 `tool-card-manage`
- `renderSkillContentBox` 的 a11y 增量（`role` / `tabindex` / `aria-expanded` / `keydown`）

**`src/styles/main.css`（新符号）**
- 令牌 `--skill-error-text`（两处主题块）
- 规则 `.tool-card-manage-note` / `.tool-card-manage-note-error` / `.tool-card-manage-note-limit`
- 规则 `.tool-card-content .ai-skill-content-box` / `.tool-card-content .ai-skill-content-box-body`
- 折叠块 header 的 `:focus-visible` 规则
- 改值（非新符号）：三档徽标的 `color-mix` 第二参数、浅色 `--skill-limit-text`

**`tests/test-ai-skills.js`（新增断言组）**
- `M 组 · Phase 49 manage_skill 卡片标记`（M1–M16）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/49-manage-skill-ai/49-02-SUMMARY.md` when done
</output>

