---
phase: 49-manage-skill-ai
plan: 07
type: execute
wave: 4
depends_on: [49-02, 49-03]
files_modified:
  - src/skill-picker-model.js
  - src/renderer.js
  - src/styles/main.css
  - tests/uat-49-g49-3-panel-layout.js
  - tests/test-skill-picker-model.js
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
  - AGENTS.md
  - .planning/phases/49-manage-skill-ai/49-UI-SPEC.md
autonomous: true
gap_closure: true
gap_ids: [G-49-3]
requirements: [MGMT-01, MGMT-05]
user_setup:
  - service: xiaomi-mimo
    why: "真实 LLM 往返 —— 仅在复用路径（已持久化的 manage_skill 终态会话）拿不到目标卡片时才会走到；realm-dev 的 realm-config.json 里该 key 为空，主进程 `ai-manager.detectEnvVar` 从环境变量解析（既有 UAT 运行即用此路径）"
    env_vars:
      - name: XIAOMI_API_KEY
        source: "小米 MiMo 平台控制台 API Keys；变量名沿用既有 UAT 运行（2026-09-13）已使用的名字"

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

key-decisions:
  - '**处置判定（本计划的核心裁决）：不缩短 `STATUS_TEXT.promptOmitted`。** 把 `src/skill-picker-model.js:242` 的 `未进提示词 · 超预算` 整体缩到 ≤ 4 字会**回退 48 的锁定决策**，证据链四条：① `48-CONTEXT.md:52` 的 **D-12** 逐字给出该串（「面板列出 + 可显式调用 + 行尾标注「未进提示词 · 超预算」或「超数量上限」」）；② `48-UI-SPEC.md:272` 标它来源为「D-12 **原文**」，`:563` 进一步写明「超限两条**刻意长度不对称**……契约要求**照写不统一**」；③ `tests/test-skill-picker-model.js:784` 的用例名即「（D-12 原文两段式，照写不统一）」并在 `:788` 逐字断言该串、`:817` 把它列入「四条行尾状态文案」的跨文件单源守卫；④ `docs/product/ai-skills.md:274-275` 的 §10.1 把该串列为面板四条定长文案之一。**且缩短对面板零收益**：面板行 `flex-wrap: wrap`（48-02 的既有护栏断言）+ 面板宽 ≥ `--ai-panel-min-width`（280px）⇒ 该串在面板里从不被裁切；缩短只会丢掉「未进提示词」这半边语义（效果），只剩「超预算」（原因）'
  - '**E1 的「≤ 4 字」处置照做，但作用对象必须纠正为真正越界的那个标注。** `49-UI-SPEC.md:561` 的 E1 overflow 原文是「若不成立，处理方式是缩短短原因文案（≤ 4 字），**不得**改成换行头部或加 system-note」——该行的 `## 九条原因码` 纪律（`:266`）把「短原因」定义为九码表且自陈「定长 ≤ 6 字，**唯一更长的 `未进提示词 · 超预算` 是复用 48 的既有字符串**」。九码表最宽 6 字 = 66px，在 129px 容器里的预算是 8 + 32 + 8 + 66 = 114 ≤ 129，**本来就成立**；本次实测越界的只有 100px 的那条复用串。因此把 ≤ 4 字施加到九码表既无效（越界仍在）也不必要（会无端改动九条已锁定文案）。E1 的处置**精神**（把头部标注宽度压回预算内）照办，落点改为复用串在**卡片语境**的渲染形态'
  - '**卡片头部改渲染同一单源的 ≤ 4 字机械投影，不新立第二份文案。** 新增 `PROMPT_OMITTED_CARD_NOTE`，值由 `STATUS_TEXT.promptOmitted.split(" · ").pop()` 派生（分隔符实测 = 空格 + U+00B7 + 空格），得 `超预算`（3 字）。之所以取**第二段**而不是第一段：`未进提示词` 是 5 字，**不满足 E1 的 ≤ 4**；`超预算` 是唯一同时满足「≤ 4」与「不新写字面量」的候选。这与本模块既有纪律同款 —— `MANAGE_SKILL_SHORT_REASON.limit_exceeded` 引用 `STATUS_TEXT.overLimit` 而非抄一份（`src/skill-picker-model.js:380-382` 明写「不得改写成第二份拷贝」）；「不新写」的可核对面**不是** `includes` —— 写死第二份字面量 `PROMPT_OMITTED_CARD_NOTE = 超预算`（被引号包裹的独立字符串）会同时满足 `typeof === "string"` / `includes` / `split(" · ").pop() ===` / `length <= 4` 四条。真正的机械保证是**源码级两条**：① 正则 `/PROMPT_OMITTED_CARD_NOTE\s*=\s*STATUS_TEXT\.promptOmitted\.split\(/` 命中（取值必须以**引用 + 投影**的形式书写），形态沿用本仓既有先例 `tests/test-skill-picker-model.js:984` 对 `MANAGE_SKILL_SHORT_REASON.limit_exceeded` 的 `/limit_exceeded:\s*STATUS_TEXT\.overLimit/`（那条的失败消息写着「必须写成引用形式」），**不另创第二套**；② 源码中不存在**被引号包裹的「超预算」独立字面量**（单引号或双引号皆算、含注释 —— 对应的正则即为该判据）—— 这一条同时封掉「声明处引用、导出处写死第二份」的形态。**语义可接受的理由**：卡片有结果区承载完整句（`该技能暂未进入模型提示词（技能段预算已满），仍可用 /skill:{name} 手动调用。`），头部标注只是可扫读的短标签；面板没有结果区，故保留两段式长文案'
  - '**不改任何 CSS 声明，只改注释。** 不存在「标注不裁切 且 技能名宽度 > 0」的第二解：容器 129px 内，标注是 `flex-shrink: 0` 的受保护元素，压缩只能落在唯一可压缩项（`.tool-card-name-text`, `min-width: 0`）上；只要不可压缩簇（标注 + 徽标 + 两处 gap）超过 129px，越界就是**必然结果而非 CSS 缺陷**。修的是**被渲染文本的宽度预算**。因此 `.tool-card-name-skill` 的 `overflow: hidden`（`main.css:6149` 继承自 `.tool-card-name`）**保留** —— 去掉它只会让溢出的标注画到卡片外面；`gap: 8px` 是 48-03 的测试锁定值（既有断言要求 8px、不得改 6px），也不动。CSS 只把实际预算算术写进 `.tool-card-manage-note` **上方**的注释（在 M14/M15 的切片窗口之外，不扰动既有规则断言）'
  - '**「声明零改动」必须由判据承担，不能只写在 `<done>` 里。** 两条互补机制（本次修订新增）：① 驱动每轮把 `src/styles/main.css` 的**声明投影**（整体剔除 `/* … */` 注释后的文本）的 sha256 写入证据 JSON，判据 A9 断言**所有轮次的 sha 彼此相等** —— 红轮在改源码之前跑（该轮 A1/A2/A3 为红即自证「确实是改源码之前」），故该相等等价于「本轮只动了注释」，且**能检出任意位置的声明改动**（含 `.tool-card-header` / `.tool-card-name-text`）；② `<verify>` 里一条纯 Node 门禁把 `.tool-card-manage-note` / `-error` / `-limit` / `.tool-card-name-text` 四个规则块的**声明集**与冻结值逐字比对。之所以必须两条：`max-width: 60px` 这类添加**同时满足** M14（只查基类五属性存在 + 无 padding/margin）、M15（只查 300 字符窗口内无 overflow/text-overflow）与 A1–A7（全部按元素外接矩形与 textContent 判定）—— 盒子窄于文本时字形画到盒外，矩形判据看不见'
  - '**估算数字不得当作实测口径写进持久产物。** `3 字 = 33px` 与由它推出的「技能名可得 48px / 九码最宽 6 字可得 15px」是**字体度量估算**（按 11px CJK 约 11px/字），无实测出处；`280 / 210 / 129 / 100 / 32 / 19px` 才是本轮实测值（见 `49-UAT.md` G-49-3 的几何表）。因此：① `main.css` 的持久注释里，实测值照写，估算值一律标 `≈` 与「字体度量估算，本轮未逐一实测」；② `verification` 第 2 条改用**证据 JSON 里 280px 档的实测标注宽**做判据（`实测宽 + 8 + 32 + 8 <= 129`），估算只作旁注、不参与判定；③ A1–A3 **不依赖**该估算 —— 唯一承重事实是「3 字 < 81px」，即只需 < 2.45em/字，任何合理字体度量都成立'
  - '**验收证据必须是 280px 真实渲染，且必须先红后绿。** 旧驱动的 `noteNotClipped` 判据是**假绿**：它比的是标注自身 `scrollWidth` / `clientWidth`（实测 100 / 100 —— 标注自身永远「不裁切」，裁切发生在**祖先** `.tool-card-name` 的 `overflow: hidden`）。新判据必须比**标注外接矩形右缘 vs 裁切祖先右缘**。执行顺序固定为「先写驱动 → 在**当前树**上跑出红证据 → 再改源码 → 重跑得绿」，红→绿两轮证据都落盘。这是本阶段已记录教训（子串/声明扫描 ≠ 最小宽度下布局成立）的直接兑现'
  - '**不得改动 `STATUS_TEXT` 另外三个键（`shadowed` / `nameClash` / `overLimit`）的取值域契约，也不得改动 `MANAGE_SKILL_SHORT_REASON` 九条取值。** 前者是 48 D-12 的锁定面、后者是 49 D-07 的锁定面，两者在 129px 预算内均成立（最宽 6 字 = 66px）。若执行中认为必须改，**停手上报**，不得顺手改'
  - '**必须保留的测试数据（不得删除、不得清理）**：`realm-dev` 的 `agent-workspace/managed-skills/` 下 `commit-style` + 14 个 `aa-budget-01..14`（每个 description 950 字符）。SDK 按 `localeCompare` 排序且 managed 先于 user ⇒ `aa-budget-*` 必然排在 `commit-style` 之前，把 8000 字符技能段预算（`ai-skills-manager.js` 的 `LIMITS.SKILLS_PROMPT_CHAR_BUDGET`）撑满，`commit-style` 才会落入 `promptOmitted`。删掉它们 = 本 gap 不可复现'
  - '**安全纪律（沿用 2026-09-13 的既有教训）**：驱动只经 playwright `_electron.launch` 启动**自己拉起的 dev 子进程**，收尾用 `electronApp.close()` + `process.exit(0)`（后者防止被「再按一次 Cmd+Q」拦截挂死）。**严禁** `pkill` / `pgrep` 模式匹配杀进程 —— 撰写本计划时实测用户正在运行正式版 `/Applications/Realm.app`（PID 22083），误杀生产实例是不可接受的副作用'
  - '**所有验证命令写成 `node …` 显式形式，不引用 `npm test`** —— 本项目 `package.json` 无该脚本，GSD 的两处 gate 恒解析成不存在的 `npm test` 是已记录的环境问题'
  - '**例数必须按 49-06 的既有纪律取实测值回填，不得硬编码预测值。** 本计划会新增用例，`tests/test-skill-picker-model.js` 与 `tests/test-ai-skills.js` 的实测 `# tests` 会变化，`AGENTS.md` 测试行三条 + `docs/product/ai-skills.md` §七两条 + §11.8 三条 = **八个账本单元**必须在同一任务刷新，并用 §11.8 既有的可重跑一致性命令验收（不是人工核对）；该命令右边界按「同一行内下一个 `test-*.js`」切分，单行多套件的形态也不会漏检'

must_haves:
  truths:
    - '端到端（Gap 靶心 · 真实渲染）：AI 面板经**真实拖拽**到达 `--ai-panel-min-width`（280px）后，同时带来源徽标 + 超预算标注的 `manage_skill` 卡片头部，标注**不被裁切** —— 标注的外接矩形右缘 ≤ 其裁切祖先 `.tool-card-name.tool-card-name-skill` 的外接矩形右缘'
    - '同一渲染下裁切祖先**自身不溢出**：`.tool-card-name.tool-card-name-skill` 的 `scrollWidth <= clientWidth`'
    - '同一渲染下技能名的退化上限是「被省略号压缩」：`.tool-card-name-text` 的 `clientWidth > 0`（修复前实测为 0 —— 整段消失、连省略号都无空间渲染）'
    - '同一渲染下来源徽标完整可读（自身 `scrollWidth <= clientWidth` 且右缘落在裁切祖先盒内）'
    - '同一渲染下头部仍为 36px 单行：`.tool-card-header` 的 `offsetHeight === 36` 且 `scrollHeight <= clientHeight`，且 520px 与 280px 两档同值'
    - '**非空真性（红→绿）**：上述三条新判据在**未修复的当前树**上跑必然为红（标注右缘越界 19px、祖先 `scrollWidth` 148 > `clientWidth` 129、技能名 `clientWidth` = 0），修复后为绿 —— 两轮证据都落盘，杜绝「断言恒真」型假绿'
    - '卡片头部超预算标注渲染的文本是 `超预算`（3 字，E1 的 ≤ 4 字收口），且它是 `STATUS_TEXT.promptOmitted` 的机械投影而非新写字面量 —— **判据不是 `includes`**（写死第二份 `超预算` 也会通过），而是源码级两条同时成立：`src/skill-picker-model.js` 中该常量以 `STATUS_TEXT.promptOmitted` 为基准、按分隔符切分后取末段的**引用形式**声明（正则命中 —— 不允许写成第二份被引号包裹的独立字符串），且该文件中**零个被引号包裹的「超预算」独立字面量**（单引号或双引号皆算，含注释）'
    - '**声明零改动的可核对面**：`src/styles/main.css` 剔除 `/* … */` 注释后的**声明投影**在红轮（改源码之前）与各绿轮之间 sha256 逐字相等，且 `.tool-card-manage-note` / `-error` / `-limit` / `.tool-card-name-text` 四个规则块的声明集与冻结值逐字相等'
    - '**权威契约的卡片文案一致性**：`49-UI-SPEC.md` 中七处陈述「卡片头部标注渲染什么」的位置（Typography 的 Label 行 / 九条原因码纪律那条 / 唯一权威清单该行 / 卡片结构契约第 3 项 / 交互契约终态（成功）行 / UI Considerations 的 E1 overflow 与 zero-one-many 行）**均只出现 `超预算`、不出现长串**；正文中其余提到 `未进提示词` 的行一律显式标明其属于 `/` 面板标注或作为投影来源'
    - '斜杠（`/`）面板的行尾状态标注**逐字未变**（`STATUS_TEXT.promptOmitted === "未进提示词 · 超预算"`）：48 D-12 的原文锁与本阶段 UI-SPEC 的「逐字复用」纪律同时成立'
    - '[assumption] 可复现前提（环境事实，非本计划交付物）：`realm-dev` 的 `agent-workspace/managed-skills/` 下 `commit-style` 与 14 个 `aa-budget-01..14`（每个 description 950 字符）保留 —— 它们按名字排序先于 `commit-style` 并撑满 8000 字符技能段预算，是把 `commit-style` 推入 `promptOmitted`、从而渲染出超预算标注的唯一前提；删掉或改写即本 gap 不可复现（真实渲染门禁会拿不到目标卡片并以非零退出，不得据此判绿）'
  artifacts:
    - 'src/skill-picker-model.js：新增并导出 `PROMPT_OMITTED_CARD_NOTE`（`STATUS_TEXT.promptOmitted` 第二段的机械投影）；`STATUS_TEXT` 四个值、`MANAGE_SKILL_SHORT_REASON` 九个值逐字不变'
    - 'src/renderer.js：`renderToolCard` 的 `manage_skill` 分支里超预算标注改取 `window.SkillPickerModel.PROMPT_OMITTED_CARD_NOTE`（不再取 `STATUS_TEXT.promptOmitted` 本体）；标注元素仍是同一个 `createElement` 挂载点、仍走 `textContent`'
    - 'src/styles/main.css：`.tool-card-manage-note` 上方注释更新为「280px 面板下的实际宽度预算算术 + 卡片短形态的理由」；实测值照写、**字体度量估算一律标 `≈` 与「估算」字样**；**声明零改动**（由 A9 的声明投影 sha 相等 + 纯 Node 的声明集比对双重把关）'
    - 'tests/uat-49-g49-3-panel-layout.js：新增可重跑的真实渲染驱动（playwright `_electron` + 真实拖拽 + 红绿两轮断言 + 证据落盘），脚本名以 `uat-` 开头故永不被 `test-*.js` 类套件拾取'
    - 'tests/test-skill-picker-model.js：派生投影值域 / 长度上限 / 48 原文冻结 / 三键契约冻结的纯逻辑护栏'
    - 'tests/test-ai-skills.js：M10 的渲染端取值来源断言改指新常量'
    - 'docs/product/ai-skills.md + AGENTS.md：卡片头部标注形态说明 + 八个账本单元按实测刷新'
    - '.planning/phases/49-manage-skill-ai/49-UI-SPEC.md：**改动面 = 七处陈述卡片渲染文案的位置 + 「未进提示词」其余出现处的表面标注** + 末尾追加的 E1 overflow 收口记录节（含为什么不改面板文案、为什么不改成换行头部 / system-note）'
  key_links:
    - 'src/renderer.js 的标注取值 → src/skill-picker-model.js 的 `PROMPT_OMITTED_CARD_NOTE`：渲染端不得复写字面量（既有「四条行尾状态文案只在本模块定义」守卫覆盖 `renderer.js` 不含长串；M10 覆盖取值来源）'
    - '.tool-card-manage-note 的渲染宽度 → .tool-card-name 的裁切盒：唯一有效判据是真实渲染的几何量，声明 / 子串扫描不构成证据'
    - '280px 面板 → 卡片 210px → .tool-card-name 129px：可核对面是 CSS 变量 `--ai-panel-min-width`，拖到 260px 应被钳制回 280px'
---

<objective>
闭合 49-UAT.md 判定的唯一 gap `G-49-3`（severity: minor）：AI 面板拖到最小宽度（`--ai-panel-min-width: 280px`）时，同时带来源徽标 + 超预算标注的 `manage_skill` 卡片头部，标注被裁切（右端 19px）、技能名整段消失而非被省略号压缩。

Purpose: 让「头部单行不变式」在最小面板宽度下真正成立 —— 徽标与短原因完整可读，唯一允许的退化回到「技能名被压缩到仅剩省略号」。本修复同时把「子串 / 声明扫描会假绿」这条本阶段已记录的教训兑现为一条**真实渲染**的回归门禁。

Output: `src/skill-picker-model.js` 的 `PROMPT_OMITTED_CARD_NOTE`（≤ 4 字机械投影，源码级「不新写」双判据）+ `src/renderer.js` 的取值切换 + `tests/uat-49-g49-3-panel-layout.js` 真实渲染驱动（红→绿两轮证据 + 声明投影 sha 相等）+ 两套测试的护栏 + 产品文档 / AGENTS.md 的同步 + `49-UI-SPEC.md` 中**七处**卡片渲染文案位置的收口与末尾收口记录节。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-manage-skill-ai/49-UAT.md
@.planning/phases/49-manage-skill-ai/49-UI-SPEC.md
@.planning/phases/49-manage-skill-ai/49-VERIFICATION.md
@.planning/phases/49-manage-skill-ai/49-VALIDATION.md
@.planning/phases/48-skill-name/48-CONTEXT.md
@.planning/phases/48-skill-name/48-UI-SPEC.md
@src/skill-picker-model.js
@src/renderer.js
@src/styles/main.css
@tests/test-skill-picker-model.js
@tests/test-ai-skills.js
@docs/product/ai-skills.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端「最小面板宽度下头部标注完整可读」—— 单源 ≤4 字投影 → 渲染端取值 → 280px 真实渲染红→绿</name>
  <files>tests/uat-49-g49-3-panel-layout.js, src/skill-picker-model.js, src/renderer.js, src/styles/main.css</files>
  <precondition>全局 playwright 可用（`NODE_PATH="$(npm root -g)" node -e "console.log(!!require('playwright')._electron)"` 输出 `true`）；`XIAOMI_API_KEY` 在环境变量中（realm-dev 配置内的 key 为空，主进程 `detectEnvVar` 从环境变量解析；frontmatter 的 `user_setup` 已登记该键）；`~/Library/Application Support/realm-dev/agent-workspace/managed-skills/` 下 `commit-style` 与 14 个 `aa-budget-01..14` 仍在。任一不成立即停手上报，不得改用合成数据或绕过真实渲染。这三项由驱动**第 0 步**逐条自检并输出可判读的失败码（见 `<action>`），不依赖执行者手工比对。</precondition>
  <action>
本任务是本 gap 的垂直切片：从内容单源一路走到真实渲染的几何量，中间不停在任何一层。**顺序不可调换** —— 先写驱动并在**当前（未修复）树**上跑出一轮红证据，再改源码，再重跑得绿。红轮证据是本判据「不是恒真断言」的唯一证明，缺失即视为本任务未完成。

**第 1 步 · 写驱动 `tests/uat-49-g49-3-panel-layout.js`（不改任何源码）**

文件头 JSDoc 必须写明：用途（G-49-3 的真实渲染门禁）、运行方式（须带全局 playwright，即 `NODE_PATH="$(npm root -g)" node tests/uat-49-g49-3-panel-layout.js`）、为什么不能用源码扫描替代（本阶段已记录教训：子串 / 声明存在 ≠ 最小宽度下布局成立）、以及脚本名以 `uat-` 开头故永不被 `test-*.js` 套件拾取。

驱动行为（逐条实现，不得省略）：

0. **前置自检（在任何启动动作之前，逐条判并输出可判读的失败码）。** 三条各给一个独立码，失败即以该码非零退出并在首行打印：`E-PW` = `NODE_PATH="$(npm root -g)" node -e "process.exit(require('playwright')._electron?0:1)"` 非零（全局 playwright 或 `_electron` 不可得）；`E-KEY` = `process.env.XIAOMI_API_KEY` 为空/未设（仅**兜底路径**需要，但仍在第 0 步先判 —— 复用路径失败时才有意义，故此项判负打印 **warning** 而不立即退出，等走到兜底路径再以 `E-KEY` 退出）；`E-DATA` = `~/Library/Application Support/realm-dev/agent-workspace/managed-skills/` 下不存在 `commit-style`，或 `aa-budget-01..14` 不足 14 个（这条**必须硬退出** —— 缺了它 `commit-style` 不会落入 `promptOmitted`，目标卡片必然不可得，此时任何「全绿」都是假绿）。第 0 步的逐条结果同时写入证据 JSON 的 `preflight` 字段。

1. `NODE_PATH="$(npm root -g)"` 下 `require('playwright')` 取 `_electron`（全局 `playwright@1.58.2`，已实测 `_electron` 存在）。用 `_electron.launch({ args: ['.'], env: { ...process.env, NODE_ENV: 'development' } })` 启动 **realm-dev**（独立 userData `~/Library/Application Support/realm-dev/`）。**不得**用 `pkill` / `pgrep` 模式匹配操作进程；收尾只用 `electronApp.close()` 然后 `process.exit(0)`。
2. 窗口选取：`electronApp.windows()` 里按 `w.url().includes('index.html')` 选出主窗口（非 index.html 的窗口一律忽略）。等待 `#aiPanel` 与 `#aiInput` 就绪；若面板 `hidden` 则先打开它（点击打开 AI 面板的入口，或用既有 `#aiPanel` 的显隐控制路径）。
3. **定位目标卡片（走真实路径，两种来源都允许，优先复用）**：
   - 优先：在已渲染的会话里查 `.tool-card-manage-note.tool-card-manage-note-limit`；若当前会话没有，遍历 `#aiConvList` 的会话逐个打开再查（持久化的 `manage_skill` 终态标记含 `promptIncluded`，重载链路会渲染同一条标注）。
   - 兜底（复用时找不到才走）：用**真实键盘输入**在 `#aiInput` 里发一条真实 LLM 请求，要求模型用 `manage_skill` 更新 `commit-style`（与 2026-09-13 那次成功驱动同形）。等待该 `manage_skill` 卡片出现且终态为「完成」+ 带 `.tool-card-manage-note-limit`。若两种来源都拿不到目标卡片，**以非零退出并在输出里写明**「目标卡片不可得」+ 已尝试的路径，不得跳过断言后报绿。
   - 断言前置：目标卡片的 `.tool-card-status` 文本为「完成」，`.slash-picker-source-badge` 文本为「托管」（即 `TIER_BADGE` 的 managed 档），`.tool-card-manage-note` 恰 1 个。
4. **真实拖拽 resize handle**：`#aiPanelResizeHandle` 上用 `page.mouse` 做真实 `down` / `move` / `up`。**硬性坑（本次实测踩过）**：handle 的坐标随面板变窄而移动，**每次拖拽前必须重新取 `boundingBox()`**；缓存上一轮坐标会让第二次拖拽静默失败（表现为宽度不变却无报错）。每次拖拽后读回 `#aiPanel` 的 `offsetWidth` 并断言它等于目标值（拖到 260px 时断言实得 280px，证明确实停在 `--ai-panel-min-width` 的下限上）。
5. 依序采集 520 / 420 / 280 / 260 四档，每一档记录：面板 `offsetWidth`、`.tool-card-header` 的 `offsetHeight` / `scrollHeight` / `clientHeight` / 外接矩形、`.tool-card-name.tool-card-name-skill` 的 `scrollWidth` / `clientWidth` / 外接矩形、`.tool-card-name-text` 的 `clientWidth` / `scrollWidth`、徽标与标注各自的 `scrollWidth` / `clientWidth` / 外接矩形 / `textContent` / `className`。全部写入 `/tmp/uat49/evidence-g49-3.json`（**不得覆盖** UAT 已引用的 `evidence3.json` 与 `uat49-t3-*.png`），并截 `/tmp/uat49/g49-3-card-280.png`（卡片元素级）、`/tmp/uat49/g49-3-window-280.png`（窗口级）、`/tmp/uat49/g49-3-card-520.png`（对照）。**每一轮**还要计算 `src/styles/main.css` 的**声明投影**（`require('fs').readFileSync(...).replace(/\/\*[\s\S]*?\*\//g, '')` —— 整体剔除注释后的文本）的 sha256，连同该轮序号写入证据 JSON 的 `cssDeclProjection`（形如 `[{round:'red', sha:'…'}, {round:520, sha:'…'}, …]`）。
6. 断言集（在 **280px 档**上判定；`0.5px` 容差）：
   - **A1**（判据 · 本次新增）：标注外接矩形右缘 ≤ 裁切祖先 `.tool-card-name.tool-card-name-skill` 外接矩形右缘 —— 即「标注未被祖先裁切」。**这是唯一能检出本 gap 的判据**：标注自身 `scrollWidth` / `clientWidth` 恒相等（裁切发生在祖先），旧驱动用它得出的「不裁切」是假绿。
   - **A2**（判据 · 本次新增）：该祖先的 `scrollWidth <= clientWidth`（容器自身不溢出）。
   - **A3**（判据 · 本次新增）：`.tool-card-name-text` 的 `clientWidth > 0`（技能名仍可见；允许且应当被省略号压缩）。
   - **A4**（既有通过项 · 防回归）：`.tool-card-header` 的 `offsetHeight === 36`，且 520px 与 280px 两档同值，且 `scrollHeight <= clientHeight`（单行不换行）。
   - **A5**（既有通过项 · 防回归）：徽标的 `scrollWidth <= clientWidth` 且右缘在上述祖先盒内。
   - **A6**（本次新增）：标注 `textContent` 为 `超预算`（3 字），`className` 含 `tool-card-manage-note-limit`。
   - **A7**：拖到 260px 后实得 280px（证明处于最小宽度，不是「还没拖到位」）。
   - **A8（条件性 · 有则判、无则如实记未覆盖）**：若会话历史里能定位到一张带 `.tool-card-manage-note-error` 的**真实**失败卡片，则对它一并判 A1 / A2。**不得**用合成 DOM 节点代替 —— 手工构造被测实现从不构造的对象正是本阶段记录过的假绿来源。定位不到时在证据 JSON 里写明 `errorNoteCovered: false` 与原因。
   - **A9（判据 · 本次新增 · 承担 `<done>` 的「CSS 声明零改动」）**：证据 JSON 的 `cssDeclProjection` 里**所有轮次的 sha 彼此相等**。红轮在任何源码改动之前跑（该轮 A1/A2/A3 为红即自证「确实是改源码之前」），故该相等等价于「本轮对 `src/styles/main.css` 只动了注释」。**单点变异自检**：给 `.tool-card-manage-note` 加一条 `max-width: 60px` → A9 必须转红；而 M14 / M15 / A1–A7 对它全绿（M14 只查基类五属性存在 + 无 padding/margin；M15 只查 300 字符窗口内无 overflow/text-overflow；A1–A7 按元素外接矩形与 `textContent` 判定，盒子窄于文本时字形画到盒外、矩形判据看不见）。
7. 全部断言通过 → 退出码 0；任一条失败 → 打印逐条布尔表 + 失败项的实测值，退出码非零。输出末尾打印面板四档实测宽度与 A1–A9 的逐条结果（含 `cssDeclProjection` 的全轮 sha），便于直接抄进 SUMMARY。
8. 可选但推荐：对 280px 档的头部元素截图做 4× 放大（`magick` 已确认存在于 `/opt/homebrew/bin/magick`），落 `/tmp/uat49/g49-3-zoom-280-header.png`，供人工目视复核标注尾部字符完整。

**第 2 步 · 在当前树上跑红（改源码之前）**

运行驱动，记录：A1 红（标注右缘越界，实测约 19px）、A2 红（祖先 `scrollWidth` 148 > `clientWidth` 129）、A3 红（名称文本 `clientWidth` = 0），A4 / A5 绿。把这一轮输出原样保存到 `/tmp/uat49/g49-3-red.log`。**若这一轮不是这个形态（例如 A1 已经为绿），说明驱动有问题（判据取错对象或拖拽没到位），必须先修驱动，不得继续改源码** —— 这是防「断言恒真」的自校验。

**第 3 步 · 改内容单源（`src/skill-picker-model.js`）**

在 `STATUS_TEXT` 定义之后新增一个模块内常量并在尾部 `api` 对象导出：`PROMPT_OMITTED_CARD_NOTE`，其值**必须逐字写成** `STATUS_TEXT.promptOmitted.split(' · ').pop()`（分隔符字面量直接写在这条表达式里，**不要另抽常量、不要拆行、不要写 `?.[1]` 之类的等价变体** —— 判据是源码正则 `/PROMPT_OMITTED_CARD_NOTE\s*=\s*STATUS_TEXT\.promptOmitted\.split\(/`，写歪即假红）。分隔符实测 = 空格 + U+00B7 + 空格，故得 `超预算`。**不得**写第二份字面量 —— 这条由源码级判据机械保证（见 Task 2 的断言 2），且**该文件里连注释都不得出现 `'超预算'` / `"超预算"` 这种独立字面量**（否则判据转红）；JSDoc 里要提这个投影值时，用常量名或「第二段投影」指代，不要把它写成带引号的字面量。上方 JSDoc 必须写明四件事：① 它是 `STATUS_TEXT.promptOmitted` 第二段的**机械投影**，不是第二份文案；② 卡片头部为什么要短形态而面板保留长形态（面板行可换行、宽 ≥ 280px 且无结果区；卡片在 280px 面板下 `.tool-card-name` 仅 129px，且结果区已承载完整句）；③ 「不新写」的可核对面**不是** `includes`（写死第二份字面量也会通过），而是源码级两条：取值必须以 `STATUS_TEXT.promptOmitted.split(' · ').pop()` 的**引用形式**书写（沿用 `MANAGE_SKILL_SHORT_REASON.limit_exceeded: STATUS_TEXT.overLimit` 的既有先例），且本文件中不存在被引号包裹的「超预算」独立字面量。**写这条 JSDoc 时务必用常量名或「第二段投影」指代那个值，不要把它写成带引号的字面量 —— 否则你为了说明纪律而写下的字面量本身就会让判据 ② 转红。** 　④ E1 的 ≤ 4 字约束与该投影的字符数（3）。
**逐字不动**：`STATUS_TEXT` 的 `shadowed` / `nameClash` / `promptOmitted` / `overLimit` 四个值，以及 `MANAGE_SKILL_SHORT_REASON` 的九个值。

**第 4 步 · 改渲染端取值（`src/renderer.js`）**

`renderToolCard` 的 `manage_skill` 分支里，成功且 `promptIncluded === false` 且 `action !== 'delete'` 那条路径的 `noteText` 改取 `window.SkillPickerModel.PROMPT_OMITTED_CARD_NOTE`。同步该处注释：说明卡片标注取的是**单源的 ≤ 4 字投影**（48 的面板串逐字未变），并保留既有两行注释里「至多一个标注 / 互斥 / 取不到则不渲染元素」的语义。**挂载结构零改动**：仍是同一个 `createElement` + `textContent` + 同一个 `appendChild(noteEl)` 挂载点（既有 M10 断言要求恰 1 处）；不改 `tool-card-manage-note-limit` 类名；不引入 `innerHTML` / 属性插值。

**第 5 步 · 改 CSS 注释（`src/styles/main.css`）**

只更新 `.tool-card-manage-note` **上方**的注释块（必须在规则之前 —— 既有 M14 取 `+400` 字符、M15 取 `+300` 字符的切片窗口落在规则之后，注释写在规则前不会扰动这两条断言）。**一条硬性写法要求**：注释里提到该类名时**绝不能带尾随的 ` {`** —— M14 / M15 用 `css.indexOf('.tool-card-manage-note {')` 定位，注释里出现同样的「类名 + 空格 + `{`」会让 `indexOf` 命中**注释**而不是规则本身，两条断言会取错切片（M14 的 `+400` 窗口还会因命中注释而把注释里的 `padding` / `margin` 字样当成声明，直接假红）。注释写在规则**之前**，故其词句不受 M14 / M15 的 `+400` / `+300` 窗口约束 —— 但要留神别把规则「挪」进窗口（改声明由 A9 与 `css-decl-freeze` 拦住）。注释要写出实际预算算术，且**实测值与估算值必须区分标注**：

- **实测（来自 `49-UAT.md` G-49-3 的几何表与本次证据 JSON）**：面板 280 → 卡片 210 → `.tool-card-name` 129（clientWidth）；徽标 32；修复前复用串 100 ⇒ 8 + 32 + 8 + 100 = 148 > 129，右端溢出 19px。
- **估算（必须带 `≈` 与「字体度量估算，本轮未逐一实测」字样）**：11px CJK 约 11px/字 ⇒ 卡片短形态 3 字 ≈ 33px，预算 8 + 32 + 8 + 33 ≈ 81 ≤ 129，技能名余量 ≈ 48px；九码表最宽 6 字 ≈ 66px ⇒ 8 + 32 + 8 + 66 = 114 ≤ 129，技能名余量 ≈ 15px。
- 同一句里不得把估算与实测并列成同一种口径 —— 「129（实测）」与「33（估算）」必须各自带自己的标签。

同时写明「声明本身不动：标注仍是 `flex-shrink: 0` + `white-space: nowrap` 永不裁切；`.tool-card-name-skill` 的 `overflow` 亦不改 —— 不存在让标注不裁切又让技能名宽度 > 0 的第二解」。**不得改动任何声明**（判据 A9 会转红）。

**第 6 步 · 重跑驱动得绿**

再次运行驱动（本轮应走复用路径更快），把输出保存到 `/tmp/uat49/g49-3-green.log`。断言全绿 + 退出码 0 才算完成。若 A1 / A2 / A3 仍红，逐条核对：标注文本是否为 `超预算`、渲染端是否真的改到了新常量、拖拽是否真的到达 280px。
  </action>
  <verify>
    <automated>NODE_PATH="$(npm root -g)" node tests/uat-49-g49-3-panel-layout.js</automated>
    <automated>node -e '
const fs=require("fs");
const css=fs.readFileSync("src/styles/main.css","utf8").replace(/\/\*[\s\S]*?\*\//g,"");
const props=(sel)=>{const i=css.indexOf(sel+" {");if(i<0)throw new Error("缺规则 "+sel);const b=css.slice(i+sel.length+2,css.indexOf("}",i));const m=b.match(/(^|;)\s*(width|max-width|min-width|flex-shrink|flex-grow|flex-basis|display|align-items|gap|overflow|text-overflow|white-space|padding|margin|font-size|font-weight|line-height|font-family|color|background|border)\s*:/g)||[];return m.map(x=>x.replace(/^;?\s*/,"").replace(/\s*:$/,"")).sort().join(",");};
const note=props(".tool-card-manage-note");
if(note!=="flex-shrink,font-size,font-weight,line-height,white-space") throw new Error("标注基类声明集被改动: "+note);
if(props(".tool-card-manage-note-error")!=="color") throw new Error("标注 -error 声明集被改动（只允许 color 一条）");
if(props(".tool-card-manage-note-limit")!=="color") throw new Error("标注 -limit 声明集被改动（只允许 color 一条）");
if(!/tool-card-manage-note-error\s*\{\s*color:\s*var\(--skill-error-text\)/.test(css)) throw new Error("-error 取值被改动");
if(!/tool-card-manage-note-limit\s*\{\s*color:\s*var\(--skill-limit-text\)/.test(css)) throw new Error("-limit 取值被改动");
const nt=props(".tool-card-name-text");
if(nt!=="font-family,min-width,overflow,text-overflow,white-space") throw new Error("唯一压缩承担者的声明集被改动（多加 flex-shrink / max-width 都会让技能名不再退化为省略号）: "+nt);
console.log("css-decl-freeze ok · note=flex-shrink,font-size,font-weight,line-height,white-space");
'</automated>
    <automated>node --test tests/test-skill-picker-model.js</automated>
    <automated>node tests/test-ai-skills.js</automated>
  </verify>
  <done>
驱动脚本存在且可重跑；第 0 步的前置自检结果落在证据 JSON 的 `preflight` 里（三条各带 `E-PW` / `E-KEY` / `E-DATA` 码，`E-DATA` 判负即硬退出）；`/tmp/uat49/g49-3-red.log` 记录修复前 A1 / A2 / A3 三条为红（A1 越界约 19px、A2 148 > 129、A3 名称宽度 0）、A4 / A5 为绿；`/tmp/uat49/g49-3-green.log` 记录修复后 A1–A9 全绿且退出码 0；`/tmp/uat49/evidence-g49-3.json` 含四档完整几何量 + 全轮 `cssDeclProjection` sha + `preflight`，且 `UAT` 既引用的 `evidence3.json` / `uat49-t3-*.png` 未被覆盖；`PROMPT_OMITTED_CARD_NOTE` 存在且渲染为 `超预算`，`STATUS_TEXT` 四值与 `MANAGE_SKILL_SHORT_REASON` 九值逐字未变；**`src/styles/main.css` 的声明零改动 —— 由 A9（全轮声明投影 sha 相等）与 `css-decl-freeze` 门禁双重判据承担，两者都能被 `max-width: 60px` 这类添加转红**；`main.css` 注释里实测值照写、字体度量估算带 `≈` 与「估算」标注；驱动收尾用 `electronApp.close()` + `process.exit(0)`，全程未对任何进程做 `pkill` 类操作。
  </done>
</task>

<task type="auto">
  <name>Task 2: 纯逻辑与源码面护栏 —— 派生投影的值域 / 长度上限 / 48 原文冻结 / 渲染端取值来源</name>
  <files>tests/test-skill-picker-model.js, tests/test-ai-skills.js</files>
  <action>
本任务只加护栏，不改生产代码。护栏的作用是让「未来有人把标注加长 / 把面板串顺手统一 / 在渲染端另写一份字面量」这三类退化在纯 Node 下秒级转红 —— 它们是 280px 布局的前提条件，**不替代** Task 1 的真实渲染判据。测试文件里出现的文案字面量属于断言期望值（不是渲染端硬编码），既有守卫（`tests/test-skill-picker-model.js` 里「四条行尾状态文案只在本模块定义」那条：`skill-picker-model.js` 中该串恰出现一次、`renderer.js` 中不得出现）继续为真、不得放宽。

**在 `tests/test-skill-picker-model.js` 的 manage_skill 卡片相关 describe 内新增用例（node:test，断言用 `node:assert`）：**

1. **值域（注意：这不是「不新写」的证明）** —— 写死第二份字面量 `PROMPT_OMITTED_CARD_NOTE = '超预算'` 会**同时满足**下面三条，所以它们只能作为值域护栏：断言 `model.PROMPT_OMITTED_CARD_NOTE` 是字符串；断言 `model.STATUS_TEXT.promptOmitted.includes(model.PROMPT_OMITTED_CARD_NOTE)` 为 `true`；断言 `model.STATUS_TEXT.promptOmitted.split(' · ').pop() === model.PROMPT_OMITTED_CARD_NOTE`。
2. **「不新写」的机械保证（本次修订新增，W3 的收口）**：读 `src/skill-picker-model.js` 源码文本，断言**两条同时成立** ——
   (a) `/PROMPT_OMITTED_CARD_NOTE\s*=\s*STATUS_TEXT\.promptOmitted\.split\(/` 命中：取值必须以**引用 + 投影的形式**书写。失败消息写明「短形态的唯一合法书写是 `STATUS_TEXT.promptOmitted.split(' · ').pop()` 的引用形式」。形态**沿用本仓既有先例**（`tests/test-skill-picker-model.js:984` 对 `MANAGE_SKILL_SHORT_REASON.limit_exceeded` 的 `/limit_exceeded:\s*STATUS_TEXT\.overLimit/`，那条失败消息写着「必须写成引用形式」），**不另创第二套**。
   (b) `/['"]超预算['"]/` 零命中：文件里不存在第二个独立的 `超预算` 字面量（**注释里也不许写**）。这一条同时封掉「声明处写成引用、导出处写死一份」的形态。
3. E1 的 ≤ 4 字收口：断言 `model.PROMPT_OMITTED_CARD_NOTE.length <= 4`（实测 3），并在断言消息里写明这是 `49-UI-SPEC.md` 的 E1 overflow 处置与 280px 宽度预算的前提。
4. 48 D-12 原文冻结（防「顺手统一」）：断言 `model.STATUS_TEXT.promptOmitted === '未进提示词 · 超预算'`，断言消息写明这是 `48-CONTEXT.md` D-12 的原文与 `48-UI-SPEC.md` 的「照写不统一」契约、本阶段不得改。
5. `STATUS_TEXT` 另三键契约冻结：逐字断言 `shadowed` / `nameClash` / `overLimit` 三个值（分别为既有三条文案），断言消息写明本轮只新增派生键、不动这三键取值域。
6. 九码短原因的长度上限：遍历 `MANAGE_SKILL_SHORT_REASON`，断言每个值都是非空字符串且 `length <= 6`；并断言 `MANAGE_SKILL_SHORT_REASON.limit_exceeded === model.STATUS_TEXT.overLimit`（既有「同值必须引用同一常量」纪律的机械保证）。断言消息写明：卡片头部 129px 预算下，最宽 6 字**估算**约 66px 仍成立，任何加长都会在这里被拦下。

**在 `tests/test-ai-skills.js` 的 M10 用例里做精确替换：** 把 `branch.includes('STATUS_TEXT.promptOmitted')` 这条断言改为断言 manage_skill 分支含新常量名（`PROMPT_OMITTED_CARD_NOTE`），并把该断言的失败消息改为说明「卡片头部超预算标注取单源的 ≤ 4 字投影（48 的面板串逐字未变，见 49-UI-SPEC 的 E1 收口）」。该用例其余断言（`MANAGE_SKILL_SHORT_REASON[` 取值来源、两个标注类存在、`appendChild(noteEl)` 恰 1 处、`if (noteText)` 不渲染空元素）一字不动。

**新增用例数按实测记录**（供 Task 3 回填账本）：跑完本节后记录两个套件各自的 `# tests` 实测值。
  </action>
  <verify>
    <automated>node --test tests/test-skill-picker-model.js</automated>
    <automated>node tests/test-ai-skills.js</automated>
    <automated>node -e "const fs=require('fs');const m=require('./src/skill-picker-model.js');const s=m.STATUS_TEXT.promptOmitted,n=m.PROMPT_OMITTED_CARD_NOTE,src=fs.readFileSync('src/skill-picker-model.js','utf8');if(typeof n!=='string') throw new Error('PROMPT_OMITTED_CARD_NOTE 未导出');if(n.length>4) throw new Error('标注超 4 字: '+n.length);if(!s.includes(n)) throw new Error('短形态不是长串的投影');if(s.split(' · ').pop()!==n) throw new Error('短形态不是该串的第二段');if(s!=='未进提示词 · 超预算') throw new Error('48 D-12 原文被改动');if(!/PROMPT_OMITTED_CARD_NOTE\s*=\s*STATUS_TEXT\.promptOmitted\.split\(/.test(src)) throw new Error('PROMPT_OMITTED_CARD_NOTE 必须以引用形式书写：PROMPT_OMITTED_CARD_NOTE = STATUS_TEXT.promptOmitted.split(...)');if(/['\"]超预算['\"]/.test(src)) throw new Error('源码中出现第二份 超预算 独立字面量（含注释）');const r=m.MANAGE_SKILL_SHORT_REASON;for(const k of Object.keys(r)){if(typeof r[k]!=='string'||r[k].length>6) throw new Error('短原因越界: '+k+'='+r[k]);}console.log('projection-ok',JSON.stringify(n),'len='+n.length);"</automated>
  </verify>
  <done>
两个套件全绿且退出码 0；`test-skill-picker-model.js` 新增 6 组断言（投影**值域**、**源码级「不新写」两条**（引用形式正则 + 零第二份字面量）、≤ 4 字、48 原文冻结、三键冻结、九码长度上限）；`test-ai-skills.js` 的 M10 取值来源断言改指新常量且其余断言未动；两个套件的实测 `# tests` 已记录待 Task 3 回填；`src/renderer.js` 仍不含面板长串字面量（既有守卫仍绿）；**已做单点变异自检**：把 `PROMPT_OMITTED_CARD_NOTE` 改成写死的 `'超预算'` 必须让源码级两条断言转红（实测已确认）。
  </done>
</task>

<task type="auto">
  <name>Task 3: 文档与账本收口 —— 卡片标注形态说明、八个例数单元按实测刷新、UI-SPEC 的 E1 收口记录</name>
  <files>docs/product/ai-skills.md, AGENTS.md, .planning/phases/49-manage-skill-ai/49-UI-SPEC.md</files>
  <action>
本任务不改任何源码与测试，只收口文档面。三件事，缺一即为文档与现实背离。

**① 产品文档的卡片形态说明（`docs/product/ai-skills.md`）**

- §11.7「可见性与时序」里描述卡片头部那条（现文大意：头部给出来源档位徽标与至多一条标注 —— 失败短原因，或「未进提示词」）改为写明**两种标注的实际形态**：失败态为九码闭合白名单的短原因（≤ 6 字）；成功且 `promptIncluded === false` 时是 `超预算` —— 由 `STATUS_TEXT.promptOmitted` 第二段**机械投影**出的 **≤ 4 字**短形态（常量名写全：`PROMPT_OMITTED_CARD_NOTE`），而 `/` 面板行尾仍用完整串（48 D-12 原文，两处共用同一单源）。补一句边界说明：卡片头部短形态之所以短，是因为 280px 面板下 `.tool-card-name` 仅 **129**px，而完整的两段式会越界被祖先裁切；卡片结果区（`该技能暂未进入模型提示词（技能段预算已满），仍可用 /skill:{name} 手动调用。`）已承载完整语义，故头部标注是**可扫读的短标签**而非唯一信息载体；面板没有结果区，因此保留长文案。
  **本条必须逐字带上四个可判别片段**（它们是判据锚点）：`PROMPT_OMITTED_CARD_NOTE`、`超预算`、`机械投影`、`≤ 4 字`（正则 `≤\s*4\s*字`）与 `129`。
- 同段补一条**诚实边界**：**失败态标注（≤ 6 字）的宽度在 280px 下由算术保证（8 + 32 + 8 + 66 ≈ 114 ≤ 129，其中 66 为 6 字 × 11px 的**字体度量估算**），本轮未用真实渲染逐条覆盖它** —— 真实渲染门禁只覆盖超预算那条（即本次 UAT 失败的形态）；这是如实标注而非声称已全量覆盖。**本条必须逐字含 `未用真实渲染`**（判据锚点）。
- §11.8 的测试清单句里，给 `tests/test-skill-picker-model.js` 的覆盖描述补上本轮新增的面，**逐字含三组词**：`≤ 4 字投影` / `48 原文冻结` / `九码长度上限`（判据锚点）。
- **不得改动 48 号命名空间的挂账句**（`TD-48-01` / `TD-48-02` / `WR-01` / `WR-02` / `WR-06` 五项，一律是 `48-REVIEW.md` 编号），也不得把 `49-REVIEW.md` 的 `WR-03` / `WR-04` / `IN-01`–`IN-06` 写成已闭合（本轮只闭合 `CR-01` / `CR-02` / `CR-03` / `WR-01` / `WR-02`，加本轮的 `G-49-3`）。

**② 例数账本（`AGENTS.md` 测试行 + `docs/product/ai-skills.md` §七两条 + §11.8 三条 = 八个单元）**

按 49-06 的既有纪律，**一律取实测值**：先跑 `tests/test-manage-skill.js`、`tests/test-ai-skills.js`、`tests/test-skill-picker-model.js` 三个套件并读出各自的 `# tests`，再把八个账本单元里的数字改成实测值（本计划会改动其中两个套件的例数，第三个应保持不变 —— 若它变了，说明 Task 2 误伤，先查清再回填）。**不得硬编码预测值**。

**③ UI-SPEC 的 E1 收口记录（`.planning/phases/49-manage-skill-ai/49-UI-SPEC.md`）**

- **改动面（本次修订扩大）。** 原计划的「只改文案表那一格、UI-SPEC 其余行零改动」**过窄**：同一契约另有六处直接陈述卡片头部标注**渲染什么**，收口后会变成假陈述。按下述三类同步，其余位置零改动：

  **(A) 陈述卡片头部标注「渲染什么」的七处** —— 一律只写收口后形态 `` `超预算` ``（+ `-limit`），并注明它由 `/` 面板的 `STATUS_TEXT.promptOmitted` 第二段**机械投影**而来。**这七行内不得再出现长串 `未进提示词 · 超预算` 的字面量** —— 要指面板那条时用常量名 `STATUS_TEXT.promptOmitted` 指代（这既是判据要求，也顺带贯彻「不重打一遍单源字符串」的纪律）：
  1. Typography 的 `` - **Label 11px / 400** `` 行；
  2. Copywriting 九条原因码的「纪律」那条（`` - 短原因**一律不含技能名 `` 开头 —— 原文把长串写成头部短原因的**唯一更长例外**，收口后卡片域最宽只是 6 字）；
  3. 「全部用户可见文案（唯一权威清单）」里 `` | 头部「未进提示词」标注 | `` 那一行（位置列可保留原叫法；「文案」列改 `超预算`，「来源」列保留「复用 48 的 `STATUS_TEXT.promptOmitted`」并写明「取该单源的 **≤ 4 字第二段投影**（`PROMPT_OMITTED_CARD_NOTE`），48 的面板串逐字未变」）；
  4. 卡片结构契约的「头部三个新增/复用元素」表第 3 项（`` | 3 | 内联标注 | ``）；
  5. 交互契约的 `` | 终态（成功） | `` 行；
  6. UI Considerations 的 `` | E1 | overflow | `` 行（含其 `{ statement: … }` 里的「最宽标注」括注 —— 改为「最宽为 6 字失败短原因；超预算形态 `超预算` 3 字」；`{ statement / verification }` 的结构与该行末尾的处置句保留）；
  7. UI Considerations 的 `` | E1 | zero-one-many | `` 行。

  **(B) 正文其余提到「未进提示词」的行一律要标明所属表面** —— 不得被读作卡片渲染文案；**每一行都要显式写出它属于 `/` 面板标注（或作为投影来源）**。落点：Color 表的 `` | Warning | `` 行、对比度核算表的 `` | 「未进提示词」标注 `` 行、CSS 规格代码块里那一行注释（`/* AI 自建技能卡片（Phase 49）：头部内联标注（…）`）、决策来源表末行。此条同时写成契约级纪律（见下 (C) ⑤）。

  **(C) 在文件末尾追加** `## Backstop 收口记录（E1 overflow）` 一节，写清五件事：① 失败事实（2026-09-13 自动化实测：280px 面板 → 卡片 210px → `.tool-card-name` 129px < 不可压缩簇 148px，标注右端 19px 被祖先 `overflow: hidden` 裁掉，且唯一可压缩项技能名被压到 0）；② 收口的**是什么**（E1 的 ≤ 4 字处置照做，落点是卡片头部的超预算标注）；③ 收口的**不是**什么（`STATUS_TEXT.promptOmitted` 逐字未变 —— 依 48-CONTEXT D-12 原文 / 48-UI-SPEC「照写不统一」/ `test-skill-picker-model.js` 的用例名与断言 / 产品文档 §10.1 四条定长文案；面板行可换行且宽 ≥ 280px，缩短对面板零收益且会丢「未进提示词」半边语义）；④ 排除的两条路线（**不**改成换行头部 —— `.tool-card-header` 恒 36px 单行是硬不变式；**不**加 system-note —— D-02 已明确否决），故 E1 的两条禁令一字未破；⑤ **把「凡提到「未进提示词」必须标明所属表面」写成契约纪律**（即上面 (B)），使后续改动无法再把长串悄悄读作卡片文案；⑥ 真实渲染证据的位置（驱动脚本路径 + 红绿两轮日志 + 证据 JSON + 截图路径）。

- 该节为**追加**；正文的改动仅落在 (A) / (B) 枚举的位置 —— 二者由下方 `<verify>` 的「七锚点 + 表面规则」机械覆盖，不靠人工核对。
  </action>
  <verify>
    <automated>node -e '
const cp=require("child_process"),fs=require("fs");
const suites=["tests/test-manage-skill.js","tests/test-ai-skills.js","tests/test-skill-picker-model.js"];
const meas={};
for(const f of suites){const o=cp.execSync("node "+(f.indexOf("picker")>=0?"--test ":"")+f,{encoding:"utf8",stdio:["ignore","pipe","ignore"]});meas[f.split("/").pop()]=String(o.match(/# tests (\d+)/)[1]);}
const FN=/test-[\w-]+\.js/g;let bad=[],cells=0;
for(const file of ["AGENTS.md","docs/product/ai-skills.md"]){const hit={};fs.readFileSync(file,"utf8").split("\n").forEach((line,ln)=>{const idx=[];FN.lastIndex=0;let m;while((m=FN.exec(line)))idx.push([m.index,m[0]]);for(let i=0;i<idx.length;i++){const base=idx[i][1];if(!(base in meas))continue;const end=i+1<idx.length?idx[i+1][0]:line.length;const uniq=[...new Set([...line.slice(idx[i][0],end).matchAll(/(\d+)\s*例/g)].map(x=>x[1]))];if(!uniq.length)continue;cells++;hit[base]=(hit[base]||0)+1;if(uniq.length!==1||uniq[0]!==meas[base])bad.push(file+":"+(ln+1)+" "+base+" 账本单元取到 ["+uniq.join("/")+"] ≠ 实测 "+meas[base]);}});for(const base of Object.keys(meas))if(!hit[base])bad.push(file+" 的账本未覆盖 "+base);}
if(cells<8)bad.push("账本单元数 "+cells+" < 8（§七 两条 + §11.8 三条 + AGENTS.md 测试行三条）");
if(bad.length)throw new Error("例数不一致: "+bad.join("; "));
console.log("counts-parity ok");console.log("cells="+cells+" measured="+JSON.stringify(meas));
'</automated>
    <automated>node -e '
const fs=require("fs");
const s=fs.readFileSync(".planning/phases/49-manage-skill-ai/49-UI-SPEC.md","utf8");
const H="## Backstop 收口记录（E1 overflow）";
const i=s.indexOf(H);
if(i<0) throw new Error("缺 E1 收口记录节");
const body=s.slice(0,i).split("\n"), tail=s.slice(i);
const LONG="未进提示词 · 超预算";
const stray=body.map((l,n)=>[n+1,l]).filter(([n,l])=>l.includes("未进提示词") && !/面板|投影|来源/.test(l));
if(stray.length) throw new Error("以下行提到「未进提示词」却未标明所属表面（须显式写明 / 面板 或 投影来源）: "+stray.map(([n,l])=>n+":"+l.trim().slice(0,48)).join(" | "));
const anchors=[["- **Label 11px / 400**","Typography Label 行"],["- 短原因**一律不含技能名","Copywriting 九码纪律那条"],["| 头部「未进提示词」标注 |","唯一权威清单该行"],["| 3 | 内联标注 |","卡片结构契约第 3 项"],["| 终态（成功） |","交互契约终态（成功）行"],["| E1 | overflow |","UI Considerations E1 overflow 行"],["| E1 | zero-one-many |","UI Considerations E1 zero-one-many 行"]];
for(const [a,label] of anchors){const line=body.find(l=>l.startsWith(a));if(!line) throw new Error("缺锚点行: "+label);if(line.includes(LONG)) throw new Error(label+" 仍把长串写成卡片渲染文案（应只写 超预算，并以常量名指代面板那条）");if(!line.includes("超预算")) throw new Error(label+" 未改为收口后形态（缺 超预算）");}
for(const k of ["PROMPT_OMITTED_CARD_NOTE","超预算","19px","system-note","换行头部"]) if(!tail.includes(k)) throw new Error("收口记录缺要素: "+k);
console.log("ui-spec-copy-parity ok · 七锚点已收口 · 表面规则零越界行 · 收口记录要素齐");
'</automated>
    <automated>node -e '
const fs=require("fs");
const t=fs.readFileSync("docs/product/ai-skills.md","utf8");
const i=t.indexOf("### 11.7 "), j=t.indexOf("### 11.8 ");
if(i<0||j<0||j<i) throw new Error("缺 §11.7 / §11.8 锚点");
const s711=t.slice(i,j), s118=t.slice(j);
for(const k of ["PROMPT_OMITTED_CARD_NOTE","超预算","机械投影","未用真实渲染"]) if(!s711.includes(k)) throw new Error("§11.7 缺本轮新写要素: "+k);
if(!/≤\s*4\s*字/.test(s711)) throw new Error("§11.7 未写明 E1 的 ≤ 4 字收口");
if(!s711.includes("129")) throw new Error("§11.7 未写明 280px 下的宽度预算算术（129px 容器）");
for(const k of ["≤ 4 字投影","48 原文冻结","九码长度上限"]) if(!s118.includes(k)) throw new Error("§11.8 覆盖描述未补本轮新增面: "+k);
for(const k of ["TD-48-01","TD-48-02","WR-06","WR-03","WR-04","IN-01","IN-02","IN-03","IN-04","IN-05","IN-06"]) if(!t.includes(k)) throw new Error("挂账项被移出本文档: "+k);
console.log("docs-ok · §11.7 本轮要素齐（机械投影 / 超预算 / 未用真实渲染 / ≤ 4 字 / 129px）· §11.8 已补 · 48+49 号挂账未移出");
'</automated>
  </verify>
  <done>
§11.8 既有的可重跑一致性命令实跑通过（`counts-parity ok`，`cells >= 8`，三个套件的账本值与实测 `# tests` 逐字一致 —— 基线实跑值为 55 / 177 / 105，本轮 Task 2 改动后按新实测回填）；`49-UI-SPEC.md` 中**七处陈述卡片渲染文案的位置**全部只写 `超预算`（长串零出现），正文其余提到「未进提示词」的行全部自带 `/` 面板或投影来源标注（`ui-spec-copy-parity ok`），末尾追加的「Backstop 收口记录（E1 overflow）」含六要素（含把 (B) 的表面纪律立为契约条款）；`docs/product/ai-skills.md` §11.7 逐字含 `PROMPT_OMITTED_CARD_NOTE` / `超预算` / `机械投影` / `≤ 4 字` / `未用真实渲染` / `129`，§11.8 逐字含 `≤ 4 字投影` / `48 原文冻结` / `九码长度上限`（`docs-ok`）；48 号命名空间的五条挂账与 49 号 `WR-03` / `WR-04` / `IN-01`–`IN-06` 一字未移出（由同一条门禁机械断言）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内容单源 → 渲染端 | 卡片头部标注的取值必须来自 `src/skill-picker-model.js` 的单源投影，渲染端不得另写一份字面量（48 D-12 的「逐字复用」与 49 的「不新写」同时挂在这一点上） |
| CSS 声明 → 真实布局 | 「声明存在」与「最小宽度下成立」是两件事 —— 本阶段的假绿教训正出在这条边界上 |
| 驱动脚本 → 用户运行中的生产实例 | 驱动必须只操作自己 `_electron.launch` 拉起的 dev 子进程；用户当时正在运行正式版 `/Applications/Realm.app` |
| 驱动脚本 → realm-dev 数据 | 真实 LLM 往返与面板宽度持久化会写 realm-dev；`managed-skills/` 下的预算占位技能是本 gap 的可复现前提 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-07-01 | Tampering | 进程管理：驱动收尾 | high | mitigate | 只用 `electronApp.close()` + `process.exit(0)`；**严禁** `pkill` / `pgrep` 模式匹配杀进程（用户正在运行生产版实例）。驱动开头可记录 `pgrep -f '/Applications/Realm.app'` 的 PID 基线用于自证未误杀，但**不据此杀任何进程** |
| T-49-07-02 | Tampering | 文档漂移：卡片标注 vs 面板标注（**权威契约的每一处位置**） | high | mitigate | 短形态由 `STATUS_TEXT.promptOmitted.split(' · ').pop()` **机械派生**，并由**源码级两条**钉住（引用形式正则 + 零第二份 `'超预算'` 字面量）+ 值域三条（`length <= 4` / 投影性 / 48 原文冻结）；`49-UI-SPEC.md` 的**七处**陈述卡片渲染文案的位置由 `ui-spec-copy-parity` 逐锚点断言（长串零出现 + `超预算` 必在），正文其余提到「未进提示词」的行由「表面规则」要求显式标明 `/` 面板或投影来源；`docs/product/ai-skills.md` §11.7/§11.8 由 `docs-ok` 锚在**本轮新写**的可判别片段上（`机械投影` / `未用真实渲染` / `≤ 4 字投影` / `48 原文冻结` / `九码长度上限` —— 均为当前零命中的新词，杜绝恒真）；渲染端含新常量名且不含面板长串（既有跨文件守卫 + M10） |
| T-49-07-03 | Repudiation | 假绿判据 | high | mitigate | 判据对象改为**标注右缘 vs 裁切祖先右缘**（旧驱动的标注自身 `scrollWidth` / `clientWidth` 恒相等故无检出力）；执行顺序强制「先红后绿」两轮证据落盘；A1/A2/A3 三条都必须先红 —— 若红轮没红则先修驱动、不得改源码；**「`main.css` 声明零改动」这条 `<done>` 约束由 A9（全轮声明投影 sha 相等）+ `css-decl-freeze`（四个规则块的声明集逐字比对）双判据承担**，两者都已用单点变异实测（`max-width: 60px` ⇒ 双红）对 M14 / M15 / A1–A7 的盲区 |
| T-49-07-04 | Tampering | realm-dev 的可复现前提被清掉 | medium | mitigate | `managed-skills/` 下的 `commit-style` 与 14 个 `aa-budget-*` 一律不删；驱动只做读取与「让模型更新 `commit-style` 的 description」，不清理技能目录 |
| T-49-07-05 | Information Disclosure | 真实 LLM 往返的载荷 | low | accept | 载荷是「用 `manage_skill` 更新 `commit-style` 的 description」这一句测试指令，不含用户隐私；API key 只经环境变量透传，不落盘、不打印 |
| T-49-07-06 | Tampering | 渲染端注入面（TD-48-01 面） | low | mitigate | 本轮不新增任何字符串注入路径：标注仍是 `textContent` + 白名单类名，挂载点与元素个数不变；`createElement` 挂载点仍恰 1 处（M10 既有断言） |
| T-49-07-07 | Denial of Service | 驱动被「再按一次 Cmd+Q」拦截挂死 | low | mitigate | 既有范式：收尾 `process.exit(0)` 强制退出，不依赖 app 自身的退出对话框 |
| T-49-07-SC | Tampering | npm / pip / cargo 安装 | n/a | accept | 本计划不新增任何依赖（playwright 走全局已装版本、`magick` 走已装 CLI），无包管理器安装面，无需 legitimacy gate |
</threat_model>

<verification>
1. **真实渲染门禁（本 gap 的唯一有效证据）**：`NODE_PATH="$(npm root -g)" node tests/uat-49-g49-3-panel-layout.js` 退出码 0；第 0 步前置自检结果落在 `evidence-g49-3.json#preflight`（三条各带 `E-PW` / `E-KEY` / `E-DATA` 码）；红轮日志 `/tmp/uat49/g49-3-red.log` 记录 A1 / A2 / A3 为红、绿轮日志 `/tmp/uat49/g49-3-green.log` 记录 A1–A9 全绿；`evidence-g49-3.json` 含 520 / 420 / 280 / 260 四档几何量。**A9 = 全轮 `cssDeclProjection` sha 相等**，与 `<verify>` 的 `css-decl-freeze` 门禁共同承担「`main.css` 声明零改动」—— 两者都能被 `max-width: 60px` 这类添加转红（`max-width: 60px` 恰好违反 UI-SPEC「徽标与内联标注……永不截断、永不让位」的不变式，而 M14 / M15 / A1–A7 对它全部保持绿）。
2. **算术可核对性（以证据 JSON 的实测值为准；估算另标）**：280px 档实测 —— 面板 280 / 卡片 210 / `.tool-card-name` clientWidth 129 / 徽标 32 / **标注宽 W（取证据 JSON 实测，不取注释里的估算）**；断言 `W + 8 + 32 + 8 <= 129` 且 `.tool-card-name-text` 的 `clientWidth > 0`。注释与文档里的「3 字 ≈ 33px」（11px CJK 约 11px/字的**字体度量估算**）与由它推出的「技能名 ≈ 48px / 九码最宽 ≈ 15px」一律带 `≈` 与「估算」字样、**不参与判定**；A1–A3 **不依赖**该估算 —— 唯一承重事实是「3 字 < 81px」（即只需 < 2.45em/字，任何合理字体度量都成立）。任一项与证据 JSON 不符即为失败。
3. **单源纪律**：`PROMPT_OMITTED_CARD_NOTE` 为 `STATUS_TEXT.promptOmitted` 第二段的机械投影，且**源码级可核**（引用形式正则命中 + 文件内零独立 `'超预算'` 字面量，见第 7 条）；`STATUS_TEXT` 四个值、`MANAGE_SKILL_SHORT_REASON` 九个值逐字未变；`src/renderer.js` 不含面板长串字面量。
4. **既有套件无回归**：`node tests/test-manage-skill.js`、`node tests/test-ai-skills.js`、`node --test tests/test-skill-picker-model.js` 三者全绿。
5. **账本一致**：`docs/product/ai-skills.md` §11.8 的可重跑一致性命令实跑通过（`counts-parity ok`，`cells >= 8`）。
6. **文档自洽（逐位置，不是抽查）**：`49-UI-SPEC.md` 中**七处**陈述卡片头部标注渲染文案的位置全部只写 `超预算`、长串零出现，正文其余提到「未进提示词」的行全部自带 `/` 面板或投影来源标注（由 `ui-spec-copy-parity` 机械覆盖）；`docs/product/ai-skills.md` §11.7 的两种标注形态 + 诚实边界与 §11.8 的覆盖描述（由 `docs-ok` 机械覆盖）；末尾收口记录节；三者与实现口径一致。
7. **源码级「不新写」**：`src/skill-picker-model.js` 里该常量以 `STATUS_TEXT.promptOmitted.split(' · ').pop()` 的**引用形式**声明，且文件内零个独立 `'超预算'` 字面量（Task 2 的断言 2 与 `<verify>` 第三块都断言这两条 —— 单点变异已实测：改成写死字面量会让两条同时转红）。
8. **安全**：全程无 `pkill` / `pgrep` 杀进程操作；`/Applications/Realm.app` 实例未受影响；`managed-skills/` 下的 `commit-style` 与 14 个 `aa-budget-*` 仍在。
</verification>

<success_criteria>
- 面板真实拖到 280px（并在 260px 处被钳制回 280px）时，带托管徽标 + 超预算标注的 `manage_skill` 卡片头部：标注完整可读（不被祖先裁切）、容器自身不溢出、技能名宽度 > 0（可被省略号压缩）、头部仍为 36px 单行、徽标完整 —— 一次驱动运行内全部为真。
- 三条新判据在修复前的当前树上**确实为红**（红轮证据落盘），证明断言非恒真。
- 卡片头部超预算标注渲染 `超预算`（3 字）；`/` 面板行尾标注仍是 `未进提示词 · 超预算`（48 D-12 原文）；两者共用同一单源，且卡片形态是**机械投影**而非新写字面量 —— **由源码级两条机械判据证明**（引用形式正则命中 + 零第二份 `'超预算'` 字面量），不是靠 `includes` 或注释里的承诺。
- **无任何 CSS 声明**、`STATUS_TEXT` 四值、`MANAGE_SKILL_SHORT_REASON` 九值被改动 —— CSS 那条由 A9（全轮声明投影 sha 相等）+ `css-decl-freeze`（四规则块声明集逐字比对）双判据承担，单点变异（`max-width: 60px`）实测双红。
- 三个既有套件全绿，八个例数账本单元与实测一致。
- `49-UI-SPEC.md` 的**七处**陈述卡片渲染文案的位置均已收口为 `超预算`（长串零出现），其余提到「未进提示词」的行均自带 `/` 面板或投影来源标注，且末尾记录了 E1 overflow 的收口（含为什么是卡片形态、为什么不动面板文案、为什么排除换行头部与 system-note，以及把表面纪律立为契约条款）。
</success_criteria>

<output>
Create `.planning/phases/49-manage-skill-ai/49-07-SUMMARY.md` when done
</output>
