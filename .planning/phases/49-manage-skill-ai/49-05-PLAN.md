---
phase: 49-manage-skill-ai
plan: 05
type: execute
wave: 2
depends_on: [49-04]
files_modified:
  - ai-manager.js
  - src/renderer.js
  - src/skill-picker-model.js
  - tests/test-ai-skills.js
  - tests/test-skill-picker-model.js
autonomous: true
gap_closure: true
requirements: [MGMT-01, MGMT-05]
user_setup: []

estimate:
  tokens: 118000
  raw_tokens: 118000
  tasks: 3
  confidence: low

key-decisions:
  - '本计划闭合 VERIFICATION.md 的 Gap 2（卡片终态标记崩塌 / truth #12）与 Gap 3（重开对话后卡片形状与实时链路逐字一致 / truth #13），二者同源：Gap 3 的 `missing` 第一条明文就是「修复 CR-01 后重新核对两条链路的键集合（含失败行）」'
  - 'CR-01 的修法**取 UI-SPEC 硬约束 1 的字面语义**：渲染端的「已存在条目」分支**并入**终态载荷，保留 start 事件给的 `action` / `name`。**否决另一条备选**（把整份 decoration 移进 `_resolveManageSkillTerminal`，让替换写也成立）—— 它会让 `tool_execution_end` 重复 start 已给过的两键，且与 UI-SPEC 的两时点分工（start 给 action / name、end 给终态三键）直接冲突'
  - '并入逻辑**抽成跨进程单源的纯函数** `mergeManageSkillMarker(prev, incoming)` 住 `src/skill-picker-model.js`（双模式导出、零依赖、零 DOM，renderer 经 `window.SkillPickerModel` 取、纯 Node 测试经 `require` 取**同一个对象引用**）。理由：① 本 phase 的假绿成因正是「测试手搓期望对象、renderer 另有实现」，只有让测试**调用 renderer 真正调用的那个函数**才能让守卫表达实现语义；② 该文件已是 `TIER_BADGE` / 三张 `MANAGE_SKILL_*` 白名单表的跨进程单源落点，新增一个纯数据形状函数属**追加**（既有导出语义零改动）；③ 内联在 `src/renderer.js` 里则纯 Node 测试无法触达，只能退回「子串存在」式假绿断言'
  - '失败态原因码的落库取**用户已裁决的方案**：在工具执行的失败出口给被抛出的错误消息加 `[code] ` 词缀（同一条消息既进 `tool_results` 的文本、也回给 LLM），重载链路用**同一个常量**把它解析回来。否决「记为已知偏差并收窄 M5b」与「另开持久化通道」（写进 `tool_calls` 列已被既有注释明确排除：会污染 `getAgentMessages` 的 LLM 上下文重建路径；新建列 / 新表则超出本 run 的范围）'
  - '词缀的写入位置固定在 catch 块**元数据写入之前**：既有的 M2 源码正则要求 `_manageSkillMeta.set(...)` 与随后的 `throw err;` 相邻，把加词缀放在前面可让该护栏**逐字不变**（它断言的是「失败出口仍然 throw」这条 LLM 语义，不该为了本次改动而放宽）'
  - '**已知且刻意的可见面变化**：失败消息因此带一个 `[code]` 前缀（LLM 与卡片展开区都会看到）。这是「失败态唯一的持久化通道是消息文本」这一机制的代价，且它**只增信息不删信息**（原中文完整文案一字不动地跟在词缀之后）。该变化必须在 `docs/product/ai-skills.md` 成文（归 49-06），不得留成未记录的偏差。WR-03（成功文案偏离 UI-SPEC 清单）仍**不在本 run 范围内**，保持挂账'
  - '三态 `promptIncluded` 的消费侧必须与 49-04 的产出一致：只有 boolean 才写进 `details` / 短期元数据；只有**严格等于 false** 才追加「技能段预算已满」那句；未命中（`undefined`）⇒ 省略键且不追加。理由：49-04 已把「不在技能集里」与「超预算」拆成两个值，消费侧再把它们合并回一句话就等于把 49-04 的修复作废'
  - '两组假绿守卫必须**纠正**而不是删除：M3 升级为「具体区域 + 语义调用」断言（断言渲染端调用共享合并函数，而不是断言某个标识符出现过），M5b 改为**调用 renderer 真正调用的那个合并函数**推导实时形状，并补失败行。删除守卫会留下无护栏的回归面，而本 phase 的教训恰是「守卫在但检不出问题」'
  - '本计划**不改** `src/skill-picker-model.js` 的既有导出语义与三张白名单表的内容（只追加一个纯函数并加进 api 对象），**不动** `renderSlashPickerList` / `renderSkillContentBox` / 卡片样式，**不扩大** TD-48-01（新增插值面全部是 `textContent` 或 DOM 属性赋值，零 `innerHTML` 模板拼接）'
  - '所有验证命令必须写成 `node tests/<file>.js` / `node --test tests/<file>.js` 的显式形式，不得引用 `npm test` —— 本项目 package.json 无该脚本'
  - '**超预算的显式裁决（本计划不拆分，理由如下，非静默放过）**：`estimate-check --calibrated` 对本计划给出 `tokens 118000` / 预算 `100000`（ratio 1.18，`over_budget: true`，建议约 2 切片）。**裁决 = 保持单计划并对该超预算显式记账**，三条理由同时成立：① `confidence: low` 的来源是**0 条校准样本** —— 118000 是未按历史实际用量校准的整估，不等于实测超支，不应按「实测超限」处置；② 两个**结构性目标都在范围内**（恰好 3 个 task；5 个文件 —— 拆分触发条件是「>3 个 task」或「任一 task 触及 >5 个文件」）；③ 唯一自然的拆切缝（tracer = 共享合并函数 + 渲染端并入 + 值域行；expansion = 词缀 + 重载还原 + M3/M5b 纠正）**不是两条可并行切片，而是一条链**：M5b 必须调用 Task 1 产出的 `mergeManageSkillMarker` 才能从渲染端真实语义推导形状，拆开后两计划仍串行（wave 2 → wave 3），却要多付一次 handoff（共享函数签名 + 值域行 + 夹具改动口径的交接），上下文总量不降反升；同时会打断本轮已验证一致的 wave / depends_on 结构（`[] / [49-04] / [49-04, 49-05]`）。**后续义务**：若日后拿到 ≥3 条实际用量样本，应按实测重估本计划，并在阶段复盘中复核本裁决'

must_haves:
  truths:
    - '端到端（Gap 2 靶心）：`tool_execution_end` 到达后，该次 `manage_skill` 执行的 `manageSkill` 标记**仍然含 start 事件给出的 `action` 与 `name`**，并**新增**终态三键 —— 即 `{action, name, tier?, code?, promptIncluded?}`，而不是退化成只剩 `{tier, promptIncluded}`'
    - 'Gap 2 的可观察后果被消除：卡片技能变体在终态仍成立（`MANAGE_SKILL_ACTION_LABEL[action]` 与 `name` 都在），因此标题是「创建 / 更新 / 删除技能「{name}」」、来源徽标与内联标注可出现、参数区是可读摘要而**不是**整份 `content` 的 JSON 墙'
    - '条件并入语义：后续不带标记字段的 update 事件（`event.manage_skill` 为空）**不得**把已写入的标记抹掉 —— 合并函数在入参为空时原样保留前值'
    - '合并函数**不修改入参**、返回新对象；`prev` 缺省时返回 `incoming` 的拷贝；`incoming` 与 `prev` 同名键时 `incoming` 胜出'
    - 'Gap 3（成功行）：重开对话后重建的 `manageSkill` 与实时链路合并后的 `manageSkill` **键集合逐字相等、取值逐字相等** —— 两侧都是 `{action, name, tier, promptIncluded}`'
    - 'Gap 3（失败行）：重开对话后重建的 `manageSkill` 与实时链路合并后的 `manageSkill` **键集合逐字相等、取值逐字相等** —— 两侧都是 `{action, name, code, tier?}`，失败历史卡片**保留短原因**（原因码经事务内的词缀落库并还原）'
    - '失败态的原因码**不重算**：重载链路从持久化文本里的词缀解析，而不是按当前磁盘状态重新判定「当时为什么失败」（后者会随技能被删除 / 改名而漂移）'
    - '改动前落库的旧失败消息（没有词缀）不设 `code` 键：解析失败即省略（宁缺勿猜，与既有 tier 口径一致）—— 旧历史卡片表现为「只有失败、没有短原因」，与改动前一致，零回归'
    - '`promptIncluded` 三态在消费侧一致：未命中（`undefined`）时 `details.promptIncluded` **不出现**、结果文本**不追加**「技能段预算已满」句；`=== false` 时才追加'
    - '守门断言可失败（**两条守卫各有独立的、可生产的失败路径**）：① **M3** 是渲染端接线守卫 —— 把 `src/renderer.js` 的合并分支临时改回覆盖写法，`node tests/test-ai-skills.js` 必然转红（M3 转红，M5b **不会**）；② **M5b** 是「共享合并函数 vs 重载重建」的一致性守卫 —— 把 `mergeManageSkillMarker` 临时改成覆盖语义（返回 `incoming` 而不并入 `prev`），M5b 必然转红（M3 **不会**，因为它只断言渲染端调用了该函数）。两条反向验证都要实跑并把转红的断言名记入 SUMMARY'
    - '反向验证的**可生产性纪律**：不得声称某条守卫会在它读不到的改动上转红 —— M5b 的 `live` 由 `src/skill-picker-model.js` 的共享函数算出，**渲染端改回覆盖写法时 M5b 仍为绿**；那条路径由 M3 独占。M5b 的职责是证明「共享合并函数与重载链路的重建结果逐字相等」，不是证明渲染端接线正确'
    - '[assumption] 成功行的 `tier` 在两条链路上取自**不同的时刻**（实时 = 该次执行后立即判定的快照；重载 = 打开对话时的当前快照）。技能事后被删除 / 改名时重载链路会省略 `tier` 键 —— 这是 49-02 已声明的既有口径（技能事后变化不影响「当时发生了什么」，但档位无法再判定时宁缺勿猜），本计划不改变它，也不把「跨时刻漂移」冒充成本 gap 的闭合面'
  artifacts:
    - 'src/skill-picker-model.js：新增纯函数 `mergeManageSkillMarker(prev, incoming)` 并加入 api 对象（双模式导出；零依赖、零 DOM）'
    - 'src/renderer.js：`tool_execution_update` 的「已存在条目」分支改为经 `mergeManageSkillMarker` 并入（保留 start 的 action / name，叠加终态三键）'
    - 'ai-manager.js：失败态原因码词缀的**单一常量 `MANAGE_SKILL_CODE_TAG`**（模块级正则，锚定消息起始）+ 失败出口给 `err.message` 加词缀（同一 Error 实例、`code` 与栈不变）'
    - 'ai-manager.js：`_manageSkillTerminalFromStored(t)` 从持久化文本还原 `code`（`t.error` 优先、`t.result` 回退），解析失败即省略键；JSDoc 同步'
    - 'ai-manager.js：`_buildManageSkillTool` 的 `promptIncluded` 消费侧按三态收口（只有 boolean 才写键、只有严格 false 才追加文案）'
    - 'tests/test-skill-picker-model.js：`mergeManageSkillMarker` 的值域行（缺省 / 覆盖 / 不修改入参 / 返回新对象）'
    - 'tests/test-ai-skills.js：失败态词缀的行为行（真跑工具 execute → 断言抛出的错误消息带词缀）+ 词缀 → 重载还原的行为行 + M3 语义化 + M5b 改从 renderer 真实语义（含失败行）'
  key_links:
    - '实时链路：SDK `tool_execution_start` → 主进程 `_resolveManageSkillMarker(action, name)` → `{action, name}` → 渲染端新条目分支写入 `manageSkill` → SDK `tool_execution_end` → 主进程 `_resolveManageSkillTerminal(toolCallId)`（读 `_manageSkillMeta` 后即删）→ 终态三键 → 渲染端「已存在条目」分支 `mergeManageSkillMarker(旧标记, 终态载荷)` → 卡片变体成立。**这条链的中间那一步（并入）就是 CR-01 的修复点**'
    - '重载链路：`ai-conversations-manager.getMessages` 从 `tool_results` 行回填 `t.result` / `t.error` / `t.details` → `ai-manager._decorateConversationMessages` 的 `toolExecutions.map` → `_buildManageSkillDecoration(t.params, _manageSkillTerminalFromStored(t))` → `{action, name, tier?, code?, promptIncluded?}`。两条链路的产出对象由**同一个**构造函数生成，键集合因此不可能漂移（这是 UI-SPEC 硬约束 3 的机制保证）'
    - '失败态原因码链：业务错误（带 `code`）→ 工具 catch 给 `err.message` 加词缀 → SDK `createErrorToolResult(message)` 把该消息落成 toolResult 文本 → `conversationStore.saveMessages` 持久化 → 重载时 `target.error` = 该文本 → `_manageSkillTerminalFromStored` 用同一个常量解析出 `code` → 终态元数据的 `code` 与实时链路逐字一致'
    - '单源护栏链：`src/skill-picker-model.js` 的 `mergeManageSkillMarker` 是 **renderer 与测试共用的同一对象引用**（`window.SkillPickerModel` ≡ `require("src/skill-picker-model")`，48-01 已建立的单源纪律）；`ai-manager.js` 的词缀常量是 encode / decode 两侧的唯一来源'
    - '回归护栏：`tests/test-ai-skills.js` 的 M2（失败出口仍然 throw 的源码正则）、M2b（终态元数据只投影三键、读后即删）、M4（渲染端零判定 / 零 HTML 拼接）、M5（装饰构造恰一处定义）、L1–L3（刷新链次数账）必须继续成立；`node --test tests/test-skill-picker-model.js` 既有 99 例必须继续全绿'
  prohibitions:
    - statement: '守门断言必须能失败：不得用「子串存在」或「手工构造期望对象」的方式断言实现语义；也不得声称某条守卫会在它读不到的改动上转红'
      status: unresolved
      verification: judgment
      reason: '本 phase 的 gap 报告已把这两类断言指认为 CR-01 得以漏网的直接原因（M3 只查子串、M5b 自己手搓了渲染端从未构造过的合并结果，172 例全绿却漏掉三条 Critical）。**后半句是本 run 追加的属性**：反向验证必须是**可生产**的 —— M5b 的实时形状由共享合并函数算出，渲染端回退到覆盖写法**不会**让 M5b 转红（那条路径由 M3 独占），因此「渲染端回退 ⇒ M3 与 M5b 都转红」是不可生产的声明，写进验收标准就会诱导执行者给 M5b 补一条子串断言来交差 —— 正是本 run 要消灭的假绿形态。两条守卫的失败路径因此必须分开声明（渲染端回退 → 只有 M3 红；共享函数改成覆盖语义 → M5b 红）'
    - statement: '渲染端不得自行判定来源档位、名称冲突或限额（判定只在主进程）'
      status: unresolved
      verification: judgment
      reason: 'UI-SPEC 硬约束 4 / 48-03 的既有纪律。本计划新增的合并函数是纯**数据形状**运算（不读路径、不读磁盘、不读技能集），因此不引入任何判定面；M4 的「三处接线区域零来源判定素材」断言必须继续保持为真'
    - statement: '持久化通道不得为了携带原因码而污染 LLM 上下文重建路径'
      status: unresolved
      verification: judgment
      reason: '既有注释已明确排除把标记写进 `tool_calls` 列（`getAgentMessages` 读同一列重建 LLM 上下文）。本计划因此选「错误消息词缀 + 同一常量解析」：它落在既有通道内、零 schema 变更、零新增列，且不改变任何工具参数的形状'
---

<objective>
闭合 Phase 49 的 Gap 2 与 Gap 3 —— 它们同源：

- **Gap 2（CR-01）**：渲染端的「已存在条目」分支用**覆盖**而不是**并入** 终态标记，`tool_execution_end` 一到就把 start 事件给的 `action` / `name` 抹掉，导致 `manageSkillOk` 恒 false、整张卡片的技能变体崩塌（标题退回工具名、徽标与短原因永不出现、参数区退回整份 `content` 的 JSON 墙）。
- **Gap 3（WR-02）**：两条链路的终态键集合实际不等 —— 实时链路被覆盖成 `{tier, promptIncluded}`、重载链路是 `{action, name, tier, promptIncluded}`；失败行还额外缺 `code`（原因码不落库），使失败历史卡片永久丢失「内置不可改删」这类短原因。

用户已裁决失败态的处置：**持久化原因码**（不接受「记为已知偏差并收窄断言」），使两条链路对失败行也逐字相等（UI-SPEC 硬约束 3 是锁定决策）。

顺带修正 Gap 1 的另一处消费侧缺陷：49-04 把 `getSkillPromptIncluded` 改成三态后，工具侧必须在未命中（`undefined`）时省略 `details.promptIncluded` 并**不**追加「技能段预算已满，仍可用 /skill:{name} 手动调用」这句失实文案。

Purpose: 让「卡片是那次工具调用的忠实记录」成为**可失败被观察**的代码事实，并让守护它的断言表达实现语义而不是意图。

Output: `src/skill-picker-model.js` 的共享合并函数；`src/renderer.js` 的真并入；`ai-manager.js` 的原因码词缀与重载还原 + 三态消费侧；`tests/test-skill-picker-model.js` 的值域行与 `tests/test-ai-skills.js` 的纠正后守卫（M3 / M5b 含失败行）。

**禁用项召回记录的归属（本计划不重跑召回）**：本 phase 的两段式禁用项召回记录（含 canon-referral 的丢弃理由与逐条结论）以 **`49-04-PLAN.md` 的那一份为准**，是本 phase 的权威副本；本计划**不重复召回**，只保留自己 `must_haves.prohibitions` 里的三条（原样保留、不删不改）。本计划的三条禁用项与 49-04 的召回结论同向：都是「守门断言可失败」与「判定只在主进程」这两条纪律在卡片标记域上的投影 —— 其中第一条在本计划里被进一步收窄为「守卫的失败路径必须**可生产**」（见 `must_haves.prohibitions` 的第一条与 Task 3 的两条实跑路径）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-manage-skill-ai/49-VERIFICATION.md
@.planning/phases/49-manage-skill-ai/49-REVIEW.md
@.planning/phases/49-manage-skill-ai/49-UI-SPEC.md
@.planning/phases/49-manage-skill-ai/49-01-SUMMARY.md
@.planning/phases/49-manage-skill-ai/49-02-PLAN.md
@.planning/phases/49-manage-skill-ai/49-02-SUMMARY.md
@.planning/phases/49-manage-skill-ai/49-04-PLAN.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 标记并入的单一实现 —— 共享合并函数 + 渲染端真并入 + 值域行</name>
  <files>src/skill-picker-model.js, src/renderer.js, tests/test-skill-picker-model.js</files>
  <read_first>
    - `src/skill-picker-model.js`（被改造文件）：`:1-21`（文件头：**双模式导出**契约 —— renderer 经 `window.SkillPickerModel`、纯 Node 测试经 `require` 取**同一个 api 对象引用**；**零 import / 零 require / 零 DOM** 的依赖纪律）、`:342-393`（三张 `MANAGE_SKILL_*` 白名单表的落点与它们的「跨进程单源」注释）、`:395-415`（api 对象的字段清单与末尾的双模式导出两行）
    - `src/renderer.js`（被改造文件）：`:9347-9398`（`tool_execution_update` 的两分支 —— 「已存在条目」分支目前只并入 `status` / `result` / `error` 再加一个条件覆盖的标记；「新条目」分支逐字段列举首事件），`:9601-9610`（`manageSkillOk` 的判定：标记的 `action` 必须命中白名单表且 `name` 是非空字符串）、`:9699-9744`（技能变体的三块内容全部 gated 在 `manageSkillOk` 上）、`:9750-9766`（参数区：标记成立时走可读摘要，否则回落整份 `JSON.stringify`）
    - `tests/test-skill-picker-model.js`（被改造文件）：`:19-25`（表驱动用例的既有写法：模块经 `require("../src/skill-picker-model")` 载入）、`:890-985`（A 组 / B 组断言范式：`Object.keys().sort()`、`Object.isFrozen`、值域逐字、以及「源码里必须是引用形式」的机械保证写法）
    - `49-UI-SPEC.md`：`## 数据契约（主进程 → 渲染端）` 的 `toolExecution.manageSkill` 形状表、硬约束 1（终态字段合并）、硬约束 3（重载链路同形，两条链路键集合逐字相等）、硬约束 4（判定只在主进程）
    - `49-REVIEW.md` 的 CR-01 全段（含被覆盖后的终态形状复现：`{tier, "promptIncluded"}`、`manageLabel` 为 undefined、`manageSkillOk` 为 false）
  </read_first>
  <behavior>
    - `mergeManageSkillMarker(prev, incoming)`：`incoming` 为空（null / undefined）时原样返回 `prev`（含 `prev` 也为空的情形）；`prev` 为空、`incoming` 非空时返回一个**新对象**且取值等于 `incoming`；两者都非空时返回新对象，同名键取 `incoming` 的值，异名键两边都保留。
    - 调用后 `prev` 与 `incoming` 两个入参对象**逐字未被修改**（深度相等比对）。
    - 契约示例：`{action:'create', name:'alpha'}` 并入 `{tier:'managed', promptIncluded:false}` ⇒ `{action:'create', name:'alpha', tier:'managed', promptIncluded:false}`；并入 `{code:'seeded_protected', tier:'builtin'}` ⇒ `{action:'create', name:'alpha', code:'seeded_protected', tier:'builtin'}`。
    - `src/renderer.js` 的「已存在条目」分支经 `mergeManageSkillMarker` 并入（区域断言通过），且该区域不再出现「用事件载荷直接赋值给标记字段」的写法。
    - 新条目分支行为逐字不变（首事件无可并入对象，仍直接写入事件给的标记）。
  </behavior>
  <action>
    **A. `src/skill-picker-model.js`：新增共享合并函数**

    1. 在三张 `MANAGE_SKILL_*` 表之后（`const api = {` 之前）新增纯函数 `mergeManageSkillMarker(prev, incoming)`：
       - `incoming` 为假值 → 原样返回 `prev`（**不**新建对象：这是「不带标记的后续 update 事件不得抹掉已写入标记」的机械保证）；
       - `prev` 为假值 → 返回 `incoming` 的浅拷贝（不返回同一引用，避免调用方后续改动串到事件载荷上）；
       - 两者都非空 → 返回 `{ ...prev, ...incoming }`。
    2. 把 `mergeManageSkillMarker` 加进 `api` 对象（追加在 `MANAGE_SKILL_SHORT_REASON` 之后，保持既有键序不动）。**不要**改任何既有导出 / 表的取值。
    3. JSDoc 必须写明四件事：① 它是**渲染端事件映射与纯 Node 测试共用的唯一合并实现**（同款单源理由见文件头）；② 条件并入的语义与理由（首个事件给 `action` / `name`，终态事件只给 `tier` / `code` / `promptIncluded`，因此**必须并入**而不是覆盖 —— 覆盖会让 `action` 丢失、卡片变体崩塌）；③ 不修改入参、每次返回新对象；④ 它是**纯数据形状**运算，不做任何来源 / 撞名 / 限额判定（判定只在主进程）。
    4. 本文件仍须保持**零 import / 零 require / 零 DOM**（新增函数不得引入任何依赖）。

    **B. `src/renderer.js`：把「已存在条目」分支改为并入**

    5. 该分支（`:9368-9378`）的条件覆盖写法改为调用共享合并函数：以该条目**已有的** `manageSkill` 为基底、把事件携带的标记作为覆盖层传进去，结果写回 `manageSkill` 键。写成一句表达式即可（保持该分支其余三个字段逐字不变）。
    6. 注释改写为表达新语义：说明这一行是「终态字段的并入」、为什么必须并入不能覆盖（RESEARCH Pitfall 4 / UI-SPEC 硬约束 1）、以及**并入逻辑的单一实现在 `window.SkillPickerModel.mergeManageSkillMarker`**（渲染端不得另写一份 `{...prev, ...incoming}`）。
    7. **新条目分支（`:9381-9394`）保持不动** —— 首个事件没有可并入的对象，直接写入事件载荷与并入语义等价；改它只会扩大 diff 面。
    8. 不得新增任何 `<style>` / 类名 / DOM 结构；不得动 `renderSkillContentBox` 与卡片样式（本计划是纯数据形状修复）。

    **C. `tests/test-skill-picker-model.js`：值域行**

    9. 在 B 组之后新增一组用例（建议命名 C 组 · `mergeManageSkillMarker`），覆盖：① `incoming` 为空时返回 `prev` 本体（含两次调用的返回值**是同一个对象**这一强断言）；② `prev` 为空时返回新对象且取值等于 `incoming`；③ 两者都非空时同名键被覆盖、异名键都保留（用上面 `<behavior>` 里的两个契约示例逐字断言）；④ 入参不被修改（调用前后 `JSON.stringify` 逐字相等）；⑤ 与真实两时点载荷的综合例：`{action:'create', name:'alpha'}` 并入 `{tier:'managed', promptIncluded:false}` 后 `action` / `name` 仍在（这正是 CR-01 的反例断言，必须能在把渲染端改回覆盖写法时**指认**它）。
    10. 保持既有 99 例全绿（不得为新增行放宽或删除旧断言）。
  </action>
  <verify>
    <automated>node --test tests/test-skill-picker-model.js</automated>
    <fails_when>`# fail` 大于 0；或 `# tests` 仍为 99（说明值域行没被加进去）</fails_when>
    <automated>node -e 'const s=require("fs").readFileSync("src/renderer.js","utf8");const a=s.indexOf("toolExecutions[existingIdx]");const b=s.indexOf("toolExecutions.push(",a);if(a<0||b<=a)throw new Error("未找到已存在条目合并分支区域");const m=s.slice(a,b);if(m.length<200)throw new Error("区域提取口径失效（长度 "+m.length+"）");if(!m.includes("mergeManageSkillMarker("))throw new Error("已存在条目分支未调用 mergeManageSkillMarker");if(m.includes("manageSkill: event.manage_skill"))throw new Error("已存在条目分支仍是覆盖写法");console.log("merge-wired ok");'</automated>
    <fails_when>非零退出（脚本抛出 `未找到已存在条目合并分支区域` / `区域提取口径失效` / `已存在条目分支未调用 mergeManageSkillMarker` / `已存在条目分支仍是覆盖写法`），或没有打印 `merge-wired ok`。**锚点纪律**：终点锚点 `toolExecutions.push(` 必须**从起点之后**搜索（`indexOf(mark, a)`）—— 全局搜索会取到文件中**第一个**同名锚点，一旦它在起点之前，切片即恒为空串、断言无论实现对不对都恒失败（固定长度窗口与全局终点锚点是同一类缺陷）</fails_when>
    <automated>node -e 'const m=require("./src/skill-picker-model.js");const f=m.mergeManageSkillMarker;if(typeof f!=="function")throw new Error("未导出 mergeManageSkillMarker");const prev={action:"create",name:"alpha"};const out=f(prev,{tier:"managed",promptIncluded:false});if(out===prev)throw new Error("并入必须返回新对象");if(out.action!=="create"||out.name!=="alpha"||out.tier!=="managed")throw new Error("并入结果形状不对");if(f(prev,null)!==prev)throw new Error("incoming 为空时必须原样返回 prev");if(prev.tier!==undefined)throw new Error("不得修改入参");console.log("merge-helper ok");'</automated>
    <fails_when>非零退出（脚本抛出 `未导出 mergeManageSkillMarker` / `并入必须返回新对象` / `并入结果形状不对` / `incoming 为空时必须原样返回 prev` / `不得修改入参`），或没有打印 `merge-helper ok`</fails_when>
  </verify>
  <done>共享合并函数就位并被渲染端的「已存在条目」分支调用；值域行覆盖四类语义与 CR-01 反例；两条测试套件全绿且例数增长。</done>
  <acceptance_criteria>
    - `require("../src/skill-picker-model").mergeManageSkillMarker` 是函数（纯 Node 可 require，证明零 DOM / 零 electron 依赖未破）。
    - `mergeManageSkillMarker({action:'create',name:'alpha'}, {tier:'managed',promptIncluded:false})` 的 `action` / `name` 仍在（CR-01 的反例断言）。
    - `mergeManageSkillMarker(prev, null) === prev`（同一引用）。
    - `src/renderer.js` 的合并区域（`toolExecutions[existingIdx]` 到 `toolExecutions.push(`）含对 `mergeManageSkillMarker` 的调用，且不含直接赋值写法。
    - `node --test tests/test-skill-picker-model.js` 输出 `# fail 0` 且 `# tests` 严格大于 99。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 失败态原因码落库与还原（词缀）+ 三态 `promptIncluded` 的消费侧收口</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - `ai-manager.js`（被改造文件）：`:720-739`（`_manageSkillMeta` 的字段声明与「为什么必须用短期元数据而不是 `result.details`」的根因注释）、`:1690-1730`（`_resolveManageSkillMarker` 的同步零 IO 判定）、`:1732-1761`（`_buildManageSkillDecoration` —— 两条链路共用的唯一构造，键形状 `{action, name, tier?, code?, promptIncluded?}`）、`:1763-1793`（`_resolveManageSkillTerminal` —— **读后即删 + 只投影终态三键**，其 JSDoc 明文假设渲染端会并入）、`:1795-1823`（`_manageSkillTerminalFromStored` —— 现状只能还原 `promptIncluded` 与 `tier`，`code` 明文声明「无法还原」）、`:1884-1942`（两个事件分支：start 给 `{action, name}`、end 给终态三键）、`:2030-2037`（`_safePreview` 的 200 字截断 —— 实时 `error` 字段是截断预览，与重载链路的完整文本不同，本计划**不改**它，也不依赖它携带词缀）、`:6120-6272`（`_buildManageSkillTool` 全文：失败出口的 `_manageSkillMeta.set(...)` 紧邻 `throw err;`，以及 6249-6258 的结果文案与 6247 的条件追加）
    - `ai-conversations-manager.js`（**只读**）：`:600-641`（`getMessages` 的 toolResult 回填：`target.result` / `target.error` = 行文本、`target.details` = 持久化的结构化元数据）
    - `node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js`（**只读**）：`createErrorToolResult(message)` 把 `message` 落成 `content[0].text` 且 `details` 恒为 `{}`；catch 分支用 `error.message` 取文本 —— 这是「消息就是唯一可持久化的失败态通道」的机制依据
    - `tests/test-ai-skills.js`（被改造文件）：`:3219-3247`（`promptCtx(env)` —— **真跑工具 `execute` 的夹具**，含 `_manageSkillMeta` / `rescanCalls` / `syncAgentSystemPrompt` 包装）、`:3411-3461`（L1：工具 execute 的成功路径行为范式）、`:3541-3553`（`manageReloadCtx(env, seeded)` 的重载链夹具）、`:3575-3607`（M2 的源码正则：失败出口写元数据后**仍然 throw**、且不得从 `result.details` 读原因码）、`:3609-3635`（M2b：终态元数据的形状与读后即删）、`:3689-3745`（M5b 现状：自己手搓实时形状）
    - `49-04-PLAN.md`（本计划的直接前置）：`getSkillPromptIncluded` 改为三态（未命中 → `undefined`）—— 本任务的消费侧必须与该产出对齐
  </read_first>
  <behavior>
    - 真跑一次会失败的 `manage_skill` 调用（如 `create` 一个 seeded 名字）：`tool.execute(...)` 抛出的错误消息**以 `[seeded_protected] ` 开头**，错误对象上的 `code` 仍是 `seeded_protected`，且消息其余部分与改动前逐字相同（词缀只增不改）。
    - 同一错误实例被抛出（`code` 与栈未被替换）；同一次执行不会重复加词缀。
    - 重载链路：给 `_manageSkillTerminalFromStored` 一个带词缀的 `t.error`（失败行）→ 返回对象含 `code`；给一个**没有**词缀的旧失败文本 → 返回对象**不含** `code` 键（旧历史零回归）。
    - 成功行不误判：`t.result` 是成功文案（不带词缀）时 `_manageSkillTerminalFromStored` 不产出 `code` 键。
    - 键集合相等（失败行）：实时链路 `mergeManageSkillMarker({action,name}, 终态)` 与重载链路 `_buildManageSkillDecoration(t.params, _manageSkillTerminalFromStored(t))` 的 `Object.keys().sort()` 与取值**逐字相等**。
    - 三态消费侧：把技能管理器的 `getSkillPromptIncluded` 打桩为返回 `undefined` 时，成功结果文本**不**含「技能段预算已满」句、`details` 上**没有** `promptIncluded` 键；打桩为 `false` 时该句出现且 `details.promptIncluded === false`。
  </behavior>
  <action>
    **A. `ai-manager.js`：失败态原因码词缀的单一常量 + 写入侧**

    1. 在模块作用域新增一个常量，**名字固定为 `MANAGE_SKILL_CODE_TAG`**（下称「词缀常量」）：它是失败态原因码词缀的正则，**锚定消息起始**、只接受小写字母与下划线、后随空白（`[a-z_]+` 词表域）。注释写明：它是 encode / decode **两侧的唯一来源**，锚定起始是「成功文案永不误命中」的机械保证。**本任务的三处消费点（失败出口的幂等判定、失败出口的改写、`_manageSkillTerminalFromStored` 的解析）必须逐字引用这一个标识符** —— Task 2 的两条源码门禁按该标识符断言，另起名字（例如 `CODE_TAG_RE`）会让门禁转红。
    2. 在 `_buildManageSkillTool` 的失败出口（catch 块）里，按固定顺序：
       - **先**给捕获到的错误加词缀（仅当它是 `Error`、`err.code` 是非空字符串、且消息尚未带同款词缀时）：把 `err.message` 重写为词缀 + 原消息，**保持同一个 Error 实例**（`code` 与栈不变）；
       - 幂等判定（「消息尚未带同款词缀」）**必须用同一个常量**做起始匹配（即 `MANAGE_SKILL_CODE_TAG.test(err.message)`），**不得**另写一份字面量正则或 `startsWith("[")` 之类的近似判据 —— 这条不只是纪律：失败出口的源码门禁要求该区域出现**同一个常量名**，唯一的满足方式就是让 encode 侧真的引用它，从而把「唯一来源」从声明变成代码事实；
       - **再**照原样写 `this._manageSkillMeta.set(toolCallId, {...})`（字段与取值一字不动）；
       - **最后**照原样 `throw err`。
       顺序的原因必须写进注释：既有 M2 的源码正则要求元数据写入与 `throw err;` 相邻，把加词缀放在前面可让那条「失败出口仍然 throw」的护栏逐字不变。
    3. `err.code` 不是字符串时**不加**词缀（不得产出无意义的前缀，例如把「意外错误」标成某个码）。

    **B. `ai-manager.js`：`_manageSkillTerminalFromStored` 还原 `code`**

    4. 在既有的 `promptIncluded` / `tier` 还原之外新增：取文本 = `t.error` 优先、`t.result` 回退（两者都是字符串；都不是字符串则不解析）；用 A 步的**同一个常量 `MANAGE_SKILL_CODE_TAG`** 做起始匹配（不要在这里另写字面量正则），命中即把捕获到的码写进 `terminal.code`。
    5. JSDoc 从「`code` 无法还原」改写为新的机制说明：失败态的原因码由工具失败出口写在**被抛出的消息**里（该消息即落进 `tool_results` 的文本），因此可解析还原；并写明三条边界 —— ① **不重算**（按当前磁盘状态重判「当时为什么失败」会随技能被删改而漂移）；② 改动前落库的旧消息没有词缀 ⇒ 不设键（宁缺勿猜）；③ 解析**只认消息起始**，成功文案不可能命中。
    6. `_resolveManageSkillTerminal`（实时链路）**不动**：它已从短期元数据给出 `code`，且其 JSDoc 里「渲染端会并入 start 值」的假设在本计划 Task 1 落地后成为事实。

    **C. `ai-manager.js`：三态 `promptIncluded` 的消费侧收口**

    7. 成功出口里：`promptIncluded` 可能是三态值。只有它是 boolean 时才写进 `details`（与短期元数据）；否则**不写该键**（不要写 `undefined` 值，避免依赖 JSON 序列化把它丢掉这种隐式行为）。
    8. 结果文案的追加条件保持「严格等于 `false`」，并在注释里写明理由：`undefined` 表示「该技能不在当前技能集里」，此时**不能**断言「预算已满」，也**不能**声称「仍可用 /skill:{name} 手动调用」（该调用同样解析不到）。**不要**改三条成功文案的文案本身（WR-03 不在本 run 范围，逐字保持现状，由 49-REVIEW 继续挂账）。

    **D. `tests/test-ai-skills.js`：行为行**

    9. 新增一组用例（建议接在 M 组的 M2b 之后）：
       - **词缀行为行**：用 `promptCtx(env)` 真跑工具 execute，`create` 一个 seeded 名字（夹具的 `getSeededSkillNamesSafe` 注入该名字即可），断言抛出的错误消息以对应的 `[code] ` 开头、`err.code` 未变、消息去掉词缀后与不带词缀的消息逐字相等。
       - **重载还原行为行**：用 `manageReloadCtx(env, seeded)` 走一次 `getConversationMessages`，构造一行 `status: 'failed'`、`params` 合法、`error` 带词缀的存储行，断言重建出的 `manageSkill.code` 等于词缀里的码；再构造一行**无**词缀的旧失败行，断言重建结果**不含** `code` 键。
       - **键集合逐字相等行（失败行）**：用共享合并函数算实时形状、用重载夹具算重载形状，`Object.keys().sort()` 与取值双向断言相等（正、反两个方向都要断言「不多键也不少键」）。
       - **三态消费侧行**：把技能管理器模块对象上的 `getSkillPromptIncluded` 临时打桩（`t.after` 复原）为返回 `undefined` / `false` 两种情况，分别断言结果文本与 `details.promptIncluded` 的在场情况（见 `<behavior>` 的最后两条）。
    10. M2 / M2b / M4 / M5 / L1–L3 必须继续全绿（尤其 M2 的「写元数据后仍 throw」正则不得被本任务改动破坏）。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>`# fail` 大于 0；或 `# tests` 仍为 172（说明行为行没被加进去）；或输出里出现 M2 的「失败出口写元数据后必须仍然 throw」断言失败</fails_when>
    <automated>node -e 'const s=require("fs").readFileSync("ai-manager.js","utf8");const start=s.indexOf("_manageSkillTerminalFromStored(t) {");if(start<0)throw new Error("未找到 _manageSkillTerminalFromStored 定义");const end=s.indexOf("_setupEventBroadcasting()",start);const b=s.slice(start,end<0?s.length:end);if(b.length<200)throw new Error("区域提取口径失效（长度 "+b.length+"）");if(!/MANAGE_SKILL_CODE_TAG/.test(b))throw new Error("_manageSkillTerminalFromStored 未用共享词缀常量解析原因码");if(!/terminal\.code\s*=/.test(b))throw new Error("未还原 code");console.log("code-roundtrip wired");'</automated>
    <fails_when>非零退出（脚本抛出 `未找到 _manageSkillTerminalFromStored 定义` / `区域提取口径失效` / `_manageSkillTerminalFromStored 未用共享词缀常量解析原因码` / `未还原 code`），或没有打印 `code-roundtrip wired`。**断言的名词已固定**：常量标识符就是 **`MANAGE_SKILL_CODE_TAG`**（A 步第 1 条已把名字写死、已列进 Artifacts 表），命令按该标识符断言、不再接受候选名 —— 产出侧与断言侧由构造即一致。**锚点纪律（本 run 已实测的缺陷）**：起点锚点必须用带函数体左花括号的**定义形态**（`_manageSkillTerminalFromStored(t) {`），终点锚点必须**相对起点解析**（`indexOf(mark, start)`）—— 该标识符在文件中另有 JSDoc 引用与重载调用点，而 `_setupEventBroadcasting(` 的**首个**出现位置在**目标方法之前**；两个锚点都取全局首个出现会让切片恒为空串，命令无论实现对不对都恒失败（属性：每条自动化命令必须在被强制的实现正确时**可能通过**）。实测锚点坐标（本 run 复核）：起点 68445、终点 69422、区域 977 字符</fails_when>
    <automated>node -e 'const s=require("fs").readFileSync("ai-manager.js","utf8");const start=s.indexOf("_buildManageSkillTool() {");if(start<0)throw new Error("未找到 _buildManageSkillTool 定义");const end=s.indexOf("_adaptHarnessTool(",start);const b=s.slice(start,end<0?s.length:end);if(b.length<1000)throw new Error("区域提取口径失效（长度 "+b.length+"）");if(!/MANAGE_SKILL_CODE_TAG/.test(b))throw new Error("失败出口未加原因码词缀");if(!/\berr\.message\s*=/.test(b))throw new Error("未改写 err.message");console.log("failure-tag wired");'</automated>
    <fails_when>非零退出（脚本抛出 `未找到 _buildManageSkillTool 定义` / `区域提取口径失效` / `失败出口未加原因码词缀` / `未改写 err.message`），或没有打印 `failure-tag wired`。**本条与 A 步第 1–2 条是同一属性**：命令要求该区域出现**同一个常量名 `MANAGE_SKILL_CODE_TAG`**（A 步已把名字写死并列入 Artifacts 表），而正确实现里唯一会引用它的位置就是失败出口的幂等判定（`MANAGE_SKILL_CODE_TAG.test(err.message)`）—— 因此这条命令在实现正确时可通过，且当 encode 侧改回「另写一份字面量正则」时必然转红。区域提取同样遵守锚点纪律：起点取带函数体左花括号的定义形态，终点相对起点解析</fails_when>
  </verify>
  <done>失败态原因码经词缀落库并在重载链路还原；旧历史失败行零回归；三态 `promptIncluded` 的消费侧收口；`node tests/test-ai-skills.js` 全绿且例数增长。</done>
  <acceptance_criteria>
    - 真跑工具 execute 的 seeded 冲突失败：抛出的错误消息以 `[seeded_protected] ` 开头，且 `err.code === "seeded_protected"`。
    - 去掉词缀后的消息与不带词缀时可得的原消息逐字相等（词缀只增不改）。
    - 带词缀的失败行经 `_manageSkillTerminalFromStored` 还原出的 `code` 等于词缀里的码；无词缀的旧失败行不产出 `code` 键。
    - 失败行的实时 / 重载两条链路 `Object.keys().sort()` 与取值逐字相等。
    - `getSkillPromptIncluded` 打桩为 `undefined` 时结果文本不含预算已满句、`details` 无 `promptIncluded` 键；打桩为 `false` 时该句出现且 `details.promptIncluded === false`。
    - `node tests/test-ai-skills.js` 输出 `# fail 0` 且 `# tests` 严格大于 172。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 纠正两组假绿守卫（M3 语义化 + M5b 从渲染端真实语义推导，含失败行）+ 反向验证</name>
  <files>tests/test-ai-skills.js</files>
  <read_first>
    - `tests/test-ai-skills.js`：`:3505-3534`（`rendererManageRegions()` 夹具 —— 合并区 `merge` 目前是「从 `toolExecutions[existingIdx]` 起**固定 900 字符**」的近似窗口；该分支本体实测 **919 字符**，所以今天的窗口**还差 19 字符**没够到 `toolExecutions.push(` 分支，即现状尚未失真、但已贴边。**必须收紧**为以已算好的 `pushIdx` 为终点：固定长度窗口是启发式，分支一旦变长就会把相邻分支的内容纳入区域、分支变短又会把本分支内容留在窗口外，两种漂移都会让区域断言不再等于「该分支本体」）、`:3536-3539`（`stripComments` 的既有写法）、`:3541-3553`（`manageReloadCtx`）、`:3637-3645`（M3 现状：只断言子串 `manageSkill` 与一个三元表达式 —— 覆盖写法同样满足，是假绿）、`:3672-3687`（M5：装饰构造恰一处定义 + 两处调用）、`:3689-3745`（M5b 现状：`const live = { ...startMarker, ...endTerminal }` 手搓期望对象 —— 验证的是意图而非渲染端/共享函数的真实语义）
    - `src/skill-picker-model.js`（Task 1 的产出）：`mergeManageSkillMarker` 的签名与语义
    - `src/renderer.js`：`:9347-9398`（合并区域的真实实现 —— M3 的断言对象）
    - `49-REVIEW.md` 的 CR-01「Why the guards didn't catch it」段与 `49-VERIFICATION.md` 的 Human Verification 第 2 条
  </read_first>
  <behavior>
    - M3 在「渲染端改用覆盖写法」时**必然转红**（改为调用共享合并函数后全绿）—— 断言对象是**具体区域**（`toolExecutions[existingIdx]` 到 `toolExecutions.push(` 之间，即该分支本体）与**语义调用**（调用了共享合并函数），不是整文件的子串存在性；区域窗口的终点必须**由锚点决定**（下一个 `toolExecutions.push(` 的位置），不是「起点 + 固定长度」。
    - M5b 的实时形状由**渲染端真正调用的那个合并函数**计算（同一对象引用），不再手搓实时形状字面量。
    - **失败归属必须可生产**：M5b 的 `live` 来自 `src/skill-picker-model.js` 的共享函数，因此**把渲染端改回覆盖写法时 M5b 仍为绿**（它读不到渲染端源码）；M5b 的失败路径是**把共享函数改成覆盖语义**（返回 `incoming` 而不并入 `prev`）→ M5b 必然转红。**不得**为了凑「渲染端回退 ⇒ M5b 也红」而给 M5b 补一条子串断言。
    - M5b 覆盖**两行**：成功行（`{action, name, tier, promptIncluded}`）与失败行（`{action, name, code, tier?}`），两行都断言「重载形状 ≡ 实时形状」且**双向**断言键集合（不多键、不少键）。
    - M3 / M4 / M5 的既有正向断言（新条目分支映射标记、装饰构造恰一处定义、渲染端零判定 / 零 HTML 拼接）继续成立。
  </behavior>
  <action>
    1. **收紧 `rendererManageRegions()` 的合并区窗口**：把 `merge` 的切片终点从「起点加一个固定长度的近似偏移」改为**已算好的 `pushIdx`**（该夹具里 `pushIdx` 已由 `src.indexOf('toolExecutions.push(', …)` 求出）。这是一个可失败的夹具修正：**窗口性质从「大致一段」变成「该分支本体」**，且随分支增长自动跟随。理据以实测为准（不要沿用「窗口会覆盖到新条目分支」的旧说法 —— 该分支本体实测 919 字符、旧窗口 900 字符，今天**还差 19 字符**没够到相邻分支，即现状尚未失真但已贴边）。同时把 `pushIdx` 的求解也改为**从 `mergeIdx` 之后**搜索（`indexOf(mark, mergeIdx)`），与终点锚点的相对解析形成同一纪律。同步更新该函数与 `content` 窗口的 JSDoc（若其边界依赖 `merge` 的长度也要一并校准）。
    2. **M3 升级为语义断言**：断言合并区（收紧后的窗口）① 调用了共享合并函数（`mergeManageSkillMarker`），② 不再出现「用事件载荷直接赋值给标记字段」的写法，③ 仍保留原有的「新条目分支映射标记」正向断言（该行在新条目分支内，用另一个窗口断言）。断言消息文本必须写明「改回覆盖写法时本断言必然转红」，让后来者知道它的判据。**③（原「窗口确实止于新条目分支之前」）在本 run 的评审中被判定为不可生产的断言，已删除**：合并区的终点锚点本身就是「起点之后的第一个 `toolExecutions.push(`」，因此「该区域不含 `toolExecutions.push(`」是**由切片构造保证恒真**的（实测：区域 = [285263, 286182)，恒不含该串）—— 它在任何实现下都不可能转红，留着只会让人误以为窗口收紧已被断言。「窗口是否真的收紧」由**夹具侧的切片终点**承担（本任务源码门禁的两条正则：`merge: src.slice(mergeIdx, pushIdx)` 必须在、`mergeIdx + \d+` 必须不在 —— 二者今天**都为红**，是本计划真正可失败的检测器）。
    3. **M5b 改从共享函数的真实语义推导**：把 `live` 的手搓字面量换成**调用渲染端真正调用的那个合并函数**（`require("../src/skill-picker-model")` 取到的同一 api 对象上的 `mergeManageSkillMarker`，入参为真实的 start 标记与真实的终态元数据）。这样 `live` 与重载形状的相等断言才是在验证实现而不是意图。
    4. **M5b 补失败行**：构造一行 `status: 'failed'` 的持久化 toolExecution（`params` 为合法的 create 入参、`error` 为带词缀的完整文案、`details` 为空对象），以及对应的实时侧终态元数据（带 `code` 与可判定的 `tier`）；断言两条链路合并 / 重建后的标记**键集合与取值逐字相等**。造可判定 `tier` 的方式：把目标名字放进 `manageReloadCtx` 的第二参（`seeded`）并在 `managed-skills/` 下造出同名目录 ⇒ `sourceTierOf` 判为 `builtin`；同时实时侧短期元数据里给同一个 `tier` 值。
    5. **反向验证（必做，两条路径分开实跑，结论写进 SUMMARY）**：
       - **路径 A（渲染端接线，验 M3）**：把 `src/renderer.js` 的合并分支临时改回覆盖写法 → 跑 `node tests/test-ai-skills.js`，确认 **M3 必然转红**（记录转红的具体断言名）→ 改回并入写法并确认全绿。
       - **路径 B（共享函数语义，验 M5b）**：把 `mergeManageSkillMarker` 临时改成覆盖语义（`incoming` 非空时直接返回 `incoming` 的拷贝，不并入 `prev`）→ 确认 **M5b 必然转红**（并预期 `tests/test-skill-picker-model.js` 的合并值域行同时转红）→ 改回并入语义并确认两套全绿。
       - **必须如实记录的反面事实**：路径 A 下 **M5b 仍为绿**（它的 `live` 来自共享函数、读不到渲染端源码）。这不是缺陷，而是两条守卫职责分工的边界；**不得**为了制造「两条都红」而给 M5b 补子串断言。
    6. 保持 `tests/test-ai-skills.js` 既有 172 例全绿（本任务只改 M3 / M5b 与区域夹具，不改其它组）。**回归口径**：M4 会读 `merge` 区域，窗口收紧后其既有断言（零来源判定素材 / 零 HTML 拼接 / 零 `filePath`）仍须为真 —— 若收紧后转红，说明旧窗口掩盖了相邻分支的内容，按「修断言对象而不是放宽断言」处理。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>`# fail` 大于 0；或 `# tests` 不大于 Task 2 结束时的例数（说明失败行没被加进 M5b）</fails_when>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("tests/test-ai-skills.js","utf8");if(/const live = \{ \.\.\.startMarker, \.\.\.endTerminal \}/.test(t))throw new Error("M5b 仍在手搓实时形状");if(!/mergeManageSkillMarker\(/.test(t))throw new Error("M5b 未调用共享合并函数");const i=t.indexOf("function rendererManageRegions");if(i<0)throw new Error("未找到 rendererManageRegions 夹具");const body=t.slice(i,i+1400);if(!/merge:\s*src\.slice\(mergeIdx,\s*pushIdx\)/.test(body))throw new Error("合并区窗口未以 pushIdx 为终点");if(/mergeIdx\s*\+\s*\d+/.test(body))throw new Error("合并区窗口仍是固定长度近似值");const r=fs.readFileSync("src/renderer.js","utf8");const a=r.indexOf("toolExecutions[existingIdx]");const b=r.indexOf("toolExecutions.push(",a);if(a<0||b<=a)throw new Error("renderer 合并区两端锚点失效");const win=r.slice(a,b);if(win.length<200)throw new Error("合并区提取口径失效（长度 "+win.length+"）");if(!win.includes("mergeManageSkillMarker("))throw new Error("合并区未调用共享合并函数（渲染端回退为覆盖写法时本命令转红）");console.log("merge-region "+win.length);console.log("guards corrected");'</automated>
    <fails_when>非零退出（脚本抛出 `M5b 仍在手搓实时形状` / `M5b 未调用共享合并函数` / `未找到 rendererManageRegions 夹具` / `合并区窗口未以 pushIdx 为终点` / `合并区窗口仍是固定长度近似值` / `renderer 合并区两端锚点失效` / `合并区提取口径失效` / `合并区未调用共享合并函数（渲染端回退为覆盖写法时本命令转红）`），或没有打印 `guards corrected`。**本命令断言的是窗口的「终点」而非「某个字符串出现在测试文件里」**：夹具侧由两条正则表达终点锚点纪律（`merge: src.slice(mergeIdx, pushIdx)` 必须在、`mergeIdx + \d+` 必须不在 —— 只看「文件里有没有那个字符串」的写法检不出未收紧的窗口，它可以被注释或另一处引用满足）；`src/renderer.js` 侧则用同一对锚点**重算一次区域**并断言该区域确实调用共享合并函数 —— 这一条**在实现正确时可过、在渲染端回退为覆盖写法时转红**（可生产）。**不要**把断言写成「该区域不含 `toolExecutions.push(`」：终点锚点就是该串，切片恒不含它，那种断言在任何实现下都不可能失败（本 run 已按此删除该恒真子句）</fails_when>
  </verify>
  <done>M3 成为区域 + 语义断言（窗口终点由锚点决定）、M5b 从共享合并函数推导并覆盖成功行与失败行；反向验证按**两条各自可生产的路径**分开实跑（路径 A → M3 红、M5b 绿；路径 B → M5b 红），结论与转红断言名记入 SUMMARY；套件全绿。</done>
  <acceptance_criteria>
    - M3 的合并区窗口终点由 `pushIdx` 锚点决定（源码断言通过），且该区域经重算后**确实调用了共享合并函数**（渲染端回退为覆盖写法时该命令转红 —— 可生产）。
    - M5b 不再含手搓的实时形状字面量，且调用了 `mergeManageSkillMarker` 取到同一 api 对象（源码断言通过）。
    - M5b 覆盖成功行与失败行两行，两行都双向断言键集合与取值。
    - SUMMARY 分两条记录反向验证结论：**路径 A**（渲染端改回覆盖写法）下 M3 转红的具体断言名，以及**明确记录 M5b 在路径 A 下仍为绿**（附原因：`live` 由共享函数算出、读不到渲染端源码）；**路径 B**（共享函数改成覆盖语义）下 M5b 转红的具体断言名。
    - `node tests/test-ai-skills.js` 输出 `# fail 0` 且 `# tests` 严格大于 Task 2 结束时的例数。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主进程事件载荷 → 渲染端标记 | 事件里的标记是**部分**载荷（start 给两键、end 给三键），渲染端必须并入而不能覆盖 |
| 工具失败 → 持久化文本 → 重载装饰 | 失败态的机器可读原因码只能经**消息文本**跨过这条边界（SDK 的错误结果 `details` 恒为空对象） |
| 渲染端 → DOM | 卡片文案与标记取值来自工具载荷，属不可信字符串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-05-01 | Tampering | `src/renderer.js` 的标记并入 | high | mitigate | 抽成跨进程单源纯函数并让渲染端调用；M3 改为区域 + 语义断言，M5b 从同一函数推导形状 —— 覆盖语义回退必然转红（反向验证记录在 SUMMARY） |
| T-49-05-02 | Repudiation | 失败态原因码不落库 | high | mitigate | 失败出口给错误消息加锚定起始的 `[code]` 词缀，重载链路用**同一常量**解析还原；边界行断言旧消息（无词缀）不设键、成功文案不误命中 —— 失败历史卡片不再丢失短原因 |
| T-49-05-03 | Spoofing | 三态 `promptIncluded` 在消费侧被合并回一句 | medium | mitigate | 只在严格 `false` 时追加「预算已满」、只在 boolean 时写 `details` 键；行为行用打桩覆盖 `undefined` / `false` 两条路径，杜绝「技能不存在 ⇒ 回复『预算已满、仍可手动调用』」的双重失实 |
| T-49-05-04 | Information Disclosure | 失败消息词缀 | low | accept | 词缀只含**白名单原因码**（`[a-z_]+`），不含被拒内容原文；原中文完整文案一字不动地跟在词缀之后。被拒文本的回显面由 49-01 的既有约定（不回显原文）承担，本计划不扩大 |
| T-49-05-05 | Tampering | 渲染端注入面 | medium | mitigate | 新增的合并函数是纯数据形状运算（零 DOM、零字符串拼接）；卡片文案仍一律 `textContent` / 白名单查表，M4 的「三处接线区域零 HTML 拼接、零来源判定素材」断言继续为真，不扩大 TD-48-01 |
| T-49-05-06 | Denial of Service | `_manageSkillMeta` 的残留 | low | accept | 读后即删的既有语义不变（M2b 的「读后即删 / Map 不得累积」断言继续为真）；IN-05 指出的「运行被拆解时残留」属既记录的技术债，不在本 run 范围 |
| T-49-05-SC | Tampering | npm / pip / cargo 安装 | n/a | accept | 本计划不新增任何依赖，无包管理器安装面，无需 legitimacy gate |
</threat_model>

<verification>
- `node tests/test-ai-skills.js` —— `# fail 0` 且例数严格大于 172（本计划新增词缀行为行、重载还原行、键集合相等行、三态消费侧行、M5b 失败行）。
- `node --test tests/test-skill-picker-model.js` —— `# fail 0` 且例数严格大于 99（合并函数值域行）。
- 三条源码 / 行为断言（合并接线、词缀 encode、词缀 decode）各自非零退出即失败，见各任务的 `<verify>`。
- 反向验证（两条路径分开实跑，结论与转红断言名记入 SUMMARY）：**路径 A** 渲染端改回覆盖写法 ⇒ M3 必然转红（M5b 仍绿，如实记录）；**路径 B** 共享合并函数改成覆盖语义 ⇒ M5b 必然转红（合并值域行同时转红）。任何「某守卫会在它读不到的改动上转红」的声明都不得写进验收标准。
- **超预算记账**：本计划的 `estimate-check --calibrated` 报 ratio 1.18（118000 / 100000，`over_budget: true`）。本计划**不拆分**并已在 `key-decisions` 里显式记账三条理由（0 校准样本；3 task / 5 文件均在结构目标内；唯一拆切缝是不可并行的链而非可并行切片）。该裁决属「显式接受」而非「未处置」——执行者不得据此减少任务或跳过反向验证。
- 回归面：M2（失败出口仍然 throw）、M2b（终态三键 + 读后即删）、M4（渲染端零判定 / 零拼接）、M5（构造恰一处）、L1–L3（刷新链次数账）全部保持为真。
- **人工验证的时序前提（提示，不产生任务）**：`49-VALIDATION.md` 人工五步表的**步骤①（卡片终态视觉面）在本计划落地前必然失败** —— 已机械证明终态标记被覆盖、`manageSkillOk` 恒 false。因此该人工观察必须在**本计划完成之后**执行；本计划不修改 `49-VALIDATION.md` 的步骤表（期望现象本身没错，错的是执行时机），只在 SUMMARY 里提示复核时机。步骤⑤（最小宽度单行不换行）仍是 backstop 级的视觉裁决，本计划不为它提供自动化证据。
</verification>

<success_criteria>
- 卡片技能变体在终态可达：`action` / `name` 不再被终态事件抹掉，标题 / 徽标 / 标注 / 参数摘要 / 正文折叠块按 UI-SPEC 的契约渲染。
- 两条链路的标记键集合对**成功行与失败行**都逐字相等（含失败行的 `code`），且相等性由**渲染端真正调用的合并函数**推导出的断言证明。
- 两组假绿守卫被纠正为可失败的断言，且各自的失败路径**可生产**：M3 由渲染端接线回退驱动、M5b 由共享合并语义回退驱动，两条路径各自实跑一次作为证据（不得用一条路径冒充两条）。
- 零回归：既有 99 例与 172 例的其余断言、九码值域、四类撞名、seeded 三入口、刷新链次数账全部不变。
- 失败消息的词缀这一可见面变化被显式记录（归 49-06 成文），不作为未记录的偏差。
</success_criteria>

## Artifacts this phase produces

本计划创建或改动的每一个符号 / 契约面（plan-review 的 source-grounding 段按此表逐项核对）：

| 类型 | 符号 / 契约面 | 落点 | 变化 |
|------|---------------|------|------|
| function | `mergeManageSkillMarker(prev, incoming)` | `src/skill-picker-model.js` | 新增（纯函数，加入 api 对象；renderer 与测试共用同一对象引用） |
| api export | `SkillPickerModel.mergeManageSkillMarker` | `src/skill-picker-model.js` | 新增导出键（既有导出键与取值零改动） |
| renderer 接线 | `tool_execution_update` 的「已存在条目」分支 | `src/renderer.js` | 条件覆盖 → 经共享合并函数并入 |
| module constant | `MANAGE_SKILL_CODE_TAG`（失败态原因码词缀正则，锚定消息起始） | `ai-manager.js` | 新增；encode（失败出口改写 + 幂等判定）与 decode（`_manageSkillTerminalFromStored` 解析）两侧共用同一标识符 |
| method（行为变更） | `_buildManageSkillTool().execute` 的失败出口 | `ai-manager.js` | 加词缀 → 写元数据 → 仍 throw；`promptIncluded` 三态消费侧收口 |
| method（行为变更） | `_manageSkillTerminalFromStored(t)` | `ai-manager.js` | 新增从持久化文本还原 `code`（起始锚定、旧消息不设键）；JSDoc 同步 |
| test case | `mergeManageSkillMarker` 值域行（缺省 / 覆盖 / 不修改入参 / 返回新对象 / CR-01 反例） | `tests/test-skill-picker-model.js` | 新增 |
| test case | 失败态词缀行为行（真跑工具 execute） | `tests/test-ai-skills.js` | 新增 |
| test case | 词缀 → 重载还原行（含无词缀旧消息的零回归行） | `tests/test-ai-skills.js` | 新增 |
| test case | 失败行实时 / 重载键集合逐字相等行 | `tests/test-ai-skills.js` | 新增 |
| test case | 三态 `promptIncluded` 消费侧行（打桩 `undefined` / `false`） | `tests/test-ai-skills.js` | 新增 |
| test fixture | `rendererManageRegions()` 的合并区窗口收紧 | `tests/test-ai-skills.js` | 改动（终点由 `pushIdx` 锚点决定，替换固定长度近似偏移；`pushIdx` 亦从 `mergeIdx` 之后解析） |
| test case（改写） | M3 语义断言 | `tests/test-ai-skills.js` | 子串断言 → 区域 + 语义调用（该区域必须调用 `mergeManageSkillMarker`；原「窗口止于新条目分支之前」子句因恒真已在评审中删除） |
| test case（改写） | M5b 共享函数推导 + 失败行 | `tests/test-ai-skills.js` | 手搓字面量 → 调用 `mergeManageSkillMarker`（渲染端同一对象引用）；补失败行；失败路径由共享函数语义回退驱动（渲染端回退只影响 M3） |

<output>
Create `.planning/phases/49-manage-skill-ai/49-05-SUMMARY.md` when done
</output>
