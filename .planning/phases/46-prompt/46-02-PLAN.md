---
phase: 46-prompt
plan: 2
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - ai-skills-manager.js
  - tests/test-ai-skills.js
autonomous: true
requirements: [SKILL-05, SKILL-06]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "布局违规不静默：`skills/a/b/SKILL.md`（相对深度 3）被跳过，且对应条目不进 `_cache.skills`，同时 `_cache.diagnostics` 中出现 `code === 'realm_layout_violation'` 的诊断，其 message 同时含实际的 `filePath` 与正确的目标路径建议（`<dir>/<name>/SKILL.md`）"
    - "契约布局（相对深度恰为 2）的技能全部保留：`skills/alpha/SKILL.md` / `managed-skills/beta/SKILL.md` 一律进集合，深度过滤不误伤"
    - "名称权威：`skills/evil/SKILL.md` 声明 `name: find-skills` 或任何与目录名不符的名字时，最终 `skill.name === 'evil'`（等于 `path.basename(path.dirname(skill.filePath))`），且伴随一条 `code === 'realm_name_rewritten'` 的诊断；技能本身**不被丢弃**（命名不规范的合法技能仍然可用）"
    - "重写后 `skill.name` 与 `path.basename(path.dirname(skill.filePath))` 对集合内**每一条**都恒相等（这条不变式是 `/skill:name` 解析确定性的前提，也是遮蔽判定无歧义的前提）"
    - "同名遮蔽（SKILL-05）：user 与 managed 出现同名技能时，user 版正常进入集合且进入 prompt；managed 版 `shadowed === true` 且 `shadowedBy === 'user'`，**仍保留在 `_cache.skills` 内**（不剔除），并带一条 `code === 'realm_shadowed'` 的诊断"
    - "去重发生在注入之前：`buildSkillsPrompt()` 对同名技能只产出**一条** `<skill>` 条目（SDK `formatSkillsForSystemPrompt` 自身无 dedup，必须由 Realm 在调用前完成）"
    - "去重与名称权威逻辑只在主进程一份（`ai-skills-manager.js`）；renderer 侧不得存在第二份优先级表或目录名重写逻辑"
  artifacts:
    - "ai-skills-manager.js：`inContractLayout(scannedDir, filePath)` 纯函数（相对深度 === 2）"
    - "ai-skills-manager.js：缓存条目形状扩展为 `{ skill, source, diagnostics: [], shadowed?: true, shadowedBy?: 'user'|'managed' }`"
    - "ai-skills-manager.js：`enforceDirNameAuthority(entry)` 与 `applyShadowing(entries)` 两个内部纯函数"
    - "新诊断码：`realm_layout_violation`（SKILL-06）、`realm_name_rewritten`（SKILL-06 / D-08）、`realm_shadowed`（SKILL-05 / D-06）"
    - "tests/test-ai-skills.js：新增 `describe('契约布局过滤（SKILL-06）')`、`describe('名称权威（SKILL-06 / D-08）')`、`describe('去重与遮蔽（SKILL-05）')` 三组断言"
  key_links:
    - "`loadSourcedSkills` 的输入顺序（managed → user）→ `applyShadowing` 的「后到者胜出」判定 → managed 版标 `shadowed` / `shadowedBy` → `buildSkillsPrompt()` 的 `.filter((e) => !e.shadowed)` → prompt 段只出一条同名条目"
    - "`skill.filePath` → `path.dirname` → `path.basename` → 目录名 → 重写 `skill.name`（D-08 的整条链路，`Skill` **没有** `location` 字段，`<location>` 只是 XML 标签名）"
    - "`inContractLayout` 只作用于**加载后**的 `filePath` 深度判定；根层污染由 46-01 的 `createSkillsEnv.listDir` 在 SDK 遍历前拦 —— 两条防护互补，不可互相替代"
  prohibitions:
    - statement: "遮蔽技能不得从 `_cache.skills` 中移除，也不得只留诊断而丢数据 —— Phase 48 需要渲染来源徽标与「被遮蔽的同名技能可见」，Phase 50 需要列表与诊断"
      status: unresolved
      verification: judgment
      reason: "D-06 标注为 costly：改为「剔除只留诊断」需 48 与 50 两个阶段同时改数据源与展示层"
    - statement: "去重 / 优先级 / 名称权威逻辑不得在 renderer 侧复制第二份，也不得由两处 Agent 创建点各自计算"
      status: unresolved
      verification: judgment
      reason: "ARCHITECTURE Anti-Pattern 5 + AGENTS.md 的 shortcut-manager 教训（「不要在 renderer 再写一份默认表」）；多份优先级表会静默分叉"
    - statement: "不得丢弃 SDK 返回的 `diagnostics`，也不得因单个技能失败而整批 throw 或清空技能集"
      status: unresolved
      verification: judgment
      reason: "SKILL-06 禁止静默失败；SDK 的失效面全部是 warning（无 throw），丢掉诊断等于把「装了但没生效」变成不可自查"
---

<objective>
在 46-01 已验证的纵切上加「加载后语义层」：把 SDK 的递归扫描结果收敛为 Realm 的契约布局，把名称的权威从 frontmatter 收回到目录名（杜绝冒名），并把同名来源冲突显式化为「遮蔽」而不是静默双份注入。

Purpose: 46-01 之后链路是通的，但三个语义空洞还在：① SDK 无深度上限，从 GitHub 拷来的多一层目录包会加载出一个「不该存在」的技能；② `validateName` 只产 warning，`skills/evil/SKILL.md` 声明 `name: find-skills` 可合法冒名（P3 前半，S1 门禁）；③ SDK 不做 name 去重，user 与 managed 同名会产出两条 `<skill>`，模型看到两个同名技能且不知道信谁。这三件事都属于「不静默、可解释」的同一族。

Output: `ai-skills-manager.js` 的三层加载后管线（布局过滤 / 名称权威重写 / 遮蔽去重）与对应诊断；`tests/test-ai-skills.js` 三组新断言。

**tracer 说明**：本阶段的唯一 `type="tracer"` 任务在 46-01（Task 1）。本计划是从那条已验证切面扩展的语义层，不含独立 tracer（本计划无跨层新链路，只细化 `refreshSkills` 内部管线）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-prompt/46-CONTEXT.md
@.planning/phases/46-prompt/46-RESEARCH.md
@.planning/phases/46-prompt/46-PATTERNS.md
@.planning/phases/46-prompt/46-VALIDATION.md

@ai-skills-manager.js
@tests/test-ai-skills.js
</context>

<artifacts_this_phase_produces>
本计划（46-02）新增的符号：

- `ai-skills-manager.js`：`inContractLayout(scannedDir, filePath)`（内部纯函数）
- `ai-skills-manager.js`：`enforceDirNameAuthority(entry)`（内部纯函数，就地重写 `entry.skill.name`）
- `ai-skills-manager.js`：`applyShadowing(entries)`（内部纯函数，返回带 `shadowed` / `shadowedBy` 的条目数组）
- 缓存条目字段：`diagnostics`（数组）、`shadowed`（布尔，仅败者）、`shadowedBy`（`'user' | 'managed'`，仅败者）
- 诊断 `code` 取值：`realm_layout_violation`、`realm_name_rewritten`、`realm_shadowed`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 契约布局深度过滤 + `realm_layout_violation` 诊断</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - ai-skills-manager.js 当前全文（46-01 后的 `refreshSkills` / `createSkillsEnv` / `_cache` / 导出面 —— 本任务的插入点）
    - 46-RESEARCH.md §D（递归深度无上限的源码证据 `skills.js:115-119`；`path.relative` 深度判定的实测结论：「对 `skills/`，`alpha/SKILL.md` → 保留，`README.md` → 丢弃，`deep/nested/SKILL.md` → 丢弃」）、§Pattern 2（两层叠加不可互相替代）、§Pitfall 3、§Open Question 2（严重度选 `warning` + `code: 'realm_layout_violation'` 的推荐理由 —— 属 D-05 第 1 层「单技能失败 = 正常态」）
    - 46-PATTERNS.md §1c（D-05 两层降级语义与「与 analog 相反，注意不要照抄」的告警）、§1d（诊断形状映射与 `realm_` 前缀不重叠要求）
    - 46-CONTEXT.md D-05 / D-07
    - tests/test-ai-skills.js 当前全文（既有 describe 分组与 `withTempRoot` 用法）
  </read_first>
  <action>
    1. `ai-skills-manager.js` 新增内部纯函数 `function inContractLayout(scannedDir, filePath)`：`path.relative(scannedDir, filePath).split(path.sep).filter(Boolean).length === 2` —— 即 `<scannedDir>/<name>/SKILL.md` 恰两段。JSDoc 注明「这是**加载后**的兜底过滤，不能替代 46-01 `createSkillsEnv.listDir` 的遍历前收窄：根层 `SKILL.md` 一旦短路整组，事后过滤救不回来」。
    2. `refreshSkills` 在 `loadSourcedSkills` 返回之后、写 `_cache.skills` 之前插入深度过滤：对每个条目取它的 `filePath` 所属扫描根（`opts.rootDirs` 中作为它前缀的那一项），用 `inContractLayout(root, entry.skill.filePath)` 判定；不通过则**不进集合**，并把一条 `{ level: 'warning', code: 'realm_layout_violation', message: \`技能 "${name}" 的布局不符合契约：实际 ${filePath}；技能必须放在 <扫描根>/<技能名>/SKILL.md，不能有中间层目录\`, path: filePath, source }` 追加进 `_cache.diagnostics`（message 必须同时含**实际路径**与**正确目标路径建议**）。
    3. 同一步建立内部辅助 `pushEntryDiag(entry, diag)`：把诊断同时写进「条目级 `entry.diagnostics[]`」与「模块级 `_cache.diagnostics`」。**口径声明（写进 JSDoc）**：D-07 所说的 `skill.diagnostics[]` 落在**缓存条目**上（条目即 D-07 语境中的「技能」）；`Skill` 本体保持 SDK 五字段形状不被注入私有字段（仅 `name` 按 D-08 在 Task 2 重写），避免向 SDK 类型污染。
    4. `tests/test-ai-skills.js` 新增 `describe('契约布局过滤（SKILL-06）')`：
       - `skills/a/b/SKILL.md` → 集合中不含该技能，且 `_cache.diagnostics` 出现 `realm_layout_violation`，其 `message` 含该技能的实际路径片段与 `SKILL.md` 目标建议。
       - `skills/alpha/SKILL.md` 与 `managed-skills/beta/SKILL.md`（各一层）→ 两者都在集合内（不误伤契约布局）。
       - 空目录 / 只有一层目录但无 `SKILL.md` → 不产生任何 `realm_layout_violation`（不误报）。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `契约布局过滤（SKILL-06）` 分组内任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`skills/a/b/SKILL.md` 不进 `_cache.skills`，且 `_cache.diagnostics` 含 `code === 'realm_layout_violation'` 条目
    - 行为断言：该诊断的 `message` 同时含实际路径片段与 `SKILL.md` 目标路径建议（可操作，不停留在「加载失败」）
    - 行为断言：`skills/alpha/SKILL.md` 与 `managed-skills/beta/SKILL.md` 均在集合内（深度过滤不误伤契约布局）
    - 源码断言：`ai-skills-manager.js` 含 `function inContractLayout(`，且其判定为 `=== 2`（相对段数，非字符数或绝对段数）
    - 源码断言：诊断 `code` 以 `realm_` 前缀，且不与 SDK 的 5 个枚举值（`file_info_failed` / `list_failed` / `read_failed` / `parse_failed` / `invalid_metadata`）重名
  </acceptance_criteria>
  <done>深嵌套技能不再静默混进技能集：被跳过且给出「实际在哪 / 应该放哪」的可读诊断，而契约布局的技能一个不少。</done>
</task>

<task type="auto">
  <name>Task 2: 目录名为权威的名称重写（D-08，杜绝冒名）+ `realm_name_rewritten` 诊断</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - ai-skills-manager.js 当前全文（Task 1 后的深度过滤与 `pushEntryDiag` 落点）
    - 46-RESEARCH.md §B（`name = frontmatterName || parentDirName` 的源码位置 `skills.js:219`；`validateName` 只产 warning 不拒绝 `:237-251`；`Skill` 五字段表 —— **路径字段是 `filePath`**）、§C（`name "seed" does not match parent directory "evil"` 实测原文）、§State of the Art（「技能名回落为父目录名 → 以目录名为权威重写」一行）
    - 46-CONTEXT.md D-08 全文（含理由：`skills/evil/SKILL.md` 可合法冒名；重写既杜绝冒名又不丢弃命名不规范的合法技能，并保证 `skill.name` 与目录名恒一致使 `/skill:name` 解析确定）
    - 46-PATTERNS.md §1d（Realm 自建诊断码用 `realm_` 前缀）
    - **勘误提醒**：CONTEXT.md 原文写作 `dirname(skill.location)` —— `Skill` **没有** `location` 字段，正确写法是 `path.dirname(skill.filePath)`
  </read_first>
  <action>
    1. `ai-skills-manager.js` 新增内部纯函数 `function enforceDirNameAuthority(entry)`：`const dirName = path.basename(path.dirname(entry.skill.filePath));`；若 `entry.skill.name !== dirName`，先 `pushEntryDiag(entry, { level: 'warning', code: 'realm_name_rewritten', message: \`frontmatter name "${entry.skill.name}" 与目录名 "${dirName}" 不一致，已按目录名生效\`, path: entry.skill.filePath, source: entry.source })`，再 **就地重写** `entry.skill.name = dirName`。`entry.skill` 是本模块独占对象（来自 `loadSourcedSkills` 的新返回），就地改安全。
    2. 在 `refreshSkills` 的管线中把该函数作用在**深度过滤之后、遮蔽判定之前**（顺序理由：重写后单目录内 name 天然唯一，遮蔽判定才无歧义）。
    3. JSDoc 标注理由（照 D-08 原文）：`validateName` 只产 warning 且 SDK 的名称是 `frontmatterName || parentDirName`，于是 `skills/evil/SKILL.md` 声明 `name: find-skills` 可合法冒名（P3 前半，S1 门禁）；重写既杜绝冒名、又不丢弃命名不规范的合法技能（GitHub 导入常见），并保证「集合内每一条的 `skill.name` 恒等于其所在目录名」这条不变式，使 `/skill:name` 解析确定。
    4. `tests/test-ai-skills.js` 新增 `describe('名称权威（SKILL-06 / D-08）')`：
       - **冒名拒绝**：`skills/evil/SKILL.md` 的 frontmatter 声明 `name: find-skills` 与合法 `description` → 集合中该条目的 `skill.name === 'evil'`；`_cache.diagnostics` 含 `realm_name_rewritten`。
       - **不变式**：对集合内每一条断言 `entry.skill.name === path.basename(path.dirname(entry.skill.filePath))`（用 `for` 循环 + `assert.strictEqual`，失败时给出具体条目）。
       - **不丢弃**：命名不一致的技能仍出现在 `buildSkillsPrompt()` 的输出里（未被静默移除）。
       - **无误报**：frontmatter `name` 与目录名一致的技能**不产生** `realm_name_rewritten` 诊断。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `名称权威（SKILL-06 / D-08）` 分组内任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：`skills/evil/SKILL.md` 声明 `name: find-skills` 时，最终 `skill.name === 'evil'`，且存在 `code === 'realm_name_rewritten'` 的诊断
    - 行为断言：集合内每一条都满足 `skill.name === path.basename(path.dirname(skill.filePath))`（系统性不变式，非单例抽查）
    - 行为断言：被重写的技能仍出现在 `buildSkillsPrompt()` 输出中（重写 ≠ 丢弃）
    - 行为断言：`name` 与目录名一致的技能不产生 `realm_name_rewritten`（不误报）
    - 源码断言：`ai-skills-manager.js` 使用 `path.dirname(entry.skill.filePath)` 取目录名，源码中**不出现** `skill.location` 或 `entry.skill.location`
  </acceptance_criteria>
  <done>被目录名重写的技能不能再冒名顶替别的技能；命名不规范的合法技能仍被保留并给出可读诊断；「name ≡ 目录名」成为集合级不变式。</done>
</task>

<task type="auto">
  <name>Task 3: 同名遮蔽去重（user &gt; managed，败者保留并标注）</name>
  <files>ai-skills-manager.js, tests/test-ai-skills.js</files>
  <reversibility rating="costly">D-06：`shadowed` / `shadowedBy` 被 Phase 48（来源徽标 /「被遮蔽的同名技能可见」）与 Phase 50（列表与诊断）消费；改为「剔除只留诊断」需两个阶段同时改数据源与展示层。</reversibility>
  <read_first>
    - ai-skills-manager.js 当前全文（Task 1/2 之后的管线：加载 → 深度过滤 → 名称重写 → 写 `_cache`）
    - 46-RESEARCH.md §B（「SDK **不做** name 去重」实测：同名 `good` 同时出现在 user 与 managed 结果中，`formatSkillsForSystemPrompt` 无条件各生成一条 `<skill>`，`system-prompt.js:12-18` 无 dedup）、§Code Examples §同名遮蔽（`applyShadowing` 的形状参考）、§SDK 契约 A（「每个源的加载顺序按 `entry.name.localeCompare` 排序；**该顺序不保证 user 优先**，Realm 必须自己定序」）
    - 46-CONTEXT.md D-06 全文（含「不进 prompt、不占预算、不参与 `/skill:` 解析，但保留在数据层」与 costly 标注）与 <code_context>「优先级逻辑只在主进程一份（renderer 只渲染）」
    - 46-PATTERNS.md §1c / §1d、§S1（跨文件契约）
    - 46-VALIDATION.md 46-02-03 / 46-02-04 两行断言（4 计划重编号后的本计划行；**不断言集合顺序** —— 顺序权威在 46-03 Task 2）
  </read_first>
  <action>
    **跨计划口径（先读，避免与 46-03 冲突）**：本任务（及本计划全部三个任务）**不对 `getSkillsSnapshot().skills` 的条目顺序做任何断言**。该可观测顺序的**唯一权威是 46-03 Task 2 的 `bySkillPriority`**（它按 `user 来源 > 可自动激活 > name 码点序` 重排 `_cache.skills` 并写回）。本计划只断言「`loadSourcedSkills` 的**输入顺序** → 遮蔽胜负」这条映射，以及遮蔽数据的完整性；顺序相关的可观测行为一律留给 46-03 断言。

    1. `ai-skills-manager.js` 新增内部纯函数 `function applyShadowing(entries)`：输入已按「managed 先、user 后」顺序排列的条目数组，输出同长度数组（**不剔除任何条目**）。实现：维护 `winnerByName` Map；遍历时若当前 `entry.skill.name` 已存在胜者，则把已入队的胜者取出标 `shadowed = true`、`shadowedBy = <当前条目的 source>`（当前条目即新胜者），并给败者 `pushEntryDiag(loser, { level: 'warning', code: 'realm_shadowed', message: \`同名技能被 ${current.source} 来源的 "${name}" 遮蔽（败者文件：${loser.skill.filePath}）\`, path: loser.skill.filePath, source: loser.source })`；随后把当前条目更新为胜者并 push。JSDoc 注明「后到者胜出 —— 输入顺序（managed → user）已经编码了优先级，本函数不重新定义优先级，只消费顺序」。
    2. 在 `refreshSkills` 的管线中把 `applyShadowing` 放在名称重写之后、写 `_cache` 之前。
    3. `_cache.promptBlock` 的组装加一条过滤：`_cache.skills.filter((e) => !e.shadowed && e.skill.disableModelInvocation !== true).map((e) => e.skill)` —— **遮蔽条目不进 prompt**（D-06）。注意：预算截断（46-03）也必须基于同一条过滤后的集合，不得让遮蔽条目占预算。
    4. `tests/test-ai-skills.js` 新增 `describe('去重与遮蔽（SKILL-05）')`：
       - 在 `skills/` 与 `managed-skills/` 下各放一个同名 `good/SKILL.md`（description 不同以便区分）→ 断言 user 版 `shadowed` 不为真且出现在 `buildSkillsPrompt()` 输出中；managed 版 `shadowed === true` 且 `shadowedBy === 'user'`；**managed 版仍在 `_cache.skills` 内**（`getSkillsSnapshot().skills.length === 2`）。
       - 断言 `buildSkillsPrompt()` 中 `<skill>` 条目数等于集合内未遮蔽技能数（同名条目**只有一条**）。
       - 断言 managed 版带 `code === 'realm_shadowed'` 的诊断，且该诊断所在条目的 `source === 'managed'`。
       - 断言**优先级方向**（顺序无关地表达「输入顺序即优先级编码」）：`shadowed === true` 的那一条，其 `skill.filePath` **必须落在 `managed-skills/` 之下**（即「user 目录里的那一份永不被遮蔽」）。这条断言把「谁先谁后」钉在**文件路径**上而非集合下标上：任何人把 `ai-manager.js` 里 `rootDirs` 的 `[getManagedSkillsDir(), getSkillsDir()]` 顺序调换（或让 `applyShadowing` 不再按输入顺序定胜负），被遮蔽的就会变成 `skills/` 下那份，断言立刻转红。
       - **本任务不断言 `getSkillsSnapshot().skills` 的条目顺序**（该顺序由 46-03 Task 2 的 `bySkillPriority` 唯一规定，见本条开头的跨计划口径）；同理，本任务也不把「集合内条目顺序」当作遮蔽判定正确性的证据。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `去重与遮蔽（SKILL-05）` 分组内任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：user 与 managed 同名时 user 版进 prompt、managed 版 `shadowed === true` 且 `shadowedBy === 'user'`
    - 行为断言：managed 版**仍在** `getSkillsSnapshot().skills` 内（长度 2，不剔除）
    - 行为断言：`buildSkillsPrompt()` 中同名技能只产出一条 `<skill>`（去重发生在注入之前）
    - 行为断言：败者条目带 `code === 'realm_shadowed'` 诊断，且该条目 `source === 'managed'`
    - 行为断言：被遮蔽条目的 `skill.filePath` 位于 `managed-skills/` 之下（优先级方向以文件路径为准，不以集合下标为准；顺序断言不属本计划 —— 见本任务开头的跨计划口径）
    - 源码断言：`ai-skills-manager.js` 的 `applyShadowing` 不删除数组元素（无 `splice` / `filter` 作用于输出集合），且 prompt 组装处含 `!e.shadowed` 过滤
  </acceptance_criteria>
  <done>同名技能不再双份出现在 system prompt（模型不必在两个同名技能间猜）；来源冲突对用户可见（败者带遮蔽标记与诊断），且数据层完整保留供 48/50 消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 技能目录内容（用户手放 / 未来 GitHub 导入）→ 加载管线 | 不可信：frontmatter `name` 可声明任意值，目录可任意嵌套 |
| `_cache.skills` → prompt 段组装 | 遮蔽条目必须在此边界被拦住，否则同名双份进入每一轮请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-02-01 | Spoofing | `skills/evil/SKILL.md` 声明 `name: find-skills` 冒名顶替 | high | mitigate | `enforceDirNameAuthority` 以 `path.basename(path.dirname(skill.filePath))` 为权威重写 `skill.name` + `realm_name_rewritten` 诊断；测试断言「集合内每一条 name ≡ 目录名」的系统性不变式（P3 前半，S1 门禁在本阶段闭合） |
| T-46-02-02 | Tampering | 深嵌套技能被静默加载进模型可见清单 | medium | mitigate | `inContractLayout` 相对深度 === 2 过滤 + `realm_layout_violation` 诊断（含实际路径与目标路径建议），属 D-05 第 1 层「正常态跳过」，不阻断其余技能 |
| T-46-02-03 | Repudiation | 同名冲突静默双份注入，用户无从判断哪个生效 | high | mitigate | `applyShadowing` 后到者胜出 + 败者标 `shadowed` / `shadowedBy` + `realm_shadowed` 诊断；prompt 组装处 `!e.shadowed` 过滤保证同名只出一条 `<skill>` |
| T-46-02-04 | Denial of Service | 遮蔽条目占 prompt 预算，挤压真实可用技能 | low | mitigate | 遮蔽条目在 `_cache.promptBlock` 组装前被过滤；46-03 的预算截断复用同一过滤后集合 |
| T-46-02-SC | Tampering | npm / 依赖安装 | high | mitigate | 本计划零新增依赖；不执行任何安装命令（`package.json` 原样），无包合法性 gate 触发；继续遵守「不得直接引入 YAML / ignore 解析库」的依赖纪律 |
</threat_model>

<flagged_assumptions>
以下条目来自 `SPECLESS_FALLBACK` 的 edge-probe 文本派生报告（`status: unresolved` / `category: unclassified`），按 §A 不得自动 resolve，显式登记：

- **SKILL-05**（unclassified）：遮蔽语义只覆盖 user / managed **两层** tier（locked 设计，不引入 oh-my-pi 的 7 层 provider priority）。若未来出现第三来源，遮蔽判定与诊断需要重新设计；本阶段无此面。
- **SKILL-06**（`empty` 探针，本轮**已 resolve 为 truth**）：无诊断时为 `[]` 而非 `undefined` —— 见 46-03 `must_haves.truths`。
- **SKILL-06**（`encoding` 探针，本轮**已 resolve 为 truth**）：SDK `type` → Realm `level` 的显式映射表 —— 见 46-03 `must_haves.truths`。
- 完整的 11 行结清表见 `46-01-PLAN.md` 的 `<edge_coverage_ledger>`（等式 11 = 4 truths + 7 flagged assumptions）。
</flagged_assumptions>

<verification>
1. `node tests/test-ai-skills.js` 退出 0（本计划三任务 + 46-01 全部断言，累计约 30 例）
2. `node tests/test-agent-workspace.js` 退出 0（回归）
3. `node --check ai-skills-manager.js` 通过
4. 源码 gate：`ai-skills-manager.js` 中不存在 `skill.location`；三处 Realm 诊断码均以 `realm_` 前缀且与 SDK 五枚举不重叠
</verification>

<success_criteria>
- [ ] 深嵌套技能被跳过并给出「实际在哪 / 应放哪」的诊断
- [ ] 契约布局（`<dir>/<name>/SKILL.md`）的技能一个不少
- [ ] 集合内每一条的 `skill.name` 恒等于其所在目录名（D-08 不变式）
- [ ] 同名冲突：user 胜出、managed 保留并标注遮蔽、prompt 段同名只出一条
- [ ] 三类新诊断码均为 `realm_` 前缀，且每条被跳过的技能都有对应诊断（无静默）
</success_criteria>

<output>
Create `.planning/phases/46-prompt/46-02-SUMMARY.md` when done
</output>
