---
phase: 46-prompt
plan: 4
type: execute
wave: 4
depends_on: [46-03]
files_modified:
  - ai-manager.js
  - tests/test-ai-skills.js
  - docs/product/ai-skills.md
autonomous: true
requirements: [SKILL-03, SKILL-04, DOC-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "技能集变更后**不重建 Agent** 即生效：`agent.state.systemPrompt` 被改写，下一轮 `prompt()` 的 `createContextSnapshot().systemPrompt` 反映新值（SDK 契约经最小真实 `Agent` 实例实测：`state.systemPrompt` 是可写普通属性，`get state()` 返回内部对象本体，`createContextSnapshot()` 每轮重读）"
    - "空闲时改写立即落地；处理中（`isProcessing` 或 `agent.state.isStreaming`）**不**在流式回调内部改 prompt，而是置 `_skillsPromptDirty = true`，在 `promptWithContext()` 的 idle 边界（`ai-manager.js:1209` 的 `this.isProcessing = false;` 之后）补刷一次"
    - "prompt 无变化时**不写**：`buildSystemPrompt()` 结果与 `agent.state.systemPrompt` 逐字符相同时直接返回，不触碰 `agent.state`（保 provider 前缀缓存）"
    - "变更经跨窗口广播：`syncAgentSystemPrompt()` 在真正改写了 prompt 之后调用 `windowManager.broadcast('skills:changed')`（本阶段只发事件，消费方在 Phase 48/50）"
    - "P8 失效链机制断言：`ai-manager.js` 中**每一个** `new Agent(` 出现点之前 60 行内都存在一次 `refreshSkills(` 调用 —— 这是 P8 门禁唯一可自动化捕捉「漏接线」的手段，漏一处即测试失败"
    - "`_recreateAgent()` 在构造 Agent 之前无条件 `await refreshSkills(...)`（D-04）—— 它同时是 P8 第 6 条失效路径（模型经 `write` / `bash` 工具直接改写 `skills/foo/SKILL.md`，不经过任何 Realm 管理器、无事件可挂）的**唯一**自动兜底：每次切会话重建 Agent 都重扫一次两个目录"
    - "`docs/product/ai-skills.md` 存在且含六节骨架（能力 / 双目录 / 优先级 / 限额 / 沙箱边界 / 已知限制），并在维护约定块中声明它是本功能的产品说明权威文档"
    - "文档如实写出三条诚实边界：① 技能的 `description` 会进入**每一次请求**的 system prompt（`escapeXml` 只做字符转义不做内容审查）→ 不要导入来源不明的技能；② 「AI 不可删改内置技能」是**工具层不变式，不是沙箱不变式**（沙箱只有一个 root）；③ 已知限制：`/compact` 不保留技能正文、`allowed-tools` 在当前 SDK **不存在该字段**、同名技能共享禁用状态"
  artifacts:
    - "ai-manager.js：实例方法 `syncAgentSystemPrompt()`；实例字段 `_skillsPromptDirty`（构造器初始化 `false`）"
    - "ai-manager.js：`_recreateAgent()` 内 Agent 构造前的 `await refreshSkills(...)` 调用"
    - "ai-manager.js：`promptWithContext()` 的 idle 边界补刷块（`:1209` 之后）"
    - "docs/product/ai-skills.md（新增）：维护约定块 + 六节骨架 + 三条诚实边界 + 与 ai-agent-workspace.md 的互引"
    - "tests/test-ai-skills.js：新增 `describe('Agent prompt 回写（SKILL-04）')`、`describe('P8 失效链机制断言（源码扫描）')` 两组断言"
    - "IPC / 事件通道名：`skills:changed`（`windowManager.broadcast` 的 channel，本阶段只发不消费）"
  key_links:
    - "`syncAgentSystemPrompt()` → `refreshSkills(sandboxEnv, {disabled, rootDirs})` → `buildSystemPrompt()` → 与 `agent.state.systemPrompt` 比对 → 不等则 `= next` → `windowManager.broadcast('skills:changed')`"
    - "`_recreateAgent()`（4 个调用点 `:942` `:1051` `:2025` `:2116`）→ 构造前 `await refreshSkills` → 覆盖「bash/write 直改磁盘」这条无事件路径（P8 第 6 条）"
    - "`promptWithContext():1209` 的 `isProcessing = false` → 若 `_skillsPromptDirty` 则补刷一次 → 忙时收到的变更不会丢失"
  prohibitions:
    - statement: "技能集变更不得通过重建 Agent 落地 —— 必须改写 `agent.state.systemPrompt` 以保留 `state.messages` 的引用链与对话上下文"
      status: unresolved
      verification: judgment
      reason: "D-03 / SKILL-04 明文「不重建 Agent」；重建会丢失当前对话的上下文连续性（与 G-42-4 相邻的一类失效）"
    - statement: "产品文档不得暗示 `allowed-tools` 在当前运行时被强制执行，也不得暗示技能目录构成额外权限边界"
      status: unresolved
      verification: judgment
      reason: "SDK 0.84.3 的 `Skill` 接口根本不存在该字段（探针实测 `Object.keys` 恰五项），任何展示都是虚假安全感（P6 / O3）；沙箱只有一个 root，模型可直接 write 到 managed-skills，这是工具层不变式而非沙箱不变式"
    - statement: "不得在流式回调内部直接改写 system prompt，也不得在 `isProcessing` 为真时改动 Agent 状态"
      status: unresolved
      verification: judgment
      reason: "`createContextSnapshot()` 已在当前轮开始时取过值，流式进行中改写对当前轮无效且会让状态机难以推理；应走 `_skillsPromptDirty` + idle 补刷"
---

<objective>
闭合 P8 失效链在本阶段可触发的三条路径，把技能集变更变成「无需重启、无需重建 Agent、下一轮即生效、且跨窗口可见」，并交付 DOC-01 的产品文档骨架。

Purpose: 到此为止技能集是「读得到、算得对」，但仍有两个空洞：① `_recreateAgent()` 尚未重扫（D-04 的兜底没接上），意味着模型用 `write`/`bash` 直接改 `skills/foo/SKILL.md` 后，磁盘与内存快照会分叉；② 没有任何「刷新 Agent prompt」的入口，Phase 48/49/50 拿到技能集后无处落地。同时 DOC-01 必须在此收口 —— 文档要描述的是**已交付**的行为，所以它排在最后一个 wave。

Output: `ai-manager.js` 的 `syncAgentSystemPrompt()` + 忙时置脏 / idle 补刷 + 跨窗口广播；`_recreateAgent()` 前的无条件重扫；P8 机制断言；`docs/product/ai-skills.md` 六节骨架。

**tracer 说明**：本阶段的唯一 `type="tracer"` 任务在 46-01（Task 1）。本计划把那条链路接到「Agent 实例」与「文档」两端，不新增跨层路径。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-prompt/46-CONTEXT.md
@.planning/phases/46-prompt/46-RESEARCH.md
@.planning/phases/46-prompt/46-PATTERNS.md
@.planning/phases/46-prompt/46-VALIDATION.md

@ai-manager.js
@ai-skills-manager.js
@window-manager.js
@docs/product/ai-agent-workspace.md
@docs/product/ai-chat-attachments.md
@tests/test-ai-skills.js
</context>

<artifacts_this_phase_produces>
本计划（46-04）新增的符号：

- `ai-manager.js`：`async syncAgentSystemPrompt()`
- `ai-manager.js`：实例字段 `_skillsPromptDirty`（构造器 `:665` 附近初始化）
- `ai-manager.js`：`_recreateAgent()` 内的 `await getAiSkillsManagerLazy().refreshSkills(...)` 调用
- 广播通道名：`skills:changed`
- `docs/product/ai-skills.md`（新增文件）
</artifacts_this_phase_produces>

<p8_gate_mapping>
## P8 门禁映射表（诚实版，不得声称 6 点全覆盖）

ROADMAP 把 **P8（S2，阻断门禁）技能缓存失效链 6 个触发点全覆盖** 定为本阶段门禁。**本阶段只存在其中 3 条**，其余 3 条的写路径尚未落地（属 48/49/50/51）。下表是逐点结清，不是「已全覆盖」的宣称：

| # | 触发点（ROADMAP 原文） | 本阶段是否存在 | 落点 | 交付阶段 |
|---|----------------------|---------------|------|---------|
| 1 | 两处 Agent 创建点（`init()`） | ✅ 存在 | 46-01 Task 1（`:808` 之后、`:821` 之前无条件 `await refreshSkills`） | 46 |
| 2 | 两处 Agent 创建点（`_recreateAgent()`） | ✅ 存在 | 46-04 Task 2（`:2432` 内、`:2453` 之前无条件 `await refreshSkills`，经 4 个调用点 `:942` `:1051` `:2025` `:2116` 生效） | 46 |
| 3 | `/` 面板列表变更 | ❌ 不存在（面板属 48） | —— 交付项：Phase 48 变更技能集后**必须**调用 `aiManager.syncAgentSystemPrompt()` | 48 |
| 4 | 设置页导入 / 卸载 | ❌ 不存在（设置页属 50、导入属 51） | —— 交付项：Phase 50 启停/卸载后、Phase 51 导入落盘后**必须**调用 `syncAgentSystemPrompt()` | 50 / 51 |
| 5 | `manage_skill` 三动作（create / update / delete） | ❌ 不存在（属 49） | —— 交付项：Phase 49 三个动作成功后**必须**调用 `syncAgentSystemPrompt()`（MGMT-05 已明文要求） | 49 |
| 6 | bash / write 工具直改 `skills/`（无事件可挂） | ✅ 由兜底覆盖 | 46-01 Task 1 + 46-04 Task 2：**每次 Agent 重建都无条件重扫两个目录**（D-04）。这是唯一能自动覆盖该路径的机制 —— 模型用 `write`/`bash` 改 `SKILL.md` 不经过任何 Realm 管理器，没有事件可挂 | 46 |

**本阶段覆盖率：3 / 6。** 本计划**不**把 48/49/50 的写路径提前到 46（那会越出 CONTEXT.md 的 scope boundary，导致同一份校验/写入逻辑出现第二份实现）。**唯一可自动化的漏接线检测**是 46-04 Task 2 的源码扫描断言：`ai-manager.js` 中每一个 `new Agent(` 之前 60 行内必须存在 `refreshSkills(` —— 未来 48/49/50/51 若新增第三处 Agent 创建点而漏掉重扫，这条断言会转红。

**后续阶段交接清单（写进各阶段 PLAN 的显式交付项，不靠记忆）：**
- Phase 48：`/skill:name` 与 `/` 面板不改变技能集（只读 `getSkillsSnapshot()`），但 `disable-model-invocation` 的显式调用路径不得绕过 `disabled` / `shadowed` 两个过滤位。
- Phase 49：`manage_skill` 的 create / update / delete 成功后调用 `syncAgentSystemPrompt()`。
- Phase 50：启用 / 禁用 / 卸载成功后调用 `syncAgentSystemPrompt()`；列表与诊断读 `getSkillsSnapshot()`。
- Phase 51：导入落盘成功后调用 `syncAgentSystemPrompt()`。
</p8_gate_mapping>

<tasks>

<task type="auto">
  <name>Task 1: `syncAgentSystemPrompt()` —— 不重建 Agent 的 prompt 回写 + 忙时置脏 / idle 补刷 + 跨窗口广播</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <read_first>
    - ai-manager.js:640-670（构造器：`:653` `this.isProcessing = false;`、`:665` `this.sandboxEnv = null;` —— `_skillsPromptDirty` 的初始化落点）、:1038-1223（`promptWithContext()` 全文：`:1094` 早退复位、`:1209` 成功完成的 `this.isProcessing = false;`、`:1213` 错误分支复位 —— idle 边界候选）、:1339-1348（`abort()` 的复位）、:2415-2425（`_cleanupCurrentAgent()`）、:3010-3020（`windowManager.broadcast('bookmarks-bar:refresh')` 的调用先例）、:5505-5515（configStore 注入式读取先例）、:20-30（顶层 `require('./window-manager')`）
    - ai-manager.js 当前 `buildSystemPrompt()`（46-01 后的第 4 段条件追加版本）
    - 46-RESEARCH.md §E（`agent.state.systemPrompt` 可写性与每轮重读的三处行号证据 `agent.js:27-30` / `:150-157` / `:280-286`；「生效边界是下一次 `prompt()` 调用，流式进行中改写不会影响当前轮」的唯一注意事项）、§Code Examples §`syncAgentSystemPrompt()`（完整形状 + `[VERIFIED: ai-manager.js:25]` / `[VERIFIED: window-manager.js:310]`）
    - 46-PATTERNS.md §3d（形状依据 + `isProcessing` 状态位的全部复位点清单 + `_skillsPromptDirty` 补刷点的选定纪律：「不要在流式回调内部直接改 prompt」）、§S6（跨窗口广播）
    - window-manager.js:305-320（`broadcast(channel, ...args)` 实现：遍历 `windows`、`isDestroyed()` 守卫、`webContents.send`）
    - 46-CONTEXT.md D-03 全文
  </read_first>
  <action>
    1. `ai-manager.js`：构造器在 `:665`（`this.sandboxEnv = null;`）附近追加 `this._skillsPromptDirty = false;`，JSDoc 注明「忙时收到的技能集变更标记；由 idle 边界补刷，避免在流式回调内改 prompt」。
    2. `ai-manager.js`：新增实例方法 `async syncAgentSystemPrompt()`：
       - `if (!this.agent || !this.sandboxEnv) return;`（早退：Agent 未就绪或沙箱未建）
       - `await getAiSkillsManagerLazy().refreshSkills(this.sandboxEnv, { disabled: this.configStore ? this.configStore.get('settings.aiSkills.disabled', []) : [], rootDirs: [getAgentWorkspaceLazy().getManagedSkillsDir(), getAgentWorkspaceLazy().getSkillsDir()] });`
       - `if (this.isProcessing || (this.agent.state && this.agent.state.isStreaming)) { this._skillsPromptDirty = true; return; }`（忙时不改 prompt，置脏标记后返回）
       - `const next = buildSystemPrompt();` 与 `if (this.agent.state.systemPrompt === next) return;`（**无变化不动** —— 逐字符比对，保 provider 前缀缓存）
       - `this.agent.state.systemPrompt = next;`（**直接改写，不重建 Agent** —— D-03；保留 `state.messages` 的引用链）
       - `windowManager.broadcast('skills:changed');`（本阶段只发事件，消费方在 48/50）
       - JSDoc 记录技术要求：「`Agent.get state()` 返回 `_state` 本体（非快照），`systemPrompt` 是 `createMutableAgentState` 里赋的普通属性（不在 `get`/`set` 访问器列表内，`tools`/`messages` 才是），`createContextSnapshot()` 每次 `prompt()`/`promptContinue()` 都重读 —— 因此下一轮即生效」。
    3. `ai-manager.js`：在 `promptWithContext()` 的 `:1209`（成功路径的 `this.isProcessing = false;`）之后插入 idle 补刷块：`if (this._skillsPromptDirty) { this._skillsPromptDirty = false; try { await this.syncAgentSystemPrompt(); } catch (err) { console.warn('[Realm AI] 延迟刷新技能 prompt 失败:', err.message); } }`。**只在 `:1209` 一处补刷**（`:1213` 的错误路径不补，避免同一次运行双刷；`:2424` 的 `_cleanupCurrentAgent` 是拆解路径，同样不补）。
    4. `tests/test-ai-skills.js` 新增 `describe('Agent prompt 回写（SKILL-04）')`：
       - **SDK 契约行为断言**（不依赖 Electron）：`const { Agent } = await import('@earendil-works/pi-agent-core');` 构造最小真实实例 `new Agent({ initialState: { systemPrompt: 'A' }, streamFn: async () => { throw new Error('noop'); } })`；断言 `agent.createContextSnapshot().systemPrompt === 'A'`；执行 `agent.state.systemPrompt = 'B'` 后断言 `createContextSnapshot().systemPrompt === 'B'` —— 这就是 D-03「不重建 Agent 即可生效」的技术前提（本机实测可行）。
       - **源码断言**：`ai-manager.js` 含 `async syncAgentSystemPrompt(`、含 `this.agent.state.systemPrompt = next`、含 `windowManager.broadcast('skills:changed')`、含 `this._skillsPromptDirty = true` 与 `this._skillsPromptDirty = false;`、含无变化早退 `=== next) return;`。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `Agent prompt 回写（SKILL-04）` 分组内任一 assert 抛出（输出含 `AssertionError`）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：最小真实 `Agent` 实例上 `state.systemPrompt` 改写后 `createContextSnapshot().systemPrompt` 反映新值（D-03 的 SDK 契约成立）
    - 源码断言：`ai-manager.js` 定义 `async syncAgentSystemPrompt()`，其内同时含 `refreshSkills`、`this.agent.state.systemPrompt = next`、`windowManager.broadcast('skills:changed')`
    - 源码断言：忙时分支置 `this._skillsPromptDirty = true` 且**不**改写 `agent.state.systemPrompt`（两行在同一分支内）
    - 源码断言：无变化早退（`=== next) return;`）位于改写之前
    - 源码断言：`promptWithContext()` 内 `:1209` 之后存在唯一的 `_skillsPromptDirty` 补刷块
  </acceptance_criteria>
  <done>技能集变化后，下一条消息就能看到新技能清单，且当前对话的上下文不丢；忙时收到的变更不会丢失，空闲后自动补上；其他窗口收到 `skills:changed` 通知。</done>
</task>

<task type="auto">
  <name>Task 2: `_recreateAgent()` 前无条件重扫（D-04）+ P8 机制断言（源码扫描，漏接线即红）</name>
  <files>ai-manager.js, tests/test-ai-skills.js</files>
  <reversibility rating="costly">D-04：`_recreateAgent()` 前无条件重扫是 P8 第 6 条失效路径（bash/write 直改磁盘）的唯一自动兜底；降级为条件重扫会让门禁失去对该路径的覆盖，并重新引入「6 个触发点逐一挂刷新」的审计负担。</reversibility>
  <read_first>
    - ai-manager.js:2427-2473（`_recreateAgent()` 全文：`:2440` 动态 import Agent、`:2453` 第二个 `new Agent(` —— 插入点）、:800-835（`init()` 的第一处 `new Agent(`，46-01 已接好，本任务的行为须与之一致）、:935-945 / :1045-1055 / :2020-2030 / :2110-2120（`_recreateAgent()` 的四个调用点 —— 本任务一处改动即覆盖四处）
    - 46-RESEARCH.md §Open Question 4（**P8 门禁在 Phase 46 的可验证形态**：机制断言 ①`ai-skills-manager` 导出 `refreshSkills`/`buildSkillsPrompt`/`getSkillsSnapshot`；②**结构化测试断言 `ai-manager.js` 每一个 `new Agent(` 出现点之前都有一处 `refreshSkills` 调用（源码扫描，漏一处即失败 —— 这是 P8 唯一可自动化捕捉「漏接线」的手段）**；③行为测试：改磁盘 → `refreshSkills` → `buildSkillsPrompt()` 反映新集合。以及「门禁映射表写进 PLAN.md」的建议）、§Code Examples §两处 Agent 创建点的接线（第二处的精确插入位置与 `this.sandboxEnv` 全文件仅 4 处引用的核验）
    - 46-PATTERNS.md §3c（两处创建点的 delta 与两条约束：`rootDirs` 顺序不是装饰；`_recreateAgent` 失败时保留上次快照）
    - 46-VALIDATION.md 46-03-02 行（源码扫描断言）
    - 本计划 `must_haves` 与上方 `<p8_gate_mapping>`
  </read_first>
  <action>
    1. `ai-manager.js`：在 `_recreateAgent()` 内、`const { Agent } = await import('@earendil-works/pi-agent-core');`（`:2440`）之后、`this.agent = new Agent({`（`:2453`）之前插入与 `init()` **完全相同**的一段：
       `await getAiSkillsManagerLazy().refreshSkills(this.sandboxEnv, { disabled: this.configStore ? this.configStore.get('settings.aiSkills.disabled', []) : [], rootDirs: [getAgentWorkspaceLazy().getManagedSkillsDir(), getAgentWorkspaceLazy().getSkillsDir()] });`
       注释写明「D-04：每次重建都无条件重扫 —— 覆盖模型经 write/bash 工具直接改写 skills/foo/SKILL.md 这条无事件可挂的失效路径；`rootDirs` 顺序 managed → user 不是装饰（D-06 遮蔽判定依赖输入顺序）」。**两处调用必须逐字一致**（便于源码扫描与人工审计；不要抽公共函数造成两处形态分叉 —— 抽函数会让「新增创建点时是否调用了刷新」变得更难静态判断）。
    2. 失败语义：`_recreateAgent()` 既有 `try/catch` 保持不变；catch 分支**不得**清空 `ai-skills-manager` 的 `_cache`（D-05 第 2 层由 46-03 在 manager 内部已实现，此处只需确认没有新增清空逻辑）。
    3. `tests/test-ai-skills.js` 新增 `describe('P8 失效链机制断言（源码扫描）')`：
       - 读 `fs.readFileSync(path.join(__dirname, '..', 'ai-manager.js'), 'utf8')`，按行切分。
       - 找出所有含 `new Agent(` 的行号；断言出现次数 **≥ 2**（两处创建点；未来新增会自然提高这个下界）。
       - 对每一处，断言其**前 60 行窗口**内存在 `refreshSkills(`；失败时用 `assert.ok(cond, \`new Agent( 于第 ${line} 行之前 60 行内缺少 refreshSkills 调用 —— P8 漏接线\`)` 给出精确行号。
       - 断言 `ai-manager.js` 中 `refreshSkills(` 的出现次数严格等于 `new Agent(` 的出现次数（两处创建点各一次；若未来出现第三处创建点而未接刷新，这条立即转红）。
       - 断言 `ai-skills-manager.js` 的 `module.exports` 同时含 `refreshSkills` / `buildSkillsPrompt` / `getSkillsSnapshot`（P8 机制断言 ①：权威面已导出可供后续阶段消费）。
       - **行为断言（P8 机制 ③，覆盖第 6 条路径）**：先用真实沙箱 env 对临时目录做一次刷新并记录 `buildSkillsPrompt()`；然后在**不经过任何 Realm 管理器**的情况下直接用 `fs.writeFileSync` 覆写 `<root>/skills/alpha/SKILL.md` 的 description；再次 `refreshSkills`（模拟 `_recreateAgent` 前的重扫）并断言 `buildSkillsPrompt()` 已反映新 description —— 证明「磁盘被外部改写 → 重建时重扫 → 新技能集生效」这条兜底链成立。
  </action>
  <verify>
    <automated>node tests/test-ai-skills.js</automated>
    <fails_when>进程非零退出；或 node:test 汇总出现 `# fail` 非 0 计数；或 `P8 失效链机制断言（源码扫描）` 分组内任一 assert 抛出（输出含 `AssertionError` 与具体行号）</fails_when>
  </verify>
  <acceptance_criteria>
    - 行为断言：磁盘 `SKILL.md` 被外部直接改写后，重扫（`refreshSkills`）使 `buildSkillsPrompt()` 反映新内容（P8 第 6 条路径的兜底链成立）
    - 源码扫描断言：`ai-manager.js` 中每一个 `new Agent(` 之前 60 行内都存在 `refreshSkills(`，且 `refreshSkills(` 出现次数严格等于 `new Agent(` 出现次数
    - 源码断言：`init()` 与 `_recreateAgent()` 两处的 `refreshSkills(...)` 调用文本完全一致（含相同的 `disabled` 表达式与 `rootDirs` 顺序 managed → user）
    - 源码断言：`ai-skills-manager.js` 的 `module.exports` 含 `refreshSkills` / `buildSkillsPrompt` / `getSkillsSnapshot`
    - 源码断言：`_recreateAgent()` 的 catch 分支不含任何清空 `_cache` 的调用
  </acceptance_criteria>
  <done>P8 失效链在本阶段可触发的三条路径全部闭合（1 init / 2 _recreateAgent / 6 bash-write 直改兜底），且「漏接线」被一条自动化源码扫描断言永久锁住。</done>
</task>

<task type="auto">
  <name>Task 3: DOC-01 —— `docs/product/ai-skills.md` 六节骨架（含三条诚实边界）</name>
  <files>docs/product/ai-skills.md</files>
  <read_first>
    - docs/product/ai-agent-workspace.md:1-8（维护约定块格式 —— 新文档逐字照抄该格式）、:27-46（「agent 工作区目录模型」小节的现状目录树，需交叉引用但**不修改** —— DOC-02 归 Phase 47）、:89-95（「安全边界（诚实声明）」小节的写法先例）
    - docs/product/ai-chat-attachments.md:3-7（互引式维护块先例：「沙箱与工具侧的说明见 ai-agent-workspace.md（其 xxx 小节与本文档互为引用）」）
    - 46-PATTERNS.md §5（Analog A/B 全文 + 六节骨架要求 + **必须写实的三条诚实边界** + 交叉引用习惯）
    - 46-RESEARCH.md §Security Domain（三条威胁的行级结论：`description` 进每次请求 system prompt 且 `escapeXml` 只做字符转义不做内容审查；「AI 不可删改内置技能」是工具层不变式非沙箱不变式；`allowed-tools` 在本 SDK 不存在该字段）
    - 46-CONTEXT.md D-01/D-02/D-06/D-09/D-10/D-11 与 <deferred>（O8 `/compact` 不保留技能正文；O3 `allowed-tools`）
    - REQUIREMENTS.md DOC-01 行与 ROADMAP.md Phase 46 §Doc sync
    - **边界纪律**：`docs/product/ai-agent-workspace.md` 本阶段**零改动**（DOC-02 明确归 Phase 47）
  </read_first>
  <action>
    新建 `docs/product/ai-skills.md`，结构与内容按以下要求：
    1. **维护约定块**（照 `docs/product/ai-agent-workspace.md:1-8` 格式）：声明本文档是 AI 助手技能功能（技能目录 / 加载 / prompt 注入 / 优先级 / 限额 / 启停 / 诊断）的**产品说明权威文档**；以后修改技能相关行为（加载语义、优先级与遮蔽、诊断透传、三条限额、启停语义、沙箱边界）**必须同步更新本文档**；并互引 `ai-agent-workspace.md`（沙箱与工作区目录的权威说明在那份，其目录树小节与本文档互为引用）。
    2. **六节骨架**（标题用中文序号，与既有产品文档风格一致）：
       - `## 一、能力`：技能是「按需加载的指令包」；模型只看到 name / description / location 三项元数据（渐进式披露），技能正文经 `read` 工具按需打开；写明「正文**不**进 system prompt」。
       - `## 二、双目录`：`agent-workspace/skills/`（source=`user`，用户自己的技能）与 `agent-workspace/managed-skills/`（source=`managed`，内置播种与 AI 自建）；启动时自动创建；契约布局为 `<目录>/<技能名>/SKILL.md`，**不能有中间层目录**（多一层会被跳过并给出诊断）。
       - `## 三、优先级`：同名技能 user **遮蔽** managed；被遮蔽者仍可见（带遮蔽标记与诊断）但不进 system prompt；名称以**目录名为权威**（frontmatter `name` 与目录名不一致时按目录名生效并产诊断，防止冒名顶替）。
       - `## 四、限额`：三条限额与其单位 —— `MAX_SKILL_MD_BYTES = 64 KiB`（单技能正文字节上限，超限在解析前拒绝）、`MAX_USER_SKILLS = 50`（用户技能条目上限，超限部分不进 prompt 但不删除）、`SKILLS_PROMPT_CHAR_BUDGET = 8000`（system prompt 中技能段的整段字符预算，按 JS `string.length` 计，超预算按 user 来源 > 可自动激活 > 名称字典序截断并在段尾给出省略提示）。三值只在 `ai-skills-manager.js` 的 `LIMITS` 一处定义。
       - `## 五、沙箱边界`：技能目录位于 AI 硬沙箱 root 内，`read`/`write`/`edit`/`bash` 与其它工作区路径同一套路径判据；技能**不构成额外权限**，也不会绕过 bash 的三档权限与确认卡片。
       - `## 六、已知限制`：至少写实四条 —— ① `allowed-tools` 在当前 SDK（`@earendil-works/pi-agent-core@0.84.3`）**不存在该字段**，任何展示都是虚假安全感，当前运行时也不强制任何按技能授权的工具集；② `/compact` **不保留**技能正文（对话压缩后模型需要重新 `read` 才能看到正文）；③ 同名技能**共享**启用/禁用状态（禁用以 name 为键、不区分来源：先禁用 managed `foo`、之后新增的 user `foo` 也会随之禁用）；④ 深嵌套（`<目录>/a/b/SKILL.md`）与根层散落的 `*.md` 不会被加载（会有诊断说明正确布局）。
    3. **三条诚实边界必须逐条写实（不得含糊）**：
       - 技能的 `description` 会进入**每一次请求**的 system prompt（转义只处理字符、不做内容审查）→ **不要导入来源不明的技能**；正文则只在被 `read` / 显式调用时进入对话。
       - 「AI 不可删改内置技能」是**工具层不变式，不是沙箱不变式** —— 沙箱只有一个 root，模型在技术上可以直接写入 `managed-skills/`；该约束由管理工具的判定承担（与 `ai-bash-policy` 的「白名单是启发式而非安全边界」同款哲学）。**不要**用只读挂载或第二个 sandbox root 去表达它。
       - 技能段变更对**已在进行中**的那一轮请求不生效（下一轮才生效）。
    4. 文档中如需给出目录树，只作为**说明性**片段；**不要**修改 `docs/product/ai-agent-workspace.md`（DOC-02 在 Phase 47 补齐目录树同步）。
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/product/ai-skills.md','utf8'); const need=['## 一、能力','## 二、双目录','## 三、优先级','## 四、限额','## 五、沙箱边界','## 六、已知限制','维护约定','description','allowed-tools','不保留','共享','沙箱']; const miss=need.filter(h=>!s.includes(h)); if(miss.length){console.error('missing: '+miss.join(' | ')); process.exit(1)} console.log('DOC-01 OK')"</automated>
    <fails_when>进程非零退出（stderr 打印 `missing: …` 列出缺失的小节或关键条目）；或 stdout 未出现 `DOC-01 OK`</fails_when>
    <automated>git status --porcelain -- docs/product/ai-agent-workspace.md docs/product/ai-chat-attachments.md</automated>
    <fails_when>stdout 非空（这两份文档在本次执行期间被新增/修改/删除即失败：本阶段 DOC-02 归 Phase 47，两份存量产品文档必须零改动）</fails_when>
  </verify>
  <acceptance_criteria>
    - 文件断言：`docs/product/ai-skills.md` 存在，且含六个中文序号小节标题与维护约定块
    - 内容断言：文档含 `description` 进入每次请求 system prompt 的诚实声明，以及「不要导入来源不明的技能」的对应建议
    - 内容断言：文档明写 `allowed-tools` 在当前 SDK 不存在该字段 / 当前运行时不被强制
    - 内容断言：文档明写 `/compact` 不保留技能正文、同名技能共享禁用状态、深嵌套与根层散落文件不被加载
    - 内容断言：文档明写三条限额的具体数值与单位（64 KiB / 50 / 8000 字符）及其单源位置
    - 源码断言：`docs/product/ai-agent-workspace.md` 与 `docs/product/ai-chat-attachments.md` 零改动（以任务 3 的 `git status --porcelain` 命令输出为空为准）
  </acceptance_criteria>
  <done>技能功能有了与实际行为一致的产品说明权威文档：能力、双目录、优先级、限额、沙箱边界、已知限制六节齐全，且三条「说实话」的边界（description 进 prompt / 工具层不变式 ≠ 沙箱不变式 / allowed-tools 不被强制）逐条写明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `agent.state.systemPrompt` → 下一次 LLM 请求 | 技能段变更在此边界生效；错写会污染进行中的对话或丢失上下文 |
| `windowManager.broadcast('skills:changed')` → 各窗口 renderer | 本阶段只发不收（消费方在 48/50） |
| `docs/product/ai-skills.md` → 用户的安全心智模型 | 文档含糊 = 虚假安全感（用户可能导入来源不明的技能） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04-01 | Tampering | 技能缓存失效（磁盘与内存快照分叉：装了不生效 / 卸了仍被描述 / 改无变化） | high | mitigate | ① 权威收敛到 `ai-skills-manager` 单模块；② `init()` 与 `_recreateAgent()` 两处创建点前无条件 `await refreshSkills`；③ 源码扫描断言「每个 `new Agent(` 前必有 `refreshSkills`」永久锁住漏接线；④ 覆盖 bash/write 直改磁盘（无事件路径）。**本阶段 3/6，其余 3 条经 `<p8_gate_mapping>` 交接给 48/49/50/51**（诚实边界，不宣称全覆盖） |
| T-46-04-02 | Repudiation | 变更未广播，多窗口技能列表不一致 | medium | mitigate | 真正改写 prompt 后 `windowManager.broadcast('skills:changed')`；本阶段无消费方，端到端到窗口的验证列为 UAT 手验项 |
| T-46-04-03 | Tampering | 忙时改写 prompt 导致当前轮上下文不一致 | medium | mitigate | 忙时（`isProcessing` / `state.isStreaming`）只置 `_skillsPromptDirty`，在 `promptWithContext():1209` 的 idle 边界补刷一次；不在流式回调内改状态 |
| T-46-04-04 | Elevation | 用户误以为技能目录是权限边界 | medium | mitigate | 文档「五、沙箱边界」与「六、已知限制」逐条写明：技能不构成额外权限；「AI 不可删改内置技能」是工具层而非沙箱不变式（沙箱只有一个 root） |
| T-46-04-05 | Information Disclosure | 文档未声明 description 进每次请求的注入面 | medium | mitigate | 文档「五、沙箱边界」+「六、已知限制」明写 description 进每次请求的 system prompt、转义不做内容审查，并给出「不要导入来源不明的技能」的建议 |
| T-46-04-SC | Tampering | npm / 依赖安装 | high | mitigate | 本计划零新增依赖；不执行任何安装命令（`package.json` 原样），无包合法性 gate 触发 |
</threat_model>

<flagged_assumptions>
以下条目来自 `SPECLESS_FALLBACK` 的 edge-probe 文本派生报告（`status: unresolved` / `category: unclassified`），按 §A 不得自动 resolve，显式登记：

- **SKILL-03**（unclassified）：「两处 Agent 创建点」由源码扫描断言锁定。**48-51 若新增第三处 `new Agent(` 路径，本阶段的断言管不到其语义** —— 但扫描断言会在「刷新调用次数 ≠ 创建点数量」时转红，构成正向防护。
- **SKILL-04**（unclassified）：本阶段**没有**广播的消费方（`/` 面板属 Phase 48、设置页属 Phase 50）。因此「多窗口列表同步」只到「事件被以 `'skills:changed'` 发出」这一层，端到端到窗口的验证列为 46-VALIDATION.md 的 Manual-Only 项（UAT 手验）。
- **DOC-01**（unclassified）：六节骨架的粒度由 plan 期决定（CONTEXT.md 的 Claude's Discretion），本阶段取「六节 + 已落地内容写实 + 未落地留占位」。若后续阶段认为粒度不足，增量补节即可（文档是增量维护的，不是一次性定稿）。
- 完整的 11 行结清表见 `46-01-PLAN.md` 的 `<edge_coverage_ledger>`（等式 11 = 4 truths + 7 flagged assumptions）。
</flagged_assumptions>

<verification>
1. `node tests/test-ai-skills.js` 退出 0（累计约 45 例，含 P8 源码扫描与 SDK 契约断言）
2. 全量回归：`node tests/test-agent-workspace.js && node tests/test-ai-bash-policy.js && node tests/test-ai-conversations.js` 退出 0
3. `node --check ai-manager.js && node --check ai-skills-manager.js && node --check agent-workspace.js` 通过
4. DOC-01 gate：六节标题 + 关键条目齐备；`docs/product/ai-agent-workspace.md` / `ai-chat-attachments.md` 零 diff
5. P8 机制 gate：源码扫描 —— `new Agent(` 出现次数 ≥ 2，且每一处前 60 行内都有 `refreshSkills(`，且两者计数相等
6. UAT 手验（end-of-phase，见 `46-VALIDATION.md` 的 Manual-Only 三项）：`npm run dev` → 两个技能目录出现 → 问 AI「你有哪些技能」应答出 name/description → 手改 `agent-workspace/skills/<x>/SKILL.md` 后切会话，下一条消息反映改动 → 问一个命中 description 的任务观察是否调 `read` 打开 `location`
</verification>

<success_criteria>
- [ ] `syncAgentSystemPrompt()` 改写 `agent.state.systemPrompt` 且不重建 Agent；无变化时不写
- [ ] 忙时置脏、idle 边界补刷一次；真正改写后广播 `skills:changed`
- [ ] `_recreateAgent()` 前无条件重扫，`init()` 与它文本一致
- [ ] 源码扫描断言：每个 `new Agent(` 前 60 行内有 `refreshSkills(`，且两者计数相等
- [ ] 磁盘被外部直改后重扫能反映新内容（P8 第 6 条路径兜底成立）
- [ ] `docs/product/ai-skills.md` 六节齐全、三条诚实边界写实
- [ ] 全量测试与回归全绿；`ai-agent-workspace.md` 零 diff（DOC-02 留 47）
</success_criteria>

<output>
Create `.planning/phases/46-prompt/46-04-SUMMARY.md` when done
</output>
