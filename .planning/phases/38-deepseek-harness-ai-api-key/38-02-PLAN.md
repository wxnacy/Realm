---
phase: 38-deepseek-harness-ai-api-key
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.html
  - src/settings-page.js
  - src/styles/main.css
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-09
  - D-10
  - D-18

estimate:
  tokens: 90000
  raw_tokens: 55000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "设置页 AI 助手区域显示左右分栏布局：左侧 280px 供应商列表，右侧 flex:1 编辑表单（per D-01）"
    - "供应商列表为空时显示'尚未配置供应商'文案和'添加供应商'CTA 按钮"
    - "供应商列表项显示提供商名称、配置状态指示器、当前激活提供商标记"
    - "供应商列表区域可滚动，最大高度 calc(100vh - 300px)"
    - "模型列表为空时显示'未检测到模型'文案"
    - "'检测模型'按钮点击后显示'检测中...'文案并 disabled"
    - "保存配置按钮点击后显示'保存中...'文案并 disabled"
    - "模型检测失败时显示错误信息和原因"
    - "环境变量未找到时在 API Key 输入框下方显示警告信息"
    - "模型名过长时截断并显示省略号，max-width 200px"
    - "用户可手动删除检测到的模型标签（per D-04）"
    - "删除供应商时弹出确认弹窗，确认后硬删除（per D-05）"
    - "添加内置供应商弹窗使用 dialog 元素，支持搜索过滤（per D-02）"
    - "用户可自定义环境变量名（per D-08），输入框下方显示环境变量检测提示（per D-09）"
    - "用户可手动输入 API Key 覆盖环境变量值（per D-10）"
    - "检测到的模型列表可由用户手动增删（per D-18）"
  artifacts:
    - src/settings.html (左右分栏 HTML 结构)
    - src/settings-page.js (供应商管理函数集)
  key_links:
    - "settings-page.js settingsApi('ai/providers') → main.js GET /api/ai/providers"
    - "settings-page.js settingsApi('ai/providers', POST) → main.js POST /api/ai/providers"
    - "settings-page.js settingsApi('ai/providers/:id/detect-models', POST) → main.js POST detect-models"
    - "settings-page.js settingsApi('ai/providers/:id', DELETE) → main.js DELETE /api/ai/providers/:id"
    - "settings-page.js settingsApi('ai/providers/:id/env-var') → main.js GET env-var"
---

<objective>
重构设置页 AI 助手配置区域为左右分栏布局，实现供应商列表管理（搜索、添加、选择）、右侧编辑表单（API Key、环境变量检测、模型管理）、内置供应商选择弹窗，以及完整的供应商 CRUD 交互。

Purpose: 用户通过设置页管理多个 AI 供应商，是 Phase 38 的核心用户界面
Output: 改造后的 src/settings.html + src/settings-page.js
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-deepseek-harness-ai-api-key/38-CONTEXT.md
@.planning/phases/38-deepseek-harness-ai-api-key/38-RESEARCH.md
@.planning/phases/38-deepseek-harness-ai-api-key/38-UI-SPEC.md
@src/settings.html
@src/settings-page.js
@src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 设置页左右分栏布局 + 供应商列表 + 编辑表单基础</name>
  <files>src/settings.html, src/settings-page.js, src/styles/main.css</files>
  <read_first>
    - src/settings.html (重点: settings-ai-assistant 区域 L258-305)
    - src/settings-page.js (重点: AI 助手设置区域 L2365-2590，loadAISettings、selectAIProvider、renderAIProviderDropdown)
    - src/styles/main.css (重点: AI 相关样式，搜索 .ai-provider, .settings-ai)
    - 38-UI-SPEC.md (布局规格和交互规格)
  </read_first>
  <action>
1. 重构 src/settings.html 的 `#settings-ai-assistant` 区域（替换现有 L258-305 的 AI 配置 HTML）：
   - 外层: div.settings-ai-split（display: flex）
   - 左侧: div.settings-ai-sidebar（width: 280px, flex-shrink: 0）
     - 搜索框: input.text-input#aiProviderSearch（placeholder: "搜索供应商..."）
     - 供应商列表容器: div#aiProviderList（可滚动区域）
     - 底部按钮区: div.settings-ai-sidebar-actions
       - button.btn.btn-primary#aiAddBuiltinBtn → "添加内置供应商"
       - button.btn.btn-secondary#aiAddCustomBtn → "添加自定义供应商"
   - 右侧: div.settings-ai-editor（flex: 1）
     - 默认空状态: div#aiEditorEmpty（显示"尚未配置供应商"文案 + CTA）
     - 编辑表单: div#aiEditorForm（初始隐藏）
       - 提供商名称: input.text-input#aiEditorName（readonly for builtin）
       - API Key 区域:
         - input[type=password]#aiEditorApiKey + 显示/隐藏按钮
         - div#aiEnvVarHint（环境变量检测提示，初始隐藏）
       - 环境变量名: input.text-input#aiEditorEnvVarName
       - 模型列表区域:
         - div#aiModelTags（模型标签容器）
         - div#aiModelEmpty（"未检测到模型"文案，初始隐藏）
         - button.btn.btn-secondary#aiDetectModelsBtn → "检测模型"
         - div#aiModelError（错误信息区域，初始隐藏）
       - 底部操作区:
         - button.btn.btn-danger#aiDeleteProviderBtn → "删除供应商"
         - button.btn.btn-primary#aiSaveProviderBtn → "保存配置"

2. 在 src/settings-page.js 中重写 AI 设置相关函数（替换 L2365-2590 的现有代码）：
   - loadAISettings(): 调用 settingsApi('ai/providers') 获取供应商列表，渲染左侧列表
   - renderProviderList(providers, activeId): 渲染供应商列表项，每项显示名称+状态指示器+激活标记，点击切换右侧表单
   - showEditorEmpty(): 显示空状态，隐藏编辑表单
   - showEditorForm(provider): 加载供应商配置到右侧表单
   - saveProviderConfig(): 读取表单值，POST 到 settingsApi('ai/providers', { method: 'POST', body: ... })
   - 新增全局变量: aiProvidersList (供应商列表缓存), aiSelectedProvider (当前选中供应商)

3. 在 src/styles/main.css 中添加左右分栏布局样式：
   - .settings-ai-split: display: flex, gap: 16px
   - .settings-ai-sidebar: width: 280px, flex-shrink: 0, border-right: 1px solid var(--border-color)
   - .settings-ai-sidebar-actions: display: flex, gap: 8px, padding-top: 8px
   - .ai-provider-item: height: 48px, padding: 0 12px, display: flex, align-items: center, cursor: pointer, border-radius: 6px
   - .ai-provider-item:hover: background var(--bg-hover)
   - .ai-provider-item.active: background var(--bg-tertiary), border-left: 2px solid var(--accent-color)
   - .ai-provider-item .provider-active-dot: width: 8px, height: 8px, border-radius: 50%, background var(--accent-color), margin-left: auto
   - .settings-ai-editor: flex: 1, padding-left: 16px
   - #aiProviderList: max-height: calc(100vh - 300px), overflow-y: auto

4. 保留连接状态区域（.settings-group 连接状态），移到左侧列表底部或右侧表单顶部
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const html = fs.readFileSync('src/settings.html', 'utf8');
console.assert(html.includes('settings-ai-split'), 'split layout exists');
console.assert(html.includes('settings-ai-sidebar'), 'sidebar exists');
console.assert(html.includes('settings-ai-editor'), 'editor exists');
console.assert(html.includes('aiProviderSearch'), 'search input exists');
console.assert(html.includes('aiAddBuiltinBtn'), 'add builtin button exists');
console.assert(html.includes('aiAddCustomBtn'), 'add custom button exists');
console.assert(html.includes('aiEditorForm'), 'editor form exists');
console.assert(html.includes('aiDetectModelsBtn'), 'detect models button exists');
console.assert(html.includes('aiDeleteProviderBtn'), 'delete button exists');
console.assert(html.includes('aiSaveProviderBtn'), 'save button exists');
const js = fs.readFileSync('src/settings-page.js', 'utf8');
console.assert(js.includes('renderProviderList'), 'renderProviderList function exists');
console.assert(js.includes('showEditorForm'), 'showEditorForm function exists');
console.assert(js.includes('saveProviderConfig'), 'saveProviderConfig function exists');
console.log('PASS: settings page structure verified');
"</automated>
  </verify>
  <acceptance_criteria>
    - src/settings.html 中 #settings-ai-assistant 区域包含 .settings-ai-split 容器（flex 布局）
    - 左侧 .settings-ai-sidebar 包含搜索框、供应商列表容器、添加按钮
    - 右侧 .settings-ai-editor 包含空状态和编辑表单（初始隐藏）
    - 编辑表单包含: 提供商名称输入、API Key 输入+显示按钮、环境变量名输入、模型标签容器、检测模型按钮、删除/保存按钮
    - src/settings-page.js 有 loadAISettings、renderProviderList、showEditorEmpty、showEditorForm、saveProviderConfig 函数
    - settingsApi('ai/providers') 调用获取供应商列表
    - settingsApi('ai/providers', { method: 'POST', body: ... }) 调用保存配置
    - src/styles/main.css 有 .settings-ai-split、.settings-ai-sidebar、.settings-ai-editor、.ai-provider-item 样式
  </acceptance_criteria>
  <done>
    设置页显示左右分栏布局，左侧供应商列表支持搜索过滤，点击列表项右侧显示编辑表单，保存按钮调用 API 保存配置
  </done>
</task>

<task type="auto">
  <name>Task 2: 内置供应商弹窗 + 环境变量检测 + 模型管理 + 删除确认</name>
  <files>src/settings.html, src/settings-page.js</files>
  <read_first>
    - src/settings.html (验证 Task 1 的 HTML 结构)
    - src/settings-page.js (验证 Task 1 的函数)
    - 38-UI-SPEC.md (交互规格: 添加内置供应商弹窗、模型检测流程、环境变量检测流程)
  </read_first>
  <action>
1. 在 src/settings.html 中添加内置供应商选择弹窗（dialog 元素）：
   - dialog#aiBuiltinDialog
     - h3: "选择供应商"
     - input.text-input#aiBuiltinSearch（placeholder: "输入以筛选..."）
     - div#aiBuiltinList（供应商选项列表，可滚动，max-height 400px）
     - div.dialog-actions: button "取消" + button.btn.btn-primary "添加"
   - 弹窗样式: 使用项目现有 dialog 样式模式

2. 在 src/settings-page.js 中实现内置供应商弹窗逻辑：
   - showBuiltinProviderDialog(): 打开弹窗，加载所有内置供应商列表（从 aiProvidersCatalog 获取）
   - renderBuiltinList(filterText): 搜索过滤内置供应商列表
   - 添加选中的供应商到已配置列表，自动切换到编辑表单

3. 实现环境变量检测功能（per D-07/D-08/D-09/D-10）：
   - 在 showEditorForm(provider) 中自动调用 settingsApi(`ai/providers/${provider.id}/env-var?customName=${envVarName}`)
   - 检测到时: #aiEnvVarHint 显示绿色提示"检测到环境变量 XXX，已自动使用"，API Key 输入框显示掩码值
   - 未检测到时: #aiEnvVarHint 显示"未检测到环境变量 XXX，将使用手动输入的 API Key"（per UI-SPEC env-var-not-found）
   - 用户修改环境变量名后重新检测
   - 用户可手动输入 API Key 覆盖环境变量值

4. 实现模型检测功能（per D-03/D-04/D-16/D-17/D-18）：
   - aiDetectModelsBtn 点击: 按钮文字变为"检测中..."并 disabled，调用 settingsApi(`ai/providers/${id}/detect-models`, { method: 'POST', body: { apiKey, baseURL } })
   - 成功: 渲染模型标签到 #aiModelTags，每个标签有 × 删除按钮
   - 失败: #aiModelError 显示错误信息"模型检测失败：{原因}。请检查 API Key 和网络连接后重试"
   - 模型标签 × 按钮: 从列表移除该模型（per D-04）

5. 实现删除供应商功能（per D-05）：
   - aiDeleteProviderBtn 点击: 弹出 confirm 对话框"删除供应商：确定要删除"{供应商名}"吗？相关配置（API Key、模型列表）将被清除，此操作不可撤销"
   - 确认后: 调用 settingsApi(`ai/providers/${id}`, { method: 'DELETE' })
   - 删除后: 从左侧列表移除，右侧切换到空状态或列表第一项

6. 实现自定义供应商添加（per D-02）：
   - aiAddCustomBtn 点击: 左侧列表新增"未命名供应商"项，右侧展开编辑表单
   - 自定义供应商表单额外显示: baseURL 输入框、协议选择（openai-chat / openai-responses）
   - 提供商名称字段可编辑（非 readonly）

7. 模型标签样式: max-width 200px, text-overflow: ellipsis, white-space: nowrap（per UI-SPEC long-text）
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const html = fs.readFileSync('src/settings.html', 'utf8');
console.assert(html.includes('aiBuiltinDialog'), 'builtin dialog exists');
console.assert(html.includes('aiBuiltinSearch'), 'builtin search exists');
const js = fs.readFileSync('src/settings-page.js', 'utf8');
console.assert(js.includes('showBuiltinProviderDialog'), 'showBuiltinProviderDialog exists');
console.assert(js.includes('detectModels'), 'detectModels function exists');
console.assert(js.includes('检测中'), 'loading state text exists');
console.assert(js.includes('环境变量'), 'env var hint text exists');
console.assert(js.includes('确认') || js.includes('confirm'), 'delete confirmation exists');
console.log('PASS: all interactive features verified');
"</automated>
  </verify>
  <acceptance_criteria>
    - src/settings.html 有 dialog#aiBuiltinDialog 弹窗，包含搜索框和供应商列表
    - src/settings-page.js 有 showBuiltinProviderDialog 函数，弹窗展示内置供应商列表并支持搜索过滤
    - 点击"检测模型"按钮后按钮文字变为"检测中..."并 disabled
    - 检测成功后 #aiModelTags 区域显示模型标签，每个标签有 × 删除按钮
    - 检测失败后 #aiModelError 显示错误信息
    - 打开供应商编辑表单时自动检测环境变量，#aiEnvVarHint 显示检测结果
    - 环境变量名输入框可编辑，修改后重新检测
    - 删除供应商时弹出确认弹窗，确认后调用 DELETE API
    - 添加自定义供应商时显示 baseURL 和协议选择字段
    - 模型标签 max-width 200px，过长时截断显示省略号
  </acceptance_criteria>
  <done>
    设置页支持完整的供应商 CRUD 流程：添加内置/自定义供应商、编辑配置、检测模型、环境变量检测提示、删除确认。所有 UI 状态（空、加载、错误、填充）正确处理
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview→main | 设置页 HTTP 请求（通过 /api/settings/* 本地服务器） |
| user→input | 供应商名称、API Key、环境变量名用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-04 | Tampering | 供应商名称输入 | low | mitigate | 使用 textContent 构建 DOM，不使用 innerHTML |
| T-38-05 | Information Disclosure | API Key 显示 | low | mitigate | 默认 password 类型，显示按钮切换，不自动填充到 DOM 属性 |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | 本阶段不安装新依赖 |
</threat_model>

<verification>
1. 设置页左右分栏布局在 1280px 宽度下正确显示
2. 供应商列表搜索过滤实时响应
3. 所有 UI 状态（空、加载、错误、填充）有对应的视觉反馈
4. 内置供应商弹窗可搜索、可选择、可取消
5. 模型标签可删除，删除后表单状态更新
</verification>

<success_criteria>
- 设置页 AI 助手区域为左右分栏布局（280px + flex:1）
- 支持添加内置供应商（弹窗选择 38 个提供商）和自定义供应商
- 环境变量自动检测并显示提示
- 模型检测调用 API 并显示结果/错误
- 供应商删除有确认弹窗
- 所有 UI 考虑状态（空/加载/错误/填充/长文本/溢出）正确处理
</success_criteria>

<output>
创建 `.planning/phases/38-deepseek-harness-ai-api-key/38-02-SUMMARY.md` 完成后
</output>
