# Phase 38: AI 助手供应商管理增强 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-21
**Phase:** 38-AI 助手供应商管理增强
**Areas discussed:** 供应商管理 UI、API Key 环境变量、聊天面板工具栏、自定义供应商、供应商删除、激活机制、默认供应商、模型检测、工具栏额外元素

---

## 供应商管理 UI 位置

| Option | Description | Selected |
|--------|-------------|----------|
| 仅扩展设置页（推荐） | 在设置页 AI 助手区域增加完整的供应商增删改列表，聊天面板只加模型选择器 | ✓ |
| 设置页+聊天面板都可管理 | 设置页做完整 CRUD，聊天面板的模型选择器旁加"管理供应商"入口 | |
| 仅在聊天面板内管理 | 把供应商管理完全移到聊天面板，设置页只保留连接状态 | |

**User's choice:** 仅扩展设置页（推荐）
**Notes:** 保持配置和使用的分离

---

## API Key 环境变量交互

| Option | Description | Selected |
|--------|-------------|----------|
| 自动检测+提示（推荐） | 输入框下方提示"检测到环境变量 XXX，已自动使用"，用户可手动覆盖 | ✓ |
| 环境变量作为默认值 | 环境变量值预填到输入框，用户可修改。保存后覆盖为自定义值 | |
| 环境变量开关 | 加一个 toggle "使用环境变量"，开启时读 env，关闭时用手动输入 | |

**User's choice:** 自动检测+提示（推荐）
**Notes:** 类似 deepseek-harness 的 apiKeyEnv 模式

---

## 聊天面板模型选择器位置

| Option | Description | Selected |
|--------|-------------|----------|
| 输入框左侧下拉（推荐） | 在 textarea 左侧加一个小下拉选择器 | |
| 输入框上方工具栏 | 在输入区上方加一行工具栏 | |
| 面板标题栏旁 | 在 "AI 助手" 标题旁显示当前模型 | |

**User's choice:** 在输入框下边增加一行工具栏，发送按钮挪到工具栏最右侧，模型选择放在发送按钮左侧
**Notes:** 用户自定义了布局方案

---

## 自定义供应商支持

| Option | Description | Selected |
|--------|-------------|----------|
| 仅配置内置供应商（推荐） | 用户只能配置 pi-ai 内置的 38 个提供商 | |
| 支持自定义 OpenAI 兼容端点 | 除了内置供应商，还支持添加自定义 baseURL + API Key | |

**User's choice:** 像 dsh 一样，两个按钮，一个增加内置供应商，一个增加自定义
**Notes:** 参考 deepseek-harness 的两种供应商添加方式

---

## 设置页供应商布局

| Option | Description | Selected |
|--------|-------------|----------|
| 左右分栏布局（推荐） | 左侧供应商列表 + 右侧编辑表单 | ✓ |
| 卡片列表展开式 | 供应商卡片列表，点击展开编辑 | |

**User's choice:** 左右分栏布局，但是上方需要有搜索框可以搜索供应商。右侧提供检测模型列表的按钮，检测后，用户可以手动删除模型
**Notes:** 用户要求搜索框和模型检测+手动删除能力

---

## 环境变量名确定方式

| Option | Description | Selected |
|--------|-------------|----------|
| 按提供商约定+可自定义（推荐） | 每个提供商约定环境变量名，自动检测。用户也可自定义 | ✓ |
| 纯手动指定环境变量名 | 只支持用户手动填写环境变量名 | |

**User's choice:** 按提供商约定+可自定义（推荐）

---

## 工具栏布局细节

| Option | Description | Selected |
|--------|-------------|----------|
| [模型▼] ---- [发送]（推荐） | 模型选择器 + 空白间隔 + 发送按钮 | |
| [提供商] [模型▼] ---- [发送] | 提供商标签 + 模型选择器 + 空白间隔 + 发送按钮 | |

**User's choice:** 只有一个模型按钮，但是点击后下拉框可以看到已经配置完成的供应商和对应模型列表，可以选择不同供应商的模型。选择后模型按钮上显示选择的模型名称
**Notes:** 下拉框跨供应商展示，按供应商分组

---

## 删除供应商行为

| Option | Description | Selected |
|--------|-------------|----------|
| 彻底删除（推荐） | 删除后相关配置全部清除，需要重新添加 | ✓ |
| 软删除+恢复 | 删除后保留 Key 和配置，可通过"恢复"找回 | |

**User's choice:** 彻底删除（推荐）

---

## 激活供应商机制

| Option | Description | Selected |
|--------|-------------|----------|
| 选择模型即激活（推荐） | 在聊天面板模型下拉中选择时自动设为激活供应商 | ✓ |
| 设置页手动激活 | 设置页单独点击"激活"按钮 | |

**User's choice:** 选择模型即激活（推荐）

---

## 默认供应商

| Option | Description | Selected |
|--------|-------------|----------|
| 保留当前 OpenAI 默认（推荐） | 首次使用时内置一个 OpenAI 供应商（无 Key），引导用户配置 | ✓ |
| 空白起始+引导 | 不预设任何供应商，首次打开显示"添加供应商"引导 | |

**User's choice:** 保留当前 OpenAI 默认（推荐）
**Notes:** 迁移旧数据

---

## 模型检测行为

| Option | Description | Selected |
|--------|-------------|----------|
| 调用 API 获取（推荐） | 用已保存的 API Key 调用提供商 API 获取可用模型列表 | ✓ |
| 使用 pi-ai 内置目录 | 直接使用 pi-ai 内置的模型目录，不实际调用 API | |

**User's choice:** 调用 API 获取（推荐）

---

## 工具栏额外元素

| Option | Description | Selected |
|--------|-------------|----------|
| 设置快捷入口 | 模型按钮旁加设置齿轮图标 | |
| 提供商标签 | 工具栏左侧显示当前提供商名 | |
| 新对话按钮 | 工具栏左侧加"新对话"按钮 | ✓ |

**User's choice:** 新对话按钮（多选中只选了此项）

---

## Claude's Discretion

以下领域由 Claude 自行决定：
- 供应商列表项的具体样式（图标/状态指示器/激活标记）
- 添加内置供应商时的选择界面（弹窗/下拉/搜索过滤）
- 自定义供应商的表单字段（baseURL、协议选择、模型名手动输入）
- 工具栏的具体 CSS 样式（高度、间距、分割线）
- 模型下拉框的分组样式（提供商名作为分组标题）
- 新对话按钮的确认行为（直接清空还是二次确认）
- 环境变量检测的时机（启动时/打开设置页时/实时）
- 模型按钮的宽度限制（模型名过长时截断策略）

## Deferred Ideas

None — discussion stayed within phase scope
