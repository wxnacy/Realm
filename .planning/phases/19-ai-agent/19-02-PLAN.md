---
phase: 19-ai-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - ai-manager.js
autonomous: true
requirements:
  - AI-01

must_haves:
  truths:
    - AIManager.init() 可成功初始化（在有 API Key 的情况下）
    - Agent 实例可创建并绑定 get_tabs 工具
    - prompt() 方法可触发 Agent 执行并调用 get_tabs 工具
    - Demo 输出到主进程控制台（per D-06）
  artifacts:
    - ai-manager.js（init 方法完整实现）
  key_links:
    - AIManager.init() → builtinModels() → Agent 创建
    - Agent.prompt() → get_tabs 工具执行 → 控制台输出
---

<objective>
验证 Agent + get_tabs 工具的端到端执行流程。

Purpose: 确认 pi-agent-core 可在 Realm 主进程环境中正常运行，工具系统可用。
Output: 可运行的 Demo，控制台输出 Agent 调用 get_tabs 的结果。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ai-agent/19-CONTEXT.md
@.planning/phases/19-ai-agent/19-RESEARCH.md
@.planning/phases/19-ai-agent/19-01-SUMMARY.md
@ai-manager.js
@main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完善 AIManager.init() 实现并运行 Demo 验证</name>
  <files>ai-manager.js</files>
  <read_first>
    - ai-manager.js（Wave 1 产出的骨架，查看当前方法签名）
    - .planning/phases/19-ai-agent/19-CONTEXT.md（D-05 ~ D-07 Demo 范围决策）
    - .planning/phases/19-ai-agent/19-RESEARCH.md（Demo 流程和 API 参考）
    - docs/plan/pi-agent-integration.md（1.2 pi-ai 核心能力、1.3 pi-agent-core 核心能力）
    - main.js（查看 tabManager 和 windowManager 引用）
  </read_first>
  <action>
1. 完善 `ai-manager.js` 的 `init()` 方法实现（per D-09 骨架可直接运行 Demo）：

   - 从 `@earendil-works/pi-ai` 导入 `builtinModels`
   - 从 `@earendil-works/pi-agent-core` 导入 `Agent`
   - 读取 API Key：`const apiKey = configStore.get('ai.apiKey')`
   - 如果无 API Key，输出日志 `[Realm AI] 未配置 API Key，跳过初始化`，返回
   - 创建 Models 实例：`this.models = builtinModels({ openai: { apiKey } })`
   - 构建工具列表：`this.tools = this._buildRealmTools()`
   - 创建 Agent 实例，使用以下配置：
     - `initialState.systemPrompt`: REALM_SYSTEM_PROMPT
     - `initialState.model`: 使用 `this.models.getModel('openai', 'gpt-4o-mini')`（per D-14）
     - `initialState.tools`: `this.tools`
     - `streamFn`: `this.models.streamSimple.bind(this.models)`
     - `convertToLlm`: 过滤消息角色为 user/assistant/toolResult
     - `transformContext`: `this._compactContext.bind(this)`
   - 设置 `this.isInitialized = true`
   - 输出日志 `[Realm AI] AI Manager 初始化完成`

2. 完善 `_buildRealmTools()` 方法（per D-07 get_tabs 工具）：
   - 需要引用 tabManager，通过构造函数参数或模块顶部 require
   - get_tabs 工具定义：
     - `name`: 'get_tabs'
     - `description`: '获取当前所有标签页列表，返回每个标签页的 ID、标题、URL 和所属容器'
     - `parameters`: `{ type: 'object', properties: {}, required: [] }`（无参数）
     - `execute`: 异步函数，调用 `tabManager.getAllTabs()` 或等效方法获取标签页列表，返回格式化的标签页信息数组

3. 完善 `prompt()` 方法：
   - 检查 `this.isInitialized`，未初始化则输出错误并返回
   - 调用 `await this.agent.prompt(message)`
   - 在控制台输出 Agent 的回复（per D-06 输出到主进程控制台）

4. 运行 Demo 验证（per D-05）：
   - 确保已设置 API Key（手动在 electron-store 中设置 `ai.apiKey`，或在 init 前硬编码测试）
   - 启动应用 `npm run dev`
   - 在主进程控制台观察：
     - `[Realm AI] AI Manager 初始化完成` 日志
     - Agent 是否成功创建
   - 通过某种方式触发 `aiManager.prompt('列出当前所有标签页')`（可在 main.js 临时添加测试代码）
   - 验证 Agent 调用了 get_tabs 工具
   - 验证控制台输出了标签页列表结果
  </action>
  <verify>
    <automated>grep -c "get_tabs" ai-manager.js</automated>
  </verify>
  <acceptance_criteria>
    - `ai-manager.js` 中 `init()` 方法包含 `builtinModels()` 调用
    - `ai-manager.js` 中 `init()` 方法包含 `new Agent()` 调用
    - `ai-manager.js` 中 `_buildRealmTools()` 返回包含 `get_tabs` 的工具列表
    - `get_tabs` 工具的 `execute` 函数引用了 tabManager
    - `grep -c "get_tabs" ai-manager.js` 输出 >= 3（工具名出现至少 3 次）
    - `grep -c "builtinModels" ai-manager.js` 输出 >= 1
    - `grep -c "new Agent" ai-manager.js` 输出 >= 1
  </acceptance_criteria>
  <done>AIManager.init() 完整实现，get_tabs 工具可被 Agent 调用，Demo 验证通过（Agent 成功执行 get_tabs 并输出结果到控制台）</done>
</task>

</tasks>

<verification>
1. 代码验证：`grep -c "get_tabs" ai-manager.js` 输出 >= 3
2. 代码验证：`grep -c "builtinModels" ai-manager.js` 输出 >= 1
3. 代码验证：`grep -c "new Agent" ai-manager.js` 输出 >= 1
4. 运行时验证：启动应用后控制台输出 `[Realm AI] AI Manager 初始化完成`
5. 功能验证：调用 prompt() 后 Agent 执行 get_tabs 工具并输出标签页列表
</verification>

<success_criteria>
- AIManager.init() 可成功调用 builtinModels() 和创建 Agent 实例
- get_tabs 工具已实现，可返回当前标签页列表
- Demo 可运行：prompt('列出当前所有标签页') → Agent 调用 get_tabs → 控制台输出结果
- 所有输出在主进程控制台（per D-06）
</success_criteria>

<output>
创建 `.planning/phases/19-ai-agent/19-02-SUMMARY.md` 当任务完成
</output>
