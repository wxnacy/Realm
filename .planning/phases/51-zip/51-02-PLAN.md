---
phase: 51-zip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [USER-03, USER-04]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

key-decisions:
  - '**本计划不设 tracer**（扩张型计划规则的解释见 51-03）：安装动作没有「薄纵切」形态可言，其端到端判据就是「两个包能从仓库根 `require` 到、且版本精确」这一条，已落在 Task 2 的门禁里'
  - '**`yaml` 必须精确钉 `"2.9.0"`，不得 `^2.9.0`（CR-2，权威度高于 CONTEXT D-14 的措辞）**：实测 `"yaml": "^2.9.0"` + SDK 精确 `"yaml": "2.9.0"` ⇒ npm 落地**两份**（根 `2.9.1` + `node_modules/@earendil-works/pi-agent-core/node_modules/yaml@2.9.0`），双实例会**静默**产出 YAML 解析差异（SDK 用它解析 SKILL.md frontmatter，本阶段也用它与 `buildSkillFileText` 的组装口径协同）。npm 默认 `save-prefix = ^`（本仓实测无 `.npmrc`）⇒ **必须带 `--save-exact`**，否则写进去的就是 `^2.9.0`'
  - '**必须插入 `checkpoint:human-verify gate="blocking-human"`**：`gsd_run query package-legitimacy check --ecosystem npm yauzl yaml pend` 的实测裁决是 `yaml` = **[SUS]（reason `too-new`）**，而**被判的是 npm `latest`（2.9.1，2026-09-11 发布，4 天前）**，不是本阶段要钉的 2.9.0（2026-05-11 发布，且**此刻已在 `node_modules/` 里**作为 SDK 传递依赖）。协议的旗标规则不因「被判版本与安装版本不同」而豁免 ⇒ 按协议保守处置：**安装前**由用户确认「钉的是 2.9.0 且与 SDK 同版」。该检查点**永不可自动批准**（`workflow.auto_advance` 与 `--auto` 均不越过它）'
  - '**关于 `human_verify_mode: end-of-phase` 的取舍（显式登记，不是遗漏）**：本仓在 `end-of-phase` 下**不使用** `checkpoint:human-verify` 作为**工作产出的验证**手段（改用 `<verify><human-check>`）。本计划的检查点是**安装前的供应链合法性闸**（Gate 语义、非产出验证），且协议对 `[SUS]` 包强制要求该元素形态与 `gate="blocking-human"` ⇒ 两者不冲突、不互替。这也是本计划 `autonomous: false` 的**唯一**原因'
  - '**两个新依赖都不需要 `asarUnpack`、都不动 `build.files`**（D-14 / P11）：两者均纯 JS、无原生模块（`.node`）、无被原生代码读取的数据文件。`build.files` 的护栏（`tests/test-builtin-skills-seeder.js` 断言**每一项都以 `!` 开头**）必须保持绿 —— 本计划**不得**为正 allowlist 添任何正向条目（`package.json` 是 JSON、无法内联注释，那条护栏的语义写在 `AGENTS.md` 的「打包排除项的前提」条目里）'
  - '**本计划与 51-03 的边界**：本计划只把包装上、把 `dependencies` 与 `package-lock.json` 落定；**任何 `require(''yauzl'')` / `require(''yaml'')` 的代码都不在本计划**（那在 51-03）。因此本计划可在 `51-03` 之前或并行（同 Wave 1）完成，判据不依赖任何导入面代码'

must_haves:
  truths:
    - '`package.json` 的 `dependencies` 含 `"yauzl": "^3.4.0"` 与 `"yaml": "2.9.0"`（**逐字，`yaml` 无 `^`/`~`/`>=` 前缀**）；`package-lock.json` 同步落定且入库（`.gitignore` 第 3 行明文要求 lock 必须入库）'
    - '**单实例判据（CR-2 的机械形式）**：`require(''./node_modules/yaml/package.json'').version === ''2.9.0''` **且** `node_modules/@earendil-works/pi-agent-core/node_modules/yaml` **不存在**（双实例形态是「根 2.9.1 + 嵌套 2.9.0」，两条同时成立才算单实例）'
    - '`require.resolve(''yauzl'')` 成功，`require(''yauzl'')` 的 `package.json.version` 为 `3.4.0`，且模块导出面含 `openPromise` / `fromBufferPromise` / `openReadStreamPromise`（Promise API 真实存在 —— `51-03` 的整条解压实现依赖它）'
    - '两个包**均无 `postinstall` 脚本**（读 `node_modules/<pkg>/package.json` 的 `scripts`，本地判据，不依赖网络）；`build.files` 的 `!` 前缀护栏保持绿、`build.asarUnpack` 未被改动（仍只有 `nodejieba` 与 `skills-builtin`）'
  artifacts:
    - 'package.json：`dependencies.yauzl`（`^3.4.0`）、`dependencies.yaml`（`2.9.0`）'
    - 'package-lock.json：两个包及其传递依赖（`pend ~1.2.0`）的解析条目'
  key_links:
    - '版本单源链：`dependencies.yaml` 的精确 `2.9.0` → `node_modules/yaml` 的**唯一**实例 → SDK 的 `dist/harness/skills.js` 的 `import { parse } from "yaml"`（同一份实现）。任何一处漂移（写 `^2.9.0` / SDK 升版改钉值）都会让这条链多出一个实例，而**解析差异是静默的**'
    - '能力单源链：`yauzl` 的 `openPromise` / `eachEntry()`（async iterator）/ `openReadStreamPromise` / `entry.canDecodeFileData()` → `51-03` 的 `extractAndValidatePackage()`。本计划的门禁断言这些**导出面**存在，使「升版改了 API 形态」在安装当刻就可见，而不是在 51-03 的解压实现里以 `TypeError` 形式出现'
  prohibitions:
    - statement: '不得把 `yaml` 写成 `^2.9.0` / `~2.9.0` / `latest`，也不得为「避免双实例」改用 `overrides` / `resolutions` / 手工删除嵌套目录'
      status: unresolved
      verification: judgment
      reason: '`overrides` 会把 SDK 的精确钉版也一起改写（等于替上游做版本决策），而手工删嵌套目录在下次 `npm ci` 时会被重建 ⇒ 只有「顶层精确钉同版」是稳定解。实测三种形态的对照见 `51-RESEARCH.md` 的双实例实测'
    - statement: '不得跳过或「事后补」依赖合法性检查点（含把它的结论改写成「已由研究确认」）'
      status: unresolved
      verification: judgment
      reason: '`yaml` 的 `[SUS]` 裁决是**门禁产物**，不是研究意见。检查点的三个人工确认点（钉的是 2.9.0 / 与 SDK 同版 / 两者均无 postinstall）各有一条研究实测支撑，但「谁批准的」必须由用户回答 —— 把批准权交给执行者即等于取消该门禁'
    - statement: '不得为两个新依赖添加 `asarUnpack` 条目，也不得为它们在 `build.files` 里添加正向 allowlist 条目'
      status: unresolved
      verification: judgment
      reason: '两者纯 JS、无 `.node`、无数据文件 ⇒ 加 `asarUnpack` 是无依据的产物膨胀；而 `build.files` 一旦出现正向条目，`!` 的「无正向条目时保留默认全量包含」语义即失效，必须同时列入 `skills-builtin/**` 与 `THIRD_PARTY_NOTICES.md`，否则 SEED-05 与 P10 同时失效（`AGENTS.md` 明文）'
---

<objective>
把本阶段唯二的两个外部依赖**按实测口径**装上并落定：`yauzl@^3.4.0`（zip 解压，库自身从不写盘）与 `yaml@2.9.0`（**精确钉版**，与 SDK 的传递依赖同版 ⇒ 单实例）。

Purpose: `51-03` 起的全部实现都直接 `require` 这两个包 —— zip 解析（P11 明文禁止自写解析器）与 SKILL.md frontmatter 解析（O5 / D-14：SDK 的 `Skill` 类型**没有** `allowed-tools` 字段，要拿到它必须自行解析 ⇒ 必须提升为直接依赖）。版本口径错一格（`^2.9.0`）会产生**静默**的双实例解析差异；依赖合法性门禁把 `yaml` 判为 `[SUS]` ⇒ 安装前有一个不可自动批准的人工闸。

Output: `package.json` 与 `package-lock.json` 的两个依赖条目 + 一条人工确认记录。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-zip/51-CONTEXT.md
@.planning/phases/51-zip/51-RESEARCH.md
@.planning/phases/51-zip/51-PATTERNS.md
@package.json
@.gitignore
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 依赖合法性闸 —— 确认 yaml 钉 2.9.0（与 SDK 同版）与 yauzl 3.4.0</name>
  <files>package.json</files>
  <read_first>
    - `.planning/phases/51-zip/51-RESEARCH.md`：`## Package Legitimacy Audit`（`:296-335`）—— 三行裁决表（`yauzl` OK / `yaml` **SUS** / `pend` OK）、「被判的是哪个版本」的逐条区分（`signals.publishedAt = 2026-09-11` 即 `latest` 2.9.1）、`postinstall` 检查的实跑输出、`build.files` 与 `asarUnpack` 的现状
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### Core`（`:228-234`）与 `**版本验证（本会话实跑命令与输出）**`（`:260-292`）—— `npm view` 五条实测输出 + 双实例实测 A/B 两组
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 1`（`:582-677`）的 ① 导出面逐字清单（`openPromise` / `openReadStreamPromise` / `fromBufferPromise` …）—— 这些是安装后要复核的**导出面契约**
    - `.planning/phases/51-zip/51-CONTEXT.md`：D-14（两个依赖的升降级理由与打包纪律）与 CR-2（精确钉版）
  </read_first>
  <action>
    向用户呈现**三个必须逐条确认的事实**，并等待明确答复（本任务不可自动批准；`workflow.auto_advance` 与 `--auto` 均不越过它）：
    1. **`yaml` 钉的是 2.9.0，不是 2.9.1** —— 门禁判 `[SUS]`（`too-new`）的对象是 npm `latest`（2.9.1，2026-09-11 发布，4 天前）；本阶段安装目标是 2.9.0（2026-05-11 发布），且**此刻已在 `node_modules/` 里**（SDK 的传递依赖）。请用户核对官方页 `https://www.npmjs.com/package/yaml` 的版本列表与发布时间，并确认「钉 2.9.0」这一决定。
    2. **与 SDK 同版是硬要求** —— SDK `@earendil-works/pi-agent-core@0.84.3` 的 `dependencies` 实测为 `"yaml": "2.9.0"`（**精确**）。若声明 `^2.9.0`，npm 会装根 `2.9.1` + 嵌套 `2.9.0` 两份 ⇒ YAML 解析差异是**静默**的。请用户确认接受「精确钉版」这一形态（代价：无补丁自动升级）。
    3. **两者均无 `postinstall`** —— 本会话实跑 `npm view <pkg> scripts.postinstall` 无输出（字段不存在），`yauzl` 的 `scripts` 只有 `{"test": "node test/test.js"}`。请用户核对 `https://www.npmjs.com/package/yauzl` 与 `https://www.npmjs.com/package/yaml` 页面上「Install / Repository / 周下载量」三项与研究中记录的（`yauzl` 3.4.0 / 2026-06-07 / 36.7M 周下载 / `github.com/thejoshwolfe/yauzl`；`yaml` 15 年包龄 / 142.9M 周下载 / `github.com/eemeli/yaml`）一致。
    **输出**：把三条确认结论（谁确认的、确认了什么、是否有附条件）写进 `51-02-SUMMARY.md`；**任何一条未确认 ⇒ 停在此处，不进入 Task 2**。
  </action>
  <what-built>
    尚未落盘任何东西 —— 本任务**只**产出「用户对三个依赖合法性问题的答复」。上游依据是 `gsd_run query package-legitimacy check --ecosystem npm yauzl yaml pend` 的实测裁决：`yauzl` = **[OK]**（3.4.0 / 2026-06-07 / 36.7M 周下载 / `github.com/thejoshwolfe/yauzl`）、`yaml` = **[SUS]（reason `too-new`）**、`pend` = [OK]；两者均**无 `postinstall`**（`npm view scripts.postinstall` 实测无输出）。`yaml` 的 SUS 标的是 npm `latest`（2.9.1，2026-09-11 发布），**不是**本阶段要钉的 2.9.0（2026-05-11 发布，且此刻已作为 SDK 传递依赖在 `node_modules/` 里）。
  </what-built>
  <how-to-verify>
    逐条回答（可用 Node inspector，**不得**用来查找模块本身）：
    1. 打开 `https://www.npmjs.com/package/yaml` 的 Versions 页 —— 确认 `2.9.0` 的发布日期是 2026-05-11、`2.9.1` 是 2026-09-11（4 天前）；确认本阶段**钉 2.9.0**。
    2. 打开 `https://www.npmjs.com/package/yauzl` —— 确认最新版 `3.4.0`、周下载量在千万级量级、仓库地址是 `github.com/thejoshwolfe/yauzl`；确认它与 `yaml` **均无 `postinstall`**（页面的 Install/Repository 区与本地实跑 `npm view <pkg> scripts.postinstall` 一致）。
    3. 确认接受「**精确钉版 `"yaml": "2.9.0"`、无 `^`**」这一形态（代价：不随补丁自动升级；收益：与 SDK 同版、单实例）。接受与否请明确回答。
  </how-to-verify>
  <verify>
    <human-check>三条逐条得到肯定答复：① 同意钉 `yaml@2.9.0`；② 接受「精确钉版、无 `^`」的形态；③ 核对 npm 页面的包身份（周下载量量级与仓库地址）后确认两者不是 slopsquatting。**三条全部**为肯定才算通过；任一条被拒 ⇒ 停下并向编排层报告（不得自行改判为「已确认」）。</human-check>
  </verify>
  <resume-signal>回复「approved」（或逐条列出你的三条确认结论）；若任一条被拒，请写明是哪一条与你的判断，执行者会停下并向编排层报告。</resume-signal>
  <done>三条依赖合法性确认逐条得到用户答复并记录进 SUMMARY；未确认则不进入安装。</done>
</task>

<task type="auto">
  <name>Task 2: 安装并落定 —— 精确钉版、单实例、导出面与打包护栏</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `package.json`：`:37-60` 的 `dependencies` 段（既有条目一律不动；新条目按字母序或就近插入）与 `:60` 之后（或 `build` 段的 `files` / `asarUnpack`）—— **本任务不改 `build` 段的任何字段**
    - `.gitignore`：`:3` 的注释「package-lock.json 必须入库（npm 官方：intended to be committed into source repositories）」—— lock 的变更要一并提交
    - `tests/test-builtin-skills-seeder.js` 的「打包排除项配置护栏」断言组（搜索 `build.files` 与 `asarUnpack` 的断言；**本任务的回归判据**：它必须保持绿）
    - `.planning/phases/51-zip/51-RESEARCH.md`：`### 实测 1` 的 ① 导出面（安装后要 assert 的 token 清单）与 `### Core` 的 Installation 段（实际命令形态）
  </read_first>
  <action>
    1. **安装（两条命令，第二条必须带 `--save-exact`）**：
       - `npm install yauzl@^3.4.0`（本仓无 `.npmrc`、`save-prefix` 实测为 `^` ⇒ 落 `"yauzl": "^3.4.0"`）。
       - `npm install --save-exact yaml@2.9.0`（**`--save-exact` 不可省**：默认 save-prefix 会写成 `^2.9.0`，那正是双实例的成因）。
       - 本仓 `postinstall` 会跑图标替换 + `npx @electron/rebuild`（既有脚本，本任务不改它）。若它在本次安装中耗时过长或失败：**允许**改用 `npm install --ignore-scripts` 重跑（两个新包均纯 JS、无 `postinstall`、无原生模块 ⇒ 本阶段不需要 rebuild），但须在 SUMMARY 里记明用了哪条路径（这会影响 `node_modules` 的最终形态）。
       - 安装后**核对 `package.json` 的写入值**：`dependencies.yaml` 必须是**裸 `2.9.0`**。若被写成 `^2.9.0` ⇒ 直接用 `Edit` 改回裸值并重跑 `npm install`（让 lock 与 json 一致），**不得**只改 json 不重跑。
    2. **单实例复核（CR-2 的机械判据）**：确认 `node_modules/yaml/package.json` 的 `version === '2.9.0'` **且** `node_modules/@earendil-works/pi-agent-core/node_modules/yaml` 不存在。若嵌套副本仍存在 ⇒ 说明 json 里还是 `^`，回到第 1 步。
    3. **导出面复核**：`require('yauzl')` 后断言 `openPromise` / `fromBufferPromise` / `openReadStreamPromise` 三个 Promise 打开器都是 function（`51-03` 的整条解压实现只走 Promise API）。
    4. **打包护栏复核**：跑 `tests/test-builtin-skills-seeder.js`（含「打包排除项配置护栏」断言组），确认：
       - `build.files` 每一项仍以 `!` 开头、无正向条目（本任务**不得**添加任何条目）；
       - `build.asarUnpack` 仍只有 `nodejieba/**` 与 `skills-builtin/**`。
    5. **提交**：`package.json` + `package-lock.json` 一并提交（lock 必须入库）。
  </action>
  <verify>
    <automated>node -e 'const p=require("./package.json");const bad=[];const d=p.dependencies||{};if(d.yauzl!=="^3.4.0")bad.push("dependencies.yauzl 应为 ^3.4.0，实为 "+JSON.stringify(d.yauzl));if(d.yaml!=="2.9.0")bad.push("dependencies.yaml 必须是裸 2.9.0（不得有 ^ / ~ / latest / >=），实为 "+JSON.stringify(d.yaml));if(bad.length){console.error("依赖声明不合格:\n"+bad.join("\n"));process.exit(1)}console.log("依赖声明 ok（yauzl ^3.4.0 + yaml 裸 2.9.0）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `依赖声明 ok`，或 stderr 指出任一依赖的版本字面量不合格</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];const v=require("./node_modules/yaml/package.json").version;if(v!=="2.9.0")bad.push("node_modules/yaml 实装 "+v+"（应为 2.9.0）");if(fs.existsSync("node_modules/@earendil-works/pi-agent-core/node_modules/yaml"))bad.push("存在嵌套 yaml 副本 ⇒ 双实例（根与 SDK 各一份，解析差异是静默的）");let y;try{y=require("yauzl")}catch(e){bad.push("require(\"yauzl\") 失败: "+e.message)}if(y){const pv=require("./node_modules/yauzl/package.json").version;if(pv!=="3.4.0")bad.push("node_modules/yauzl 实装 "+pv+"（应为 3.4.0）");for(const k of ["openPromise","fromBufferPromise","openReadStreamPromise"])if(typeof y[k]!=="function")bad.push("yauzl 缺 Promise 导出 "+k)}if(bad.length){console.error("安装结果不合格:\n"+bad.join("\n"));process.exit(1)}console.log("安装结果 ok（yaml 单实例 2.9.0 + yauzl 3.4.0 + 三个 Promise 打开器）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `安装结果 ok`，或 stderr 指出双实例 / 版本不符 / 缺导出</fails_when>
    <automated>node tests/test-builtin-skills-seeder.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <fails_when>输出里出现计数非 0 的 `# fail` 行，或缺少 `# pass` 行，或进程以非零退出（打包排除项护栏会在此转红）</fails_when>
    <automated>node -e 'const fs=require("fs");const bad=[];for(const f of ["node_modules/yauzl/package.json","node_modules/yaml/package.json"]){const s=JSON.parse(fs.readFileSync(f,"utf8")).scripts||{};if(s.postinstall)bad.push(f+" 含 postinstall: "+JSON.stringify(s.postinstall))}const p=require("./package.json");const files=(p.build&&p.build.files)||[];if(files.some(x=>x[0]!=="!"))bad.push("build.files 出现非 ! 前缀条目（护栏语义失效）");const au=(p.build&&p.build.asarUnpack)||[];if(au.length!==2||!au.includes("node_modules/nodejieba/**")||!au.includes("skills-builtin/**"))bad.push("asarUnpack 被改动: "+JSON.stringify(au));if(bad.length){console.error("打包面不合格:\n"+bad.join("\n"));process.exit(1)}console.log("打包面 ok（两个新包无 postinstall + files 全 ! + asarUnpack 未动）")'</automated>
    <fails_when>non-zero exit，或 stdout 未出现 `打包面 ok`，或 stderr 指出 postinstall / files / asarUnpack 任一项不合格</fails_when>
  </verify>
  <acceptance_criteria>
    - 四条门禁全绿；`node tests/test-builtin-skills-seeder.js` 的 `# fail 0`。
    - `package.json` 的 `dependencies.yaml` 是**裸 `2.9.0`**（门禁一逐字比对 JSON 值，不是 grep 文本）。
    - **单实例**：`node_modules/@earendil-works/pi-agent-core/node_modules/yaml` 不存在 —— 这是 CR-2 在**磁盘**层的判据（`51-RESEARCH.md` 的双实例实测 A/B 给出了两种形态的逐字对照）。
    - **可失败性（执行者落地前自行复现一次）**：门禁二是**正命题 + 反例并列** —— 把 `dependencies.yaml` 改成 `^2.9.0` 并重装会让「存在嵌套 yaml 副本」那一条转红（红轮形态已由研究实测给出：`node_modules/yaml = 2.9.1` + 嵌套 `2.9.0`）；门禁四的 `postinstall` 与 `asarUnpack` 两条可用「临时在 json 里加 `postinstall`」/「临时加一个 asarUnpack 项」各做一次单点变异（跑完复原）。
    - **lock 入库**：`git status --porcelain package.json package-lock.json` 两者都被提交（lock 不入库会让其他机器 `npm ci` 拿到不同版本）。
    - Task 1 的三条确认结论出现在 `51-02-SUMMARY.md` 里（含「谁确认的、确认了什么」），否则本任务视为未完成。
  </acceptance_criteria>
  <done>两个依赖按实测口径落定：`yauzl@^3.4.0` + `yaml` 裸 `2.9.0`（单实例、与 SDK 同版、无 postinstall），打包面（`build.files` / `asarUnpack`）零改动且护栏全绿，`package-lock.json` 一并入库。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `node_modules` | 新增两个包的代码将进入主进程（`ai-skills-manager.js` 直接 require），且**随应用打包分发** |
| `package.json` 的版本声明 → 运行时实际加载的实现 | 声明与磁盘实装不一致时，差异是**静默**的（双实例各自解析同一份 YAML） |
| `package.json` 的 `build.*` → 发布产物形状 | 正向 allowlist / `asarUnpack` 的改动会改变随包内容（SEED-05 / P10 的承重面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-07 | Tampering | `yaml` 依赖（门禁裁决 `[SUS]`，reason `too-new`） | high | mitigate | Task 1 的 `checkpoint:human-verify gate="blocking-human"`（不可自动批准）+ 三条人工确认（钉 2.9.0 / 与 SDK 同版 / 无 postinstall）+ Task 2 的磁盘级单实例判据 |
| T-51-08 | Tampering | `yauzl` 依赖（zip 解析层，直接消费不可信字节） | medium | mitigate | 门禁裁决 `[OK]`（36.7M 周下载 / 真实上游仓库 / 无 `postinstall` / 唯一依赖 `pend`）+ 无 `postinstall` 的本地复判（门禁四）+ 导出面契约断言（升版改 API 形态会在安装当刻可见） |
| T-51-09 | Tampering | 双实例导致的**静默**解析差异 | high | mitigate | 精确钉版 + 门禁二的「嵌套副本不存在」判据；`prohibitions` 具名禁止用 `overrides` / 手工删目录「绕过」 |
| T-51-10 | Elevation of Privilege | 依赖随包分发时的产物面被顺手改动（加 `asarUnpack` / 加正向 allowlist） | medium | mitigate | 门禁四断言 `asarUnpack` 恰两项且 `build.files` 全 `!`；既有护栏套件 `tests/test-builtin-skills-seeder.js` 作第二道 |
| T-51-SC | Tampering | npm 安装动作本身 | high | mitigate | **即本计划的承重面**：合法性门禁 + blocking-human 检查点 + 无 `postinstall` 复判 + lock 入库（可复现安装）。这是本阶段唯一的安装动作 |
</threat_model>

<verification>
- 四条 automated 门禁全绿 + 套件 `tests/test-builtin-skills-seeder.js` 的 `# fail 0`。
- 安装动作**只有两条命令**（`yauzl@^3.4.0`、`--save-exact yaml@2.9.0`），无其它依赖被顺带升级：以本计划起点 sha 为基线（`BASE=$(cat .git/gsd-plan-head-before-51-02 2>/dev/null || git rev-parse HEAD)`），`git diff $BASE -- package-lock.json` 里除这两个包与其传递依赖 `pend` 外**不应出现第三个包的版本变化**（若出现，说明 npm 顺带动了别的包 ⇒ 必须还原后重装）。⚠️ 判据必须带基线 sha：本计划改动未提交时裸 `git diff` 恒空。
- 同一个基线 sha 下，`git diff $BASE -- package.json` 只应有两行新增（`yauzl` / `yaml`），`build` 段零改动。
- 人工面：Task 1 的三条确认（本计划唯一的非自动化判据）。
- 本计划**不**验证 zip 解析与 YAML 解析的**行为**（那在 `51-03`）；它只保证「包在、版本对、导出面在、打包面没被搅动」。
</verification>

<success_criteria>
- `yauzl@^3.4.0` 与 `yaml@2.9.0`（裸版）落进 `dependencies`，`package-lock.json` 同步入库。
- `yaml` 在磁盘上**只有一个实例**（根 2.9.0，无嵌套副本）。
- `yauzl` 的三个 Promise 打开器可 require。
- 两个包均无 `postinstall`；`build.files` / `asarUnpack` 零改动且既有护栏套件全绿。
- `[SUS]` 裁决走完 blocking-human 检查点并留下记录。
</success_criteria>

<output>
Create `.planning/phases/51-zip/51-02-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

> 本节供 plan-review-convergence 的 source-grounding 通道使用：**以下符号是本阶段新建的**，不是既有符号的漂移。评审时不得要求它们「在 base 上存在」。

### 本计划（51-02）新建

| 类别 | 符号 | 落点 |
|------|------|------|
| 依赖声明 | `dependencies.yauzl`（`^3.4.0`） | `package.json` |
| 依赖声明 | `dependencies.yaml`（裸 `2.9.0`，**精确**） | `package.json` |
| 锁文件条目 | `yauzl` / `yaml` / `pend` 的解析结果 | `package-lock.json` |

### 其余计划将新建的符号

见 `51-01-PLAN.md` 末尾的同一登记表（避免重复；本计划只新增上表三项）。
